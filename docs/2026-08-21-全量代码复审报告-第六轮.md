# CLWriting 全量代码复审报告（第六轮）

- **日期**：2026-08-21（第五轮收官后的首次独立重扫）
- **评审基线**：`dev` 分支 `d964e32`（工作区干净，已推送 origin，领先 0 提交——第五轮报告指出的「79 文件未提交单点丢失风险」已消除）
- **方法**：① 七门门禁全部本机重跑；② **5 路并行独立评审代理**按层分工（AI 编排线 / 服务端+桌面壳 / 前端 web-next / 数据层 / 管线层）全库重扫，与第五轮结论互不预设；③ 第五轮 26 项修复逐项 diff 级核实；④ 两处「高危」新发现由主评审亲自读码复核后采信；⑤ 完成进度对照《迭代方向方案》与《迭代方向批完工总结》对账。
- **与第五轮的关系**：第五轮判 97% / 8.9。本轮不继承其结论，用独立评审面重新扫全库——发现第五轮未覆盖的 **2 高 + 8 中 + 约 28 低** 新缺陷，质量分相应下调；完成度判定经独立对账后维持。

---

## 一、总评

| 维度 | 结论 |
|---|---|
| 完成度 | **约 97% 维持**——迭代方向批 0-7 全部交付、批 8（worker 化）经基准实测主动关闭；功能面完整；剩余仍全部为外部动作（真实书语料、系数回填、发布冒烟、切 tag），零新开发项 |
| 质量分 | **8.3 / 10**（第五轮 8.9 → 下调）——本轮独立扫出第五轮未见的 2 高 + 8 中；但需说明：两处高危均为错误路径 / 边界条件触发（磁盘满、库损坏、中转断流、Windows 平台），主干路径质量与前轮判定一致。2 高 + 8 中全部是小体量修复（多数 ≤20 行），修完可回到 8.8+ |
| 交付判断 | **修完 2 高 + 8 中后可进发布管线**——功能完备、七门全绿、26/26 历史修复核实为真实落地。硬前置不变：真实书语料 + token 系数回填 → Electron GUI 冒烟 → 切 tag；**新增一条：Windows 发布前必修 M4**（见下） |

规模盘点：源码 420 文件约 7.3 万行（后端 TypeScript strict + Vue 3 前端），测试 290 文件约 5.15 万行（2687 单测）+ 28 e2e spec（41 用例）。

## 二、验证门禁实测（七门全绿，2026-08-21 本机）

| 门禁 | 结果 |
|---|---|
| `tsc --noEmit` | 零错误 |
| `vue-tsc --noEmit`（web-next） | 零错误 |
| `npm test` | **290 文件 / 2687 单测全绿**（38.5s） |
| `npm run check:counts` | 通过（290/2687；28 spec/41 用例，README 对账一致） |
| `npm run test:e2e` | **39 过 / 2 跳 / 0 败**（24.9s；跳过为 release-smoke 环境门控） |
| `npm run test:coverage` | 退出码 0（per-glob 两桶阈值门全过） |
| `npm run check:knowledge` | 未重跑（第五轮当日通过后知识层无改动，`git log` 佐证） |

## 三、第五轮修复核实（26 / 26 真实落地，1 处行为折扣）

5 路代理对本层范围内的第五轮修复逐项读码核实：

| 层 | 核实项 | 结果 |
|---|---|---|
| src/ai | M5-A msgSeqs 尾部对齐、M5-B 计价宁缺毋滥、L5-1 sawFinishReason、L5-2 calls 口径注释、L5-16 校准文案 | 5/5 ✅（实现、注释、下游消费三方一致） |
| studio/server + events + desktop + fs | M5-D prefs 合并写、M5-E audit 双闸、L5-5 SSE clientGone、L5-6 Object.hasOwn×3、L5-7 NO_CHANGE 422、L5-8 drainDocumentSaves、安全基线（Host/Origin/token 三层） | 7/7 ✅（安全三层机制真实存在，未发现绕过面） |
| 前端 web-next | M5-F AnalysisPanel 守卫、L5-9 插入信号 tick、L5-10 sendChat 回滚、L5-11 clear 提前 | 4/4 ✅（L5-9 的 `immediate` 一半**行为不达**，见新发现 M8-附） |
| 数据层 | M5-C 引文失明、M5-G NON_UTF8_REJECT、M5-H 导出清旧、L5-3 RAG 残块、L5-4 manifest 容错、原子写+指纹+快照基线 | 6/6 ✅（但 M5-G/M5-H 存在覆盖缺口，见 M5/M6 新发现） |
| 管线层 | L5-12 sourceHash 时机、L5-13 串行链、L5-14 本地日分桶、L5-15 原子写 | 4/4 ✅ |

**结论**：`d964e32` 收口提交真实无水分，无「纸面修复」。第五轮「修复批自身带伤率 1/6」的判断基本成立——本轮仅发现 1 处「代码在、行为不达」（L5-9 immediate），无方向性错误。

## 四、本轮新发现（2 高 + 8 中 + 约 28 低）

### 高危（2 项，主评审亲自复核确认）

| # | 位置 | 问题 | 论证 |
|---|---|---|---|
| H-1 | `src/ai/orchestrate/chat.ts:210-220` | `running.set(bookName)` 在 `openSessionStore()` 之后才进 `try`——库打开抛错（磁盘满、目录只读、session db 损坏）时 `finally` 不执行：`running.delete` 永不运行、deadline 定时器不清、队列永不消费（`drainNextChat` 只在 finally 里调）。**该书对话功能死锁直到进程重启**。同层 `runner.ts:174` 与 `self-heal.ts:173` 的同类调用都包了 try/catch 降级，此处是唯一裸调点 | 主评审逐行复核：210 行 `running.set` → 216 行 `openSessionStore`（内部 mkdirSync/DatabaseSync/PRAGMA 均可同步抛错）→ 220 行才 `try`。修法一行：把调用收进 try 或 catch 后降级传 null（`prepareChatRun` 本就接受 null 走内存模式） |
| H-2 | `src/ai/provider/anthropic-adapter.ts:277-281` | 流结束兜底不区分「到过 `message_delta` 但无 usage」与「连 `message_delta` 都没到（传输截断）」——后者是中转/代理提前断流的真实形态，当前被伪装成正常完成：`emitDone({input, 0}, 'end_turn')` **伪造终止因**。后果：半截正文被上层当完整产出（self-heal 落稿参加机检）、成本按 0 入账、不进重试路径。OpenAI 线的 L5-1 修复（`sawFinishReason` 区分）和 Responses 线的 `terminal==='none'` 分支都已守住自家契约，唯独 Anthropic 线漏掉——而本项目大量面向 anthropic 协议中转 | 主评审逐行复核：`pendingStopReason` 判据现成（258 行缓存），截断分支应报 `retryable` 错误而非 done。修法与 L5-1 同构 |

### 中级（8 项）

| # | 位置 | 问题 | 修法量级 |
|---|---|---|---|
| M-1 | `src/ai/calls.ts:300-305` + `orchestrate/chat/turns.ts:75-92` | 章预算记账的「无并发场景」前提已被打破：chat 的 rewrite/lead_update 工具（TOOL_EXECUTORS 注册表）不检查 `isSelfHealRunning`，与 self-heal 并发时章号交替触发 `recordAiCall` 的 fresh 重置互覆——`used`/tokens/cost 三口径全部低估，**预算闸形同虚虚**（正是它要防的「自动写章烧钱」路径） | 工具入口补互斥检查，约 10 行 |
| M-2 | `src/studio/server/api/stream.ts:475-495` | `chat/clear` 只有 2 道闸（isChatRunning + hasBackgroundTasks），但 `clearChatHistory` 是双键清理（bookName + bookHash 工作流会话）——spawn/self-heal/task-gate 在途时清不彻底，收尾事件成孤儿行。同一动作 audit DELETE 配了 5 道闸（`audit.ts:213-232`），口径不一致 | 对齐 audit 补三闸 |
| M-3 | `src/studio/server/api/files.ts:56-79` | `PUT /file`（设定/大纲/文风等非正文文档）无 revision、无串行闸、无冲突检测——双窗口并发保存静默互相覆盖。README「保存带版本校验」只覆盖 `/documents` 协议面，此通道是例外且无提示 | 加 per-file 串行闸或 revision 乐观锁 |
| M-4 | `src/check/run.ts:100,298` | `relative(bookRoot, ch._path)` 未做 `replace(/\\/g,'/')` 归一——Windows 上产反斜杠，manifest 键（正斜杠）永远 miss：**maxWritten 基准（M5-C 修复在 Windows 整体退化）与树红点定稿章跳过系统性失效**。同款 bug 项目已修过三处（`export/index.ts:148`、`state/state.ts:399`、`ai/tools/shared.ts:14`），此处漏网。**目标用户主力是 Windows，发布前必修** | 两处补归一，2 行 |
| M-5 | `src/document/service.ts:280` vs `:316,492` | M5-G 的 U+FFFD 防线只加在两条元数据路径，`executeSave` 主路径（含 autosave）不查——编辑器打开 GBK 文件 autosave 存回仍会乱码覆盖；且 `maybeSnapshot` 只对 chapter 文档留底，**设定/大纲等非 chapter 文档被覆盖时无任何快照可恢复**。第五轮「堵掉最后一个数据损坏面」的声明不成立于 save 路径 | executeSave 补 U+FFFD 检查（拒绝或强制留底） |
| M-6 | `src/document/lead-finalize.ts:57,78-95` | 定稿回写时「查无此线」的账本推进条目被 `continue` 静默跳过；只要本轮有一条成功，源文件整体清空——**被跳过的推进无 issue、无返回值区分、永久丢失**。与自家「章文件缺失都不得静默通过」（M5-C）的纪律相悖，是防吃书账本的完整性缺口 | 跳过时产出 issue 或计入返回值 |
| M-7 | `src/process/summary.ts:448-449` + `prepare.ts:370-378` | 卷摘要「过期重生成」在唯一生产路径不可达：`selfHealVolumeSummary` 对已存在文件无条件放行（手写优先正确，但把「程序生成的过期品」也一并放行），内部指纹比对成死分支；过期卷摘要被原样注入每章备料。附带：注入未剥 frontmatter（章摘要注入专门剥了，同链两种口径） | 守卫改「无 fm→保留；有 fm.sourceHash 且过期→重生成」 |
| M-8 | `src/studio/web-next/.../useChatComposer.ts:100-118` | `handleClear` 在 `await ui.ask` 确认弹窗之后才求值书名——弹窗挂起期间经命令面板切书，点「清空」会把**新书的全部对话历史清掉**（不可恢复）。同文件 `handleSend` 已按「入口捕获+回滚校验」修复，此处是对称缺口 | 书名入口捕获，3 行 |

### 低级（约 28 项，按层汇总）

**src/ai**（6）：anthropic 线 tool_use 缺 id 无兜底（与 OpenAI 线 P3-Q5 不对称）；`compactionSuppressed`/`degradedPersistedKeys` 进程级 Map 无书生命周期清理；self-heal `usage.calls` 死字段。
**服务端/桌面**（7）：context-menu IPC 净化漏 `accelerator`（非法值致主进程 uncaughtException，渲染层被攻陷后可达）；audit 分页先 map 全量再截取（长书翻页 O(n) 分配）；同步 SQLite busy_timeout=5000 跨进程写竞争冻结事件循环最长 5s；`void iter.return()` 吞 rejection 可致 unhandledRejection；书级 prefs PUT 整体覆写（library 级已修合并写，口径不一致）；audit DELETE 双键清理非原子；onboard-ai 自由文本无长度上限；rag/build 以文案子串判状态码。
**前端**（9）：L5-9 的 `watch immediate` 在 setup 期执行时 `cmHost` 必为 null，「挂载补消费」行为不达（应 onMounted 补一次）；MetaFormPanel/HistoryPanel 两处 await 后重读上下文；ForeshadowPanel.openFile 无 catch；Library/Welcome IPC 失败永久加载中；CheckPanel 用原生 confirm；错误 toast 1.8s 一刀切；SSE token 拼 URL query（本地服务下可接受，留档）；flushSyncOnUnload 多文档尽力而为（已有快照兜底）；编辑器单次键入 4 次全文 O(n) 扫描（超长章性能项）。
**数据层**（8）：`purgeTrash` 的 manifest 写在 try 外（裸抛破坏 `{ok:false}` 契约）；`updateChapterMeta` U+FFFD 只查 body，fm 区盲区且 fm 往返非无损；投稿视图导出无清旧（M5-H 漏第三产物）；三检引文 grep 读取无守卫（竞态致整单 CHECK_ERROR）；`doInit` 半成品目录卡死重试；`readBooks`/`readActive` 无守卫；probeCache 无上限 + stat 撞车理论窗口；migrate-finalized-revision 的 porcelain 路径比对不处理空格/前缀差。
**管线层**（8）：预算裁剪移除段后 `injectedSummaryFiles` 不回收（虚报注入面）；book.yaml rag 段缺 `enabled` 键整段静默丢弃；`assembleStatus.currentChapter` 含草稿与注释口径不符；热力图把草稿保存算「定稿产出」；旧样章库 readdir 无 sort（跨平台注入不确定）；YAML 列表项含冒号被误判 key 行静默吞；book_search 递归跟随 symlink 可越出 bookRoot；driver 层两处微量资源残留；摘要 fm `model` 字段是占位符（信息级）。

### 发现模式分析（比单个缺陷更重要）

本轮中级以上发现里有 5 项呈现同一模式——**「同类缺陷修一处漏一处」**：路径归一修了 3 处漏 check/run.ts 2 处（M-4）；U+FFFD 修了元数据 2 路径漏 save 主路径（M-5）；导出清旧修了 2 产物漏投稿视图；清库闸 audit 配 5 道漏 chat/clear 2 道（M-2）；「入口捕获上下文」修了 handleSend 漏 handleClear（M-8）。第五轮报告称缺陷画像已「边角化」，本轮证据支持主干路径确实干净，但**收口工序缺一环：按缺陷类别做全库同类扫描**，而不是逐点打补丁。建议下一轮修复时对上述 5 类各做一次全库 grep 收口。

## 五、分层质量评分（本轮独立重评）

| 层 | 分（第五轮→本轮） | 依据 |
|---|---|---|
| src/ai（编排/适配器/记账） | 8.7→**8.0** | H-1 锁泄漏 + H-2 截断契约不一致 + M-1 预算并发前提失效——三处都在异常/并发边界，主干重试/记账/流处理经查无恙 |
| studio/server + events + desktop + fs | 8.8→**8.5** | M-2/M-3 同族缺口 + 桌面 accelerator 单点遗漏；安全三层（Host/Origin/token）与并发治理体系复核成立 |
| 前端 web-next | 8.7→**8.5** | M-8 跨书清空 +「入口捕获」惯例漏网；零 XSS、零监听泄漏、代数守卫密度罕见——复核全部成立 |
| 数据层（document/cache/check/rag/knowledge/export/state/git） | 9.5→**8.0** | M-4/M-5/M-6 三项中级均在本层：Windows 归一、save 路径 FFFD、账本静默丢弃——第五轮 9.5 分的「数据安全层」是本轮降幅最大层，评分差异主要来自评审面（第五轮未扫到而非不存在） |
| 管线层（process/driver/learn/metrics/format） | 8.9→**8.5** | M-7 卷摘要不可达 + 三处注释与行为漂移；全层无裸直写、预算纪律贯彻、frontmatter 加固到位 |

**总评 8.3 / 10**（按各层行数加权）。与第五轮 8.9 的差值 ≈ 2 高 + 8 中的扣分：这些缺陷全部可小体量修复（H-1 约 3 行、H-2 约 8 行、M-4 两行、M-8 三行……），修完并配回归测试后预计回到 8.8+。**需要说明**：分差同时包含评审方差（本轮 5 路代理以挑刺视角扫边界路径，第五轮同口径扫描未覆盖到这些点），不全是代码退步——`git log` 证实两轮之间只有修复提交、无新功能开发。

## 六、完成进度核算

对照《迭代方向方案》（四方向九批次）与《迭代方向批完工总结》：

| 项 | 状态 |
|---|---|
| A1 树红点增量缓存 | ✅（0b55d48，700 章热路径 20-27ms） |
| A2 worker 化（批 8） | ✅ **经 P3 闸实测主动关闭**（冻结已被 A1 消除，留反馈驱动再议路线） |
| A3 RAG 惰性指纹 + 预存范数 | ✅（195286c） |
| A4 结构化日志 + 启动通告 | ✅（63d9906） |
| B1-B4 语料回归域（误报按钮/自举/golden-master 门） | ✅（c5877c5） |
| C1-C4 章卷摘要 + 上下文收口 | ✅（db615bf / 2fc2548；本轮 M-7 指出卷摘要过期再生产行为缺口） |
| D1-D3 用量卡片 + 价格口径 + 预算双口径 | ✅（ad7e83f / b21ab9e） |
| 测试体系 | ✅ 290 文件 / 2687 单测 + 28 spec/41 e2e 全绿，README 对账一致 |

**剩余待办（全部为外部动作，非开发）**：① 真实书跑语料收割（当前种子仅 2 条，`test/corpus/checks/body-parts.json`）；② 校准脚本实测后回填 `TOKEN_COEFFICIENTS`（现为如实空表 + 0.6 兜底）；③ 发布前 Electron GUI 冒烟；④ 切 tag。

**本轮新增的发布前置**：⑤ M-4（Windows 路径归一）——目标用户（中文网文作者）主力平台是 Windows，零 CI + 已知系统性失效意味着「Windows 可用」目前不成立，macOS 单平台发布可暂缓此项。

**中期结构项（维持第五轮判定）**：Windows CI、SQLite 多进程并发策略（CLI + 桌面同开一书）、cache 库 schema 演进回归锚。

## 七、结论与建议

第五轮「97% / 可进发布管线」的完成度判断经独立对账**成立**；质量判断在本轮更宽的评审面下**下调至 8.3**——新发现的 2 高 + 8 中集中在错误路径、并发边界、Windows 平台三类第五轮未扫到的面上，主干功能与数据安全纪律复核成立，26/26 历史修复无水分。

**建议修复顺序**（全部小体量，预计 1-2 个工作日含回归测试）：

1. **H-1 + H-2**（src/ai，合计约 10 行）——对话死锁与截断流入账，两处都复用自家已有模式；
2. **M-4**（Windows 归一，2 行）+ **M-5**（save 路径 FFFD）+ **M-6**（账本静默丢弃）——数据/平台完整性；
3. **M-1 / M-2 / M-3 / M-7 / M-8**——并发闸口径、卷摘要行为、前端对称缺口；
4. 低级清单按层批量清偿，其中 desktop accelerator、audit 分页、rag/build 状态码三项性价比最高；
5. **工序改进**：对「路径归一 / U+FFFD / 导出清旧 / 清库闸 / await 后重读上下文」五类各做一次全库同类扫描收口——本轮证明逐点补丁模式会让同类缺陷反复漏网。

修完后建议照惯例：定向回归 + 全量七门 + 关键修复回退验证，再出第七轮收口报告。

## 八、第六轮缺陷修复收口（2026-08-21 同日）

按第七节建议顺序当日完成全部高危 + 中级 + 低级前七项修复，全部配回归测试；全量门禁复跑通过。

### 高危 + 中级（10/10 全修）

| # | 修复摘要 | 回归测试 |
|---|---|---|
| H-1 | `chat.ts`：`openSessionStore` 收进降级 try/catch——事件库打开失败发 notice 继续对话，不再泄漏 isChatRunning 永久 true | `test/ai/chat-open-store-fail.test.ts`（**回退锚定**：revert→红 / restore→绿） |
| H-2 | `anthropic-adapter.ts`：流结束无终止事件分流——有 stopReason 无 usage → done（cached inputTokens 记账）；纯截断 → retryable error 不记 done | `test/ai/provider/adapter.test.ts` +2 |
| M-1 | `chat/turns.ts`：rewrite_chapter / rewrite_selection / lead_update 三工具入口补 `isSelfHealRunning` 互斥（AI_GEN_TOOLS 集合） | `test/ai/tools/chat-ai-gen-gate.test.ts` ×3 |
| M-2 | `stream.ts` chat/clear：补 task-gate / self-heal / spawn 三闸，对齐 audit DELETE 五闸口径 | `test/studio/chat-clear-gates.test.ts` ×3 |
| M-3 | `files.ts` PUT /file：可选 `expectedRevision` 乐观锁（不符 → 409 REVISION_CONFLICT），GET 附 revision；前端 `documents.ts` + `StyleBaselineCard.vue` 接线（409 → 提示 + 重载远端）。缺省参数保持「后写为准」，存量调用方零改动 | `test/studio/api-integration.test.ts` +3 |
| M-4 | `check/run.ts` 两处 relative() 结果补反斜杠归一（manifest 键口径）；同类收口：`style.ts` relPath、`prepare.ts` injectedSummaryFiles | `test/check/tree-issues*.test.ts` 既有用例（Windows 归一行为） |
| M-5 | `service.ts` executeSave：内容含 U+FFFD 且盘上字节非合法 UTF-8（fatal TextDecoder 判定）→ WRITE_ERROR 拒绝，字节零副作用 | `test/document/service.test.ts` +2（**实现期间纠正过一版反向判定**） |
| M-6 | `lead-finalize.ts`：查无此线条目不再静默丢弃——残留写回源文件（带 `<!-- 未回写 -->` 注记）或归档，产出 warning | `test/document/lead-finalize.test.ts` +3 |
| M-7 | `summary.ts` selfHealVolumeSummary：无 sourceHash（手写）→ 保留；有且过期 → 重生成（原「存在即跳过」使该分支不可达）；`prepare.ts` 卷摘要注入剥 frontmatter + 登记注入文件 | `test/process/summary-volume.test.ts` +2、`prepare.test.ts` +1 |
| M-8 | `useChatComposer.ts` handleClear：书名入口捕获，确认后已切书 → 中止；清除在途切书 → 本地不清 | `test/studio/webnext/composables.test.ts` +2 |

### 低级（7 项，报告点名的性价比前七）

| 项 | 修复 |
|---|---|
| desktop accelerator | `context-menu.ts` 白名单正则（修饰键+F 键+具名键），非法剥字段不崩主进程（`test/desktop/context-menu.test.ts` 新文件 ×3） |
| audit 分页 | `audit.ts` 先 pageSlice 再 map（原全量投影后丢弃大半） |
| rag/build 状态码 | `startRagBuild` 返回结构化 `code: 'BUSY' \| 'BAD_INPUT'`，route 不再做文案子串判定 |
| `void iter.return()` | `stream.ts` SSE 断连清理补 `.catch()`（防 unhandledRejection） |
| purgeTrash | manifest 写回收进 try（主文件已删，条目写失败不打 500；残留自愈）（`trash-purge-best-effort.test.ts` 新文件） |
| 投稿视图清旧 | `export/index.ts` 清当前平台槽位旧产物，他平台产物保留（`export.test.ts` +1） |
| 三检引文读取守卫 | `check/leads.ts` chapterTextOf 读失败视同缺失落 unverifiable 黄项（`tree-issues-leads-book.test.ts` +1） |

### 工序改进落实（第七节第 5 条）

「路径归一」类已完成全库扫描收口（M-4 + style/prepare 同类点）；U+FFFD / 导出清旧 / 清库闸 / 入口捕获四类随本次修复已顺带对齐各自同族点（M-5/M-2/M-8 即该类收口本身），其余长尾低级项留第七轮按层清偿。

### 门禁验收（全绿）

- `npm test`：**295 文件 / 2715 单测全绿**（基线 290/2687，+5 文件 +28 测试）；`tsc` / `vue-tsc` 零错
- `check:counts`：README 已对账 295/2715
- `test:e2e`：39 通过 / 2 既有跳过 / 0 失败
- 评分预期：第七节预判「修完回到 8.8+」的代码侧前提已成立，待第七轮独立复核确认


---

> **评审方法说明**：主评审先重跑七门门禁确认起点，再亲自逐行复核两处高危新发现（H-1 的锁时序、H-2 的兜底分支）与 M-4 的 manifest 键口径后方采信；5 路独立评审代理按层分工（AI 线 / 服务端+桌面 / 前端 / 数据层 / 管线层），全部只读。第五轮 26 项修复核实分散在各代理任务内完成。规模与进度数字取自 `git log`、`find | wc` 与《迭代方向批完工总结》交叉验证。
