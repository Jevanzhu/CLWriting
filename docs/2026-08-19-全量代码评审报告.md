# CLWriting 全量代码评审报告（事故后重建版）

- **日期**：2026-08-19（本文件为同日深夜重建，见文末说明）
- **评审基线**：`b9f8691`（巨石拆分批 1 收口时点）；267 个测试文件 / 2446 单测全绿，`tsc` + `vue-tsc` 零错，`check:counts` 门禁通过，Playwright 26 specs / 39 用例
- **方法**：四路并行代码探查（同步 IO / RAG / 机检与裁决 / 上下文与记账）+ 前端与管线人工走查，结论均带 file:line 证据

---

## 一、总评

| 维度 | 结论 |
|---|---|
| 完成度 | **约 92%**——核心写作链路（开书→大纲→细纲→正文→机检→归档）全部可用；缺口集中在三处「最后一公里」接线与收尾打磨 |
| 质量分 | **8.5 / 10**——测试文化、配置容错、失败出口收敛是长板；数据源接线缺口与文档数字漂移是短板 |

技术栈定格：Node 24+ / TypeScript strict 后端约 38k LOC；Vue 3 + Pinia + Vite + CodeMirror 6 前端约 30k LOC；Electron 桌面壳；存储 node:sqlite（事件库 / RAG 索引）+ JSON/YAML 配置；AI 侧三个协议适配器（Anthropic / OpenAI Chat / OpenAI Responses）统一走 runTask 编排，重试、超时、用量归口。

## 二、分层走查

| 层 | 代表位置 | 状态 | 评语 |
|---|---|---|---|
| 配置/格式 | `src/format/`（yaml / iron-rules / manifest / types） | 成熟 | 三层配置链（book.yaml → global.json → 内置兜底）完整；`_raw` 保留未知段，round-trip 忠实 |
| 机检 | `src/check/`（runner / count / growth / leads / report / run） | 可用，两处缺口 | 检查项、分级（红/黄/蓝）、报告与重写格式齐备；imagery / leak 词表在生产链路未接数据源（见第四节 R2） |
| 生成管线 | `src/process/`（draft-pipeline 等） | 成熟 | 草稿管线水源回退链完整；runTask 编排统一重试/超时/用量 |
| 事件 | `src/events/`（store） | 可用，一处窗口 | SQLite 事件库 append-only 全量保留 + 每书唯一清理口（两步确认）定版；**改名迁移存在 WAL 侧车丢失窗口**（见 R3） |
| 缓存/账本 | `src/cache/`（schema / sync） | 成熟 | 布线/悬念两端闭合（declared vs actual）有集成测兜底 |
| Studio 服务 | `src/studio/server/api/` | 成熟 | 按域拆分；chat 巨石批 1 已拆三模块，六失败出口收敛 finishTurn |
| 前端 | `src/studio/web-next/` | 可用 | views / panels / ui 分层清晰；展示域巨石（Audit / Learn）待后续批次拆（后由批 6b/6c 完成） |
| 桌面 | Electron 壳 | 可用 | 打包与升级链完整，走查未见阻塞性缺陷 |
| e2e | `test/e2e/` | 成熟 | 26 specs 覆盖关键用户路径，含版本恢复等竞态修复用例 |

## 三、可长保的资产（优点）

1. **测试文化**：2446 单测 + `check:counts` 数字门禁（README 声称数对不上直接红）+ Playwright 冒烟，三层防回归。
2. **配置容错**：未知段保留、解析错误不抛异常走「修复确认」、三层兜底，用户配错不崩。
3. **失败出口收敛**：批 1 把 chat 十余个失败出口收敛为六个具名出口统一 finishTurn，可观测、可测。
4. **事件史定版**：append-only + 手动两步确认清理，审计与回放有据。

## 四、风险清单（当时口径）

| 级别 | 项 | 说明 | 去向 |
|---|---|---|---|
| 必须修 R1 | README 测试数漂移 | README 声称 2446，实际 2451（后续修复落地后为 2463/269 文件） | 当晚已修，随事故丢失；数字校准并入重建 S6 |
| 建议修 R2 | 机检词表未接线 | `checkImagery` / `checkInfoLeak` 生产链路两侧恒空 → 恒绿，机检形同虚设 | 重建项 S5（种子表与测试文件已幸存） |
| 建议修 R3 | events 迁移 WAL 窗口 | 改名迁移搬主库不搬 `-wal`/`-shm` 侧车，checkpoint 依赖隐式行为，存在丢事件窗口 | 重建项 S4 |
| 观察项 | AuditView 两处小瑕 | diffMode 死代码、reload 后选中态重置漂移（批 6b 走查发现） | 待同事定夺 |

## 五、附：同事七提交走查结论（08-19 晚，批 2~6c + 后续）

- **批 2（自愈）**：行为等价——11 个出口 / 6 个事件 / 记账逐项核对一致。
- **批 3 ~ 6a、6c**：等价或更强（LearnView 655→321 收口）。
- **批 6b**：AuditView 673→511，两处小瑕（同上观察项）。
- **a20f8eb（Beta 徽章精修版，事故后同事自行恢复）**：表面挂徽（三审 / 对话 / 角色关系图 / 文风验收 / AI 分析等）+ Ribbon 四处 tooltip + 「工作台」改名「AI 工作台」+ Settings 子项去徽去重——**优于丢失的原版**；`vue-tsc` 零错验证通过。残留一行修（BetaBadge 缺 `text-transform: none`）见执行方案 S3/S6。

## 六、结论

项目整体处于「功能完备、质量扎实、收尾在三」的状态：92% 完成度、8.5/10 质量分。三条修复线（README 数字 / 机检接线 / events WAL 窗口）+ AuditView 打磨项处理后即可视为交付级。后续迭代方向（架构成色 / 语料回归库 / 超长篇上下文 / 成本可视化）另见《2026-08-19-迭代方向方案》。

---

> **重建说明**：本报告原文件与 docs/ 目录在 08-19 工作区事故后的目录删除中丢失，本文件按当晚会话留档重建。总评、分层结论、风险清单与原始版本一致；事故本身（评审子代理执行 `git checkout` 覆写工作区致未提交改动丢失）及重建安排见《2026-08-19-工作区事故与重建执行方案》。
