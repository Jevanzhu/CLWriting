# 全量代码复审报告 · 第九轮（2026-08-22）

- 范围：src 全目录（约 5.1 万行 / 664 文件）、test、scripts、构建配置、README/CLAUDE.md 承诺对照
- 方法：typecheck + 全量单测基线，5 路并行分模块逐文件评审（src/ai、服务端+桌面、前端 web-next、数据与基础层、测试与工程化）
- 基线：`tsc --noEmit` 通过；单测 2799 中 2798 通过，1 失败为时序敏感断言（见 T-1）

## 总评

| 维度 | 结论 |
|---|---|
| 功能完成度 | ≈97%（README/CLAUDE.md 承诺的对外功能 100% 落地且有 e2e，无空壳端点；版本 1.0.0-rc.0 相称） |
| 代码质量 | 8.7 / 10（第八轮 8.6 基础上，本轮高危仅 1 项且已修复） |
| 测试 | 2799 单测 + 41 e2e，check-counts 对账门实测通过 |

分模块完成度：src/ai 97%、服务端/桌面 96%、前端 web-next 98%、数据与基础层 95%、测试与工程化 98%。

## 缺陷清单（本轮新发现，均已修复）

### 高危

| # | 位置 | 问题 |
|---|---|---|
| H-1 | `src/ai/tools/rewrite.ts` | 选段改写确认落盘整章覆盖：local 模式 prompt 只返回选段新文本，工具面却将 spill 内容整体替换章节正文（HTTP 端点 `api/rewrite.ts` 有 slice 拼回，工具面漏配），作者确认后整章其余内容丢失。同时缺选区唯一性校验（同句多次出现替换错位） |

### 中危

| # | 位置 | 问题 |
|---|---|---|
| M-1 | `src/studio/server/api/audit.ts`、`stream.ts` | 三审运行闸 `isReviewRunningForBook` 未接入「清空事件史」「清空对话」端点，三审在途清库会清不彻底（busyGate 家族同族遗漏） |
| M-2 | `test/document/tree-perf.test.ts` | 缓存命中预算下限 0.1ms 对 GC/JIT 噪声无余量，确认为 flaky（本轮全量跑测即复现 1 次） |

### 低危（均已修复）

| # | 位置 | 问题 |
|---|---|---|
| L-1 | `src/studio/server/api/books.ts` GET 单书 | book.yaml 损坏时未查 `ok`，解构 undefined 抛 TypeError → 裸 500，与 config.ts 口径分叉 |
| L-2 | `src/studio/server/api/providers.ts` tiers.chat | `body === null` 分支因 http.ts readJson 归一为 `{}` 而不可达，死代码造成契约错觉 |
| L-3 | `src/desktop/main.ts` | bootstrap 部分成功后 activate 重试会再起新 server 并覆盖变量，旧 server（端口/SSE 计数）滞留至进程退出 |
| L-4 | `src/studio/web-next/src/api/client.ts` | 外部 signal 在调用前已 abort 时 apiJson 不生效（不超时也不取消），缺 aborted 预检 |
| L-5 | `src/events/store.ts` | 引用计数 close() 非幂等，重复 close 会使另一方持活引用踩 "database is closed" |
| L-6 | `src/log/index.ts` | dayFile() 入队时取日期，跨本地零点排队的日志行写错文件（轮转边界错位） |

> L-7（short-index.ts 死类型）经核实为误报：评审所指「设计期占位」是记录既往删除的注释，`ShortPlanningView`/`DistributionItem` 等在文件内部实际被 `analyzePlanningView` 等使用，不修。

### 留档不修（信息级，有明确取舍）

- `src/document/words-diary.ts` append 无 fsync：service.ts 已注明「字数丢失可接受」。
- `src/export/index.ts` 分章导出 rm+重建窗口：纯派生产物可重跑。
- `src/rag/store.ts` 迁移 TOCTOU：单进程桌面形态不可触发，多进程化前需收敛（同 migrateBookSession 的「目标位已存在即放弃」探测）。
- `src/ai/provider/store.ts` mtimeMs 缓存同刻盲区：单用户桌面几乎不可达。
- `test/rag/scale.test.ts` 2000ms 绝对界值负载敏感：本机余量 7 倍，暂保留。
- `package.json` files 未含 `resources/`：electron-builder 打包不受影响，npm publish 前需补充。

## 重点核查确认无问题项（防复报）

- 服务端安全面：路径穿越（resolveWithinRoot 双侧 realpath）、Host/Origin/token 三重校验、SSE 计数、优雅退出均闭环。
- 前端：SSE 重连/清理、拖拽监听、定时器/rAF 全部配对清理；bookGen 切书代数、乐观锁、compReqId 竞态防护成熟。
- 数据层：原子写（tmp+rename+fsync）、编码防线（UTF-8 fatal 字节级探测）、「读失败≠空文件」哨兵口径全线统一；14 处 DatabaseSync 均 finally 收口。
- src/ai：runner 中断清理、记账防护、self-heal 单一 finally 收口、三协议「截断 vs 无 usage」分流、脱敏、vault 版本守卫均正确。

## 修复与回归

以上 H/M/L 项已全部修复并配回归测试（H-1 补「spill 拼回全文」「选段多次拒绝」两条用例，M-1 补运行闸，M-2 放宽断言下限）；修复后 `tsc --noEmit` 通过、全量单测 2800 全绿（含新增用例）。
