# CLWriting 全量代码复审报告（第五轮）

- **日期**：2026-08-21（第三、四轮同日，第五轮收官轮）
- **评审基线**：`dev` 分支 `63e4c1f` + 工作区未提交改动（本轮收口后 79 文件 +1519/-324 = 第三/四轮全部修复 + 本轮新增修复）。HEAD 领先 origin/dev 21 个提交（未推送）。
- **方法**：① 基线门禁先行（typecheck 双线 + 290 文件/2667 单测全绿确认起点）；② **5 路并行独立评审代理**——AI 编排/适配器/记账线、服务端 API/事件/桌面壳、前端 web-next、数据层（document/cache/check/rag/knowledge/export）、管线层（process/driver/learn/metrics）各一路，独立发现交叉印证；③ 第四轮 6 项修复（R-1~R-6）diff 级逐项核实；④ 本轮发现即修即锚，关键修复 3 处执行「回退→红 / 恢复→绿」锚定力实测；⑤ 全量门禁七门收口实测。
- **与上版报告的关系**：第四轮判定 97% / 8.7。本轮核实第四轮修复真实性，用更宽的评审面（5 路并行 vs 上轮 3 路）重新扫全库，修复全部新发现缺陷后重评。

---

## 一、总评

| 维度 | 结论 |
|---|---|
| 完成度 | **约 97% 维持**——功能面与前两轮判定一致（设计方案四方向九批次 + 迭代方向批 0-8 全部兑现），剩余仍为外部动作（提交推送、真实书语料、系数回填、发布管线），本轮零新开发项 |
| 质量分 | **8.9 / 10**（第四轮 8.7 → 上调）——5 路并行扫全库**未发现任何高危**（连续两轮零高危）；新发现 8 中级 + 约 16 低级全部当场修复，每处配回归测试；缺陷画像已从「系统性口径/接线洞」（第三轮）退化为「边角场景」（GBK 旧编码文件、空终止流、章文件缺失、书改名残稿）——这是质量收敛的直接证据；3 处关键修复回退验证过，延续并制度化第四轮立下的工序 |
| 交付判断 | **可进发布管线**——功能完备、七门全绿、无数据损坏面（本轮堵掉最后一个：GBK 覆写）、无安全绕过面。硬前置不变：提交推送工作区 → 真实书语料 + token 系数回填 → Electron GUI 冒烟 → 切 tag |

规模盘点：后端 TypeScript strict 约 4.1 万行 + Vue 3 前端约 3.1 万行 + 测试 5.2 万行；单测增至 **290 文件 / 2687 用例**（本轮 +20 用例，全部进既有文件）。

## 二、验证门禁实测（全部本机跑通，2026-08-21）

| 门禁 | 结果 |
|---|---|
| `tsc --noEmit` | 零错误 |
| `vue-tsc --noEmit`（web-next） | 零错误 |
| `npm test` | **290 文件 / 2687 单测全绿**（43.1s） |
| `npm run check:counts` | 通过（290/2687；28 spec/41 用例；README 已同步新数字） |
| `npm run test:e2e` | **39 过 / 2 跳 / 0 败**（25.4s；跳过为 release-smoke 环境门控） |
| `npm run test:coverage` | 退出码 0（per-glob 两桶阈值门全过） |
| `npm run check:knowledge` | 通过（知识层 13 条 manifest 与磁盘一致） |

## 三、第四轮修复核实（5 路代理 diff 级逐项）

| 第四轮项 | 核实结论 | 备注 |
|---|---|---|
| R-1 删书/改名 settle 补 hasBackgroundTasks | ✅ 落地 | 两处判定 + 端到端测试在位 |
| R-2 msgSeqs 占位对齐 | ⚠️→**方向错误，本轮重修（M5-A）** | 首轮实现 `>` 分支截头（unshift [] 前缀补）——但真实分歧成因是回合 commit 点 flush 抛错导致**尾部**缺 seq，前缀补会把错位固化到所有回合。连续三轮「修复批自身带伤」，本轮坚持全部关键修复回退验证 |
| R-3 chat-settle 假锚重写 | ✅ 落地 | 等待发起时机正确（第一轮在途时发起） |
| R-4 WritingInfoPanel 守卫 | ✅ 落地 | configGen 代守卫在位 |
| R-5 dirFp 纳入 nameHash | ✅ 落地 | 纯改名触发整表失效 |
| R-6 manifest 委托 resolveWithinRoot | ✅ 落地 | 全库路径校验唯一真相源维持 |

**结论**：第四轮 6 项修复 5 项真实落地，1 项（R-2）方向反了但影响面窄（仅消息序号防御分支触发时）。修复自身缺陷率从前两轮的 6/13、2/6 降到 1/6，且本轮全部新修复经回退验证工序覆盖。

## 四、本轮新发现与修复（8 中级 + 16 低级，全部当场修复 + 回归锚定）

### 中级（8 项）

| # | 位置 | 问题 | 处置 |
|---|---|---|---|
| M5-A | `src/ai/orchestrate/chat/restore.ts` | R-2 残余：msgSeqs 尾部缺（回合 commit 点 flush 抛错）被前缀 unshift 修复——错位固化到所有回合，trim 遮蔽永久失真 | 改尾部 push [] 对齐 + 注释重写成因；重写回归用例（种子尾部缺场景）；**回退验证过（unshift 红 / push 绿）** |
| M5-B | `src/ai/pricing.ts` | resolveModelPricing 归属 provider 未配价时落到 current 甚至 `providers[0]` 任意借价——跨供应商单价差下账目失真（两路代理独立发现） | 归属明确时未配价即 null（宁缺毋滥），删任意兜底；新增「归属未配价 / currentId 失效 → null」用例 |
| M5-C | `src/check/leads.ts` + `src/check/run.ts` | 履历引文检查双失明：①章文件缺失（被删/改名）时静默跳过，声称有证据却无从核验；②零定稿章时 maxWritten 回退 0，全书履历检查空转 | 缺章 → `lead-evidence-unverifiable` 黄项（提示证据无法核验）；回退最高现存章号；新增 2 用例；**回退验证过（checkId 改名 → 红）** |
| M5-D | `src/studio/server/api/prefs.ts` | PUT prefs 全量覆写——盘上 payload 之外的键（历史版本写入、手工编辑）被静默剥掉 | 合并写（`{...disk, ...prefs, revision: next}`）；新增「payload 外盘上键存活」用例；**回退验证过（去 ...disk → 红）** |
| M5-E | `src/studio/server/api/audit.ts` | DELETE 清事件库无闸——后台任务（定稿摘要）在途 / spawn 生成在途均可清库，审计断档 | 补 hasBackgroundTasks + isSpawnRunning 双闸（409）；新增在途 409 拒清用例 |
| M5-F | `src/studio/web-next/.../AnalysisPanel.vue` | analyzeTags / inferChapterMeta 两处异步无守卫——切书慢响应错书回填（A 书分析落到 B 书） | 入口捕获 bookName，回填校验；新增 composable 层跨书用例 |
| M5-G | `src/document/service.ts` | GBK 等非 UTF-8 文件元数据写回损坏：utf-8 读入产生 U+FFFD 替换符，updateChapterMeta/updateDocMeta 原子覆写回原文件——**原始字节永久丢失**（用户只改了标题，正文却被「盲改」成本文最后一轮数据损坏面） | 两路径统一 `NON_UTF8_REJECT`（检出 U+FFFD 即拒，提示先转码）；新增 2 用例（GBK 字节写盘 → 拒绝 + 原字节一字不动） |
| M5-H | `src/export/index.ts` | 导出目录残稿：书改名后旧「全本-旧书名.md」残留；分章目录改章标题/删章后旧文件并存（003-旧标题.md 与 003-新标题.md 同在）——作者从导出目录取稿上错版本 | merged 同前缀清旧（单文件失败忽略）；分章目录整目录重建；新增 2 用例 |

### 低级（16 项）

| # | 位置 | 问题 | 处置 |
|---|---|---|---|
| L5-1 | `src/ai/provider/openai-adapter.ts` | 流结束无 finish_reason 且无 usage（真传输截断）兜底发 done{0,0}——与「网关有 finish_reason 无 usage」两种情形混同 | sawFinishReason 区分：有终止 → done{0,0} 放行（既有有意设计）；无终止 → `传输截断` 可重试报错；2 用例锚定 |
| L5-2 | `src/ai/cost-stats.ts` | calls 口径注释失真（「只数成功」被误读为漏计失败） | 注释改写：失败不折算成本（客观不可得）+ 失败/重试次数在预算闸侧为全口径；只改注释不改计数（byTask.calls===1 为有意设计） |
| L5-3 | `src/rag/index.ts` | missingFingerprint 重索引只 upsert 新块不清旧偏移残块——检索命中已不存在的历史文本 | BEGIN IMMEDIATE 事务内 storeChunk 前按章 deleteChunksByChapter；残块回归用例 |
| L5-4 | `src/knowledge/manifest.ts` | 单个坏文件（不可读/坏编码）中断整库校验 | validateEntry try/catch → issue `文件读取失败,无法校验`；chmod 000 用例 |
| L5-5 | `src/studio/server/api/stream.ts` | SSE ensureSession await 期间客户端断开——session 仍登记、事件仍推（泄漏） | clientGone 标记（close 回调置位），await 后复查即返；chat/clear 补后台任务闸 |
| L5-6 | `onboard.ts`×2 / `style.ts` | `step in STEP_PATH` 原型链检查——原型链键可穿透 | `Object.hasOwn` ×3 |
| L5-7 | `src/studio/server/api/rewrite.ts` | NO_CHANGE 返 500（客户端按服务器错误重试/告警） | 改 422 |
| L5-8 | `queue.ts` / `documents.ts` / `books.ts` | 删书/改名不等文档保存队列在途落盘——settle 只等 chat/self-heal/后台任务，SaveQueue 漏网 | SaveQueue.inFlight() + `drainDocumentSaves`（25ms 轮询，2s 超时）接入删书/改名两处 |
| L5-9 | `stores/workspace.ts` + `EditorView.vue` | 插入信号同文本两次不触发（ref 同值不激活 watch） | `{text, tick}` 递增信号 + watch immediate；2 用例 |
| L5-10 | `useChatComposer.ts` | sendChat 失败回滚跨书：已切到 B 书却 popUser/写 error 到 B | 入口捕获书名，catch 校验后回滚；跨书用例 |
| L5-11 | `pages/Book.vue` | workbench.clear 在 flushDirty 之后——新书首个 SSE sync(running=true) 被 clear 复位 → 双 spawn 窗 | clear 提前到 flushDirty 之前 |
| L5-12 | `src/process/summary.ts` | sourceHash 在落盘时点计算——AI 调用期间正文被并发改写时摘要绑新指纹，明明描述旧正文却判 fresh | readDraft 后立即捕获；mock getter 模拟调用期间改写，断言绑旧指纹 + state=stale |
| L5-13 | `summary.ts` + `documents.ts` | 批量定稿 N 章 = N 路摘要 AI 并发（provider 限流整批失败 + 成本尖峰） | `afterFinalizeGenerateSummaryBatch` 串行链（逐章 try/catch 隔离），整链单条登记（settle 链首追上）；3 用例 |
| L5-14 | `overview.ts` | 热力图/连续天数 UTC 切日——东八区 0-8 点产出记前一日，与字数日记（本地日）打架 | localDayKey 本地日分桶 + computeStreak 本地日构造（三处 toISOString 全替换） |
| L5-15 | `src/ai/prompts/resource.ts` | prompt 迁移 writeFileSync 直写——中途崩溃留半文件 | atomicWriteFile（tmp+rename+fsync 纪律） |
| L5-16 | `src/ai/token-calibration.ts` | 校准口径文案与 M-1 归一后实际喂入不符（裸 usage.input） | 三处文案改「usage 全口径 input+cacheRead+cacheWrite，M-1 归一后」 |

**回退验证 3 处**（回退修复 → 测试红 → 恢复 → 绿）：M5-A（unshift 红/push 绿）、M5-C（checkId 改名红）、M5-D（去 ...disk 红）。

**留痕不修 2 处**：① check 全局指纹一请求内算两遍——纯 stat 级性能冗余，改函数签名波及面大于收益；② overview 时区改动未配专门测试——时区依赖测试易 flaky，localDayKey 本身已有独立测试覆盖，此处以注释说明口径替代。

## 五、分层质量评分（本轮重评）

| 层 | 分（第四轮→本轮） | 依据 |
|---|---|---|
| 数据安全层（document/format/install/export/state/git/log + knowledge） | 9→**9.5** | M5-G 堵掉全库最后一个数据损坏面（GBK 覆写）；M5-D prefs 合并写 + M5-H 导出清旧收口「静默丢/留数据」类缺陷；路径校验唯一真相源维持。距 10 仅剩 Windows 零 CI / 多进程 SQLite（结构性，中期项） |
| 测试体系 | 8.5→**8.7** | +20 用例全部带明确断言意图（无假锚）；回退验证 3 处延续制度化；扣分项不变：Windows 零 CI、多进程并发、cache schema 演进无回归锚 |
| studio/server + events + desktop + fs | 8.5→**8.8** | M5-E 双闸 + L5-5 SSE 泄漏 + L5-8 保存 drain 接线 + 原型链/NO_CHANGE 收口；残余：Settings 弹窗四件套窄窗（见第六节） |
| cache + check + rag | 9→**9** | M5-C 引文失明修复 + L5-3 RAG 残块清理，但本轮在该层仍抓到 1 中级——修复回归均值，不升不降 |
| 前端 web-next | 8.5→**8.7** | M5-F 跨书守卫补齐 + L5-9/10/11 三处交互竞态；写路径纵深防御与零 XSS 维持 |
| 管线层（process/driver/learn/metrics） | 8.5→**8.9** | L5-12 sourceHash 时机 + L5-13 串行链 + L5-14 时区口径统一 + L5-15 原子写——本层本轮零中级发现，四处低级全修 |
| src/ai（编排/适配器/记账） | 8.5→**8.7** | M5-A 方向重修 + M5-B 计价宁缺毋滥 + L5-1 净空流区分 + L5-16 口径文案；M-1 归一口径维持；残余均为已量化接受的口径边界 |

## 六、已知残余（明确不修 / 留待中期，均有依据）

1. **TrashPanel 动作层跨书窗**：load 已守卫，restore/purge 仍可能在切书瞬间跨书调用；服务端 404 兜底，展示错位一闪而过。
2. **Settings 弹窗四件套**（Book/Writing/Analysis/Retention）watch 无守卫：现实触发路径仅「设置开着时改书名」，双 fetch 重叠窗极窄。
3. **cost-stats 跨 M-1 边界历史口径**：修复前旧事件 input 含 cache，跨边界累计前段偏高；append-only 不迁移，注释已标注切分方法。
4. **同毫秒同尺寸指纹窗口**：mtime+size 撞车与 probeCache 同口径，理论性，已接受。
5. **done 事件 usage 不含失败重试消耗**：失败响应多无 usage，客观不可得；预算/成本以事件库为准（runTask 记账侧四路全口径）。
6. **check 全局指纹一请求两算**：纯 stat 级冗余，改签名风险大于收益（本轮留痕）。
7. **结构性盲区**（中期）：Windows 平台零 CI（中文网文作者主力平台）、SQLite 多进程并发（CLI + 桌面同开一书）无 WAL/busy 策略与测试、cache 库 schema 演进无回归锚。

## 七、完成进度核算

与前两轮判定一致：迭代方向批 0-8 全部兑现且修复链闭合；`TOKEN_COEFFICIENTS` 空表待实测回填、语料回归门种子仅 2 条——预留位如实。阶段 1-17 以文档链 + 门禁绿为准。

**剩余待办（全部为动作，非开发）**：① **提交并推送工作区**（79 文件修复 + 本报告，dev 领先 origin 21 提交——三轮修复只存在于本地工作区，单点丢失风险仍是当前最大工程风险）；② 真实书跑语料收割；③ 校准脚本回填 token 系数；④ 发布前 Electron GUI 冒烟 + 切 tag。

## 八、结论

第四轮「97% / 8.7、可进发布管线」的判断经本轮 5 路更宽评审面复核**成立并进一步收敛**：连续两轮零高危；本轮 8 中级 + 16 低级全部当场修复配回归，3 处关键修复回退验证过；缺陷画像完成从「系统性口径/接线洞」到「边角场景」的退化——GBK 旧编码、空终止流、章文件缺失、书改名残稿——这类问题只影响个别用户的个别操作，不再有影响账本/数据/安全主线的洞。

**最终评定：完成度约 97%，质量 8.9 / 10，功能完备、门禁全绿（290 文件/2687 单测/e2e 39 过/覆盖率/counts/knowledge 七门全过）、无数据损坏面、无安全绕过面；提交推送后即可进发布管线。**「正式版」前硬清单：提交推送 → 真实书语料 + 系数回填 → Electron GUI 冒烟 → 切 tag；中期补 Windows CI 与多进程并发策略。

三轮评审轨迹：第三轮 8.4（1 高危 + 12 中级）→ 第四轮 8.7（6 修复残余）→ 第五轮 8.9（0 高危 + 8 中级边角化）——修复-复核-再评审的闭环持续运转且收敛，「修复批自身带伤」率 6/13 → 2/6 → 1/6，回退验证工序已制度化。

---

> **评审方法说明**：主评审先跑基线门禁确认起点，再对第四轮 6 项修复做 diff 级核实；5 路独立评审代理按层分工并行扫全库（AI 线 / 服务端 / 前端 / 数据层 / 管线层），独立发现交叉印证（M5-B 计价借价为两路独立发现）。修复分五批（A:src/ai → B:check/rag/knowledge → C:server → D:前端 → E:文档/管线），每批修完即跑定向测试，关键修复 3 处做「回退→红 / 恢复→绿」锚定力实测（M5-A/M5-C/M5-D）。评审阶段只读；修复阶段全部改动含于工作区，门禁数字为修复后本机实测。
