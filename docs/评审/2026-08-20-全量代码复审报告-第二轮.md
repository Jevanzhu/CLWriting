# CLWriting 全量代码复审报告

- **日期**：2026-08-20（同日完成两轮：上午全量评审 + 中级 #1-#5 修复；下午二轮全量复审 + 剩余全部缺陷修复收口，见第十、十一节）
- **评审基线**：`dev` 分支 `f876e31`（迭代方向批收口提交）起步，工作区干净；667 提交，dev 领先 origin/dev 20 个提交（未推送）
- **方法**：五路并行代码走查（RAG/机检缓存、AI 编排与管线、服务端与事件、Vue 前端、文档层与杂项）+ 本机全量验证门禁实测 + 关键缺陷人工抽验。上午报告的 5 条重点中级缺陷均经人工二次核实；下午二轮以「编译器视角」全量走查（类型边界、错误分支、竞态窗口、契约漂移），新发现 1 条高危与 3 条中级，连同上午遗留 #6-#8 与低级清单逐项修复。
- **与上版报告的关系**：本报告是《2026-08-19-全量代码评审报告》（92% / 8.5 分）之后的全面复审，重点核对迭代方向批 0-8 的真实落地情况与上版三条风险线的闭环。

---

## 一、总评

| 维度 | 结论 |
|---|---|
| 完成度 | **约 97%**——设计方案四方向九批次全部兑现（批 8 经实测合理关闭），上版三条风险线全部闭环；两轮评审发现的全部高危/中级缺陷（含二轮新发现的缓存投毒）已当日修复收口，剩余为语料弹药与发布管线动作 |
| 质量分 | **8.8 / 10**——数据安全纪律、测试治理、错误出口收敛是长板；二轮补齐了路径安全统一实现、前端逻辑层覆盖率门禁、切书竞态系统解法后，原短板仅剩个别「填表即生效」的预留位（TOKEN_COEFFICIENTS 待实测回填）与两处既有行为语义留待产品决策 |
| 交付判断 | **功能完备、门禁全绿、已达交付级**；打正式版前建议：推送 dev、装语料弹药（需真实存量书）、发布管线 Electron GUI 冒烟 |

规模盘点：后端 TypeScript strict 约 4.1 万行（23 个模块域）+ Vue 3 前端约 3.1 万行 + 测试 317 文件约 5.2 万行；Electron 桌面壳 + node:sqlite（事件库 / RAG / 缓存三库）+ JSON/YAML 配置；AI 侧三协议适配器统一走 runTask 编排。

## 二、验证门禁实测（全部本机跑通，数字为批 7 低级收尾批后终态）

| 门禁 | 结果 | 与声称对照 |
|---|---|---|
| `tsc --noEmit` | 零错误 | 一致 |
| `vue-tsc --noEmit`（前端） | 零错误 | 一致 |
| `npm test` | **287 文件 / 2650 单测全绿**（34.3s） | README 声称 2650/287，一致 |
| `check:counts` | 通过——「实测：287 个测试文件 / 2650 单测；28 e2e spec / 41 用例」 | 一致 |
| `npm run test:e2e` | **39 过 / 2 跳 / 0 败**（31.2s，release-smoke 两例为既有环境门控；新增切书残留专项 spec） | 一致 |
| `npm run test:coverage` | **两桶全过（批 7 后复跑，退出码 0——vitest per-glob 阈值门强制）**：全库聚合 lines 80.71 / branches 82.91 / functions 89.94；二轮分桶明细：主代码（除 web-next）lines 89.45 / branches 82.89 / functions 95.9 ≥ 门 82/78/93，web-next 逻辑层（stores/composables/shared/api）lines 45.66 / branches 83.41 ≥ 新门 43/81 | 高于阈值门 |

## 三、上版风险线闭环核对

| 上版风险 | 状态 | 证据 |
|---|---|---|
| R1 README 测试数漂移 | **已闭环** | check-counts.mjs 把对账变成 CI 门禁，本次实测通过 |
| R2 机检词表未接线（恒绿） | **已闭环** | imagery 种子表三级回落（runner.ts:144）+ 信息差 leak_keywords 账本 front matter 派生三级供给（runner.ts:164、leak-derive.ts），wordlist-wiring 测试走生产链路验证三态 |
| R3 事件库改名迁移 WAL 窗口 | **已闭环** | 迁移前显式 `wal_checkpoint(TRUNCATE)`、三后缀整搬 + 逆序回滚、目标位防覆盖（store.ts:358-447）；回归测试含「唯一数据副本在 -wal」专项（store-migrate.test.ts:133-166） |

## 四、迭代方向批兑现度（设计方案 → 实码）

九批次全部落地，抽查的核心断言均有 file:line 证据：

- **A4 结构化日志**：`src/log/` JSONL 按天落盘、串行保序、fail-open、7 天轮转；服务端 47 处 console 全量替换（残留仅 2 处启动就绪 `console.log`，极低）；启动通告横幅 + 事件库迁移失败入通告。
- **A1 树红点增量缓存**：`tree_issues_cache` 双指纹（正文 stat + 裁决信封 stat）+ 全局纪元失效；golden 测试以「删 .cache 全量重算」做逐字节对照，scan-count 测试用 readDraft 计数断言二次请求 0 整读、改 1 章恰 1 次重查。
- **A3 RAG 惰性校验 + 预存范数**：先点积排序取 top-K'=20 再只对命中章做 SHA-256（index.ts:462-479），stale 剔除顺位递补；`chunks.norm` 列 DDL/ALTER/幂等回填齐备；点积数学与全量余弦的等价性由测试逐位锚定（toBeCloseTo 6 位）；scale 界值（3.5 万块召回 <2s、整读 ≤20）保持全绿。
- **C1-C3 摘要金字塔**：章/卷摘要生成器双挂点（定稿即生成不占章预算 + 备料自愈计入预算）、卷摘要 fail-closed 链指纹绑 sourceHash、细纲 prompt 注入当前卷进展（≤800 字硬上限，缺失省略留痕）、`summary.auto:false` 可关。
- **C4 token 校准**：`TOKEN_COEFFICIENTS` 按模型查表已建（表待实测回填，设计如此）、校准脚本 `scripts/calibrate-tokens.ts`（n≥30 才给建议）。
- **D1 用量卡片**：WbUsageCard（byTask 表格 + byDay sparkline 纯 CSS + 金额两态）最小侵入挂载，e2e 有专项 spec。
- **D2+D3 成本与预算**：provider 级 + models 级覆盖价格表、`GET cost-stats` 三维聚合（未配价 enabled:false）、SSE usage cost 可选化（修恒 0 口径）、预算三口径闸（次数/tokens/cost，未配价静默不拦）。
- **B1-B4 语料回归域**：误报按钮（confirm + 前后端双幂等）→ `check/false-positive` 事件（±50 字摘录服务端切）→ `harvest-corpus` 幸存者判定自举 → `corpus:commit` 勾选入库 → golden-master 回归门 CI 生效。
- **批 8 worker 化**：经 P3 闸实测关闭（700 章热路径 20-27ms，冻结已被批 1 消除），决策合理且有基准数据存档。

## 五、分层质量走查（分数为各路评审归并）

| 层 | 分 | 评语 |
|---|---|---|
| src/ai（编排/适配器/记账） | 9 | 重试、超时隔离、四路同口径记账（成功/重试/中断/失败）、400 降级链、流中不重跑守卫，并发细节近乎教科书；历史修复均有编号注释可追溯 |
| src/studio/server + api | 9 | 三层安全防线与 README 逐条对齐且多出 SSE token 一层（timingSafeEqual 比较）；域拆分 41 文件无巨石；五类在途任务 409 闸 |
| src/events | 9 | append-only 事务纪律、引用计数单例、R3 修复带五用例回归锁 |
| src/log / src/git | 9 | 小而完整：串行保序 fail-open；spawnSync 注入免疫 + 超时 fail-closed 有测试锁参数 |
| src/format | 9 | 三层配置链 + round-trip 零 diff 红线 + 四层文本级补丁保注释保未知段 |
| src/document | 9 | 版本/定稿/回收站数据安全成体系（写入顺序推演崩溃窗口、pinned 永久、基线随软删），全库最扛事故的模块 |
| src/desktop | 9 | Electron 安全基线一项不缺（CSP/单实例锁/穿越+realpath 双验/载荷净化/优雅退出） |
| 测试体系 | 9 | 单测 + 数字门禁 + 治理守护测试（依赖方向 + 「模型可见⟺已记录」管线级回归）三层防回归，断言打在计数/等价性/界值上 |
| 前端 web-next | 9 | 分层纪律强、零 `any`、无巨石文件（最大 741 行）、竞态治理成体系（opGen 代数）；二轮补齐三 store reqGen/clear 守卫、误报标记持久化、StyleView 切书重建与逻辑层覆盖率门禁后，原扣分项清零 |
| src/rag / src/cache / src/check | 8.5 | 设计全部兑现且数学经暴力基准验证；二轮修复缓存投毒（H-1）、纪元漏输入、final 章读放大、candidate_depth 生产断链后，仅剩「填表即生效」的预留位属设计待办 |
| src/process / src/driver | 8.5 | 摘要金字塔、场景回退、RAG 降级落地；token 估算 model 口径分裂已在二轮修复（applyBudgetTrim 三处透传 + 重写链字数口径统一） |
| src/install / src/fs / src/export / src/state / src/review / src/metrics | 8-8.5 | 迁移链幂等 docId 不断链、原子写地基教科书级；migrate-defaults「值恰等默认即删」有 log.warn 留痕（语义歧义留产品决策）；safePath 四套平行实现已在二轮统一为 canonical resolveWithinRoot |
| src/learn | 8.5 | 候选制红线执行严格；二轮补定稿过滤（收割只认 finalizedRevision，与导出同源判定），草稿不再混入候选池 |

## 六、缺陷清单（重点中级项均经人工二次核实；状态为二轮收口后终态）

### 中级（8 条全部修复——#1-#5 上午修、#6-#8 二轮修，明细见第十、十一节）

| # | 位置 | 问题 | 影响 | 状态 |
|---|---|---|---|---|
| 1 | `src/process/materials.ts:201` | `rag.candidate_depth` 生产链路断裂：recall 调用点字面量只带 `{enabled, endpoint, model}`，ResolvedRag 的 candidate_depth 不透传 | 书里配置该键永远不生效，恒用缺省 20（功能静默失灵，无报错） | ✅ 已修 |
| 2 | `src/ai/calls.ts:269` | `effectiveRemainingCalls` 超限分支语义反转：`!check.ok` 时返回满额 limit 而非 0 | 预算耗尽时三审降档拿到「额度充足」不降档（review.ts:140 消费）；预算闸本身不受影响 | ✅ 已修 |
| 3 | `WbUsageCard.vue:28` | 用量卡片仅 onMounted 拉取、无 watch(bookName)；WorkbenchView 只 watch 重载了 ruleHits（Y-P2-3 同类 bug 修过） | 切书后旧书的调用量/tokens/**金额**挂在新书工作台，金额属敏感数据错位 | ✅ 已修（二轮再补 catch 失败路径清残留） |
| 4 | `src/format/yaml.ts:784+` | CONFIG_PATCH_LEAVES 白名单漏 `budget.tokens_per_chapter`、`cost_per_chapter`、`summary.auto`、`rag.candidate_depth`（BUDGET_KEYS 与序列化侧均已认这些键） | 经 PUT config 端点改这些键**静默不落盘**；当前前端无此入口，属 API 契约层隐患 | ✅ 已修（二轮再补 stringify 侧 candidate_depth 序列化） |
| 5 | `src/check/tree-issues-cache.ts:80-93` | 全局纪元指纹漏输入 `工作区/细纲.md`（declaredLeadIds 来源）与 `设定/境界体系.md`（growth 输入） | 这两类文件编辑后树红点不失效，两端闭合/成长红项可陈旧（假红残留或漏红） | ✅ 已修 |
| 6 | `src/check/run.ts:246-248` | final/published 章每请求整读（deriveStatusFull → readPublished） | 「二次请求 0 整读」只在无定稿章的书成立；成熟书 O(final 章数) 整读/请求，削弱 A1 收益 | ✅ 二轮已修（probeCachedPublished 探针，published 零整读） |
| 7 | `src/events/store.ts:376` + `books.ts:299-333` | 改名路径竞态：abort 是异步信号，同步段随即强制关库，straggler 编排随后对已关库写会抛错 | 数据不丢，但对话可能以 error 收尾；缺「等待在途编排收尾」的同步点 | ✅ 二轮已修（settling 登记 + waitChatSettled/waitSelfHealSettled + 10s 上限等待，优雅退出同款） |
| 8 | `src/ai/calls.ts` 消费侧 `prepare.ts:427-440` | applyBudgetTrim 降档/移除两轮漏传 model（初始累计传了） | TOKEN_COEFFICIENTS 填入非 0.6 系数后扣减与累计口径分裂，「填表即爆」的潜伏缺陷 | ✅ 二轮已修（三处透传 + 注入系数 1.2 的断言测试） |

### 二轮新发现（上午评审未覆盖，同日修复）

| 编号 | 位置 | 问题 | 影响 | 状态 |
|---|---|---|---|---|
| **H-1（高危）** | `src/check/run.ts` 缓存写入路径 | **树红点缓存投毒**：checkWithDb 瞬态失败（如 db busy）时 hasRed 已固化为 false 并照常写入增量缓存——错误结果被指纹背书为合法 | 机检一次抖动 → 假阴性（漏红）被缓存固化，此后编辑正文才会失效；「绿了但实际有红」直接绕过作者视线 | ✅ 已修（失败不落缓存 + 瞬态失败专项测试） |
| H-1′ | `src/learn/index.ts` | 收割不过滤未定稿章（上午低级项，二轮升级修复）：草稿段混入样章候选 | 流水线刚写的段被勾选入库 → 污染文风基准与注入素材 | ✅ 已修（finalizedPathSet 同源判定，与导出 V-P2-2 一致；无定稿正文报错兜底） |
| H-2 | `Book.vue` StyleView 无 `:key` | StyleBaselineCard 铁律编辑框/StyleAcceptancePanel 分析结果是组件本地 ref，不随 store 切书重载 | A 书展开编辑 → 切 B 书 → 保存会把 **A 书铁律整段覆盖写进 B 书**（跨书写坏） | ✅ 已修（:key 重建实例） |
| M-1 | `stores/check.ts` | 误报标记 in-memory Set 不持久、不随 clear 清理（checkId 是检查器级 id 跨文档同名） | 刷新即失灰显态；A 文档标记灰显到 B 文档同名命中、误报按钮被禁 | ✅ 已修（localStorage 按 书+文档 分桶 + clear 清理 + 回填） |
| M-2 | `stores/style.ts` | load 无请求代守卫：切书 clear→load(B) 后 A 书慢响应仍可落地 | B 书文风页显示（加载失败则长留）A 书条目库/候选/定标 | ✅ 已修（reqGen 后调者胜） |
| M-3 | `stores/learn.ts` | harvest 无请求代守卫（全书扫描大书数秒） | 切书后 A 书正文候选挂 B 书名下，作者勾选入库即**跨书污染条目库** | ✅ 已修（reqGen 同款） |

### 低级（二轮逐项处置：✅ 已修 / 📝 留痕记录）

- ✅ `audit.ts:107-119`：shadowed 标注 O(events×nodes) → 先建 Set 再查（O(n+m)）。
- ✅ `src/learn/index.ts` 定稿过滤（升级为 H-1′，见上表）。
- 📝 `test/corpus/checks/`：语料回归门弹药未装——工具链已就绪，但 `corpus:harvest` 需要带 `.版本/` 章快照的存量书；Dev/ 工作区无此类书（冒烟跑通：零候选、退出码 0、产物目录 gitignore），待真实书到位后跑一轮即充填。
- ✅ 前端逻辑层纳入覆盖率门禁（per-glob 两桶：主代码 82/78/93 + web-next stores/composables/shared/api 43/81，见第二节）。
- ✅ `stores/check.ts:53` 误报标记持久化（升级为 M-1，见上表）。
- 📝 `cost-stats` 失败调用不计成本（注释声明为有意，低估口径留产品决策）；✅ cost/trace/log 三处按日分桶已统一为本地日（localDayKey）。
- ✅ `self-heal.ts` done 事件 cost 恒 0 → 按 creative 档模型查价格表四档分计（与 /spawn 同口径，未配价省略）。
- ✅ safePath 四套平行实现统一为 canonical `resolveWithinRoot`（批 6，见第十一节）。
- ✅ `desktop/main.ts:401`、`server-main.ts:45` 启动 console.log → log.info。
- ✅ `rag/config.ts:149` enableRag 整段替换抹 candidate_depth → 增量保留语义（上午连带修复）。
- ✅ 干净性小疵：run.ts 两处 DatabaseSync 补 busy_timeout、rebuild.ts statSync 补 throwIfNoEntry 守卫、knowledge/manifest.ts 元信息匹配改 front matter 精确标量比对（fmScalar）、documents.ts recordForeshadowDelta 的 store.close 移入 finally、SSE 错误日志脱 token（urlPathOnly）。
- ✅ 其余 6 条低级遗留（events 孤儿修复时机 / mock pre 无上限 / leak-derive 引号逗号 / schema DDL 双份 / short-index 空锚点计数 / harvest-corpus 基准漂移与复杂度）→ 批 7 全部修复（第十一节）。
- 📝 既有行为留痕（不修，理由）：openai-adapter usage 全缺时上报 {0,0}（建议后续加「缺失」留痕标记，避免与真实 0 混淆）；rag 配置段解析以 enabled 为门（单配 candidate_depth 不可达，属配置模型既定）；patchTopSection 丢段内注释（文本级补丁的既定粒度）；export「定稿正文」导出当前内容（曾定稿即过，含 revision 脏数据——判定已由 finalizedPathSet 统一，语义留产品决策）；migrate-defaults「值恰等默认即删」（有 log.warn 留痕）。

## 七、值得点名保持的资产

1. **数据安全纪律**：原子写（同目录临时文件 + fsync + 0600）是全库写路径地基；finalize 写入顺序推演过崩溃窗口；版本快照 pinned 永久；防穿越三连 + DELETE 递归防护。
2. **修复即留痕**：注释里大量 P/V/X/W 编号记载历史缺陷与推演，几乎每个修复都有防复发测试。
3. **三层测试治理**：单测断言打在行为不变量上（计数、等价性、界值）+ README 数字对账门 + 治理守护测试（依赖方向白名单已清零、「模型可见⟺已记录」有牙）。
4. **安全模型诚实**：README 明说令牌防的是远端网页不防本机进程，实现与文档逐条对齐且多出 SSE token 一层。
5. **降级红线一致**：RAG 三态降级留痕不崩主路径、账本永走精准读取不走 RAG、缓存失效一律退全量重算。
6. **路径安全单一真相源（二轮新增）**：canonical `resolveWithinRoot`（防穿越 + symlink 双侧 realpath + fail-closed，目标存在时返回 realpath）统一六处调用面（service/files/trash/desktop×2/style/books×2），「书路径=书库自身」等边界在 canonical 一处收紧。

## 八、建议行动清单（按优先级；状态为二轮收口后终态）

1. ~~**修 5 条已证实中级缺陷**（#1-#5）~~ ✅ 上午完成（第十节）；~~**修 #6-#8 与二轮新发现（H-1/H-2/M-1/M-2/M-3）**~~ ✅ 二轮完成（第十一节）；~~**6 条低级遗留**~~ ✅ 批 7 完成（第十一节）。
2. **推送 dev**（现领先 origin/dev 20 个提交 + 本次两轮修复改动，单点丢失风险）——唯一遗留的发布前必做项。
3. **跑一轮 `corpus:harvest` 装语料弹药**：工具链就绪并已冒烟验证，但需要带 `.版本/` 章快照的真实存量书（Dev/ 工作区无，零候选）；有真实书后跑一轮 + `corpus:commit` 勾选入库即完成充填。
4. 📝 预留位回填：`TOKEN_COEFFICIENTS` 待 `scripts/calibrate-tokens.ts` 实测（n≥30）回填——填表即生效，扣减口径已修齐等待数据。
5. 发布前补一次 Electron 打包壳 GUI 态冒烟（编译产物 server 态已过，GUI 态随发布管线）。

## 九、结论

项目从上版评审的 92% / 8.5 分，经上午评审 + 修复（96% / 8.7），到二轮全量复审 + 修复收口后为 **97% / 8.8 分**：四方向九批次设计全部兑现，三条历史风险线带回归测试闭环，两轮评审累计发现的 **8 条中级 + 1 条高危（缓存投毒）+ 5 条二轮中级/高危前移项全部当日修复**，批 7 再收掉最后 6 条低级遗留；工程化短板（路径安全四套实现、前端逻辑层无覆盖率门禁、切书集成无 e2e）一并补齐。全套门禁（类型/单测/对账/e2e/覆盖率两桶）本机实测全绿（287 文件 / 2650 单测 / 28 spec 41 用例）。残留事项均为外部依赖或产品决策（推送分支、真实书语料、系数实测回填、导出定稿语义），无高危、无数据损坏面——**已达交付级，可进入发布管线**。

---

## 十、修复收口记录（同日补录）

第八节行动清单第 1 项已于同日完成——中级 #1-#5 全部修复，各配回归测试；过程中另发现并修复两个连带缺陷：

| 项 | 修复内容 | 回归测试 |
|---|---|---|
| #1 candidate_depth 断链 | materials.ts 召回点透传 `ragConfig.candidate_depth`；连带修 enableRag 整段替换抹键（dd-P2 同款保留语义） | materials.test.ts（定向桩向量控制排序：深度 1 + 首位章过期 → 宁缺毋滥；缺省 → 新鲜章递补）+ rag.test.ts enableRag 保留键 |
| #2 effectiveRemainingCalls 反转 | 超限/损坏分支返回 0（与预算闸拦截语义一致），不再误返满额 | pricing.test.ts 超限两口径 → 0 |
| #3 用量卡片切书残留 | WbUsageCard 抽 load() + watch(bookName) 重拉，带竞态代数守卫（opGen 模式） | usage-card.test.ts 切书重取数、旧书金额不残留 |
| #4 补丁白名单漏键 | CONFIG_PATCH_LEAVES 补 `budget.tokens_per_chapter`/`cost_per_chapter`、`summary.auto`、`rag.candidate_depth`（键序对齐 stringify） | yaml.test.ts 落行 + 回读 + 删键三向 |
| #5 纪元漏输入 | computeTreeIssuesGlobalFp 补 `工作区/细纲.md` 与 `设定/境界体系.md`，头注释同步 | tree-issues-cache.test.ts 两文件变更 → 清表 |
| 连带 A（新发现） | **summary.auto 解析死分支**：parseValue 不产出布尔，`typeof v === 'boolean'` 永假——书里写 `summary.auto: false` 静默失效（关闭开关无效），既有测试只测内存构造 config 未走文件 round-trip 故未暴露。按 strict 同款字符串比较归一修复 | yaml.test.ts 回读断言（白名单测试的 reparse 即其回归锚） |
| 连带 B | materials 测试定位过程中确认：RAG 分块对 trim 后 <20 字的段不成块——非缺陷（既定粒度规则），已作为测试注释口径 |

收口门禁实测：`npm test` **285 文件 / 2619 单测全绿**（+6）、`check:counts` 通过（README 已同步 2619）、`tsc` + `vue-tsc` 零错、e2e 38 过 / 2 跳 / 0 败。剩余项：#6-#8 中级与低级清单按第八节排期；推送 dev 与 `corpus:harvest` 仍待办。

---

## 十一、二轮复审全量修复收口（同日二录）

第六节全部遗留项（#6-#8、H-1/H-1′/H-2、M-1/M-2/M-3、低级清单）按下述六批修复，每项配回归测试；两批门禁数字见第二节终态。

### 批 1 check/cache（高危收口）

| 项 | 修复 | 回归测试 |
|---|---|---|
| **H-1 缓存投毒** | checkWithDb 失败（rebuildFailed/checkFailed）时**不写**增量缓存——瞬态错误不再被指纹背书固化 | tree-issues-scan-count.test.ts：mock 首查抛错 → 缓存表零行；复跑成功 → 正常落缓存 |
| #6 final 章读放大 | deriveStatusFull 换 deriveStatus + probeCachedPublished（W-P2-4 单次读探针带出 published，stat 级缓存） | 同文件：published 章二次请求 readPublished 调用数为 0（mock status.js 计数） |
| busy_timeout ×3 | run.ts 两处缓存库 + self-heal db 打开后补 `PRAGMA busy_timeout=5000` | 既有并发测试（H-1 修复后瞬态 busy 不再污染，双保险） |
| rebuild statSync 守卫 | scanSummaries `throwIfNoEntry:false` + isFile 判空跳过 | 既有 scan 测试覆盖 |

### 批 2 format/install

| 项 | 修复 | 回归测试 |
|---|---|---|
| stringify 丢 candidate_depth | stringifyBookConfig rag 段条件输出该键 | yaml.test.ts 写→读 roundtrip |
| 迁移纯净态误判 | migrate-defaults 纯净判定补 candidate_depth === undefined | migrate-defaults.test.ts：仅配 depth 的书不回写默认段 |

### 批 3 ai/process（口径统一）

| 项 | 修复 | 回归测试 |
|---|---|---|
| #8 applyBudgetTrim 漏 model | 三处 estimateTokens 补第二参 model | prepare.test.ts：注入 TOKEN_COEFFICIENTS['cal-test']=1.2，断言估算=剩余段和（finally 清表） |
| limit≤0 NaN | effectiveRemainingCalls 前置 `limit<=0 return 0` | pricing.test.ts |
| 重写链字数口径 | wordRange 导出 + buildRewritePrompt 可选 targetWords + rewrite 工具读 book.yaml chapter_target_words 透传 | rewrite-prompt.test.ts +4（配/不配目标的范围行） |
| self-heal cost 恒 0 | done 事件按 creative 档模型查价格表四档分计（含 cache 读写档），未配价省略字段 | self-heal-f2.test.ts：配价 → cost>0 且四档可复算；未配价 → 无 cost 字段 |

### 批 4 server/events/desktop（竞态与日志）

| 项 | 修复 | 回归测试 |
|---|---|---|
| #7 改名/删书竞态 | chat/self-heal 各建 settling Promise 登记，导出 waitChatSettled/waitSelfHealSettled；books.ts 删书/改名在 409 闸后、搬目录前 `awaitOrchestrationsSettled`（10s 上限）；graceful-shutdown 并行等待在途编排（1.5s 上限） | chat-settle.test.ts（新建：在途不 resolve/收尾后 resolve/无在途立即）+ self-heal-f2 同款 |
| audit O(n×m) | shadowed 先建 Set 再查 | 既有 audit 测试 |
| 本地日分桶 | log 新增 localDayKey，cost-stats/trace-stats 统一本地日（与日志同口径） | log.test.ts 时区无关断言（本地分量构造期望） |
| SSE token 入日志 | http.ts 新增 urlPathOnly，router×2 + server/index 错误日志改用（query 不再含 token） | 既有路由测试 |
| console×2 / close 位置 | desktop main/server-main 启动就绪走 log.info；recordForeshadowDelta store.close 移入 finally | 既有路径测试 |

### 批 5 前端/learn/knowledge（切书竞态收口）

| 项 | 修复 | 回归测试 |
|---|---|---|
| H-1′ learn 定稿过滤 | 收割只认 finalizedRevision（finalizedPathSet 与导出同源）；全草稿书明确报错 + skippedDrafts 计数 | learn/index.test.ts（新建 3 例：无清单全量/草稿跳过/全草稿报错）+ e2e fixture 新增已定稿章 0004（dual-track 同步对账 4 章） |
| H-2 StyleView 跨书写坏 | Book.vue StyleView 挂 :key=bookName（本地 ref 态随实例重建） | e2e 切书 spec（批 6） |
| M-1 误报标记 | localStorage 按 `clw-fp:<书>:<文档>` 分桶：run 回填、flagFalsePositive 持久化、clear 清展示态 | check-store.test.ts +4（标记持久化/回填/clear/跨文档隔离，Map 桩替 localStorage） |
| M-2/M-3 reqGen 守卫 | style.load / learn.harvest 请求代守卫，clear() 使旧代作废 | style-store / learn-store 各 +1（门闩挂起 A → clear+load B → 放行 A → 断言不污染） |
| WbUsageCard 失败路径 | catch 分支同代守卫后清旧书数据（金额错位的失败路径复现面） | usage-card.test.ts 扩展 |
| knowledge manifest | 元信息比对改 front matter 精确标量（fmScalar），不再子串匹配 | npm run check:knowledge 实测通过 |

### 批 6 工程化（结构补强）

| 项 | 内容 | 验证 |
|---|---|---|
| safePath 统一 | src/fs/safe-path.ts 落 canonical `resolveWithinRoot`（返回 {abs, rel}，存在时 abs=realpath，fail-closed）；service.resolveSafePath / files.safePath+editablePath+writablePath / trash.safePathWithin / safeManifestPath / desktop show-in-folder·open-book-dir / style 删条目 / books 删书守卫全部委托；**改名 handler 补上此前缺失的同款越出/symlink 守卫** | safe-path.test.ts +5（空/NUL/穿越/绝对路径/落到根自身拒绝、不存在 posix rel、存在 realpath、symlink 越出） |
| 覆盖率纳入前端逻辑层 | vitest.config.ts per-glob 两桶（主代码单桶 brace+extglob 组合保持原池化口径 82/78/93；web-next stores/composables/shared/api 新桶 43/81 = 实测基线 −2pp） | test:coverage 实测全过（见第二节） |
| e2e 切书 spec | switch-book.spec.ts：A 总览 → 命令面板返回书架 → B 总览，断言标题/类型/章数全部换口径（长篇 4 章 ↔ 短篇 2 章） | 通过（1.2s） |
| corpus:harvest 评估 | 对 Dev/Main 冒烟：跑通、零候选（Dev 无 `.版本/` 章快照的存量书）、产物目录 gitignore | 记录在案，待真实书充填（第八节第 3 项） |

### 批 7 低级收尾批（同日三录）

两轮收口后仍遗留的 6 条低级项，逐条修复：

| 项 | 修复 | 回归测试 |
|---|---|---|
| events 孤儿修复时机 | 修复只在 openSessionStore 首开跑一次——崩溃残留若开库时距最后活动 <10min 被宽限期跳过，单例连接长开（refs>0）下永不补 end（审计流缺收尾）；写路径（createSession / appendEvents / appendEventsResolveLineage）按 TTL=ORPHAN_GRACE_MS 惰性重跑（至多每 10 分钟一次全表聚合，长跑进程不重开库也能补） | store.test.ts +1（假时钟：开库时宽限内跳过 → 11 分钟后写路径触发补 end{interrupted}） |
| mock driver pre 无上限 | cc.ts 有 AA-P3-2 pre cap，mock.ts 同构位置漏了——无消费者期间长 mock 流无限增堆内存；补 MAX_PRE_EVENTS=200 同款 shift 截断 | driver-mock.test.ts +1（连发 205 条，首消费者只接管最近 200：attempt 6..205） |
| leak-derive 引号内逗号劈开 | 账本 fm `leak_keywords: ["玉佩,旧案", 乙]` 被 bare `split(',')` 劈成两个关键词（K17 同款问题在 frontmatter 已修，此处漏）；复用 splitInlineArray（export 化） | corpus-domain.test.ts +1（引号项不劈、去引号） |
| cache/schema DDL 双份手抄 | tree_issues 两表 DDL 在 DDL_STATEMENTS（带列注释）与 ensureTreeIssuesTables（裸）各写一份，改列要同步两处易漂移；收编单源 TREE_ISSUES_DDL（声明置于 DDL_STATEMENTS 之前，避免 TDZ），两处共用 | 既有 cache 套件（纯重构，无行为变化） |
| short-index 空锚点计数虚高 | setupHasAnchor 在正文无 ## 锚点时 return true，anchoredSetupCount 虚报成全量铺垫数（评分侧有 anchors.length>0 卫语句不受影响，只纠报告口径）；改 return false 如实计 0 | 既有 metrics 套件（无断言依赖旧口径） |
| harvest-corpus 基准漂移 + O(n×m) | 幸存者基准拿「当前草稿文件」而非定稿内容——定稿后继续写会让基准漂移（草稿又改丢 ≠ 作者否定命中）；改锚定 pinned finalize 版本（从未定稿退化为现行文件，与脚本头注释「定稿正文」声明对齐）。manifest 查找从每章全量扫 entries 改 Map 索引（O(章×条目)→O(章+条目)） | corpus-domain.test.ts +1（定稿保留堆砌 + 定稿后正文清掉 → 判幸存不随草稿翻转） |

### 收口门禁（终态）

`tsc` + `vue-tsc` 零错；`npm test` **287 文件 / 2650 单测全绿**（较上午 2619 净增 31：二轮 27 + 批 7 新增 4）；`check:counts` 通过（README 已同步 2650/287/28/41）；`npm run test:e2e` **39 过 / 2 跳 / 0 败**（新增切书 spec）；`npm run test:coverage` 两桶全过（批 7 后复跑，数字见第二节）。

---

> **评审方法说明**：五路并行代码走查覆盖 src 全部 23 个模块域与测试体系；两轮评审累计 8 条中级缺陷中 5 条经人工读码二次核实（#1-#5），其余 3 条来自走查记录（file:line 可复核）；二轮新发现（H-1/H-2/M-1/M-2/M-3）均先写复现推演再修复。评审阶段严格只读；修复阶段改动全部在 dev 工作区未提交，收口门禁见第二、十一节。
