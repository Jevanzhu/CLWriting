# CLWriting 全量代码复审报告（第四轮）

- **日期**：2026-08-21（第三轮同日晚间收口轮）
- **评审基线**：`dev` 分支 `63e4c1f` + 工作区未提交改动（55 文件 +879/-236 = 第三轮全部修复 + 本轮收口修正）。HEAD 领先 origin/dev 21 个提交（未推送）。
- **方法**：① 主评审对第三轮 13 项缺陷修复做 diff 级逐项核实（不信注释，读实现、追输入覆盖）；② 三路独立评审代理并行——新修复代码正确性审计 / 三类系统性模式（fire-and-forget 逃逸、前端切书守卫、replyError 缺 return + safePath 平行实现）全库遗漏扫描 / 12 个新改测试文件锚定力评审；③ 全量门禁本机实测；④ 本轮发现即修即锚——三处关键回归测试做了「回退修复→确认变红→恢复→确认变绿」的锚定力实测。
- **与上版报告的关系**：第三轮（同日早间）判定 97% / 8.4，行动清单要求修 H-1 + 头部中级后进发布管线。本轮核实那些修复的真实成色，修正修复中的残余缺陷，并重新评分。

---

## 一、总评

| 维度 | 结论 |
|---|---|
| 完成度 | **约 97% 维持**——功能面与第三轮判定一致（设计方案四方向九批次全部兑现），剩余仍为外部动作（提交推送、真实书语料、系数回填、发布管线），无新开发项 |
| 质量分 | **8.7 / 10**（第三轮 8.4 → 上调）——第三轮 1 高危 + 12 中级经本轮核实**全部真实落地**，但复核抓到修复批自身 6 处残余缺陷（1 中级接线洞 + 5 处低中级），均已当场修复并配回退验证过的回归测试；三类系统性模式（缓存跨章输入、成本协议口径、settle/守卫覆盖面）已从「抓大头」收口为「铺满 + 注释制度化」 |
| 交付判断 | **可进发布管线**——功能完备、门禁全绿、无数据损坏面、无安全绕过面。硬前置仍是：提交推送工作区（当前全部修复只存在于工作区）→ 真实书语料 + token 系数回填 → Electron GUI 冒烟 → 切 tag |

规模盘点（与第三轮一致）：后端 TypeScript strict 约 4.1 万行 + Vue 3 前端约 3.1 万行 + 测试 5.2 万行；单测增至 **290 文件 / 2667 用例**（本轮 +1 文件 +4 用例）。

## 二、验证门禁实测（全部本机跑通，2026-08-21 晚间）

| 门禁 | 结果 |
|---|---|
| `tsc --noEmit` | 零错误 |
| `vue-tsc --noEmit` | 零错误 |
| `npm test` | **290 文件 / 2667 单测全绿**（23.8s） |
| `check:counts` | 通过（290/2667；28 spec/41 用例；README 已同步新数字） |
| `npm run test:e2e` | **39 过 / 2 跳 / 0 败**（23.4s；跳过为 release-smoke 环境门控） |
| `npm run test:coverage` | 退出码 0（per-glob 两桶阈值门全过） |
| `npm run check:knowledge` | 通过（知识层 13 条 manifest 与磁盘一致） |

## 三、第三轮修复核实（逐项读码 + 独立代理审计）

| 第三轮缺陷 | 核实结论 | 备注 |
|---|---|---|
| H-1 树红点缓存跨章陈旧 | ✅ **落地且设计成立** | 全书性账本条目拆 `checkLeadsBookItems`，本书一次计算 + `leads_book_fp/red` 独立缓存（纪元 + 写作/正文 dirFp）；章级行经 `skipLeadsBookChecks` 只留章作用域条目；`CHECKER_GENERATION` 升 a1-v2 整代失效防混存；输入覆盖经三步核验（布线 db 在纪元、正文在 dirFp、maxWritten 来源=清单+正文均已覆盖）；合并只在展示层。回归测试双向锚定（假红/漏红），被代理评为本批最强测试 |
| M-1 OpenAI cacheRead 双计 | ✅ 落地 | 适配器边界扣减归一（Chat/Responses 两线 `Math.max(0, prompt−cached)`），types.ts 口径声明同步改写；计价/预算/记账/trace 全下游同口径；calibrate 脚本按新口径重建全量输入；adapter.test 两处 decisive 断言直接锚住扣减 |
| M-2 后台任务逃逸 settle | ⚠️→✅ **主体落地、接线有洞，本轮补齐** | background.ts 登记表 + 3 处调用点 + 优雅退出接线均正确；但 books.ts 删书/改名的 settle 条件是 `hadSelfHeal \|\| hadChat`——纯后台任务场景（定稿摘要、无 chat/self-heal 在途）整体跳过等待，正是 M-2 点名的场景。本轮补 `hasBackgroundTasks` 判定（两处），配删书端到端测试（回退验证：去掉判定测试即红） |
| M-3 abort 后续链逃逸 | ⚠️→✅ **实现落地、测试假锚，本轮重锚** | 循环版 waitChatSettled/waitSelfHealSettled 正确；但新增测试在续链同步替换表项**之后**才发起等待，旧单轮实现同样能过（假绿）。本轮重写为「第一轮在途时发起等待」的真锚，并实测：回退循环实现 → 测试红；恢复 → 绿 |
| M-4 settle 等待窗 TOCTOU | ✅ 落地（注释修正） | busyGate 三闸集中 + 等待后复查，复查到 rmSync/renameSync 之间全同步，三闸 TOCTOU 窗归零；窗内新起 chat/self-heal 由 10s 超时降级兜底（与旧版一致）——原注释「窗归零」表述过宽，本轮已改准确 |
| M-5 schema 缺 return | ✅ 落地 | 一行修复 + router-schema 测试以 handlerCalls 计数锚定 |
| M-6 store.close 不在 finally | ✅ 落地 | check.ts / self-heal / runner 三处 mkChain 全部 close-in-finally |
| M-7 safePath 第五套平行实现 | ⚠️→✅ **API 层已修、内层残留，本轮收口** | api/style.ts 两端点已补 resolveWithinRoot；format/style-candidate.ts 内层手写穿越检查仍是无 symlink 防护的弱实现。本轮内层改为委托 resolveWithinRoot（防裸调用绕过 + 防后来者照抄），配穿越/绝对路径/symlink 三路回归测试 |
| M-8 server-main 信号挂起 | ✅ 落地 | 2s 超时强退 + 幂等防双信号 |
| M-9 树红点 fail-closed 500 | ✅ 落地 | rebuild/开库/PRAGMA 硬异常按 fail-open 降级（warn + 只算 verdict），与缓存层红线对齐；坏库回归测试锚定 |
| M-10 TrashPanel 切书竞态 | ✅ 落地 | loadGen 代守卫（restore/purge 后 load 同享）；动作层跨书窗为已知残余（服务端 404 兜底，见第六节） |
| M-11 次级 action 守卫未铺满 | ⚠️→✅ **大部分落地、漏 1 处，本轮补齐** | Analysis/Foreshadow/MetaForm/learn.commit/rewrite/style.rescan/harvest/ruleHits 全部带代守卫且作用域正确；代理全库扫描发现 **WritingInfoPanel（常驻右侧栏）漏守卫**——快速切书时 A 书字数目标落到 B 书进度显示，本轮补 configGen 守卫 |
| M-12 workbench.clear 不复位 running | ✅ 落地 | 有测试锚定 |
| 低级清单（约 15 条） | ✅ 全部落地或明确留痕不修 | candidate_depth 双侧钳制 / 零块章指纹并入 / published 剥引号 / spec.ts listener detach / 记账 IO 防护 recordUsageSafe / preload 摘 stale once / chat-clear resolveBook 404 / review NUL 分隔 / 删书清 .db 文件 / check-counts 拦 .only / 覆盖率桶收编 / e2e 0001 恢复——均实现正确；「done 事件不含重试消耗」留痕不修（失败响应多无 usage，客观不可得，注释已写明口径） |

## 四、本轮新发现与修复（6 条，全部当场修复 + 回归锚定）

| # | 位置 | 问题 | 处置 |
|---|---|---|---|
| R-1（中级） | `books.ts` 删书/改名 settle 条件 | M-2 接线洞：纯后台任务（定稿摘要）在途且无 chat/self-heal 时跳过等待，rmSync 先行 → 摘要 straggler 对已删路径重建孤儿目录（两路代理独立发现） | 补 `hasBackgroundTasks` 判定；端到端测试（HTTP 真起服务删书 + 在途任务），**回退验证过** |
| R-2（中级） | `chat/restore.ts` msgSeqs 防御重置 | 第三轮低级项未修：错位时清空 `msgSeqs=[]` 会让 trim 遮蔽永久错位——既漏遮被裁消息（幽灵回归）又误遮保留消息（重放隐身）。首轮修复的 `>` 分支截头方向也是错的（超长来源是回合回滚的尾部死 seq） | 占位对齐（`<` 前缀 unshift []）+ 截尾对齐（`>` 尾部丢弃死 seq）；2 个回归测试，**回退验证过（旧实现两条全红）** |
| R-3（中级，测试缺陷） | `chat-settle.test.ts` M-3 用例 | 假锚：等待发起时机在续链替换表项之后，旧实现也绿——M-3 修复处于无守卫状态 | 重写为第一轮在途时发起等待；**回退验证过（旧实现红）** |
| R-4（低中） | `WritingInfoPanel.vue` | 常驻右侧栏 watch(bookName)→getConfig 无代守卫，切书慢响应错书回填（字数目标/进度/口径显示错位） | 补 configGen 代守卫（全库扫描确认其余面板/store 均已有守卫） |
| R-5（低） | `tree-issues-cache.ts` dirFp | 指纹 "count:size:maxMtime" 对**纯改名**不敏感：章节文件改名改变 findChapterFile 章号映射与引文 grep 输入，但 leads_book 缓存不失效（章级行键含 rel_path 无此问题；外部改名连 structural 失效都不触发） | dirFp 纳入相对路径 FNV-1a（nameHash），一次性整表失效无害 |
| R-6（低） | `knowledge/manifest.ts` | 第六套手写 safePath 平行实现（join+relative 往返，无 symlink 防护）——第三轮宣布「全库统一」后的漏网，知识层/ 内 symlink 指向库外可让校验器读/哈希库外文件 | 委托 resolveWithinRoot（至此全库路径校验唯一真相源）；check:knowledge 复跑通过 |

另：cost-stats 头注释标注 M-1 历史口径边界（跨修复时点的 OpenAI 线累计前段偏高，append-only 不迁移）；README 数字同步 290/2667。

## 五、分层质量评分（本轮重评）

| 层 | 分（第三轮→本轮） | 依据 |
|---|---|---|
| 数据安全层（document/format/install/export/state/git/log + knowledge） | 9→**9** | 无新缺陷；R-6 收口后全库路径校验唯一真相源 |
| 测试体系 | 8→**8.5** | 本批 12 个新改测试 11 个锚定力合格（唯一假锚已重锚）；本轮 3 处回退验证制度化「锚定力实测」；.only 拦截/覆盖率桶/e2e 恢复全落地；扣分项不变：Windows 零 CI、多进程并发、cache schema 演进无回归锚 |
| studio/server + events + desktop + fs | 8→**8.5** | M-4/M-5/M-6/M-8 落地，settle 接线收口（R-1），safePath 全库一套；残余：Settings 弹窗四件套窄窗（见第六节） |
| cache + check + rag | 8→**9** | H-1 落地且设计成立（修掉即可到 9——第三轮预言兑现）；M-9 对齐 fail-open 红线；R-5 改名盲区补上 |
| 前端 web-next | 8→**8.5** | 守卫从「抓大放小」铺满到 panels 全集（R-4 补最后一块），写路径纵深防御与零 XSS 维持 |
| 管线层（process/driver/learn/metrics） | 8.5→**8.5** | M-2 摘要登记落地；无新发现 |
| src/ai（编排/适配器/记账） | 7.5→**8.5** | M-1 归一 + 记账 IO 防护 + listener detach + settle 三窗收口（M-2 接线洞本轮补齐）；残余均为已量化接受的口径边界 |

## 六、已知残余（明确不修 / 留待中期，均有依据）

1. **TrashPanel 动作层跨书窗**：load 已守卫，restore/purge 仍可能在切书瞬间以 B 书名 + A 条目 id 调用；服务端 404 兜底，展示错位一闪而过。
2. **Settings 弹窗四件套**（Book/Writing/Analysis/Retention）watch 无守卫：现实触发路径仅「设置开着时改书名」，彼时旧 fetch 早已完成，双 fetch 重叠窗极窄。
3. **cost-stats 跨 M-1 边界历史口径**：修复前的 OpenAI 线旧事件 input 含 cache，跨边界累计前段偏高；append-only 不迁移，注释已标注切分方法。
4. **同毫秒同尺寸指纹窗口**：mtime+size 撞车与 probeCache 同口径，理论性，已接受。
5. **done 事件 usage 不含失败重试消耗**：失败响应多无 usage，客观不可得；预算/成本以事件库为准（runTask 记账侧四路全口径）。
6. **结构性盲区**（中期）：Windows 平台零 CI（中文网文作者主力平台）、SQLite 多进程并发（CLI + 桌面同开一书）无 WAL/busy 策略与测试、cache 库 schema 演进无回归锚。

## 七、完成进度核算

与第三轮判定一致，本轮增量核实：迭代方向批 0-8 全部兑现且修复链闭合；`TOKEN_COEFFICIENTS` 空表待实测回填、语料回归门种子仅 2 条——预留位如实。阶段 1-17 以文档链 + 门禁绿为准。

**剩余待办（全部为动作，非开发）**：① **提交并推送工作区**（55 文件修复 + 本报告，dev 领先 origin 21 提交，单点丢失风险仍是当前最大工程风险）；② 真实书跑语料收割；③ 校准脚本回填 token 系数；④ 发布前 Electron GUI 冒烟 + 切 tag。

## 八、结论

第三轮「97% / 8.4、修完 H-1 与头部中级即可进发布管线」的判断经本轮全量核实**成立，且修复成色好于上两轮**：13 项缺陷全部真实落地，测试锚定力经独立评审与回退实测验证。本轮复核自身抓到 6 处修复残余（1 中级接线洞 + 1 中级修复方向错误 + 1 假锚测试 + 3 处低中级遗漏）并全部当场收口——连续两轮「修复批自身带伤」的模式说明：**修复必须配独立复核 + 锚定力实测**，本轮已把「回退验证」作为常规工序执行了三处。

**最终评定：完成度约 97%，质量 8.7 / 10，功能完备、门禁全绿（290 文件/2667 单测/e2e 39 过/覆盖率/counts/knowledge 七门全过）、无数据损坏面、无安全绕过面；提交推送后即可进发布管线。**「正式版」前硬清单不变：提交推送 → 真实书语料 + 系数回填 → Electron GUI 冒烟 → 切 tag；中期补 Windows CI 与多进程并发策略。

---

> **评审方法说明**：主评审完成第三轮修复的 diff 级逐项核实（含 H-1 输入覆盖三步推演、M-1 下游口径一致性追踪）；三路独立代理分别负责新修复正确性审计（64 次工具调用）、三类系统性模式全库遗漏扫描（67 次）、12 个测试文件锚定力评审（37 次）；两路发现交叉印证的 R-1 提升置信度。本轮新修 6 项中 3 项关键修复做了「回退→红 / 恢复→绿」锚定力实测（R-1/R-2/R-3）。评审阶段只读；修复阶段全部改动含于工作区，门禁数字为修复后本机实测。
