# 全量代码复审报告 · 第十轮（2026-08-22）

- 范围：src 全目录约 420 文件 / 7.39 万行（不含 web-next 嵌套 node_modules）、test 343 文件 / 5.68 万行、scripts、构建配置、README/CLAUDE.md 承诺对照；并对第九轮遗留的 11 个未提交文件逐项验证修复正确性
- 方法：本机基线五件套实测（tsc / vue-tsc / 全量单测 / e2e / check:counts）+ 5 路并行分模块逐文件评审（src/ai、服务端+桌面、前端 web-next、数据与基础层、测试与工程化）+ 头条发现人工复核（本报告标注「已复核」处均经主评审通道二次验证）
- 基线：`tsc --noEmit` 通过；`vue-tsc --noEmit` 通过；单测 310 文件 / 2800 用例全绿（31s）；e2e 28 spec / 41 用例 = 39 过 + 2 发布门跳过（release-smoke 需 `test:e2e:release` 带构建跑，属有意设计）；`check:counts` 红（见 H-1）

## 总评

| 维度 | 结论 |
|---|---|
| 功能完成度 | ≈97%。README/CLAUDE.md 承诺的对外功能 100% 落地且有 e2e 背书（前端 11 大功能面逐一核对全落地）；剩余约 3% 为注释留档未接线项、webnext api 层测试覆盖、npm 分发形态收尾 |
| 代码质量 | 8.6 / 10（第八轮 8.6、第九轮自评 8.7。本轮微降 0.1 的原因：第九轮 1 项修复自身引入回归（M-1）、1 项带边界回归（低-1），说明「修复-回归测试」闭环存在缺口，而非存量代码劣化） |
| 测试 | 2800 单测 + 41 e2e；覆盖率主桶 lines 89.50 / branches 83.18 / functions 95.94（门 82/78/93），webnext 桶 47.71 / 83.22（门 43/81） |

分模块完成度与质量分：

| 模块 | 规模 | 完成度 | 质量分 |
|---|---|---|---|
| src/ai（provider/orchestrate/tools/prompts/rules/contract） | 73 文件 / 10,311 行 | ~95% | 9.0 |
| src/studio/server + src/desktop | 44 + 8 文件 / 8,749 行 | ~95% | 8.5 |
| src/studio/web-next/src（前端） | 173 文件 / 31,314 行 | 100% | 9.0 |
| 数据与基础层（document/format/process/check/install/rag/events 等 19 模块） | 122 文件 / 23,485 行 | 100% | 8.0 |
| 测试与工程化 | 343 测试文件 / 56,776 行 | ~95% | 8.0 |

项目自 2026-05-14 起步，约 3.2 个月，当前 1.0.0-rc.0。src 内零 TODO/FIXME 残留，测试无 skip/only 遗留（仅 2 处条件门控，理由正当）。

## 一、第九轮未提交修复验证（11 文件）

| 项 | 文件 | 结论 |
|---|---|---|
| H-1 选段改写拼回全文+唯一性 | src/ai/tools/rewrite.ts | ✅ 正确。拼回偏移全程基于同一 body 字符串（readChapterBody 无归一化，无索引错位）；唯一性校验与端点 X-P2-13 逐字同构（含重叠自匹配拒绝）；配套 2 条用例覆盖充分 |
| M-1 三审闸接入清空端点 | audit.ts + stream.ts | ✅ 方向正确（闸位在清库动作前、无循环依赖、双钥匙口径成立），缺直接回归测试（见 M-6） |
| M-2 tree-perf 断言放宽 | test/document/tree-perf.test.ts | ✅ flaky 缓解合理（下限 0.1ms→1ms）；备注：这是性能断言口径连续第三次放松（低-a） |
| L-1 单书 book.yaml 损坏显式 500 | books.ts | ✅ 方向正确，但错误文案把 ParseError 串成 `[object Object]`，且书架列表侧同场景仍静默回空 title——只修了一半（低-2/低-3） |
| L-2 tiers.chat 死分支清理 | providers.ts | ✅ 实际救活了「无 expectedRevision 的清档」（旧代码该路径 400 不可用），但引入 JSON 原语静默清档边界（低-1） |
| L-3 bootstrap 重试关旧 server | desktop/main.ts | ✅ 正确（顺序、no-op 安全性、before-quit 时序均核）；预存的退出途 activate 重建问题仍在（低-9） |
| L-4 apiJson 预 abort signal | web-next api/client.ts | ✅ 正确可提交（预检时机无竞态窗口、AbortError 语义未被伪装成超时）；残余 once 监听器不移除无实害（低-f） |
| L-5 close() 幂等化 | events/store.ts | ⚠️ **守卫拦截了 migrateBookSession 的「置零后强制关库」路径，引入回归（本轮 M-1，已复核）** |
| L-6 dayFile 跨零点 | log/index.ts | ✅ 正确（flush 时取日期、串行队列保序）；换 logsDir 后在途行的 null 边缘为可接受降级 |

## 二、本轮新发现缺陷清单

### 高危（1）

| # | 位置 | 问题 |
|---|---|---|
| H-1 | README.md:8/76/85/89 | 单测数四处写 2799，第九轮净增 1 条用例后实测 2800，`check:counts` 当前必红，CI 对账门被自己的修复改动打破。性质是门禁红而非运行时缺陷，但属提交前必修（四处 2799→2800） |

### 中危（7）

| # | 位置 | 问题 |
|---|---|---|
| M-1 | src/events/store.ts:411-414（已复核） | 第九轮 L-5 守卫 `refs <= 0 直接 return` 恰好拦截 migrateBookSession 的强制关库：该路径先 `entry.refs = 0` 再调 `close()`，依赖旧的「递减到负仍真关」行为，现在 early-return 什么都不做。后果：改名/移动书后旧库连接永不关闭、openStores 残留僵尸条目；自家连接不关则 wal_checkpoint(TRUNCATE) 可能 busy=1 → 迁移整体放弃且每次重试同样失败；即使搬移成功，残留连接仍指向旧 inode，WAL 写入分裂。修法：migrateBookSession 改专用强制路径（如 `entry.refs = 1` 再 close，或直接 delete + closed 置位），并补「改名迁移时残留连接」回归测试 |
| M-2 | src/document/manifest.ts:99-106 | finalizedPathSet 缺「读失败≠空文件」哨兵：readManifest 吞掉 EACCES/EBUSY 等瞬态读失败返回空清单 → 返 null → 三消费方把 null 当「无法判定」降级放行：export/index.ts:141（未定稿草稿混进全本/分章/投稿导出）、metrics/style.ts:136 与 learn/index.ts:127（草稿污染文风趋势与候选池，违反 learn H-1「草稿不进候选池」红线）。姊妹函数 finalizedChapterSetOfBook 第八轮 M-13 已加探测，此处漏同步 |
| M-3 | src/ai/tools/rewrite.ts:75-97 | apply_spill 不校验 spill 与章的归属及新鲜度：模型转述错 locator（`{chapter:3}` 配第 5 章的 spill）会把第 5 章全文写进第 3 章；改写分钟级窗口内作者编辑该章后，落盘的是基于旧 body 拼回的全文，静默覆盖新编辑。两者均有 saveDraft 快照兜底可恢复，但无任何告警。建议 spill 落盘带章号元数据并校验，或落盘前校验首尾段与当前 body 吻合 |
| M-4 | web-next StyleAcceptancePanel.vue:36-39、OnboardView.vue:101/119（已复核） | AI 分析完成回调无书名守卫：`await runStyleAnalysis(props.bookName)`（可达 120s）后直接 `style.load(props.bookName)`，无「入口捕获 + await 后复检」。切书时 keyed 视图死实例的 props 冻结在旧书 A，分析完成时死实例同步改写共享 store——B 书文风页从此显示 A 的数据，后续确认/忽略/收割全部写向 A 书。修法：入口 `const book = props.bookName`，await 后 `if (props.bookName !== book) return` |
| M-5 | 五处命名口径 + src/document/tree.ts:109（已复核） | 章节文件名补零混存五种口径：前端新建不补零（useChapterTreeActions.ts:267 `${nextChapterNo()}-未命名`）、前端复制 4 位、服务端长篇 4 位（service.ts:491）、短篇 3 位（:474）、草稿管线 3 位（format/draft.ts:67）；而树排序是 `localeCompare(zh-Hans-CN)` 无数值 collation——手建 `5-x` 排到 `020-y` 之后、`0100-y` 排到 `099-x` 之前，目录树实际错序。编号解析三口径都认所以功能不坏，纯展示序错乱。建议 compareNode 提取章号做数值优先比较（兼容存量混名），或统一 4 位并迁移 |
| M-6 | 第九轮 6 项 src 修复 | audit/stream 三审闸、books.ts IO 路径、providers.ts 清档、client.ts 预 abort、log.ts 跨零点、desktop 重试关旧 server——均无回归测试，违反本仓「各配回归测试」纪律。M-1 的回归正是这个缺口的直接后果：若当时配了 migrateBookSession 用例，L-5 守卫冲突提交前就会红 |
| M-7 | web-next src/api/（约 2,176 行） | analysis/audit/check/cost-stats/foreshadows/io/learn/onboard/overview/review/rhythm 等十余文件 0 lines 覆盖：stores 测试全部 vi.mock 掉 api 模块，参数/响应映射逻辑零测试；它们落进聚合桶（lines 门 43）被 stores 高覆盖均值掩盖，单文件回退对阈值门不可见。建议 api 层单列覆盖桶或补参数映射用例 |

### 低危（30，按模块收敛）

**服务端/桌面（9）**

1. providers.ts:165（L-2 修复引入）：JSON 原语（`5`/`true`/`[]`/`""`）经 `Object.keys(primitive).length === 0` 误入清档分支，从 400 变静默清档 + revision bump。加 `typeof === 'object' && !Array.isArray` 守卫
2. books.ts:464 + config.ts:33：错误文案 `[object Object]`（error 是 ParseError 对象），两处同改 `.message`（state.ts:48 已是正确写法）
3. books.ts:102-125：书架列表对损坏 book.yaml 仍静默回空 title，与单书 500 口径分叉——L-1 只修了一半
4. outline.ts:223：volumeProgressOf 读 raw 未过 applyGlobalDefaults，全局 defaultVolumeSize≠50 且书级未设时细纲 prompt 注入错卷摘要
5. analysis.ts:291-307：score/emotion 趋势对坏信封 payload 无形状守卫，与同函数 hooks 的 X-P3a 守卫口径分叉
6. books.ts:261：删书清史失败 `console.warn` 绕过 logger，打包态失明
7. M-1 三审闸两处新端点零测试覆盖（并入 M-6）
8. desktop runBootstrap 无任何测试（Electron main 基建缺失，留账）
9. desktop/main.ts:714-730（预存）：before-quit 2s 窗口内 activate 可触发退出途重 bootstrap，建议 `if (shutdownStarted) return` 收口

**src/ai（6）**

1. orchestrate/chat/finish.ts:78：checkpoint 摘要 registerCtrl 缺 `'chat'` owner 分槽，与第八轮 M-1 修复口径分叉，两书同时压缩互相 abort
2. orchestrate/chat/turns.ts:49：chat 工具侧改写与 studio /rewrite 端点无互斥（AI_GEN_TOOLS 不查 task-gate），双 spill 后写赢
3. tools/rewrite.ts:126：selection trim 与端点 X-P2-13「raw 定位」口径分叉，注释声称同口径但定位串不同
4. orchestrate/chat/turns.ts:36-38：read_chapter 20k 截断与 spill 通知「调用 read_chapter 取回全文」承诺矛盾，超长章中段不可达
5. orchestrate/chat/state.ts:65-74：getHistory LRU 可逐出在途对话的书（>8 本并发使用才触发，留档即可）
6. tools/tree.ts:66-67：copyChapter 对无连字符章文件名产出 `章.md- 副本.md` 畸形名

**数据与基础层（5）**

1. process/materials.ts:206：RAG 无命中降级分支漏传 writingChapter（三处调用点唯一遗漏），L-P3 卷摘要晚一章注入
2. format/style-candidate.ts:319：查重 key 注释称 SOH 分隔防碰撞、实现无分隔符，当前词表无碰撞属潜伏隐患
3. format/style.ts:94、style-candidate.ts:128、style-entry.ts:134：readdir/stat 竞态裸 statSync 三处（leads.ts:277 与 cache/rebuild.ts:290 有修好先例可对齐）
4. document/service.ts:599：doMoveOrRename catch 块内 appendAborted 自身可抛穿透，违反 `{ok:false}` 契约
5. document/foreshadow.ts:227/264：伏笔 trail 以标题为 Map key，同标题互相覆盖

**前端（6）**

1. api/chat.ts:65：branchId 双重编码（手编 + params.toString() 再编），现字母数字无实害
2. WorkbenchView.vue:221-222：draftSaved 赋值在切书守卫之前，B 书工作台残留 A 的「已存 N 字」徽标
3. ChapterMetaDialog.vue:46 + documents.ts:324：章号不查整数，`3.5` 通过后文件名 `03.5-` 从章号特性中脱落
4. ForeshadowPanel.vue:92-101：create() await 后无书名复检，切书后可能顶开 B 书既有伏笔标签
5. TierSection.vue:37-51：超时分钟 `@input` 逐键校验，小数值几乎无法直接输入，改 `@change`/失焦
6. api/client.ts:30（修复残余）：外部 signal 的 once 监听器永不移除，当前无调用方传 signal

**测试与工程化（4）**

a. tree-perf.test.ts:100-102：性能断言口径连续第三次放松，快机器上防回退意义减弱（可改多次命中取中位数）
b. 38 处真实定时器固定等待（fire-and-forget 落定），极慢 CI 上可能漏等，整体可控
c. package.json files 不含 resources/：npm pack 形态缺 prompt/技巧包，CI 只验能打包不验内容（electron-builder 无此问题；npm 分发前需补）
d. CLAUDE.md 与实际脱节（评审文档落位描述不符；且被 .gitignore 忽略属本地私有文件）

## 三、留档不修（信息级）

第九轮 6 项留档经复核全部仍成立、未恶化：words-diary 无 fsync、分章导出 rm+重建窗口、rag/store.ts 迁移 TOCTOU、provider/store.ts mtimeMs 同刻盲区、scale.test.ts 绝对界值、files 缺 resources（本轮低-c 已收录）。本轮新增留档 2 项：events/chat-bridge.ts:170 批内 seq 连续性假设（单事务内成立，非 bug 但脆弱）；log 换 logsDir 后在途行的 dayFile(null) 边缘（fail-open 至多丢一行）。

## 四、重点核查确认无问题项（防复报）

- **服务端安全面**：Host/Origin/token 三重校验、static.ts 防穿越、SSE 计数与生命周期、删书/改名六闸+settle+drain+复查的 TOCTOU 收口、task-gate/spawn/review 三闸 NUL 分隔键与 release 幂等、事件库引用计数单例各调用点 try/finally 配对——全部闭环。
- **Electron 基线**：三窗统一工厂（contextIsolation+sandbox+nodeIntegration:false）、will-navigate/setWindowOpenHandler 拒绝、CSP 仅本地 origin、IPC 载荷净化、IPC 路径全走 resolveWithinRoot/realpath、优雅退出 2s 总超时。
- **src/ai**：runner 中断清理、记账防护、self-heal 单一 finally、三协议「截断 vs 无 usage」分流、脱敏三层、vault 双界守卫；chat 编排的 steer 续链/waitConfirm 幂等/msgSeqs 防御/sanitizeHistory/deadline；provider 层三协议 usage 合并与 400 降级链、registry 原子绑定、pricing 不跨 provider 借价。
- **数据层**：原子写、UTF-8 fatal 字节级探测、14 处 DatabaseSync finally 收口、check 的 H-1 拆分与纪元指纹、install 的 DA-3 读失败拒写与四重路径防线、四个 layout 迁移的幂等闸。
- **前端**：代守卫体系全线到位（reqGen/opGen/bookGen/compReqId 等 13 种）、await 前书名/docId 入口捕获 20+ 处复核到位、定时器/rAF/监听器配对清理、乐观锁链路（409 双出路/revision 回传/重同步）、纯展示组件零状态污染。
- **测试与工程化**：无恒真断言、无跳过债、mock 全部打在边界、tsconfig 真 strict（含 noUncheckedIndexedAccess 等）、CI 完整（2 OS × 2 Node + 全部门禁 + 独立 e2e job）、README 安全承诺有测试背书。

## 五、结论

**完成进度：约 97%，与 1.0.0-rc.0 相称。** 功能面无缺口——README 承诺的建书/设定/正文/全自动写章/三审/定稿/导出主链路及伏笔、字数曲线、文风、选段改写分析、对话助手配套全部落地且有 e2e。剩余约 3% 是工程收尾而非功能：webnext api 层覆盖（M-7）、若干注释留档的未接线项（catalog A7 seeder 等）、npm 分发形态（低-c）。

**完成质量：8.6 / 10。** 十轮迭代后，防御体系（闸、代守卫、哨兵口径、原子写、fail-closed 分层）已高度收敛，本轮 0 运行时高危；扣分集中在「最后一公里」：第九轮修复自身引入 1 处回归（M-1）且 6 项修复未配回归测试（M-6），对账门被打破（H-1）。这些是流程纪律问题而非架构问题，均可小改收口。

**建议动作排序：**

1. **提交前必修**：H-1（README 四处 2799→2800）+ M-1（migrateBookSession 强制关库一行修复）+ 补 M-1 回归测试——之后第九轮遗留的 11 文件方可入库
2. **中危批**：M-2（finalizedPathSet 哨兵对齐 M-13 口径）、M-3（spill 归属/新鲜度校验）、M-4（死实例书名守卫三处）、M-5（树排序数值比较）、M-6（六项补回归测试）——各配测试
3. **低危批**：按上方模块分组收敛（服务端口径类一起修、前端守卫类一起修）
4. **工程改进**：M-7（api 层单列覆盖桶）、低-a（性能断言改中位数口径）
5. 修完 1-2 后全量回归（tsc + vue-tsc + 2800 单测 + e2e + check:counts 五件套全绿），rc.0 → rc.1 可发
