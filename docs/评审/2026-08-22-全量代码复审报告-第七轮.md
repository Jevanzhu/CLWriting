# 全量代码复审报告 · 第七轮（独立复核轮）

- 日期：2026-08-22
- 基线：`6c3c72c`（dev，工作树干净）；门禁全绿：`npm test` 301 文件 / 2741 测试、`tsc`、`vue-tsc`、`check:counts` 301/2741、e2e 39 通过 / 2 跳过（沿用第六轮收口时基线，本轮只读评审未改代码）
- 性质：本轮为第六轮修复（`28cbaa4` 高危 2 + 中级 8 + 低级 7；`6c3c72c` 长尾 26）的**独立复核轮**——5 路并行评审代理不带预设重扫全库 + diff 级核实两批修复 + 层评分；主审对全部中级发现逐项读码亲核。

## 一、总评

**结论：8.6 / 10（第六轮 8.3 → +0.3）；完成度约 98%。**

- 第六轮两批共 34 项修复：**45 个核实点全部真实落地、无一回归**，其中 37 项配了针对性回归测试（质量高于历史轮次）。
- 新发现：**0 高危 / 12 中级 / 31 低级**；12 项中级全部经主审逐项亲核确认属实。
- 第六轮报告预测「修完回到 8.8+」，实际 8.6：差值几乎全部来自一个模式——**「修点未修类」**。第六轮每个修复主题（编排互斥、await 上下文捕获、UTF-8 字节防线、定稿口径收口）都存在同族兄弟调用点未横向收敛：前端 M-8 类只修清单 4 点而同文件近邻 3 个危险同级点原地未动（层分 8.7→8.4 唯一回落）；AI 互斥只修 chat→self-heal 单向；编码判据只升级 2 个写点中的 1 个；定稿口径在空集/近章复述两处残留旧语义。

## 二、方法

同第六轮：5 路并行 Explore 代理（AI 编排线 / 服务端+桌面壳 / 前端 web-next / 数据层 / 管线层），各自独立执行 ① 全量重扫找新缺陷（不预设前几轮结论）② `git show 28cbaa4 / 6c3c72c` diff 级逐项核实本层修复 ③ 层评分 /10；主审汇总后对 12 项中级逐一读码复核（`stream.ts`、`context-menu.ts`、`audit.ts`、`useChapterTreeActions.ts`、`workspace.ts`、`TrashPanel.vue`、`StyleEntryPanel.vue`、`service.ts`、`scaffold.ts`、`books.ts`、`prepare.ts`、`assemble.ts`）。

## 三、第六轮修复核实矩阵（45 个核实点）

判定口径：✅ 真实到位且无回归；⚠️ 到位但带注明事项。

### AI 编排线（9 项）

| # | 项 | 判定 | 依据摘要 |
|---|---|---|---|
| 1 | chat.ts openSessionStore 收降级（H-1） | ✅ | try/catch 降级 notice 继续对话；锁/队列 finally 清；真实损坏 db 回归测试锁三件事（完成、锁归 false、可重启） |
| 2 | anthropic 截断流分流（H-2） | ✅ | 有 stopReason 无 usage → done；纯截断 → retryable NETWORK 真进重试链；与另两协议线终止契约一致 |
| 3 | 三工具 self-heal 互斥（M-1） | ✅ | AI_GEN_TOOLS 注册表分派层单闸覆盖三件；测试锁文案/无副作用/空闲放行。⚠️ 反向闸缺失见新缺陷 AI-1 |
| 4 | tool_use 缺 id 兜底 | ✅ | `block.id \|\| toolu_${index}`（块级唯一） |
| 5 | compactionSuppressed 上移 + LRU/清库同步 | ✅ | 逐出与 clear 两路径同步 delete；清空+逐出回归测试 |
| 6 | RunState.usage 收敛 | ✅ | 只剩 {outputTokens, cost}，死字段全删，TS 结构类型保证无残留读者 |
| 7 | driver dispose 不复活 Map | ✅ | push 短路 + stream 早退 + interrupt 不懒建；双 driver 泄漏测试锁计数恒定 |
| 8 | chapter_status 定稿口径 | ✅ | 第 4 参传 finalizedChapterNumbers；SQL IN 限定；「缓存草稿不计入」测试 |
| 9 | TaskOk.model 透出 | ✅ | 类型强制全构造点覆盖（rewrite.test 桩补 null 即编译期证据）；summary 两处落真实模型 |

### 服务端 + 桌面壳（10 项）

| # | 项 | 判定 | 依据摘要 |
|---|---|---|---|
| 1 | chat/clear 补三闸（M-2） | ✅ | 五闸与 audit DELETE 同口径；真实占闸 409 + 空闲 200 两侧测试 |
| 2 | PUT /file 乐观锁（M-3） | ✅ | GET 附指纹 / 不符 409 / 缺省后写为准；三态集成测试 |
| 3 | rag 结构化 code | ✅ | code:'BUSY'\|'BAD_INPUT' 映射状态码，文案子串判定已删 |
| 4 | audit 先切片再投影 | ✅ | events 两路（对话+工作流）均 pageSlice 后 map。⚠️ visible 双数组未分页见新缺陷 SV-2 |
| 5 | iter.return().catch | ✅ | SSE 断连防线补齐 |
| 6 | purgeTrash best-effort | ✅ | manifest 写回收 try 吞错；EACCES 故障注入三段测试 |
| 7 | books.prefs.put 合并写 | ✅ | {...disk, ...prefs} 自定义键存活测试 |
| 8 | clearBooks 单事务双键 | ✅ | BEGIN 内循环两键 DELETE；chat 侧同步接线。⚠️ 缺直接单测（回滚路径仅间接覆盖） |
| 9 | onboard 5 万字符上限 | ✅ | premise/discussionContext 双字段 400 + 放行用例 |
| 10 | accelerator 白名单 | ✅ | 正则无灾难回溯；6 样例剥除断言 |

### 前端 web-next（8 项）

| # | 项 | 判定 | 依据摘要 |
|---|---|---|---|
| 1 | handleClear 书名双闸（M-8） | ✅ | 弹窗后 + 请求在途后各查一次；2 条回归测试——本批覆盖最完整 |
| 2 | pendingInsert 三口补消费 | ✅ 代码 / ⚠️ 测试 | watch immediate + onMounted + open 后 nextTick 三口齐全；无消费回归测试 |
| 3 | MetaFormPanel.onSave 入口捕获 | ✅ 代码 / ⚠️ 测试 | 双条件守卫 refresh、单条件守卫 toast；无组件测试 |
| 4 | HistoryPanel.onRestore 捕获 | ✅ 代码 / ⚠️ 测试 | baselineRevision 用入口快照；无测试 |
| 5 | ForeshadowPanel.openFile 捕获 + toast | ✅ 代码 / ⚠️ 测试 | 到位；同缺陷类 3 个兄弟调用点未同步（新缺陷 FE-5） |
| 6 | Library/Welcome onMounted 失败态 | ✅ 代码 / ⚠️ 测试 | loadError + 重试按钮；无测试 |
| 7 | CheckPanel ui.ask 替换 confirm | ✅ 代码 / ⚠️ 测试 | 全库已无 window.confirm 残留；无单测 |
| 8 | toast 分级时长 | ✅ | error 5s / 其余 1.8s；专门回归测试 |

小结：8 项代码本体全部落地；测试覆盖 2/8——下轮回归护栏不足是本层扣分主因之一。

### 数据层（10 项）

| # | 项 | 判定 | 依据摘要 |
|---|---|---|---|
| 1 | executeSave FFFD+字节探测（M-5） | ✅ | 「原始字节一字不动」双向测试；置于 journal pending 之前零副作用 |
| 2 | updateChapterMeta 字节级判据 | ✅ | fm 区 GBK 盲区消除、合法 UTF-8 真 � 放行。⚠️ 同款漏了 updateDocMeta（新缺陷 DA-1） |
| 3 | lead-finalize 残留写回 + 注记（M-6） | ✅ | 主文件/归档两分支 + 修复后重试幂等闭环测试 |
| 4 | relative() 反斜杠归一（M-4） | ✅ | check/run、prepare、api/style 收口。⚠️ 任务单所列 revision.ts 从无 relative() 调用（归属笔误，非漏修）；现存漏点仅 settings.ts:234 展示用途 |
| 5 | doInit 半成品恢复 | ✅ | 三场景测试（可续/有内容仍拒/已登记冲突不变）。⚠️ 「幂等 scaffold」前提有洞（新缺陷 DA-2） |
| 6 | readBooks/readActive 读失败降级 | ✅ | 不裸抛测试。⚠️ 降级空表 × 后续整写 = 清库窗口（新缺陷 DA-3） |
| 7 | probeCache 4096 FIFO + bigint | ✅ | mtimeNs+size 双指纹；同长改写不吃缓存测试。⚠️ FIFO 上限本身无直接测试 |
| 8 | YAML 列表项先于冒号 + rag 缺省 | ✅ | 含冒号列表项入 listItems；rag 有实质键即收 |
| 9 | porcelain 路径归一 | ✅ | 引号解转义 + rename 箭头；空格/非 ASCII 两测。⚠️ R 行归一分支无测试 |
| 10 | finalizedChapterNumbers 上移 | ✅ | manifest.ts 单源；四端接线 grep 复核无遗漏 |

### 管线层（8 项）

| # | 项 | 判定 | 依据摘要 |
|---|---|---|---|
| 1 | M-7 卷摘要自愈可达 | ✅ | 无 sourceHash 永不动 / 有且过期重生成双向测试 |
| 2 | injectedSummaryFiles 随段登记 | ✅ | 极小预算 → 清单空且段确实没了。⚠️ 降档路径虚报（新缺陷 PL-3） |
| 3 | assembleStatus 定稿口径四端收口 | ✅ | 四端接线齐全。⚠️ 空集回落全量（新缺陷 PL-2） |
| 4 | overview 热力图只算定稿 | ✅ | 真 HTTP 端到端：草稿同日 mtime 不计数 |
| 5 | 样章库 readdir 显式 sort | ✅ | 先写 002 再写 001 断言顺序 |
| 6 | book-search symlink 防越界 | ✅ | 目录+文件双形态测试。⚠️ 书内 symlink 环无防护（PL-4） |
| 7 | 投稿清当前平台旧产物 | ✅ | 改名清旧 generic、他平台产物留存测试 |
| 8 | 三检引文读取守卫 | ✅ | chmod 000 实测：不崩、落黄、不误报 miss |

## 四、新缺陷（0 高危 / 12 中级 / 31 低级）

### 中级（12 项，全部经主审读码确认）

| 编号 | 层 | 位置 | 问题 | 修法 |
|---|---|---|---|---|
| AI-1 | AI 编排 | `api/stream.ts:252/329/349` | `/auto-write` 与 `/spawn` 均不查 `isChatRunning`——M-1 只修了 chat→self-heal 单向，反向缺口完整：chat 改写在途时可并发启动 self-heal（两路 runTask 不同章号互覆预算章块），且后到的 register 触发 cc「换新先 abort 旧」静默掐断在途对话 | auto-write 两处检查（含二次检查）与 spawn 入口补 chat 闸 409；配并发窗口回归测试 |
| SV-1 | 服务端 | `desktop/context-menu.ts:39` | submenu 净化递归无深度上限——被攻陷渲染进程可经结构化克隆构造数万层嵌套（不受 JSON.parse 深度限制），RangeError 栈溢出崩主进程，恰是该文件头宣称的威胁模型 | 净化加深度参数，超 5 层剥 submenu（对齐 accelerator 白名单降级思路） |
| SV-2 | 服务端 | `api/audit.ts:125-129` | modelVisible/humanVisible 仍每次全量投影出网（每节点 200 字 preview），与「长书几万事件不再一次全量进 HTTP 响应」注释契约漂移 | 两数组同 pageSlice（或按 seq 窗口投影） |
| FE-1 | 前端 | `useChapterTreeActions.ts:362` | doDelete 的 `await ui.ask` 后重读 `deps.bookName()` 再删——legacy docId 纯路径派生不分书，弹窗滞留跨窗切书后 A 书确认会命中 B 书同路径文件（错书删除，进回收站可救） | 入口捕获书名，确认后不匹配即中止（镜像 M-8） |
| FE-2 | 前端 | `TrashPanel.vue:44` | purge 同款：await 后 `props.bookName` 取请求时刻值，永久删除请求可能发到切换后的书 | 同上 |
| FE-3 | 前端 | `StyleEntryPanel.vue:60` + `stores/style.ts:84` | onRemove 同款：`style.remove(path)` 在 store 内取调用时刻书名，两书同路径条目互串或空书名裸抛 | ask 前捕获书名 |
| FE-4 | 前端 | `stores/workspace.ts:66` | setBook 不清 `pendingInsert`——非编辑器视图点「插入」→ 切书 → 新书编辑器三口消费把 A 书设定名插进 B 书正文 | setBook 内 `pendingInsert.value = null` |
| DA-1 | 数据层 | `document/service.ts:525` | updateDocMeta 仍是旧「字符串含 U+FFFD」判据——第六轮只升级了 updateChapterMeta，同族 fm 区 GBK 盲区残留（卷纲/总纲写回路径） | 改 `isUtf8Bytes(readFileSync(abs))` 同口径 |
| DA-2 | 数据层 | `install/init.ts:70` + `scaffold.ts:89-218` | 半成品恢复的「幂等 scaffold」前提部分不成立：占位 .md（大纲/总纲/卷纲/设定/文风/简介 8 处）无条件覆盖写，addEntry 无去重——复跑即覆盖未登记书已积累的真实内容、禁词条目翻倍 | 占位 .md 存在即跳过；预置禁词按类型+正文去重 |
| DA-3 | 数据层 | `install/books.ts:61` | readBooks 读失败降级空表后，appendBook/writeBooks 会以仅含新书的单行覆写 books.jsonl（EACCES 挡读不挡 tmp+rename 写）——其余登记全丢（repairBooks 可重建兜底，非永久丢） | readBooks 区分缺文件（[]）与读失败（null），写方 null 时拒绝重写 |
| PL-1 | 管线 | `process/prepare.ts:244` | 前章正文结尾选章 off-by-one：定稿口径下 `prevChapterNo = currentChapter - 1` 使写第 N 章拿到 N-2 原文（N-2 同时吃摘要+原文、真正的前章 N-1 只有转述）——第六轮口径收口后错位由「重写场景偶发」变为确定性 | `prevChapterNo = snapshot.currentChapter`；同步改 prepare.test 两组夹具 |
| PL-2 | 管线 | `process/assemble.ts:82` | 空定稿集（清单在册、零定稿的新书）回落「全量含草稿」MAX(number)——与修复语义自相矛盾；测试把空集钉成「夹具兼容」混淆了「无清单」与「清单无定稿」 | 区分 undefined（全量）与空集（currentChapter=0） |

### 低级（31 项）

**AI 编排线（5）**：① `chat/state.ts:89` clearChatHistory 裸调 openSessionStore（H-1 同型残留，清史半完成态 500）② `chat/turns.ts:39` M-1 注释漂移（lead_update 不传 chapter、无章记账，注释称三工具均按章记账）③ `provider/models.ts:88` listModels OpenAI 线无 404/405 宽降（兼容网关误报「连通失败」）④ `chat/turns.ts:411` 超时 abort 也回「作者取消了该操作」（归因误导）⑤ cc.ts/mock.ts 文件尾缺换行。

**服务端 + 桌面（4）**：① `desktop/main.ts:692` before-quit 的 workDir 回读与 bootstrap 实际发现脱钩（孤儿会话只能靠 10 分钟宽限）② `main.ts:645` whenReady 回调无 .catch（绕过 runBootstrap 错误通道）③ `api/prefs.ts:86/142` JSON.parse(disk) 未验形状（数组/标量 prefs 损坏扩散一轮）④ clearBooks 回滚路径无直接单测。

**前端（6）**：① SearchPanel/ContextQuickPanel/useRelationGraph 三处 `catch {}` 静默吞打开失败（Foreshadow 修复未横向收敛）② MetaFormPanel 死 CSS ~70 行 ③ ui-store.test.ts 头注释仍写「toast 1.8s」 ④ Library/Welcome 的 chooseLibrary/switchTo IPC await 无捕获（交互路径 unhandled rejection）⑤ WorkbenchView onSpawn/onAutoWrite await 后重读书名（亚秒级跨书漂移窗）⑥ doc.ts save 在途切书 updateWordCount 可能写脏新书同路径节点（树刷新即校正）。

**数据层（8）**：① `service.ts:349` maybeSnapshot 对 restore 目标不查 existsSync（ENOENT 误报 WRITE_ERROR）② `init.ts:70` bookRoot 为同名文件时 readdirSync 裸抛 ENOTDIR ③ porcelain ` -> ` indexOf 首匹配可被文件名字面箭头误切 ④ `layout.ts:22` piece-body 幽灵枚举 + 前端三处注释失实 ⑤ words-diary 两处读无守卫（端点直接 500）⑥ safe-path `..` 前缀误杀 `..foo.md` 类文件名（fail-closed 方向安全）⑦ `api/settings.ts:234` relative() 无反斜杠归一（仅展示）⑧ doCopy 对非 UTF-8 源复制出乱码副本（原件无损）。

**管线层（8）**：① 近章结尾降档（slice(-1)）后 summaryFiles 仍登记两章（「可见⟺记录」红线降档漏网）② book-search 书内 symlink 环无限递归栈溢出（无 visited 集）③ generateVolumeSummary 无 inFlight 去重（并发窗口重复 AI 调用）④ recentChapters 近章节奏未按 finalized 过滤（复述含在写草稿钩子）⑤ check/run.ts 双遍全书扫描 + 「一次预扫两处共用」注释漂移 ⑥ state.ts:482 孤儿 doc 注释 ⑦ summary.ts:392 错误文案空 join 拼出「缺第 章摘要」 ⑧ export/index.ts 平台 label 后缀误判（书名恰以「-公众号」结尾时 generic 槽位旧产物永不清）。

### 留档不修（沿用第六轮口径）

SQLite busy_timeout（单进程单连接）、SSE token 走 URL query（本地回环 + 一次性 token，可接受）、flushSyncOnUnload、编辑器 4×O(n) 扫描（n=单文件行数）。TOKEN_COEFFICIENTS 仍为空待真实用量校准（`scripts/calibrate-tokens.ts`，期间 0.6 兜底）。

## 五、层评分与差距归因

| 层 | 第六轮 | 第七轮 | 变化 | 主因 |
|---|---|---|---|---|
| AI 编排线 | 8.4 | **8.7** | +0.3 | 两高危根修兑现 + 长尾纪律高；反向互斥缺口（AI-1）封顶 |
| 服务端+桌面 | 8.6 | **8.7** | +0.1 | 10 项全兑现无回归；submenu 深度 + audit 半分页两中级残留 |
| 前端 web-next | 8.7 | **8.4** | **-0.3** | 唯一回落：M-8 类修点不修类（3 个危险同级点原地未动）+ 修复测试覆盖 2/8 + pendingInsert 跨书污染 |
| 数据层 | 8.4 | **8.8** | +0.4 | M-5/半成品两大主扣分项高质量根修；编码防线 1/2 写点 + scaffold 前提两残口 |
| 管线层 | 8.5 | **8.6** | +0.1 | 8 项全兑现；但定稿口径收口自身留 off-by-one 与空集回落两处相邻缺口 |
| **总体** | **8.3** | **8.6** | +0.3 | 预测 8.8+ 差 0.2 ≈ 前端 -0.3 回落 + 管线两口径残余 |

归因一句话：**修复本身全部兑现且无回归；失分全部来自「同类未横向收敛」**——第六轮按清单修点，本轮独立重扫证明每个缺陷类都有清单外的兄弟调用点。这也是本轮最有价值的产出：下一轮修复的单位从「项」升级为「类」。

## 六、结论与建议

1. **第六轮修复质量得到独立确认**：45 核实点全落地、零回归、37 项带回归测试——「修 + 锁」纪律成立。
2. **建议第八轮修复按类收口**，优先级：
   - **P1**（互斥/崩溃面）：AI-1 编排互斥矩阵补全、SV-1 submenu 深度上限；
   - **P2**（跨书数据面）：FE-1/2/3/4「await 上下文捕获」类横向收敛（含 WorkbenchView 两处低级同族）+ pendingInsert；
   - **P3**（数据层三件）：DA-1 编码判据对齐、DA-2 scaffold 幂等前提、DA-3 读失败清库窗口；
   - **P4**（口径残余）：PL-1 前章 off-by-one、PL-2 空集语义、SV-2 audit 半分页；
   - **P5**：31 项低级随后清偿。
3. 按本轮归因推算，类级收口完成后预期 **8.9~9.0**。

## 七、第八轮修复收口（2026-08-21）

§四全部 43 项（12 中级 + 31 低级）按 §六优先级修完，每项配回归测试；门禁全绿。

### P1 互斥/崩溃面（2 中级）

- **AI-1**：`/spawn` 与 `/auto-write` 双向补 `isChatRunning` 闸（auto-write 首查 + 快照竞态二查）→ 新增 `test/studio/orchestrator-mutex-gates.test.ts`（3 用例）。
- **SV-1**：右键菜单递归深度上限（`MAX_SUBMENU_DEPTH=5` + `parseContextMenuSpecs` depth 参数）→ `context-menu.test.ts` +1（深层嵌套截断）。

### P2 跨书数据面（4 中级）

- **FE-1/2/3**：doDelete / TrashPanel purge / StyleEntryPanel onRemove 统一「await 前捕获 bookName、await 后守卫」——M-8 类横向收敛 → `composables.test.ts` +2。
- **FE-4**：`setBook` 清 `pendingInsert`（跨书插入污染）→ `workspace.test.ts` +1。
- **FE-9**：WorkbenchView `onSpawn`/`onAutoWrite` 同款捕获守卫（§四低级同族随类收敛）。

### P3 数据层三件（3 中级）

- **DA-1**：`updateDocMeta` 编码判据对齐 save 主路径（isUtf8Bytes fatal 探测，盘上非 UTF-8 即拒）→ `service.test.ts` +2。
- **DA-2**：scaffold 幂等前提补齐（`writeIfAbsent` 占位不覆写 + book.yaml/文档清单条件写 + 禁词预设按现存去重）→ 新增 `test/install/scaffold-idempotent.test.ts`（3 用例）。
- **DA-3**：books.jsonl 读失败不再误清库（`readBooksStrict`：读失败返 null → append 拒绝并给修复指引、remove 跳过、rename 流同步收口）→ `books-guard.test.ts` +2（chmod 000 实测）。

### P4 口径残余（3 中级）

- **PL-1**：前章 = currentChapter（最后定稿章），写作第 N 章拿 N-1 原文 → `prepare.test.ts` fixture 149→150。
- **PL-2**：`finalizedChapterSetOfBook` 哨兵语义（清单缺失 → undefined 全扫兼容旧夹具；清单在册空集 → 真口径 0 章不回落草稿计数）→ `pipeline-lowlevel.test.ts` 空集断言 + `summary.test.ts` fixture 对齐。
- **SV-2**：audit 翻页 `offset>0` 不再重发 modelVisible/humanVisible 全量 diff 数据（首页才是全量、后续页前端本就弃用）→ `audit.test.ts` +1。

### P5 低级 31 项（AI / 服务端 / 前端 / 数据层 / 管线五批清偿）

- **AI**：OpenAI 404/405 → 模型列表返空不误报（新增 `models-openai-404.test.ts` 3 用例）；清史 openSessionStore 失败降级 warn 继续跑（`chat-open-store-fail.test.ts` +1）；确认超时与作者取消文案/语义区分（`chat-abort.test.ts` +1，`confirmTimedOut` 标记）；driver 产物尾换行补齐。
- **服务端/桌面**：prefs 合并写磁盘 JSON 形状校验（数组/标量视作空，`prefs-merge-onboard-cap.test.ts` +1）；desktop `before-quit` 用 bootstrap 记录的 workDir 关停内嵌 server（书库切换后不再关错目录）+ whenReady 链失败显式退出。
- **前端**：SearchPanel err 状态、ContextQuickPanel/useRelationGraph 失败 toast、Library/Welcome 载入失败分支、doc store save 跨书守卫（`doc.test.ts` +1）、MetaFormPanel 死 CSS 清理、doc.ts piece-body 注释修正。
- **数据层（8 项全修，6 回归测试）**：maybeSnapshot restore-to-missing 跳过快照正常落盘（`service.test.ts` +1）；doCopy 原始字节复制（GBK 不经 utf-8 往返转写，`service-struct.test.ts` +1 字节级断言）；init 同名文件占位给可读原因（`init-half-scaffold.test.ts` +1）；porcelain 仅 R 状态切「 -> 」（`migrate-finalized-revision.test.ts` +1 字面箭头文件名不误建基线）；safe-path 段级 `..` 判定（`safe-path.test.ts` +1，`..foo.md` 放行、`../`、`..\` 仍拦）；words-diary 读失败返 null（新增 `words-diary-read-fail.test.ts`）；layout piece-body 注释、settings 反斜杠归一。
- **管线（8 项全修）**：① 近章结尾降档同步收缩 summaryFiles（`degradedSummaryFiles` 机制，`prepare.test.ts` +1）② walkMd realpath visited 剪枝防书内 symlink 环（`pipeline-lowlevel.test.ts` +1）③ generateVolumeSummary inFlight 去重（新增 `summary-volume-inflight.test.ts`：并发只放行一个、键释放后可 skipped）④ recentChapters 按定稿线过滤（`assemble.test.ts` +1，在写草稿钩子不进近章节奏）⑤ check/run 正文双遍扫描合并（预扫列表传入 `maxWrittenChapterOf`，锚测试 3→2）⑥ state.ts 孤儿注释删除 ⑦ 卷摘要错误文案空 join 兜底 ⑧ export 平台槽位归属改「当前书名+平台后缀」精确名判定（`export.test.ts` +1 书名以 label 结尾场景）。

### 口径变更明示（2 处既有断言随行更新）

- **export 槽位清理**：他平台「旧书名」残留从「保留」改为「一并清」——拿错稿风险同源；各平台「当前书名」产物仍精确保护（多平台视图不互删的本意不变）。
- **tree-issues-scan-count 锚**：readChapterDir 全书调用次数 3→2（双扫合并后「一次预扫两处共用」注释与实现终于一致）。

### 门禁与对账

vitest **306 文件 / 2774 单测**全绿（301/2741 → +5 文件 +33 测试）；`tsc --noEmit`、`vue-tsc`、`check:counts`、e2e（39 过 + 2 跳过）全过；README 数字已对账。本轮修复单位即「类」——43 项全按类横向收敛，§六建议落地。
