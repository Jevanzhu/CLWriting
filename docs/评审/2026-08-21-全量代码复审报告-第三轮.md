# CLWriting 全量代码复审报告（第三轮）

- **日期**：2026-08-21
- **评审基线**：`dev` 分支 `f876e31` + 工作区未提交改动（72 文件 +1287/-318，即 8-20 两轮评审的全部修复 + 4 个未跟踪新测试文件）。HEAD 领先 origin/dev 20 个提交（未推送）。
- **方法**：六路并行代码走查（AI 编排计费 / 缓存机检 RAG / 服务端桌面 / 数据安全 / 写作管线 / Vue 前端）+ 测试体系独立评估 + 本机全量验证门禁实测 + 高危发现由主评审逐步坐实。其中两路（数据安全层、写作管线层）走查以「核实点全覆盖 + 核心文件通读」方式由主评审亲自完成，非全量逐行。
- **与上版报告的关系**：本报告是《2026-08-20-全量代码复审报告》（97% / 8.8 分）之后的第三轮复审。核心问题：上版声称「两轮评审缺陷当日全部收口、已达交付级」——本轮独立重查验证该结论成色，并重新挑错。

---

## 一、总评

| 维度 | 结论 |
|---|---|
| 完成度 | **约 97% 维持**——设计方案四方向九批次全部兑现（本轮抽验 A1/A3/C1/D1/B1 落地均实）；上版两轮评审的全部修复（8 中级 + 1 高危 + 5 前移项 + 低级清单）经本轮逐项核实**确已落地且带回归测试**；剩余为外部依赖动作（推送、提交、真实书语料、系数回填、发布管线） |
| 质量分 | **8.4 / 10**（上版自评 8.8，本轮独立重查后下调）——门禁全绿、数据安全纪律与测试治理仍是长板；但本轮新发现 **1 条高危 + 12 条中级**上版两轮均未捕获的缺陷，集中在三处系统性模式：缓存跨章输入漏失、成本口径的协议维度分裂、settle/竞态守卫「抓大放小」未铺满 |
| 交付判断 | **功能完备、门禁全绿；「已达交付级」需加限定**——建议先修本轮高危与头部中级（均为局部可修，不动摇架构）、提交并推送工作区，再进发布管线。无数据损坏面、无安全绕过面 |

规模盘点：后端 TypeScript strict 约 4.1 万行（23 个模块域）+ Vue 3 前端约 3.1 万行 + 测试 5.2 万行；Electron 桌面壳 + node:sqlite（事件 / RAG / 缓存三库）+ JSON/YAML 配置；AI 侧三协议适配器统一走 runTask 编排。

## 二、验证门禁实测（全部本机跑通，2026-08-21）

| 门禁 | 结果 | 与声称对照 |
|---|---|---|
| `tsc --noEmit` | 零错误 | 一致 |
| `vue-tsc --noEmit` | 零错误 | 一致 |
| `npm test` | **287 文件 / 2650 单测全绿**（35.4s） | 一致 |
| `check:counts` | 通过（287/2650；28 spec/41 用例） | 一致 |
| `npm run test:e2e` | **39 过 / 2 跳 / 0 败**（25.0s；跳过为 release-smoke 环境门控） | 一致 |
| `npm run test:coverage` | 退出码 0（per-glob 两桶阈值门全过）；聚合 lines 80.71 / branches 82.92 / functions 89.94 | 一致 |
| `npm run check:knowledge` | 通过（知识层 13 条 manifest 与磁盘一致） | 一致 |

## 三、上版修复核实（逐项读码验证，不信注释）

上版报告第六、十、十一节全部修复项，本轮按域分派核实，结论：**全部落地**，其中两项为「主体修复、留有残余面」（残余面计入第四节新发现）：

| 上版修复项 | 核实结论 | 证据（当前工作区） |
|---|---|---|
| H-1 缓存投毒（失败不落缓存） | ✅ 已修复 | run.ts:284-297 `checkFailed` 置位 + 落缓存闸门；rebuildFailed 时 db=null 双保险；回归测试在 tree-issues-scan-count.test.ts |
| #1 candidate_depth 透传 | ✅ 已修复 | materials.ts:203 召回点显式透传；rag/index.ts:408 消费 `?? 20`；enableRag 保留键（rag/config.ts:144,156） |
| #2 effectiveRemainingCalls 反转 | ✅ 已修复 | calls.ts:267-274 `limit<=0 return 0` + `!check.ok return 0` |
| #3 用量卡片切书残留 | ✅ 已修复 | WbUsageCard.vue:29-59 load() 抽取 + loadGen + watch(bookName) + catch 清残留 |
| #4 补丁白名单漏键 | ✅ 已修复 | yaml.ts CONFIG_PATCH_LEAVES 补 tokens_per_chapter/cost_per_chapter/summary.auto/candidate_depth；stringify 侧 rag 段条件输出 candidate_depth（两路口径一致） |
| #5 纪元漏输入 | ✅ 已修复 | tree-issues-cache.ts:92-93 细纲.md + 境界体系.md 入纪元 |
| #6 final 章读放大 | ✅ 已修复 | run.ts:21,250-255 deriveStatus + probeCachedPublished；probe 按 (mtimeMs,size) 缓存（tree.ts:229-259），published 二次请求零整读有测试锚 |
| #7 改名/删书竞态（settling） | ⚠️ **部分修复** | 主体严密：settling 登记与 running 同步前缀完成、覆盖 finally 全部清理链、cleanup 换链守卫、消费方接线正确（books.ts:214/333、graceful-shutdown）、chat-settle.test.ts 锁三态。**残余三窗**见第四节 M-3/M-4/M-5 |
| #8 applyBudgetTrim 漏 model | ✅ 已修复 | prepare.ts:414,430-432,443 三处透传；materials.ts 五处 resolveTier model 上游齐备 |
| H-1′ learn 定稿过滤 | ✅ 已修复 | learn/index.ts:24,125-134 finalizedPathSet（与导出同源）+ skippedDrafts 计数 + 全草稿报错 |
| H-2 StyleView 跨书写坏 | ✅ 已修复 | Book.vue:103 `:key="bookName"`（同模板 Overview/Learn 等同样挂 key） |
| M-1 误报标记持久化 | ✅ 已修复 | check.ts:33-50 `clw-fp:书:文档` 分桶 + run 回填 + clear 清理 + 配额异常静默降级 |
| M-2/M-3 reqGen 守卫 | ✅ 已修复 | style.ts:46-67 / learn.ts:29-50 load·harvest 守卫 + clear bump |
| summary.auto 死分支 | ✅ 已修复 | yaml.ts sectionsToConfig 字符串比较归一（parseValue 不产出布尔） |
| migrate-defaults 纯净判定 | ✅ 已修复 | migrate-defaults.ts:98 `candidate_depth === undefined` |
| safePath 统一 | ⚠️ **部分修复** | 六大调用面（service/files/trash/desktop×3/style/books×2/safeManifestPath）确认统一委托 resolveWithinRoot；**残留第五套**：format/style-candidate.ts:164,185 手写穿越检查（无 symlink realpath），见 M-7 |
| events 孤儿 TTL 惰性重跑 | ✅ 已修复 | store.ts:189-238 三写路径入口挂 maybeRepairOrphans（10 分钟 TTL），store.test.ts 假时钟覆盖 |
| SSE/日志脱敏 | ✅ 已修复 | http.ts:55-59 urlPathOnly，三处错误日志出口全覆盖；全库 grep 无其他 req.url 落点 |
| localDayKey 统一 | ✅ 已修复 | log/index.ts:57 + cost-stats.ts:72 + trace-stats.ts:45 |
| self-heal cost 四档分计 | ✅ 已修复 | self-heal.ts:764-778,823-824（小瑕疵：生成完成后重解析模型，跑批中途换模型有轻微口径漂移） |
| 批 7 低级六项（孤儿时机/mock pre 上限/leak 引号/DDL 单源/空锚点/harvest 基准） | ✅ 全部修复 | harvest-corpus.ts:84,95-101 Map 索引 + pinned 锚定；short-index.ts:367-371 return false；等 |

## 四、新发现缺陷清单（本轮独立挑错；均经代码证据核实，高危项经主评审三步坐实）

### 高危（1 条）

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| **H-1** | `src/check/leads.ts:51-60,91-107` + `src/check/tree-issues-cache.ts:83-98` + `src/check/run.ts` | **树红点章级缓存漏「跨章输入」**：账本三检的 lead-evidence-miss 对每条履历 `chapterTextOf(entry.章号)` 直读 写作/正文 **任意章**当前正文，其红项进入**每一章**的 report 与 hasRed（checkWithDb 逐章调用、不按被检章过滤）；而全局纪元指纹刻意不含 写作/正文（「改 1 章只破那 1 章行指纹」） | 作者改第 100 章正文补上引文 → 只有第 100 章行指纹失效重查变绿，**其余全部章缓存红点陈旧**（假红残留）；反向编辑则**漏红**。违反缓存核心不变量「缓存命中 = 与全量重算逐字节等价」（tree-issues-cache.ts:5）。**缓解面**：定稿防吃书闸读实时数据不受影响；下次纪元输入变化（定稿/改配置等）连坐清表自愈；陈旧仅表现于树红点显示层。附带同类（次要）：futureBaselineChapter 经 maxWrittenChapterOf 吃全书 fm 章号，外部改最高定稿章 fm 章号同样陈旧 |

### 中级（12 条）

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| M-1 | `src/ai/pricing.ts:68-80` + `calls.ts:217-251` + `openai-adapter.ts:237-245` | **OpenAI 线 cacheRead 系统性双计**：provider/types.ts:247-263 自己声明两协议 inputTokens 口径不同（OpenAI prompt_tokens 已含 cached；Anthropic 不含）且「勿混算成本」，但 computeCallCost / checkAiCallBudget 的 token 口径均按 Anthropic 不相交公式混算 | OpenAI 系网关（缓存命中率高的长上下文写作场景）下：cost-stats、done 事件 cost、ai-calls.json、cost/token 预算闸全部系统性高估（输入侧接近 2 倍），作者被提前熔断；新测试恰好按 Anthropic 语义断言掩盖了分裂 |
| M-2 | `src/ai/orchestrate/self-heal.ts:585`（exitPass）+ `documents.ts:174,207`（summary 同类） | **fire-and-forget 任务逃逸 settle**：账本推进草稿 / 章摘要生成挂在 `void` 上，不属于 settling promise；runSelfHeal 返回后 running 已清 | 正常完成后立即删书/改名：闸看不见在途任务 → 不 abort 不等待 → straggler 对已删/搬走的 bookRoot 重建孤儿目录（工作区/账本推进.md 等）+ 白烧 API 费。批量连写窗口跨章累积 |
| M-3 | `src/ai/orchestrate/chat.ts:92-99,135-164` | **abort 后 drainNextChat 拉起新 run 逃出 settling**：waitChatSettled 只捕获当时 的 promise；abort 落在 chat_done 之后 + 恰有排队消息时，straggler finally 里续链新 run 不被等待 | 窄窗下改名继续执行，新 run 对旧路径写库/写事件（孤儿目录 + 事件失联）；books.ts「等待把这一窗收敛为零」注释与实际不符 |
| M-4 | `src/studio/server/api/books.ts:191-214,312-333` | **409 闸与 settle 等待之间的 TOCTOU**：闸检查全部在 `await awaitOrchestrationsSettled`（最长 10s）之前，等待期间新编排可启动，恢复后不复查直接 rmSync/renameSync | 等待窗内新起的编排持旧 bookRoot 闭包，收尾在已删/搬路径重建孤儿目录 |
| M-5 | `src/studio/server/api/schema.ts:52-62` | **parse 失败分支缺 `return`**：BAD_INPUT 400 已发出后，handler 仍以 `input=undefined` 执行 | 当前三个 parse 路由恰好立即解引用 → TypeError 被 dispatch 兜底成日志（噪音 + 多余副作用，如 chat.send 先建会话再炸）；未来 handler 若不立即解引用即成真漏洞。违反「400 后不执行业务」契约 |
| M-6 | `src/studio/server/api/check.ts:109-116` + `self-heal.ts:168-176` / `runner.ts:176-185` mkChain | **store.close 不在 finally**：openSessionStore 成功（refs++）后 workspaceSession 抛错被吞，引用永不释放 | 长驻进程中 SQLite 连接被永久 pin 住；documents.ts 与 rule-hits/author-signal 已修同款，此处漏网 |
| M-7 | `src/format/style-candidate.ts:164,185` | **safePath 第五套平行实现残留**：confirmCandidate/ignoreCandidate 手写穿越检查，无 symlink 双侧 realpath、无 fail-closed | 候选目录内放指向外部的 symlink 可穿透读外部文件内容入库（需本地 fs 写权限，可利用性低）；上版「统一全部调用面」声明漏此一处 |
| M-8 | `src/desktop/server-main.ts:49-53` | **SIGINT/SIGTERM 的 server.close 回调对 SSE 长连接永不触发**，且无超时兜底（desktop main.ts 有 2s race，此入口没有） | 编译产物 server 态 Ctrl+C 挂起；不中断在途编排 |
| M-9 | `src/check/run.ts:192-204` + `src/cache/rebuild.ts:266-270` | **collectTreeIssues 硬异常 fail-closed 500**：rebuild 抛出 / DatabaseSync 打开失败 / PRAGMA 失败均无 catch，穿透到 router 兜底 500 | index.db 损坏或跨进程锁超 5s 时树红点端点整体 500（前端树不可用），与缓存层声明的 fail-open 红线（「读写失败跳过缓存走全量」）冲突 |
| M-10 | `web-next/components/panels/TrashPanel.vue:24-27,58` | **回收站列表切书竞态无守卫**（watch bookName 重拉无请求代）；残留条目上的恢复/彻底删除以 B 书名 + A 条目 id 调用 | 显示错位；id 跨书碰撞时存在误删写风险（通常服务端 404 兜底）。同目录 Search/HistoryPanel 均已有守卫，此面板漏 |
| M-11 | `web-next/views/WorkbenchView.vue:56-63` + `stores/style.ts:100-114`（rescan/harvest）+ `stores/learn.ts:74-95`（commit） | **次级 action 竞态守卫未铺满**（约 8 处同模式残留）：loadRuleHits 无代守卫、style.rescan 趋势跨书错位、learn.commit 在途回填清掉新书用户勾选 | 展示态错位为主（A 书数据挂 B 书界面）；learn.commit 直接吞用户输入；写路径被乐观锁/fail-closed 第二道闸拦住，未破跨书写坏红线 |
| M-12 | `web-next/stores/workbench.ts:132-140` | **clear() 不复位 running**：切书后 B 工作台显示「中断」并禁用生成按钮，靠 SSE sync 事件自愈 | SSE 断连重连期间按钮长时间错锁 |

### 低级（摘要，共约 15 条）

- `rag/config.ts:40-60`：candidate_depth 无下界校验——配 0 时 `verdict.size(0)>=0` 恒 break，召回恒空静默降级为「无 RAG」无告警；建议读侧钳制。
- `check/tree-issues-cache.ts:34-41`：纪元指纹无同毫秒写缓冲（rebuild 基准有 `Math.ceil+1`，纪元没有）——理论性窗口。
- `rag/index.ts:197-220` + `store.ts:226-229`：零块章指纹键永久残留，破坏「指纹集合 == 已索引章集合」不变量（当前无害）。
- `document/tree.ts:262-268`：probe 的 published 解析与 parseFlat 口径在引号值上不一致（`已发布: "true"` 两链路判断分裂；注释宣称同口径不实）。
- `ai/tasks/spec.ts:94-99`：ctrlFromSignal 的 abort listener 不摘除（forwardAbort 已修同款 ee-P1-2，此处漏），长对话累积 listener。
- `ai/runner.ts:333-409`：记账调用无 IO 异常防护——磁盘满时裸异常可穿 {ok:false} 封套且同次调用可能双计。
- `self-heal.ts:761-828`：done 事件 usage 不含重试消耗，与 ai-calls.json 口径分裂（一致地低估）。
- `chat/restore.ts:71` + `finish.ts:117-124`：msgSeqs 防御性重置后与 history 恒错位，trim 遮蔽失准可致「幽灵消息」回归（低频防御路径）。
- `desktop/preload.ts:61-67`：showContextMenu 两次连续弹菜单时 stale once listener 误触发。
- `api/stream.ts:470-480`：chat/clear 不走 resolveBook（书不存在回 200 非 404，契约漂移）；与 pendingChats 存在窄窗。
- `api/review.ts:50-53`：isReviewRunningForBook 用 `book+'/'` 前缀匹配，name 含 `/` 时误判持闸（task-gate 已用 NUL 修过同型）。
- `books.ts:239`：DELETE 只清行不删 `<hash>.db` 文件，长期残留空库文件。
- **测试门禁**：check-counts.mjs 对 `test.only(` 计数如常（Playwright 无 CI 下 allowOnly 兜底，`.only` 可静默缩水覆盖面且对账仍过）；覆盖率两桶漏网 `web-next/src/main.ts`、`router.ts`（include 内但不匹配任何桶）；e2e 隔离靠约定而非机制（共享可变 workDir，edit-save 写 0001 不恢复）。
- **结构性盲区**（测试体系评估）：SQLite 多进程并发（CLI + 桌面同开一书）无 WAL/busy 处理无测试；Windows 平台零 CI（对中文网文桌面应用是现实风险）；cache 库 schema 演进无回归锚；删/改书 10s 超时降级分支无测试。

## 五、分层质量评分（本轮各路走查归并）

| 层 | 分 | 评语 |
|---|---|---|
| src/document + format + install + export + state + git + log（数据安全层） | 9 | 定稿写入顺序推演（回写先于基线，ee-P1-4 永久丢失窗口论证）、原子写地基（临时文件+fsync+0600+目录 fsync）、版本 pinned 永久 + 指纹缓存防误吞留底、回收站基线随软删——全库最扛事故的模块，本轮核实四修复点 + 核心文件通读未发现新缺陷 |
| 测试体系 | 8 | 断言质量显著高于均值（计数不变量/round-trip 等价/界值/调用次数回归锚，0 snapshot，无假绿样本）；门禁链完整真接 CI。扣分：e2e 隔离靠约定、覆盖率桶漏网、`.only` 盲区、多进程并发与 Windows 两结构性盲区 |
| src/studio/server + events + desktop + fs | 8 | 三层安全防线与 README 对齐、事务纪律扎实、fail-closed 一致；扣分：schema.ts 缺 return 硬伤、settle 三个残余窗、safePath 第五套残留、两处引用计数泄漏 |
| src/cache + check + rag | 8 | 事务与降级纪律上乘、五项上版修复全部真实落地；扣分：跨章输入漏失击穿缓存等价性声明（H-1）、fail-closed 500 与红线冲突、边角校验缺失。修掉 H-1 可到 9 |
| 前端 web-next | 8 | 竞态守卫成体系（reqGen/opGen 三件套）、写路径纵深防御（乐观锁 + fail-closed + key 重建）、零 XSS、零监听泄漏；扣分：守卫模式「抓大放小」——panels 层与次级 action 约 8 处未守卫残留，缺一条制度化约定 |
| src/process + driver + learn + knowledge + metrics（管线层） | 8.5 | 摘要金字塔双挂点、场景三级水源回退设计干净、六核实点全过；draft-pipeline 通读无新缺陷 |
| src/ai（编排/适配器/记账） | 7.5 | 编排成熟度、中断/重试/降级链完整度在水准之上；扣分：cacheRead 双计是三口径体系的协议维度裂缝（M-1）、settle 残余窗（M-2/M-3）、记账 IO 防护与 listener 泄漏等已修模式漏拷贝 |

## 六、完成进度核算（设计方案 → 实码）

**迭代方向批 0-8（阶段 18）**：九批次全部兑现，本轮抽验均有实码：A1 树红点缓存（probeCache + 双指纹 + 纪元）、A3 惰性校验 + 预存范数（DDL/ALTER/回填幂等）、C1-C3 摘要金字塔（summary.ts 双挂点 + 链指纹 fail-closed + 细纲卷进展注入）、D1 用量卡片（WbUsageCard + e2e spec）、D2+D3 价格表与预算双口径、B1-B4 语料回归域（误报按钮 → 事件 → harvest → commit → 回归门）；批 8 worker 化经 P3 基准实测关闭（决策有据）。**预留位如实**：`TOKEN_COEFFICIENTS` 空表待实测回填（prepare.ts:108-111，管线已通）；语料回归门种子仅 body-parts.json（2 条），待真实存量书跑 `corpus:harvest` 充填。

**阶段 1-17（历史计划）**：按总览文档均已收口（事件溯源 F1 全家 / 三审 / RAG 接线 / Responses 协议 / 服务提供方管理 / 各轮清偿批），本轮未重复核验，以文档链 + 门禁绿为准。

**发布管线就绪度**：CI 双工作流（ci.yml 四腿矩阵 + desktop.yml mac 打包）门禁链完整；electron-builder 配置齐（asar + resources/ 发布资产门）；「win/linux 暂缓、仅 mac」为既有拍板。编译产物 server 态冒烟已过；Electron 打包壳 GUI 态冒烟随发布动作。

**剩余待办（全部为动作，非开发）**：① 提交工作区 72 文件修复 + 4 个未跟踪文件；② 推送 dev（领先 origin 20 提交）；③ 真实书跑语料收割；④ 校准脚本回填 token 系数；⑤ 发布前 Electron GUI 冒烟 + 切 tag。

## 七、行动清单（按优先级）

1. **立即：提交并推送当前工作区**——昨日全部修复 + 今日评审报告只存在于工作区，dev 领先 origin 20 提交，单点丢失风险是当前最大工程风险（8-19 曾发生工作区覆写事故）。
2. **修 H-1（树红点缓存跨章陈旧）**：推荐把「全书账本三检」从章级缓存拆出、全书单次计算单独缓存（保住「改 1 章只重查 1 章」）；或接受 写作/正文 stat 入纪元（牺牲增量性换正确性，需权衡明示）。当前实现两头都没占。
3. **修 M-1（OpenAI 线 cacheRead 双计）**：computeCallCost / checkAiCallBudget 按协议感知扣减，或 toUsage 侧统一成 Anthropic 不相交口径；补 OpenAI 语义的成本断言测试。
4. **修 M-5（schema.ts 缺 return）**：一行修复 + 「400 后不执行业务」契约测试。
5. **settle 残余三窗收口**（M-2/M-3/M-4）：fire-and-forget 任务纳入 settling 或登记在途；abortChat 置停摆标志；等待后复查闸。
6. **前端守卫铺满**（M-10/M-11/M-12）：TrashPanel/loadRuleHits/style.rescan/learn.commit/workbench.running 补代守卫——建议同时立一条 lint/评审约定「watch bookName 必带代守卫」把模式制度化。
7. **M-6/M-7/M-8/M-9 按序清偿**；低级清单可随下批顺手（candidate_depth 钳制、close 入 finally、probeCache 书隔离等）。
8. **测试门禁补强**：check-counts 拦 `.only`；覆盖率桶收编 main.ts/router.ts；e2e 0001 写入恢复。
9. 中期：Windows CI 腿（中文网文作者主力平台）、SQLite 多进程并发策略（WAL/busy 处理）、cache 库 schema 演进回归锚。

## 八、结论

上版报告「97% / 8.8、已达交付级」的判断**成色核实为：完成度属实，质量分偏高**。工程基本面无可挑剔——全套门禁本轮实测全绿（287 文件/2650 单测/e2e 39 过/覆盖率两桶/check:counts/check:knowledge），上版两轮的全部修复经逐项读码确认真实落地且带回归锚，数据安全层与测试治理仍是同类项目中的长板。

但本轮独立重查证明上版「缺陷全部收口」的结论下早了：**新发现 1 条高危（缓存跨章陈旧，违反缓存层自己声明的核心不变量）+ 12 条中级**，且集中在三个系统性模式——① 缓存失效输入的枚举缺「跨章消费」视角；② 成本口径统一了「四档」却分裂于「协议」维度（types.ts 自己的警告没被 pricing 层消费）；③ 竞态守卫与 settle 机制抓大头、留尾巴（fire-and-forget、次级 action、TOCTOU 等待窗）。这三类都不是孤立 bug 而是模式覆盖面问题，解释了为何两轮评审（分别从「五域功能」和「编译器视角」走查）都漏掉。

**最终评定：完成度约 97%，质量 8.4 / 10，功能完备、无数据损坏面、无安全绕过面；修完 H-1 与头部中级（均为局部可修）、完成提交推送后，可进发布管线。**「正式版」前的硬清单：提交推送 → H-1/M-1 修复 → 真实书语料 + 系数回填 → Electron GUI 冒烟 → 切 tag。

---

> **评审方法说明**：六路并行走查中四路由独立评审代理完成（AI 层 / 缓存机检 RAG / 服务端桌面 / 前端 / 测试体系，其中两路因账号速率限制由主评审以「核实点全覆盖 + 核心文件通读」顶替：管线层 6 核实点 + draft-pipeline 全文、数据安全层 4 核实点 + finalize/atomic/version/trash 通读）；所有门禁数字为本机实测；高危发现 H-1 由主评审按「输入读取（leads.ts chapterTextOf）→ 结果归章（run.ts checkWithDb）→ 失效输入（tree-issues-cache 纪元枚举）」三步坐实，缓解面（定稿闸实时读、纪元变化自愈）一并核实。评审阶段严格只读，未改动任何源码。
