# 项目指引（治理正本）

本文件自包含、不指向具体 Dev/Docs 文件名；入口文档按职责角色提及，经 `Dev/Docs/README.md` 解析实际文件名，按需读取、不默认通读。本文件是规则正本，不是记录——不写批号、日期、沿革（正本 = git 历史）。

## 恒常纪律

- 子 agent 派发上限：单波 ≤4（在途 ≤4），超量拆多波顺序派发。
- 纪律条落点：作者一次性纪律指令只写本文件正本，不另落他处；常规批记档照旧。
- 冲突优先级：总览 > README；任何文档操作不得只改一处；历史正文不改写（只改索引/状态/路径行）。
- 入库判据：以 `.gitignore` 实文为准——不在忽略名单即入库；`AGENTS.md` 为本文件的指针实体文件（内容唯指向本文件）。

## 入口（按需读，角色 → 用时经 README 解析文件名）

- **README**（`Dev/Docs/README.md`，目录/命名/计数）：做文档操作前读。
- **总览**：开放任务/顺序/决策/待拍板（设计文档只做展示，不纳入计划）；领任务、改状态时读所需节，勿通读。

## 文档操作链

- **分工**：README 只记目录+计数；总览只记开放任务+现行口径+待拍板（已收口记录不入）。正本唯一，他处只留指针。
- **新文档**：`主题-类型[-状态]-YYYY-MM-DD.md` → 入 `01-评审 / 02-执行 / 03-设计 / 04-调研` → 同步 README 计数 + 总览文档地图（+实施顺序/决策表如需）。
- **评审命名**：评审报告名须含执行模型名：`主题-评审-<执行模型>-YYYY-MM-DD.md`，报告头部同步记「执行模型」行。仅约束新档，既有档不追改。
- **开工/完成**：改总览状态行；完成时决策由待拍板→已拍板回改方案，方案归位 02-执行（可开工）或 03-设计（暂缓/现行规范标「现行」），留验收证据。
- **评审**：报告落 `01-评审/`；报告完成≠收口——P1/P2 修复+回归通过才收口；收口后归档上一轮入 Archive（扁平）；未收口不归档。
- **归档/精简**：只归已收口/失效档；同步总览地图移出 + README 计数；未收口评审、未拍板决策、唯一计划、现行规范不归档。删档直删不 zip，删前核验 git 复原锚（锚树在位+与工作树零差异），锚号记入 commit message；埋在待删内容里的未了结事项先单立开放项再删。
- **记档**：总览行 = 一句结论 + 指针（报告正本/批记 commit），不装实施明细——明细随 commit message 自考；已收口行随批移出（行全文 = git 复原锚）；README 不设变更记录节。
- **篇幅纪律**：机器门 `npm run check:docs`（CI 实挂）按两面卡——索引面（本文件 / `Dev/Docs/README.md` / 总览）只写结论 + 指针，不挂批号、修订日期、沿革；介绍面（根 `README.md`）只对外介绍，禁项目进展，仅放行 check:counts 依赖的口径数字。细则与判定 = `scripts/check-docs.mjs`（直测 `test/scripts/check-docs.test.ts`）。
- **校验**：文件树与索引一致、相对链接 0 断链、计数一致；内容移位后全链引用随批改指，不留悬空指向。

## 测试分层

- **L0 零测试面**：纯文档/注释/运行时用户数据/纯类型面——不跑测试，回复中声明改动面。
- **L1 相关面（日常默认）**：`npm run test:related -- <改动文件>` + `npm run typecheck`（注意：路径写错 vitest 会静默空跑退出 0）；动前端加 `npm run typecheck:web-next`；纯测试改动只跑被改文件 + typecheck。
- **L2 终门（收口/提交前必跑一次）**：vitest 全量 + tsc/vue-tsc + eslint + 四 check + e2e；coverage 由 CI 阈值门兜底（CI 单腿）；收口记写实测数字。
- **测试命名**：新测试文件按被测行为命名，批次号只留文件头注释与源码锚注。

## AI 链路开发守则

- **模型可见 ⟺ 已记录**：注入 prompt 的一切内容必须可回溯到事件记录。
- **默认值显式 resolve**：链路参数逐层显式 resolve（或显式声明 fallback），保证重放可精确重建。
