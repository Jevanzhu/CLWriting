# 全量代码复审与进度质量评估二十八轮（评审报告）

- 日期：2026-08-30（链第八十轮）
- 基线：HEAD `e86f4b9`〔dev，与二十五轮评审同基线提交〕+ 工作树 **157 文件改（+4187/−720，其中实质改动 +4032/−565）+ 31 个未跟踪新文件**（二十五轮 R77 / 二十六轮 R26-xx / 二十七轮 R27-xx 三批**已收口但未提交**的修复整体在树）——工作树现状整体纳入评审面。
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，告诉我项目完成进度，完成质量，最后形成一个文档给我。记得编排下任务。」
- **本轮不含修复**；修复计划见 §六（批 A-E）待作者指令。
- **确认闸记**：报告路径 `Dev/Main/01-评审/全量代码复审与进度质量评估二十八轮-评审报告-2026-08-30.md` 经 AskUserQuestion 报作者，**获批**（作者选「标准路径落盘」）。

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审方法与覆盖口径

- **独立性铁律**：六路分域禁读子代理（prompt 明令禁读 `Dev/` 下任何 .md 与一切评审报告，防锚定）；主评审未读任何历史评审报告正本（仅读 README/总览/01-评审 README 索引与报告头注状态行做链态定谳，及总览第三节核对任务状态）。
- **域划分与覆盖**（src 全量 ≈8.8 万行 + test 盘点）：
  - A = src/ai + rag + knowledge + learn + driver（89 文件 ≈1.47 万行，逐文件 + 未提交 diff 17 文件 + 未跟踪测试 6 个全读）
  - B = src/check + format + review + events + cache + metrics（55 文件 ≈1.36 万行，主干逐文件 + 未提交 diff 逐行 + 新测试逐断言）
  - C = src/document + fs + state + export + git + process + install + log（59 文件 ≈1.37 万行，保存链/锁/原子写/迁移高风险面逐行 + 未提交 diff 逐文件）
  - D = src/studio/server + src/desktop（68 文件 ≈1.22 万行，路由/任务闸/安全面 + 未提交 diff 21 文件逐个）
  - E = src/studio/web-next（190 文件 ≈3.68 万行，逻辑层逐文件 + 高风险组件定向 + 未提交 diff 19 文件全读）
  - F = test/（607 文件盘点 + 抽样深读 + **全部 27 个新增未跟踪测试逐行**）+ scripts/ + 根配置（package/playwright/eslint/tsup/electron-builder/CI 工作流）
- **子代理事故记档**：C 域首发子代理 17:48 撞账户速率限制 **1302 阵亡**，错峰重发成活——六域结论齐备（链内惯常形态，如实记档）。
- **主评审亲验**：P2×5 逐条亲读源码/实测复现（§五）；B 域 1 条 P2 经 node 实测**证伪排除**；B-7 注释漂移抽验属实；门禁八件套亲跑（§二）。
- **本轮覆盖重点**：三批在树未提交修复（R77 + R26 + R27，跨三轮、含 25 个新增测试文件）此前**未经任何独立评审**——本轮六域对其 100% 逐 diff 审阅（见 §五.4）。

## 二、门禁实证（主评审亲跑，2026-08-30）

| # | 门 | 结果 |
|---|---|---|
| ① | tsc --noEmit | ✅ 0 错 |
| ② | vue-tsc（web-next） | ❌ **红 1**：`test/studio/webnext/use-shelf-delete.test.ts(115,15) TS2554: Expected 2-3 arguments, but got 1`（=R28-1） |
| ③ | eslint . | ✅ 0 错 |
| ④ | vitest run | ✅ **567 文件 / 4467 单测一次全绿**（122.5s，零观察项） |
| ⑤ | check:counts | ✅ 实测 = README 声称（567/4467、29 e2e spec/45 用例） |
| ⑥ | check:packaging | ✅ |
| ⑦ | check:knowledge | ✅ 13 条 manifest 双向对账一致 |
| ⑧ | e2e（build:web + Playwright） | ✅ **43 过 2 跳**（26.2s 零噪音，共享 workDir 顺序契约无红） |

- **七绿一红**：红点系 r27 修复批自带测试文件少参（非产品源码），vitest 不做类型检查故单测照绿——但类型门是合入门槛，**树现状不可直接提交**（见 R28-1）。
- 单测/用例/e2e 计数与根 README 声称**零漂移**。

## 三、完成进度评估

### 总体：**≈97%**（核心 mac 生产形态 **≈98%**；win RC 形态 **≈95%**）

依据：

1. **实施路线 23 个阶段：21 个已完成/收口**（阶段 0-12、14-20、22、23——总览第三节逐行核对）。开放仅 2 项，且均**非代码阻塞**：
   - **阶段 13（Responses 中转）**：代码面 100% 在位（`scripts/verify-responses-relay.ts` 本轮实证在位；encrypted 中段链路 7 用例直测 `test/ai/provider/responses-encrypted.test.ts` 在位——二十五轮 R77-5 已补），仅余**作者网关凭据的真机验证**（外部依赖，维持）。
   - **阶段 21（win 适配）**：J0-J4/J7 已完成；**J5 体验面大件全部在树**（本轮逐项实证：菜单栏隐藏 bb9c344 / 无框 WCO+避让 2d9f402 / window-state.ts 多屏 bounds 校验 R26-86 已接线 main.ts / 字体 IPC 缓存 font-cache.ts R77-1 / git ENOENT 人话引导+坚果云冲突副本特征 / >260 长路径冒烟 / rename EPERM 重试）；**J6 披露面在位**（根 README「Windows 版使用须知」SmartScreen/无签名说明，unsigned 系 D4 拍板），签名/自动更新按决策延后；CI=PR→main + workflow_dispatch（push 腿频推期摘除、注释留恢复锚）。
2. **六域完成度**：A 92%（AI 链路——残项为零参工具契约漂移等边缘面）/ B 95% / C 95% / D 95% / E 95% / F 95%。
3. **收敛趋势**：r26 清偿 111 项、r27 清偿 60 项后，本轮全库从零复审 P2 面仅 5 条（且全部为修复批自身边缘回归，非结构性缺口）——二十六轮 20 → 二十七轮 10 → 本轮 5，单调收敛。
4. **距离 RC 终点的残项清单**（全部为外部依赖/发布决策，非代码）：① 阶段 13 真机验证（待凭据）；② win 签名与更新通道（D4 决策延后）；③ CI push 腿恢复时点（频推期结束，作者拍板）；④ **工作树三轮修复批未提交**（157+31 文件，含 25 个测试新文件未入库——提交面滞后于收口状态，建议尽快落提交）。

### 分阶段摘要（详情以总览第三节为准）

| 阶段 | 状态 |
|---|---|
| 0-12 / 14-20 / 22 / 23 | 已完成/已收口（21 个） |
| 13 Responses 启用批 | 开放（外部依赖）——代码面本轮实证全在位 |
| 21 win 适配 | 已开工——J0-J5/J7 完成（J5 大件本轮逐项实证在树）、J6 披露在位/签名延后 |

## 四、完成质量评估

### 总体：**8.7 / 10**（分域 A 9.0 / B 8.5 / C 9.0 / D 9.0 / E 8.5 / F 9.0）

**亮点（加分面）**：

- **保存链五要素纪律**（跨进程 save 锁 + journal pending/settled + 基线校验 + 快照 + 锁内复核）经 27 轮迭代加固至偏执级，C 域判定「同类项目罕见水准」；本轮对三批未提交修复逐 diff 复核，其锁纪律/原子写/fail-closed 取向全部成立。
- **安全面**：回环 + Host/Origin + 双 token + ticket 四闸、密钥脱敏单出口（replyError 统一过 redactSecret、console 镜像过 maskKeys）、CSP、窗口工厂安全标志后置——D 域判定高质量，未发现 P1/P2。
- **注释文化**：本轮「注释认账排除」命中 ≈19 处认账在案（取舍如实登记）；注释与代码失实仅 3 处（R28-3/R28-12/R28-17 均为注释级）。
- **测试基建**：567/4467 带对照臂/反证臂文化；27 个新增测试逐行审阅全部有效；spec 顺序门 + 零参 skip 检出 + pageerror 升红门等治理门持续在位；e2e 零噪音。

**扣分主线**：

- **修复边际回归**：五条 P2 **全部**位于未提交修复批自身（R26-43 正则放宽、R26-81 cancel 时机、R27-79 键名错、r27 测试少参、R27-43 收编漏段）——高频修复轮次下「修复引入回归」的边际成本上升，且 vue-tsc 类型门现存红 1（r27 批终态未经 web-next 类型门复跑或在终跑后又有改动）。核心稳定面（保存链/事件库/服务端安全/测试基建）连续十一轮 P1×0，新缺陷密度集中在最近两批修复的边缘面。
- **未提交窗口过长**：三轮修复（含 25 个测试文件）堆在树上未经独立评审合入，本轮评审基线被迫含 +4187/−720 在途面——建议后续批次收口即提交，缩短在树窗口。

## 五、发现清单

### 1. P1×0（连续十一轮，自十七轮起）

### 2. P2×5（全部主评审亲验属实）

**R28-1 | F/E 域 | test/studio/webnext/use-shelf-delete.test.ts:115**
`const e = new ApiError('not found')` 少参——构造器签名为 `(message, status, code?)`（client.ts:14）。vue-tsc 红（TS2554），**类型门被 r27 批自带测试打破**；vitest 不做类型检查故单测照绿（门禁盲区实证）。修法：`new ApiError('not found', 404, 'NOT_FOUND')` 并删 116-117 行手工赋值；顺带在批 A 复跑 vue-tsc 复绿。

**R28-2 | B 域 | src/check/count.ts:764**
`/^##\s*.+$/gm` 把 `###`/`####` 子标题误计为节标题——node 实测：`### 手记` 命中该正则、旧正则 `/^##\s.+$/` 不命中（R26-43 放宽 `\s`→`\s*` 时未排除更深 `#` 前缀）。后果：节数虚高 → `section-count` 假黄（:786 `sections !== expected`）；短篇 strict 清单（runner.ts:299-305 含 `section-count`）**提红拦定稿**。新测试 r27-batch-b 只测围栏/紧排形态未覆盖此边界。修法：`/^##(?!#)\s*.+$/gm`（紧排 `##标题` 仍计、裸 `##` 仍不计，R26-43 语义不变）；补 `###` 混排短篇 strict 断言回归。

**R28-3 | E 域 | src/studio/web-next/src/composables/useShelf.ts:220**
删书清残留键 `` `clw2.tree-first-open:${name}` ``（**冒号**）vs 写入方 ChapterTreePanel.vue:84 `'clw2.tree-first-open.'`（**点号**）——grep 实锤。R26-74 写下的真实首开标记删书时**永远删不掉**：同名重建书 `consumeFirstOpen` 读到旧标记返回 false，永不套章节树默认展开（R27-79 修复目标落空一半；onboard-premise 键是对齐的，仅此键错）。:214 注释自认「键名对齐 TREE_FIRST_OPEN_KEY」与代码不符；回归测试 use-shelf-delete.test.ts:95 用同一冒号键**自证通过**（测试与实现同错互相掩蔽）。修法：改点号 + 测试改用 ChapterTreePanel 导出常量拼键（单一事实源）。

**R28-4 | E 域 | src/studio/web-next/src/api/client.ts:117**
401/403 分支**无条件** `r.body?.cancel()` 后，:119 token 未变/为 null 的不重放路径把**已废响应**返回调用方 → apiJson `await r.json()` 必抛 TypeError → catch 落 `hasEnvelope=false` → 服务端真实错误信封（403 Origin/权限类，本可解析并提示真因）被伪造成「本地服务未连接 / LOCAL_API_DOWN」误导排查方向；与 :112「透传不空转」注释矛盾，系对修复前行为（信封可透传）的回归。R26-81 的连接池动机成立，仅 cancel 时机错。修法：cancel 移入确定重放分支（token 比对后、递归调用前）；e2e-client-reboot 补「不重放路径 body 可读」断言。

**R28-5 | C 域 | src/document/service.ts:333 / 353 / 369**
executeSave 临界段 try{…}finally{docSaveLock()} **无外层 catch**：锁内 R76-22 复核链 `lookupPathByDocId`（legacy docId 收编 → upsertManifestEntry → withManifestLock 2×5s 超时 **throw**）、`computeRevision` / `readFileSync`（win 杀软 EBUSY/EACCES 读盘错）任一抛出 → queue reject → `save()` 变 rejected promise / API 500，**而非契约承诺的 WRITE_ERROR 信封**（manifest.ts:231 R73-33 注释明确宣称「executeSave 内 catch → WRITE_ERROR」，对该段不实）；R27-43 只收编了取锁前同款调用（:279-287，注释自认「前段」），锁内同款漏修——亲验结构实锤。触发窄（legacy docId + 他进程持清单锁 >10s，或读盘错），发生在 appendPending 之前**无数据损伤**。修法：333-371 段同款 SaveResult 收编。

### 3. 证伪排除 ×1（子代理上报、主评审实测推翻）

- B 域上报「iron-rules.ts:48 `(\d*\.?\d+%?)` 可空匹配 → `对话标签占比: 待定` 得 0 阈值全量误报」——**node 实测证伪**：捕获组末段 `\d+` 强制至少一位数字，非数字值整体不匹配（返回 null），不存在空捕获→parseRatio('') 路径；R26-39 无此缺陷，维持原状。

### 4. 未提交三批修复（R77/R26/R27）独立复核结论

六域对 157 改 + 31 新**逐 diff 审阅**：**除上述 R28-1~R28-5 五条外全部成立**——修复方向正确、与库内防御口径一致、注释声称与代码行为零失实（其余面）、无隐藏半成品/断链残件；25 个新增测试全部有效（断言带对照臂）。三批可随批 A 修复后一并提交。

### 5. P3×27（子代理上报、主评审抽验；修复批落地时逐项验证）

| 编号 | 域/出处 | 摘要 |
|---|---|---|
| R28-6 | A · gen.ts:285 | 零参工具 + 尾随长文本撞 length 上限时完整合法调用被误判截断抛 MAX_TOKENS——现网不可达（零参 schema 仅 chat agent 域、turns.ts:538 整体拒收不经 generateTool），纯契约漂移；建议 contract 层断言锁「零参工具不得走 generateTool」 |
| R28-7 | A · learn/commit.ts:83 | commitDeduped 指纹不含技法指令：同批同场景同正文不同指令的第二条静默去重丢失；建议命中时 warn 或标记 dedup |
| R28-8 | B · count.ts:213 | DIALOGUE_TAG_RE 动词集窄于引语归属集（骂/吼/喝/斥/嘀咕等缺席），对话标签占比系统性偏低（漏检向黄，不拦闸） |
| R28-9 | B · count.ts:757 | 孤立闭合 ``` 围栏（无开栏）会把 inFence 翻真吞掉其后全部 `##` 标题 → 节数少计误红（罕见输入） |
| R28-10 | B · check/lead-updates.ts:96 | R26-32 warn 误伤 `---` 分隔线与嵌套子列表行（文案误导，方向不变） |
| R28-11 | B · format/frontmatter.ts:218 | R27-26 块标量分支行号提示失真（指向块后而非重复键起始行） |
| R28-12 | B · review/contract.ts:144 + run.ts:102 | 注释仍写「有 config.short」，实装为 `kind === 'short'`（review.ts:176）——亲验属实，纯注释漂移 |
| R28-13 | C · document/service.ts:681 | R26-51 快照二次读盘：同函数 R27-45（:642-653）刚把两读收敛单次并注「同源派生」，此处口径不一 + 改名/移动不持 save 锁的两读窗口可致「覆盖前留底」存档错内容（错档非丢失）；修法直喂 fileBytes（Buffer 已被 R26-52 支持） |
| R28-14 | C · process/summary.ts:206/236 | 摘要锁命名 `.摘要锁-章N` 不以 `.lock` 结尾，sweepAbandonedTmpFiles 的 `endsWith('.lock')` 清扫分支永不命中——崩溃残留锁文件永久堆积（可 stale 接管不永锁）；命名补 `.lock` |
| R28-15 | C · fs/cross-process-lock.ts:247 | 等待为同步 Atomics.wait（封顶 5s）；基建注释前提「争用为毫秒级 IO」对 R26-19 摘要锁调用点（持有方为 10 分钟级 AI 调用）不成立——GUI+CLI 双开时第二进程同步冻结事件循环整 5s（Electron 主进程 IPC 冻结）；建议异步化或缩首等档 |
| R28-16 | C · export/index.ts:417 | 「有定稿章 N 章」文案用正文区全部章数，finalizedPaths 兜底/跳草稿时计数失真（文案级误导） |
| R28-17 | C · document/version.ts:249 | 注释称字节档「不去重每次落写」，指纹缓存路径对 Buffer 同样去重——行为无害，注释口径不实 |
| R28-18 | C · test/fs/long-path-smoke.test.ts:30,59 | 裸 mkdtempSync（未走 mkdtempTracked），断言失败即泄漏 $TMPDIR——与同批 R26-107 惯例相悖 |
| R28-19 | D · desktop/server-boot.ts:75 | 端口校验只查 finite：`--port -1/65536/78.5` 落 `server.listen()` 同步抛 RangeError 绕开 boot-error 信封（桌面链路恒合法数字，实害限手工脚本）；补 isInteger + 0..65535 |
| R28-20 | D · desktop/window-state.ts:33 | 畸形 bounds（如 `{"bounds":1}`）解构抛 TypeError——现被 main.ts 外层 catch 兜住无崩溃，纯函数契约宜自守 |
| R28-21 | D · desktop/server-manager.ts:607 | SIGKILL 升级前 pid 已捕获：2s 等待窗内子进程死亡且 pid 被复用可误杀无关进程（极窄理论窗）；升级前重读 proc.pid |
| R28-22 | D · server/api/rag.ts:108 | rebuild 先清库再后台建：建索引期 embed 失败（网络/配额）→ 旧索引已删新索引未成，recall 归零直至重建成功；非数据丢失（可重建），建议前端明示「索引已清空」状态 |
| R28-23 | E · useChatComposer.ts:16 | 文件头注释仍称「ChatPanel 挂 :key 全量重建」，与 R27-75 新注释及 WorkbenchView.vue:326 实状矛盾（文档性瑕疵） |
| R28-24 | E · pages/Book.vue:61 | 「拒绝 → 原书状态原封」对 F1 路径过度承诺（workbench.clear() 在 :96 已提前执行）；注释收窄为「不重复清」 |
| R28-25 | E · StyleCandidateBox.vue:36 等 | 书名守卫读 style.bookName 依赖 load 入口同步置位：路由重建 → setup→load 之间一渲染 tick 窗口内死实例动作 settle 时守卫放行，A 书 toast 落 B 书界面（窗口极窄） |
| R28-26 | E · SettingsBookAnalysis.vue:283 | 1.5s 轮询无重叠去重：单次 refresh 慢于周期时 ragFailStreak 可被重叠调用连加，提前进失败终态（终态有手动重试出路，影响轻微） |
| R28-27 | F · e2e/spec-order.guard.test.ts:30 + check-counts.mjs:213 | 守卫用默认 `.sort()`（码元序），Playwright 实际收集序为 localeCompare——当前 29 个纯小写名两序恰同未假绿；混大小写/标点新 spec 时快照序可与真实执行序分叉而守卫照绿；守卫改用 localeCompare 镜像 |
| R28-28 | F · check-counts.mjs:216-246 ↔ snapshot.txt | 同一顺序契约双真相源（mjs 硬编码数组 + txt），增删 spec 须改两处（漏改由门红兜底非失效）；mjs 改读 txt 单一真相源 |
| R28-29 | F · ci.yml:3-11 | 触发仅 PR→main + dispatch，push 腿频推期摘除（注释留恢复锚）——恢复时点待作者拍板 |
| R28-30 | F · eslint.config.js:10,75 | lint 射程 = src/**/*.ts + 根 JS；test/**（567 文件）与 scripts/*.ts、.vue 明示不接——未用变量/死 mock 无机器检；增量扩射程 |
| R28-31 | F · scripts/check-knowledge.ts 末行 | 缺末行换行（纯格式） |
| R28-32 | F · README/e2e-ports | 「29 spec 共享单一临时 workDir」措辞过宽：usage-card 等 4 spec 各持独立 server+workDir（文档口径，非缺陷） |

### 6. 注释认账登记（不修类，≈19 处，源码注释原文在案）

- A 域 2：runner.ts:583 重试分支 recordUsageSafe(null)（现网无可重试带 usage 形态）/ turns.ts:562 事件 usage 双口径（现网两值恒等，r27-batch-f 已注）。
- B 域 3：realms.ts R26-40 前邻连接语素误拒回落黄项 / count.ts R26-29 意象阈值口径统一 / leads.ts R27-21 词表外动词红降黄（并集击穿理由成立）。
- C 域 6：linkOrRenameExclusive 降级窄窗竞态（atomic.ts:197「宁窄窗回归旧语义」）/ 降级 renameSync 无重试 / R26-51 快照 fail-open「留底是兜底不是闸」/ doTrash 重试耗尽孤儿条目无害自证 / Y-5 symlink 窗口不扩面（R27-42 明示）/ 非 NTFS 卷降级 warn 留痕。
- D 域 5：draft.ts 互斥范围收窄自认 / files·onboard 留底 fail-open / relaunch 锁窗双开最坏自退出 / task-gate 漏登记只削查询完备性（R77-2 机器门已补）/ server-boot 测试契约回落缺省端口。
- E 域 3：ChatDock :key 重建丢未发送输入（注释认账取舍）/ workspace.ts 迁移损坏降级不持久化 / WorkbenchView refreshCachedDoc 异步不阻塞跳转。

## 六、修复计划（待作者指令；批 A 合入前必修，批 B-E 文件面互斥可并行）

- **批 A（P2×5，合入前必修）**：R28-1 ApiError 补参（vue-tsc 复绿）/ R28-2 节标题正则 `(?!#)` + strict 短篇回归 / R28-3 键名点号 + 测试改导出常量 / R28-4 cancel 移重放分支 + body 可读断言 / R28-5 executeSave 锁内段 SaveResult 收编。每项配回归；完成后门禁八件套回归。
- **批 B（机检与文案）**：R28-8/9/10/11/12 + R28-16 + R28-7。
- **批 C（数据与锁）**：R28-13/14/15 + R28-19/20/21 顺手。
- **批 D（前端打磨）**：R28-23/24/25/26。
- **批 E（测试工程）**：R28-27/28（spec-order 单一真相源）/ R28-30（eslint 扩射程增量）/ R28-31/18/6（契约锁）/ R28-22 + R28-32。
- **范围外登记（作者拍板项）**：R28-29 CI push 腿恢复时点；阶段 13 真机验证待凭据；**三批未提交修复 + 批 A 建议一并落提交**（当前 157+31 文件未提交跨三轮，提交面显著滞后于收口状态）。

## 七、评审链记档

- **索引滞后补正**：`Dev/Main/README.md`（目录地图 + 变更记录）与 `Dev/Main/01-评审/README.md`（当前暂存节）此前**未记二十六/二十七轮条目**（两轮正本与总览 1.1 行均在、均已收口，仅上述两 README 滞后至二十五轮口径）——本轮落盘时一并补正（暂存 6→9 篇）。
- **链态**：二十五（R77）/二十六（R26-xx 111 项）/二十七（R27-xx 60 项）三轮均已在总览 1.1 记「已收口（2026-08-30）」；三批修复在树未提交，本轮独立复核结论见 §五.4。
- **子代理事故**：C 域首发撞 1302 阵亡、错峰重发成活（本链多次同型事故的惯常形态，记档）。

## 八、结论

- **完成进度 ≈97%**（核心 mac 生产形态 ≈98%、win RC 形态 ≈95%）：实施路线 21/23 阶段完成，开放两项均为外部依赖/发布决策而非代码缺口；r26/r27 两批清偿后全库从零复审仅余 P2×5，二十六轮 20 → 二十七轮 10 → 本轮 5 单调收敛。
- **完成质量 8.7/10**：核心数据链/安全面/测试基建高水准且连续十一轮 P1×0；本轮五条 P2 全部为最近修复批自身的边缘回归（正则放宽/cancel 时机/键名/测试少参/收编漏段），无结构性缺陷——边际修复成本上升与未提交窗口过长是当前质量面的两个系统性信号，建议：修复批收口即提交、缩短在树窗口；批 A 五条修完随三批存量一并入库。
- 门禁现状：七绿一红（vue-tsc 红 1 = R28-1），**树不可直接提交**，批 A 完成后复绿即恢复全绿基线。

## 九、修复收口记录（2026-08-30 同日）

- **作者指令**：「开始修复，全部修复，编排下任务，并行做。」
- **执行形态**：八代理两波并行、文件互斥（波1=批A P2×5 四路：shelf/count 家族/client/service 家族；波2=批B-E P3 四路：机检文案杂项/锁与桌面/前端打磨/测试工程）——**全部首发成活零阵亡**；主评审逐 diff 亲验批 A 五条 + 抽验波 2 关键面（R28-15 零档、R28-14 锁名、R28-25 armed 守卫、R28-19 端口校验）。
- **办结账（R28-1~R28-32）**：
  - **P2×5 全修**（R28-1 ApiError 补参+替身构造器镜像·vue-tsc 复绿 / R28-2 节标题正则 `##(?!#)` + strict 链路回归 / R28-3 键名收敛 shared/storage-keys.ts 单一事实源 + 反证用例 / R28-4 cancel 移入确定重放分支 + 信封可读双场景 / R28-5 executeSave 锁内段外层 catch 收编 WRITE_ERROR + 锁释放断言）。
  - **P3 修 25 + 注释级 4**（R28-6 契约锁·test/ai/contract 清单锁定零参工具不变量〔gen.ts 行为不动〕/ R28-7 去重命中 warn 留痕 / R28-8 动词集对齐 SPEECH_VERBS·既有断言零翻转 / R28-10 warn 收窄·分隔线与嵌套子项静默 / R28-11 块标量行号 keyLineNo / R28-13 快照直喂 fileBytes / R28-14 摘要锁补 `.lock` 后缀接通 sweep / R28-15 锁等待档 5s→0 try-acquire·两挂点均跳过无害 / R28-16 导出文案分口径 / R28-18 long-path 改 mkdtempTracked / R28-19 端口 0-65535 整数校验走 fatal / R28-20 window-state 畸形入参自守 / R28-21 升级 kill 前重读 pid / R28-22 重建失败「索引已清空」条件提示 / R28-26 轮询 inFlight 去重 / R28-27 守卫镜像 localeCompare / R28-28 snapshot.txt 单一真相源·fail-closed / R28-30 eslint scripts 纳管 0 错·test/** 45 错>30 登记暂缓 / R28-31 末行换行 / R28-32 README workDir 措辞；注释级 R28-12/17/23/24）。
  - **R28-9 主诉证伪转维持 + 连带实修**：「孤立 ``` 吞后续 ##」按 CommonMark 判定系正确行为（围栏延伸至文末）——证伪维持；实测另发现真实偏离（``` 提前闭合 ~~~ 栏、信息串行行误判闭栏）→ 记开栏字符+长度做同类配对最小修复 + 4 形态测试。
  - **登记维持 1**：R28-29（CI push 腿恢复时点）——范围外作者拍板项，ci.yml 注释恢复锚维持。
  - **方案偏离记档**：R28-3 组件导出方案被否（`<script setup>` 不允许 export）改新建 shared 模块；R28-25 per-instance armed ref 被否（死实例 ref 冻结照放行）改 `armed(book)` 路由即时判定（route.params 为全局活值，等价代次比对）；R28-15 300ms 档被否（照旧冻结纯付出）定 0 档。
- **回归账**：+5 测试文件/+49 用例（r28-count-fixes / r28-client-cancel / r28-save-envelope / r28-style-armed-guard / commit-dedup-warn + 存量扩充）；**567/4467 → 572/4516**，根 README 计数修账 4 处同步。
- **终门八件套全绿**：**572 文件/4516 单测一次全绿**（104.4s）+ tsc/vue-tsc（复绿）/eslint 0 错 + check:counts（修账后一致）/packaging/knowledge 过 + **e2e 43 过 2 跳**（30.2s 零噪音）。
- **确认闸遵守**：全批零 .md 新增/移动（根 README 修账与措辞为既有文件修改）。
- **收口提交（作者指令「提交改动，并且push」）**：`5e1785b`（dev，199 文件 +9306/−786——含 R77/R26/R27/R28 四批修复合并入库，37 个新文件=3 新源码模块〔font-cache/window-state/storage-keys〕+ 33 测试文件 + spec-order 快照；e86f4b9..5e1785b 已推送 origin/dev），工作树归净。
