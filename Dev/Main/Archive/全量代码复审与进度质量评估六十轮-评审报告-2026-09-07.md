# 全量代码复审与进度质量评估·第六十轮——评审报告

> 📦 **归档记（2026-09-08）**：已收口，随「README 整理与清理」批移入 `../Archive/`（扁平冷库；确认闸 2026-09-08 修订——规则内免确认留痕）；历史正文不改写。

- 日期：2026-09-07
- 评审形态：主审（ZCode）+ 四域并发子代理（A AI 链路 / B 桌面与服务 / C 数据内核 / D 前端），全部交卷零阵亡
- 基线：dev HEAD `4373390a`（dev←win R55-R58 六批修复合流提交）+ **未提交在途「R59 报告附 §九清偿批」工作树**（57 文件：34 处源码/文档修改 +475/−89 含根 README 修账、2 个新 src 文件、21 个新测试文件；Dev/ 链文档同步已在盘）——按当前工作树实态评审，**全程只读零改动**
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。记得编排下任务。」
- 路径确认闸：AskUserQuestion 报备**未获回复**——按十六轮先例（报备未获回复、系原指令直接交付物）以六十轮正典名落盘，**待作者追认**
- 禁读声明：未读任何既有评审报告正本（`01-评审/` 与 `Archive/` 全链）；进度对账基线取自总览第三节阶段行，质量结论独立来自代码本身与实跑门禁
- 收口状态：**已收口（2026-09-07 修复批同日办结——作者指令「开始修复，编排下任务，并发做。」：批 1 P2×1 主审亲修 + 批 2 高价值 P3×5 四路文件互斥并发代理全修全配回归 + L2 终门九件套亲跑全绿 + 根 README 修账 955/6255；批 3 维持登记；收口记 §八）**

## 一、评审范围与方法

**规模（干净口径，排除 node_modules）**：src 464 个 ts/vue 文件 / 104,972 行；test 950 个 `*.test.ts` / 145,865 行；scripts 11 个。分域：A 域（ai+rag+knowledge+learn）16,288 行；B 域（desktop+process+driver+install+studio/server）23,283 行；C 域（document+events+cache+check+format+fs+git+state+log+metrics+review+export）27,864 行；D 域（web-next src）37,501 行。

**方法**：
1. **四域并发子代理深评**（并发 ≤4 避开账户限额，本轮全成活）：各域先建文件地图，核心链路文件全文精读（A ≥15 件 / B ≥16 件 / C ≥15 件 / D ≥28 件），其余按风险模式扫读（空 catch 吞错、监听器/计时器泄漏、同步 IO 阻塞、TOCTOU、竞态、错误路径不留痕、密钥泄漏、注入面、CRLF/BOM、无限增长结构）。
2. **主审亲审**：在途 diff 35 文件逐行（§二）；新增 2 个 src 文件全读 + backlog 测试抽验；P2/P3 候选逐条亲读源码定档（prefs.ts / manifest.ts / check.ts / 面板接线面）；阶段 24 标记物独立扫描（§四）；工程与测试基建亲审（tsconfig / eslint.config / vitest.config / playwright.config / tsup.config / ci.yml / desktop.yml）。
3. **门禁九件套亲跑**（§三）。

**覆盖度如实声明**：105k 行源码非逐行全读——四域核心链路精读 + 全域风险模式扫读 + 在途改动全量逐行；长尾文件以风险模式快扫为主。与近轮（R57 八域 / R59 主审亲审）覆盖深度同水位。

## 二、在途工作树（R59 附 §九清偿批）评审结论

在途批为 R55~R58 四报告「批 3 维持登记」遗留项的清偿（Dev/Main/README 变更记录 2026-09-07 条在盘佐证：21 修 + R55-G-3 降级登记 1，五路并发代理 + 主审复核，其文档链已同步完毕、仅代码未提交）。**本轮 diff 逐行亲审（34 改 +475/−89）+ 新增文件抽验：零新缺陷，判定已达收口质量。**要点：

- **AI 链路面**：promptMeta 增可选 `tools` 摘要键（chat 每轮 15 工具 schema 模型可见⟺已记录，铁律②契约补全；旧事件无键向后兼容，hash/chars 不受扰动——trace.test 新 2 例钉死）；kimi `trimStop` 补「各 ≤32 字节」UTF-8 安全截断（`truncateUtf8Bytes32` 序列完整性判定正确）+ MODEL_QUIRKS_VERSION 依规 bump `2026-09-07.1`；openai-adapter 传输截断估计面并入 toolAccum 残留（R26-25 取舍不变只修估计入账）。
- **数据内核面**：tree-issues 缓存行级纪元戳 `epoch_fp`（DDL 增列 + 存量库 `ensureTreeIssuesTables` 幂等 ALTER 迁移 + 读侧无锚/不匹配一律 miss——双进程混纪元形态收口，旧格式行天然一次性失效）；rebuild 摘要目录非 .md 文件补 warn 留痕；events ChainRecorder 缓冲截断补「丢事件必留痕」；store.ts maskSelfCheckData 注释勘误（原称 O(1) 实为全表扫，登记不修有据）。
- **桌面/服务端面**：切库取消回滚基线（三入口收敛 `saveCurrentArmingRollback`，取消路径 `rollbackCancelledSwitch` 回写 workdir.json、过不可回头点作废——测试三入口 + 正常退出对照 5 例全钉）；dev-port 拒 0 端口 fail-loud（Vite 代理固定 7878 静默失联面）；static MIME 补 .txt/.gif/.webp/.woff；log 掩码 URL 值类排除 `\"` 防越界毁 JSONL 行。
- **前端面**：镜像清理属主改 payload 精确判定（书名含 `:` 跨书误伤收口）；`reloadFromRemote` 决断时刻内容快照 + 双窗口复检（await 窗口新键入不再静默丢）；删除确认前 `waitInflightSave` 有界等待（在途保存窗口误报收口）；Book.vue activeDocId 校验补 ownerBook 属主门；书级 prefs 两键补正数有限守卫；api client 外部 abort 直通不误报 MALFORMED_RESPONSE；Check/ReviewPanel v-for 稳定键（新单源 shared/issue-keys.ts，\u0000 分隔对齐 fpKey 口径）；useSse dev 双基址失配三连击诊断。
- **测试与修账**：+21 测试文件（20 backlog + 外来 r47）/+72 用例 → 950 文件/6238 收集（6234 过 + 4 跳 0 败）；根 README 修账 929/6162→950/6234 四处，check:counts 实测对账一致。抽验 `backlog-r55a3-switch-cancel-rollback.test.ts`：全 electron 假件驱动真实 bootstrap、断言锚 workdir.json 落盘行为契约、三入口 + 正常退出对照——与主链测试同水位。

**处置建议**：该批已达收口质量，提交待作者指令（57 文件在工作树未提交）〔→ 2026-09-07 已随作者「提交改动」指令与 R60 修复批合并提交 dev `e074e7b5`，了结〕。

## 三、门禁九件套实测（全绿）

| 门禁 | 实测 | 结果 |
|---|---|---|
| vitest 全量 | 950 文件 / 6234 过 + 4 跳（6238 收集）0 败，127.35s | ✅ |
| tsc --noEmit | 0 错 | ✅ |
| vue-tsc --noEmit | 0 错 | ✅ |
| eslint . | 0 error / 0 warning（R59 清偿批已清存量 31 warning） | ✅ |
| check:counts | 实测 950/6234 与 README 声称一致；29 e2e spec / 45 用例 | ✅ |
| check:packaging | resources/ 入清单、prompt 版本表对账一致 | ✅ |
| check:knowledge | 知识层 13 条 manifest 与磁盘一致、反向扫描无未登记 | ✅ |
| build:web | 过（424ms；Book chunk 823kB >500kB 为既有体积提示非错误） | ✅ |
| e2e（playwright） | 43 过 + 2 跳（29.0s，EXIT=0；2 发布 smoke 需 CLWRITING_E2E_RELEASE 另跑） | ✅ |

coverage 按 L2 纪律由 CI 三腿矩阵 + 阈值门兜底（本轮未本地跑，与 R59 同口径）。

## 四、项目完成进度：≈97%

- **阶段 0-23 全部收口**（总览第三节：阶段 13/21 于 2026-09-04 收口、阶段 23 于 2026-08-27 关闭、其余更早；win 适配 J0-J6b 全办结）。
- **阶段 24「章节结构操作」：设计（v2，D1-D7 拍板）+ 执行方案（S2-S5 代码级蓝图）就绪，实施待作者指令。**本轮独立标记物扫描零命中：`structure.merge` 事件 ×0、fm `序` 键解析 ×0、合并/拆分章流程 ×0（「并入」49 处命中全为无关注释）——与链记「零实施」一致。
- 代码实态与计划对账无相悖：无 stub 主线、无半接线新增面（仅存登记在案的 test-only 滞留与外来保留文件，见 §六）。
- **结论：进度 ≈97%**（口径与 R55-R59 各轮一致：23/24 阶段完成 + 末阶段方案就绪待实施）。

## 五、完成质量：9.1/10（A 档）

| 域 | 范围 | 行数 | 评分 | 要点 |
|---|---|---|---|---|
| A AI 链路 | ai/rag/knowledge/learn | 16,288 | 9.0 | 三协议适配器 + quirks 七族表 + runTask 统一重试/超时/计量/trace；三红线（留痕/显式 resolve/密钥脱敏）落点齐全；A7 前决策表半接线与 test-only 滞留面登记在案 |
| B 桌面与服务 | desktop/process/driver/install/studio-server | 23,283 | 9.0 | 三级 flush 链闭环、三重闸信任边界、路径消毒多层；本轮多域中成熟度最高之一，仅剩 3 条 P3 卫生/纵深项 |
| C 数据内核 | document/events/cache/check/format 等 12 模块 | 27,864 | 9.5 | 原子写/锁序/journal 恢复/纪元失效/规范形/密钥掩码全面落地且带可回溯注记；零 P1/P2 |
| D 前端 | web-next src | 37,501 | 9.0 | 切书代数守卫、脏缓冲镜像 + baseRev 复活门、IME 守卫、双飞防护纪律红线全有防御；1 条窄窗 P2 |
| 工程与测试基建 | 配置/CI/构建链 | — | 9.5（主审亲审） | CI 三平台 × 双 Node 矩阵 + 覆盖率分桶阈值门（域级子桶防稀释）+ 内存闸 forks 池；e2e 顺序契约/禁重试/首因标记；eslint 逐规则中文 rationale；tsup 多入口 worker 伴生件 + node: 协议保留——每处决策带出处注记 |

**行数加权：(16288×9.0 + 23283×9.0 + 27864×9.5 + 37501×9.0) / 104936 ≈ 9.14 → 9.1（A 档）。**

测试体系（950 文件 / 6,238 用例 / 145,865 行，src:test 行数比 ≈ 1:1.4）：跨轮回归族以编号锚定历史缺陷（r27/r34d/r47/r55/r57/backlog 等），机检/锁/纪元/脱敏/断流各有专项；覆盖缺口均为窄边界或页面级薄层（Relations/Overview/HistoryPanel/Library/Welcome 视图编排无直接测试、C 域两处竞态形态无专项用例、driver 直接测试仅 2 文件但间接覆盖厚），无主链裸奔面。

## 六、问题清单与修复计划

**P1×0 / P2×1 / P3×11 / 观察×1**（P2/P3 候选均经主审亲读源码核实；子代理候选证伪项不列）。

### P2

- **R60-D-1** `src/studio/web-next/src/stores/prefs.ts:404-405`——`flushPendingPersist` 的 `if (putInFlight) return` 先于定时器清理：关窗钩子恰逢在途 PUT 时立即空返回，主进程视为冲刷完成即销毁窗口；**在途 PUT 快照之后的偏好改动（<500ms 窗）随防抖定时器与窗口一同死亡而丢失**。窗口极窄、仅偏好面（非稿件）。修法：在途时 `await putInFlight` 落定后再按最新快照直发一次（或钩子回传 Promise 由主进程预算内等待）+ 回归测试。（R58-B-2 接线引入；主审亲验）

### P3（11）

- **R60-B-1** `src/desktop/main.ts:1240` 主窗 `loadURL` 无本地 catch（书架/书库窗均有 `.catch`）——子进程 ready 回传后、loadURL 前崩溃的窄竞窗走 bootstrap reject →「启动失败」+ quit（已被 main.test.ts:1083-1086 固化为设计路径），did-fail-load 退避重试永不执行：冷启动窄竞窗闪退而非自愈，重启即恢复、无数据丢失。补 catch 留痕或纳入退避面。
- **R60-B-2** `src/studio/server/book-context.ts:27` bookRoot 仅词法校验（install/books.ts:139-145），无 workDir 级 `resolveWithinRoot` 双侧 realpath——DELETE（api/books.ts:423）/书架守卫有该层而 files/documents 等读端点无，防线不对称；仅本机同信任域可触（书库内自建 symlink），纵深防御补齐项。
- **R60-B-3** `src/desktop/main.ts:348-352` `saveWinState` 的 `catch { /* 忽略 */ }` 零留痕——main.ts 全文件唯一（其余忽略处均有 log），窗口状态持久化失败不可见，违全域「失败留痕」纪律。
- **R60-A-1** `src/ai/orchestrate/chat/turns.ts:137` `call.input as Record<string, unknown>` 无空值/非对象守卫——模型产出 `input:null`（args 为 "null"）时工具以兜底 catch 失败、文案不可诊断；契约侧 `src/ai/contract/chapter.ts:94` 有对称守卫，执行侧缺位。
- **R60-A-2** test-only 滞留面三处（`src/rag/index.ts:936` recall 兼容包装 34 处测试引用 / `src/ai/calls.ts:523` recordAiCall 已被 recordUsageBoth 取代 / `src/rag/store.ts:150` unlinkWithRetry 双线并存）——头注已登记，随存量断言改造批次清偿。
- **R60-C-1** `src/document/manifest.ts:212/:123`（同型 `:254`）`finalizedPathSet`/`finalizedChapterSetOfBook` 的 existsSync 前置探测与 readManifestCore 二读间 µs 级 TOCTOU：探测后清单被外部删除，readManifestCore 把 ENOENT 折叠为 `ok:true` 空清单 → 函数返回「在册空集」而非 null，单次口径偏移（currentChapter 误判 0 / 导出跳定稿章），下次读取自愈；M-13/R48-44 覆盖「读失败」未覆盖此形态。（主审亲验）
- **R60-C-2** `src/document/manifest.ts:427-429` `withManifestLockAsync` 重入分支「fn 体零 await」不变量仅注释无断言（R43-10）——未来新增 async 调用方会静默失去同进程互斥，防御性登记缺口。
- **R60-D-2** `src/studio/web-next/src/composables/useSse.ts:176-186` 换票失败回退把 token 拼 `?token=` 进 URL（R50-D2-2 自我留档过渡债）——服务端 ticket 通道全上面后应移除该回退。
- **R60-D-3** `src/studio/web-next/src/stores/check.ts:170-180` `clearFalsePositiveMarksForDoc` 用 `startsWith(prefix)` 删键——docId 为另一 docId 字符串前缀时连带误删兄弟键；docId 路径派生含 `.md` 后缀实际碰撞近不可能、仅伤 best-effort 灰显态。精确等值匹配即可。（主审亲验）
- **R60-D-4** `pages/Shelf.vue:67` / `components/ui/ShelfModal.vue:85` / `App.vue:50` 直写/直读字面量 `clw-last-book` 三处未收敛 `shared/storage-keys.ts` 单源（R28-3 已点名此断裂族）。
- **R60-主审-1** `src/studio/web-next/src/composables/useDebouncedSource.ts` **零生产消费方**——右栏面板族实际接线 `useDebouncedWordCount`/`useDebouncedFmFields` 专项版（WritingInfoPanel.vue:10、FocusStatsBar/WbDraftCard 同源），通用版仅定义 + 测试（r47-debounced-source.test.ts 5 例）。系登记的「外来 R47 WIP 保留文件」（01-评审/README 维护日期记档：字节同 e502df24 快照兼容保留）——建议删除或接线推广，处置待作者拍板。

### 观察（1）

- **R60-C-3** `src/log/index.ts` maskKeys 为已知密钥格式族枚举（Bearer/x-api-key/URL-query/sk-/xai-/智谱/Gemini 族）——新 AI 供应商引入新密钥形态时需同步补族；当前无泄漏实例，接入清单加检查项即可。

### 修复计划（批 1/批 2 已于 2026-09-07 修复批办结——见 §八；批 3 维持登记）

- **批 1（P2×1，主审亲修量级）**：R60-D-1（在途落定后补直发 + 回归测试，约 20 行）。
- **批 2（高价值 P3×5）**：R60-B-1 loadURL catch 留痕 / R60-B-3 saveWinState 补 log / R60-A-1 input 守卫对齐契约侧 / R60-D-4 clw-last-book 键单源化 / R60-D-3 精确等值匹配。
- **批 3（维持登记/待拍板）**：R60-B-2（symlink 纵深补齐，涉及面广可单立）/ R60-A-2 / R60-C-1 / R60-C-2 / R60-D-2（待 ticket 全面上线）/ R60-主审-1（外来文件处置待拍板）。
- **另行**：在途清偿批 57 文件提交待作者指令。

## 七、结论

1. **进度 ≈97%**：阶段 0-23 收口、阶段 24 方案就绪零实施（标记物独立扫描零命中）；在途 R59 清偿批已把 R55-R58 全部遗留登记项清偿完毕，代码处于「待提交的收敛态」。
2. **质量 9.1/10（A 档）**：L2 九件套亲跑全绿（950/6234 零败、双 tsc 零错、eslint 0/0、三 check 过、e2e 43/2）；四域零 P1、仅 1 条窄窗偏好面 P2 与 11 条 P3 卫生/纵深项；三红线（编辑不丢、密钥不漏、路径安全）与「模型可见⟺已记录」契约逐域核验在位。
3. 与链记口径交叉验证一致：R59（干净树）零缺陷 → 清偿批 +21 测试后本轮仅 1 条新 P2（且系 R58-B-2 已提交代码、非在途批引入）——修复链有效性持续得到盲审佐证，高频面成熟度收敛。
4. 后续两步走：①作者指令提交在途清偿批；②开工批 1/批 2 修复后本轮即可收口（P2 清零 + 回归绿 = 收口条件）——**② 已于同日办结（收口记 §八）；① 在途清偿批提交仍待作者指令**。

——评审止于评审；修复批已于 2026-09-07 同日办结，收口记见 §八。

## 八、修复批收口记（2026-09-07 同日）

作者指令「开始修复，编排下任务，并发做。」——§六 修复计划照单执行：批 1 + 批 2 全修全配回归，批 3 维持登记。

- **批 1（P2×1，主审亲修）**：R60-D-1 prefs.ts——`flushPendingPersist` 从「在途即空返回」改为「await 在途真链落定 → 清防抖定时器 → 按届时最新快照直发补一笔」，返回真链 Promise 交主进程 `flushRendererWithBudget` 预算内等待；配套新增 `runPutChain` 占位单源（`putInFlight` 从已 resolve 占位符改存真链 Promise——原占位 await 即穿透等不到 PUT 落定，flush 无从续链；同步占位先于任何宏任务可观察点，R33D-24 单飞不变式保持 + 链尾守卫清位防多路交叠误清对方占位）；App.vue:31 类型注记 `() => void | Promise<void>`（丙代理代办一行；`main.ts` executeJavaScript 本就 await 钩子返回值，主进程侧零改动）。回归测试 `backlog-r60d1-prefs-flush-inflight.test.ts` 4 用例（在途补发带最新快照与对齐 revision / 冲刷 Promise 落定前不得 resolve / 无在途直发原路径不回归 / 防抖定时器随冲刷清掉不叠发）——**红 4/4 → 绿**；prefs 全家族 8 文件 43 用例零回归。
- **批 2（高价值 P3×5，四路文件互斥并发代理）**：
  - 甲 AI：R60-A-1 turns.ts `executeChatTool` 入口 input 空值/非对象守卫（对齐契约侧 chapter.ts 口径：null/undefined/字符串/数字拒收回可诊断文案〔含工具名与实际类型，模型可按 schema 重发〕，数组按契约侧 typeof 'object' 放行走字段校验；留痕走既有 toolResultEvent 通道不新造）——`backlog-r60a1` 8 用例**红 5/8 → 绿**；turns 相关族 7 文件 34 用例绿。
  - 乙 桌面：R60-B-1 主窗 loadURL 补本地 catch 留痕（镜像书架/书库窗 R74-16 习语；原样上抛，不吞 main.test 时序 2 固化的「bootstrap reject → 启动失败 + quit」设计契约）+ R60-B-3 saveWinState catch 补 warn（本文件唯一零留痕忽略处收口；吞错不阻断关窗语义不变）——`backlog-r60b-main-leftovers` 2 用例**红 → 绿**；main.test.ts 74 过 + 1 跳零回归。
  - 丙 前端基础：R60-D-4 `clw-last-book` 四处字面量收敛 `shared/storage-keys.ts` `LAST_BOOK_KEY` 单源（R28-3 键名断裂族收口：App.vue 读 / Shelf.vue 写 / ShelfModal.vue 读删写）——`backlog-r60d4` 3 用例**红 3 → 绿**；src/ 内字面量零残留（grep 复核）。**过程记档**：代理中途一次验证命令 `git stash push` 误执行（自有 4 文件入栈），自行发现后 pop 恢复并 drop；主审收卷后 `git stash list` 核对——仅存原 stash@{0}（R47/R48 147 文件现场），工作树 8 文件逐 diff 亲验完好。
  - 丁 前端 store：R60-D-3 `clearFalsePositiveMarksForDoc` 删键从整键前缀匹配改「书前缀 \u0000 边界命中 + docId 段精确等值」（`a.md` 清除时 `a.md2` 兄弟键存活；倒序扫描与 best-effort catch 语义保留）——`backlog-r60d3` 4 用例**红 1 → 绿 4**；check store 族 9 文件 44 用例绿。
- **主审复核与两处亲修涟漪**：四代理产出逐 diff 亲验全通过（守卫口径/留痕纪律/固化契约保持/单源收敛/精确匹配逐项核对）；prefs.ts `chain` 自引用闭包触发 vue-tsc TS2454×2 → 改 `runPutChain` 助手形态根治 + 测试一处 noUncheckedIndexedAccess 索引 TS2532 修正；其间一次 Edit 重复函数头致括号失衡（vitest 收集红 8 文件 + vue-tsc TS1128），当场发现修复，双 tsc 复跑 0 错。
- **L2 终门九件套亲跑全绿**：vitest **955 文件 / 6255 过 + 4 跳（6259 收集）0 败**〔117.53s〕+ tsc 0 错 + vue-tsc 0 错 + eslint 0 error 0 warning + 三 check 过（check:counts 修账后 955/6255 与 README 一致）+ build:web 过 + e2e 43 过 2 跳〔28.7s〕。
- **修账**：根 README 测试计数四处 950/6234 → **955/6255**（+5 测试文件 / +21 用例，全部平台中立用例——win 差 73 口径不变）。
- **批 3 维持登记（不修，与 §六 一致）**：R60-B-2（symlink 纵深补齐，涉及面广可单立）/ R60-A-2（test-only 滞留三处，随存量断言改造批次清偿）/ R60-C-1（manifest µs 级 TOCTOU，自愈型）/ R60-C-2（重入不变量防御断言缺口）/ R60-D-2（ticket 全面上线后移除回退）/ R60-主审-1（外来 useDebouncedSource 处置待作者拍板）+ 观察 R60-C-3。
- **工作树**：批 1 + 批 2 改动（8 源文件 + 5 测试文件）叠加在途「R59 附 §九清偿批」57 文件之上——**已随 2026-09-07 作者指令「提交改动」合并提交 dev `e074e7b5`（69 文件 +3757/−139），待提交项了结**。

——收口条件核对：P2 清零（R60-D-1 已修 + 回归绿）✓ / 批 2 全修全配回归 ✓ / L2 九件套全绿 ✓ → **本轮收口**。归档移动待作者确认闸/归档批。
