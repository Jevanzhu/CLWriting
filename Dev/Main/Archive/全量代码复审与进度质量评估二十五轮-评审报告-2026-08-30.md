# 全量代码复审与进度质量评估二十五轮（链第七十七轮）

- 落盘：2026-08-30（评审执行 00:21–01:00）
- 基线：HEAD `e86f4b9`（dev，二十四轮 R75/R76 修复批合并提交）**干净树**（`git status` 零改动）。
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，告诉我项目完成进度，完成质量，最后形成一个文档给我。记得编排下任务。」
- **本轮不含修复**；源码零改动，唯一写入面 = 本报告 + 4 处索引 .md（Dev/Main/README、总览 1.1、01-评审/README 计数）。
- 确认闸记档：报告目标路径经 AskUserQuestion 报作者（推荐 `Dev/Main/01-评审/全量代码复审与进度质量评估二十五轮-评审报告-2026-08-30.md`），未获直接答复（作者回「继续」）——系原指令直接交付物，按链内先例（二十二轮/二十轮同款口径）依命名规范落盘，头注记档。

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审组织与覆盖口径（如实记档）

- 任务编排：TodoWrite 全程滚动（11 项，见会话记录）。
- 评审组织原案 = 六路分域禁读子代理（A AI 链路 / B 机检事件 / C 核心数据 / D 服务端桌面 / E 前端 / F 测试工程），00:25 六路**全部首发撞账户五小时使用上限 1308 阵亡**（限额 2026-08-30 02:59:56 重置；本链历轮第四次同型事故，二十三轮 A/B/C/D/E、二十二轮 B/E、二十一轮 A/B/C/D 同因）。按历轮标准流程改**主评审亲审**收口。
- 主评审亲审覆盖面（置信度以此为准，如实窄于六路全量通读轮）：
  - **深读全文 ≈3,300 行**：D 域核心六件 2,078 行（server/http.ts 154 + router.ts 118 + index.ts 452 + static.ts 144 + api/task-gate.ts 222 + desktop/main.ts 988）；C 域基座三件 629 行（fs/atomic.ts 254 + fs/safe-path.ts 116 + fs/cross-process-lock.ts 259）+ 保存链 190 行（document/service.ts save→executeSave 临界段）+ version.ts/filename.ts 头部 120 行；B 域 check/run.ts 主链 140 行。
  - **定向风险面扫描**（grep+定点读）：互斥矩阵 15 action 占闸点对账、busyGate 双侧合并、providers 密钥脱敏、stream 凭据闸、三适配器 usage estimated 标记、runner 累计口径、tree-issues 缓存 verdict 指纹失效链、events store BEGIN IMMEDIATE、前端 boot token/重 boot 去重/WCO 避让条件挂类、vitest 覆盖率分桶门、playwright workers=1/retries=0、check-counts 的 e2e 顺序快照门+空洞测试门。
  - **门禁八件套全量亲跑**（§二）。
- 禁读防锚定遵守：未读任何历史评审报告正本；`Dev/Main/README.md` 与总览仅用于**进度核算**（任务状态唯一入口，操作链要求），质量结论全部来自本轮代码实证。

## 二、门禁实测（八件套全绿，与根 README 零漂移）

| 门禁 | 结果 |
|---|---|
| `npm test`（vitest run） | **539 文件 / 4282 单测全绿**（84.25s，一次全绿零观察项） |
| `npm run typecheck`（tsc --noEmit） | 0 错 |
| `npm run typecheck:web-next`（vue-tsc） | 0 错 |
| `npm run lint`（eslint） | 0 错 |
| `npm run check:counts` | 过——实测 539/4282 + 29 e2e spec/45 用例，与 README 声称一致 |
| `npm run check:packaging` | 过——resources/ 入打包清单、prompt 版本表对账一致 |
| `npm run check:knowledge` | 过——知识层 13 条 manifest 与磁盘一致、反向扫描无未登记 |
| `npm run test:e2e`（build:web + Playwright） | **43 过 / 2 跳**（27.8s；2 跳 = 发布 smoke 需 `CLWRITING_E2E_RELEASE`；pageerror 升红基线零噪音） |

## 三、完成进度评估：**≈96%（核心 mac 生产形态 ≈98%）**

### 3.1 总阶段核算（对照总览第三节阶段行，逐项代码实证）

- 阶段 0-12、14-20、22（utilityProcess 拆分）、23（迭代清偿，已关闭）：**全部已完成**。
- 开放任务仅两项，均非 mac 核心功能面：

### 3.2 阶段 13（Responses 启用批）——开放（外部依赖）

- `scripts/verify-responses-relay.ts`（16KB）在位：两轮真机验证脚本，key 支持 env 注入（R61-15 口径），**待作者网关凭据**，不阻塞其他工作。
- encrypted 中段链路直测缺口**维持**：`reasoningEncrypted` 在 test/ 零命中（本轮亲测 grep），src/ 2 处命中——RC 后补挂账沿用。

### 3.3 阶段 21（win 适配）——已开工，J5 部分/J6 未动

| 任务 | 状态（本轮代码实证） |
|---|---|
| J0 打包构建（nsis/x64/图标/desktop.yml win 腿） | 已完成（electron-builder.yml win 段在位） |
| J1 脚本可移植 / J2 文件名消毒 / J3 平台门 / J4 CI win 腿 | 已完成（ci.yml windows-latest 腿在位） |
| J5 菜单栏隐藏 | 已完成（main.ts:348/382 平台显式口径 + setMenuBarVisibility 双保险） |
| J5 WCO 无框标题栏 + overlay 主题联动 + 避让 | 已完成（main.ts:354-363 + TabBar/SidebarRight `wco-avoid` 条件挂类 + `env(titlebar-area-*)` 自适应，2d9f402 修正后在位） |
| J5 网盘副本检测 | 已完成（git/exec.ts:110 scanCloudCopies + state.ts:284 接线 + git/exec.test.ts 契约测试） |
| J5 字体 IPC 缓存（降半档） | **未做**（main.ts:658 每调用全量枚举，无缓存） |
| J5 git 引导 / 长路径冒烟 / atomic rename EPERM 重试 | **未做**（src/ 零命中亲测） |
| J6 发布面（签名/SmartScreen 披露/覆盖安装指引） | **未动**（README 无 SmartScreen/unsigned 披露；D4「首版 unsigned+披露」的披露半边缺位） |
| J6b 跨平台书库迁移 | 挂账（需 mac 侧） |

### 3.4 CI 触发策略

`.github/workflows/ci.yml` = `pull_request → main` + `workflow_dispatch`（R74-5 恢复锚在位；push 腿留待频推期结束恢复）。

### 3.5 分域完成度

| 域 | 完成度 | 缺口 |
|---|---|---|
| A AI 链路 | 92% | 中转真机验证（外部）、encrypted 直测 |
| B 机检·事件·评审 | 92% | 边角规则口径随长跑演化 |
| C 核心数据 | 95% | 仅余长尾加固空间 |
| D 服务端·桌面 | 95% | J6 发布面、字体缓存 |
| E 前端 | 95% | win 体验余项（字体下拉等） |
| F 测试·工程 | 90% | .vue 直测部分覆盖、win 腿盲区、encrypted 直测 |

## 四、完成质量评估：**8.8/10**

### 4.1 问题清单

**P1×0 / P2×0（本轮评审面内）/ P3×4 + 登记维持×2**（覆盖口径见 §一——本轮为亲审轮，窄于六路全量轮；「未发现」指深读+定向扫描+门禁面内无新增，不等于全量通读意义上的穷尽）：

| 编号 | 级别 | 位置 | 问题 | 处置建议 |
|---|---|---|---|---|
| R77-1 | P3 | `src/desktop/main.ts:658-665` | `desktop:get-system-fonts` 每调用全量枚举系统字体（font-list 起子进程），设置页反复打开有可感知延迟；总览 J5「字体 IPC 缓存（降半档）」未做 | 随 J5 余项：TTL 或会话级缓存 |
| R77-2 | P3 | `src/studio/server/api/task-gate.ts:147-163` | KNOWN_ACTIONS 手工注册表：本轮亲核与 15 个 `acquireTaskGate` 占闸点**对账零漂移**，但新增 action 漏登记只削弱跨进程 busyGate 查询完备性（注释自认） | 补一条静态对账单测（grep 字面量 vs 注册表），把「零漂移」从本轮人工对账变成机器门 |
| R77-3 | P3 | 计划项 | J5 余项四件未做：git 引导 / 长路径冒烟 / atomic rename EPERM 重试 / 字体 IPC 缓存（src/ 逐项零命中亲测） | 挂账登记，随 J5 收尾批办结 |
| R77-4 | P3 | 计划项 | J6 发布面未动：unsigned SmartScreen 披露、README 覆盖安装指引、签名决策留白（electron-builder 配置侧已就绪） | win 正式发布前必办 |
| R77-5 | 登记 | test/ | encrypted 中段链路无直测（`reasoningEncrypted` 零命中）——阶段 13 挂账维持 | 维持登记 |
| R77-6 | 登记 | playwright.config.ts | e2e 共享 workDir 顺序契约——已有三重守卫（E2E_SPEC_ORDER_SNAPSHOT 机器门 + workers:1 + retries:0），风险受控 | 维持登记 |

### 4.2 分域评分与依据

| 域 | 评分 | 本轮亲审依据 |
|---|---|---|
| D 服务端·桌面 | 9.3 | 深读最厚（2,078 行全读）：三防线闭环（Host 精确匹配 + Origin 白名单 + 写双闸/GET token 闸 + 豁免显式表）、错误信封单出口统一脱敏、静态面 canonical 判界、跨进程任务闸（O_EXCL+续期+载荷校验释放）、安全五件套工厂不可让渡、崩溃自愈+稳定窗复位、IPC 全带路径校验 |
| C 核心数据 | 9.2 | 原子写（fsync 文件+目录/linkSync 独占建/流式全量写/清扫带 pid 存活+年龄双门）、safe-path fail-closed 双侧 realpath+锚点回走、保存链五重防线（入队串行→跨进程保存锁 5s fail-closed→锁内双重复核→revision 基线→非 UTF-8 无条件拒写+journal 全文快照先行）、版本 pinned+分层保留、文件名码位+字节双封顶+win 保留名 |
| B 机检·事件 | 8.8 | run.ts 失败契约化（NOT_CHAPTER/REBUILD_FAIL/CHECK_ERROR）、db 句柄 finally 收口、win 路径归一、三源同拍防窗口；tree-issues 缓存 verdict 指纹失效链+裁决 forget 接线在位；events BEGIN IMMEDIATE 事务 |
| A AI 链路 | 8.8 | 三适配器 usage 估算兜底（estimated 标记）全接线、runner 全 attempt 累计口径；中转验证脚本在位（定向扫描面） |
| E 前端 | 8.8 | boot token 注入+re-boot 防抖去重、WCO 避让条件挂类正确（TabBar 右栏关时避让/SidebarRight 常挂）、离线态容错启动 |
| F 测试·工程 | 9.0 | 门禁八件套实测全绿零漂移；覆盖率域级分桶门（ai 88/events 95/server 86 等实阈）、e2e 顺序快照门+空洞测试门入 check:counts |

### 4.3 亮点（本轮亲审实证）

1. **纵深防御成体系**：同一风险（路径穿越/密钥泄漏/丢稿/双进程竞态）普遍有 2-4 层独立防线，且每层注释标注轮次出处——可回溯性极强。
2. **残余窗口如实记档**的文化：跨进程锁的 proper-lockfile 级已知语义窗口、SIGSTOP 误接管等极端态均在源码头注声明，不假装闭合。
3. **机器门替代人工约定**：e2e 顺序契约有快照门、测试数有 check:counts、知识层有 sha256 manifest、打包有 packaging 门——README 声称与实际零漂移是制度性保证而非自觉。

### 4.4 上轮（R75/R76 修复批，HEAD `e86f4b9`）抽查复核

- R75-5 跨进程 busyGate：books.ts:103-110 双侧合并去重判 409 **在位** ✓
- R76-29 安全标志后置不可让渡：main.ts:371-379（contextIsolation/sandbox/nodeIntegration 置于 spread 后）**在位** ✓
- R76-22 保存锁内复检：service.ts:314-335 **在位** ✓
- R75-7 pageerror 升红：本轮 e2e 43 过零噪音**佐证** ✓
- R74-1 usage estimated：三适配器全接线 ✓
- R76-30 console 镜像过 maskKeys：未亲验（登记）。

## 五、结论

- **完成进度 ≈96%（核心 mac 生产形态 ≈98%）**——与总览任务状态一致：全部功能阶段已完成，余量为 win 发布面（J5 四小件+J6）、阶段 13 外部验证、测试补面三项，均非 mac 核心功能。
- **完成质量 8.8/10**——门禁八件套全绿零漂移 + 本轮独立亲审（深读 ≈3,300 行核心 + 六域定向扫描）未发现新增 P1/P2；残留 P3 均为已登记计划项或低危维护性点。置信度声明：本轮覆盖窄于六路全量通读轮（子代理限额阵亡改亲审，§一记档）。

## 六、建议（待作者指令，本轮不执行）

1. **J5 收尾批**：字体 IPC 缓存 + git 引导 + 长路径冒烟 + EPERM 重试四件合批。
2. **J6 发布面**：unsigned SmartScreen 披露 + README 覆盖安装指引 + 签名决策（D4 已拍板首版 unsigned，只差披露落地）。
3. **R77-2**：KNOWN_ACTIONS 静态对账单测（半小时级，把人工对账变机器门）。
4. **阶段 13**：中转网关凭据到位后跑 `scripts/verify-responses-relay.ts` 留证收口。
5. 暂存区 5 篇已收口历史报告的归档批（二十一/二十二/二十三/二十四轮）待作者指令；工作树本轮纯文档改动未提交。

## 七、收口记（2026-08-30，修复批全办结）

作者指令「**开始修复，全部修复，编排下任务。**」（2026-08-30）——§六建议 1/2/3 全办结 + 维持项 R77-5 补直测；§六.4 阶段 13 待外部凭据（范围外维持）；§六.5 归档批涉 .md 移动，按确认闸惯例「开始修复」不构成移动确认（待另行指令）。子代理限额未重置，全链主会话亲修。

### 7.1 批 A-G 办结明细

| 批 | 项 | 落点 | 状态 |
|---|---|---|---|
| A | R77-1 字体 IPC 缓存降半档 | 新增 `src/desktop/font-cache.ts`（60s TTL + 在途合并 + 失败不缓存）+ `main.ts` `desktop:get-system-fonts` 接线 + `test/desktop/font-cache.test.ts`（5 用例） | 修 ✓ |
| B | R77-3 git 引导 | `src/git/exec.ts`：`git()` ENOENT 特判人话（未检测到 Git → 引导装 Git for Windows）+ `scanCloudCopies` 补坚果云「冲突副本」特征（全角/半角，母本收紧防误报）+ `test/git/exec.test.ts` +2 用例 | 修 ✓ |
| C | R77-3 长路径冒烟 | 新增 `test/fs/long-path-smoke.test.ts`（>260 字符断言前提：atomicWriteFile 覆盖/Stream/Exclusive + 跨进程锁 + sweep 清扫，2 用例）。**longPathAware 记档维持**：electron-builder 无此字段（app-builder-lib schema 零命中复核）——对策收口为 Node/libuv `\\?\` 自动前缀 + README 披露「路径建议 <200 字符」 | 修 ✓（manifest 项维持） |
| D | R77-3 rename 重试 | `src/fs/atomic.ts` 新增 `renameWithRetry`（仅 EPERM/EBUSY 进重试，3×50ms 指数退避 50/100/200ms，rename/sleep 可注入）接管 atomicWriteFile/atomicWriteStream 两处 rename + `test/fs/atomic-rename-retry.test.ts`（6 用例） | 修 ✓ |
| E | R77-2 KNOWN_ACTIONS 对账 | `task-gate.ts` KNOWN_ACTIONS 导出 + 新增 `test/governance/known-actions-audit.test.ts`（双断言：字面量集合 == 注册表；出现数 == 捕获数——动态 action/签名漂移都炸）——漏登记从「注释自觉」变机器门 | 修 ✓ |
| F | R77-4 J6 发布面披露 | 根 README 新增「Windows 版使用须知」节（SmartScreen「更多信息→仍要运行」/ 无自动更新覆盖安装 / Git 前置 / 书库勿放同步盘〔坚果云式副本可自动检测；OneDrive 式副本与合法标题不可分，按设计方案批 5 选项 2 走披露〕/ 路径 <200 字符 / `%APPDATA%\CLWriting`）+ userData 段补 win 路径 + `electron-builder.yml` R77-4 落账注释 | 修 ✓ |
| G | R77-5 encrypted 直测（维持项补面） | 新增 `test/ai/provider/responses-encrypted.test.ts`（7 用例：出站 include 有/无工具两态、入站 reasoning_item 带/无 itemId/无 encrypted 三态、回合回插位置+双条件收紧） | 补测 ✓ |
| — | R77-6 e2e 顺序契约 | 已有三重守卫（顺序快照门 + workers:1 + retries:0） | 维持（不动） |

### 7.2 门禁回归（八件套全绿）

`npm test` **544 文件 / 4306 单测**全绿（+5 测试文件 / +24 用例）· tsc 0 错 · vue-tsc 0 错 · eslint 0 错 · check:counts 过（根 README 计数 4 处同步：4282→4306、539→544）· check:packaging 过 · check:knowledge 过 · e2e **43 过 2 跳**（27.1s 零噪音；2 跳 = 发布 smoke 需 CLWRITING_E2E_RELEASE）。

### 7.3 尾账

- 修复面：src 改 3 文件（main.ts / git/exec.ts / atomic.ts）+ task-gate.ts 导出 1 行 + src 新增 1（font-cache.ts）；test 新增 5 + 追加 1（exec.test.ts）；根 README「Windows 版使用须知」+ 计数 4 处；electron-builder.yml 注释。Dev/ 索引同步 4 处 .md（本报告 §七 + 总览 1.1 状态列 + Dev/Main/README 变更记录/目录地图 + 01-评审/README 维护日期/暂存条目）。
- 未办（范围外维持）：阶段 13 中转网关真机验证（待作者凭据）；历史报告归档批（待另行指令）；工作树未提交（待作者指令）。
