# 全量代码复审与进度质量评估四十九轮·评审报告

- 轮次身份：第四十九轮 = 链第一百一十七轮。日期：2026-09-05。
- 基线：**dev HEAD `8e20061d`（2026-09-05 16:44，四十五轮修复批收口提交）干净树**——注意与四十七/PM/四十八轮各报告所记「HEAD `dfb943d` + 工作树在途」基线不同：彼三批改动未落 dev，详见 §七。
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。记得编排下任务。」（会话内追加「继续，记得编排任务，并发做」）。
- 路径确认闸：按四十三/四十四/四十八轮同款指令先例，正典命名 `全量代码复审与进度质量评估四十九轮-评审报告-2026-09-05.md` + `R49-N` 编号；会话自主执行无法即时确认闸，**待作者事后追认**。
- 禁读声明：本轮未读 `01-评审/` 任何既有报告正文与 `Archive/` 正本，评审结论全部从零独立得出；计划基线仅用总览/台账/README 的索引面（状态行），未采信任何评审结论。
- 本轮零源码改动、零测试改动（评审轮不含修复；修复计划见 §九）。**报告状态：已收口——2026-09-05 修复批同日办结（作者指令「开始修，建议修的全部都修，编排下任务，并发做」），收口记 §十一。**

> 📦 **归档记（2026-09-06）**：随「归档 6 篇」批（作者确认闸拍板——链记已收口且修复批经核验在 dev 在位：四十三/四十四/四十五/四十六/四十九轮 + 全库代码重评审；四十七/PM/四十八三篇修复批未落 dev、待拍板了结前留暂存）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审口径与执行形态

- **评审面**：src 全量 459 文件 / 约 9.95 万行（其中前端子包 web-next 约 3.6 万行）+ 根配置全套（tsup/vitest/playwright/eslint/electron-builder/CI 两工作流/scripts 九脚本）+ test 工程面盘点（856 文件/13.1 万行，抽样核验断言质量）。
- **执行形态**：八域分域禁读子代理两波并发（波 1：A 桌面进程 / B 服务端 / C AI 链路 / D 数据核心，四路同发全成活；波 2：E RAG·机检·导出 / F 前端状态 / G 前端组件 / H 工程测试承诺，首发撞 1308 五小时限额×2，17:20 重置后四路同发补发全成活）+ 主审亲验（P2×3 全部亲读源码证实、P3 抽验 4 条证实）+ 进度盘点主审亲核。
- **分级口径**：P1 = 用户可见功能损坏 / 数据丢失或损坏 / 安全实质缺口；P2 = 明确缺陷、显著性能债、承诺与实现不符；P3 = 小缺陷 / 卫生 / 注释失实 / 可维护性。

## 二、门禁基线实测（mac 本机，主审亲跑）

| 门 | 实测 | 结论 |
|---|---|---|
| vitest 全量 | **814 文件 / 5645 过 + 3 条件跳，0 败**（148.0s） | 绿 |
| tsc（node 侧） | 0 错 | 绿 |
| vue-tsc（web-next） | 0 错 | 绿 |
| eslint | 0 error / 0 warning | 绿 |
| check:counts | 过（README 声称 814/5645 与实测一致，机器对账） | 绿 |
| check:packaging / check:knowledge | 过 / 过 | 绿 |
| e2e（Playwright） | **43 过 + 2 跳**（30.6s；2 跳 = 发布 smoke 需 CLWRITING_E2E_RELEASE） | 绿 |

九件套全绿，当前 dev 树自洽且与根 README 声称零漂移。

## 三、发现清单（P0×0 / P1×0 / P2×3 / P3×34 = R49-1~37）

### P2（3 项，全部主审亲验成立）

**R49-1〔C 域〕finish.ts:113-114 历史压缩摘要 onRetry 错误文案未过 redactSecret——SSE warning 第二层脱敏旁路**
证据：`onRetry: (attempt, error) => emit(opts, { type: 'warning', message: \`历史压缩摘要生成异常（${error}）…\` })` 裸拼 error；对照同款回调 turns.ts:494-496 与 self-heal.ts:865-870 均已过 `redactSecret(error)`（R43-19 口径），runner.ts:658 出口传原始 `e.message` 不另清洗。`grep "type: 'warning'" src/ai/` 四处命中仅此一处漏。影响：重试期异常原文绕过第二层脱敏直达前端 SSE（常见路径有适配器错误工厂第一层兜底，实际泄漏需两层同时失效且 message 含词表外格式 key）。修复：`redactSecret(error)` 包一层（一行），或 runner.ts:658 出口统一脱敏根治全部 onRetry 消费方。

**R49-2〔E 域〕export/index.ts:115 purifyBody 围栏判定不认缩进代码块——`#%` 批注存在泄漏进导出稿的窗口，且与 checkSectionCount 围栏口径分裂**
证据：`if (respectFence && line.trimStart().startsWith('```'))` 任意缩进深度都翻转围栏状态；对照 check/count.ts:954 已按 CommonMark 收敛 `^\s{0,3}(`{3,}|~{3,})`。正文含 4+ 空格缩进代码块时其内 ``` 行被误当围栏开关，后续真实 `#% 备注` 行被当围栏内容保留；若误开栏恰好成对闭合（unclosed=false），IR-5 两遍收口兜底不触发，批注直接进导出稿（触碰「导出不泄漏定稿批注」红线）。触发条件窄（网文正文缩进代码块+批注组合罕见）。修复：purifyBody 与 checkSectionCount 共用同一围栏状态机单源（或采 `\s{0,3}` 前缀约束）。

**R49-3〔H 域〕README.md:93 安全节「GET 读接口不校验令牌，这是有意的设计」与实现不符（实现更强，文档滞后）**
证据：server/index.ts:390-402（S7/T2-3）已对全部 GET/HEAD `/api/*` 强制 `x-studio-token` 常量时间校验（豁免仅 `/api/boot` 与 SSE stream 两条精确路径，:142）；`?token=` query 通道已收窄至仅 SSE。README 整段「GET 读接口不校验令牌……本机任何程序都能调这个接口拿到它」建立在不存在的机制上。影响：①威胁模型叙述失真（本机第三方程序直接 GET /api/books 实吃 403，本地集成者按 README 写会失败）；②安全承诺与实现不符。修复：改写安全节第三层为现行机制（GET 亦需令牌；boot 免凭据发放故「本机进程可两步取得凭据」的信任域结论仍成立）。

### P3（34 项，按域列；存疑 4 项标注）

**A 域（桌面壳/进程/安装/git/日志，9/10）**
- R49-4 server-manager.ts:472-477 stopChild 的 settleStarting 裸 await 无时间预算，与 shutdown 的 2s 预算（shutdownSettleBudgetMs）不对称——bootstrap 重试×握手挂起病理态并发时最坏阻塞 ~34s。修复：同款 race + 超时对 startingProc 走 killProcAwaitEscalating 收口。
- R49-5 main.ts:727-731 vs 1300-1307 closeFlushInFlight 与 quitFlushInFlight 两旗互不感知——关窗 flush 在途时 Cmd+Q 可对同窗并发二刷、极端时序双确认框（有 isDestroyed/try-catch 守卫，不崩不丢数据）。修复：before-quit 检查 closeFlushInFlight，在途即只拦不再起第二链。
- R49-6 main.ts:760-774+1302 session-end 已直接下发 shutdown，但 before-quit 不消费 sessionEnding 旗，OS 关机窗口内仍排完整退出链（靠 shutdown 幂等早退收敛，功能无损害；信息性）。

**B 域（HTTP 服务端，8.5/10）**
- R49-7 api/settings.ts:354-357 listMdRecursive 用 `f.startsWith('._')` 过滤，但 `readdirSync(recursive:true)` 条目带子目录前缀（`卷一/._001.md`）——嵌套 AppleDouble 文件漏拦，污染 countChapters 章数与 relations.mine 的「已写正文节选」（前 200 字拼入 AI prompt 上下文）。修复：段级判定 `f.split(/[\\/]/).some(seg => seg.startsWith('._'))`。
- R49-8 outline.ts:69 / lead-updates.ts:37 / foreshadows.ts:111 三处 `_req` 实际被 readJson/parseRequestUrl 消费——下划线命名（本仓约定「未使用参数」）失实，误导排查路径。修复：去下划线。
- R49-9〔存疑〕books.ts:465 + install/books.ts:370-387 删书尾段 removeBookEntryAsync 的 writeBooks IO 异常上抛——书目录已入墓地（删除实质完成）而客户端收 500，重试 404 可收敛、repairBooks 有 missing 报告。语义错位但触发苛刻、多层兜底在位，裁量是否对齐另两条失败路径的降级口径（log.warn + 幽灵条目）。

**C 域（AI 链路，9/10）**
- R49-10〔存疑〕learn/commit.ts:44-73 入库指纹去重 RMW（读指纹集→逐 pick add）无跨进程锁——双进程并发提交互不可见各落一条（同域先例：learn/index.ts:246、knowledge/update.ts:156 均有锁）。缓解充分（低频显式动作 + addEntry O_EXCL 排他 + 注释自认「尽力而为」），后果仅可见可删的重复条目。裁量：补 5s 锁或头注显式声明取舍。

**D 域（fs/文档/事件/状态核心数据层，9/10）**
- R49-11〔存疑〕fs/case-probe.ts:39-59 探针名固定，双进程并发探测时 finally 清理互删对方探针——大小写不敏感卷上可产出「敏感」假阳性警告（fail-open 方向安全，无数据影响）。修复：探针名掺 pid/随机后缀。
- R49-12 document/version.ts:518-537 listVersionEntries 逐版本 readVersion 全量整读（含 content），readVersionMeta（64KB 头读，R66-19）在而未用——版本面板每次打开 N 版本×全文 IO；writeVersion/pruneVersions 已迁移漏此处。修复：先 readVersionMeta，仅 words 缺失回落全读。
- R49-13 format/style-migrate.ts:158-177 铁律幂等闸/瘦身按标题子串匹配（`/反和解|AI\s*味替换/`），作者自建含关键词的段（如「## 反和解心得」）会被当遗留段整段删除——与 docstring「保守：未知段一律保留」不符，条件性作者内容损失（列表行可经条目库找回，散文丢失）。修复：精确段名锚定。
- R49-14 format/filename.ts:93-109 sanitizeFullFileName 对纯点文件（`.gitignore`）产出「未命名.gitignore」——stem 惰性匹配空串+ext 吞全名，隐藏文件被改名劫持。修复：stem 空且 ext 以 `.` 开头（无第二个点）按无扩展名完整名处理。
- R49-15 rmWithRetry 收编残留 2 处：style-candidate.ts:203（confirmCandidate 裸 rmSync 候选文件）+ state.ts:455（healMovePending 同 inode 硬链中间态裸删旧位）——win 瞬时锁下误报（前者条目已入库可幂等重试、后者保守报红可自愈），无数据损伤，退避纪律不一致。修复：两处换 rmWithRetry。
- R49-16 document/trash.ts:392-394 purgeTrash 条目定位用容错版读清单（读失败返 []）→ 瞬态读失败误报 NOT_FOUND 而非 500 可重试（同文件 restore/purge RMW 段均 strict）。修复：定位读改 strict。
- R49-17 events/store.ts 事件库无容量治理——compaction 只遮蔽不删除、无保留策略/VACUUM（append-only 全量落库系既定设计，审计完整性优先；长年累月后审计视图 IO 线性增长且库文件无收缩通道。设计性登记，供路线图裁量）。
- R49-18 document/manifest.ts:49-59 + service.ts:312/414 清单无读缓存——单次保存链全量解析 2-3 次（锁前能力闸 + 锁内复核 + maybeUpdate），千章书毫秒级×3，正确性所需但缺 mtime 签名缓存。修复：进程内 mtimeNs+size 签名缓存，写路径锁内强制 miss。

**E 域（RAG/机检/审稿/导出/驱动，9/10）**
- R49-19 rag/store.ts:383 readAllChunks 死代码 + 头注「召回用」失实（生产召回已走 streamChunkScores，src 零调用方）——220 行重复实现成漂移面。修复：删除或头注如实化。
- R49-20 rag/index.ts:848-853 硬截断探针行 `rows.pop()` 在混 model 边界错删一条合法命中（produced++ 先于 model 过滤，rows 最末未必是第 N+1 产出）——触发需 10 万块+混 model 双条件（正常流程不可达），纯边界瑕疵。修复：返回探针标志不盲 pop。
- R49-21 rag/index.ts:527-538 commitIndexBatch 全量物化 `Float32Array[]` 至 COMMIT（10 万块档 ≈600MB 峰值）——注释声明避免跨 embed 持锁正确，但 R73-5 续传路径已示范「整章小事务」更优解（锁窗纪律不变、峰值降单章级），系「200 万字不崩」下已登记的最大单点内存账。修复：正常路径改逐章提交（chapterSpans 结构已在）。
- R49-22 check/leads.ts:95-105 与 rag/index.ts:180-196 重复章号文件选择口径不一致（目录遍历序首见 vs 路径字典序最小）——R35-43 已登记近似且双路径有告警留痕，登记在案口径分裂非新缺陷。修复：两侧统一单源选择函数。
- R49-23 check/tree-issues-cache.ts:212-218 mtime_ms 列名/签名实存 µs 值（调用方传 `Number(mtimeNs/1000n)`）——量纲契约靠注释维系，按列名语义写入毫秒值将静默 miss（自愈无害）。修复：改名 mtime_us 或注释声明量纲。
- R49-24 check/count.ts:651 style-dialogue-tag 修复建议对「X地道」形态产出「他道」——advisory 文案可读性差。修复：DIDAO 形态单独文案。

**F 域（前端状态/数据层，9/10）**
- R49-25 useChapterTreeActions.ts:427-429 删除确认弹窗 unsaved 判定在 F8 排队落盘时序下失实（`!(await save)` 未复查 dirty——对照 rewrite.ts:44 R34D-22 同款已复查）——窄窗口「未保存修改将丢失」恐吓性文案与事实不符（内容已落盘），可能导致误取消删除。修复：对齐 R34D-22 复查口径。
- R49-26 useSse.ts:139 SSE 凭据回退通道把长期 token 拼 URL query（ticket 端点故障时 `?token=` 以 60s 封顶节奏反复进 URL）——本地单机暴露面小，过渡期权衡登记。修复：ticket 稳定后移除回退（服务端已 urlPathOnly 不落日志）。
- R49-27 stores/check.ts:12-14 误报灰显 localStorage 键 `clw-fp:${name}:${docId}` 冒号拼接——书名含冒号时前缀歧义可跨书误清（需构造性书名碰撞；展示态 best-effort）。修复：分隔符换 `\u0000`（learn.ts sampleKey 先例）。
- R49-28 stores/provider.ts:66-70 注释宣称「任一在拉」可由 `fetchingModelIds.size>0` 派生——普通 Set 非响应式无法派生（当前无消费方，纯注释失实）。

**G 域（前端组件/视图/编辑器，9/10）**
- R49-29 ChapterMetaDialog.vue:80 容器级 keydown.enter 吞掉「取消」按钮上的 Enter——焦点在取消钮按 Enter 先触发 onKeySave（保存+关窗）再激活取消 click，用户想取消却执行了保存（Tab 一次即达，路径真实可达）。修复：`e.target.closest('button')` 排除或只绑输入框。
- R49-30 MetaFormPanel.vue:133-164、178-184 每键 2 次全文级 parseFmFields（watch + computed 同源 content）——splitFrontMatter 全文 split/join O(全章长度)，漏接 R46-5 防抖家族（AnalysisPanel/EditorView/WritingInfoPanel 三处同族均已 150ms 防抖）。修复：tagValues 改走 useDebouncedFmFields。
- R49-31 pages/Shelf.vue:239-246 书架页局部 `--font-size-*` 用固定 px 覆盖，丢失 `calc(NNpx + var(--font-size-step))` 链路——「界面字号档」与 win +1px 基准在书架页整体失效，跨页字号不一致。修复：改 calc 形态保持差值。
- R49-32 CreateBookModal.vue 缺焦点圈与 dialog ARIA（对照 ConfirmDeleteModal R37-33 同族已补）——Tab 可跑出弹窗到背景内容，可及性一致性缺口。修复：接 useFocusTrap + role="dialog"。
- R49-33〔存疑〕styles/tokens.css 暗色 `--text-faint:#666666` 对 `#1e1e1e` 对比度 ≈2.9:1（低于 WCAG AA 4.5:1）——faint 层级系刻意弱化设计，是否缺陷取决于设计口径，登记供拍板。

**H 域（工程/测试/README 对账，8.5/10）**
- R49-34 README.md:108 lint 描述停留在旧口径「JS/MJS 最小门」——eslint.config.js:90 已纳管 src/scripts/test 全 TS 面（typescript-eslint recommended）。修复：README 改现行口径。
- R49-35 eslint.config.js:38-48 ignores 缺 `dist-electron/**`——本地 build:desktop:dir 后 npm run lint 会扫解包产物（当前仓内 dist-electron 9 个 .js 恰全在 node_modules 路径下被内置忽略挡住故现状 0 错；一旦解包出非 node_modules 的 .js 即海量假红；CI 不受影响，受害面是本地终门）。修复：ignores 补一行。
- R49-36 .github/workflows/ci.yml:9-11 无 push main 腿（注释自认「频推期结束后恢复」未兑现）——直推 main 零自动验证。修复：补 push 腿。
- R49-37 test/e2e/page-error-baseline.ts:20-23 头注 bullet 整行重复（R75-7 改写复制残留）。修复：删重复行。

## 四、分域质量评价

| 域 | 范围 | 评分 | 一句话 |
|---|---|---|---|
| A 桌面壳/进程/安装/git/日志 | 52 文件 ≈11K 行 | 9/10 | 跨进程时序纪律罕见地严，kill 升级链/定时器配对/IPC 校验全核销干净 |
| B HTTP 服务端 | 48 文件 ≈11K 行 | 8.5/10 | 手写无框架 HTTP 层安全三层/并发闸矩阵/缓存族范式系统性设计，全仓质量最高区域之一 |
| C AI 链路 | 83 文件 ≈13.7K 行 | 9/10 | 事件流重放可重建性教科书级，三协议边界处理密度极高；唯一 P2 系脱敏第二层铺漏一处 |
| D 数据核心层 | 68 文件 ≈19.4K 行 | 9/10 | 防御性工程密度最高：原子写全链/锁体系/保存定稿协议/事件库崩溃语义全收口；家族性修复各残留 1-2 漏点 |
| E RAG/机检/审稿/导出/驱动 | 28 文件 ≈8.1K 行 | 9/10 | 边界纪律系统性（CRLF/BOM/NFC/码点/正则状态），fail-noisy 降级贯穿，内存峰值账算到 MB 级 |
| F 前端状态/数据层 | 81 文件 ≈10.5K 行 | 9/10 | 竞态防护体系化（操作代+书名快照+await 复检三件套），SSE 换票/退避纪元成熟，XSS 零面 |
| G 前端组件/视图/编辑器 | ≈120 文件 ≈20K 行 | 9/10 | XSS 零面，CodeMirror 集成（IME/undo/外部替换）扎实，长列表全有帽；家族修复各漏一兄弟件 |
| H 工程/测试/README 对账 | 配置+脚本+测试工程 | 8.5/10 | check 三件套近无懈可击，治理测试机器门化是独有亮点；README 行为描述滞后于安全演进 |

**综合：A−（8.9/10）**。零 P0/P1，P2×3（一条脱敏卫生、一条窄窗导出泄漏、一条文档滞后型承诺不符），P3×34 集中在「家族性修复漏点、注释考古层、口径分裂」三类维护性债务。

## 五、正面核销面与专项结论（八域合并）

1. **安全**：Host 精确匹配（boot 前）/ Origin 白名单写 403 / token 常量时间比较全量 GET+写覆盖（豁免仅 boot+stream 两条精确路径）/ URL 归一化双轨防绕过 / 路径穿越四层 / books.jsonl 篡改防御（绝对路径/..段/NUL/自指全拒）/ 密钥 vault 信封加密（HKDF→KEK→DEK→AES-GCM+版本防降级）/ redact 五层双出口 / render 层安全工厂三窗统一（contextIsolation/sandbox 不可被调用方放宽+生产 CSP 恒注入）。
2. **XSS**：前端全库 v-html/innerHTML/insertAdjacentHTML/document.write **0 处**（两域独立 grep 互证）；动态样式仅 CSS 变量 setProperty；SVG path 全数值坐标。
3. **内存与生命周期**：监听器/定时器/ResizeObserver/AbortController/子进程/worker/SSE 连接全域配对核销；缓存全族 FIFO/TTL/字节预算有上限（docs LRU20 dirty 不驱逐、log 500、chat 200、md-text-cache 64MB、待写队列 1024 丢最旧）；无无界增长结构。**零传统内存泄漏**（与既有各专项结论一致，本轮独立复核）。
4. **平台适配**：win 大小写折叠单源（platformCaseFold 六处收编、锁键字节一致）/ 保留名全集 / EPERM·EBUSY 退避族 / windowsHide 全 spawn 面 / trueCasePath 逐段归一 / NFC 创建点 + 容忍读规范写 / 同 inode 判重 / 字体枚举整流解码防 CJK 劈裂。
5. **原子性与崩溃一致性**：tmp+fsync+rename 重试+父目录 fsync+孤儿清扫；journal pending→settled 全生命周期；executeSave 单读同源派生；清单/回收站 RMW strict 读防空表重写；事件库迁移墓碑+回滚失败保墓碑。
6. **README 承诺对账（46 条）**：45 条在位、1 条不符（R49-3，文档滞后型，非功能缺失）。安全三层、自动保存+版本校验、快照恢复、机检打回循环（attempt 触顶 escalate 交作者实现在位）、三审、防吃书闸、伏笔提醒、字数曲线、文风系统、选中改写、对话助手确认闸、导出、回收站、跨机复制、Key 三不承诺（不进 git/不明文日志/本机加密）、win/mac 披露——逐条 file:line 落点见 §三/§六。
7. **测试工程**：断言质量高（负向断言显式圈定合法状态集、raw socket 构造攻击形态直测）；治理测试把配置漂移/依赖方向/mock 零计费/注册表对账/「模型可见⟺已记录」全部机器门化；e2e 顺序契约三层防线（workers:1 + 快照静态门 + vitest guard）。

## 六、进度评估（对照总览计划基线，代码实证）

- **阶段 0–23 全部收口**（总览第三节）：代码实证抽查全部符合——阶段 13 Responses 三适配器+中转验证收口、阶段 21 win 适配 J0-J6 标记物在位（A 域亲核：保留名/env 清除/samePhysicalPath/字体链/README 披露）、阶段 22 utilityProcess、阶段 23 批 1-4。功能面 46 承诺 45 落位（§五.6）。
- **阶段 24（章节结构操作）零实施**（设计如此——已拍板待作者指令开工）：本轮亲核无 structure.* 事件、无 renumber、无 `序`/`并入` fm 键消费面，与台账一致。
- **字体系统（03-设计，不纳入计划）**：选择器全链在位（FontPicker/win-fonts/font-cache/font-list 依赖均实存）；**F0 渲染适配（链记录称 2026-09-05 已落地 base.css:22-33）在当前 dev 树不在位**（全库 grep `subpixel` 零命中，base.css 仅有 J5 禁合成粗体块）——见 §七；D1-D5 待拍板、样张未做。
- **挂账面**：台账 §三 PM-10 尾读 / PM-12 font-list 生产 kill 接线两项待拍板维持；R42-40/R43-25/R35 已办结销项。
- **结论：进度 ≈97%**——计划内 24 个阶段 23 个收口（唯余阶段 24 待开工）+ 字体系统待拍板；核心产品闭环（建书→设定→全自动写章→审稿→定稿→导出+全套配套）完整落地且 e2e 覆盖主链。**附加集成警示：dev 集成点落后于链上评审前沿约 147 项修复（§七）**，若按「链上最新已收口态」口径则前沿已完成，但未进 dev。

## 七、链记录对账——R47/PM/R48 修复批与字体 F0 未合入 dev（本轮独有重要发现）

**事实**：
1. dev 分支 HEAD = `8e20061d`（四十五轮收口，2026-09-05 16:44），工作树干净；历史含四十六轮收口 `6a484f3f` 与 R46-12 `18c4a0bf`。
2. 链记录（总览 1.1/台账 §一/Dev README 变更记录）声称四十七轮（修 26 项，vitest 832/5740）、PM 专项（修 10 项）、四十八轮（修 93 项，836/5768）三批修复**已收口**，且字体 F0 **已落地**——各报告自记基线为「HEAD `dfb943d` + 工作树在途未提交改动」。
3. 本轮五项独立标记物在当前 dev 树**全部零命中**：`getForeshadowsCachedAsync`（R48-19）、`scoreAllChunks`（R47-2 流式打分）、`contentBytes`（PM-4）、`useDebouncedSource`（R47-1 防抖族）、`subpixel`（字体 F0）；实测测试计数 814/5645 ≠ 声称的 832→836/5740→5768。

**定性**：三批修复与 F0 改动系在**其他会话/克隆的未提交工作树态**上评审并「收口」的，未提交回 dev；当前工作树干净意味着那批未提交改动不在本机树中。**风险**：若彼会话/克隆的工作树未保留，约 147 项已「收口」的修复面临丢失或需重做；链索引状态（已收口）与 dev 实态（不含）持续分歧也会误导后续轮次基线。
**建议**（待作者拍板）：①确认彼三批所在会话/克隆的位置与存留状态；②若在，按 R47 §8.3 既定口径执行 win→dev（或树→dev）合并后逐项在位核验（链上「下一轮核实」纪律）；③若已丢失，按各报告 §六/§八 修复清单重做（三份报告正本仍在 01-评审/，重做有据）；④此后并行批「收口」前置条件建议加「改动已提交或已合并到 dev」。

## 八、总结论

- **进度 ≈97%**（计划 24 阶段 23 收口，阶段 24 已拍板待开工；字体系统待拍板；dev 集成点落后链上前沿——§七）。
- **质量 A−（8.9/10）**：门禁九件套全绿（814 文件/5645 单测 0 败 + 双 tsc 0 错 + eslint 0 错 0 警 + 三 check + e2e 43/2）；零 P0/P1；P2×3（R49-1 脱敏一行修 / R49-2 围栏单源 / R49-3 README 安全节改写）+ P3×34（维护性债务为主）。较四十八轮报告的 8.6 回升——注意口径不同：本轮评的是**干净 dev 树**（不含在途批次），四十八轮评的是混合在途树。
- **核心判断**：这是一个防御性工程密度极高、测试纪律罕见严格的成熟代码库；剩余问题全部是窄触发窗口的卫生项与维护性债务，无功能性阻断、无数据安全红线失守。

## 九、修复计划（已执行——2026-09-05 修复批同日办结，收口记 §十一）

- **批 A（P2×3，建议优先）**：R49-1 finish.ts 脱敏一行（或 runner 出口单点根治）/ R49-2 purifyBody 围栏与 checkSectionCount 单源收编 / R49-3 README 安全节+lint 口径改写（连带 R49-34）。
- **批 B（P3 一行/低风险顺手修）**：R49-8（改名）/ R49-12（readVersionMeta 迁移）/ R49-15（rmWithRetry×2）/ R49-16（strict 读）/ R49-19（死代码）/ R49-23（量纲名）/ R49-25（复查口径）/ R49-27（分隔符）/ R49-29（Enter 排除）/ R49-31（calc token）/ R49-35（ignores）/ R49-37（删重复行）。
- **批 C（P3 需设计裁量）**：R49-4/5（退出链旗消费）/ R49-7（段级过滤）/ R49-13（精确段名）/ R49-14（纯点名）/ R49-30（防抖收编）/ R49-32（焦点圈）/ R49-36（CI push 腿）/ R49-20（探针标志）。
- **登记/待拍板**：R49-9/10/11/22/33（存疑与既定近似）/ R49-17/18/21/26（容量与权衡类，随路线图）。

## 十、索引同步记

本轮落盘同步四处 .md（评审轮唯一写入面，零源码改动）：本报告落 `01-评审/`（未收口）+ `Dev/Main/README.md` 变更记录（本条）+ `01-评审/README.md`（维护日期 + 当前暂存表回填如实：8 篇）+ 总览 1.1（新增行）+ 台账 §一（新增未收口行）。根 README 计数不动（零测试改动）。确认闸按先例自主执行待追认。

## 十一、修复批收口记（2026-09-05 同日办结）

作者指令「开始修，建议修的全部都修，编排下任务，并发做」——**24 项（批 A P2×3 + 批 B 12 + 批 C 9）全办结，登记不修 13 项维持（§三存疑/权衡标注）**。执行形态：两波文件互斥并行代理（波 1 四路 W1-A/B/C/D，波 2 三路 W2-E/F/G）全成活 + 主审终门亲跑。

**终门实测（mac 本树）**：vitest **821 文件 / 5676 过 + 3 跳 0 败**（124.9s）+ tsc/vue-tsc 0 错 + eslint 0 error/0 warning + 三 check 过〔根 README 修账 814/5645→**821/5676** 四处锚点：徽章/npm test/合入门槛/技术栈；e2e 数不变〕+ e2e 43 过 2 跳（26.3s）。改动面：39 文件改 + 7 新测试文件，+754/−110 行；+31 用例（各路变异验证抽查：R49-1/R49-20/R49-7 三处回退旧代码单跑如期红）。

**修复落点摘要**（逐项 detail 见各路代理输出，此处记锚点）：R49-1 finish.ts redactSecret + r43-sse-error-redact +1 例（真回调驱动）；R49-2 围栏单源收编——新建 `src/format/fence.ts` matchFenceLine，count.ts 与 export purifyBody 两侧同源（export 只认反引号不扩大 ~~~，N-6 口径不变）；R49-3+34 根 README 安全节改写为「GET/HEAD 同过令牌闸、豁免仅 boot+stream」+ lint 口径如实化；R49-4 stopChild 套 shutdownSettleBudgetMs race + 超时 killProcAwaitEscalating 收口（最坏 ~34s→~6s）+ 入口先置停机门堵「被杀在途 fork 误触重启」窄窗；R49-5 三旗提模块级 + 新增 quitDuringCloseFlush——quit 撞 close flush 改 preventDefault 等待、close 链收尾统一复位并补发 app.quit 汇入（多窗态唯一汇入路，FakeWin 假件端到端 3 例）；R49-7 settings.ts 段级 `._` 过滤；R49-8 三处 `_req` 去 `+1 行命名理由注释；R49-12 listVersionEntries 头读快路径（meta 头读失败也回落整读——比计划宽一档，保 fm>64KB 旧版不消失）；R49-13 精确段名锚定 LEGACY_RULES_HEADING_RE（确切标题经 git 考古 install/scaffold.ts 旧版确认）；R49-14 纯点名按无扩展名完整名处理；R49-15 两处 rmWithRetry 收编；R49-16 purgeTrash 定位读 strict → WRITE_ERROR；R49-19 readAllChunks 头注如实化（主审收口期亲修——派工移位遗漏后补办；生产零调用、消费面仅 13 个测试文件作断言原语，头注标明「勿接回召回热路径」+ R46-9 流式召回指向，同 unlinkWithRetry 登记先例，不删免动 13 测试文件）；R49-20 streamChunkScores 返回 lastProducedWasMatch 探针标记，index.ts 条件截断；R49-23 mtimeUs 改名（列名不动——存量库列改名会静默全 miss，量纲注释声明）；R49-25 R34D-22 同款 dirty 复查；R49-27 fpKey 分隔符 `\u0000`；R49-29 Enter 目标 button 豁免；R49-30 MetaFormPanel 接 useDebouncedFmFields；R49-31 Shelf.vue 六 token calc 化；R49-32 CreateBookModal useFocusTrap+ARIA（初始焦点 nextTick 后定向落书名）；R49-35 eslint ignores 补 dist-electron；R49-36 ci.yml 补 push main 腿（concurrency group 按 ref 分组天然隔离，无需改并发逻辑）+ 挂账注释兑现；R49-37 头注重复行删。

**方案偏离/顺手收紧记档**：①R49-4 附带 stopChild 入口先置 shutdownStarted（堵窄窗竞态，注释留痕）；②R49-5 附带 close 撞 quit flush 反向互斥（原「原生放行」改 preventDefault——不修则两链互斥留对向豁口）；③R49-27 顺手清旧键评估后放弃（旧冒号前缀恰是含冒号书名新键的前缀，清了重蹈本项覆辙，注释留痕）；④R49-16 strict 抛错按既有 PurgeResult 信封 WRITE_ERROR 收口（物理删除前移）。

**遗留观察（登记，不阻塞收口）**：a) `src/format/iron-rules.ts` extractSection 的 `/反和解/` 子串提取与 parseAiFlavorRows 段定位未随 R49-13 收紧（不在本批文件面；作者「反和解心得」自建段散文仍可能被拆词扫描，后续触碰顺修）；b) `src/studio/web-next/src/composables/useShelf.ts` migrateBookKeyedState 仍按旧冒号键式迁移 fp 灰显（书改名后新键式标记不随迁——best-effort 展示态，无损伤，建议后续对齐 fpKey）；c) §七 链记录对账（R47/PM/R48 约 147 项 + 字体 F0 未合入 dev）**维持待作者拍板**，不随本批收口。

**待办交接**：工作树 47 处改动未提交（含根 README 修账），待作者指令；§七 三批在途修复的存亡确认与合并/重做决策仍开放。
