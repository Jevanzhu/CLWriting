# 全量代码复审与进度质量评估五十八轮-评审报告-2026-09-06

> 📦 **归档记（2026-09-08）**：已收口，随「README 整理与清理」批移入 `../Archive/`（扁平冷库；确认闸 2026-09-08 修订——规则内免确认留痕）；历史正文不改写。

- 轮次：第四十八轮起算的第五十八轮全量重评（独立评审；**不读取任何既有评审文档**，结论全部基于本轮亲自核实）
- 重点：内存泄漏与性能（用户明示「注意查找内存泄露和性能问题」）
- 基线：win 分支 HEAD `5db3d97c`（五十六轮修复批收口提交）干净工作树
- 指令：作者同文指令「忽略现有的评审文档，重新评审一遍项目所有代码，注意查找内存泄露和性能问题，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。记得编排下任务。然后按照文档的修复计划，来修复找到的问题，全部修复。」
- 路径：经确认闸 AskUserQuestion 获批（链内 `01-评审/`，接五十八轮正典名 + R58-N 编号）
- 状态：**已收口（2026-09-06 评审+修复批+L2 终门同日办结——批 1〔P2×2〕+ 批 2〔P3×10〕全修全配回归；维持登记 4；终门九件套 win 全绿：vitest 910 文件/6081 用例 6004 过 + 77 跳 0 败 + typecheck×2 0 错 + eslint 0 + 三 check 过 + e2e 43 过 2 跳；收口记见 §七）**

## 〇、评审方法

- 范围：`src/` 全部 462 个已跟踪 TS/TSX/Vue 文件（约 10.4 万行）+ 共享层/服务端/前端全链；`test/` 951 文件（约 4.3 万行）只读不评；`node_modules`/`dist`/知识层不在源码评审面。
- 方法：四路只读分域并行扫描（主进程/基础设施、AI 领域核心、Studio 服务端与 SSE/存储、Web 前端 Vue 生命周期与编辑器热路径）→ 全部命中点由主审亲自复核源码（含文件:行号、注释语境、测试锚点）后才入清单；另跑基线：vitest 全量（win）、`npm run typecheck`、`npm run eslint`。
- 着眼模式清单：addEventListener/on/once 与 remove 配对、setInterval/setTimeout 清理、EventEmitter 订阅、AbortController/流 reader 释放、Observer 断开、Pinia 订阅、模块级 Map/Set/数组无界增长、SQLite 句柄、子进程/utilityProcess/Worker 生命周期、SSE/EventSource 清理与重连风暴、大文本热路径（逐键击全量 parse/join/物化）、同步 IO 阻塞事件循环、IPC/HTTP 大 payload、重复读盘与重复建缓存。

## 一、项目完成进度（2026-09-06 实测口径）

- 阶段总览（`Dev/Main/00-总览与实施路线-2026-08-15.md` 第三节）：**阶段 0~23 全部已完成/已收口**；阶段 24「章节结构操作」方案（留洞制 fm `序`/`并入`，D1-D7 已拍板）已落盘，**实施待作者开工指令**——为当前唯一开放实施项。
- 决策状态表：「真开放待拍板：无」（CC-RAG-1 / KN-H-1 / KN-H-2 / FF-F1 / GG-F1 / win D1-D6 均已拍板或销项）；既有挂账/未收口台账见 `00-未收口与挂账-台账-2026-09-04.md`。
- 代码规模（git 跟踪）：1483 文件 = src 462 + test 951 + scripts/resources/知识层等；单测收集 909 文件 / 6080 用例。
- 发布态：v1.0.0-rc.1（tag 已发，发布冒烟绿）；mac dmg / win NSIS 双平台上包。

## 二、项目完成质量（基线实测，win 真机）

| 质量面 | 实测 | 说明 |
|---|---|---|
| 单测全量（win） | **909 文件 / 6080 用例：6003 过 + 77 跳 0 败**（exit 0，267s） | win 平台门（skipIf(win32)）77 项跳过，与既有 win 口径一致；README 宣称 6074（mac/linux 口径） |
| typecheck | `tsc --noEmit` **0 错误** | 全量通过 |
| eslint | 后台基线运行，结果以本次 L2 终门为准 | — |
| 单测规模增长率 | 历史轮次 813/5642 → 909/6080 | 评审链每轮带测试回补，门禁持续收紧 |
| 纪律面 | 生命周期配对、缓存上限、流清理、注释留痕全覆盖 | 见 §三「已核实要点」 |

**质量总评：9.0/10（A）**。理由：当前代码面泄漏纪律与性能意识极高——四路扫描 + 主审复核后**未发现任何 P1 级确认泄漏**；P2 仅 2 项（其一为既有设计取舍的量化残余），P3 卫生项 10 项 + 维持登记 4 项。扣分项为：① App.vue 生命周期钩子错位属真实注册缺陷（虽根组件不卸载无实害，但为全库唯一 on/off 不成对实例，且每次启动产 dev 告警）；② state.ts 网盘副本扫描未节流为常态请求路径同步全树扫描（SMB 卷上秒级冻结）；③ 三处设计取舍（audit 全量重放 / mac 字体超时孤儿 / 大章逐键击全量 fm 合并）登记不修，各有硬约束。

## 三、评审发现（内存泄漏与性能专项）

### P1（确认级泄漏/严重性能）：无

四路扫描 + 主审逐点复核后，未发现无界增长、不可回收的确认级泄漏。重点确认干净的高危面（供反向索引）：

- 事件总线/Store：SSE 心跳、连接记账、背压判死、关闭三路径清理完备（`server/api/stream.ts`）；ticket 有界 256；`listEvents` 已游标流式 + 逐行容错（`events/store.ts:869-918`）。
- 定时器：全库 `setInterval` 仅 2 处（开库标记续期 / 跨进程锁续期），均有 clearInterval 闭环；`setTimeout` 逐处核验——窗口级、探测级、退避级均 finally/closed 清理（**唯一不成对一处见 P3-B-1**）。
- 监听器：preload 三个订阅均返回退订函数成对移除；main.ts 11 处 webContents/window 监听随 'closed' 清理；`process.on` 均为进程生命周期一次性注册。
- 模块级缓存：全部有 FIFO/字节闸/生命周期失效挂点（mdTextCache 4096+64MB、chapterCache 2048、booksReadCache 16、shelfGuards 128、树聚合、RAG、pricing 32……）。
- 句柄/进程：SQLite 全部 open/close 配对；utilityProcess fork 有退避 [0,5s,15s]×3 与 SIGKILL 升级；worker 有 terminate + 资源限制；git spawn 有 TERM→2s→SIGKILL；fs watcher 生产面零命中。
- 前端生命周期：编辑器 EditorView.destroy、SSE disconnect、自动保存 interval、全局监听器 18 处全部成对；store 有界化（doc LRU 20、聊天 200、日志 500、toast 5、审计渲染 2000）；全库 `deep: true` 零命中。
- 热路径：大文本流式读写、导出 worker 卸载、RAG 流式打分、树聚合 async 让出、O(n²) 与循环内正则已清零。

### P2（有界但管理不当 / 中等损耗）——2 项，列入修复批 1

| 编号 | 位置 | 问题 | 证据 | 修法 |
|---|---|---|---|---|
| A-1 | `src/studio/web-next/src/App.vue:22-35` | **生命周期钩子注册错位**：`onBeforeUnmount` 写在 `onMounted` 回调体内，Vue 在该时机无活动组件实例，钩子永不绑定——`offNavigate`/`offMenuAction` 清理函数成为死代码（全库唯一 on/off 不成对实例），且 dev 下每次启动产「no active component instance」告警（测试日志 `r35-sse-backoff-epoch` 已见同款 `[Vue warn]: onUnmounted is called…`） | 见文件，`onMounted(() => { … onBeforeUnmount(() => { offNavigate?.(); offMenuAction?.() }) })` | 订阅句柄提升为 setup 顶层 `let`，`onBeforeUnmount` 提升到 setup 顶层注册 |
| A-2 | `src/state/state.ts:375-403`（配合 `src/git/exec.ts:292-335`） | **网盘副本扫描每 5s 全树同步递归**：`healthCheck` 每次 `detectState`（/api/state 5s TTL）都执行 `scanCloudCopies(bookRoot)`——`readdirSync` 递归全树 + 逐文件 `existsSync` 验母本。R43-2 已把同类全树同步扫（tmp 清扫）按 6h 节流，**本扫描漏网**；SMB/网盘卷上每轮可冻结事件循环数百 ms~秒级 | `state.ts:376`；`exec.ts:307-333`（walk 全树） | 对齐 R43-2 纪律：每书 60s TTL 节流表，过期才真扫；删书失效挂点与测试复位同步补上（语义：冲突副本检出延迟 ≤60s，登记取舍） |

### P3（低风险卫生/建议）——10 项，列入修复批 2

| 编号 | 位置 | 问题 | 修法 |
|---|---|---|---|
| B-1 | `src/desktop/server-manager.ts:310-312` | 子进程稳定窗计时器未存句柄、child 提前退出时不清除——闭包滞留死 `proc` 引用最长 5 分钟并空跑一次回调 | 句柄存局部/active 记录，exit 路径 clearTimeout |
| B-2 | `src/studio/web-next/src/stores/prefs.ts:338-366` | 全局偏好写回 500ms 防抖，关窗前最后一次改动（500ms 窗内）丢失；`__clwFlushBeforeClose` 只冲 doc 不冲 prefs | 新增 `flushPendingPersist()`；App.vue 暴露 `window.__clwFlushPrefs`；main.ts `flushRendererBeforeClose` 表达式串接预 flush（保持返回形状） |
| B-3 | `src/studio/web-next/src/components/panels/CheckPanel.vue:96,120` | 红/黄项 v-for 以数组下标作 key（`'r'+i`/`'y'+i`）——结果重排/删除后 DOM 复用错位 | 改用稳定 `it.checkId` |
| B-4 | `src/ai/orchestrate/chat/turns.ts:244` | `read_chapter` 超限判定前先 `Array.from(body)` 全量物化码点数组（超长章瞬态指数级大分配） | `body.length` 粗判先行，仅在可能超限时才物化判精确值 |
| B-5 | `src/ai/orchestrate/self-heal.ts:956` | `emitMockPreview` 全量 `Array.from(body)` 再 12 码位分片（mock 链路，同构病） | 改码点流式分片（`for…of` 累积，不物化数组） |
| B-6 | `src/cache/rebuild.ts:212` | `sourceProbeLastScan` 以 bookRoot 为键只增不减，删书后条目永驻（无逐书失效挂点） | `forgetChapterParseCacheForBook` 内补 `sourceProbeLastScan.delete(bookRoot)` |
| B-7 | `src/rag/embed.ts:24` | `lastWarnAt` 失败留痕去抖 Map 无上限（键=用户配置端点） | FIFO 上限 64 |
| B-8 | `src/format/global-defaults.ts:85` | `defaultsCache` 按 userDataPath 键无上限（多库/临时目录场景缓涨） | FIFO 上限 16 |
| B-9 | `src/studio/server/api/startup-notices.ts:31-39` | 通告数组无上限（启动迁移 + 改名失败等累计 push） | 环形上限 100（超限 shift 保最新） |
| B-10 | `src/studio/server/api/analysis.ts:49-62` | `styleCorpusCache` 5s TTL 过期条目只在被读时失效，未读则驻留（FIFO 32 本 × 全书语料/样本串，极端可驻留百 MB 级陈旧数据） | 写入时顺手清扫已过期条目（O(n≤32) 廉价，语义不变） |

### 维持登记（P3 观察，**本批不修**，理由如下）——4 项

| 编号 | 位置 | 问题 | 不修理由 / 建议方向 |
|---|---|---|---|
| C-1 | `src/studio/server/api/audit.ts:98-161` + `src/events/store.ts:869-918` | 审计端点每次请求全表读 + `foldSurface` 全量重放（长书几万事件视角：分页只截出网不截解析；首页另附全量 model/human 对照列表） | **PM-10 已登记设计取舍**：`eventsTotal`/`shadowedCount`/首屏全量对照（SV-2）依赖完整事件流，且 `foldSurface` 谓词强依赖 `data` 载荷，拆尾读需数据面改判。建议方向：数据面拍板后按 seq 尾读 + 元数据投影；本批不擅动契约 |
| C-2 | `src/desktop/font-cache.ts:244-299` | mac/linux 字体枚举超时「只放弃等待」不杀子进程（孤儿进程残留）；deps.command 自管 kill 路径生产未接线 | **PM-12 备案**：接线需 mac 打包态验证（asar 内二进制），本机 win 无验证通道；维持登记，随台账拍板 |
| C-3 | `src/studio/web-next/src/views/EditorView.vue:62-99` | 大章每键击 `body` computed 全量 `splitFrontmatter` + `onBodyChange` 全量 `mergeFm`（拼/比两趟）——3~4 趟 O(n) 大分配 | 常态章级（<1 万字）毫秒级无感；R36-6/R31-30 前导空白语义有钉子测试锚定，重写有契约风险。建议方向：doc store 改 fm/正文分离存储（结构性，另立项） |
| C-4 | `src/studio/web-next/src/composables/useSse.ts:105-109` | 429 探测走真实建流后立即 abort（服务端完成鉴权/登记/建 iter 全链后销毁） | 服务端清理链核实完备，仅瞬态建流成本，退避期每轮一次无风暴；可忽略（若要零建流可加轻量 HEAD 探测端点） |

## 四、已核实「设计取舍/有界」要点（非缺陷，反向索引）

- chat 会话历史 LRU 逐出可命中在途对话的书（`state.ts:69-83` 注释登记，>8 本并发才触发）；世界观相关缓存 LRU 8。
- SSE 退避 2s→60s 封顶 + fail-closed 接管 + rebootstrap promise 去重（`useSse.ts`）。
- RAG 索引 10 万块硬截断；journal/字数日记有 compaction；index.db/rag.db 只增不删面均有手册清理入口。
- `scanCloudCopies` 跳过 `.git`/`node_modules`/`.cache`/`.版本`/`.trash`（非浪费面）。
- 事件库 `listEvents` 已游标迭代 + 逐行容错，无 JSONL 重放面（PM-10 更正记录）。
- 同步 IO 全部有 worker 卸载或 async 让出或有节流（`progress.ts`、`snapshots.ts`、`overview.ts`、`export` worker、`rebuild` 3s TTL）。

## 五、修复计划（按作者本次指令执行）

### 批 1：P2×2（验证：`npx vitest run test/state/r43-sweep-throttle.test.ts test/state/n5-finalized-lost.test.ts test/state/r29-doc-wiring-missing.test.ts test/studio/webnext/r50-d1-app-lastbook-route.test.ts` + `npm run typecheck` + `npm run typecheck:web-next`）

1. **A-1** App.vue 生命周期钩子提升（订阅句柄 setup 顶层 + `onBeforeUnmount` 顶层注册；顺带挂 `__clwFlushPrefs` 暴露，见 B-2）。
2. **A-2** state.ts 网盘副本扫描 60s TTL 节流（`cloudScanLastAt` Map + `forgetStateSweepStamp` 补删 + `__resetSweepThrottleForTest` 同步复位；`r43-sweep-throttle.test.ts` 既有测试保持绿）。

### 批 2：P3×10（逐项验证 + 全量回归）

3. **B-1** server-manager 稳定窗定时器成对清理（`test/desktop/server-manager.test.ts`）。
4. **B-2** prefs flush 链（`test/studio/prefs-revision.test.ts` + webnext 相关）。
5. **B-3** CheckPanel 稳定 key（webnext 面板测试）。
6. **B-4** turns.ts 粗判先行（`test/ai/chat.test.ts` 等 read_chapter 面）。
7. **B-5** self-heal 码点流式分片（`test/ai/self-heal-mock-chain.test.ts`）。
8. **B-6** rebuild 失效挂点补删（`test/cache/rebuild.test.ts` 族）。
9. **B-7** embed FIFO（`test/rag/embed.test.ts`）。
10. **B-8** global-defaults FIFO（`test/format/global-defaults.test.ts` + `r64-defaults-cache.test.ts`）。
11. **B-9** startup-notices 环形上限（`test/log/log.test.ts` 等）。
12. **B-10** analysis 过期清扫（`test/studio/d3-style-ttl.test.ts` + `r42-style-corpus-cache-ts.test.ts`）。

### 批 3：维持登记 4 项（C-1~C-4），不修——理由见 §三
### 收口验证（L2 终门）

vitest 全量（909 文件）+ `npm run typecheck` + `npm run typecheck:web-next` + `npm run lint`（对照基线）+ `npm run check:counts` + `npm run check:packaging` + `npm run check:knowledge` + e2e（29 specs，环境可跑则跑，如实记录）。

## 七、收口记（2026-09-06）

- 修复执行：批 1 P2×2（A-1 App.vue 生命周期钩子提升 + 关窗偏好冲刷钩子接线 / A-2 网盘副本扫描 60s TTL 节流）+ 批 2 P3×10（B-1~B-10）全部落地；新增回归测试 1 个（`test/state/r58-cloud-scan-throttle.test.ts`，行为锚定 A-2 节流语义）。
- L1 验证：`tsc --noEmit` 0 错、`vue-tsc --noEmit` 0 错；相关单测 21 文件 255 用例全绿；批 2 扩面复跑 39 文件 368 用例全绿。
- 观察（如实记录）：`test/ai/r54-chat-promptmeta-sanitized.test.ts` 在首次并发批跑（冷缓存首轮）出现过 2 用例红（6.9s/13.4s 超窗），单独复跑 263ms 全绿、同批重跑全绿——该文件不消费本轮任何改动面，判定为并行资源竞争抖动，非本轮引入；建议后续单独调研，不在本轮修复范围。
- L2 终门（win 真机实测）：vitest 全量 **910 文件 / 6081 用例：6004 过 + 77 跳 0 败**（基线 909/6003 之上 +1 文件 +1 用例）；`tsc --noEmit` 0 错；`vue-tsc --noEmit` 0 错；`npm run lint` 0；check:counts / check:packaging / check:knowledge 全过（根 README 按「差 71 恒定」口径修账 909/6074 → 910/6075，win 实跑段同步 910/6004+77）；e2e **43 过 2 跳 0 败**（29 specs 全跑，2 个发布 smoke 未设 `CLWRITING_E2E_RELEASE` 环境变量跳过）。
- 维持登记 4 项（C-1 audit PM-10 全量取舍 / C-2 font-cache PM-12 待拍板 / C-3 大章逐键击 fm 合并 / C-4 useSse 429 探测）：不修理由见 §三，随台账拍板或结构性立项时处置。
- 链同步：`01-评审/README.md` 维护日期条目 + 暂存 14→15 篇；`Dev/Main/README.md` 变更记录条目；总览 1.3 按先例由暂存区索引承接。

## 六、链同步（本报告落盘时执行）

- `01-评审/README.md`：维护日期新条目（本报告未收口状态）+ 当前暂存 14→15 篇。
- `00-总览与实施路线-2026-08-15.md`：1.3 评审区新增本报告行（状态行只记状态/出处）。
- `Dev/Main/README.md`：变更记录新增本报告条目。
