# 全量代码复审与进度质量评估四十二轮（评审报告）

- 日期：2026-09-04。
- 状态：**已收口**（修复批 R42-1~42 全处置 = 修 36 + 维持 5〔登记拍板面〕+ 挂账 1〔R42-40 递归删源待 rmWithRetry 增 recursive 档〕；终门九件套全绿：vitest 788 文件/5514 过 + 3 跳 0 败 + tsc/vue-tsc 0 错 + eslint 0 error〔31 warn 存量〕+ 三 check 过〔根 README 修账 778/5463→788/5514〕+ e2e 43 过 2 跳 + coverage 多桶过；收口记 §九）。
- 基线：HEAD `73d4a3a`（dev 分支，四十一轮修复批收口提交）干净树。
- 作者指令（原文）：「阅读 Dev/Main/01-评审/全量代码复审与进度质量评估四十一轮-评审报告-2026-09-04.md，然后按照文档的修复计划，来修复找到的问题，全部修复。完成修复之后，忽略现有的评审文档，重新评审一遍项目所有代码，要注意mac和win平台的适配问题，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。记得编排下任务。然后按照文档的修复计划，来修复找到的问题，全部修复。修复完成之后，再重新评审一次，注意是重新！不要依靠之前的，从0开始再重新完成一次评审任务，并且形成文档，然后修复问题。」——本轮即指令中的「重新评审一遍」（四十一轮修复批收口后的第一轮从零重评；修后再来一轮从零重评即四十三轮）。
- 报告路径系原指令直接交付物按命名规范落盘（「结果形成一个文档给我」——沿三十二/三十三/三十八/四十一轮确认闸头注记口径；四十二轮正典名 + R42-N 编号；修复批与四十三轮从零重评按同指令随后执行）。
- 禁读口径：全链子代理与主评审均未读既有评审报告（Dev/Main 与项目根历史评审正本一律禁读），从零独立评审；本轮分域角度全新划分（六域，与四十一轮九域不同轴）。
- 编号说明：本报告为 dev 分支链上的第四十二轮全量评审（四十一轮修复批同日收口后启动；评审期间未读任何前轮正本）。

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审口径与执行形态

### 1.1 任务编排

TodoWrite 全程编排：基线门禁亲跑（九件套，含四十一轮收口批的 coverage 复跑）→ 六域禁读子代理**两波各三路**（防 1302 小波次派工——四十一轮九路并发五路阵亡的教训）→ P2 候选主审逐条亲验（9 条全验，2 条改判 P3）→ P3 抽验 → 平台专项归纳 → 进度/质量结论 → 报告落盘 + 索引同步 → 修复批 → 终门 → 四十三轮从零重评。

### 1.2 六域划分与成活情况

| 域 | 范围 | 成活 |
|---|---|---|
| A | 核心数据（document/ fs/ state/ events/） | ✅ |
| B | 服务端与前端逻辑层（studio/server/ + web-next api/stores/composables/shared） | ✅ |
| C | AI 链路（ai/ 编排·适配器·计价·记账·rules·prompts·contract） | ✅ |
| D | 前端组件层（web-next components/views/pages/editor/styles） | ✅ |
| E | 桌面·进程·安装·导出·机检·格式（desktop/ process/ install/ export/ format/ check/ review/） | ✅ |
| F | 平台适配专项 + 基建（grep 横切 + cache/driver/git/knowledge/learn/log/metrics/rag） | ✅ |

两波 3+3 全部首发成活**零阵亡**（小波次派工生效）。评审面 = src 全量（后端 71,588 行 TS + web-next 25,029 行 vue/ts ≈9.7 万行）+ 根配置全套。E 域自报完成度 80%（format style 族外围与 process 外围编排粘合层未逐行，主审以 F 域横切扫描互补覆盖）。

### 1.3 亲验纪律

- **P2 候选×9 全部主审逐条对照源码亲验**：R42-1（stream.ts:872-882 + useSse.ts:128-170 链路核对）、R42-2（pricing.ts:55-66 逐行）、R42-3（Toast 全库唯一挂载点 grep + App.vue 模板核对）、R42-4（三模态挂载点 + useAppActions 派发面 + router 默认路由核对）、R42-5（check/run.ts 三组 join 点 :142-151/:484-485/:560-561 逐段）、R42-6（learn:160 + metrics/style:190/217 + book-search:87/211 四点）——**6 条成立，2 条改判 P3**（C-2 双事件仅 trace-stats 观测面失真、recordUsageSafe 未重复调用无计费影响；F-2 与 R38-9/R41-6 `.MD` 家族 P3 先例同口径），**1 条降级登记**（D-8 系测试覆盖债非运行时缺陷，作 P3 登记）。
- P3 抽验 5 条通过（A-1 宽松读位置 / A-3 正则原文 / B-2 同毫秒墓地路径 / E-3 yaml BOM 三处已修点对照 / F-5 改名路径无 normalize 对照 init.ts:105）；其余 P3 由代理提供文件:行号级证据，修复批实修时逐条再核。

## 二、门禁基线（主审亲跑九件套，对基线 HEAD 实测）

| 门禁 | 结果 |
|---|---|
| vitest 全量 | **778 文件 / 5463 过 + 3 条件跳，0 败** |
| tsc --noEmit | 0 错 |
| vue-tsc --noEmit | 0 错 |
| eslint | 0 error / 31 warn（存量口径） |
| check:counts | 过（773/5436→778/5463 修账后零漂移） |
| check:packaging | 过 |
| check:knowledge | 过 |
| e2e | **43 过 2 跳**（27.4s） |
| coverage | **过**（events 桶 95.88/90.41/100/95.88——四十一轮收口终门发现并补覆盖的存量缺口回升至门上） |

基线树含四十一轮修复批（R41-1~16）全部改动，经本轮六域从零深审**无回归**。

## 三、发现清单（R42-1 ~ R42-42）

分级：P0×0 / P1×0 / **P2×6** / **P3×36**，共 42 项。平台标注：[win]/[mac]/[both]/[n/a]。主审改判 2 条、降级 1 条已按最终口径归位（原代理定级记档于各条）。

### 3.1 P2（×6）

**R42-1〔both〕清空对话后事件流必经 403+退避断流（约 3-5s）** — B 域，主审亲验成立

- 证据：`src/studio/server/api/stream.ts:877-879`（chat.clear → clearChatHistory + forgetSseCount = 本书在途 SSE 全销毁，S2 设计「强制换新连接」）；`src/studio/web-next/src/composables/useSse.ts:141-167`（浏览器 EventSource 以同 URL（含**已消费的一次性 ticket**）自动重连 → 服务端 403 → fail-closed → 前端接管指数退避首档 2s → doConnect 重取票才恢复）。
- 触发：主路径功能「清空对话」每次必触发；断流窗内 connected 徽章闪灰、对话事件丢失到重连 sync。自愈无数据损坏，故 P2 非 P1。
- 修复：fail-closed 接管时非 429 形态（403 族 = 票失效类）首档立即（0ms）换票重连，429 维持退避。

**R42-2〔n/a〕价格表按模型 id「首归属 provider」查表，同模型 id 挂多家时计价错位** — C 域，主审亲验成立

- 证据：`src/ai/pricing.ts:58` `const owner = store.providers.find((p) => p.models?.some((m) => m.id === model))`——按 providers 数组序取第一家，与当前启用 provider 无关。
- 触发：Provider A（官方价 X）与 B（中转价 Y）都登记 `gpt-5.1`、当前启用 B 生成 → 按 X 计价；chapter 记账/预算闸/self-heal done/cost-stats 全链系统性失真，且切 provider 不改写归属。
- 修复：resolveModelPricing 先在**当前启用 provider** 的 models[] 内找归属，未命中回落全局首归属。

**R42-3〔both〕Toast 仅挂载于工作区外壳——/welcome、/library、书库独立窗口上的 ui.toast 全部静默失效** — D 域，主审亲验成立

- 证据：全库唯一挂载点 `WorkspaceShell.vue:182`；`App.vue:42-47` 模板无 Toast；`Welcome.vue:49`、`Library.vue:60/69` 的 `ui.toast(...)`（switchLibrary 取消原因、openLibraryDir 失败）在这些路由上无渲染点。
- 触发：书库失效落 /welcome、书库管理独立窗口（渲染 Library.vue）——错误反馈通道整页死区，store 内 5s 计时白起停。
- 修复：`<Toast />`（顺带核查 ConfirmPrompt 同形态）上移 App.vue 全局挂载，一次修复全部独立路由/子窗口。

**R42-4〔both〕系统菜单「偏好设置/新建书/导出」在非工作区路由上静默空操作** — D 域，主审亲验成立

- 证据：`src/desktop/main.ts:955/971/985`（CmdOrCtrl+, / Cmd+N / Cmd+E 恒发主窗）→ `App.vue:21` dispatchAction → `useAppActions.ts:22-24`（ui.openSettings/openShelf/openExport 只置 store 标志位）——而 SettingsModal/ShelfModal/ExportDialog 仅挂载于 `WorkspaceShell.vue:179-181`；`router.ts:8` 默认路由 `/shelf`（新装首启/返回书架后主窗即在此）。
- 触发：/shelf、/welcome 页按全局加速键或点菜单——点了毫无反应，导出/设置主入口阶段性不可达且无提示。
- 修复：三动作在非工作区路由先导航回工作区再开模态（或模态随 Toast 上移 App.vue，按组件对工作区上下文的依赖实测定法）。

**R42-5〔win〕check/run.ts 三组 join 键无折叠——case-only 改名后机检假红阻断定稿** — E 域，主审亲验成立

- 证据：`src/check/run.ts:142-151`（maxWrittenChapterOf finalized 集精确串）、`:484-485/613`（pathToDocId）、`:560-561/603`（entryByPath）——R38-14/R41-2 折叠族在 tree/state/export/overview/service 全收编后，机检链漏网。
- 触发：win 资源管理器 case-only 改名定稿章 → `maxWrittenChapterOf` 低估 → checkLeadsForm 以偏低基准跑 → `lead-chapter-future` **假红**驱动自愈打回/LEAD_GATE 阻断定稿（纯外观改名造成主路径功能断裂）；collectTreeIssuesCore 侧 case 漂移草稿章静默漏红。
- 修复：三组 join 两侧统一 `docJoinKey`（与 tree.ts 同款迁移）。

**R42-6〔win〕learn/文风重扫/定稿搜索三处定稿集 join 无折叠——收割静默跳过/文风样本丢失/搜索不可见** — F 域，主审亲验成立

- 证据：`src/learn/index.ts:160`、`src/metrics/style.ts:190/217`、`src/process/book-search.ts:87/211`（同步/异步）——`finalizedPathSet` 返回登记原文路径，四处消费均精确串比较（仅 `\\`→`/` 归一）。
- 触发：与 R42-5 同前提（case-only 改名 / NFD 文件名）→ ① learn 收割把定稿章当草稿跳过（样章/金句候选静默丢失）；② 文风趋势/漂移判定丢样本；③ 搜索 scope=定稿 该章消失。
- 修复：四处比较面 set/has 两侧统一 `docJoinKey`；补「case-only 改名后 learn/health 仍认定稿」回归。

### 3.2 P3（×36，分域明细）

**A 域（核心数据，7 项）**

- **R42-7〔win〕executeSave 回收站复活守卫用宽松读** — `src/document/service.ts:330/407`：Z-6 守卫复用只读面 `readTrashManifest`（读失败返 []），EBUSY/EACCES 瞬态窗内已软删文档可复活双份；写路径族 R27-40 已建 strict 版唯此守卫漏网。修复 = 换 readTrashManifestStrict。
- **R42-8〔both〕unfinishedPieceNames 定稿集未随 R41-2 收编** — `src/state/state.ts:646/655`：同文件 findUnfinishedChapter 已 docJoinKey、此处仍精确串；近况复述 off-by-one（被章号侧兜住，显示口径问题）。修复 = 两侧同升 docJoinKey。
- **R42-9〔n/a〕chapterFromFile 兜底正则不要求分隔符** — `src/state/state.ts:612` `^0*(\d+)` 接受任意数字开头名（`2023复盘.md`→2023）→ 数字开头备忘文件劫持态 4「中断章」。修复 = 对齐 `^0*(\d+)(?:[-—]|\s|$)`。
- **R42-10〔win〕move/rename/软删三处删源裸 rmSync** — `src/document/service.ts:1359/1690/1103`：退避族已收编 trash/lead-finalize 删源，此处漏网——杀软扫新链接时 EPERM/EBUSY 直接触发回滚链整操作失败（可重试，鲁棒性）。修复 = 换 rmWithRetry。
- **R42-11〔both〕lead-finalize 布线文件双重整读** — `src/document/lead-finalize.ts:247/263`：解析读与编码判据读分离（单读派生纪律漏网）+ 双 IO 放大。修复 = 一次整读 Buffer 同源派生。
- **R42-12〔win〕卷纲 stem 收集 .md 大小写敏感且无 NFC** — `src/document/tree.ts:159/314-320`：`.MD` 卷纲/NFD 卷名关联静默缺失（展示层）。修复 = 大小写不敏感 + toNfcName 归一比对。
- **R42-13〔n/a〕findUnfinishedChapter 依赖 readdir 顺序** — `src/state/state.ts:586-593`：多草稿时态 4 报哪个章号取决于目录序。修复 = 收集后取最小章号。

**B 域（服务端与前端逻辑层，6 项）**

- **R42-14〔both〕并发双删同书同毫秒同墓地路径 → 500 文案与事实相反** — `src/studio/server/api/books.ts:391-409`：第二 rename ENOTEMPTY → 500「书未受影响，可重试」（实际已删成）。修复 = 墓地名加 ULID 后缀或 ENOTEMPTY/EEXIST 同 ENOENT 按「已删」收口。
- **R42-15〔n/a〕apiJson 超时预算被 rebootstrap 吃掉** — `src/studio/web-next/src/api/client.ts:109-148`：401→re-boot（最坏 ≈22s）期间计时器照走 → 假 TIMEOUT。修复 = rebootstrap 期间暂停/重置计时。
- **R42-16〔both〕analyze-style 缓存条目用扫描前时间戳** — `src/studio/server/api/analysis.ts:574/606`：TTL 被扫描时长稀释（只会多扫不会读陈）。修复 = set 时取当刻。
- **R42-17〔n/a〕API 面永不 405** — `src/studio/server/index.ts:412-441`：方法错一律 404。**维持登记**（项目信封统一口径，机器消费方未见实际受害面；改 405 属契约变更需拍板）。
- **R42-18〔both〕SSE `?token=` 兼容通道仍活跃** — `stream.ts:260-279` + `useSse.ts:133-139`：token 进 URL 暴露面（R30-25 已登记移除条件未满足——前端回退仍在）。**维持登记**。
- **R42-19〔win〕卸载兜底同步 XHR 系 Chromium 废弃特性** — `src/studio/web-next/src/stores/doc.ts:492-536`：`xhr.open(..., false)` 在 unload 窗口——Chromium 已列废弃，未来版本禁用即静默失效（有 .版本 快照兜底）。**维持登记 + 实机复验挂账**。

**C 域（AI 链路，7 项）**

- **R42-20〔both〕退避 sleep 中 abort 同 attempt 落两条 llm/call 事件**（代理原判 P2，主审改判 P3：仅 trace-stats 观测面，计费/账本不受影响） — `src/ai/runner.ts:640/647-649`：error 事件后 sleep-abort 再落 ABORTED 事件 → trace-stats 计数/avgAttempts/successRate 虚涨。修复 = sleep-abort 分支不重复 trace。
- **R42-21〔both〕resetDegradedChannels 同 path 重连注释承诺不成立** — `src/ai/runner.ts:208-211/231`：测试钩子文档化承诺与守卫行为不符（生产零影响）。修复 = 对齐语义（钩子重置 degradedActivePath 或改注释）。
- **R42-22〔both〕cost-stats currency 缺省 'USD' 承诺未实现** — `src/ai/cost-stats.ts:33-34/122/136`。修复 = `currency ?? 'USD'` 或修注释（按前端消费面定）。
- **R42-23〔both〕仅设 currency 的模型行价格覆盖被 isPriced 闸整体丢弃** — `src/ai/pricing.ts:40-42`：与 R42-22 同族。修复 = 放宽为「含任一已知键参与合并，最终 isPriced(merged) 收口」。
- **R42-24〔both〕探测降级记忆按活跃 path 而非探测目标 path 路由** — `src/ai/provider/probe.ts:67` → store.ts:473-481：多库桌面 B 库测试连接的降级记忆写进 A 库 providers.json。修复 = probeCapabilities 透传 userDataPath。
- **R42-25〔both〕httpStatusToCode 的 context 正则过宽** — `src/ai/provider/failure.ts:24`：`invalid context id` 类文案误归 CONTEXT_WINDOW_EXCEEDED。修复 = 短语级收紧。
- **R42-26〔both〕chat 失败出口对中断/档位超时统一落 'error' 遮蔽** — `src/ai/orchestrate/chat/turns.ts:533`：与轮首同因出口口径分裂（ABORTED→error 而非 interrupted）。修复 = finishTurn 按 out.code 分流。

**D 域（前端组件层，6 项）**

- **R42-27〔both〕AuditView 整页 rem 硬编码字号** — `src/studio/web-next/src/views/AuditView.vue:349-530`：绕过 `--font-size-step` 偏好与 token 体系。修复 = 换 var(--font-size-*) 档位。
- **R42-28〔both〕ModelEffortBar.fitSelect 用 value 测宽而显示的是 label** — `ModelEffortBar.vue:25/59`：显示名被裁或留白。修复 = 取 selectedOptions[0].textContent。
- **R42-29〔both〕TooltipHost 宽度启发式全按全角估** — `TooltipHost.vue:38-39`：ASCII tooltip 估宽约 2 倍。修复 = 按码位分类累加。
- **R42-30〔both〕叠层弹窗一按 Esc 多层同关** — `SettingsModal.vue:95-105` + `ShelfModal.vue:69-82`：层间无让渡互检（useHotkeys 有完整名单先例）。修复 = 补 overlay 让渡检查。
- **R42-31〔both〕书架独立窗口 openBook IPC 失败零反馈** — `pages/Shelf.vue:73-74`：void 无 catch → unhandledrejection。修复 = 补 .catch 留痕。
- **R42-32〔both〕34 个 .vue 组件零测试引用**（代理原判 P2 测试盲区，主审降级 P3 登记） — 整页零覆盖：Welcome/Library/App；逻辑组件零直测：EditorDocHead/RelationGraph/WbHealCard 等。D-1/D-2 形态（挂载层级回归）正落在零覆盖页。修复 = 补 App 级路由×模态挂载回归 + Welcome/Library mount 冒烟（本批最小面），其余按优先级滚动补。

**E 域（桌面·进程·安装·导出·机检，5 项）**

- **R42-33〔win〕book-search 定稿 scope join（并入 R42-6 同批）** — `src/process/book-search.ts:87/211`。
- **R42-34〔win〕book.yaml 解析本体不剥 BOM** — `src/format/yaml.ts:83-135`：`\uFEFFspec_version` 键被静默丢弃（frontmatter/books.jsonl/matchesKeyLine 三处已修，解析器本体漏网）；`kind: short` 首行形态被静默路由长篇轨。修复 = 入口剥 BOM 一次。
- **R42-35〔mac/win〕建书大小写判重只比登记名** — `src/install/books.ts:259` + `src/install/init.ts:159-164`：大小写不敏感卷上 `Foo`/`foo` 判重绕过 → 半成品续跑路径双条目同库 → 删任一 rmSync 共用目录。修复 = appendBookLocked 补 samePath 目录占用检查。
- **R42-36〔both〕卷摘要 sourceHash 对整文件 grep** — `src/process/summary.ts:581/660/692`：正文含行首 `sourceHash:` 时误判 fresh/stale（章摘要侧已限定 fmRaw）。修复 = 三处对齐 splitFrontMatter。
- **R42-37〔n/a〕export format 参数不门控短篇投稿视图** — `src/export/index.ts:547`：`format:'split'` 仍产投稿视图。**裁决项**——format 语义圈定全本/分章两产物面、投稿视图系短篇主产物独立面，维持现行为 + 注释言明（不当代码修）。

**F 域（平台专项 + 基建，5 项）**

- **R42-38〔win〕cache/rebuild 摘要扫描 .md 大小写敏感**（代理原判 P2，主审按 R38-9/R41-6 家族先例改判 P3） — `src/cache/rebuild.ts:394/408/413`：`.MD` 摘要静默不入库且不进健康报告（过滤先于命名白名单）；同函数章扫描（walk-md）大小写不敏感——单函数内两口径。修复 = isMdFileName + 大小写不敏感剥尾。
- **R42-39〔win〕.MD 家族残余精确匹配点（横切清单）** — version.ts:333（版本档案列表失明）、tree.ts:131/159、foreshadow.ts:72/159、overview.ts:147、settings.ts:35/268/327、files.ts:230/244、snapshots.ts:94、setting-rule.ts:130/142、init.ts:222、books.ts:447、migrate-layout-v3.ts:110、knowledge/manifest.ts:141（`.MD` target 跳过 fm 校验）。修复 = 统一 isMdFileName（单源既立，逐点替换）。
- **R42-40〔win〕trash purge 连删族与伏笔归档删源裸 rmSync** — `src/document/trash.ts:423/432/437` + `src/document/foreshadow.ts:136/141`：退避族文档宣称收编伏笔归档但实现未到。修复 = 五处换 rmWithRetry（或修 atomic.ts 头注对齐文档）。
- **R42-41〔mac〕书名 NFC 归一建书有、改名无** — `src/studio/server/api/books.ts:477-499` 无 normalize（init.ts:105 有）——改名入口重新打开「NFD 跨机找不到文件」病灶，且注释承诺的存量迁移 v4 已拆除无自愈。修复 = parse 段补 normalize('NFC')。
- **R42-42〔mac〕折叠族静态 platform 判定对 mac 不敏感卷脱钩** — R40-23/R41-13 同口径登记项，本轮独立再发现（三入口 warnIfCaseSensitive 实测拦截在位，残余风险窄）。**维持登记**（probeCaseSensitive 驱动折叠涉 bookHash 存量迁移，收益/风险比不足）。

## 四、平台适配专项结论（mac/win）

**win 侧**：windowsHide 全 spawn（git 双入口 + PowerShell + utilityProcess 唯三子进程）、退避族主面（atomic 写全链 + trash/lead-finalize 删源）、保留名三单源（含 HTTP 请求面拦截）、trueCasePath 大小写归一、SystemRoot 兜底、env 大小写清除——经本轮独立横切**全部在位**。残余集中在**同构家族收编不彻底**：折叠族三漏网点（R42-5/R42-6/R42-8——R38-14/R41-2 家族）与 `.MD` 家族两波及面（R42-38/R42-39——R38-9 家族）。域 F 结论原文：*「家族收编缺少终态盘点机制——每轮修复点名若干调用点，无人核对全部同构点是否清零」*——本轮修复批按「终态盘点」口径一次收口两个家族的全部残余点。

**mac 侧**：NFC/LF/无 BOM 规范形写侧单源、文件名 NFC 入口归一（建书面）、case-probe 三入口实测拒入、ad-hoc 签名 identity "-" 语义正确、AppleDouble/网盘冲突副本检测均在位。实质缺口 = R42-41（改名入口 NFC 缺失，一行修）与 R42-42（维持登记）。NFD join 面已由 R41-2 docJoinKey 收编，本轮 R42-5/6/8/12 补齐残余消费点后 mac 分解形身份面完整。

**跨平台**：spawn 三处全 windowsHide、git 超时升级 kill 链、双进程锁族、编码容忍族（BOM/CRLF 读侧）横切无新缺口。

## 五、进度结论

**总体 ≈98%（mac 生产形态 ≈98.5% / win RC ≈97.5%）**，与三十八~四十一轮口径一致（本轮 0×P0/0×P1；P2×6 中 2 条系 R41 轮前端域子代理阵亡导致的覆盖盲区存量首次曝光〔R42-3/R42-4〕，非新增回归；2 条系 R38-14/R41-2 折叠家族残余点首次全量盘点曝光〔R42-5/R42-6〕；其余 2 条为窄面）。

- 分域完成度：A ≈94%；B ≈92%；C ≈96%；D ≈96%；E ≈80%（自报，外围粘合层由 F 域横切互补）；F ≈95%。B/E 偏低 = 域内登记维持项多；本轮 P2 承载域 = 前端外壳盲区（D）+ 折叠族残余（E/F）。
- 开放任务（对照总览第三节，维持不变）：阶段 13 relay 真机验证（待凭据）、阶段 21 J6b mac 跨机书库复制实机确认、CI 形态/签名公证（作者拍板项）。

## 六、质量结论

**A−（8.8/10）**。依据：

- 门禁九件套全绿基线（778 文件/5463 单测 0 败 + e2e 43 过 2 跳 + 双 tsc 0 错 + eslint 0 error + 三 check 过 + coverage 多桶过）；
- P0/P1 连续多轮为零；P2×6 较四十一轮（3 条）回升，但成因是**覆盖面扩张**（本轮六域全成活 vs 四十一轮五域阵亡由主审薄补）与**家族终态盘点**（首次全量清点折叠族/.MD 族同构点）而非质量回退——四十一轮修复批（R41-1~16）经本轮从零深审零回归；
- 扣分项 = R42-3/R42-4（前端外壳结构性盲区：错误反馈与主菜单入口在非工作区页整面死区）+ R42-5/R42-6（机检/收割/文风/搜索四链折叠族分裂——同数据两口径）+ R42-2（多 provider 计价错位）。

## 七、修复计划（批 A-C，文件互斥）

| 批 | 项 | 文件面 | 修法 |
|---|---|---|---|
| A | R42-1 | web-next useSse.ts | fail-closed 非 429 形态首档 0ms 换票重连（429 退避维持）；补回归 |
| A | R42-2 | ai/pricing.ts | 归属查表先当前启用 provider 再全局首归属；补双 provider 同 id 用例 |
| A | R42-3 | web-next App.vue + WorkspaceShell.vue | Toast（核查 ConfirmPrompt 同形态）上移全局挂载；补 /shelf 路由 toast 渲染回归 |
| A | R42-4 | web-next useAppActions.ts（或模态上移随 R42-3 定） | 三动作非工作区路由先导航再开模态；补回归 |
| A | R42-5 | check/run.ts | 三组 join 两侧 docJoinKey；补 case-only 改名机检回归 |
| A | R42-6（含 R42-8/R42-33 同族） | learn/index.ts + metrics/style.ts + process/book-search.ts + state/state.ts | 定稿集/比较面 docJoinKey 成对替换；补收割/文风认定稿回归 |
| B | R42-7 | document/service.ts | Z-6 守卫换 readTrashManifestStrict |
| B | R42-9 | state/state.ts | chapterFromFile 正则加分隔符边界 |
| B | R42-10 | document/service.ts | 三删源点 rmWithRetry（与 R42-7 同文件合批） |
| B | R42-11 | document/lead-finalize.ts | 单读派生（整读 Buffer 同源） |
| B | R42-12 | document/tree.ts | 卷纲 .md 大小写不敏感 + toNfcName 比对 |
| B | R42-13 | state/state.ts | 未定稿章收集取最小 |
| B | R42-14 | studio/server/api/books.ts | 墓地名 ULID 后缀（或 ENOTEMPTY 按 404 收口）；R42-41 同文件合批 |
| B | R42-15 | web-next api/client.ts | rebootstrap 期间暂停超时计时 |
| B | R42-16 | studio/server/api/analysis.ts | 缓存 ts 取 set 当刻 |
| B | R42-20 | ai/runner.ts | sleep-abort 分支不重复 trace |
| B | R42-21 | ai/runner.ts | resetDegradedChannels 语义对齐（与 R42-20 同文件合批） |
| B | R42-22 + R42-23 | ai/cost-stats.ts + ai/pricing.ts | currency 缺省 + 覆盖合并放宽（pricing 与 R42-2 同文件先 A 后 B） |
| B | R42-24 | ai/provider/probe.ts | 透传 userDataPath |
| B | R42-25 | ai/provider/failure.ts | context 正则短语级收紧 |
| B | R42-26 | ai/orchestrate/chat/turns.ts | finishTurn 按 code 分流 interrupted/timeout |
| B | R42-27~31 | web-next 组件五处 | AuditView 字号 token 化 / fitSelect 取 label / Tooltip 码位测宽 / Esc 层间让渡 / openBook catch |
| B | R42-32 | test/studio/webnext 新增 | App 级路由×模态挂载回归 + Welcome/Library mount 冒烟（最小面） |
| B | R42-34 | format/yaml.ts | 解析入口剥 BOM |
| B | R42-35 | install/books.ts + install/init.ts | appendBookLocked 补 samePath 目录占用判重 |
| B | R42-36 | process/summary.ts | 三处 sourceHash 限定 fmRaw |
| B | R42-38 | cache/rebuild.ts | isMdFileName + 大小写不敏感剥尾 |
| B | R42-39 | 13 文件 .MD 残余点 | 统一 isMdFileName（终态盘点） |
| B | R42-40 | document/trash.ts + document/foreshadow.ts | 五删源点 rmWithRetry |
| B | R42-41 | studio/server/api/books.ts | rename parse 段 normalize('NFC')（与 R42-14 同文件合批） |
| 维持 | R42-17/18/19/37/42 | — | 405 契约 / token-in-URL 移除条件 / 同步 XHR 实机挂账 / 投稿视图 format 语义 / mac 折叠登记——五项登记记档（作者拍板面） |

执行口径：批 A（P2×6）主审亲修先行；批 B P3×30 按文件互斥推进；每项补回归测试；终门九件套（vitest 全量 + tsc/vue-tsc + eslint + 三 check + e2e + coverage）。

## 八、四十三轮预告

按作者同文指令，修复批收口后启动**四十三轮从零重评**（不读本轮与前轮正本、全新扫描角度、小波次派工），形成第三份报告并修复其发现。


## 九、收口记录（2026-09-04，修复批办结）

修复批 R42-1~42 全处置（修 36 + 维持 5 + 挂账 1），+10 测试文件/+51 用例；根 README 修账 778 文件/5463 单测 → 788 文件/5514 单测。执行形态：七路文件互斥并行子代理（join 族 / AI 域 / document 域 / 服务端安装缓存 / yaml+summary / .MD 终态盘点 / 前端组件 P3）全部首发成活零阵亡 + 批 A 前端三项（R42-1/3/4）主审亲修。

- **R42-1**：useSse fail-closed 首档退避 2s→0ms 立即换票重连（第 2 档起 4s/8s/… 指数不变）——清空对话后事件流断流 3-5s → 毫秒级。sse-reconnect 四处契约断言随行更新 + r73-sse-429-guide afterEach 排干级联（0ms 定时器在测试生命周期内会触发的新形态）。
- **R42-2/R42-23**：resolveModelPricing 归属查表先当前启用 provider 再全局首归属；模型行 pricing 覆盖放宽为「含任一已知键参与浅合并、最终 isPriced(merged) 收口」——currency-only 行可生效但不单独计费。
- **R42-3/R42-4**：Toast/ConfirmPrompt/SettingsModal/ShelfModal/ExportDialog 五件上移 App.vue 全局挂载（均 Teleport to body / fixed、store 驱动无 props），WorkspaceShell 去重——非工作区路由/书库独立窗口的错误反馈与三菜单入口复活；新增 r42-global-overlays 回归（toast 渲染点/设置模态/静态防双挂载锚）。
- **R42-5/R42-6/R42-8/R42-33（join 族终态收口）**：check/run.ts 三组 join（maxWrittenChapterOf/pathToDocId/entryByPath）+ learn + metrics/style 同步异步 + book-search 同步异步 + state.unfinishedPieceNames 全部 docJoinKey 双侧成对——R38-14/R41-2 折叠族至此全量清点收口；test/check/r42-join-fold.test.ts 十臂（win/posix 互证）。
- **R42-7**：executeSave Z-6 守卫（取锁前 + 锁内复核两处）换 readTrashManifestStrict，读失败按保守拒绝（fail-closed）。
- **R42-9**：chapterFromFile 兜底正则加 `(?:[-—]|\s|$)` 边界。
- **R42-10**：service 三删源点（move/rename/doTrash/syncRenamePieceList）rmWithRetry。
- **R42-11**：lead-finalize 单读派生（整读 Buffer 同源；readLeadFromBytes 消费侧最小解析孪生，带同步契约注释）。
- **R42-12**：卷纲 stem 收集 isMdFileName + 盘上真名 Map；卷名 join 两侧 toNfcName，命中构造盘上真实路径。
- **R42-13**：findUnfinishedChapter 未定稿集收集后取最小章号（消除 readdir 序依赖）。
- **R42-14**：删书墓地名加 ULID 后缀 + ENOTEMPTY/EEXIST 同 ENOENT 按 404 收口（并发双删双保险）。
- **R42-15**：apiJson 超时计时在 rebootstrap 等待期暂停（gauge pause/resume）。
- **R42-16**：styleCorpusCache ts 取 set 当刻（变异验证过）。
- **R42-20**：退避 sleep-abort 分支不再重复 trace（同 attempt 单事件）。
- **R42-21**：resetDegradedChannels 注释对齐实况（运行时零改动）。
- **R42-22**：cost-stats currency ?? 'USD'。
- **R42-24**：probeCapabilities 透传 userDataPath（探测降级记忆落目标库；providers.ts 调用链同步）。
- **R42-25**：httpStatusToCode context 正则短语级收紧（invalid context id 不再误中）。
- **R42-26**：chat 失败出口 finishTurn 按 out.code 分流 interrupted/timeout。
- **R42-27~31**：AuditView rem→token 档位 16 处；fitSelect 取 selectedOptions label；TooltipHost 码位分类测宽；SettingsModal/ShelfModal Esc 层间让渡；Shelf openBook 补 catch。
- **R42-32（最小面）**：r42-shell-mount（label 测宽/Tooltip 估宽/openBook catch）+ r42-global-overlays（本批）——34 组件零覆盖清单按登记滚动补。
- **R42-34**：parseBookConfig 入口窄剥 BOM（实证漏网点为 BOM 计入首行缩进致子键弹栈丢失）。
- **R42-35**：appendBookLocked 补 samePath 目录占用判重（Foo/foo 双登记同库堵死）。
- **R42-36**：卷摘要 sourceHash 三处收口 volumeSourceHash（splitFrontMatter 限定 fmRaw）。
- **R42-38**：cache/rebuild isMdFileName + 大小写不敏感剥尾。
- **R42-39**：.MD 家族终态盘点 12 点收编（version/overview/settings/files/snapshots/setting-rule/migrate-v3/knowledge-manifest；migrate-v3 与 knowledge 两点语义从「跳过」转「纳入」按修复目的）。
- **R42-40**：trash purge 连删族两处（分析信封/journal）+ foreshadow 归档删源两处 rmWithRetry；**挂账**：trash.ts 版本目录递归删（rmWithRetry 无 recursive 档，误收编恒 EISDIR——待原语增档后收编，注释记档）。
- **R42-41**：书名 rename parse 段 normalize('NFC')；init.ts v4 注释更正。
- **维持 5**：R42-17（405 契约）/R42-18（token-in-URL 移除条件未满足）/R42-19（同步 XHR 实机复验挂账）/R42-37（投稿视图 format 语义，裁决维持现行为）/R42-42（mac 折叠 R40-23 同口径）。

终门（全量亲跑）：vitest **788 文件/5514 过 + 3 跳 0 败**（91.16s）+ tsc 0 错 + vue-tsc 0 错 + eslint 0 error（31 warn 存量）+ check:counts 过（修账后零漂移）+ check:packaging 过 + check:knowledge 过 + e2e **43 过 2 跳**（27.3s）+ coverage 复跑过（全桶零违例；ai 桶 95.69/events 桶 95.88）。