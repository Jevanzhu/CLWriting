# 全量代码复审与进度质量评估三十六轮——评审报告

- 轮次：第三十六轮 = 链第九十六轮
- 日期：2026-09-01
- 状态：**已收口**（2026-09-01 修复批全办结，收口记见 §十）
- 基线：HEAD `d206e99`（fix(review): 三十五轮修复批收口——46 项全办结）干净工作树
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，告诉我项目完成进度，完成质量，最后形成一个文档给我。记得编排下任务。」
- 确认闸：报告路径经 AskUserQuestion 报作者**获批**（正典名「三十六轮」+ R36-N 编号）
- 评审组织：TodoWrite 全程编排；**七路分域禁读子代理两波（4+3）从零深审全部首发成活零阵亡** + 主评审逐条亲验 P1/P2 + 门禁九件套亲跑

---

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审面与组织

本轮按作者指令**忽略全部既有评审文档**（禁读 `Dev/` 全链与一切历史报告），七路分域子代理独立从零深审；子代理全程禁读 `Dev/`，仅以根 `README.md` 为规格基线、总览第三/五节为规划对照基线（该两份仅在确认「进度对照基准」时由主评审读取，子代理不读）。

**评审面盘点（实测）**：

| 面 | 规模 |
|---|---|
| src/ 非 web-next TS | 263 文件 ≈68,056 行 |
| src/studio/web-next（ts+vue） | 182 文件 ≈34,873 行 |
| 测试 | 677 文件 ≈110,539 行 |
| e2e | 29 spec / 45 用例 |
| 工程配置全套 | package.json / eslint / vitest（coverage 多桶门）/ playwright / tsup / tsconfig / electron-builder / 两 workflows / .gitattributes / .gitignore |

**分域**：A AI 链路（75 文件 12,293 行）/ B 机检·格式·事件·三审（49 文件 ≈12,900 行）/ C 核心数据（39 文件）/ D 服务端·桌面·安装·进程（89 文件 ≈19,300 行）/ E1 前端逻辑（88 文件 ≈18,700 行）/ E2 前端组件（88 件 .vue + 样式 ≈21,200 行）/ F RAG·知识·learn·脚本·测试工程·配置。

## 二、门禁基线（主评审亲跑，2026-09-01）

| 门 | 结果 |
|---|---|
| vitest 全量（非 coverage） | **677 文件 / 4985 用例一次全绿**，108.23s |
| tsc --noEmit | **0 错** |
| vue-tsc --noEmit (web-next) | **0 错** |
| eslint . | **0 error / 31 warn**（全为 test/** no-explicit-any 存量） |
| check:counts | 过（677/4985 / 29 spec / 45 用例，与 README 零漂移） |
| check:packaging | 过（resources+pil 对账一致） |
| check:knowledge | 过（13 条目与磁盘一致） |
| e2e | **43 过 2 跳**，27.5s |
| coverage 多桶门 | 配置在位；本机全量复跑两趟受负载基础设施抖动（①1 例 startup-notices 落痕轮询假红——单跑三连绿定性；②1 次 tinypool worker `ERR_IPC_CHANNEL_CLOSED` 无任何测试失败标记，R73-21 同族）——**不以 coverage 单跑为本轮干净基线，如实记档**；阈值门配置与双重锁测试（coverage-threshold-globs）在位 |

观察项：唯一候选 flaky 面 = startup-notices 落痕轮询（历史三代已登记），本轮单跑三连绿；coverage 全量跑在并发负载下偶发 infra 级通道抖动，不构成本轮发现。

## 三、分域结论

| 域 | 完成度 | 质量 | 一句话结论 |
|---|---|---|---|
| A AI 链路 | 92% | 8.5 | 三线流式契约/四路记账/预算闸/金库封装达准 RC；缺口 = Anthropic 思考块不回传 + 两处窄形态计量边角 |
| B 机检·格式·事件·三审 | 90% | 8.5 | 容错与并发纪律扎实；**CRLF 履历条目正则双失配（P1）** 与境界体系解析同族缺口 |
| C 核心数据 | 93% | 9.0 | 保存链/清单/版本/回收站/锁基建数据安全纵深闭环；残留两处服务进程同步阻塞 |
| D 服务端·桌面·安装 | 92% | 9.0 | HTTP 安全闸/删书全链/SSE 凭证/桌面进程生命周期超 RC 预期；个别读端点残留同步重 IO |
| E1 前端逻辑 | 95% | 9.0 | 切书守卫/SSE 自愈/乐观锁/IME 防御超成熟；仅 1 个 P2（前导空行拽回）与 3 个 P3 |
| E2 前端组件 | 93% | 9.0 | 竞态/在途锁/键盘纪律执行高度一致；v-html 全库零使用；书架键盘可达性缺口 |
| F RAG·脚本·测试工程 | 93% | 9.0 | 四类测试病零检出、测试门三重自守护；CI push 腿/win e2e/主桶阈值漂移为流程性项 |

**总体：进度 ≈93%（核心 mac 生产形态 ≈95% / win RC 形态 ≈92%）；质量 8.7/10。**

> 口径说明：三十五轮收口后宣称 ≈98%。本轮从零独立复审捕获 1 P1 + 5 P2，且按「可发布生产 RC 形态」从严口径（P1 涉及数据物理清空、win RC 无 e2e、J6 发布面未动）评定 **≈93%**——两数值非同一把尺（上轮含修复批收口确认，本轮按发现面从严），报告以本轮从严值为准并按域给出可执行清单（§九）。

## 四、P1 / P2 明细（主评审逐条亲验成立，零证伪）

### P1 ×1

- **[R36-1] 履历条目正则对 CRLF 行尾双失配：条目全量丢弃 + 定稿回写物理清空履历**
  `src/format/leads.ts:65,71,133,161`（HISTORY_ENTRY_RE / LOOSE_RE / parseHistory 对**未 trim** 原始行匹配，`$` 无 `m` 标志不认 `\r` 前行尾）→ `src/document/lead-finalize.ts:252-265`（定稿回写 `writeLead` 整文件重序列化，只回放解析出的条目）。
  机理（主评审 node 实测复现）：`- 第012章 埋下：证据\r` 对主/宽松两正则均 null → 落「形似条目」分支被 log.warn 丢弃；`writeLead` 按 `stringifyHistory(lead.履历)` 重写 → **既有全部履历条目物理删除、不可恢复**；机检对它们失明（防吃书账本整体失真：引文假绿 + 两端闭合按「无履历」假红）。
  触发：任一账本 `.md` 行尾为 CRLF（win 记事本/编辑器手改、git autocrlf 检出、同步盘转码；win 为 RC 目标平台）；文件仍为合法 UTF-8，穿过 lead-finalize 的 M-9 非 UTF-8 防线。对照：同族 `lead-updates.ts`（先 trim）、`frontmatter.ts`（值侧 trim）、`count.ts:916`（R33-1 显式 `\r?` 容忍）均安全——**唯 parseHistory 一族是防线外缺口**。
  修复方向：parseHistory 匹配前对原始行做 `\r` 行尾归一（或匹配前 trim）。

### P2 ×5

- **[R36-2] Anthropic 线扩展思考块零回传：多轮工具链可能 400 → 降级链静默剥工具**
  `src/ai/provider/anthropic-adapter.ts:267-286`（流侧只消费 tool_use/text_delta/input_json_delta，thinking/signature/redacted_thinking 全弃）、`:84-89`（回传丢 reasoning）、`:153-157`（output_config.effort 发码）+ `model-quirks.ts:191`（claude 原生走 output_config）+ `store.ts:77,518-529`（effort 默认并回退均为 xhigh）。
  机理：基于 Anthropic 文档「思考+工具必须回传带签名 thinking 块」，缺口下原生 claude 多轮工具请求可被 400 → `buildDegradeAttempts` 换剥工具重发 → 文本模式成功；每轮翻倍请求且静默失去工具能力（降级不持久化）。思考文本也从不以 reasoning 事件透出（与 openai 线体验分叉）。代码侧「零回传」确证；**400 实际执行频率需真机验证**（标待验证）。
- **[R36-3] parseRealmSystems 名称/序列行对 CRLF 失配：境界体系整体解析为空 + 红项文案误导**
  `src/format/frontmatter.ts:443,451`（对原始行 `$` 锚定匹配，主评审 node 实测 CRLF 失配）→ systems=[] → 成长线境界跳跃/回退红闸整体失效 + `settings-context.ts:152` 注入失明；`growth.ts:46-51` 兜底信号实为 **red**（非黄）且文案「没有可解析的 front matter」与文件明明有冲突，误导排障。修复方向同 R36-1（\r 归一族）。
- **[R36-4] writeBookAnalysis 同步锁（Atomics.wait ≤5s）残留在服务 HTTP 热路径**
  `src/studio/server/api/analysis.ts:475` → `src/document/analysis.ts:239-257`（withAnalysisLock → `acquireCrossProcessLockWithTimeout` 同步睡）。R34D-19 只给 per-doc 建了异步孪生，全书级 `writeBookAnalysis` 漏建——同族「半异步残留」漏网点。双进程同书争用时服务事件循环冻结最长 5s（SSE/全部接口停摆）。
- **[R36-5] recordAiVersion 同步 spawnSync git（15s×2 超时窗）挂在保存/改稿/连写链**
  `src/git/exec.ts:34,52-62` → `src/git/ai-track.ts:131-137`（hash-object + update-ref 两次同步 spawn）→ 调用点 `api/draft.ts:65`、`api/rewrite.ts:163`、`self-heal.ts:496`（连写每章一次）。紧邻注释 R32-5 宣称「保存锁等待不再冻结事件循环」，同一 try 块内恰是漏网反例；git 无响应（网盘挂载 .git/杀软锁）时每次阻塞最长 15s×2。
- **[R36-6] 正文前导空行在首次后续键入时被拽回（R31-30 补笔的残余缺口）**
  `EditorView.vue:61-84` + `editor/CmHost.vue:253-285` + `shared/words.ts:26-29`（mergeFm 无条件剥 `/^\n+/`）。主评审 /tmp 实验确证往返：回车后静止窗补笔成立 → 下次键入 `merged !== e.content` 走 mergeFm 分支剥前导 \n → `body` computed 变化触发 CmHost 全量替换把 CM6 的前导回车拽掉。作者刻意留的正文前留白在首次键入后丢失。

## 五、P3 汇总（R36-7 ~ R36-30，24 项；分四组，落地提级处理）

**服务端 / 性能（7）**
- R36-7 `analysis-overview` 与 `version-stats` 两个 GET 端点请求路径全量同步内容读、无 TTL 缓存（`api/analysis.ts:300-370`、`api/snapshots.ts:67-111`；同型端点均已配 5s 书键缓存）
- R36-8 进门健康检查同步全树扫描 + 增量失效走全量 rebuild（`state.ts:330-343,124`；R35-5 只异步化了锁等待）
- R36-9 GUI 建书请求路径吃同步 books.lock（`api/books.ts:237 → install/init.ts:135 → install/books.ts:161-199`；`books.ts:151-156` 登记「余面不在请求窗口」与实际不符，删/改名已走异步唯建书漏网）
- R36-10 `工作区/导出/` 未纳入 isInternalBookPath 内部簿记前缀（`layout.ts:65-68`）——文档 CRUD 可按路径直达写/删导出产物目录（产物可再生，危害低）
- R36-11 同步孪生原语注释漂移 + 生产零调用死码面（`finalize.ts:18-20,233-250` 头注「存量调用方」已不实、`journal.ts:132-163` 自认零调用、`analysis.ts:153`）
- R36-12 `setting-rule` 每次全量同步读设定目录无缓存（`rules/setting-rule.ts:41-80`；AI 热路径反复 readdirSync+readFileSync）
- R36-13 前端卸载兜底同步 XHR 无超时（`stores/doc.ts:61-71,457-510`——Chrome 禁 sync XHR timeout，服务半死时关窗可卡数十秒；已知权衡）

**AI / RAG / 脚本（6）**
- R36-14 openai 线 `usage:{}` 空对象按 0/0 入账，绕过 R73-1 估计兜底窄形态（`openai-adapter.ts:253-262,432-443`）
- R36-15 responses 线兜底 tool id 用 map.size 非单调，同流可重复（`responses-adapter.ts:344`；openai 线 fallbackToolSeq 单调，两线口径不一致）
- R36-16 RAG `recall()` 兼容包装丢弃 `truncated` 截断信号（`rag/index.ts:780-793` + `process/materials.ts:211`；目标场景 ~3.5 万块未触界，挂账）
- R36-17 覆盖率主桶阈值与同段注释基线漂移（`vitest.config.ts:87-90`——拆桶时未随实测重算，F 域有效防线弱于宣称口径）
- R36-18 CI 无 push 腿（`ci.yml:9-11`；作者拍板项沿 R28-29 口径，发布物由 desktop.yml tag 门兜底）
- R36-19 win RC 目标平台零 e2e（`ci.yml:127-158 + desktop.yml:79-90`；D5 拍板维持，win 腿有全量单测+启动冒烟部分缓解）

**前端（6）**
- R36-20 清空目标字数输入写 0 而非清键（`SettingsBookWriting.vue:141-157`；`Number('')===0` 穿过闸，R72-11 helper 全库唯一偏离点，注释自称与行为相反）
- R36-21 `onActivated` 续轮询缺「仍激活」复检，关窗后可新起后台轮询（`SettingsBookAnalysis.vue:360-366`）
- R36-22 StyleEntryPanel/StyleBaselineCard await 后 toast 无书名复检，切书错位（`StyleEntryPanel.vue:44-85`、`StyleBaselineCard.vue:104`；同域 StyleCandidateBox 已设防）
- R36-23 书架主导航（书卡 grid/list + 弹层 hero）键盘不可达（`BookCard.vue:40,63`、`ShelfModalHero.vue:21-51`；R72-12 修复漏网，无替代键盘路径）
- R36-24 `putBookPrefs` 无乐观锁并发整份覆盖（`api/prefs.ts:23-29`；与全局 prefs 409 恢复链防御不对称，仅布局类数据）
- R36-25 AuditView load 无 script 层代守卫（`AuditView.vue:52-82`；当前被模板禁用态封死，属可维护性）

**测试工程 / 杂项（5）**
- R36-26 CLI 建书同步锁（并入 R36-9 同根，独立条目备查：`install/books.ts:178-199` 注释登记「余面均不在请求窗口」与 GUI 建书事实矛盾）
- R36-27 eslint ignores 未排除本地 `tmp/`（`eslint.config.js:35-44`；现盘无 .js，一行可闭合）
- R36-28 工作区残留过期 dist 构建产物含 token gate 修复前旧代码（gitignored；实测旧产物 `GET /foo/../api/books` 无 token 返回 200，源码版正确 403——反证修复承重；建议清理或重建）
- R36-29 短篇合审分包焦点文案硬编码「覆盖三视角」实际五视角（`review/run.ts:169`）
- R36-30 短篇清单 payoff 核对 detail 与 location 同值冗余（`check/runner.ts:388`；契约字段未承载）

## 六、开放任务实证（对照总览第三/五节，本轮从零核实）

- **阶段 13（Responses 启用批）**：脚本 `scripts/verify-responses-relay.ts` 在位；`responses-encrypted.test.ts` 7 用例在树（encrypted 直测已闭，总览「仍缺」口径过时）；**仅余中转网关真机验证**（待作者凭据，外部依赖）。
- **阶段 21（win 适配）**：J7/J1/J2/J3/J4/J0 已完成（跨进程锁、脚本可移植、消毒单源、平台门、CI win 腿、打包构建）；J5 体验面大体在树（菜单隐藏/外观对齐 mac/字体缓存/win 字体族/git ENOENT 人话/坚果云冲突副本特征/长路径冒烟/rename EPERM 重试）；**J6 发布面未动**（签名系 D4 拍板延后；README SmartScreen 披露已在）；**J6b 跨平台迁移挂账**；win e2e 维持 D5 拍板（仅 typecheck+build+单测）。
- **CI**：PR→main + workflow_dispatch；push 腿未恢复（作者拍板项，R36-18）。
- **章节结构操作**：设计稿落 03-设计/ 待拍板（不阻塞代码面）。
- 总览第三节其余阶段（0-12/14-20/22/23）均已完成，无新的未决任务。

## 七、完成进度评估

按总览阶段行基准 + 本轮从零代码实证：

- **核心 mac 生产形态 ≈95%**：编辑/机检/三审/定稿/AI 编排/记账/导出/版本恢复全链可用且测试门全绿（e2e 43 过 2 跳）；所缺 = R36-1 修复（P1）、R36-6 前端瑕疵、零散 P3 与 J6 发布面。
- **win RC 形态 ≈92%**：代码层跨平台就绪 + J0-J5 大体收口；所缺 = R36-1/R36-3 的 CRLF 族（win 主战场！）、win 零 e2e、J6 发布、J6b。
- **总体 ≈93%**（按「可发布生产 RC」从严口径，P1 修正后可比肩上轮宣称值）。
- 特别提示：**R36-1 与 R36-3 是同一 CRLF 失配族，且 win 正是 CRLF 高发平台**——建议随批 A 一并修。

## 八、质量评估

**8.7/10**（分域 A8.5/B8.5/C9.0/D9.0/E1 9.0/E2 9.0/F9.0）。依据：

- **强项**：保存链锁序全仓闭环 + 非 UTF-8 字节保真三面闭环 + 版本治理「宁多勿失」；三协议「伪成功」契约对齐 + 四路记账无遗漏 + 预算闸 fail-closed；token 闸双层防线实测全变体 403；删书全链收口（busyGate→abort→drain→recheck→墓地→缓存清理）；SSE 三凭证 + 背压精确计量；切书守卫族/IME 防御/undo 栈真清；事件迁移 ABBA 防护 + 半迁移自愈；测试工程四类病零检出 + 六合一自守护门 + coverage 阈值双锁。
- **弱点**（按影响排序）：① CRLF 行尾归一防线存在系统性遗漏点（R36-1/3，历史轮次修复了 count.ts/frontmatter/lead-updates 却漏 parseHistory/realms）；② 服务事件循环「同步阻塞归零」不变量仍有 3 处漏网（writeBookAnalysis/recordAiVersion/建书 books.lock）；③ 防御体系高度依赖注释契约（R36-4/5 皆因注释宣称与实际调用面漂移而漏网）；④ win 平台自动化覆盖为单测 + 启动冒烟（无 e2e）。
- 整体判断：成熟度已达「可交付 mac RC、修 P1+P2 后可推进 win RC」线；发现的缺陷均具明确触发条件与修复路径，无架构级返工必要。

## 九、修复计划建议（待作者指令；本轮不含修复）

> 沿历史口径：报告完成≠收口——P1/P2 修复 + 回归通过才收口。以下批 A-C 供「开始修复」指令照单执行（文件互斥分组可按历史惯例派并行代理）。

- **批 A（P1 必修）**：R36-1 CRLF 履历正则行尾归一（parseHistory 匹配前对原始行做 `\r` 归一，或 trim 后匹配）→ 补 CRLF 账本端到端回归（读-改-写往返 + 定稿回写）；R36-3 realms CRLF 同族（名称/序列两处 `$` 锚定改 `\r?` 容忍）→ 补成长线境界体系 CRLF 回归；并做全库行式解析器 CRLF 防线在一次扫描式复核（防第四处漏网）。
- **批 B（P2）**：R36-2 Anthropic thinking 回传接通（receive 侧缓存 thinking/signature 块 + 回传侧透传带签名块；或至少对 claude 原生+effort 组合显式禁思考防 400）→ 需真机验证 + 三线 reasoning 事件口径对齐回归；R36-4 writeBookAnalysis 异步孪生（对齐 R34D-19 口径）；R36-5 recordAiVersion 异步化（spawn 化或移出同步链；修 draft/rewrite/self-heal 三调用点）；R36-6 前导空行补笔分支收口（mergeFm 剥 `/^\n+/` 与 CM6 现状对齐或补笔改保前导）。
- **批 C（P3，按价值排序）**：R36-7 两 GET 补 TTL 缓存；R36-9 建书锁异步化；R36-20 目标字数清键语义（接 R72-11 helper）；R36-10 导出前缀收编；R36-12 setting-rule 缓存；R36-16 RAG truncated 接线；R36-17 coverage 阈值随实测重算；R36-27/28/25 卫生三件；R36-22/23 前端补齐；其余（R36-8/11/13/14/15/18/19/21/24/26/29/30）逐条登记维持或轻修，理由随修复批记档。

## 附：亲验与实验记录

- R36-1 主正则/宽松正则 CRLF 双失配（node -e 实测，false/false；trim 后 true）
- R36-3 名称/序列正则 CRLF 失配（node -e 实测，false/false）
- R36-6 前导空行往返（/tmp 纯 JS 复刻 mergeFm 分支，确证拽回）
- R36-2 代码面 grep 证实无任何 thinking/signature/pause_turn 处理；effort 默认解析链（store.ts:77/518-529 + model-quirks.ts:191）核实在位
- R36-4/5/9 调用链逐层读证（api→document/git/install→cross-process-lock/spawnSync）
- 门禁：tsc/vue-tsc/eslint/三 check/e2e 亲跑如上；coverage 复跑受 infra 抖动如实记档

（副本落 `01-评审/`；索引同步：Dev/Main/README 变更记录 + 目录地图 01-评审 行 16→17、总览 1.1 增三十六轮行 / 1.3 暂存计数、本文件维护日期 + 暂存节。）

---

## 十、收口记（三十六轮修复批，2026-09-01）

- **作者指令**：「开始修复，编排下任务，全部修复。」
- **执行组织**：批 A-C 按 §九 照单执行，文件互斥两波并行——波 1 批 A（AI·RAG）/批 B（CRLF 族+全库防线扫描）/批 C（分析链）/批 D（服务端）四路代理**全部首发成活零阵亡**；波 2 批 E（前端）/批 F（工程杂项）两路代理**全部首发成活零阵亡**；代理产出全经主评审逐 diff 语义核销（**P1×1/P2×5 全亲验零证伪**）。
- **结果：30 项 = 修 26 + 维持 4 零降级**（P1×1 修；P2×5 修〔R36-2 方案偏离：保守路径 + 流侧半接通〕；P3 修 20 + 维持 4）。
- **要点**（每项配回归测试）：
  - R36-1 `parseHistory` 匹配前逐行 `\r` 行尾归一（endsWith 守卫只剥行尾、不动内容侧空格；headingEndsSection 条目前瞻共用同数组）→ CRLF 账本读-改-写**端到端 + 定稿回写全部保留**（LF↔CRLF 逐位一致防退化）；**同族全库扫描补第四处 `metrics/short-index.ts:351`**（`##` 锚点 `\r?` 容忍，对齐 R33-1 口径）
  - R36-3 `parseRealmSystems` 同口径行尾归一（名称/序列两处一次闭合）；`check/growth.ts` 兜底文案如实（明示「换行格式异常」可能诱因），信号维持 red（fail-closed，与 R29-6 口径一致）
  - R36-2 **保守路径**：claude 原生 + effort 组合显式 `thinking:{type:'disabled'}` 防 400（deepseek anthropic 端点不禁——无 thinking 参数语义）；流侧 thinking 文本即时透出 `reasoning` 事件（三线口径对齐）+ 块含签名流内缓存 + 思考 token 计入 R73-1 估计；完整签名回传受 `usage-estimate.flattenMsgContent` exhaust 类型击穿所限留待 gen/turns 跨批（注释登记）；**真机 400 频率待验证**
  - R36-4 `writeBookAnalysisAsync` 异步孪生（`withAnalysisLockAsync` 走 acquireCrossProcessLockAsync；api/analysis.ts 调用点迁移 await）；同步版删除（R36-11 复核 src/scripts/test 零调用）
  - R36-5 `gitAsync`（spawn + 有界超时 15s + AbortSignal + GIT_MAX_BUFFER 上限 + ENOENT/ENOBUFS 人话；**超时直接 settle 不依赖 close 收口保证严格有界**，永不 reject）+ `recordAiVersionAsync` + draft/rewrite/self-heal 三调用点 await（失败降级语义不变）
  - R36-6 `mergeFm` 分来源剥前导（编辑路径 `{stripLeading:false}`；加载/粘贴/对账默认口径不变）+ `body` computed 只剥 fm/body 分隔首换行——作者留白在补笔与后续键入后不再被拽回（R31-30 补笔分支归位兜底 no-op）
  - R36-7 analysis-overview/version-stats 5s 书键 TTL 缓存（**逐文件 mtime/size 探针即时失效** + 写侧 forget 钩子 + TTL 兜底 + FIFO 32；books.ts 家族接线由主评审补接）
  - R36-9/26 建书锁异步化（`appendBookAsync`/`doInitAsync`，GUI 端点与 CLI 建书统一；`appendBookLocked` 单源防两版漂移）
  - R36-24 书级 prefs `revision` 乐观锁契约收口（GET 剥离单独回传 + PUT expectedRevision 失配 409，对齐全局 prefs GG-P2-7 全链；客户端常不带 = 直通向后兼容，前端接线另行）
  - R36-17 覆盖率主桶阈值随实测重算 **82/78/93/82 → 89/83/95/89**（2026-09-01 全量实测 91.19/85.10/97.44/91.19，−2pp 向下取整；注释口径同步实况化）
  - R36-28 过期 dist 清理（含 token gate 修复前旧产物，已备份 /tmp）；其余 P3 全落：R36-10 导出前缀收编 / R36-11 死码删除（journal 同步孪生三件）+注释实况化 / R36-12 setting-rule TTL 缓存 / R36-14 usage 空对象走估计兜底 / R36-15 兜底 tool id 单调计数器 / R36-16 truncated 信号接线 / R36-20 目标字数清键语义 / R36-21 激活复检 / R36-22 书名复检 / R36-23 弹层 hero 键盘可达（BookCard 复核本已是原生 button）/ R36-25 load 存活守卫 / R36-27 eslint ignores 补 tmp/ / R36-29 焦点文案动态化 / R36-30 payoff detail 语义修正
- **维持 4（理由记档）**：R36-8 进门健康检查全树扫描（启动段低频 + R35-5 已异步化锁等待，非热路径）；R36-13 卸载兜底同步 XHR 无超时（Chrome 禁 sync XHR timeout，OS 级超时才放，平台性已知权衡）；R36-18 CI push 腿（作者拍板项沿 R28-29 口径）；R36-19 win RC 零 e2e（D5 拍板维持，win 腿有全量单测 + 启动冒烟缓解）。
- **方案偏离记档 3 条**：R36-2 保守路径（显式禁思考代签名回传，跨批接通后收窄）；R36-7 逐文件探针（dir-only 会让既有「直写盘立即 GET 新鲜度」测试退化为 5s 可见窗）；R36-24 服务端先行（前端 opt-in 待接线）。
- **主评审亲修涟漪 2 处**：books.ts `forgetBookKeyedCaches` 补接 `forgetAnalysisOverviewCache`/`forgetVersionStatsCache`（批 C↔D 跨批衔接缺口）；self-heal.ts `persistFinal` 缩进修复。
- **+22 测试文件/+100 用例 = 677/4985 → 699/5085**（根 README 修账四处 + check:counts 对账过）。
- **终门九件套全绿**：vitest **699/5085** 98.70s 一次全绿 + tsc/vue-tsc **0 错** + eslint **0 error**（31 warn 全存量）+ 三 check 过 + **e2e 43 过 2 跳** 27.8s + **coverage 多桶门过**（新主桶阈值重跑两趟全绿，无 infra 抖动）。
- **确认闸合规**：全批零新增/移动 .md（新增测试 `.test.ts` 不在确认闸面）；**工作树未提交待作者指令**。
- 索引同步：本条（状态 未收口→已收口 + 收口记）+ Dev/Main/README 变更记录（三十六轮修复批收口条目）+ 目录地图 01-评审 行（三十六轮条目 未收口→已收口）+ 总览 1.1（三十六轮行状态回改）+ `01-评审/README`（维护日期 + 三十六轮条目收口记）。