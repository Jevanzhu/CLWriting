# 全量代码重审与进度质量评估（独立重评）

> 📦 **归档记（2026-09-08）**：已收口，随「README 整理与清理」批移入 `../Archive/`（扁平冷库；确认闸 2026-09-08 修订——规则内免确认留痕）；历史正文不改写。

- 日期：2026-09-07。轮次：**全量独立重审**（不接轮号；上一轮全量重评为六十一轮，本审从零独立进行，不依赖其结论）。
- 基线：dev HEAD `22ecdb5d` 干净树（git status 全 clean，评审全程只读零改动）。
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。记得编排下任务。」
- 报告路径：经确认闸报备未获回复——按六十轮/六十一轮先例以正典名落盘**待追认**，文件名 `全量代码重审与进度质量评估-评审-2026-09-07.md`。
- 评审纪律：**禁读既有评审全链**（01-评审/、Archive/、台账结论一律不读），所有结论来自代码本身 + 实测门禁；子代理只读、零写盘。

## 一、评审方法与范围

**方法**：两波并发只读子代理（A-H 八域，波 1：A-E；波 2：F-H，波内 ≤5 并发全成活零阵亡）+ 主审亲验（P2 发现抽样逐行回验 + 阶段 24 标记物全域独立扫描 + 风险模式快扫）+ 门禁九件套本机实测。

**域划分与覆盖（子代理如实声明）**：

| 域 | 范围（行数） | 评分 | 精读覆盖 |
|---|---|---|---|
| A | src/ai + src/rag + src/knowledge（≈16k） | 9.0 | ~68% |
| B | src/desktop + src/process + src/install + src/driver（≈11.5k） | 8.7 | ~70% |
| C | src/document + src/state + src/cache + src/events + src/log + src/metrics（≈13.6k） | 9.0 | ~75% |
| D | src/check + src/format + src/fs + src/review + src/export + src/git + src/learn（≈20k） | 8.5 | ~68%（另 20% grep 级触及） |
| E | src/studio/server（≈11.8k） | 9.0 | ~65% |
| F | web-next api/composables/stores/editor/shared/types（≈12k） | 9.0 | ~35%（P1 高危路径全覆盖） |
| G | web-next components/views/pages（≈22.6k） | 9.0 | ~32%（高风险面全覆盖） |
| H | test/（≈151k，抽样）+ scripts/ + 根配置 + CI | 9.0 | 抽样（见 §三） |

**行数加权质量 ≈ 8.9/10（A−）**。

**方法学局限（如实声明）**：① 子代理覆盖为抽样而非逐行全读（各域 ~32-75%），未精读文件可能存在未发现缺陷；② 测试面 15 万行为抽样审查（H 域反模式扫描 + 代表性文件精读）；③ macOS 本机门禁实测，win 腿（CI）与 coverage（CI 兜底）未在本机跑；④ e2e 发布 smoke（CLWRITING_E2E_RELEASE）未跑（常规腿已跑）。

## 二、完成进度评估 ≈ 97%

按总览实施顺序口径（阶段 0-23 已完成/收口，唯阶段 24 开放）：

- **阶段 0-23 全部收口成立**：总览第三节逐阶段核对无滞空；v1.0.0-rc.1 已发（阶段 20）；win 适配全收口（阶段 21）。
- **阶段 24「章节结构操作」方案就绪零实施**：设计 + 执行方案已落盘（2026-09-04），实施待作者指令。**独立标记物扫描零命中**——`structure.merge` 事件名全仓零出现；frontmatter「序/并入」键零实现（grep 命中均为无关注释与导出格式词）；章合并/拆分流程/API/UI 零命中（export 的 merged/split 仅为导出格式）。
- 已具备的产品能力（实测/亲读确认）：建书/写设定/正文/机检/三审/定稿/导出全链、伏笔与设定账本、版本快照与恢复、RAG 设定索引、文风系统、工作台对话助手（工具确认闸）、多协议 AI 适配（Anthropic/OpenAI Chat/Responses）、事件审计、win 平台适配、跨平台书库直拷可用。
- **结论：核心产品已完备并收口，唯一未实施的功能面为阶段 24 章结构合并/拆分（+ 持续的 P3/观察卫生登记）。进度 ≈ 97%（23/24 阶段收口，阶段 24 设计就绪零实施）。**

## 三、完成质量评估（红线核销 + 分域结论）

### 红线核销（P1 全部零命中）

八域独立评审 + 主审快扫均无 P1：无编辑静默丢失/覆写、无密钥或书稿泄漏（sk- 字面量零、.env/models 不入 git、日志 maskKeys 脱敏全链、错误出口 redactSecret 同词表）、无污染数据进账本、无跨站读写洞（Host 精确白名单 / Origin 白名单 / 写 token / GET-HEAD 读闸全覆盖，豁免面精确收敛为 /api/boot + SSE）、无路径穿越（resolveWithinRoot 双侧 realpath）、AI 生成链路零 spawn CLI、无无限重试烧钱（重试 3 次封顶 + 预算闸三口径 + deadline/超时）。

### 亮点（跨域综合）

1. **保存与崩溃闭环**：原子写（tmp+fsync+rename+目录 fsync）+ 跨进程锁 + 覆写快照留底 fail-closed + 关窗/退出/关机三链预算化 flush + 渲染层 dirty 镜像（2s 节流 + baseRev 时效门复活）——「编辑永不静默丢失」红线被系统性实现。
2. **机检正确性**：章级指纹 × 全局纪元指纹双锚 + rebuild 首遍 fp 前移 + 终核整批丢弃，陈旧命中/混纪元结构性排除；回收站三重校验 + linkSync 原子占位消 TOCTOU。
3. **「模型可见⟺已记录」机器可验证**：promptMeta(hash+chars+files+tools) + lineage 事件 + CLW_VERIFY_VISIBLE 抽样校验，注入与登记同源派生。
4. **跨书守卫体系化**：切书链 8 store 逐个 clear + `:key=bookName` 重建 + 书名快照/活源复检/代际作废三件套遍布跨 await 动作。
5. **SVG 服务端安全面**（E 域）：GET/HEAD 读闸 WHATWG URL 归一化防绕过、SSE 生命周期句柄化记账 + 背压双判死、删书/改名 busyGate + drain + 闸后复检。
6. **门禁机器化**（H 域）：check:counts 将「README 账实对照」升级为多道静态机器门（.only/无条件 skip/空洞测试/断言缺失/pageerror/spec 顺序快照）；CI 三平台矩阵 + coverage 多桶 + 发布链三重冒烟焊进 tag 流程。
7. **测试真断言度**：port 0 真起 server、真实临时目录 + fixture、错误路径断言前置（85 处 `if (!r.ok) return` 抽查均有前置 expect）、mock 只打边界。

### 门禁九件套实测（macOS 本机，全部通过）

| 门 | 实测 |
|---|---|
| vitest 全量 | **964 文件 / 6280 过 + 4 跳 0 败（113.14s）** |
| tsc | 0 错 |
| vue-tsc（web-next） | 0 错 |
| eslint | 0 error（exit 0） |
| check:counts | 过——README 声称 964/6280 与实测一致 |
| check:packaging | 过——resources/prompt 版本表对账一致 |
| check:knowledge | 过——13 条 manifest 与磁盘一致，无未登记资产 |
| build:web | 过 |
| e2e（Playwright） | **43 过 + 2 跳 0 败（27.2s）** |

## 四、问题清单（独立发现）

### P1：0 条

### P2：18 条（全主审抽样亲验，按族归并）

**甲族·失联网络卷同步 IO 阻塞主进程（同根，已修切库/开书库两入口，本族残余面）**
1. [B] bootstrap 启动链 `statSync(store.current)` + `findWorkDir(cwd)` 无异步预探——失联网络卷冷启动主进程同步冻结 — src/desktop/main.ts:991-1002（亲验成立）
2. [B] `show-in-folder`/`open-book-dir`/`open-library-dir` 三 IPC 每次同步 `readBooks`（stat+readFileSync），会话中网络卷失联即阻塞 — src/desktop/main.ts:1342-1371、1428-1438
3. [B] utilityProcess 崩溃退避重启后子进程内存态重置、main 无「重启完成」广播；重启期 flush 失败静默落 autosave 兜底 — src/desktop/server-manager.ts:382-406
4. [B] 启动段 `repairBooks` 同步走 `tryBooksLock`（Atomics.wait 最坏 5s），双进程争用窗口冻结启动段 — src/desktop/books.ts:231-244、556-570
5. [A] 生成收尾记账在 SSE/HTTP 热路径同步 fsync（recordUsageSafe→atomicWriteFile），慢盘阻塞事件循环 — src/ai/calls.ts:338 / src/ai/runner.ts:553（亲验：锁获取已异步、写段同步系 R30-3「记完即读」设计权衡，建议文档化/异步化评估）

**乙族·解析与往返正确性（数据正确性级）**
6. [D] 账本推进解析：裸 `---` 分隔后自由备注行仍折入上条证据 → 证据污染 + lead-finalize 持久写 + lead-declared-not-done 假红硬拦（R48-4 只堵 ATX 标题前置形态，裸分隔漏网）— src/check/lead-updates.ts:103-119（亲验成立）
7. [D] folded(`>`) 块标量内部双空格折叠成单空格（`replace(/  +/g,' ')`），读改写往返篡改手写多行值 — src/format/frontmatter.ts:222（亲验成立）
8. [D] `atomicWriteFile` rename 成功后 `fsyncDir` 抛非 EPERM（如 EIO）上抛 → 目标已写却报失败、误判 WRITE_ERROR 重试 — src/fs/atomic.ts:166-170、262-265（亲验成立）
9. [D] 导出合并+分章模式中途失败错误信封 `files:[]` 清零，已落盘部分产物无列表返回 — src/export/index.ts:523-532

**丙族·锁与留痕**
10. [D] 跨进程锁 open 'wx' 后 writeSync ENOSPC 半写：非 EEXIST 上抛路径不清理锁文件，依赖 500ms grace + 接管自愈且无自愈留痕 — src/fs/cross-process-lock.ts:199-240
11. [D] 单章机检 `checkWithDb` catch 只转 CHECK_ERROR 信封不回写日志（树聚合路径有 warn，单章端点无）— src/check/run.ts:363-365

**丁族·服务端与前端守卫**
12. [E] PUT /file 无 expectedRevision 时跨进程并发覆盖残窗（快照留底兜失、非数据丢失；B-22 注释自认）— src/studio/server/api/files.ts:159（亲验成立）
13. [E] SSE `?token=` 兼容回退通道仍放行，token 进 URL 暴露面（R30-25 登记，无移除时间点）— src/studio/server/api/stream.ts:256-266
14. [F] SSE 换票失败回退同上 `?token=` 把鉴权 token 拼进 EventSource URL — src/studio/web-next/src/composables/useSse.ts:183-185（与 13 同根两端）
15. [F] apiJson 对重放仍 401/403 透传原始错误，boot 重试与重放双失败后无统一降级文案 — src/studio/web-next/src/api/client.ts:119-139
16. [F] `doc.refresh`/`syncCleanWithTree` 失败返 false 且消费方未必提示（refresh 系「fm 以服务端为准」关键路径，失败无 UI 面）— src/studio/web-next/src/stores/doc.ts:522-525、563-565
17. [G] 切书守卫链挂起期跨书混合态：Book.vue 链尾才 `ws.setBook(n)`、树面板已按新书加载；确认弹窗段被全屏 mask 挡住点不到（亲验收窄），但 flushDirty await 段可点时 `ws.bookName` 仍为 A、`doc.open` 抓 A 书同路径内容，窗口内编辑/⌘S 写回 A 书（内容污染/编辑丢失风险；E-2 守卫快照比对失效）— src/studio/web-next/src/pages/Book.vue:168-243 + components/panels/ChapterTreePanel.vue:56-69（亲验成立，暴露窗收窄为 flushDirty 段）

**戊族·测试基建**
18. [H] 真实墙钟与产品定时器竞速（sleep(120) 越 80ms 锁超时 / sleep(600) 越 400ms backoff / sleep(200)×多），CI 2-fork 慢机 flake 面 — test/ai/r30-batch-a.test.ts:73、test/desktop/r51-a3-a6-server-manager.test.ts:82、test/desktop/server-manager.test.ts:886,913,931

### P3：约 36 条（按域）——代表性列示，非逐条全文

- A ×7：responses 适配器缺「终止事件后 error 守卫」；knowledge commit 回滚丢 BOM；calls/store 写队列排队分支生产不可达死代码；task-gate 自 ai 层反向 import studio/server（分层债）；self-heal abort 分支不发批量进度；orchestrateBatch 不可达兜底死代码；RAG commitIndexBatch 零块章（complete 章）指纹跳过。
- B ×3：`relaunch` setTimeout 不可取消无 unref（取消退出后 100ms 再触发 quit）；font-cache deps 分支准死代码；渲染崩溃封顶 3 与 server 重启封顶 3 同值不同源。
- C ×4：save 契约 operationId 声明「幂等去重」但 executeSave 零消费；rollbackMetaOnRenameFail 静默无留痕；writeVersion 写后全目录 readdir 双扫；模块级缓存多书死键依赖 forget* 手工接线（均已接线，卫生项）。
- D ×5：合审档回收全部视角标已收集（issues 无 lens 字段，缺视角永不判 incomplete）；learn rmSync(recursive) 未收编 rmWithRetry；growth realmIndex 「不可能 -1」断言无防御；foldSegs +$ trim 与 join(' ') 行尾空格语义漂移；全目录零 TODO 已清。
- E ×5：boot 对无 Origin 请求回发 studio token（本机进程同信任域决策，建议文档化边界）；static 404 文案泄漏前端构建指令；错误文案偶带盘内绝对路径×2；书架网关无页深限（千本书串行摘要，TTL 30s）。
- F ×5：workbench healResult 断线窗口残留终局卡片；SSE text 事件无事件 id 去重；evictLRU 超额多遍历一轮；getDraftPrompt 无显式 timeout 口径不一；useSse resync 与心跳双触发无去抖。
- G ×2：EditorView next.startsWith('\n') 兜底残留死代码；wordCount 切文档首帧闪 0。
- H ×5：check-knowledge 裸 readdirSync 无 TOCTOU 容错；verify-responses-relay 无 npm script 接线；.gitattributes 知识层 eol 冗余行；eslint.config.js 注释「Dev/ 为第三方参考项目」陈旧相悖；.skip 机器门对 skipIf 全豁免（平台门与条件式跳过不可分，长期无人复核恒真）。

### 观察：约 17 条（摘要）

预算闸锁外快照 check-then-act 超额窗（R72-12 既定取舍）；RAG 全量向量驻留 ≈215MB 显式权衡；journal compact stat 复核 µs 窗裸写（R31-21/N4 已认）；事件库 close 标记毫秒窗；开口标记 10 分钟超龄误判 SIGSTOP 活进程（安全方向）；崩溃 tmp 6h TTL / 网盘副本 60s TTL 取舍；log 队列 1024 丢最旧；进程内同步 rebuild 无 in-flight 合并；book-search 同步 walkMd 孪生；session-end 停机链 readBooks 同步遍历；chat 工具卡确认闸依赖服务端时序（建议后端回归覆盖）；SettingsBookAnalysis 组件本地记忆提示丢失；AiProviderEditor Number(currency) 未校验与空 key 透传依赖服务端契约；e2e retries:0+共享 workDir 任 flake 阻塞 tag 发布（有意取舍）；web-next .vue SFC 在 coverage/lint 射程外（vue-tsc + 29 e2e spec 兜底）；README「6280 全绿」为 mac/linux 口径（win 腿 77 平台门跳过）；verify-responses-relay 依赖记忆发现。

### 证伪/降档记录

- G 域「确认弹窗秒级窗口可点树」经主审亲验收窄：`cp-mask` 为 `position:fixed; inset:0; z-index:200` 全屏遮罩，弹窗段树面板不可交互，该 P2 真实暴露窗仅剩 flushDirty await 段（17 号按此定档）。
- A 域 P2 记账同步 fsync 经亲验系 R30-3「记完即读」既定语义，非回归（5 号按设计权衡定档）。

## 五、风险与遗留（超出代码问题的发布面)

1. **阶段 24（章合并/拆分）未实施**：唯一开放功能面，方案已就绪待开工。
2. **平台口径**：全绿为 mac/linux 口径；win 腿 77 例平台门静默跳过（EACCES/symlink 语义零验证），win 侧「全绿」徽章不成立。
3. **安全边界声明缺口**：boot token 对本机恶意进程实际防护为零（代码明确拍板「本机进程=同信任域」），但该边界仅隐于代码注释，README 已披露「本机不受信程序视为可完全读写」——建议维持文档化。
4. **coverage 由 CI 三腿矩阵 + 阈值门兜底**，本机未跑（L2 纪律）；`.vue` SFC 组件行为靠 29 个 e2e spec 兜底。
5. **未签名发布包**（mac ad-hoc / win NSIS unsigned）为已知首版披露项，非缺陷。

## 六、修复计划（已执行完毕——2026-09-07 作者指令「修复，编排任务做！」，处置明细见 §七收口记）

- **批 1（P2×18，主审亲修 + 文件互斥并发代理）**：
  - 甲族网络卷同步 IO（1-4）：bootstrap/三 IPC 预探异步化（R61-B-1 同款防线扩面）+ repairBooks 启动段异步化 + server 重启完成广播通道（5 号记账同步 fsync 维持语义，补文档化）。
  - 乙族解析正确性（6、7、8、9）：lead-updates 裸 `---` 置折入守卫、frontmatter folded 值去双空格折叠、atomic fsyncDir 失败语义与调用方分诊、export 失败信封回填已落产物。
  - 丙族锁留痕（10、11）：锁半写清理 + 自愈留痕、单章机检 catch 补 warn。
  - 丁族守卫（12-17）：PUT /file expectedRevision 契约收敛或文档化、SSE token 移除时间点、doc.refresh 失败 UI 面、切书挂起期书名守卫改造（ws 增加 pending target 或 doc.open 校验目标书）。
  - 戊族测试（18）：真计时用例改注入式假时钟/确定性等待。
- **批 2（高价值 P3 ×~12）**：responses 终止后 error 守卫、self-heal abort 批量进度、knowledge BOM 回滚、RAG 零块章指纹、workbench healResult 清理、SSE text 事件去重、check-knowledge TOCTOU 容错、verify-responses-relay 接线、EditorView 死代码清除、.gitattributes 冗余行、eslint 注释校正、task-gate 分层注记。
- **批 3（维持登记）**：其余 P3 ≈24 条 + 观察 ≈17 条（按既有惯例登记不修/待逐条裁决）。

## 七、收口记

**已收口（2026-09-07）**：修复批 1（P2×18 定置）+ 批 2（高价值 P3×12 定置）全部落地，L2 终门九件套实测全绿；批 3 维持登记不动。执行编成五路文件互斥（主审亲修 + α/β/γ/δ 四路并发代理），全程红绿验证，主审逐 diff 亲验零越界。

### 批 1（P2×18）处置明细

**主审亲修 ×4 代码修 + 1 降档记档 + 测试面连带修复**：
- **重审-1** bootstrap 失联卷预探：`CLW_BOOTSTRAP_PROBE_TIMEOUT_MS`（缺省 2s 可注入）+ `probeDirReachable` 参数化；store.current 预探 unreachable → 原生错误框 + 回落发现链（不退出），findWorkDir 前预探 cwd（unreachable → /welcome 引导，维持启动零弹选择器口径）。红绿：statGate 挂死 + 150ms 注入 → errorBox「暂不可达」+ 主窗载 /welcome。
- **重审-2** 三 IPC 预探：show-in-folder / open-book-dir / open-library-dir 改 async，readBooks/realpathSync 前经预探（unreachable → errorBox + return；invalid 落回原静默守卫语义）。红绿：三入口各一框 + shell 零触。
- **重审-3** 服务重启成功广播：server-manager deps 新增 `onRestarted?: (port) => void`（doRestart / restartPinned 两成功挂点，钩子抛错隔离防伪装握手失败）→ main 广播 `desktop:server-restarted` 至全部存活窗口 → preload `onServerRestarted` 订阅（desktop.d.ts 同步登记）→ Book.vue 订阅（sse.resync() 断旧连新 + info toast，浏览器版判空降级，卸载退订）。红绿：崩溃自动重启 / session-end 自愈两路径均断言主窗收到 `['desktop:server-restarted', 45678]`；渲染层广播回调断旧 SSE 连新 + toast + 退订 + 判空降级 4 用例。
- **G-17（丁族 17 号）** 切书挂起期跨书守卫：ChapterTreePanel.onSelect 前置 `props.bookName !== ws.bookName` 一致性守卫——挂起期（flushDirty/确认弹窗窗口）route 已到新书、树已重载而 ws.bookName 链尾才 setBook，E-2 快照基线取旧书名致复检恒过、新书 docId 开进旧书工作区。红绿 2 用例（挂起拦截 + 稳态不误伤）。
- **重审-4（甲族 4 号）主审降档维持登记**：repairBooks 调用点在服务子进程 pre-listen（server/index.ts:198-200）而非主进程，阻塞仅推迟首请求；三十四轮已登记「异步化与收益不成比例」（install/books.ts:201-209）。原报告按主进程冻结定级系夸大，降档。
- **测试面连带（主审）**：① main.test.ts fs/promises stat mock 缺省路径改 statSync 同步垫底（ok/invalid 语义等价、消线程池宏任务依赖——bootstrap 预探链全微任务化，既有单次 setImmediate / fake-timer advance 冲刷假设保持成立）；② 三处同步调用测试改 async/await（handler 已 async）；③ **r57 假件补 `once` 方法——暴露既有隐性测试债**：FakeUtilityProc 原缺 `once`，launch 的 R58-B-1 exit 观察者登记在本文件恒抛 TypeError → bootstrap 一直静默失败（onError quit），仅因失败 quit 恒落在用例 quit 基线快照之前被时序掩盖；预探引入真实 IO 后时序位移暴露。补齐后启动链真成功，quit 计数语义还原。

**代理 α（乙族 6、7、8、9 + 丙族 10、11，6 代码修 + 6 测试文件/16 用例）**：重审-06 裸 `---` 分隔线升格小节边界（嵌套子列表维持 R48-4 现值）；重审-07 folded 去双空格折叠、行尾清理改逐行 join 前剥；重审-08 **前提证伪转留痕修**（fsyncDir bare catch 本就吞全部错误不上抛，「假失败」不成立；真实缺口零留痕 → 非 EPERM 补 log.warn，仍不抛）；重审-09 export 两 catch 错误信封回填已落盘 files（split 项 ⟺ 已落盘、merged 名仅在完整发布后列，不虚列）；重审-10 锁 open-wx 后写失败 best-effort 删刚创建残锁 + warn 后原样上抛、陈锁接管补 warn 带原 pid；重审-11 单章机检 catch 补 warn。附带改 r48-lead-updates-fold-guard 既有用例（原钉旧节界语义，正是 06 修正对象，注明）。

**代理 β（丁族 15、16，2 代码修 + 3 测试文件/13 用例）**：重审-15 apiFetch 增模块私有 `_replayed` 出参，apiJson 对「重放仍 401/403」换统一文案（status/code 原样保留，不重放路径信封原样透传——r28-client-cancel 已锁口径不回归）；重审-16 doc.refresh/syncCleanWithTree catch 补 warning toast（同文案合并去重防刷屏；后者带书名守卫防切书后旧书提示落新书）。

**维持登记 ×4（12、13、14 号 + 4 号降档）**：PUT /file expectedRevision（B-22 三十四轮已自认取舍）、SSE ?token= 两处同根兼容期面（13、14）——维持登记由既有轮次结论覆盖；5 号（calls.ts 记账同步 fsync）转注释记档由 γ 执行（见批 2）；18 号（戊族测试竞速）由 δ 执行（见批 2 工具链）。

### 批 2（高价值 P3×12）处置明细

**修 ×6**：批2-1 responses-adapter completed 后 trailing error/failed 忽略（终态守护，Anthropic/OpenAI Chat 线无此形态）；批2-2 self-heal abort 两分支补发 batch_progress 终点（事件形状与 escalate/failed 逐字一致、零新枚举）；批2-4 RAG 零块章续传（dimOk 只约束带 span 章；零向量事务不写 model/dim 对齐全成功口径；章级小事务不变量保持）；批2-A workbench sync(running=false) 残留复位块补 healResult 清理（断线窗后终局卡片与「结果未知」提示同屏矛盾）；批2-B EditorView R31-30 补笔兜底分支删除（构造串与 mergeFm 输出逐字节同构、patch 同串恒 no-op，纯死代码）；批2-6 check-knowledge TOCTOU 容错（ENOENT/ENOTDIR warn 跳过，其余照抛，fail-closed 语义保留）。

**工具链 ×4（δ）**：批2-7 package.json 接 `verify:responses` script；批2-8 .gitattributes 删 `知识层/**` 冗余行（与全局 text=auto eol=lf 对纯文本等价，记档注释留）；批2-9 eslint.config.js 注释校正（Dev/ = Main + 参考项目）；重审-18 测试竞速轮询化 ×3（r30-batch-a vi.waitFor / r51-a3-a6 + server-manager elapseBeyond 轮询越过危险窗，deadline 到点未达即红，语义不弱化）。

**证伪 ×2（零改动留证）**：批2-3 knowledge BOM 回滚——`readFileSync(utf8)` 不剥 BOM（Node 实测 charCodeAt(0)===0xfeff）+ atomicWriteFile 写路径原样落盘，回滚保真还原 BOM 恰是「两文件均保持原态」承诺；批2-A done 分支——服务端先 self_heal_result 后紧接 done，done 清 healResult 会让终局卡片永不显示（既有回归已锁跨 done 存续），改注释记档 + 对照测试锁反向边界。

**注释记档 ×3**：重审-05 calls.ts R30-3 同步写取舍（权衡理由 + 异步化三前置条件）；批2-5 turns.ts task-gate 分层债（债务三件 + 重构触发条件 + 修复方向）；批2-C useSse text 重连重复拼接维持登记（前端无凭据去重的证据链：事件无 id/seq、SSE 帧无 id: 行、sync 快照无内容水位、前缀对拍不安全——修复需服务端契约变更）。

### 测试增量与 L2 终门实测

- **测试增量**：+13 测试文件 / +46 用例（主审 1/10、α 6/16、β 3/13、γ 3/7）→ 964 文件/6280 → **977 文件 / 6330 收集 = 6326 过 + 4 跳 0 败（110.97s）**。
- **L2 终门九件套（macOS 本机实测，全绿）**：vitest 全量 977/6326 过 + 4 跳 0 败；tsc 0 错；vue-tsc 0 错；eslint 0 error/0 warning；三 check 过（counts 修账后对账一致 / packaging / knowledge 13 条）；build:web 过；e2e 43 过 2 跳（27.9s）。
- 根 README 修账四处：6280→6326（badge/开发节/技术栈节）、964/6280→977/6326（合入门槛节）；win 口径行为带日期的历史实测记录保持原样。
- **git**：工作树保持未提交（+34 修改文件 / +13 新测试文件），待作者「提交改动」指令。
- 主审逐 diff 亲验：四路代理改动面合计 22 源文件 + 13 测试文件全部逐 diff 复核，零越界（各自严格限于许可文件清单）；三处证伪（重审-08 前提、批2-3 BOM、批2-A done）证据链亲验成立。
