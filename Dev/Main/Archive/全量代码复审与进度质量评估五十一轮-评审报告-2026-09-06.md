# 全量代码复审与进度质量评估·五十一轮·评审报告（2026-09-06）

> 📦 **归档记（2026-09-08）**：已收口，随「README 整理与清理」批移入 `../Archive/`（扁平冷库；确认闸 2026-09-08 修订——规则内免确认留痕）；历史正文不改写。

- 基线：dev HEAD `1ab9c962`（dev←recover 合并提交）干净树；评审指令：「忽略现有的评审文档，重新评审一遍项目所有代码，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。记得编排下任务。然后按照文档的修复计划，来修复找到的问题，全部修复。」
- 报告路径经确认闸 AskUserQuestion 获批（五十轮正典名延续 + R51-N 编号）；发现项编号 R51-<域>-N，红测试三项另编 R51-RED-1~3。
- 独立性：十域子代理与主审均**禁读** `Dev/Main/`（01-评审/、Archive/、总览、台账）全链，从零评审；子代理只读不改、不跑测试。
- 方法：十域并发（A desktop+install / B process+events+log+metrics / C ai / D document+fs+git+state / E check+cache+rag+knowledge+learn+review / F format+export+driver / G studio/server / H web-next 基础层 / I web-next 界面层 / J scripts+e2e+构建链），覆盖 src 全部 467 个 .ts/.vue（约 10.5 万行）+ scripts 11 文件 + e2e 29 spec + 构建链配置。子代理全程只读；F 域首发卡死由作者手动终止，原任务在终止前已完成交卷（重发副本即时停止，零重复计工）；I 域首发停止后重派成活。
- 主审核验纪律：**全部 P1/P2（17 项）由主审逐条亲验代码证据后采纳**；P3 由修复批落地前现场复核。

---

## 一、结论先行

- **完成进度：≈97%。** 总览第三节 24 个阶段：23 个已完成/收口，唯一余量阶段 24（章节结构操作）为「方案就绪、实施待作者指令」——本轮以标记物亲验（`structure.merge` 事件、fm `序`/`并入` 接线、renumber 脚本全零命中）证实其代码零实施，与链记声明一致。十域子代理一致结论：**无存根、无 TODO 欠账、无未接线幻影功能**；唯二声明性留白（PM-10 尾读通道、PM-12 生产 kill 接线）均为台账在册待拍板项，非隐瞒。README 功能承诺 29 项对账 **29/29 成立**（§六）。
- **完成质量：8.8/10（A−）。** 十域分值 A9 / B9 / C8.5 / D9 / E8.5 / F8.5 / G9 / H8.5 / I9 / J8.5。较五十轮（9.1）小幅回落，主因有二：① dev HEAD 全量测试**4 败**（R51-RED，recover 合并整编「同题择版」后测试/实现两侧失同步的账单，合并时按作者「不用测试」口径未跑门禁所致）；② 出现本轮唯一 P1（R51-F-1 履历分组标题回写物理丢失）。两者均为「边界面失同步」而非结构性缺陷。
- **发现总量：P0×0、P1×1、P2×16（含 R51-RED×3）、P3×35，另有证伪/不予采纳×0（子代理零误报入选，全部亲验通过）。**

## 二、基线门禁实测（评审起点，2026-09-06）

| 门 | 实测 | 结论 |
|---|---|---|
| vitest 全量 | **859 文件：5891 过 + 4 败 + 4 跳**（116s） | **红**——4 败见 §三 R51-RED |
| tsc / vue-tsc / eslint | 0 错 / 0 错 / 0 error 0 warn | 绿 |
| check:counts / packaging / knowledge | 三门过（859/5895 口径与 README 一致） | 绿 |
| e2e | 本轮未跑（修复批终门统一补跑） | — |

**4 个失败测试的根因（主审逐一定位，同属「recover 合并整编失同步」家族）：**

- **R51-RED-1** `test/git/re-review-15-delete-ai-versions-warn.test.ts`（2 用例）：R48-68 把 ai-track 裸 `unlinkSync` 换成 `rmWithRetry`（走 `rmSync`，EPERM 入退避集）后，测试的 EPERM 注入口（mock `node:fs.unlinkSync`）不再命中，删失败场景失真 → `deleted=2` ≠ 期望。**修法：测试注入口改钉 `rmSync`（实现升级为有意，测试通道跟进；EPERM 退避路径留真）。**
- **R51-RED-2** `test/studio/r48-server-fixes.test.ts` R48-79（1 用例）：recover 合并裁决记「overview 择 R50-C-2 warn 降级口径（R48-79 的 500 系 stash 树孤立分叉）」，但 R48-79 测试文件随 stash 并入——`state.ts` 现行 `!cfgResult.ok → warn + 默认配置代答`，测试期望 500 IO_ERROR。**修法：恢复显式 500 语义（测试即作者意图正本）：overview 在 book.yaml 读失败时 `replyError(500, 'IO_ERROR', …book.yaml…)`，不再静默代答默认身份。**
- **R51-RED-3** `test/studio/r50-c4-static-spa-fallback-symlink.test.ts`（1 用例）：合并把 R50-C-4 canonical 守卫前置后，`resolveWithinRoot` 对断链 symlink（realpath 抛）判 null → 主路径与 fallback 统一 403 BAD_PATH（fail-closed 更严、两侧口径一致）；测试仍钉旧口径 404。**修法：测试期望改 403 BAD_PATH 并更新头注（安全不变量「无内容外泄」保持）。**

## 三、发现总表

| 编号 | 级 | 域 | 一句话 | 处置 |
|---|---|---|---|---|
| R51-F-1 | **P1** | F | 履历段分组标题（后随条目的 ATX 行）回写被物理删除——解析侧 `continue` 丢弃、三保真槽位均不收、写侧整体重建，作者账本内容不可逆丢失且无登记取舍 | 修（主审） |
| R51-RED-1 | P2 | 主审 | re-review-15 两用例红：EPERM 注入口与 R48-68 rmWithRetry 通道失配 | 修（主审） |
| R51-RED-2 | P2 | 主审 | R48-79 用例红：book.yaml 读失败静默代答 vs 显式 500（合并择版失同步） | 修（主审） |
| R51-RED-3 | P2 | 主审 | R50-C-4 断链 symlink 用例红：canonical 前置后 403，测试钉旧 404 | 修（主审） |
| R51-D-1 | P2 | D | 回收站 `.trash/` 前缀校验施加于原始串，`工作区/.trash/../…` 形态借 `safePathWithin` 只取 `.abs` 弃归一 rel 穿过两道闸——篡改 trash-manifest 可 purge 书内任意文件（Y-18/R27-49 防线失效） | 修（主审） |
| R51-D-2 | P2 | D | `platformCaseFold`/`samePath` 仅 win32 折叠；mac 默认 APFS（大小写不敏感）上 case-only 改名使布线锁键/清单锁键/docJoinKey 分裂——R26-6 族 lost update 在 mac 复现（批次 E 静态平台折叠的设计缺口，case-probe 已把敏感卷拒于门外） | 修（主审） |
| R51-D-3 | P2 | D | `doCopy` 目录段「已存在则原样保留」使 `..` 段原文进入清单登记（doCreate 有 PATH_ESCAPE 前置、copy 无）——docId 身份分裂、保存恒 REVISION_CONFLICT | 修（主审） |
| R51-A-1 | P2 | A | relaunch 三连（relaunch+releaseSingleInstanceLock+quit）不可回滚：flush failed 确认框取消后应用带着「已释放单实例锁+已武装重启」继续运行（双开窗口 + 退出变重启） | 修（主审） |
| R51-A-2 | P2 | A | 渲染崩溃自愈 reload 无 did-fail-load 处理：reload 落在 server 退避重启窗口 → 白屏滞留，server 恢复后无人拉起，打包态无人工出口 | 修（主审） |
| R51-C-1 | P2 | C | openai 线「传输截断且无 usage」不做估计入账（anthropic/responses 两线同场景恒带估计）——此类网关真实消耗记 0，预算/报表系统性偏低 | 修（主审） |
| R51-E-N1 | P2 | E | rebuild「健康报告级」错误（摘要命名不合规、book.yaml 解析失败）被消费面当 REBUILD_FAIL 硬闸：单章机检全 500、树红点机检全灭、R29-5 book-config-degraded 黄项稳态不可达 | 修（主审） |
| R51-E-N2 | P2 | E | strict 短篇升红集不含 unreadable/degraded 族黄项——「检查没跑成」可绿灯过定稿闸 | 修（主审） |
| R51-F-2 | P2 | F | `ensureChapterNotFinalized` 数字前缀匹配不限 `写作/正文/`——定稿章纲（`大纲/章纲/0012-x.md`）误触发正文覆盖拦截 | 修（主审） |
| R51-G-1 | P2 | G | prefs 双端点同一失败形态错误码漂移（书级 `IO_ERROR` vs 全局 `ERROR`） | 修（主审） |
| R51-H-1 | P2 | H | `apiJson` 对「2xx + 非 JSON 体」静默回退 `{}`——getContent 得 `content: undefined`、sha256Revision('undefined') 错误基线，首存必吃 REVISION_CONFLICT | 修（主审） |
| R51-I-1 | P2 | I | `onInterrupt` 是 R70-10「书名捕获+await 后复检」家族唯一漏网：A 书中断失败横幅常驻 B 书工作台无清除路径 | 修（主审） |
| R51-J-1 | P2 | J | harvest-corpus 关键词提取与机检消息模板口径漂移：simile/parallel-streak/sentence-overlong 恒判「被改掉」、repeat 类静默零候选（R50-F-1 同构新形态：漂移点挪到 message 模板） | 修（主审） |
| R51-A-3 | P3 | A | restartPinned 不取消挂起重启，与 start() 口径不对称（现网不可达，API 契约脆弱点） | 修 |
| R51-A-4 | P3 | A | open-library/switch-library 中 saveCurrent 抛错绕过 `{ok:false,reason}` 契约，切库静默无反馈 | 修 |
| R51-A-5 | P3 | A | win 大小写漂移下 recent 判重/current 交换用字符串全等（展示面污染） | 修 |
| R51-A-6 | P3 | A | 握手超时 kill 路径用模块常量而非注入 killWaitMs（测试注入口径不完备） | 修 |
| R51-B-1 | P3 | B | store.ts 首开注释称「登记在锁外」实为锁内——误导后续维护重开双检窗口 | 修（注释） |
| R51-B-2 | P3 | B | saveDraft 锁内复核缺「他者文档移入同路径」方向——极端撞窗产出重复路径清单条目 | 修 |
| R51-B-3 | P3 | B | short-index 对不可读章产 0 字假条目（style.ts 同源助手是跳章）——画像/反转分失真无留痕 | 修 |
| R51-B-4 | P3 | B | SessionRecorder.close 多遮蔽段逐段独立事务——第二段起失败留「部分遮蔽」半态且幂等闸关死重试 | 修（并单事务） |
| R51-C-2 | P3 | C | responses 线 user 消息 text 先于 function_call_output，与 openai 线 R31-6 块序相反（当前不可达） | 修（对齐+注） |
| R51-C-3 | P3 | C | anthropic message_delta 末见 wins 合并丢先前 delta 的 cache 计量（非标网关三条件叠加） | 修（保留前值） |
| R51-D-4 | P3 | D | `isWithinRoot` 对不存在路径无条件放行且头注失实——误用脚枪（现调用点均不可达利用） | 修（词法校验） |
| R51-E-N3 | P3 | E | buildIndex 维度失配晚检：全量 embed 烧完才比对既有索引维度（纯烧钱） | 修（首批后即检） |
| R51-E-N4 | P3 | E | `dirFp` 把非 .md 文件计入纪元指纹——目录混入临时文件即整表清空（增量缓存永久失效） | 修（只计 .md） |
| R51-E-N5 | P3 | E | 意象检查不剥对白（禁词/开头检查同文件均剥）——对白密集章意象黄项刷屏 | 修（剥对白） |
| R51-F-3 | P3 | F | book 段 volume_size/target_words/chapter_target_words 坏值静默忽略，违背同文件 R37-11/R76-15 warn 纪律 | 修（补 warn） |
| R51-F-4 | P3 | F | `parseRealmSystems` 不支持块式序列且无留痕——标准 YAML 手写形态整段失明 | 修（补 warn） |
| R51-F-5 | P3 | F | `slimIronRules` 全局压 `\n{3,}`——无遗留段也改写排版且 details 失真 | 修（条件化） |
| R51-F-6 | P3 | F | 章 `_raw` 数组型未知字段 `String()` 化——leads 同族已修（R64-17）此处未跟 | 修（对齐） |
| R51-F-7 | P3 | F | 导出全空白（非空串）正文章计入章数产空壳；`patchFlatFm` 重复键静默丢弃无留痕 | 修（trim 判空+warn） |
| R51-G-2 | P3 | G | 多个写端点占闸先于 readJson：body 悬持窗口最长 300s 悬持书级闸 | 修（readJson 闲置超时） |
| R51-G-3 | P3 | G | router 路径段转义集不含 `()`——未来含括号路由的埋雷 | 修（一行） |
| R51-H-2 | P3 | H | prefs.init 对「200 缺 prefs 字段」无防御——TypeError 沿 top-level await 冒出致应用不挂载 | 修（?? {}） |
| R51-H-3 | P3 | H | doc.refresh 成功分支不推进 treeRev——refreshed 文档每次树刷新冗余重拉 | 修（对齐 doSave） |
| R51-H-4 | P3 | H | clearFalsePositiveMarks 内联重拼键前缀，与 R50-D2-1 单源自违 | 修（一行） |
| R51-H-5 | P3 | H | useSse 换票失败回退通道仅 console.warn 每连接一条，无去重观测口 | 修（去重） |
| R51-I-2 | P3 | I | `@` 补全触发对外部全量替换事务同样生效——后台同步后自动弹补全浮层 | 修（userEvent 判别） |
| R51-I-3 | P3 | I | 切文档分支不处理 selection——切章后光标落章末（同文档路径已收口） | 修（锚定章首） |
| R51-I-4 | P3 | I | 流式生成中可存半章草稿并切离工作台（按钮/入口均不查 running） | 修（入口闸） |
| R51-I-5 | P3 | I | OnboardView 重新生成确认弹窗滞留切书后仍发计费请求（ask 后缺 stillOn） | 修（一行） |
| R51-I-6 | P3 | I | CmHost 切文档分支无 IME 组合期守卫（同文档路径有）——组合期切章丢字 | 修（组合期挂起） |
| R51-J-2 | P3 | J | corpus-commit 对「合法但非数组」存量 JSON 裸 TypeError 崩整轮合并 | 修（Array.isArray） |
| R51-J-3 | P3 | J | dev-api EADDRINUSE 指引带进死胡同（PORT 与 vite 代理双写死） | 修（env 单源） |
| R51-J-4 | P3 | J | 语料回归门无 checkId 存在性守卫——silent-only 文件在 checkId 死亡后恒真绿 | 修（守卫用例） |
| R51-J-5 | P3 | J | e2ePort 偏移无上界机器校验（MAX_PORT_OFFSET 仅注释性） | 修（断言） |
| R51-J-6 | P3 | J | usage-card.spec 缺 EADDRINUSE 人话提示（同批 5 spec 均有） | 修（对齐） |

## 四、P1/P2 详录（证据链要点）

> 逐条完整证据（file:line + 引文 + 失败场景 + 修法 + 置信度）见 §三 表内修法列与各域子代理报告存档；此处录主审亲验中承重的事实锚点。

1. **R51-F-1（P1）**：`src/format/leads.ts:145-148` 分组标题（`headingEndsSection` 为 false 的 ATX 行）走 `continue`——不进 `entries`、不进 `preamble`（仅收 `entries.length===0` 的散文，且 ATX 分支先行）、不进 `_bodyAfterHistory`（仅节终标题）；`writeLead`（:382-400）按三槽位整体重建正文。R75-2 注释自述「分组标题（后随条目）跳过」仅是解析侧取舍，全库无「写入即删」登记，`test/format/leads.test.ts:408` 往返测试只钉节终标题。定稿回写（lead-finalize）、作者保存（service）、红补生成（check/run）三个写入口全部触发。**定级 P1：作者可写内容的静默不可逆丢失。**
2. **R51-D-1（P2）**：`trash.ts:234-238/:409-413` 前缀校验施加于 `entry.trashedPath` 原始串；`safePathWithin`（:479-481）= `resolveWithinRoot(...)?.abs ?? null` 归一 rel 被弃。`工作区/.trash/../项目/文档清单.jsonl` 双闸全过、归一落书内放行——purge 永久删主清单（strict 读 ENOENT → 空表重建，R27-49 描述的事故复现）。代码自身把 trash-manifest 定为可篡改数据面（Y-18），故为现实威胁模型下的防线缺口。
3. **R51-D-2（P2）**：`safe-path.ts:142-144` 恒等分支 `process.platform === 'win32' ? key.toLowerCase() : key`；`case-probe.ts:5` 自认「win（NTFS）与 mac（默认 APFS 卷）均大小写不敏感」。mac 默认卷 case-only 改名（Finder 手滑）→ 布线锁 `wiringFileLockKeyOf` 两消费源大小写漂移 → 双 `.lock` 互斥失效（R26-6/R29-7/R40-15 修掉的回写覆盖窗在 mac 重开）；`docJoinKey` 失配 → executeSave 恒 REVISION_CONFLICT。批次 E 的 probe 门只拒敏感卷、不补默认卷折叠。**决策注记：本修使 mac 侧键字节变更（锁文件为瞬态、无持久数据迁移面）；新旧版本进程混跑窗内互斥按「升级窗口」口径披露于收口记。**
4. **R51-E-N1（P2）**：`cache/rebuild.ts:348-349/:514-517` 把 book.yaml 解析失败与摘要命名不合规推进 `errors`（健康报告语义，:15 头注「单章解析失败不中断重建」）；`check/run.ts:74-82` 对任意 error 一律 500 REBUILD_FAIL、:473-477 `rebuildFailed → 机检红点全灭`、:112-125 R29-5 降级黄项被前置闸阻断为稳态不可达。一个手放 `笔记.md` 即瘫单章机检+树红点+三审打包链。
5. **R51-H-1（P2）**：`api/client.ts:201-207` 非 JSON 体 catch 内仅超时分支上抛，其余 `body = {}`；注释口径（304/204 无体合法）与本 API 面不符（服务端统一 JSON 信封，无 200-无体端点）。getContent（documents.ts:4-9）直取 `data.content`。
6. **R51-J-1（P2）**：`scripts/harvest-corpus.ts:61-69` quotedOf 只认引号片段与 `词×N`；引擎侧 simile（count.ts:919「像…」模板字面量）、parallel-streak（:701）、sentence-overlong（:630）的引号内均为截断前缀+省略号 → `finalBody.includes` 恒 false 恒判「被改掉」；复读消息（:196/202）无锚点 → 零候选。采集面数据双失真 + corpus 门假红风险。

## 五、P3 要点归组（修复批按域分组落地）

- **契约一致性族**：A-3/A-4/A-6（desktop API/注入口径）、C-2（块序对齐）、G-1 已升 P2 表、H-4（单源自违）、F-6（leads 已修章未跟）、J-6（错误提示对齐）。
- **留痕缺失族**（违背 R37-11「写了但不生效无迹可查」纪律）：F-3/F-4/F-7B（warn 补齐）、B-3（跳章留痕）、H-5（回退去重留痕）。
- **窗口/半态族**：B-2（复核补向）、B-4（并单事务）、G-2（readJson 闲置超时）、I-4/I-5（入口闸复检）、I-6（组合期挂起）、E-N3（维度早检）。
- **口径/性能族**：A-5（samePath）、E-N4（dirFp 只计 .md）、E-N5（剥对白）、F-5（条件化收敛）、H-3（treeRev 对齐）、I-2（userEvent 判别）、I-3（切章锚定章首）、G-3（转义补 ()）、D-4（词法校验）、J-2/J-3/J-4/J-5（脚本与门守卫）。

## 六、根 README 29 项承诺对账（29/29 成立）

逐项核对根 README 用户可见承诺与代码实态（对账面 = 十域子代理高风险面结论 + 主审抽查）：建书目录自动生成✓ / 设定表单✓ / 全自动写章（机检打回+重试上限停问）✓ / 三审✓ / 逐章定稿+防吃书闸✓ / 伏笔全程追踪✓ / 字数曲线预警✓ / 文风系统（样章/手法/禁词，机检与 AI 同源）✓ / 选区 AI 改写分析✓ / 对话助手（风险动作先问）✓ / 编辑器自动保存+⌘S✓ / 版本校验（重载/覆盖）✓ / 保存前自动快照分层保留✓ / 历史恢复且恢复本身留底✓ / 一书一文件夹结构✓ / 短篇集三段式✓ / book.yaml 阈值+全局默认继承+全局固定✓ / 只绑 127.0.0.1✓ / Host 精确匹配（DNS rebinding 防护）✓ / Origin 白名单✓ / 会话令牌闸（读+写，/api/boot 与 SSE 票据豁免面最小化）✓ / Key 加密存储不进 git 不明文入日志✓ / 定稿原子写+指纹校验✓ / AI 链路零 spawn CLI（历史/迁移 spawn Git）✓ / 事件 append-only 全量落库（每书 SQLite，审计视图清理）✓ / 书库文件夹跨机互拷✓ / `.版本/` 恢复✓ / 「本地服务未连接」与 AI 繁忙文案分流✓ / 平台披露事项（签名/更新/Git/同步盘/大小写卷）✓。

注：承诺本体全部成立；R51 新发现中的 D-1/D-2/A-1/H-1 属承诺防线的**边界缺口**（正文保存红线主链完好，「编辑永不静默丢失」在受影响边角仍有兜底：回收站留底/冲突流/版本历史），修复批闭合后完全体。

## 七、修复计划（作者指令「全部修复」）

- **批 1（主审亲修，17 项）**：R51-RED-1/2/3 + F-1（P1）+ D-1/D-2/D-3 + A-1/A-2 + C-1 + E-N1/E-N2 + F-2 + G-1 + H-1 + I-1 + J-1。P1/P2 全配回归测试（RED-1/3 为测试对齐改，RED-2 为实现恢复）。
- **批 2（文件互斥并行代理，P3×35 按域三路）**：α 服务与脚本面（G-2/G-3/E-N3/E-N4/J-2/J-3/J-6）；β 前端面（H-2/H-3/H-4/H-5/I-2/I-3/I-4/I-5/I-6）；γ 引擎面（B-1/B-2/B-3/B-4/C-2/C-3/D-4/E-N5/F-3/F-4/F-5/F-6/F-7/J-4/J-5/A-3/A-4/A-5/A-6）。
- **终门（L2 九件套）**：vitest 全量 + tsc + vue-tsc + eslint + 三 check + e2e；收口记回填本报告 §十。
- **验收口径**：全量绿（0 败）+ 发现项逐条对账（修/办结/登记三分，登记项须给出依据）。

## 八、测试覆盖缺口汇总（修复批顺带收口 P1/P2 相关项）

十域合计提出 50 条缺口，P1/P2 直接相关已入修复批：trash `..` 段变体（D-1）、darwin casefold 回归（D-2）、copy `..` 段（D-3）、relaunch 取消后效（A-1）、apiJson 坏体（H-1）、REBUILD_FAIL 输入分级（E-N1）、strict×unreadable 组合（E-N2）、onInterrupt 切书守卫（I-1）、quotedOf×消息模板（J-1）、ensureChapterNotFinalized 非正文（F-2）、openai 截断无 usage 契约（C-1）。其余 P3 缺口择要随批补（不强制全量补齐，避免测试面膨胀失焦）。

## 九、进度评估依据

- 总览第三节 24 阶段行逐行核对：w/x/y/aa~23 全部已完成/收口/关闭；**阶段 24 唯一开放**（执行方案 2026-09-04 落盘，实施待作者指令）——标记物亲验零实施与声明一致。
- 台账开放项：PM-10 尾读通道、PM-12 生产 kill 接线（两项待作者拍板立项，非缺陷）；win 侧快照/RAG/历史面日常补验（挂账）；R47/PM/R48 三报告销项归档待门绿后办结（其修复批已经 recover 合并在位）。
- 功能面：十域「完成度」结论一致为满分或无存根（H 域 10/10，余 9~10/10）；仅 ReviewPanel 定位增强为注释声明的后续切片。
- 综合判定：**进度 ≈97%**（余量 = 阶段 24 实施 + 两项待拍板性能挂账 + 本轮 52 项发现修复）。

## 十、收口记（2026-09-06 回填——修复批全量落地 + L2 终门九件套全绿）

### 10.1 修复批执行记

- **批 1（主审亲修 17 项）**：RED-1/RED-2/RED-3 三红收口（RED-1/3 测试对齐改〔注入口钉 rmWithRetry / 断链 symlink 期望改 403 BAD_PATH〕，RED-2 实现恢复——overview 在 book.yaml 读失败时显式 `replyError(500, IO_ERROR)` 不再静默代答默认身份，测试即作者意图正本）+ F-1（P1 履历分组标题 writeLead 保留）+ F-2 + D-1/D-2/D-3 + A-1/A-2 + C-1 + E-N1/E-N2 + G-1 + H-1 + I-1 + J-1。行为修复全配回归测试并做红绿验证（新增测试 8 文件：r51-c1 / r51-d1-d2-d3 / r51-f1-f2 / r51-en1-en2 / r51-g1 / r51-h1 / r51-i1 / r51-j1）。**契约级变更披露**：① D-2 `samePath`/`platformCaseFold` 平台折叠集收口 darwin（默认 APFS 卷大小写不敏感）——mac 侧键字节变更（锁文件瞬态、无持久迁移面），新旧进程混跑窗互斥按「升级窗口」口径；② A-1 relaunch 武装时机后移到不可退点（确认放弃分支显式清除意图，「退出变重启」劫持消除）；③ E-N1 rebuild 产出 `warnings`/`errors` 分桶（建议性留痕与健康失败分离，消费面语义不变）。
- **批 2（P3×35 三路文件互斥并行代理，≤5 并发约束内 3 路同发）**：α 服务与脚本 7/7 修（G-2 readJson 闲置超时 408、G-3 路由转义补 `()`、E-N3 rag 维度首批即检、E-N4 dirFp 只计 .md、J-2 corpus-commit 非数组防御、J-3 dev-api 端口 env 单源〔新增 `src/studio/server/dev-port.ts`〕、J-6 usage-card.spec EADDRINUSE 提示对齐）；β web-next 前端 9/9 修（H-2 prefs 防御、H-3 refresh 推进 treeRev、H-4 fp 前缀单源、H-5 SSE 告警去重、I-2 `@` 补全 userEvent 判别、I-3 切文档锚定章首、I-4 存草稿 genBusy 闸、I-5 Onboard stillOn 复检、I-6 切文档 IME 组合期挂起）；γ 引擎 19 项 = 18 修 + 1 注释勘误（A-3/A-4/A-5〔复用 D-2 samePath〕/A-6、B-2〔真身 process/draft-pipeline.ts〕/B-3/B-4〔真身 events/chat-bridge.ts〕、C-2 块序对齐/C-3 cache 计量保留前值、D-4 isWithinRoot 词法校验、E-N5 意象剥对白、F-3~F-7 warn 纪律族、J-4 语料门 checkId 守卫〔纯测试〕、J-5 e2ePort 上界断言；B-1 锁内注释勘误）。三路合计新增回归测试 30 文件，红绿回退验证 35 cycle 全成立（α 5 / β 8 / γ 22，含 5 次假红重做）。

### 10.2 终门拦下并顺带修复的两处（收口前终门实证）

- **r44 占用判重回退臂过时**：`test/install/r44-occupy-dev-ino.test.ts` stat 失败回退臂钉「darwin 字符串全等放行」旧口径，与批 1 D-2 darwin 折叠决策冲突（全量首跑 1 败暴露）——按新口径对齐（mac 默认卷 Foo/foo 回退路径亦拒双登记；大小写敏感卷合法异名库仍由 dev+ino 主路径放行），install 面 170 用例复绿。
- **playwright testIgnore 泛化（e2e 静默未跑缺陷）**：γ 新增 `test/e2e/r51-j5-*.test.ts`（vitest 用例）命中 playwright 默认 testMatch 被收成第 30 个 spec，加载期 vitest import 在 playwright 进程无内部状态 → runner 级崩溃、整轮 e2e 没跑，且 `| tail` 管道掩盖真实退出码。`testIgnore` 从单点豁免泛化 `'**/*.test.ts'`（playwright 只认 `.spec.ts`），复跑 EXIT=0。

### 10.3 L2 终门九件套实测（2026-09-06，macOS arm64）

vitest 全量 **897 文件 / 6009 过 + 4 跳 / 0 败**（4 跳为既有平台条件跳过）；tsc **0 错**；vue-tsc **0 错**；eslint **0 错**；check:counts **过**；check:packaging **过**；check:knowledge **过**（知识层 13 条 manifest 对账一致）；e2e **43 过 + 2 跳**〔release smoke 2 例需 `CLWRITING_E2E_RELEASE`，README 常规命令口径 43/45〕，EXIT=0。

### 10.4 发现项三分对账（52 项）

**修 52 / 办结 0 / 登记 0**：批 1×17（RED-1/3 测试对齐 + RED-2 实现恢复 + 15 项代码修复）+ 批 2×35（34 项代码修复 + B-1 纯注释勘误 + J-4 纯测试守卫 + C-2 不可达面防御性对齐）。§八测试缺口 P1/P2 相关 11 项全随批落地，其余 P3 缺口按 §七口径择要不强制全量补齐。

### 10.5 README 修账与索引回改

- 根 README：测试文件数 859→**897**、单测数 5895→**6009**（徽章/npm test/技术栈/状态段四处 + 开发段文件数），check:counts 门禁绿。
- 总览 1.1 文档地图 / 台账 §一 / 01-评审 README 维护行与清单行：R51 状态「未收口」→「**已收口**（本节为验收证据）」。
- **遗留（待作者确认闸放行）**：R50 报告归档（R51 已收口触发「归档上一轮」）；R47/PM/R48 三报告销项归档（§九原注「待门绿后办结」——门绿已达成）——四篇移动涉确认闸，目标路径 `01-评审/Archive/`（扁平）报请作者确认后执行。win 腿实测数字（README 注记仍钉四十六轮 814/5580）待 win 侧日常补验刷新。
