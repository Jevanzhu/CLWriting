# 全量代码复审与进度质量评估（三十轮）·评审报告

- 日期：2026-08-30（第三十轮 = 链第八十二轮）
- 基线：HEAD `5e1785b`（dev，二十八轮收口提交）+ 工作树 92 处改动（66 文件 +1075/−242 + 26 新测试文件，即二十九轮修复批已收口未提交）——工作树现状整体纳入评审面。
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，告诉我项目完成进度，完成质量，最后形成一个文档给我。记得编排下任务。」
- **本轮不含修复**；报告路径经确认闸 AskUserQuestion 报作者**获批**（`01-评审/全量代码复审与进度质量评估三十轮-评审报告-2026-08-30.md`）。
- 独立性声明：六路分域子代理与主评审全程**禁读 `Dev/` 全部历史评审报告正本**（仅读根 README 作产品宣称基线与总览路线作规划对照基线），从零独立取证；结论全部来自源码与实测。

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审方法与覆盖

- **六路分域禁读子代理**（A AI 链路 / B 机检格式 / C 核心数据 / D 服务端桌面 / E 前端 web-next / F RAG·知识·导出·脚本·测试工程）全部首发成活零阵亡；各域逐文件盘点 + 核心文件深读。
- **主评审亲验**：P2×8 逐条对照源码亲验属实（含 ABBA 锁序两侧锁型核对、`doc.refresh` 吞错链核对、roster 类型核对、CI 触发面核对）；B 域两条经子代理 /tmp 脚本实测复现。
- 评审面：后端 `src/` 21 模块 ≈6.34 万行 TS + `src/studio/web-next` ≈3.39 万行（vue+ts，node_modules 剔除）+ `test/` 638 个 .ts（≈10.4 万行；598 个 .test.ts + 29 e2e spec 盘点 + 抽样深读）+ `scripts/` 11 件 + 门禁配置全套（vitest/eslint/tsconfig/playwright/tsup/ci.yml/electron-builder）。
- 编排：TodoWrite 全程任务编排（摸底 → 门禁 → 六域并行 → P2 亲验 → 汇总 → 确认闸 → 落盘同步）。

## 二、门禁实测（主评审亲跑，八件套全绿零观察项）

| 门禁 | 结果 |
|---|---|
| vitest 单测 | **598 文件 / 4623 单测全过**（119.65s，一次全绿） |
| tsc --noEmit | 0 错 |
| vue-tsc --noEmit（web-next） | 0 错 |
| eslint | 0 错 |
| check:counts | 过——实测 598/4623 + 29 spec/45 用例，与根 README 零漂移 |
| check:packaging | 过 |
| check:knowledge | 过 |
| e2e（Playwright，mock 驱动） | **43 过 / 2 跳**（发布 smoke 需环境变量），29.2s 零误红 |

## 三、完成进度结论

**综合 ≈97.5%：核心 mac 生产形态 ≈98.5% / win RC 形态 ≈96%。**

对照总览规划逐项实证：

| 规划项 | 状态 |
|---|---|
| 阶段 0-12、14-20、22、23（主线全部批次） | 已完成/已收口 |
| 阶段 13 Responses 启用批 | 代码+测试双绿；唯余「中转网关真机验证」——外部凭据依赖，非代码欠账；encrypted 直测缺口（responses-encrypted.test.ts 7 用例）已在树内关闭 |
| 阶段 21 win 适配 | J0-J4、J7 完成；J5 体验面大件在树（菜单隐藏 / WCO 外观对齐+避让修正 / 字体缓存 / git ENOENT 人话+坚果云副本检测 / 长路径冒烟 / rename EPERM 重试）；J6 披露已落（根 README Windows 须知 + electron-builder），签名按 D4 拍板延后；J6b 跨平台书库迁移挂账 |
| CI | 3OS（macos/ubuntu/windows）× 2 Node 矩阵在位；触发面见 R30-8 |

分域完成度（子代理逐模块盘点）：

| 域 | 完成度 | 依据 |
|---|---|---|
| A AI 链路 | 98% | provider 97 / runner 99 / calls 100 / orchestrate 98 / driver·scripts 100；唯一留白 failure.ts switch/shrink 动作无消费者（已登记） |
| B 机检格式 | 98% | check 98 / format 97 / review 100；全实装无 stub，余死代码 1 件（R30-17） |
| C 核心数据 | 99% | document 98（snapshot.ts 兼容别名层待退役）/ state·events·fs·cache·metrics·git 100 |
| D 服务端桌面 | 100% | server/api 99（104 处内联校验未迁 parse 声明，校验本身在）/ desktop·install·process 100 |
| E 前端 web-next | 100% | 16 stores / 14 composables / 27 api / views / 102 组件全部接线，0 无引用文件，零 v-html |
| F RAG·导出·脚本·门禁 | 95% | rag 97 / knowledge·learn·export·log 95 / scripts 92 / 门禁工程 90 |

## 四、完成质量结论

**综合 9.1/10**（分域 A9.2 / B8.7 / C9.2 / D9.3 / E9.2 / F9.2）。

- 质量主线：经二十九轮持续修复后处于本链历史最高位——零 P0/P1（P1 连续轮次归零后维持），P2 从上轮 13 条降至 8 条且无一条属主链路功能错误，方向集中在「启发式边界漏报」「跨进程争用下的延迟/等待」「已登记取舍的口径披露」三类。
- 工程纪律贯穿可见：单源化（引号集/说话动词/两端闭合/HANZI 区间）、fail-noisy（读失败产黄不静默）、锁基建成体系（跨进程锁+超时+续期+锁内复核）、每处历史修复有 R 编号注释锚点与对应回归测试。
- 测试工程质量高：无 .skip/todo/expect(true)；断言为行为级（真子进程锁、raw socket、字节不变性）；平台门 skipIf 78 处注明互补腿；元门禁 check:counts 对账 README。

## 五、发现问题（P0×0 / P1×0 / P2×8 / P3×22）

P2×8 全部主评审亲验属实；其中 R30-4、R30-8 带「已登记取舍/临时态」注记。

### P2（8 条）

| # | 位置 | 问题与影响 |
|---|---|---|
| R30-1 | [src/check/quotes.ts:25 + src/check/count.ts:110] | QUOTED_SPAN_RE 字符类 `[^闭引号]` 天然匹配换行，checkBannedWords 对**整 body** 剥离——某段对白漏写闭引号（AI 草稿常见）时，从该开引号到下文任意闭引号（可隔多段）之间的全部叙述被当对白剥除，**禁词红闸对其静默漏报**且无黄项提示（子代理实测：63 字剥掉 60 字）；checkOpeningNoEnv 同源受影响。方向=漏报（该打回的不打回）。quotes.ts:28 注释「按行调用」与实际整 body 用法漂移。 |
| R30-2 | [src/check/count.ts:374] | checkNewNames 以 `roster.includes(name)` 全文子串判「已登记」（roster 为名册文件全文字符串）——名册更长名字包含候选（「林晚晴」⊃「林晚」）时误判已登记，**独立新角色漏报**（实测确认）。 |
| R30-3 | [src/ai/calls.ts:268-281 / src/ai/provider/store.ts:343-354] | 记账与配置保存在生成收尾路径**同步**执行跨进程锁等待（Atomics.wait 封顶 5s）——CLI+桌面双进程争用时承载 SSE/全部接口的服务进程事件循环冻结至超时。 |
| R30-4 | [src/ai/runner.ts:143-160] | 降级记忆通道按模块级「最近 resolve 的 userDataPath」路由——同进程双库并发生成时降级标记写错 providers.json（注释已登记「活跃库优先」取舍，单活跃库进程不触发）。 |
| R30-5 | [src/document/service.ts:343-469 ↔ src/document/finalize.ts:62-148] | 锁序反向对：executeSave 持布线文件锁内取清单锁（maybeUpdateManifest:466），finalizeRevision 持清单锁内取同名布线文件锁（applyLeadUpdates）——双进程对同一布线文件并发时 ABBA 等待；靠 5s/2×5s 超时兜底（不死锁不丢数据，清单跳过自愈/回写留源重试），但双侧注释各自称「单向无环」，**交叉对未在锁序分析中覆盖**。 |
| R30-6 | [src/fs/cross-process-lock.ts:247 + service.ts:331-364 + manifest.ts:246-265] | 全部锁等待为主线程同步睡：单次 executeSave 最坏阻塞事件循环 ≈20s（save 5s + 布线 5s + 清单 2×5s）——各点均注明取舍，但**链式最坏值无一处汇总标注**，GUI 真实争用下可感知停顿。 |
| R30-7 | [src/studio/web-next/src/components/panels/HistoryPanel.vue:121-123 ↔ stores/doc.ts:278] | `doc.refresh` 内部 catch{} 静默吞错后，HistoryPanel 仍 toast「已恢复」——恢复落盘成功但编辑器基线未对齐时提示失真，下次编辑以 REVISION_CONFLICT 自愈暴露（有兜底但提示失真）。 |
| R30-8 | [.github/workflows/ci.yml:9-11] | CI 触发仅 PR→main + workflow_dispatch——push main 的 merge commit 无自动验证（注释声明频推期临时态，R74-5 已恢复 PR 腿；push 腿恢复无定时兜底，**作者拍板项，沿 R28-29/R29-11 口径登记**）。 |

### P3（22 条，登记清单）

| # | 位置 | 问题 |
|---|---|---|
| R30-9 | [src/ai/provider/responses-adapter.ts:399-414] | 流中 failed/error 事件恒 retryable:false，与 anthropic/openai 线按 status 可重试不一致（有意保守+注释论证，vendor 瞬态 5xx 退化为终态） |
| R30-10 | [src/ai/provider/anthropic-adapter.ts:77-87] | block 全为 reasoning 时 flatMap 产出空 content:[] 会 400，依赖上游 sanitizeHistory 兜底（最后防线非保证） |
| R30-11 | [src/ai/provider/redact.ts:27] | 智谱 key 形态正则（hex32.hex32）误伤正文中同形 hash 对（仅影响错误文案，保守方向） |
| R30-12 | [src/ai/provider/anthropic-adapter.ts:103] | 注释「兜底 8192」与实际 MAX_TOKENS=16384 漂移 |
| R30-13 | src/ai/provider/* | stopReason 命名三线未归一（'stop' vs 'end_turn'），现消费方无实害 |
| R30-14 | [src/check/manifest-check.ts:68] | realCurve 混入 NaN 强度时 Math.max 传染 → emotion-curve-peak-low 漏判（有黄项兜底，strict 下提红，仅单项漏报） |
| R30-15 | [src/format/style-inject.ts:52] | 禁词注入段整段 join 注入与机检侧拆词口径分裂——unparsed 条目两侧禁词面不一致 |
| R30-16 | [src/review/run.ts:66] | planned_calls 注释「满审/顺序审=3」过时（已改按 lenses 动态） |
| R30-17 | [src/check/lead-updates.ts:38] | readChapterLeadUpdates 已登记零生产调用（R66-15），删除未落地 |
| R30-18 | [src/document/service.ts:80,91 / journal.ts:279 / manifest.ts:210 / lead-finalize.ts:40 / batch-pause.ts:27] | `export let` 超时常量 6 处——events/store.ts R26-105 已认定该模式可被外部静默改写并收口，document/state 侧未跟进 |
| R30-19 | [src/document/version.ts:546-551] | migrateVersionsDir 失败日志称「读取方仍兼容旧位置」不实——listVersions 只读 .版本，旧快照 UI 不可见（盘上文件仍在） |
| R30-20 | [src/document/service.ts:565-572] | snapshotPolicy 每次 save 都 existsSync+readFileSync global.json，autosave 高频下徒增 IO |
| R30-21 | [src/studio/server/api/stream.ts:354] | driver.stream() 同步抛出时 SSE 已 writeHead(200) 且 headersSent 不回错不 end——连接悬挂至客户端自断（概率极低） |
| R30-22 | [src/install/migrate-layout-v2.ts:79-89 对 149-151] | migrateManifestPaths 按前缀无条件改写 entry.path——moveTree 同名冲突跳过的文件其清单条目也改指新路径，docId 挂到冲突胜者上（有 errors 横幅兜底可见性） |
| R30-23 | [src/studio/server/static.ts:101-118] | 静态响应缺 `X-Content-Type-Options: nosniff`（Electron 态有 CSP 兜底，CLI 直跑态无） |
| R30-24 | [src/desktop/main.ts:688-689,703-704] | desktop:open-shelf / open-library-window 两 handler fire-and-forget 调 async——窗工厂早期抛错成 unhandledRejection |
| R30-25 | [src/studio/server/api/stream.ts:253-256] | SSE `?token=` URL 兼容通道保留——旧客户端 token 仍进 URL（进程列表/代理日志面；新前端已切 ticket） |
| R30-26 | [src/studio/web-next/src/composables/useShelf.ts:222 ↔ OnboardPremise.vue:11] | 梗概键 `clwriting:onboard-premise:${name}` 双源硬编码，未收敛进 shared/storage-keys.ts（R28-3 同族隐患） |
| R30-27 | [src/studio/web-next/src/composables/useSse.ts:161] | 未知名/空 type 事件仍进 workbench.log，无白名单过滤 |
| R30-28 | [eslint.config.js:12] | test/（598 文件）不在 lint 射程（存量 45 错已登记待清）——最大代码面零机器风格约束 |
| R30-29 | [scripts/harvest-corpus.ts:88] | 解构丢弃 readChapterDir 的 errors——章级解析失败静默跳过，系统性故障以「候选 0 条」成功口径收场 |
| R30-30 | [test/export/export.test.ts:92] | 注释「3 位补零」与实现/断言 4 位（0001）漂移 |

## 六、亮点（跨域合并）

1. **保存协议纵深**（C/E 域）：per-doc 队列+跨进程 save 锁+revision 基线+journal pending/settled/aborted+写前快照+字节级 UTF-8 防线（GBK 覆写拒写）+回收站复活守卫+独占防覆盖；前端配合快照保存/manual 链式/conflict 跳过 autosave/卸载同步兜底。
2. **安全三层防线实现超出宣称**（D 域）：宣称「GET 不校验令牌」，实际 GET/HEAD 已全量 token 闸（豁免仅 /api/boot 与 SSE，SSE 另有一次性 ticket 双凭据）+ timingSafeEqual + Host 精确匹配 + 日志只记 path 不记 query。
3. **vault 信封加密**（A 域）：HKDF→KEK→DEK→AES-GCM + 0600 原子写 + bak 残留收敛 + 迁移回读校验；apiKey 全仓唯一 console 出口已掩码。
4. **预算与记账**（A 域）：三口径预算闸（次数/tokens/cost）+ 四路同口径入账 + 损坏保守阻断 + 重试不可绕闸；三协议 usage 统一「input 不含 cache」四档分计。
5. **fail-noisy 纪律**（B 域）：禁词解析不出词产黄、名册/章纲读失败产黄、book.yaml 降级透黄——系统性消灭「静默失明」（本轮 R30-1/2 即是从残余漏报面里挖出的仅存两条）。
6. **原子写与崩溃恢复**（C/F 域）：tmp+fsync+rename+目录 fsync 全套、EPERM/EBUSY 退避、崩溃残留 tmp 双门清扫；RAG 整章小事务续传 + 嵌入响应三重校验（防向量错配毒库）。
7. **utilityProcess 管理**（D 域）：三档退避+封顶转对话框+稳定窗清零+SIGKILL 升级前重读 pid 防复用误杀。
8. **测试即门禁**（F 域）：check-counts 元门禁（README 对账+.only 拒绝+零断言文件门+spec 顺序快照）；coverage 多桶阈值门；e2e pageerror 升红。

## 七、修复计划建议（待作者指令）

- **批 A（机检正确性，建议优先）**：R30-1（引号剥离按行/或跨段护栏+黄项提示）、R30-2（roster 按行/按名精确匹配）；连带 R30-15 口径统一、R30-14 NaN 守卫。
- **批 B（并发与锁）**：R30-5（锁序分析补交叉对，统一 save→file 单向或 finalize 侧先取布线锁）、R30-3/R30-6（记账路径锁等待异步化或链式最坏值汇总标注）、R30-4（登记维持或 path 维度显式传参）。
- **批 C（边角与工程）**：R30-7（refresh 失败 toast 失真）、R30-8（作者拍板项：push 腿恢复时点）、P3 高价值件（R30-18 常量收口、R30-22 清单条目守卫、R30-26 键名单源化、R30-28 lint 射程、R30-29 解构告警）。

## 八、结论

三十轮独立从零复审（禁读全部历史评审）：门禁八件套亲跑全绿与 README 零漂移；**进度 ≈97.5%（核心 mac 生产形态 ≈98.5% / win RC 形态 ≈96%）/ 质量 9.1/10**——本链历史最高位；P0×0 / P1×0 / P2×8（全部亲验，无主链路功能错误，集中于启发式边界漏报×2、跨进程争用延迟×3、取舍披露×2、CI 临时态×1）/ P3×22。上轮（二十九轮）修复批（在树未提交）经本轮全域深审无回归发现。修复计划 §七 批 A-C 待作者指令。

## 九、收口记（2026-08-30，作者指令「开始修复，全部修复，记得编排下任务，并发做，修完之后提交改动」）

**30 条全办结：修 25 + 登记维持 5。** 执行形态：主评审先单独提交二十九轮收口存量（`cdb19c0`，92 文件，干净归因）→ 主评审亲修共享基建（fs/cross-process-lock.ts 新增 `acquireCrossProcessLockAsync` 异步占锁原语 + 5 单测，供批 A/C 共用防同文件冲突）→ 三波文件互斥并行修复代理**全部首发成活零阵亡**（波 1 = 批 A AI 链路 / 批 B 机检格式 / 批 C 核心数据锁；波 2 = 批 D 服务端桌面 / 批 E 前端；波 3 = 批 F 工程质量），波间全量回归。

- **P2×8 = 修 7 + 维持 1**：R30-1 QUOTED_SPAN_RE 字符类补排 `\n`（引号片段不跨行，63 字吞噬场景禁词检出，quotes-matrix 契约补 2 行跨行用例）；R30-2 新增 parseRosterNames 局部单源精确判重（林晚晴⊃林晚 仍报候选）；R30-3 记账/providers 保存争用路径改异步轮询等待（无争用快路同步直行，r29-2② 同步 throw 契约保持）；R30-4 降级记忆显式 path 贯穿（resolve→store 通道→分发器，resolve A→B 后 A 的降级落 A）；R30-5 锁序统一「布线锁→清单锁」（finalize 预取全部目标布线锁 + applyLeadUpdates 拆持锁核心/自取锁包装，spawn 双进程 ABBA/反向竞态回归 3 用例）；R30-6 save/finalize 链锁等待全异步化（withManifestLockAsync 新增 + finalizeRevisionAsync 孪生 + 服务端 documents.ts 两调用点切换【批 D 连带】，事件循环不阻塞定时器先行回归）；R30-7 doc.refresh 返回 boolean + HistoryPanel 失败分流 warning toast（Toast 增 warning 级）；R30-8 CI push 腿**登记维持**（作者拍板项，沿 R28-29/R29-11 口径）。
- **P3 修 18**：R30-10 全 reasoning 消息从请求历史剔除；R30-12 注释 8192→16384；R30-14 NaN 过滤；R30-15 注入侧禁词同源取词；R30-16/30 注释改正；R30-17 死导出删除（test/process 引用改走活单源）；R30-18 六处 `export let`→const+注入钩子；R30-19 迁移失败日志如实化；R30-20 snapshotPolicy stat 缓存；R30-21 stream 同步抛错 SSE 错误事件收口；R30-22 迁移清单条目按实际搬移过滤；R30-23 nosniff 三处；R30-24 桌面 handler promise 接管（+菜单同款两处）；R30-26 梗概键收敛 storage-keys 单源；R30-27 workbench 日志 17 型白名单；R30-28 lint 纳管 test/**（存量 46 错清偿：15 机械修 + 31 处 no-explicit-any@test 降 warn 记档）；R30-29 harvest 章级失败告警+计数。
- **登记维持 4**：R30-9（responses 流中事件缺 status，保守终态防重试风暴，锚注在位）/ R30-11（过度脱敏保守方向，锚注在位）/ R30-13（stopReason 归一影响面 >4 文件、消费方无实害，登记锚 anthropic-adapter）/ R30-25（盘点发现非测试消费方——useSse ticket 回退通道，移除条件落注：回退下线且 e2e 无残余后收口）。连同 P2 侧 R30-8，维持合计 5。
- **测试**：+15 测试文件 / +73 用例（598/4623 → **613/4696**），根 README 修账四处。
- **终门八件套全绿**（主评审亲跑）：vitest **613 文件/4696 单测**全过 + tsc/vue-tsc 0 错 + eslint 0 错（31 warn 全为已记档 no-explicit-any@test）+ 三 check 过 + **e2e 43 过 2 跳**（31.9s 零误红）+ coverage 门过。
- **方案偏离记档**：① R30-3 未按「整体 async 化」改写——src/rag/index.ts:38 同步消费 recordTaskUsage（批 A 禁改域），async 化会把锁超时变 unhandled rejection；改混合式（快路同步/争用异步），对外契约逐位不变。② R30-5 连带语义收紧：applyLeadUpdates 批量条目由「逐条取放、单条超时仅该条留源」改为「整批预取、任一超时整批留源」（fail-closed 不降级裸写，注释已记档）。③ R30-22 迁移清单改写新增「旧路径无文件→预改写落 v2」分支（documents-api 依赖的既有契约保全）。④ R30-17 按需清理了两处 test/process 引用（批 B 禁改域外的必要最小改动，仅换活 API 未删用例）。
- **遗留观察项（下轮评估）**：src/ai/rule-hits.ts:68 仍用同步锁等待（批 A 发现，调用方为同步上下文未列入本轮任务）。
- **收口提交**：dev 分支单笔提交（作者指令「修完之后提交改动」授权；二十九轮存量已先行单独提交 `cdb19c0`）。确认闸遵守：全批零新增/移动/重命名 .md（收口记录为本报告 §九 追加）。
