# 全量代码复审与进度质量评估·三十三轮〔dev 树〕（2026-08-31）

> **确认闸头注记**：本报告系作者原始指令「……修复完成之后，再重新评审一次，注意是重新！不要依靠之前的，从 0 开始再重新完成一次评审任务，并且形成文档，然后修复问题」的直接交付物，按命名规范落盘（自主模式，沿二十五/三十一/三十二轮口径）。
> **文件名记档**：正典名 `全量代码复审与进度质量评估三十三轮-评审报告-2026-08-31.md` 已被并行会话的 **win 分支树**三十三轮报告（链第八十七轮，基线 win HEAD `b072ced7`）占用——dev 侧与 win 侧系**同一作者复合指令在两条分支树上的并行执行**。本报告 = dev 树（本链全部历史评审与修复批的执行地，基线含三十二轮修复批未提交改动）的从零重评，文件名加 `dev` 后缀区分，finding 编号用 **R33D-N** 避让 win 报告的 R33-N 命名空间。**链第八十八轮**。

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、基线与方法

- **基线**：dev 工作树实态 = HEAD `bdffa9c`（dev）+ 三十二轮修复批全部改动（未提交）。**评审从零开始**：七个评审子代理全部禁读任何 .md（含三十二轮报告、win 树报告与全部历史评审），不引用任何既有结论，按当前代码实态独立深审。
- **门禁八件套（评审时亲跑全绿）**：`npm test` **620 文件 / 4747 单测** 92.07s + tsc / vue-tsc 0 错 + eslint 0 error（31 warn = R30-28 登记存量）+ check:counts / packaging / knowledge 过 + e2e **43 过 2 跳** 28.0s。
- **方法**：七路分域禁读子代理（A AI 链路 / B 机检格式事件 / C 核心数据 / D 服务端桌面 / E1 前端逻辑 / E2 前端组件 / F 测试工程），分三波派发（波 1 = A/C/D 成活 + B 撞用户并发上限阵亡；波 2 = B/E1/E2 全部成活；波 3 = F 成活）。**P2×10 全部由主评审逐条对照源码亲验成立，零证伪**。

## 二、结论

- **进度**：≈98%（核心 mac 生产形态 ≈99% / win RC 形态 ≈96.5%）——三十二轮修复批收口后，核心链路（三协议口径、锁纪律、切书竞态、保存协议）的主干缺陷面已清空，本轮 P2 全部为窄面残余（见 §三特征）。win RC 口径较 dev 侧历史自评略上调但仍低于 mac（win 系统适配面以 win 树链评审为准，本报告不重复计）。
- **质量**：**8.6 / 10**（分域 A 8.5 / B 8.5 / C 8.5 / D 8.5 / E 8.5〔E1+E2〕/ F 9.0）。较三十二轮 8.7 微降 0.1 系评审粒度深化（P3 检出密度上升）而非质量回退——七域一致结论「问题密度已相当低，残余全部为惯例覆盖完整性与低频窗口」。
- **finding 总账**：P0×0（连续十三轮）/ P1×0（连续十四轮）/ **P2×10 / P3×27（R33D-1~37）**，登记维持 5（R33D-16/22/23/27/32，均为历史轮已登记取舍的再确认），实修 32。
- **三十二轮修复批回归核查**：七域从零深审**未发现回归**；E1/E2 域还实证了 R32-8/9/25~34 的修复实态在位。

## 三、P2（×10，全部亲验成立）

| # | 域 | 位置 | 描述 |
|---|---|---|---|
| R33D-1 | A | src/state/batch-pause.ts:49 + src/ai/orchestrate/self-heal.ts:329/335 | batch-pause 是同步锁口径唯一成规模漏网且被服务进程直调——self-heal 编排内 writeBatchPause/clearBatchPause 经 `acquireCrossProcessLockWithTimeout`（Atomics.wait，2s）微睡，双进程争用时冻结服务事件循环；同族 calls/rule-hits/learn 已全改异步孪生（R30-3/R32-13 口径），唯此站点漏网。修法：withPauseLock 改 `acquireCrossProcessLockAsync`，writeBatchPause/clearBatchPause 异步化（self-heal 调用点本就 fire-and-forget 语义） |
| R33D-2 | A | src/ai/provider/openai-adapter.ts:397-401,418-421 + anthropic-adapter.ts:300,340 | 三线分叉：openai 线 `finish_reason='content_filter'`、anthropic 线 `stop_reason='refusal'` 原样透传 → emitDone 正常完成——被过滤的半截正文按成功落稿（generateText 截断检查与 self-heal 截断告警全不触发，消费侧只判 'max_tokens'，gen.ts:223 / self-heal.ts:888 实证）；responses 线同因却判 error（R1 缺口 2）。修法：两线把非正常终止值归一为 error（usage 随错上抛，对齐 responses 口径）+ 三线对齐回归 |
| R33D-3 | B | src/check/lead-updates.ts:135-144 | readLeadUpdateChapterTag 不剥 BOM：首行 `\uFEFF# 第5章` 对 `/^#\s*第(\d+)章/` 恒 miss → tag=null → mainIsThisChapter 对任意章为 true——带 BOM 的账本推进在定稿他章时被当本章源，applyLeadUpdatesLocked（无证据过滤）把标签章推进写成被定稿章履历 + 清空主文件销毁第 5 章真实待确认内容（同库 splitFrontMatter/Q-14 均已剥 BOM，唯此函数裸 split）。修法：读入后剥 `\uFEFF` 再匹配 + BOM 回归 |
| R33D-4 | C | src/document/lead-finalize.ts:255-262 + finalize.ts:111 | 定稿清空 账本推进.md 以锁外快照为准、锁内不复核：resolveLeadUpdateTargets（含 mainIsThisChapter 判定）在布线锁/清单锁外，锁内清空按 existsSync(mainPath) 命中即 atomicWriteFile 清空——布线锁+清单锁等待窗口（最长 ~10s）内 generateLeadUpdateDraft(N+1) 写入的新草稿（含作者 PUT /file 手改，该文件在编辑白名单）被无痕抹掉，清空路径无快照留底（generate 侧 R74-4 有）。修法：锁内清空前重读章标签复核（仍为本章才清，否则改走归档 rename 保两代），或 finalize 预取集中加入 账本推进.md.lock |
| R33D-5 | C | src/document/journal.ts:197 | journal appendLine 仍是同步锁且挂在服务进程保存链热路径：executeSave/saveDraft 每笔保存 2-3 次调用走 `acquireCrossProcessLockWithTimeout`（Atomics.wait 2s）——同一条保存链的 save/布线/清单锁已全部异步化（R30-6/R31-20/R32-5），唯每笔必经的 journal 追加漏网；双进程同文档并发保存时 GUI 服务进程 SSE/HTTP 冻结至 2s/次。修法：加 appendLineAsync（acquireCrossProcessLockAsync 孪生），executeSave/saveDraft 三处 await，降级语义原样平移；同步孪生保留给 state.ts healMovePending/compact 真同步语境 |
| R33D-6 | D | src/studio/server/api/stream.ts:272-279 vs 302 | SSE 凭据「预检/消费」两段口径分叉：预检三凭据（ticket/?token=/x-studio-token 头）任一通过，消费点只认 ?token= 与 ticket——仅带头的请求通过预检 + 429/404 书域校验后在建流前必 403（R31-32「fetch 型客户端走头通道」契约只在预检半边落地，现网唯一头消费方是前端 429 探测对 403 静默，故无用户可见故障但属零覆盖死路）。修法：消费条件补认 headerToken + header-only 建流成功回归 |
| R33D-7 | D | src/studio/server/api/books.ts:247-297,441-509 | 删书/改名 recheck 不覆盖 chat/self-heal：busyGate（:103-113）只含 spawn/review/任务闸；入口 abort 只杀已在跑的——settle 循环退出后的 drain 段 await 窗口内新起的对话/自愈既不在 recheck 之列也无 abort，贯穿 rmSync/renameSync 继续跑分钟级（收尾重建孤儿目录 + 白烧 API 费，R32-6/M-4 声称归零的同类危害在 drain 段残余）。修法：recheck 并入 isChatRunning/isSelfHealRunning（命中即 abort 后重走 settle 或保守 409）+ 修正 M-4 注释与实现一致 |
| R33D-8 | E1 | src/studio/web-next/src/stores/chat.ts:169,388,564-570 | chat pendingReseed 补种被自身空列表守卫废掉：seedHistory 守卫 `messages.length > 0 → return`（:388），而 pendingReseed 登记的前提恰是「历史已种子化（非空）+ sync 无可续气泡」——自愈机制在其目标场景恒 no-op，在途回合的回复界面缺失直到手动刷新（事件库无损）。修法：为 pendingReseed 路径提供替换式补种入口（gen 守卫下先清 messages 再种子化，对齐 switchBranch 写法） |
| R33D-9 | E2 | src/studio/web-next/src/pages/Book.vue:99,147 | 切书 Z-8/F1 守卫在「Book 组件重挂」路径失守：守卫代次源是组件实例本地的 lastBook——/book/A（有 conflict+dirty）→ /shelf（Book 卸载，doc store 单例仍持 A 脏缓存）→ 书架开书 B：新实例首跑 watch lastBook===''，Z-8/F1 双双跳过，setBook('B') 清缓存，A 的未落盘编辑静默丢失（仅 .版本 快照兜底）。修法：守卫改用仍存活的权威源 doc.bookName（prevBook && prevBook !== n 时照常跑守卫），或最低成本 setBook 清缓存前留痕 |
| R33D-10 | E2 | src/studio/web-next/src/components/style/StyleCandidateBox.vue:162 | 「已忽略」区「仍要收录」按钮未对齐 R32-30 全局单飞禁用口径：仍用单卡旧形态 `acting === c._path`——任一候选在途时可点但入口 `if (acting) return` 静默吞掉（R32-30 注释命名的「死按钮残余点」类别漏改一处兄弟按钮）。修法：`:disabled="acting !== null"` |

## 四、P3（×27）

| # | 域 | 位置 | 描述 |
|---|---|---|---|
| R33D-11 | A | src/ai/gen.ts:89-125 | withFirstByteTimeout 消费侧早退不关源迭代器（responses 线 error 事件后适配器生成器停在 yield 上，SDK SSE 连接悬挂至服务端 FIN；有界、无重试放大）。修法：wrapper 整体 try/finally 补 it.return() |
| R33D-12 | A | src/ai/provider/responses-adapter.ts:331,341 | output_item.done 双缺 id 时产出空串 tool id（回灌历史可致下轮 400；R65-10 只兜 delta 面）。修法：空 id 兜底 `call_${序号}`（对齐另两线）【待验证：现实概率】 |
| R33D-13 | A | src/ai/orchestrate/chat/turns.ts:121,608-618 | TOOL_RISK/TOOL_EXECUTORS 普通对象按模型 name 直索引——'toString' 类原型链键绕过未知工具拒收与 write 确认闸（无代码执行风险，防线语义穿透）。修法：Object.create(null) 或 hasOwn 守卫 |
| R33D-14 | B | src/check/run.ts:254-257 + outline-leads.ts:65 | 声明侧读失败静默跳两端闭合（known:false），缺兑现侧同款 fail-noisy 黄项——降级可见性两侧不对称（R31-3 只闭合兑现侧）。修法：三态加 reason，read-failed 时产黄项 |
| R33D-15 | B | src/format/global-defaults.ts:89 | readGlobalBookDefaults 指纹缓存仍 mtimeMs 粒度（同族已升 mtimeNs，唯此漏网；同毫秒等长重写 → short.strict 闸门口径漂移）。修法：升 bigint mtimeNs |
| R33D-16 | B | src/events/store.ts:164,418 | events 首开/迁移段同步 Atomics.wait 锁（仅首开段、非热路径，争用窗最长 5-10s）——**登记维持**（events 层同步 node:sqlite 语境 + 生产单进程，异步化需 worker 化根治，登记尺度如 D 域裁定） |
| R33D-17 | B | src/check/run.ts:576-583 | 纪元失效后章缓存逐行独立提交（数百章书一轮 = 数百次 WAL commit，纯性能）。修法：终核通过后单事务包批 |
| R33D-18 | C | src/document/service.ts:415,431,454,578 + draft-pipeline.ts:70,167,174 | executeSave/saveDraft 单次保存对正文 4 次整读（含读间微 TOCTOU；autosave 高频热路径纯浪费）。修法：锁内单读 Buffer 四路同源派生（R27-45/R72-5 先例） |
| R33D-19 | C | src/process/summary.ts:354,431 + state.ts:459 | manifest 路径 3 处未过 safeManifestPath 直拼 join（防篡改纪律不一致，只读逃逸面；可触发性依赖清单被篡改）。修法：三处改 safeManifestPath 非法跳过 |
| R33D-20 | C | src/process/draft-pipeline.ts:125-177 | saveDraft 缺锁内复核（对照 executeSave R76-22 纪律）：等锁窗口内并发改名可复活旧路径副本（丢数据面否，executeSave 已收口此处未对齐）。修法：锁内复核 manifest 登记路径仍等于 relPath，不等上抛 |
| R33D-21 | C | src/document/analysis.ts:132-143 + service.ts:1510 + trash.ts:283,313,391 | 服务进程低频同步锁清单（analysis 5s / doTrash / restore / purge 各至多 2×5s）——与 R33D-1 同类；analysis 面沿 R32-18 登记维持，trash 族切 withManifestLockAsync 实修（本就可 async 化） |
| R33D-22 | D | src/studio/server/api/stream.ts:439-449,557-562,677-682,764-770 | 生成端点族任务闸只查进程内 Set，跨进程持闸不可见（busyGate/audit 已按 R75-5 合并，生成族未合——登记的口径半程态，双进程 dev/CLI 形态复刻 R67-13 混态面）——**登记维持**（生产单 server 进程不触发；收口方向：收敛 allHeldTaskGatesFor 单一 helper） |
| R33D-23 | D | src/studio/server/api/books.ts:304,538 + install/books.ts:176 | books 删/改名端点经 tryBooksLock 同步锁 5s（R32-18 显式登记维持的窄面）——**登记维持**（调用面 2 处可渐进切异步孪生，随下次批顺手收口亦可） |
| R33D-24 | E1 | src/studio/web-next/src/stores/prefs.ts:296-321 | prefs 单飞在 !revisionKnown 分支被 await 撕裂（判空在 GET await 之前 → 双重 PUT → 伪 409 告警；R32-27 自建不变式的组合盲区，需 init 离线 + 双写 + 慢 GET 三条件）。修法：占位提前到 GET 前 |
| R33D-25 | E1 | src/studio/web-next/src/stores/workspace.ts:78-95,104-107 | 切书未清 prefs 书级覆盖，loadBookPrefs 失败时 A 书 pageWidth/autosaveInterval 泄入 B 书（纯展示/生效面，无数据污染）。修法：setBook 失败分支显式复位两键 |
| R33D-26 | E1 | src/studio/web-next/src/stores/doc.ts:82-89,111-132 | inflightOpens 不随切书清理 + 代守卫吞结果 → 跨书同 legacy docId 的 open 静默空手而归（窗口窄可自愈）。修法：setBook 中 inflightOpens.clear() |
| R33D-27 | E1 | src/studio/web-next/src/composables/useSse.ts:121-123 | SSE ?token= 回退通道把长效凭据放 URL（代码内已自知的过渡债，R30-25/R32-23 登记链）——**登记维持**（收口依赖服务端下线兼容期；建议补回退留痕） |
| R33D-28 | E2 | src/studio/web-next/src/components/shell/ChatDock.vue:113-120 + ChatPanel.vue:105-110 | 对话发送按钮未随 busy/sending 禁用（工作台生成期间可点但入口静默 return；E1a steer 的 chat.running 不禁是正确口径，busy 面漏禁）。修法：`:disabled="!input.trim() \|\| busy \|\| sending"` |
| R33D-29 | E2 | TrashPanel.vue:112 + ForeshadowPanel.vue:192 | 两处在途锁有、按钮 disabled 无（恢复/新建伏笔在途期点击静默忽略，与 HistoryPanel/R73-62 惯例不一致）。修法：补 :disabled + 在途文案 |
| R33D-30 | E2 | SampleCandidateList.vue:104 + stores/learn.ts:60-81 | 样章候选以正文全文作 v-for key 与勾选身份（金句侧 R32-31 已修，样章侧同款残留；整章级长文重复概率低【待验证】）。修法：比照金句 sampleKey = 出处+正文 |
| R33D-31 | E2 | SettingsBook.vue:74-77 + Ribbon.vue:36-38 + ShelfGrid.vue:37 + Library.vue:61 | 桌面 IPC 交互路径 fire-and-forget 失败零反馈（加载路径已修、交互路径残留）。修法：四处统一 try/catch + toast |
| R33D-32 | F | .github/workflows/ci.yml:9-11 | main 直推零门禁（push 腿摘除系 2026-08-29 R74-5 登记的频推期临时收窄，PR 合并路径已覆盖；若 main 分支保护强制 PR 则残余风险仅 hotfix/revert 面）——**登记维持**（沿 R30-8/R29-11/R32-10 作者拍板链；恢复 push 腿为既定方向，待作者拍板） |
| R33D-33 | F | scripts/check-counts.mjs:142-147 | e2e 用例静态计数不计 test.each 参数化组（当前 29 spec 零 .each 无实伤；漂移方向为计数失真）。修法：正则补 .each 分支或头注登记 |
| R33D-34 | F | test/check/corpus.test.ts:29-31,92-94 | 语料回归门可整组静默解除武装（目录缺失/清空 → describe.skipIf 整组 skip、CI 照绿；当前门是活的）。修法：CORPUS_DIR 不存在文件级哨兵红（fail-closed） |
| R33D-35 | F | test/e2e/e2e-ports.ts:21-27 | 端口基址校验未含偏移余量（放行 65520-65535，e2ePort(16) 越界违背自述意图）。修法：上界改 65536-16（MAX_OFFSET 同源） |
| R33D-36 | F | vitest.config.ts:85-118 + eslint.config.js | 覆盖桶外暗区残留 1 文件（web-next/vite.config.ts 进报告无阈值桶）+ 根目录 config（vitest/playwright/tsup）不在 lint 射程。修法：exclude 补一行 + lint files 追加或显式登记 |
| R33D-37 | F | scripts/check-counts.mjs:178-182,367-376 | 空洞测试门（R76-40）不覆盖 e2e spec（实测 29 spec 全含 expect 无实伤）。修法：findAssertionFreeTestFiles 扩跑 e2eSpecs |

## 五、修复计划（批 A-F；登记维持 5：R33D-16/22/23/27/32）

- **批 A（AI 链路）**：R33D-1 batch-pause 锁异步化 + writeBatchPause/clearBatchPause 异步化（self-heal 调用点收口）；R33D-2 openai content_filter / anthropic refusal 归一 error（usage 随错上抛）+ 三线对齐回归；R33D-11 wrapper finally 关源；R33D-12 空 tool id 兜底；R33D-13 工具表 hasOwn/null-prototype 守卫。
- **批 B（机检/格式/事件）**：R33D-3 readLeadUpdateChapterTag 剥 BOM + 回归；R33D-14 声明侧 read-failed 黄项（三态加 reason）；R33D-15 global-defaults 升 mtimeNs；R33D-17 章缓存批事务提交。（R33D-16 登记）
- **批 C（核心数据）**：R33D-4 lead-finalize 清空锁内复核（重读章标签）或预取集中加入账本推进锁 + 回归；R33D-5 journal appendLineAsync + 三调用点 await；R33D-18 单次保存单读派生；R33D-19 三处 safeManifestPath；R33D-20 saveDraft 锁内复核；R33D-21 trash 族 withManifestLockAsync（analysis 面沿 R32-18 登记）。
- **批 D（服务端）**：R33D-6 SSE 消费点补认 header + header-only 回归；R33D-7 recheck 并入 chat/self-heal + M-4 注释修正 + 回归。（R33D-22/23 登记）
- **批 E（前端）**：R33D-8 chat 替换式补种入口；R33D-9 Book 守卫改 doc.bookName 权威源；R33D-10 忽略区按钮对齐；R33D-24 prefs 占位提前；R33D-25 切书复位书级覆盖两键；R33D-26 inflightOpens.clear()；R33D-28/29 按钮 disabled 对齐；R33D-30 sampleKey 身份统一；R33D-31 IPC 四处 toast。（R33D-27 登记）
- **批 F（工程）**：R33D-33 each 计数补正则；R33D-34 语料门哨兵；R33D-35 端口上界余量；R33D-36 覆盖桶外 + lint 射程收口；R33D-37 空洞门扩 e2e。（R33D-32 登记待作者拍板）
- 修后终门八件套全绿为收口门；新增/改动测试数记 §七。

## 六、索引同步记

本报告 + `Dev/Main/README.md`（变更记录本条 + 目录地图 01-评审 行）+ `00-总览` 1.1（三十三轮〔dev 树〕行）+ `01-评审/README`（维护日期 + 暂存节）。评审轮源码零改动，本节 4 处 .md 为唯一写入面。win 树链第八十七轮报告已由并行会话自行落盘并索引（本报告不覆写其条目，只作增量补记）。

## 七、收口记

**已收口（2026-08-31，主评审亲修——沿三十二轮单线六批口径，不再派并行代理）**：37 项 = **修 32（P2×10 全修 + P3×22 实修）+ 登记维持 5**（R33D-16 events 首开同步锁 / R33D-22 生成族跨进程闸 / R33D-23 books 端点同步锁 / R33D-27 ?token= 通道 / R33D-32 CI push 腿，均沿既有登记链再确认；R33D-21 的 analysis 面沿 R32-18 登记，trash 族实修）。分批办结：

- **批 A（AI 链路）**：R33D-1 batch-pause 锁异步化（withPauseLock → acquireCrossProcessLockAsync；writeBatchPause/clearBatchPause 异步化，self-heal recordPause 保持 fire-and-forget void+catch、开批清暂停 await，4 测试文件 await 收口）；R33D-2 openai content_filter / anthropic refusal 双线双路径（有/无 usage）归一 error PROTOCOL（usage 随错上抛，responses 线本已判错——三线口径闭合）；R33D-11 withFirstByteTimeout 整体 try/finally 关源（消费方 throw / 正常收束 / 超时三退出路径全覆盖，Q2/M-3 语义保留）；R33D-12 responses done 项空 id 兜底 call_N；R33D-13 TOOL_RISK/TOOL_EXECUTORS hasOwn 守卫。回归 = 新增 `test/ai/provider/r33d-batch-a.test.ts`（8 用例）。
- **批 B（机检/格式/事件）**：R33D-3 readLeadUpdateChapterTag 剥 BOM（回归 2 例）；R33D-14 声明侧三态扩 OutlineDeclaration.reason（read-failed → 黄项 lead-outline-unreadable，chapter-mismatch 维持静默；memo 版同步），R69 三态测试口径更新；R33D-15 global-defaults 缓存升 mtimeNs bigint（R73-27 同口径，值空间隔离天然失效旧代）；R33D-17 章缓存批量落盘 writeTreeIssuesCacheBatch（单 BEGIN…COMMIT 包批，批失败回退逐行 best-effort）。回归 = 新增 `test/check/r33d-batch-b.test.ts`（7 用例）。
- **批 C（核心数据）**：R33D-4 lead-finalize 清空路径锁内重读章标签复核（liveTag ≠ 本章 → 跳过清空保护新草稿 + warn，本章归档照常清理）；R33D-5 journal appendLineAsync（appendPending/appendMovePending/appendSettled/appendAborted 异步化，保存链 10 处调用 await 收口；state.ts healMovePending 真同步语境新增 appendSettledSync/appendAbortedSync 孪生——报告已预告；降级精简行语义原样平移）；R33D-18 executeSave/saveDraft 盘上内容单读派生（hashBytes/UTF-8 判定/字数 delta/快照留底四路同源 Buffer，maybeSnapshot 增 diskContent 透传）；R33D-19 summary×2 + tree probeFile 过 safeManifestPath（非法按不存在语义 null）；R33D-20 saveDraft 锁内复核 manifest 登记路径（R76-22 纪律对齐）；R33D-21 restoreTrash/purgeTrash 异步化（withManifestLockAsync ×3，端点 await 收口；appendTrashEntry 保留同步版——同步语境 + 测试面）。
- **批 D（服务端）**：R33D-6 SSE 消费点补认 x-studio-token 头（票抢消费语义不变，header-only 建流 200 + 无凭据 403 回归 2 例）；R33D-7 删书/改名 recheck 补 chat/self-heal（取保守 409——作者正主动用书删除可重试，报告预告的 abort 重走 settle 未取；M-4 注释如实化），R32-6 测试 mock 同步 abort 生效语义。
- **批 E（前端）**：R33D-8 chat seedHistory 增 replace:true 替换式补种（先清后种对齐 switchBranch；非 replace 路径恢复 fetch 后非空守卫防 Y-P2-5 回归），pendingReseed watch 换 replace 口径；R33D-9 Book 切书守卫代次源补权威回退 prevBook = lastBook || doc.bookName（Z-8/F1 重挂路径照常跑，取消分支回退/清污/resync/补种全走 prevBook，R26-18 不变式维持——报告「最低成本留痕版」未取，取完整守卫版）；R33D-10 候选箱忽略区按钮对齐单飞；R33D-24 prefs 单飞占位提前（putInFlight = Promise.resolve() 先占位，PUT 段抽 doPersistPut，409 处理不变）；R33D-25 setBook 清书级 prefs 覆盖两键；R33D-26 doc.setBook 清 inflightOpens；R33D-28 ChatDock/ChatPanel 发送按钮 busy||sending 禁用；R33D-29 TrashPanel/ForeshadowPanel 按钮补 disabled；R33D-30 样章身份统一 出处+正文（sampleKey，learn store + SampleCandidateList + 3 测试文件 API 换整对象签名；toggleSampleByBody 兼容面零调用死代码不保留）；R33D-31 IPC 四处失败 toast（SettingsBook/Ribbon/ShelfGrid/Library）。
- **批 F（工程）**：R33D-33 e2e 计数正则补 test.each 形态；R33D-34 语料门目录哨兵（目录不存在 fail-closed 红，单文件坏 JSON 容错保留）；R33D-35 e2e 端口上界留偏移余量（65536-16，MAX_PORT_OFFSET 同源常量）；R33D-36 coverage exclude 补 web-next/vite.config.ts（桶外暗区清零）+ eslint 收编根目录 *.config.ts；R33D-37 空洞测试门扩跑 e2e spec。
- **测试账（check:counts 实测口径）**：620/4747 → **622 文件 / 4763 单测**（+2 文件：r33d-batch-a/b；+16 用例 = 批 A 8 + 批 B 7 + R33D-6 1），根 README 四处数字修账。
- **终门八件套**：`npm test` 622/4763 全绿 91.14s + tsc / vue-tsc 0 错 + eslint 0 error（31 warn = R30-28 登记存量）+ check:counts / packaging / knowledge 过 + e2e **43 过 2 跳** 28.4s。收口门达成。
- 工作树未提交（沿本链惯例待作者指令）；本轮零新增/移动/重命名 .md 之外仅报告 1 篇（原指令直接交付物头注记口径，正典名被 win 树并行报告占用故加 dev 后缀）。
- **并行链记档**：win 树三十三轮（链第八十七轮）报告已由并行会话落盘并自行索引，其修复批不在本链范围；两条链后续合流时其 P1×2（CRLF 围栏 / win 三审冒号路径）与 P2×14 需独立收口。
