# 全量代码复审与进度质量评估二十六轮（评审报告）

- 日期：2026-08-30（链第七十八轮）
- 基线：HEAD `e86f4b9`〔dev〕+ 工作树 12 文件未提交（二十五轮 R77 修复批在树：font-cache / atomic rename 重试 / git ENOENT 特判 / task-gate KNOWN_ACTIONS 导出 / electron-builder / 根 README 等）——工作树现状整体纳入评审面。
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，告诉我项目完成进度，完成质量，最后形成一个文档给我。记得编排下任务。然后按照文档的修复计划，来修复找到的问题，全部修复。修复完成之后，再重新评审一次，注意是重新！不要依靠之前的，从0开始再重新完成一次评审任务，并且形成文档，然后修复问题。」
- **确认闸记**：报告路径 `Dev/Main/01-评审/全量代码复审与进度质量评估二十六轮-评审报告-2026-08-30.md`（修复后复审为二十七轮同名格式）经 AskUserQuestion 报作者，未获直接答复——系原指令直接交付物，按命名规范落盘（二十二轮/二十五轮头注先例），特此记档。
- **本轮不含修复**；修复按 §八 批 A-H 于报告落盘后执行（作者指令已预授权「全部修复」）。修复后从零复审（二十七轮，禁读本报告与历史评审）另文交付。

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审方法与覆盖口径

- **门禁八件套主评审亲跑**（§二）。
- **九域分域从零深审**：九路子代理（每波 ≤3 错峰派出）按文件清单逐个全文阅读，禁读 `Dev/` 全链与任何历史评审报告（防锚定）。分域：A=src/ai（75 文件 11735 行）/ B=src/check+format+review（40 文件约 9000 行）/ C=document+export+state+cache+fs+git+knowledge+learn（39 文件约 9600 行）/ D=studio/server（46 文件 9399 行）/ E1=web-next components（90 文件 19790 行）/ E2=web-next 其余（api/stores/composables/views/pages/shared/editor 等 86 文件约 12000 行）/ F=desktop+install+driver+rag+log+metrics+scripts（45 文件约 8700 行）/ G=process+events（26 文件约 4470 行）/ H=测试工程（584 文件 94755 行，盘点+36 文件深读抽查）。
- **覆盖口径**：src 生产代码约 10 万行全域逐文件阅读（九路代理自证 + 主评审对 P2 全量引文亲验）；测试工程按盘点+抽查口径（H 域明示）；e2e 走 H 域抽查。**口径边界**：知识层内容（`知识层/`）、`src/studio/web-next/node_modules`、`dist*` 构建产物不在评审面。
- **亲验纪律**：P2×20 全部经主评审对照源码逐条亲验属实（引文逐条核对），零虚报；P3 在修复批内逐条核销时亲验（误报即裁定不修并记档）。

## 二、门禁实证（2026-08-30 02:07–02:15 亲跑）

| 门 | 结果 |
|---|---|
| `tsc --noEmit` | 0 错 |
| `typecheck:web-next`（vue-tsc） | 0 错 |
| `eslint .` | 0 错 |
| `vitest run` | **544 文件 / 4306 用例全绿**（93.7s，零红零跳） |
| `check:counts` | 过（README 声称值与实测一致：544/4306，e2e 29 spec/45 用例） |
| `check:packaging` | 过（resources 入打包清单，prompt 版本表对账一致） |
| `check:knowledge` | 过（知识层 13 条 manifest 与磁盘一致） |
| e2e / build | 本轮未亲跑（二十五轮收口记 43 过 2 跳；修复批终门补跑） |

与根 README 声称值零漂移。

## 三、进度评估

**结论：整体 ≈97%（核心 mac 生产形态 ≈98%）。** 较二十五轮的 ≈96% 小幅上调，依据为阶段行代码实证而非口径放松：

- 阶段 0-12、14-20、22、23 全部已完成已收口（总览第三节逐行核对）。
- 阶段 21（win 适配）：J0/J1/J2/J3/J4/J7 已完成；J5 体验面大件在位（菜单隐藏 `bb9c344`、外观对齐 mac `300bc87`、wco 避让修正 `2d9f402`、字体 IPC 缓存/git 引导/长路径冒烟/rename 重试均在树 R77 批）；J6 发布面已落地披露件（根 README「Windows 版使用须知」+electron-builder 落账，签名按 D4 拍板首版 unsigned）。**余**：J6b 跨平台书库迁移（需 mac 侧，挂账）、SmartScreen 实机观察。
- 阶段 13（Responses 启用批）：代码+测试双绿 10/11，**余中转网关真机验证**（`scripts/verify-responses-relay.ts` 在位，待作者网关凭据——外部依赖，非代码欠账）。encrypted 中段直测 R77-5 已补 7 用例（工作树在树），该挂账可销，待回归确认。
- 产品功能面：README 宣称的建书/设定/写章/机检/三审/定稿/防吃书/伏笔/节奏/文风/改写分析/对话助手/导出全链路均有实装与测试锚定，未发现宣称-实现背离的新增面。

**真实余量**：①阶段 13 真机验证（外部）；②J6b 迁移（外部环境）；③本轮评审新立 P2×20/P3×91 的修复批；④`TOKEN_COEFFICIENTS` 校准表空转（G 域 P3，估算精度维持全局 0.6 系数）。

## 四、分域质量评分

| 域 | 范围 | 评分 | 完成度 | P1 | P2 | P3 |
|---|---|---|---|---|---|---|
| A | src/ai | 8.5 | ~90% | 0 | 5 | 8 |
| B | check/format/review | 8.0 | 完成态 | 0 | 4 | 19 |
| C | document/fs 等核心数据 | 8.5 | 完成态 | 0 | 2 | 9 |
| D | studio/server | 8.5 | 完成态 | 0 | 2 | 11 |
| E1 | web-next components | 8.5 | 完成态 | 0 | 1 | 8 |
| E2 | web-next 数据层 | 8.5 | 完成态 | 0 | 2 | 10 |
| F | desktop/install/rag 等 | 8.5 | 完成态 | 0 | 2 | 10 |
| G | process/events | 8.0 | 完成态 | 0 | 2 | 11 |
| H | 测试工程 | 8.5 | — | 0 | 0 | 5 |
| **合计** | | **8.4/10** | | **0** | **20** | **91** |

**总体质量判语**：连续第八轮 P1×0。这是典型的多轮评审沉淀型代码库——300+ 条带轮次编号的修复注释可回溯、并发/崩溃/安全纪律系统化成文（跨进程锁族、原子写三段、TOCTOU 收口、脱敏单出口、六类前端守卫体系）、测试工程达到「测试即决策记录」水准（负向破坏用例+前提自查+防循环论证锚点、覆盖阈值门+glob 守护元测试）。本轮 P2 的共性不再是主路径缺陷，而是**交叉条件组合**（非标准网关形态 × 记账终兜底、外接盘文件系统 × 硬链原语、定时失败 × 轮询终态、双进程 × 旁路写点）与**跨文件口径一致性**（布尔键/短篇判定/命中面三套处置并存）——恰是主路径打磨到极致后最后一圈边角。

## 五、发现清单

编号 R26-1 起。P2 逐条附引文位置与修法；P3 一行一条。凡「存疑」者系触发条件依赖外部形态，仍按可防御方向修复。

### P2（20 条，全部主评审亲验属实）

- **R26-1（A）**`src/ai/gen.ts:271` generateTool 漏检「有 tool_use 但参数被截断」：仅 `!tool && stopReason==='max_tokens'` 抛错；截断发生在 tool arguments 中途时 `toolCalls[0]` 存在，适配器把解析失败兜成 `{_raw}`（openai-adapter.ts:372-376），7 条工具型链路（self-heal/review/analysis/rewrite/relation-mine）拿到残缺 input 按成功入账，MAX_TOKENS 出路文案全丢。修：判据扩为「max_tokens 且（无 tool 或 input 含 `_raw`）」抛 GenError(MAX_TOKENS)。
- **R26-2（A）**`src/ai/provider/anthropic-adapter.ts:245,291` usage.input_tokens 双点均无 `?? 0` 终兜底（cache/output 侧都有）：非标网关缺 input_tokens 时 TokenUsage.inputTokens=undefined→calls.ts `+=` 得 NaN→`checkAiCallBudget` 对 NaN 恒 false，该进程内 token 预算闸静默失效。修：`?? inputTokensFromStart ?? 0`，或 emitDone 出口 Number.isFinite 归一。
- **R26-3（A，存疑）**`src/ai/provider/openai-adapter.ts:316,397` usage「首见即定」：emitDone 幂等门使逐 chunk 回 usage 的网关记到早期部分值，末 chunk 完整 usage 被丢弃；与 Anthropic 线「持续取最新」语义不一致。修：缓存最近 usage，done 延迟到流末统一 emit。
- **R26-4（A，存疑）**`src/ai/provider/responses-adapter.ts:358-362` completed 空产出判据只看 `r.output` 不看本流已流出文本：网关省略 output 数组但 delta 已出正文时误判空产出且 `retryable:false` 不重试，token 白烧。修：`hasOutput = toolYielded || outText.length>0 || r.output?.some(...)`。
- **R26-5（A）**`src/ai/orchestrate/self-heal.ts:529,619` 首稿/重写 `ctx.save` 抛错（磁盘满/EACCES）未收编：异常直穿 runChapter→orchestrateBatch，goal 无 block 收口、批量不落暂停记录——R76-11 修机检同型问题时漏掉的同族路径。修：两处 save 包 try，失败走 exitEscalateBlocked/exitCheckCrash 同款收口。
- **R26-6（C）**`src/document/lead-finalize.ts:44-45,91,146` 定稿账本回写对布线文件读全目录→内存改→整文件写回，**无任何跨进程锁**；作者经 executeSave 保存同一布线文件只持 per-docId 锁，毫秒窗内 writeLead 以旧内容+新履历覆盖，作者编辑静默丢失（履历不可重生成，是防吃书闸账实依据）。修：回写链对每个 leadFile 套跨进程短锁（复用 J7 锁基建），拿不到按 LEAD_WRITE_ERROR fail-closed。
- **R26-7（C）**`src/fs/atomic.ts:167` linkSync 独占创建原语仅特判 EEXIST：exFAT/FAT32/部分 SMB（外接盘常态）不支持硬链，新建/复制/移动落位/回收站还原/伏笔迁移全线报裸 EPERM。修：EPERM/ENOSYS 回退 `existsSync→renameSync` 降级语义 + log.warn + 人话诊断。
- **R26-8（D）**`src/studio/server/api/stream.ts:209` SSE 通道 `message: out.error` 直透 provider 错误原文，绕过 redactSecret（同文件 :222/:359 均已脱敏）：可能携带 key/Authorization 痕迹下发前端并入库。修：一行 `redactSecret(out.error)` + 治理测试。
- **R26-9（D）**`src/studio/server/api/files.ts:110-135` PUT /file 覆盖写无任何留底且 expectedRevision 可选（后写为准）：作者经编辑器保存的 设定/大纲/布线/文风/工作区确认文件被覆盖后不可恢复；同批 AI 侧覆盖写（onboard-ai/outline/draft-save）均已补留底，唯此漏网。修：写前 `snapshotBeforeOverwrite`（fail-open，对齐 R74-4 口径）。
- **R26-10（B）**`src/format/yaml.ts:535` 短篇 budget 段外层条件只查 `calls_per_chapter`：短篇+未设 calls 时整段跳过，`tokens_per_chapter/cost_per_chapter` 写盘静默丢失（解析侧三处按长短共用设计）；config.ts:82 解析失败回落全量重生成路径会真实丢键→预算上限消失（漏拦方向）。修：外层条件扩三键任一；补 round-trip 测试。
- **R26-11（B）**`src/check/count.ts:379` 对话标签占比分子虚高（V-P1-7 修复不彻底）：tagRe 对剥引号文本无结构锚定，「知道/味道/道理」等普通词引号外高频命中；实测「“走吧。”他知道已经拦不住了。」单行 ratio=1，作者配 maxDialogueTagRatio 后对白章恒黄。修：加提示语边界正则（动词前后锚定标点/行尾）+ golden 语料补基线。
- **R26-12（B）**`src/format/yaml.ts:277,304,327,338,421` 五处布尔键只认字面 `true/false`：`strict: yes`（YAML 1.1 合法写法）/`enabled: 1`/`auto: True` 一律静默按 false 处理无 warn——短篇 strict 是黄升红生产硬闸，静默失效即闸门失明；对比 R76-15 数字键已有坏值 warn 口径。修：统一 parseStrictBool（认 yes/no/1/0/on/off 大小写）+坏值 warn。
- **R26-13（B）**`src/check/runner.ts:99,168-212,249` vs `src/format/kind.ts:13` 短篇判定源分裂：路由按 `kind==='short'`，机检按 `config.short` 对象存在性；`kind: short` 无 `short` 段的书（手写 book.yaml/外部导入可达）字数区间/五段结构/strict 黄升红全部静默不跑，global-defaults 托底同样不生效。修：runner 统一 `config.kind==='short' ? config.short ?? {} : undefined`；补组合测试。
- **R26-14（E1）**`src/studio/web-next/src/components/ui/SettingsBookAnalysis.vue:223-250` RAG 索引轮询无失败终态：getRagStatus 持续失败时 catch 吞错、ragBuilding 恒 true，1.5s interval 无限轮询、界面永挂「构建中…」（对比同库 ChatPanel R65-50 口径）。修：连续失败 ≥5 次终态化「索引状态获取失败」+复位 building。
- **R26-15（F）**`src/rag/index.ts:471,490-492,404-406` 零块章「指纹刷新但旧向量不删」：删旧块集合取自 allChunks（有块章），指纹写入取自 chapterHashes（全部成功读取章）——曾索引的章正文改到零块后旧向量残留且指纹恒匹配，陈旧向量以新鲜身份通过召回闸，offset 指向已不存在的正文，永久自愈不了；续传路径同病。修：删旧块集合改 chapterHashes 全部章号（全新章删除是空操作）。
- **R26-16（F）**`src/rag/index.ts:222-229,452-462,570` 「请重建索引」是死路：全 src 无任何删除/重置 rag.db 的端点/脚本/启动逻辑（grep 证实），换 embedding 模型后 build 永远失败于一条无法执行的指引、recall 静默空。修：补 rebuild 原语（事务内清 chunks+rag_meta）+失败信封带可执行指引+recall 失配在 status 端点可见。
- **R26-17（E2）**`src/studio/web-next/src/views/WorkbenchView.vue:115-131,282-291` + `EditorView.vue:177-180` AI 生成/存草稿落盘后 openTab 命中 doc 缓存（`if (doc.get(id)) return`）直接渲染旧正文：作者在陈旧文本上继续编辑保存会撞 REVISION_CONFLICT；对照 MetaFormPanel/HistoryPanel/EditorDocHead 均已补 `doc.refresh`，唯工作台两条 AI 落盘路径漏配。修：openTab 前 clean 缓存后台 refresh（dirty 不刷，对齐 CC-P2-15）。
- **R26-18（E2）**`src/studio/web-next/src/pages/Book.vue:58-124` 切书 watch 无同书短路：守卫弹窗选「留在本书」→router.replace 回原书→watch 重入全量执行 `workbench.clear()`——作者明确选择留下，工作台未存生成稿（textOut 仅存内存）被清空丢失。修：watch 体内 `if (n === lastBook) return`（保留首载路径）。
- **R26-19（G）**`src/process/summary.ts:170,182-184` 摘要生成链是唯一无跨进程互斥的 AI 写路径：inFlight 是进程内 Set，GUI+CLI 双开同书时各自过 stale 检查重复调 AI 重复计费（对比 saveDraft/账本推进/batch-pause 均已补 J7 锁）。修：状态判定+AI 调用+落盘外套 `工作区/.摘要锁`，拿不到返回 skipped。
- **R26-20（G）**`src/events/types.ts:103` vs `store.ts:50-53` sourceSeqs 同名双语义（全局 seq vs 批内 0-based 索引）仅靠注释维持：新调用方按 types.ts 自然语义传全局 seq 进 appendEventsResolveLineage 会越界抛错回滚整批——M-1 失败出口遮蔽整回合消息。修：类型级拆分 `sourceIdxs`（批内）→落库改写 sourceSeqs（全局），或解析器对大值显式报错提示双语义。

### P3（91 条，一行一条）

**A 域（8）**
- R26-21 `runner.ts:154-160` 降级记忆按活跃 userDataPath 路由，多库并发 A 库 provider 键写进 B 库（注释已认取舍，维持登记）。
- R26-22 `provider/registry.ts:141` bound conf 浅拷贝共享 models 数组引用，原地 mutate 时缓存行为漂移。
- R26-23 `provider/probe.ts:65` 探测实例入 LRU（容量 8）连续探测挤出真实 provider 缓存（性能）。
- R26-24 `provider/anthropic-adapter.ts:261` 不处理 thinking_delta：思维链增量静默丢弃（存疑，现役表不发 thinking；补估计口径）。
- R26-25 `provider/openai-adapter.ts:404-414` 传输截断兜底先 flush 残留 tool 事件再发 error，事件序列矛盾；截断 tool 参数抬高 output 估计。
- R26-26 `trace.ts:34` promptMeta.chars 用 UTF-16 length，estimateTokens 用码位，代理对因子致校准轻微失准。
- R26-27 `mock-tool.ts:47-54` mock 表按工具名人工维护，新增工具忘配穿透真实计费（补启动断言）。
- R26-28 `orchestrate/chat.ts:132` 队列满提示对短预览恒加省略号（文案）。

**B 域（19）**
- R26-29 `count.ts:308/653/699` 「每词允许 N 次」阈值边界 `>=` 与 `>` 混用，与文档语义不一。
- R26-30 `check/leads.ts:114` 引文 grep 读整文件含 fm，`lead-updates.ts:156` 用剥 fm body——两闸命中面不一致。
- R26-31 `check/leads.ts:324` 词表外动词（错别字/繁体）状态闭合与 drift 全盲，仅成长线有合法性黄项。
- R26-32 `check/lead-updates.ts:86` `- ` 开头不匹配条目正则的行静默丢弃无 warn（声明侧少条目→假红）。
- R26-33 `piece-list-core.ts:116-129` 伏笔回收 `=>`/全角`／`格式不符静默跳过；重复 `## 同名段` 后者静默覆盖（与 yaml fail-loud 口径相反）。
- R26-34 `piece-list-core.ts:187-192` 空情绪曲线烘 5 行「待定 N/10」假数据落盘。
- R26-35 `frontmatter-core.ts:367` 有起始 `---` 未闭合时报「未找到起始 ---」，文案误导自愈方向。
- R26-36 `frontmatter.ts:61` `[` 开 `]` 结尾非数组值被静默拆数组，消费侧 String() 失真。
- R26-37 `yaml.ts:71` tab 缩进按 1 空格计（真 YAML 非法），多层嵌套错挂段树无 warn。
- R26-38 `yaml.ts:166` `spec_version: abc` 静默回落 1（同文件 growth 坏值 fail-loud，两套容错）。
- R26-39 `iron-rules.ts:150` parseRatio 不认全角 `％`/`.5`，该规则静默缺失。
- R26-40 `realms.ts:63` 只查后边界不查前边界，「伪金丹」命中「金丹」误判境界。
- R26-41 `style.ts:37` 来源非法值 as 直通不校验（对比 leads 类型非法 fail-loud）。
- R26-42 `count.ts:185-199` 豁免词表子串判定（「林晚晴」豁免「林晚」）漏报向启发式（校准语料覆盖，登记）。
- R26-43 `count.ts:720` `/^##\s.+$/` 要求 ## 后必有空白，`##开头钩子` 报文案失真（strict 下升红误导）。
- R26-44 `check/leads.ts:99` 章号→文件映射首见优先，`5-` 与 `0005-` 并存时引文 grep 选择非确定。
- R26-45 `draft.ts:26-34` 单章路径同一文件双读，机检/三审端点存在两读竞态窗。
- R26-46 `format/leads.ts:130-136` 履历段后无节终标题时作者备注被折进最后一条履历证据。
- R26-47 `count.ts`/`quotes.ts:29` 每次调用重建 10+ RegExp（数百章树聚合可缓存，性能）。

**C 域（9）**
- R26-48 `service.ts:1026-1068` doTrash 仍 existsSync→renameSync，双进程并发软删同名回收站目标后到者静默覆盖（有留底兜底，降 P3；随 R26-7 一并收 linkSync）。
- R26-49 `service.ts:337-344,382` save 新建路径（expectedRevision=null）跨进程同名新建 rename 静默覆盖，双 docId 认领同一文件。
- R26-50 `journal.ts:199` 锁超时降级裸写的「append 近似原子」断言在 SMB/NFS 大行（数万字节）不成立（边界，登记）。
- R26-51 `service.ts:594,755` updateChapterMeta/updateDocMeta 无 journal pending 无快照留底，写坏时无账可回。
- R26-52 `service.ts:829,1015`+`version.ts:266` 结构性留底快照 GBK 源读成 U+FFFD 失真副本且无 M-5 拒绝（trash→purge 后原字节仅剩失真副本）。
- R26-53 `export/index.ts:362-410` 全部章空正文时空 `全本-*.md` 落盘残留 + error 文案误报「均未定稿」。
- R26-54 `cache/schema.ts:67`+`sync.ts:105-107` chapters 表 INSERT OR REPLACE 跨卷重复章号静默后者胜，rebuild 无 warn（对比 foreshadow 同场景有 warn）。
- R26-55 `service.ts:498-513` createDocument 不消毒 relPath（win 保留名 con/aux/尾点必失败裸报错；章节路径有单源消毒，自由区无纵深）。
- R26-56 死代码残留：manifest.ts:157 removeEntry / cache/sync.ts:66 loadLeadFromCache / git/exec.ts:94 commit 死分支 / snapshot.ts 别名层（O-12 已列迁移清单，随批清理可清部分）。

**D 域（11）**
- R26-57 `api/check.ts:186-190` tree-issues 三条件同用键 `warning` 互相覆盖，降级诊断只存活最后一个（改 warnings: string[]）。
- R26-58 `api/books.ts:432-437` rename 同名早退分支在编排闸检查前直写 book.yaml（破坏「闸后写盘」口径）。
- R26-59 `api/onboard.ts:176-186` onboard-save 无留底无乐观锁（R69-26 登记的取舍；与 R26-9 同口径补留底）。
- R26-60 `api/stream.ts:687-688` `Boolean(body['ok'])` 弱类型，字符串 "false" 判真误放行（改 `=== true`）。
- R26-61 `api/rag.ts:96-107` 建索引收尾无条件 set 复活已删书的死状态条目（加存活复检）。
- R26-62 server 模块级单例（lockRoot/sseConnections/filePutChains 等）跨 startServer 实例共享，与 per-instance 原则不一致（生产单实例不触发，登记+测试侧注记）。
- R26-63 `api/providers.ts:391-394` 注释称容错坏 JSON 实际 rethrow，契约漂移（对齐注释或改实现）。
- R26-64 `api/audit.ts:105`/`chat-history.ts:52` 每请求全量重放长书事件库（架构性已知取舍，登记）。
- R26-65 `api/stream.ts:411-413`/`documents.ts:218` readJson 前占闸，悬挂 body 可占书级闸至 requestTimeout（本机信任域可接受，登记）。
- R26-66 `api/stream.ts:263-267` SSE 429 后 EventSource 默认间隔无限重试无退避（前端补退避）。
- R26-67 `api/snapshots.ts:137-184` POST /versions/prune 无任务闸同步扫描删除，大书阻塞事件循环（补闸或缓存口径）。

**E1 域（8）**
- R26-68 `ExportDialog.vue:39-41` catch 无切书复检，失败 toast 落 B 书（补同款复检）。
- R26-69 `StyleBaselineCard.vue:43-44` onFreeze catch 无书名复检（同文件成功路径已有门，catch 漏配）。
- R26-70 `StyleBaselineCard.vue:93-122`（存疑）saveRules 无入口复检，切书后 style.load 可把旧书写回共享 store（补 style.bookName 复检）。
- R26-71 `StyleAcceptancePanel.vue:24-28` onRescan catch 无书名复检（onAnalyze 已有，漏配）。
- R26-72 `SettingsBookRetention.vue:24-28` onPrune 成功/失败 toast 均无书名复检。
- R26-73 `StyleCandidateBox.vue:39-63` 三动作 toast 无书名复检（错书操作风险≈0，仅提示错域）。
- R26-74 `ChapterTreePanel.vue:94-96`（存疑）`treeExpanded.length<=1` 判首开，用户只展开 1 目录时切书重进被默认展开覆盖（改显式初始化标志）。
- R26-75 `ShelfModalHero.vue:21-50` 两处 section 无键盘接法（可达性，对齐 R72-12）。

**E2 域（10）**
- R26-76 `stores/workbench.ts:146-151` 批量进度 done/total 无 Number.isFinite 守卫，NaN 可直入 UI（与 R72-11 自称纪律不一）。
- R26-77 `composables/useHeartbeat.ts:20-33` beat 无超时无在途去重，服务端挂起时缓慢堆悬挂请求。
- R26-78 `composables/useSse.ts:68-90` probeSseBusy fetch 无超时，悬挂时 429 探测整纪元失效。
- R26-79 `stores/workspace.ts:110-124` localStorage 迁移只验 Array.isArray 不验元素类型，且旧键 `clw2.*` 残留不清（对齐 prefs clearLegacy）。
- R26-80 `stores/chat.ts:495-505`（存疑）>200 条 trim 边角 regenerate 本地视图与分支态短暂不一致。
- R26-81 `api/client.ts:109-117` 401/403 re-boot 重放时首响应 body 未 cancel（连接滞留，补 cancel）。
- R26-82 `api/audit.ts` clearAudit 走 30s 默认档，超大事件库有误超时风险（改长档）。
- R26-83 `composables/useChatComposer.ts:18` failedDrafts 书删后条目残留（可忽略量级，随批清理）。
- R26-84 `stores/ui.ts:94-122` aiAvailable 探测为 true 后单向，后端 AI 后续故障不反转为 false（登记：G4 降级面口径）。
- R26-85 `api/client.ts:149` `controller?.signal ?? init?.signal` 右侧死代码（清理）。

**F 域（10）**
- R26-86 `desktop/main.ts:214-227` loadWinState 只校验主屏 bounds，副屏合法位置被判无效回退（扩全 display 迭代）。
- R26-87 `desktop/server-manager.ts:337-345`（存疑）kill 超时放行无 SIGKILL 升级，SIGTERM 被吞时残留占端口烧退避（补 kill 后置强杀）。
- R26-88 `install/migrate-layout-v3.ts:51-76` 细纲目标已存在时 continue 无 errors 留痕，源文件永久滞留（与 R72-9 口径自相矛盾）。
- R26-89 `install/migrate-layout-v2.ts:61`（存疑）伏笔 manifest 重映射与物理搬迁两套迁移编排顺序未审（启动链核实后登记或收口）。
- R26-90 `driver/cc.ts:47-118`（存疑）execRing 跨编排互踩：并发 chat×self-heal 回放缺结局/串场（best-effort 面，有 sync 兜底；登记）。
- R26-91 `rag/embed.ts:115-118` 超时与网络错误同文案，30s 超时不可辨识（分流文案）。
- R26-92 `desktop/server-utility.ts:90-93` unhandledRejection 一律 exit(1)（有意策略，登记）。
- R26-93 `desktop/workdir-store.ts:98-100` filterValidRecent 只查 existsSync 不查 isDirectory（补齐口径）。
- R26-94 `desktop/server-boot.ts:58-61` `--port ''`/非数值静默落缺省端口（NaN 显式报错）。
- R26-95 `log/index.ts:172-175` Bearer 掩码 slice(0,5) 产出「Beare***」破损外观（修掩码口径）。

**G 域（11）**
- R26-96 `process/spill.ts:137` 阈值判定前全量码点展开，no-spill 快路付全额瞬态分配（改非分配计数，对齐 prepare.ts 口径）。
- R26-97 `process/draft-pipeline.ts:463` buildDraftPrompt 注入细纲无章号门（同文件水源③有门），上一章细纲可静默串入本章起草。
- R26-98 `process/materials.ts:44`（存疑）recall 切片信任建索引时点偏移，正文改后错位（R26-15 修复后残留面收窄；登记观察）。
- R26-99 `events/types.ts:154-156` StepEndData.reason 注释含 'timeout' 但词表无（文档漂移，对齐）。
- R26-100 `draft-pipeline.ts:275-279`+`style-samples.ts:42-44` 场景重复不去重，同样章段落重复注入 prompt（水源①②补去重）。
- R26-101 `summary.ts:170` 并发第二调用方拿「已在途」被自愈链当失败 warn（改 skipped 语义）。
- R26-102 `events/branch-tree.ts:101-103` 无 parentSeq 分支组 isDefault 恒 false（根组字段失真，行为正确）。
- R26-103 `events/projection.ts:207-209` 同一坏事件双告警（seq 未递增+重复）噪音（合并）。
- R26-104 `process/book-search.ts:72` fp.slice 假定 bookRoot 无尾分隔符，带斜杠路径错一位（归一化入参）。
- R26-105 `events/store.ts:118` SESSION_MIGRATE_LOCK_TIMEOUT_MS 以 export let 裸导出可变模块状态（收命名或配置对象）。
- R26-106 `process/prepare.ts:115-118` TOKEN_COEFFICIENTS 空表挂账逾一周无 owner/期限（登记：待校准语料；报告 §三 已列入真实余量）。

**H 域（5 + 观察）**
- R26-107 `test/fs/atomic-rename-retry.test.ts:93` mkdtempSync 未接 mkdtempTracked，每轮泄漏临时目录（一行改）。
- R26-108 `test/ai/chat-abort.test.ts:109,156,181` 三处裸墙钟上界断言无 CI 容差，慢机假红风险（对齐 scale ×12+CI×2 口径或改确定性断言）。
- R26-109 e2e 顺序契约（workers=1/retries:0）是登记在案的架构债（维持现状，中期显式化 fixture 前置——维持登记）。
- R26-110 .vue 组件层不在任何覆盖口径（显式登记取舍，维持；组件反复回归时按组件补直测——本轮随 E1 批为 style 两卡补直测验证守卫）。
- R26-111 `test/state/n5-finalized-lost.test.ts` 「不误报」用例 else 分支宽断言（改断言 issues 无 finalizedLost）。
- 观察：`test/corpus/` 空目录删除。

## 六、测试盲区汇总（跨域合并，修复批对应补测）

1. trace-stats / token-calibration / usage-estimate 零直测（A 域）。
2. gen 流中途 stall、runner 三个失败出口、calls NaN 入账防护（A 域）。
3. 短篇 budget round-trip、布尔坏值、`kind:short` 无 `short` 段组合、tagRe 引号外普通词（B 域 R26-10~13 回归锚）。
4. 定稿回写 vs 同书另一进程保存同文件的竞态；非硬链盘降级行为（C 域）。
5. SSE type:'error' 出口脱敏治理测试（D 域 R26-8 回归锚）；PUT /file 留底行为。
6. 零块章重索引旧向量清理；失配→重建→恢复闭环（F 域 R26-15/16 回归锚）。
7. Book.vue 回退重入路径、RAG 轮询失败终态（E 域）。
8. 摘要链跨进程去重（G 域）。
9. 日志密钥掩码零直测（F 域，H 域交叉发现）。

## 七、登记维持项（不修复，理由在案）

- R26-21（多库并发降级记忆键路由——注释已认取舍，无错账）
- R26-42（豁免启发式漏报向——校准语料覆盖）
- R26-50（journal 降级裸写 SMB 边界——本地盘单 syscall 成立）
- R26-62（模块级单例——生产单实例形态不触发）
- R26-64（全量重放——分页/TTL 已缓解，架构性）
- R26-65（readJson 前占闸——有意 TOCTOU 收窄）
- R26-84（aiAvailable 单向——G4 降级面既定口径）
- R26-89/90/92/98（存疑类：启动编排顺序、execRing best-effort、unhandledRejection 策略、RAG 偏移时效——核实后如属实转修）
- R26-106（TOKEN_COEFFICIENTS——待校准语料，非代码欠账）
- R26-109/110（测试工程架构取舍——有守卫在位）

## 八、修复计划（批 A-H，作者指令「全部修复」已预授权）

| 批 | 范围 | 修复项 | 补测 |
|---|---|---|---|
| A | src/ai | R26-1~5 + 21~28（21/24 存疑酌情） | MAX_TOKENS tool 截断、NaN 入账防护、responses 空产出、self-heal save 收编 |
| B | check/format | R26-10~13 + 29~47 | 短篇 budget、布尔坏值、kind 组合、tagRe golden |
| C | document/fs/export | R26-6~7 + 48~56 | 回写锁竞态、非硬链降级、导出空产物 |
| D | studio/server | R26-8~9 + 57~67 | SSE 脱敏治理、留底、ok 强类型 |
| E | web-next | R26-14,17,18 + 68~85 | 回退重入、RAG 轮询终态、doc 缓存刷新 |
| F | desktop/rag/driver | R26-15,16 + 86~95 | 零块章、重建闭环、掩码 |
| G | process/events | R26-19,20 + 96~106 | 摘要锁、sourceSeqs 拆分、细纲门 |
| H | test | R26-107,108,111 + 空目录 | — |

终门：tsc/vue-tsc/eslint 0 错 + vitest 全绿 + 三 check 过 + 新增回归全绿；README 计数同步。修复完成后**从零复审（二十七轮）**，禁读本报告。

## 九、修复收口记录（2026-08-30 验收）

**结果：111 项全办结——修 103，登记维持 5，裁定不修（复核驳回）3。** 门禁八件套全绿：tsc/vue-tsc/eslint 0 错；vitest **555 文件 / 4404 单测**全绿（基线 544/4306 + 净增 11 文件/98 单测）；check:counts（README 计数同步 4404/555）、check:packaging、check:knowledge 过；e2e 43 过 / 2 skipped（release smoke 按 CLWITING_E2E_RELEASE 门控，口径不变）。

**执行形态**：批 B/D/E 子代理成活完成；批 A/C/F/G 子代理中途配额耗尽（1308），改动大半已在树——主评审逐 diff 核销后亲笔补完（含 7 个测试失败修复、writeVersion/writeSnapshot Buffer 直存补齐、导出 publish 裁定与文案如实化、cache 重复章号 warn、git 死分支删除）；R26-27 治理测试、批 A 回归锚（`test/ai/r26-batch-a.test.ts` 9 例）、批 H 测试卫生主评审亲修。

**裁定不修（复核驳回，证据在案）**：
- R26-36（数组字面量解析）：round-trip 锁定行为，非缺陷；
- R26-66（SSE 无指数退避）：实为已存在，复核确认；
- R26-111 初判（n5 测试 else 分支）：健康书恒为态 7（起草新章），finalizedLost 只在态 1 出现——else 非吞断言而是原测试空转；已按真实契约改写（钉态 7 + 无 issues 字段），非「钉态 1」。
- 批 H 附记：test/corpus/ 非空目录（4 个黄金语料 JSON，test/check/corpus.test.ts 消费），删除项驳回。

**连带更新**：test/export/r73-stream-export（R26-53 新文案）、test/install/migrate-layout-v3（R26-88 防覆盖跳过留痕新契约）、test/e2e/check.spec（R26-31 新黄项致首项断言失稳，改按内容定位）、README 测试计数徽章/命令行/门禁行/技术栈行四处同步。

后续：从零复审（二十七轮）另起报告，禁读本报告。
