# 全量代码复审与进度质量评估二十三轮（链第七十五轮）

- 日期：2026-08-29
- 基线：HEAD `4d83431`（二十二轮修复批收口提交）干净树（`git status` 零改动）。
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，告诉我项目完成进度，完成质量，最后形成一个文档给我。记得编排下任务。」
- 确认闸：报告路径经 AskUserQuestion 报作者**获批**（`Dev/Main/01-评审/全量代码复审与进度质量评估二十三轮-评审报告-2026-08-29.md`）。
- 本轮**不含修复**；修复计划见 §十一 批 A-F，待作者指令。

---

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、结论总览

| 维度 | 结论 |
|---|---|
| **完成进度** | **≈96%（核心 mac 生产形态 ≈98%）**：总览第三节 23 个阶段中 21 个已完成；开放 2 项（阶段 13 余中转网关真机验证〔外部依赖，待作者凭据〕、阶段 21 余 J5 收尾 + J6 发布）；六域「相对生产可用」完成度 86-92%（均值 ≈90%） |
| **完成质量** | **8.6 / 10**（六路分域子代理独立评分均 8.5；主评审亲审面〔E 数据层 ≈3100 行、C 保存链、D 互斥矩阵、九条 P2 逐条亲验、门禁八件套亲跑〕无追加缺陷且防御密度一致，综合微调 8.6） |
| **缺陷分布** | **P1×0（连续六轮）/ P2×9（全部主评审亲验属实）/ P3×40（个别为打包条目）/ 观察项×2** |
| **门禁实测** | 八件套全绿（与根 README 声明零漂移）：tsc/vue-tsc/eslint 0 错 + 三 check 过 + 单测 530 文件/4208 用例（1 例偶发红定性观察项，单跑 ×2 绿）+ e2e 42 过 2 跳 |
| **完成度信号** | 全 src 零 TODO/FIXME/stub（唯一 grep 命中系 knowledge/update.ts:109 prompt 模板占位符，非欠账）；六域均报告「全部真实现、无桩无半成品」 |

---

## 二、评审组织与覆盖口径

- **评审方法**：TodoWrite 全程编排；六路分域**禁读**子代理从零深审（全程禁读 `Dev/`、`Archive/` 与一切历史评审报告，防锚定；主评审同样未读任何历史评审报告正本，仅读 README/总览索引行确立计划基线 + 根 README 产品叙述做对账）。
- **分域**：A AI 链路（src/ai 75 文件 + rag/knowledge/learn/cache/metrics/driver，77 文件）/ B 机检格式事件（check/format/events/review，48 文件）/ C 核心数据（document/fs/export/git/install/state/log/process，59 文件）/ D 服务端桌面（studio/server 46 + desktop 12，58 文件）/ E 前端（web-next 组件/视图/composables，agent 覆盖 ≈120 文件）/ F 测试工程与构建 CI（test/ 570 文件盘点抽样 + scripts/ + 全部工程配置）。
- **子代理阵亡与重发记档**：首发 A/C/D/B/F 五路 + E 补发一路；E 首发撞速率限制 1302 阵亡；13:47 前后 A/B/C/D/E 五路撞账户五小时上限 1308 阵亡（F 首发成活并交付完整报告）；14:00:01 限额重置后错峰重发 A/C/D/B 四路全部成活，E 组件层二发成活——**最终六域报告齐整，无覆盖缺口**。
- **主评审亲审补强**（E 域数据层 + 全部 P2 亲验）：`api/client.ts`、`api/stream.ts`、`stores/sse-guards/workspace/doc/workbench/prefs`、`composables/useSse/useChatComposer`、`components/editor/EditorDocHead.vue`、`editor/CmHost.vue` 全文精读（≈3100 行）零缺陷；九条 P2 逐条亲读源码验证属实（§五）。

## 三、门禁实测（主评审亲跑）

| 门禁 | 结果 |
|---|---|
| `npm run typecheck`（tsc --noEmit） | 0 错 |
| `npm run typecheck:web-next`（vue-tsc） | 0 错 |
| `npm run lint`（eslint） | 0 错 |
| `npm run check:counts` | 过：实测 530 测试文件/4208 单测、29 e2e spec/44 用例，与根 README 声明一致 |
| `npm run check:packaging` | 过：resources/ 入打包清单、prompt 版本表对账一致 |
| `npm run check:knowledge` | 过：知识层 13 条 manifest 与磁盘一致、反向扫描无未登记 |
| `npm test`（vitest 全量） | **4207/4208 过**：1 红 = `test/studio/startup-notices.test.ts`「日志同步落痕」（全量并发负载下轮询窗内日志落盘传播慢）；**单文件复跑 ×2 全绿**（3/3、3/3）→ 定性测试隔离/负载竞态，登记观察项①，非产品缺陷 |
| `npm run test:e2e`（build:web + playwright） | **42 过 / 2 跳**（44 用例，28.4s），与基线口径一致 |

## 四、分域评审摘要

| 域 | 范围 | 质量分 | 完成度 | P1 | P2 | P3 | 摘要 |
|---|---|---|---|---|---|---|---|
| A AI 链路 | src/ai + rag/knowledge/learn/cache/metrics/driver | 8.5 | 90% | 0 | 1 | 5 | 编排/重试/计费/加密/事件记录全链完备；RMW 台账全部跨进程锁+原子写；铁律①（模型可见⟺已记录）经 promptFiles/lineage 通道抽验成立；`catalog.gen.ts` 三件套系 A7 预建资产暂未接线（已拍板 RC 后接）非死码 |
| B 机检格式事件 | check/format/events/review | 8.5 | 90% | 0 | 1 | 5 | 11 项机检+中文计量口径（码点/引号/分句）成熟；事件库（WAL/墓碑/孤儿修复/坏行降级）与 frontmatter/YAML 往返实测稳定；三审 draft_hash 契约闭合 |
| C 核心数据 | document/fs/export/git/install/state/log/process | 8.5 | 92% | 0 | 2 | 6 | 原子写（tmp+fsync+rename+目录 fsync）、journal 崩溃恢复、回收站「登记先于移文件」、导出归档不删、迁移幂等全链闭合；五套跨进程锁；域内 ≈18.3K 行测试锁定 |
| D 服务端桌面 | studio/server + desktop | 8.5 | 90% | 0 | 1 | 10 | 58 文件全通读；路径穿越全链封死（resolveWithinRoot/safeDocId/ULID）；错误信封统一+脱敏；Electron 加固（contextIsolation/双导航拦截/utilityProcess 崩溃退避自愈）；扣分在跨进程互斥查询缺口与同型端点闸/缓存不齐 |
| E 前端 | web-next 组件/视图/composables（数据层主评审亲审） | 8.5 | 90% | 0 | 1 | 7 | 竞态防护体系化（代守卫/捕获复查/在途锁/IME 让渡/渲染上限/清理逐一核对）；数据层（client/stores/SSE）亲审零缺陷；残余集中在数值输入健壮化覆盖不全与个别 catch 漏配守卫 |
| F 测试工程 | test/ + scripts/ + CI/构建配置 | 8.5 | 86% | 0 | 3 | 7 | 元治理罕见扎实（README 对账门/spec 顺序快照守卫/覆盖棘轮/架构治理测试/零假覆盖实锤）；扣分在渲染层异常不红、.vue 组件零直测、Electron 壳无 UI 级自动化 |

## 五、P2 清单（R75-1~9，全部主评审亲验属实）

1. **R75-1（A）风格一致规则计数维量纲错配 → 系统性假黄**：`src/ai/rules/style-rule.ts:90-91` 把单章 `adjStackHits`（去重命中**计数**，`check/count.ts:362` `.length`）与 `parallelStreakMax`（拼接语料**最大值**）和 `metrics/style.ts:341` `overall = computeFullStats(全部样章 join('\n\n'))` 的**拼接语料基线**做百分比偏离比较——样章库 ≥2 条时计数维天然偏低常态超 40% 阈值，机检稳定产出假黄项并经 `self-heal.ts:569` 流入重写反馈。比率维（超限占比/复读率等）尺度天然无关不受影响。修法方向：计数维按长度归一（次/千字）或基线改单样章均值。〔亲验：三处源码对读属实〕
2. **R75-2（B）账本履历段 `###`/散文行折入上一条证据 → 引文假红卡定稿 + 回写改排版**：`src/format/leads.ts:73` 段结束只认 `/^##\s/`，`:92-97` 把段内一切非条目非空行（含 `###` 子标题、备注散文）追加进上一条 `证据`；`check/leads.ts` 引文命中以整段证据针串比对 → 正文只含短证据时三候选全 miss → `lead-evidence-miss` 红项硬卡定稿闸；且一次读改写即把作者原排版折成单行。〔亲验：源码对读属实，子代理 node 复现同判〕
3. **R75-3（C）编辑器保存链 UTF-8 留底条件缺口（R66-1 只闭合 AI 草稿路径）**：`src/document/service.ts:318` M-5 闸仅在「新内容含 U+FFFD」时探测盘上编码；作者用干净内容整体替换 GBK 文件时不触发，而 `maybeSnapshot`（`:400`）无条件 `readFileSync(utf-8)` 留底失真快照后原子覆写——原字节唯一可恢复形态已失真（`process/draft-pipeline.ts:64-67` 对同面是无条件 fail-closed，两处口径不一致）。窄场景故 P2。〔亲验：三处源码对读属实〕
4. **R75-4（C）可选清单刷新失败把「已成功的写」报成 WRITE_ERROR 且 journal 补记 aborted 与事实相反**：`src/document/service.ts:346` atomicWrite 落盘成功 → `:350` `maybeUpdateManifest`（清单锁 2×5s fail-closed）超时抛错 → `:361-372` catch 标 `appendAborted` 返回 WRITE_ERROR（catch 注释「atomicWriteFile 失败已自清 tmp，未落盘」在该路径不实）。后果：盘上已成功但客户端收「保存失败」，重试撞 REVISION_CONFLICT；journal 审计面实际完成的写被标 aborted。清单刷新本是 best-effort 路径订正，不应升级整笔失败。〔亲验：调用链对读属实〕
5. **R75-5（D）跨进程删除/重命名互斥查询缺口（J7 面）**：`src/studio/server/api/task-gate.ts:124-131` `heldTaskGatesFor` 只读进程内 Set（注释自认「跨进程场景交由真锁 J7 收口」），`api/books.ts:100` busyGate 不扫 `.clwriting/task-gate/*.lock`——dev-api/脚本与 GUI 多进程并存时，进程 B 的 DELETE/RENAME 感知不到进程 A 持有的分钟级闸，graveyard 改名后任务收尾原子写会在旧路径重建孤儿目录并白烧 API 费。生产单进程 Electron 形态不触发，故 P2 不升 P1；任务闸 acquire 侧本身有 O_EXCL 跨进程锁，缺的只是反向查询。〔亲验：两文件对读属实〕
6. **R75-6（E）SettingsBook 数值输入空串→0 直写 → 页宽塌 0px**：`src/studio/web-next/src/components/ui/SettingsBook.vue:178-179/202-203` 数字输入 `@change="onPfwInput(Number(...))"`——清空失焦 `Number('')===0` 直写 `prefs.bookPageWidth`（`stores/prefs.ts` setPageWidth bookOnly 分支无钳制，apply() 直写 `--page-width: 0px`），编辑器排版崩坏且专注模式辅助条被隐藏；自动保存间隔同型写入 0（定时器被 `pages/Book.vue:156` `Math.max(5,…)` 兜底，影响限于显示误导）。`shared/numeric-input.ts` 的 `parseNumericInput` 已接入 SettingsAi/Analysis/BookAnalysis/Editor/Retention 五处，唯漏 SettingsBook——R72-11 修复的 bug 类残留。〔亲验：源码+接入面 grep 属实〕
7. **R75-7（F）e2e 渲染层异常基线只 warn 不红**：`test/e2e/page-error-baseline.ts:23-31` `page.on('pageerror')` 与 console error 均 `console.warn`，29 个 spec 的唯一渲染层异常防线不参与通过判定——Vue 组件抛未捕获异常时用例照常绿。头注自认「待基线噪音摸清后再评估升级」；本轮 e2e 实跑日志中 usage-card/version-restore 确有 400 console-error 噪声，升断言前需先清噪音。〔亲验：源码+实跑日志属实〕
8. **R75-8（F）102 个 .vue 组件零直测、零覆盖门**：`vitest.config.ts:81` coverage `include: ['src/**/*.ts']` + `:85` 显式排除 components/types，注释自认「.vue 组件层仍不入口径」——组件内部逻辑仅靠 44 个 e2e 用例间接兜，不经 e2e 路径触发的分支（错误态 UI、边角交互）无机器守护。〔亲验：配置对读属实〕
9. **R75-9（F）Electron 壳无 UI 级自动化（含 win 安装器未复验）**：desktop.yml 冒烟 = mac 打包态 .app + win-unpacked 解包态 exe 的「存活 + ready 标记 + 无 renderer-crash」三重判；真实 Electron API 行为面（原生菜单/dialog/preload IPC 实链）自动化空白，release-smoke 只 spawn server-main.js 不启壳；win NSIS 安装器本体未实测（与 J6 未动口径一致）。〔亲验：workflow 对读属实〕

## 六、P3 清单（分域压缩，40 条目；修复批落地时逐项验证/裁定）

- **A（5）**：resolveProvider 失败 trace `model:''` 落空桶（runner.ts:415）/ overlay existsSync→readFileSync TOCTOU 窗口（prompts/resource.ts:93,167）/ search 命中行 UTF-16 截断半代理对（tools/search.ts:15）/ MOCK_USAGE 按引用共享（测试面）/ rules 层 toPrompt 与 check 双读盘（性能小账）。
- **B（5，含打包 5 小项）**：`开启章` 无 isFinite 守卫——手写「三」→ NaN 落库 NULL→「悬太久」误报 + 回写 `NaN` 污染（leads.ts:199；同库 chapters.ts:88-92 R64-19 已修同款，**主评审亲验属实**）/ 树红点纪元复核 R71-20 后为「轮前一次」，循环期间外部变更检测不到（check/run.ts:401,478，自愈方向）/ store.close() 中 db.close() 抛错则清理跳过——30s 续期定时器永刷、迁移被无限期拒（events/store.ts:796-807，失败方向安全）/ countWords 计入全部中文标点（与行业「不含标点」习惯偏差 ~10-15%，口径问题非 bug）/ 打包项：禁词纯子串无 ASCII 词边界、checkOpeningNoEnv 码元口径、iron-rules 缓存浅拷贝、review 满审注释与实现漂移、n-gram 码元切片。
- **C（6）**：清单锁外路径/回收站守卫 ms 级 TOCTOU（service.ts:263-295；finalize.ts:46 锁外 relPath 同族）/ migrate-layout-v2 清单重写不跟踪同名跳过文件（一次性迁移，errors 可见）/ executeSave 每保存同文件全量读 4-5 次（读放大，finalize 已做 R72-5 单读改造此处未跟进）/ books.jsonl 路径校验不查 symlink（仅词法）/ 同步锁 Atomics.wait 最长 10s 阻塞事件循环（全同步架构已接受取舍记档）/ git 网盘扫描未跳 spills 与 .journal（遍历噪声）。
- **D（10，第 10 条打包）**：/relations/mine 有闸无 orchestrationBusyFor（settings.ts:80-128）/ words-diary appendBaseline 无闸可双记（documents.ts:263-278）/ rewrite 查 self-heal+spawn 未直查 isChatRunning（注释自认嵌套工具持同闸缓解）/ 单条 finalize 无闸对照 batch 有闸（documents.ts:175-199，同步执行缓解）/ GET /state 与 tree-issues 无 TTL 缓存（state.ts:53、check.ts:145，health.ts 同型有 5s）/ 双写协议并存（PUT documents 队列 vs PUT /file 链互不感知，无 expectedRevision 时最后写胜）/ providers test 探测窗配置被改→旧 caps 回写新配置（providers.ts:371-410）/ 删书 abortSelfHeal/abortChat 先于 busyGate（books.ts:234-239，窗口窄）/ draft-save 无闸绕过 DocumentService 队列（saveDraft 内 snapshot 缓解）/ 打包项：导出等待队列跨书无全局上限、ai-status env 风格、dev setProxy 无 catch。
- **E（7）**：SettingsWriting 每卷章数清空→0 被 clamp 成 5 静默写入（SettingsWriting.vue:16-18）/ saveTiers 缺在途重入锁（AiServicePanel.vue:228，同文件 save/saveRag 均有 R73-62 锁）/ 失败 toast 跨书落地三处（StyleAcceptancePanel.vue:53、ForeshadowPanel.vue:112、useRelationGraph.ts:269——成功路径有守卫 catch 漏配）/ 伏笔新建无在途锁（ForeshadowPanel.vue:92）/ MetaFormPanel 清空字段 `if (v==='') continue` 不发请求——服务端保留旧值但 toast 报已保存（MetaFormPanel.vue:223，待服务端 merge 语义核）/ Esc 处理无 IME 让渡两处（Shelf.vue:45、ShelfModal.vue:68）/ 打包项：ContextMenu 子菜单仅 hover 可达、ChapterTreeItem blur+Enter 理论双发（状态守卫实际拦截）。
- **F（7）**：CI push 腿关闭（PR→main + 手动，频推期临时态已登记）/ lint 射程缺口（test/ 530 文件 + scripts/*.ts + .vue 不在射程，无格式门系有意）/ win 腿零 e2e + e2e 仅 chromium（D5 决策口径）/ mac 仅 arm64 DMG（H-2 登记）+ win NSIS 安装器本体未实测（同 R75-9）/ 计时启发式用例假红面（rag/scale 界值两次被迫复校实录、cc-cancel-stream 150ms 启发式）/ 小桶薄（learn 5 用例、fs 50 用例；29 spec 中 14 个仅 1 用例）/ Electron 冒烟固定 sleep 25 后 grep（极慢 runner 边缘假红面）。

## 七、观察项

1. **startup-notices 全量并发偶发红**：本轮全量跑 1 红（「日志同步落痕：失败通告同时写入 userData/logs/ JSONL」），单文件复跑 ×2 全绿——轮询窗内日志落盘传播在并发负载下变慢所致，测试隔离/负载竞态非产品缺陷。维持登记（历轮同判）。
2. **e2e console-error 400 噪声**：usage-card/version-restore spec 各 3-4 条「400 Bad Request」资源加载报错进 warn 基线——R75-7 升断言前需先清此噪音（定位 400 的端点与语义后收敛）。

## 八、开放任务代码实证

| 项 | 代码实证 | 状态 |
|---|---|---|
| 阶段 13 余中转网关真机验证 | responses-adapter.ts（448 行）三协议适配完备，R74-1 用量估算兜底在位（`:272` `estimated:true`） | **待作者网关凭据**（外部依赖，代码侧无欠账） |
| 阶段 21 J0-J4 | 图标 `build/icon.png` 512×512 单真相源（electron-builder.yml mac/win 双引用）；win NSIS x64 配置在位（D1/D3/D4 拍板口径）；CI windows-latest × Node24 腿在位（R69-6/J4）；CI PR→main 自动腿已恢复（R74-5）；45+ skipIf(win32) 守卫 | 已完成 |
| 阶段 21 J5 体验面 | 无框标题栏+WCO 窗控+主题联动+避让修正（`bb9c344`/`300bc87`/`2d9f402`/`439b934`）+ prefs.applyTheme 的 setTitleBarOverlay 联动 | 大件在位，余收尾打磨 |
| 阶段 21 J6 发布 | desktop.yml win 腿已备（打包+冒烟），NSIS 安装器实测未做（R75-9）；发布节奏待作者 | **未动** |
| H-1/H-2 决策 | H-1 图标接入方式已销（build/icon.png 单源）；H-2 win arch（x64 only）已随 D3 拍板落配置；mac arm64-only 无 Intel 产物（F 域登记） | 大体销项，Intel mac 产物待拍板 |

## 九、完成进度评估

- **计划面**：总览第三节 23 个阶段，21 个已完成（阶段 1-20、22、23）；开放 2 项均有明确外部/决策依赖（阶段 13 待凭据、阶段 21 J5 收尾 + J6 发布执行），非代码欠账。
- **代码面**：六域「相对生产可用」完成度 86-92%（A90/B90/C92/D90/E90/F86，均值 ≈90%）；全 src 零 TODO/stub；产品叙述（防吃书账本/三审/伏笔/文风系统/安全三层防线/200 万字界值测试）与代码设施一一对应。
- **剩余大头**：J6 win 发布执行（冒烟基建已备、安装器未实测）、阶段 13 真机验证（外部）、九条 P2（全部窄条件、不阻塞日常使用）、组件直测面起步。
- **综合：进度 ≈96%；核心 mac 生产形态 ≈98%**（mac 侧功能性收口只差 P2 尾巴与外部验证）。

## 十、完成质量评估

- 六域子代理独立评分均 8.5（各自附独立理由，非协商值）；主评审亲审面（E 数据层全文、C 保存链、D 互斥矩阵、九条 P2 逐条亲验、门禁八件套亲跑）未发现子代理漏报的高危缺陷、亦未追加新缺陷，防御密度与注释可回溯性（轮次编号修复链）在六域间一致。
- 正信号：P1×0 连续六轮；门禁八件套与 README 声明零漂移；测试元治理（对账门/顺序守卫/覆盖棘轮/架构治理测试/零假覆盖实锤）罕见扎实；「模型可见⟺已记录」铁律经运行期抽样校验成立。
- 扣分主线：九条 P2（系统性假阳 R75-1/假红 R75-2、留底编码缺口 R75-3、失败语义倒挂 R75-4、跨进程互斥缺口 R75-5、输入边界回归 R75-6、测试面三缺口 R75-7/8/9）+ P3 群的「一处加固未传播到同族路径」不对称。
- **综合：8.6 / 10**。

## 十一、修复计划建议（批 A-F，文件面互斥可并行；待作者指令）

- **批 A（src/ai + src/metrics）**：R75-1 风格规则计数维归一（次/千字）或基线改单样章均值 + 回归（多样章基线假阳复现用例）；连带 P3：runner trace `model` 回填、overlay 读失败收编、search 码点切片。
- **批 B（src/format + src/check）**：R75-2 parseHistory 段内 `#` 系标题/散文行处理（标题终止段落、散文行不折入证据或显式警告）+ 引文命中回归；连带 P3：`开启章` isFinite 守卫（对齐 chapters.ts R64-19 口径）。
- **批 C（src/document）**：R75-3 maybeSnapshot 编码探测对齐 draft-pipeline fail-closed 口径；R75-4 maybeUpdateManifest 失败语义收编（best-effort 化：成功返回 + startup-notice/日志留痕，不标 aborted）；连带 P3：executeSave 单读同源化（对齐 finalize R72-5）。
- **批 D（src/studio/server）**：R75-5 busyGate 补跨进程闸锁文件扫描（或按 J7 台账登记统一收口，作者拍板）；连带 P3：/relations/mine 补 orchestrationBusyFor、words-diary 闸、/state 与 tree-issues TTL 缓存、providers test 探测窗快照复核。
- **批 E（web-next）**：R75-6 SettingsBook 两处数字输入接入 parseNumericInput + 下限钳制；连带 P3：SettingsWriting 卷大小同款、saveTiers 在途锁、失败 toast 跨书守卫三处、伏笔新建在途锁、Esc IME 让渡两处、MetaFormPanel 清空语义核服务端。
- **批 F（test/ + CI）**：R75-7 pageerror 基线升断言（先清观察项②的 400 噪音：定位端点语义→修或白名单→升红）；R75-8 组件最小直测面起步（错误态/边角交互优先的 5-10 个组件）；R75-9 与 J6 合办（win 安装器实测 + Electron UI 自动化评估）。
- 优先级建议：批 B（卡定稿闸的假红）> 批 C（数据正确性）> 批 A（重写反馈污染）> 批 E（用户可踩）> 批 D（dev 形态）> 批 F（测试面升级）。

---

- 索引同步：本报告落盘 + `Dev/Main/README.md`（变更记录 + 目录地图 01-评审 行暂存 3→4 篇）+ 总览 1.1（二十三轮行）+ `Dev/Main/01-评审/README.md`（维护日期 + 当前暂存 3→4 篇）。
- 源码零改动（本轮唯一写入面 = Dev/ 文档链四处）。

## 十二、修复批执行记录（2026-08-29 收口）

作者指令「**开始修复**」，按 §十一 批 A-F 执行。执行形态：批 B/C 主评审亲修，批 A/D/E 三路文件互斥并行代理首发（15:16 派发）；**15:27 三代理齐撞账户五小时上限 1308 阵亡**（限额 19:00:36 重置；本链第三次同型事故），改动大半在树——按二十一轮先例由主评审逐 diff 核销补齐。**P2×9 = 修 8 + 范围外登记 1**；+5 测试文件 / +39 用例（530/4208 → **535/4247**）；终门八件套全绿（535 文件/4247 单测 + tsc/vue-tsc/eslint 0 错 + 三 check 过〔根 README 同步 4247/535〕+ e2e 42 过 2 跳）。

### 批 B（亲修，R75-2 定稿假红主线）

- **parseHistory 标题折入收口（双侧）**：条目段内 ATX 标题（`#`~`######` 后随空白）不再折入上一条证据——此前 `### 手记` 等标题被 R64-17 续行折拼进证据（needle 命中正文必败 →「声明了没兑现」定稿假红；回写 dedup 精确比对永不相等 → 重复条目固化）。节界双态：**节终标题**（其后到下一标题/文末无条目）终断条目段、其后内容由 bodyAfterHistory 保真回写；**分组标题**（后随条目）跳过、条目照常解析（不丢条目——防闭合校验反向假红）。`#` 一级/`###` 三级此前不触发 `## ` 终断、一律折入，同修收口。parseHistory 与 bodyAfterHistory 共用 `headingEndsSection` 单出处防口径漂移。
- **声明侧同步（check/lead-updates.ts parseLeadUpdateLines）**：R73-23 对齐口径随之补标题分流——分组标题跳过、节终标题终断（其后人工备注不再折入声明证据）；与履历侧共用 headingEndsSection。
- **`开启章` NaN 守卫**：`Number()` 无守卫（手写「十二」→ NaN 落模型）→ 对齐 chapters.ts R64-19 口径非有限数回落 0。
- 维持登记：无标题尾随散文续行仍折入证据（R64-17 折行证据与尾注散文不可区分，维持既有口径记档）。
- 回归：leads.test +6（含节终往返保真/空履历尾段/分组不丢条目/开启章回落）+ lead-updates.test +3。

### 批 C（亲修，数据正确性）

- **R75-3 编码防线条件缺口收口**：M-5 原仅拦「新内容含 U+FFFD 且盘上非 UTF-8」——新内容干净时静默放行覆写 GBK 文件，maybeSnapshot 以 utf-8 读盘留底的是失真快照（假留底）。对齐 draft-pipeline R66-1 / lead-finalize M-9 无条件口径：盘上非 UTF-8 一律拒绝（文案同步改写）。
- **R75-4 清单刷新 best-effort 化**：文件已原子落盘后 maybeUpdateManifest 抛（锁超时/磁盘满）此前落外层 catch → journal 误记 aborted + 返回 WRITE_ERROR（保存成功却报失败，重试撞 REVISION_CONFLICT）。改 try/catch + log.warn 留痕后照常 settled + 返回成功（对齐 P2-BE-4 字数日记口径；清单为可重建索引，树扫盘自愈）。
- 维持登记：executeSave 双读（M-5 字节探测 + wordDelta utf-8 读）未合并单读——两读编码口径不同、同锁窗口内小文件，合并收益小于改动面。
- 回归：service.test +2（干净内容覆写 GBK 拒绝且字节不动；清单目录占位制造读抛 → 保存仍 ok）。

### 批 A（代理阵亡→主评审核销，代码全在树）

- **R75-1 计数维量纲归一**：ref.overall 是全部样章 join 拼接语料指纹而比对对象是单章——计数维直比在样章库 ≥2 条时天然偏低常态超 40% 阈值稳定产假黄。修法分维：**形容词堆叠**（真计数维）双侧密度化（次/千字，FullStyleStats 增可选 charCount 归一因子，v1 旧基线缺字段降级跳过不伪造）；**排比连续度**（极值统计，E[max] 对数级增长，密度归一会把旧假阳换成新高假阳）只保「偏高」单侧（低侧为结构性噪声放弃，绝对上限另有文风铁律兜底）。
- P3 三件：runner 失败路径 trace model 桶回填（modelHint=provider:id / tier:kind，不再落空桶）；overlay 读失败收编带路径上下文错误（fail-fast 不变）；book_search 命中行截断改 clipByCodePoints 码位口径（不切半个代理对）。
- 主评审补齐 2 处：search 码位测试的边界反证断言算术错（4 汉字+28 emoji=60 码元恰完整收尾，反证取 61 才切高代理项）。
- 回归：rules-style +130 行（密度/单侧/旧基线降级）、metrics/style +31 行（charCount 往返）、chain-events/prompt-resource/search 增量。

### 批 D（代理阵亡→主评审核销，代码全在树）

- **R75-5 busyGate 跨进程缺口**：进程内 Set 与跨进程锁文件扫描合并去重——dev-api/脚本与 GUI 双进程并存时，此前只查 heldTaskGatesFor 看不见他进程分钟级任务闸，放行删/改后收尾原子写在旧路径重建孤儿目录。新增 queryLockHeld 只读锁状态查询（复用 judgeStaleLock 完整陈锁语义，绝不取锁绝不清理）+ crossProcessHeldTaskGatesFor 正向枚举已知 action 的锁文件（15 action 注册表，陈锁不算在持防永久 409）。
- P3 四件：/relations-mine 补 orchestrationBusyFor 409（编排互斥矩阵补角）；/state 与 /tree-issues 5s TTL 结果缓存（health.ts styleScanCache 同款：书键 Map+FIFO+纯 TTL，删书/改名挂 forgetBookKeyedCaches）；providers test 探测窗配置指纹复核（探测 10s+ 窗口内供应商被编辑 → 旧快照 caps 丢弃不回写 + warn 留痕）。
- **主评审连带修（批 F 时发现）**：/tree-issues 纯 TTL 自愈假设对 verdict 流不成立——ReviewPanel 的 UI 契约是「裁决写完立即 loadIssues 刷新红点」，写后首读恰命中缓存 → 红点变化被吞且无轮询兜底（e2e tree-issues 实证红）。review-verdict 端点补 forgetTreeIssuesCache 单点失效（树红点唯一写侧来源，不属「每写端点平添接线」的过度设计）。
- 主评审补齐 3 处：state.ts 悬空 import（代理死于收尾）；TTL 测试短篇书 fixture 缺定稿登记（未定稿篇计入 excludeNames 不推 nextChapter，三态无从观测）；既证明测试串扰为编辑半态非缺陷。
- 维持登记：words-diary 写点不另加任务闸——appendWordsDelta 已 best-effort catch（P2-BE-4），闸属过度设计。
- 回归：+4 新测试文件（busy-gate 6 用例含陈锁/死 pid/注册表漏登记语义、providers-writeback 3、relations-mutex 1、TTL 三态 3）+ state-batch-pause/tree-issues-api 注入 TTL=0 保即时语义。

### 批 E（代理阵亡→主评审核销，代码全在树）

- **R75-6 SettingsBook 数值健壮化**：书级 num-input 两处接 parseNumericInput + 组件层钳制（纸宽 600-1400、间隔 5-120）——此前 `Number('')===0` 穿透直写 prefs.bookPageWidth，apply() 落 `--page-width: 0px` 页宽当场塌掉。
- P3 六件：SettingsWriting 每卷章数同款 helper（清空不再被 store clamp 静默钳成 5）；saveTiers 在途锁（双击双 POST revision 双 bump）；失败 toast 跨书守卫三处（useRelationGraph onMine 成/败双门 + ForeshadowPanel create catch + StyleAcceptancePanel catch）；伏笔新建在途锁；Esc IME 让渡两处（ShelfModal/Shelf，isImeComposing 单源判据）；MetaFormPanel 清空字段照发不跳过（此前 skip 掉「清空不持久化但 toast 报已保存」的静默谎言）+ shared/words unquoteFmValue 配套（服务端 `key: ""` 落盘读回剥引号，表单不再显示两个引号字符）。
- 主评审补齐 1 处 + 新增回归：use-relation-graph 测试 doc store mock 缺 bookName 字段（新守卫误判已切书 → 三用例连红）改 getter 活源；补切书守卫回归用例（mid-flight 改书名 → 不 toast 不重载旧书）。
- 记档：E 域 P3 群无独立新测试文件（组件逻辑薄、挂账 R75-8 直测面扩展）。

### 批 F（亲修，测试面升级）

- **R75-7 pageerror 基线升红**：全量 e2e 摸底 108 条噪音全部是浏览器对**预期 4xx**（负路径用例断言对象）自动打的「Failed to load resource」日志，pageerror 事件为零——升级条件成熟。pageerror 监听器内 throw（Playwright 记为用例失败；临时探针 spec 注入 pageerror 实证必红后删）；console error 滤除资源加载日志后维持 warn（真实应用 error 保留排查面）。升红后全量 e2e 噪音归零。
- **R75-8 组件直测面起步**：SettingsBook num-input 挂真组件直测 4 用例（清空不写/非数字不写/越界钳制/正常写）——本书页此前零组件直测。**起步落地 1 组件，其余触改组件挂账**（Foreshadow/StyleAcceptance/AiServicePanel 等，随下轮测试面扩展）。
- **R75-9 范围外登记**：win 安装器实测 + Electron UI 自动化评估维持 J6 合办挂账（阶段 21 J6 未动）。

### 门禁与索引

- 终门八件套全绿：tsc 0 错 / vue-tsc 0 错 / eslint 0 错 / check:counts 过（README 同步 4247/535）/ check:packaging 过 / check:knowledge 过 / **vitest 535 文件 4247 用例全绿** / e2e 42 过 2 跳（pageerror 升红后噪音 0）。
- 确认闸遵守：全批零 .md 新增/移动/重命名（本收口记录为既有报告 §十二 追加）。
- 工作树未提交（待作者指令）。索引同步 4 处 .md：本节 + `Dev/Main/README.md`（变更记录收口条）+ 总览 1.1（二十三轮行已收口）+ `Dev/Main/01-评审/README.md`（维护日期 + 收口记）。
