# 内存泄露与平台适配专项重评——评审报告（2026-09-02）

> **状态：已收口（2026-09-02 同日修复批）——MP-1 办结、观察项 4 维持登记**（收口记见 §五）

- **类型**：评审报告（两域专项：内存泄露〔后端 + 前端〕+ mac/win 平台适配）
- **finding 编号**：**MP-N**（Memory + Platform 两域合编）
- **基线**：HEAD `991b598`（dev，win FontPicker 无损合并后）**干净树**（git status 零输出）。相对上次独立重评基线 `db16c39` 增量 **27 文件 +1353/−185**（IR 修复批 `37f1989` + win 字体下拉自绘 FontPicker `4419f8b`→`bd9d9e1` + 两笔无损合并 `bd01d1c`/`991b598`），全部纳入本轮评审面。
- **作者指令**（2026-09-02）：「忽略现有的评审文档，重新评审一遍项目所有代码，重点检查内存泄露，也要注意mac和win平台的适配问题，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。记得编排下任务。」
- **确认闸**：本报告路径 `01-评审/内存泄露与平台适配专项重评-评审报告-2026-09-02.md` 经 AskUserQuestion 报作者**获批**——**专项命名不接轮号**（本轮为两域专项而非全量八域，与「项目全量独立重评」独立命名先例同例）+ **MP-N 编号**。
- **禁读口径**：未读任何既有评审报告正本，两域从零评审。文中历史防线/修复编号（B3/C4/D1/D2/IR-9/R1W/R2W 等）系本轮亲读源码内注释与 git 历史所载，非报告间引用。
- **写入面**：源码零改动；唯一写入 = 本报告 + 索引同步 3 处 .md（`Dev/Main/README.md` 变更记录与目录地图、总览 1.1、`01-评审/README.md`）。

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、结论速览

| 维度 | 结论 |
|---|---|
| 发现 | **P0×0 / P1×0 / P2×0 / P3×1（MP-1）+ 观察项×4（登记）** |
| 门禁 | 八项全绿（vitest 747 文件/5265 单测 + e2e 43 过 2 跳 + tsc/vue-tsc 0 错 + eslint 0 error〔31 warn 存量〕+ 三 check 过） |
| 内存泄露专项 | 前后端**均未发现可累积增长的缺陷**；既有防线体系（§6.3）亲验全在位；唯一发现 = KB 级卫生项 MP-1 |
| 平台适配专项 | mac/win 双闸成网（§6.4）；**未发现新适配缺口**；历史修复面逐点亲验在位 |
| 完成进度 | **≈98%**（mac 生产形态 ≈98.5% / win RC ≈97%），与总览台账一致 |
| 完成质量 | **A（9.0/10，专项口径）**——聚焦域零实质发现 |

## 二、评审范围、方法与执行形态（如实记档）

### 2.1 范围

- **内存泄露专项**：`src/` 后端（Electron 主进程 + utilityProcess 服务端）与 `src/studio/web-next/` 前端（Pinia stores / composables / 组件生命周期）全部长生命周期面——定时器、事件监听、子进程与流句柄、SQLite 句柄、缓存与 Map·Set 增长点、SSE 连接与 EventSource。
- **平台适配专项**：mac/win 双平台分支——路径语义（大小写/分隔符/保留名/长路径）、文件占用行为（EPERM/EBUSY）、文本编码（CRLF/BOM）、子进程（windowsHide/env）、窗口与字体（WCO/字体栈）、退出信号、打包配置。
- 进度与质量评估对照基线：根 README 功能承诺面 + 总览第三节任务台账（任务状态唯一入口）。

### 2.2 执行形态

- **前端内存专项子代理：成活**。全量扫描 web-next 六类候选面（stores 模块级状态 / composables / 组件生命周期清理 / EventSource / 定时器 / 集合增长点），上报存疑 5 项。
- **后端内存深评子代理 + 平台适配深评子代理：双双撞账户 5 小时使用上限（1308，2026-09-02 23:32:29 重置）阵亡**——本链又一宗同型限流事故，按既定协议转**主线程串行深评**。
- **主线程串行深评（后端）**：先以 12 类长生命周期候选面 grep 摸图（setInterval/setTimeout 持句柄、EventEmitter 监听、模块级 Map·Set 增长点、process.on、spawn/utilityProcess、DatabaseSync open、流句柄、EventSource、ResizeObserver·MutationObserver、Pinia 模块级状态、缓存对象），命中文件全文亲读 ≈4,400 行（stream.ts 884 行 / desktop/main.ts 1,074 行 / server-manager.ts 685 行 / log/index.ts 377 行 / git/exec.ts 335 行 + events-store / cross-process-lock / style / provider store 关键段）+ DatabaseSync 错误路径三处精读 + 七个 API 缓存 forget 挂点 grep 核验。
- **主线程串行深评（平台）**：platform 分支文件全文亲读（main.ts / server-manager.ts / user-data-path.ts / electron-builder.yml）+ 定向扫（路径大小写比较点 / 保留设备名 / 字体 / windowsHide / env 清除 / 退出信号 / 打包配置）+ 历史平台修复面（R1W / R2W / CRLF·BOM 族 / IR 批）逐点亲验在位。
- **覆盖口径记档**：主线程亲读保底深度（全部结论以亲读源码为准）；并行交叉复核密度低于全代理成活轮——定性为深评密度略降、不影响结论置信度，如实登记。

### 2.3 亲验纪律

代理上报与自查候选全部由主评审对照源码逐行核实后定级（§4.1 行号即亲验产出）；证伪/排除记录见 §6.2。

## 三、门禁结果（本轮亲跑）

| 门 | 结果 |
|---|---|
| vitest 全量 | **747 文件 / 5265 单测**（5265 过 + 2 平台跳，138.64s） |
| tsc（typecheck） | 0 错 |
| vue-tsc（typecheck:web-next） | 0 错 |
| eslint | 0 error / 31 warn（存量，与历轮登记一致） |
| check:counts | 过（与根 README 零漂移） |
| check:packaging | 过 |
| check:knowledge | 过 |
| e2e（playwright） | 43 过 2 跳（25.6s） |
| coverage 多桶门 | 未复跑——评审轮零源码/零测试改动，绿态维持 IR 修复批终门（exit 0）；其后增量仅 FontPicker CSS 与两笔合并 |

## 四、发现清单

**P0×0 / P1×0 / P2×0 / P3×1（MP-1）+ 观察项×4（登记）**

### 4.1 MP-1（P3·前端·内存卫生）：provider store 测试结果缓存无删出口

- **位置**：`src/studio/web-next/src/stores/provider.ts`
  - `remove()` :205-219——删提供方时清 `modelsByProvider`（:210）、`probeModels`（:211），**漏 `testResults`**；
  - `removeRag()` :322-333——删 RAG 配置**整体漏 `ragTestResults`**；
  - 写入口 `test()` :240-246（`s.set(id, r)` :245）与 `testRag()` :335 起（`m.set(id, r)` :340）只 set，无对应清理。
- **影响**：删除提供方/RAG 配置后，其连通性测试结果条目残留于两个响应式 Map。单条为小对象（结果 + 错误文案），KB 级；读侧按 providerId 键取，残留项不再被读到——**无功能影响、无累积增长路径**（同 id 重建会被覆盖），纯卫生缺口。
- **定级**：P3（卫生项），本轮唯一 code finding。
- **亲验**：主评审对照 :195-345 段源码逐行核实（本节行号即亲验产出）。

### 4.2 观察项×4（登记，不构成缺陷）

| # | 位置 | 事实 | 定性 |
|---|---|---|---|
| O-1 | CM6（CmHost / EditorView） | 编辑器内部自管资源（测量缓存等）由库持有 | 随 `EditorView.destroy()` 释放；宿主销毁链（卸载 → destroy）亲验在位——库行为，接受 |
| O-2 | useSse | 重连时 es 闭包引用与新实例存在替换竞态 | 旧 EventSource 已 close / 可被 GC，无句柄累积——鲁棒性备注，非泄漏 |
| O-3 | workbench `textOut` | 常驻「末代整章草稿」文本 | 设计使然（导出/复制缓冲用途），切书/清空释放 |
| O-4 | `src/metrics/style.ts` :106-118 | chapterBodyCache FIFO 上限 4096 章正文缓存 | **有界设计权衡**：stat 指纹失效 + 文件消失自清（:118）+ FIFO 驱逐三闸在位；最坏驻留 = 4096 章 × 章均字节数（3KB/章量级 ≈ 12MB，可控）——超大书库（>4096 章活跃编辑）场景关注，登记观察 |

## 五、修复建议与处置（待作者指令）

- **MP-1**：`remove()` 补 `testResults.value.delete(id)`、`removeRag()` 补 `ragTestResults.value.delete(id)`——两行，与既有 :210-211 同款写法；可选配套直测一条（删提供方后 `testResults` 不再含该 id）。**属 P3 可随任意下批顺修**。办结（或作者拍板维持登记）后回填本节收口记，本报告方转已收口。
- **观察项**：不修。O-4 建议在台账登记「超大书库内存画像」观察点（量级估算已记 §4.2）。

### 收口记（2026-09-02 同日修复批）

作者指令「全部修复，编排任务，并发做。」——**MP-1 办结，观察项 4 维持登记**（代码线 + 文档线并行执行，主线程并行工具调用，小面积不派代理）：

- **修面**：`src/studio/web-next/src/stores/provider.ts`——`remove()` 补 `testResults.value.delete(id)`、`removeRag()` 补 `ragTestResults.value.delete(id)`（两行，与既有 modelsByProvider/probeModels 同款直接 delete 写法，各带一行 MP-1 编号注释）。
- **回归**：`test/studio/webnext/provider-learn-stores.test.ts` 新增「MP-1」describe 块 2 用例——remove 后 testResults/modelsByProvider/probeModels 三缓存不再含该 id；removeRag 后 ragTestResults 不再含该 id（复用该文件既有 provider store mock 面）。实施期纠错 1：测试种子 `caps: null` 与 TestResult 类型不合（vue-tsc 抓到——mock 实现无类型约束而直赋 Map 有约束），改 `{ ok: true }` 复绿。
- **连带**：`Dev/Main/README.md` 目录地图「项目全量独立重评」内联摘要状态滞后（未收口→已收口，其余三处早已一致）一并校正；根 README 计数修账 5265→5267（badge / 开发 / 合入门槛 / 技术栈四处）。
- **终门八项全绿**：定向 vitest 6/6 + **全量 vitest 747 文件/5267 单测一次全绿**（113.60s，+2 平台跳）+ tsc/vue-tsc 0 错 + eslint 0 error〔31 warn 存量不变〕+ check:counts 过（修账后零漂移）+ check:packaging 过 + check:knowledge 过 + e2e 43 过 2 跳（28.1s）。
- **状态：未收口→已收口（2026-09-02）**。工作树未提交待作者指令。

## 六、已排查干净面（亲验记录）

### 6.1 内存·后端（主线程全文/关键段亲读）

| # | 文件 | 亲验要点 |
|---|---|---|
| 1 | `src/studio/server/api/stream.ts`（884 行全文） | SSE 生命周期三路清理（req close :326 / 正常收束 :410 / 断连边沿 clientGone :353）+ 30s 心跳；连接句柄幂等记账 :315-332；背压双闸 createSseWriter :108-134（1MB 字节 + 240 次滞留）；MAX_SSE_PER_BOOK=5；cancelStream 唤醒 + B-19；forgetSseCount :66-71 挂删书 |
| 2 | `src/events/store.ts` | openStores 引用计数——close() :922-938 归零才真关库 + 清 markerTimer :934 + 注销开口标记；打开期 B3 闸（catch 内 db.close() :612-617）；迁移段 rewriteSessionKeys / checkpoint 均 finally db.close()（:1011-1014、:1076-1078） |
| 3 | `src/fs/cross-process-lock.ts` | renewTimer unref :185 + release 清 :190 + utimes 失败自停 :181-183；release 幂等校验；fd finally closeSync :221-228 |
| 4 | `src/metrics/style.ts` | chapterBodyCache FIFO 4096（CHAPTER_BODY_CACHE_MAX :106）+ stat 指纹失效 + 文件消失自清 :118（→观察项 O-4） |
| 5 | 七个 API 缓存（state/progress/overview/knowledge/search/health/check.ts） | 全部 TTL（5s/30s）+ FIFO 上限 32 + forgetXxx 失效挂点接 books.ts 删书/改名 |
| 6 | `src/studio/server/api/documents.ts` | services Map :38 在 books.ts :378（删书）/ :598（改名）forgetService |
| 7 | `src/desktop/main.ts`（1,074 行全文） | registerIpc whenReady 一次性注册；窗口监听随窗闭包；崩溃自愈封顶 3 次 + 稳定窗互撤计时器 unref :132；E-9c 目录选择循环封顶 10 次 |
| 8 | `src/desktop/server-manager.ts`（685 行全文） | utilityProcess 重启路径 exit 监听随 child 消亡 :264-271；restartTimer 三面取消 + unref :296；stdio 转发监听随 child；splitLines 单行 1MB 内存闸 :555-561；killProcAwaitEscalating（TERM→2s→KILL，pid 重读防复用） |
| 9 | `src/log/index.ts`（377 行全文） | MAX_PENDING_WRITES=1024 丢最旧 :272-284；单泵串行；7 天轮转清理；stdout-only 短路；IR-9 泵内逐行盯目录幂等 mkdir :304-326 |
| 10 | `src/git/exec.ts`（335 行全文） | git() 15s 超时 + 64MB maxBuffer + windowsHide :101；gitAsync() killWithEscalation（TERM→2s→KILL，升级定时器 unref）:57-75；abort 监听 settle 即摘 :174；缓冲 64MB 截断后流继续排空 :213-221 |
| 11 | DatabaseSync 错误路径三处 | `src/ai/tools/status.ts`（第二段 try 有 finally db.close()）/ `src/cache/rebuild.ts` tryIncrementalRebuild（finally 包住全部早退 return）/ `src/ai/orchestrate/self-heal.ts`（:285 if (db) db.close()）——全部干净 |
| 12 | `src/desktop/font-cache.ts`（44 行全文） | 60s TTL + 在途合并 + 失败不缓存 |
| 13 | `src/desktop/preload.ts`（96 行全文） | 订阅 API（onNavigate/onMenuAction/onFullScreenChange）全返回退订函数（removeListener，:50-79）；showContextMenu pending once 摘旧 :87 |
| 14 | 长生命周期候选面摸图 | 12 类候选（§2.2）全量过筛，命中面即上述文件——无遗漏长生命周期文件 |

### 6.2 内存·前端（代理全量扫描 + 主评审亲验定谳）

- 扫描面：web-next Pinia stores（模块级状态全量）/ composables / 组件生命周期（onUnmounted 清理）/ EventSource / 定时器 / 集合增长六类候选面。
- 上报存疑 5 项 → 主评审逐条定谳：**1 项证伪闭**（preload 订阅退订在位，§6.1-13）；**1 项成立**（MP-1）；**3 项转观察项**（O-1/O-2/O-3）。

### 6.3 内存防线体系（亲验在位——分层结构本身是质量信号）

打开期句柄闸 B3 / 建库期 PRAGMA 并入大 try C4 / 传输面 stdio 单行 1MB 截断 D1 / 队列面日志待写 1024 丢最旧 D2 / 缓存三件套（TTL + FIFO 上限 + forget 挂点，§6.1-4/5/6）/ 进程面 kill 升级链（server-manager + git/exec 两处 TERM→2s→KILL）/ 连接面 SSE 背压双闸 + 每书上限 / 引用计数面 openStores 归零关库 / 监听面 IPC 一次性注册 + 窗口监听随窗闭包 + 订阅退订函数——**不是零散补丁，是分层防线**，且全部亲验在位。

### 6.4 平台适配（mac/win）

| # | 面 | 亲验要点 |
|---|---|---|
| 1 | 路径同一性 | win 大小写折叠三处锁键：samePath（user-data-path.ts :36-39，win32 双侧 toLowerCase）+ document/manifest.ts :243-251 + knowledge/manifest.ts :18-22；isLibraryDir/--book 直达等跨来源比较点收编 |
| 2 | 保留设备名 | 前后端双闸：isInvalidBookName 首段守卫（书名/卷名/章节名入口）+ 静态托管 404 守卫 |
| 3 | git 子进程 | 双入口 windowsHide（exec.ts :101 spawnSync + gitAsync spawn）——win 打包态无闪黑窗 |
| 4 | utilityProcess env | 大小写清除（server-manager）——PATH/path 双形态污染免疫 |
| 5 | 窗口 | WCO 窗控 overlay win-only（main.ts :377-386）+ setTitleBarOverlay 运行时配色白名单 + nativeTheme.themeSource dark 同步；autoHideMenuBar win-only :369；mac 走原生交通灯 |
| 6 | 字体 | win 中文字体栈 + 自绘 FontPicker（`bd9d9e1`：flex-shrink:0 修 46 项挤进 360px 被压扁——本轮基线内亲验） |
| 7 | 退出兜底 | session-end（win 注销/关机 flush）:582 + SIGINT/SIGBREAK :1020-1021 |
| 8 | userData | 三平台统一大写目录名（user-data-path.ts :21-28）——防 dev/打包大小写分裂（Linux 敏感）、win Roaming 对齐 Electron 默认 |
| 9 | 长路径 | Node/libuv 绝对路径 \\?\ 自动前缀 + long-path-smoke.test.ts（>260 字符）冒烟在位 + 披露「书库路径建议 <200 字符」（electron-builder 无 longPathAware 字段，记档维持） |
| 10 | 文件占用 | EPERM/EBUSY 退避族：renameWithRetry（3×50ms 指数退避）+ rmQuietly/rmWithRetryQuietly 清理原语（「编辑器持句柄」真形态对策） |
| 11 | 大小写改名 | 纯大小写文档改名 dev/ino 双闸（win 大小写不敏感 FS 误判「未变更」免疫）；.md 过滤大小写不敏感 |
| 12 | 文本编码 | CRLF 行尾归一（parseHistory / realms / lineDiff / deletedSegments）+ BOM 容忍（book.yaml 补丁族 / 账本推进归档判定 / firstKeyColon） |
| 13 | win 特有 IO | journal fsync 只读 fd 守卫（EPERM 免疫）；三审缓存目录原始 docId 冒号净化（win mkdir 非法字符） |
| 14 | 打包 | electron-builder.yml：mac dmg + win NSIS x64；图标 build/icon.png 512 单真相源（ico/icns 自动派生）；unsigned 披露 D4（SmartScreen「更多信息→仍要运行」/无自动更新覆盖安装指引在根 README「Windows 版使用须知」） |
| 15 | 路径安全 | safe-path：ESCAPE_SEGMENT_RE 双分隔符转义 + 双侧 realpath + 不存在目标的最近祖先锚点（Y-5）——分隔符漂移免疫 |

## 七、完成进度评估：**≈98%**（mac 生产形态 ≈98.5% / win RC ≈97%）

三重依据：

1. **任务台账**（总览第三节，任务状态唯一入口）：开放任务仅 2 项，且均非代码缺口——阶段 13 中转网关真机验证（待作者凭据，纯外部依赖，脚本与代码面全在树）；阶段 21 J6b 跨平台书库迁移挂账（产品级工具，非发布阻断）。
2. **功能承诺面**：根 README 承诺功能（建书/设定/正文/三审/定稿/伏笔/字数/文风/AI 改写分析/对话助手等 17 项）逐项有代码落点（上次独立重评已逐项实证；本轮增量 27 文件均为修复/适配，无功能面缩减，抽验在位）。
3. **发布面**：mac dmg 打包链在位（check:packaging 过）；win NSIS x64 + 2026-09-01 win 真机回归九门全绿（总览阶段 21 行已记）+ FontPicker 显示修复（bd9d9e1）已并入 dev。

分平台口径：mac ≈98.5%（余真机验证边际项与 J6b）；win RC ≈97%（余 unsigned 披露维持 D4、win 零 e2e 维持 D5、J6b 挂账）。与上轮独立重评（≈98%）一致——期间增量全部为修复/适配收口，无新功能面开张，无回退信号（门禁同口径 747/5265 全绿）。

## 八、完成质量评估：**A（9.0/10，专项口径）**

**加分**：

1. 聚焦域零实质发现——内存泄露（前后端）与平台适配两专项 P0/P1/P2 = 0；唯一 P3 为 KB 级卫生项（MP-1）。
2. 内存防线**成体系**（§6.3 十四类分层）而非零散补丁，且全部亲验在位。
3. 平台适配**双闸成网**（§6.4 七层：路径语义/文件占用/文本编码/子进程/窗口字体/退出信号/打包披露，前后端与主进程多层互备）。
4. 门禁全绿零回归信号（747/5265 与 IR 修复批终门同口径）。

**扣分**：

1. MP-1「删配置不清缓存」类卫生缺口虽小，属可避免项。
2. O-4 最坏驻留场景（超大书库）未做内存画像实测——设计权衡已登记，但缺数据锚点。
3. 执行形态打折——两路深评代理 1308 阵亡转主线程串行：亲读保底深度，但并行交叉复核密度低于全代理成活轮（§2.2 如实记档）。

**对比**：上轮独立重评 A−（8.8）→ 本轮 A（9.0）：期间 IR 修复批 9 项办结 + win 真机九门全绿 + FontPicker 修复并入；本轮两域专项零新增 P1/P2。口径注：本轮为两域专项分，非全量八域分。

## 九、开放任务与收口条件

- **开放任务**（非本轮范围，台账维持）：阶段 13 中转网关真机验证待作者凭据；阶段 21 J6b 跨平台书库迁移挂账。
- **收口条件**：报告完成 ≠ 收口——本轮 P1/P2 零发现，无阻断性修复义务；MP-1 办结（两行 delete，可随任意下批顺修）或作者拍板维持登记后，回填 §五 收口记方为已收口。
- **归档**：收口后按链规则归档；未收口不归档。

（报告完）
