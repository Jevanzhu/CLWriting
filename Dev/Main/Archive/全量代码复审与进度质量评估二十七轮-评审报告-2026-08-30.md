# 全量代码复审与进度质量评估二十七轮（评审报告）

> **头注记（确认闸报备）**：按 AGENTS.md 确认闸（2026-08-24 起），新增 .md 前须报作者确认目标路径。本报告路径 `Dev/Main/01-评审/全量代码复审与进度质量评估二十七轮-评审报告-2026-08-30.md` 已按链内命名规范拟定；本次会话作者持续指示「继续」且原始指令明确要求「形成文档」，未再单独收到路径确认回复——沿用二十二/二十六轮先例（规范落盘 + 本注记留痕报备）。如需改路径，移动时同步 README/总览。

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审方法与覆盖口径（从零，禁读二十六轮）

- **独立性铁律**：本轮为作者指令的**从 0 重新评审**——九个域评审员均为全新子代理，prompt 明令禁止读取 `Dev/Main/` 下任何 .md（含二十六轮报告与全部历史评审/设计文档）；主评审同样不复用二十六轮结论，只做亲验与汇总。
- 域划分：A `src/ai`（约 1.18 万行）/ B `src/check + src/format`（8310 行）/ C `src/document + src/fs + src/export + src/git + src/cache` / D `src/studio/server`（41 路由文件）/ E `src/studio/web-next` / F `src/desktop + src/rag + src/driver` / G `src/process + src/events`（26 文件约 6400 行）/ H `test/`（工程面）/ I `src/install + resources + scripts + 根配置`。
- 各域评审员对每条疑似先执行「注释认账排除」（引用认账原文再定），P1/P2 须逐字引用代码并给出触发时序/输入。执行中四域（B/C/H/I）首派撞 1302 限流，隔批重派成功——九域结论齐备。
- 主评审对 P1×1 + P2×9 全部亲验（逐条读源核对），零误报。〔验收时订正：初稿标头误写 P2×8/P3×49/共 58，按 §五域小节实列数（P2 九条、P3 五十条、共 60）订正，发现项本身无增删。〕

## 二、门禁实证（2026-08-30 12:32–12:50 亲跑，二十六轮修复后基线）

tsc/vue-tsc/eslint 0 错；vitest **555 文件 / 4404 单测全绿**；check:counts（README 同步 4404/555）、check:packaging、check:knowledge 通过；e2e 43 过 / 2 skipped（release smoke 门控不变）。

## 三、进度评估

**总体 ≈ 97.5%（核心 mac 腿 ≈ 98.5%）**。与二十六轮（≈97%）持平微升：本轮无新增「整功能缺失」类发现，60 项全部是既有功能上的边界/防御/一致性缺口——功能面在二十六轮后无回退。win 腿 J0-J5 合流后未见新问题（本轮 F 域跨平台分支复核通过）。

## 四、分域质量评分

| 域 | 评分 | 一句话 |
|---|---|---|
| A ai | 8.0 | 无 P1/P2；扣分在记账口径三线分叉与窄窗口防御 |
| B check/format | 8.0 | 红闸链两处静默漏报面（R27-20/24）+ 一处红项语义失真（R27-21） |
| C document/fs | 7.5 | **唯一 P1**（清单读失败→破坏性重写）+ 自家纪律未覆盖的同族写点 |
| D server | 9.0 | 唯一 P2 是双进程新形态下旧假设失效；防线密度全域最高 |
| E web-next | 8.5 | 1 P2（聊天失败草稿销毁）+ 新功能面未继承既有惯例 |
| F desktop/rag | 8.0 | R26-87 不变式在第三条 kill 路径漏应用；边界防御缺口 |
| G process/events | 8.5 | 3 P3：锁常数耦合、无断言不变量、过期材料静默注入 |
| H test | 8.0 | self-heal 一族资源卫生掉队；断言风格全域罕见整齐 |
| I install/资源 | 7.5 | v2/v3 细纲乒乓（每次启动双 rename）+ 四处静默/收编遗漏 |

**综合 8.1 / 10**（二十六轮 8.4 口径下同类横比略低，主因 C 域 P1 与本轮更深的并发/双进程视角；发现总量 111→60、P1+P2 20→10，趋势向好）。

## 五、发现清单（60 项：P1×1 / P2×9 / P3×50）

### P1

- **R27-40 清单/trash 清单「读失败→空清单→整文件重写」**：`readManifest`（manifest.ts:51-55）catch 返回空 entries 与 ENOENT 不可区分；doTrash 删条目（service.ts:1191-1195）、finalize 补建基线（finalize.ts:142-155）、upsertManifestEntry、restore 回写、readTrashManifest+writeTrashManifest 同族。持锁写中 readFileSync 撞 EBUSY/EACCES/EIO（win 杀毒/索引器/网盘）→ 空表原子替换整文件 → 全书 docId 登记/定稿基线/回收站条目一次性丢失 → 防覆盖闸失守。读侧 M-13 有哨兵，**写侧零守卫无认账**。【主评审亲验属实】

### P2（9，全部主评审亲验属实）

- **R27-20 iron-rules 禁词段内 `###` 子标题截断采词**：extractSection（iron-rules.ts:183-185）`inSection` 后遇任意 `#{1,6}` 标题即 break——`## 硬禁词` 下用 `### 网文套话` 分组的禁词全不进 bannedWords；因段内已采到词，R73-15 失明黄项也不触发，双重静默，红闸漏报。
- **R27-21 lead-verb-invalid 黄项文案与 checkStatusClosure 行为矛盾**：闭合校验用**跨类并集**（RESOLVE_VERBS/DROP_VERBS 全类型 flatMap，leads.ts:333-334），悬念线末动词「突破」（成长线 resolve）同时产「词表外……状态闭合校验对其不生效」黄项 **和** lead-status-open **红项**——R26-31 宣称的「词表外只黄不红」未实现。
- **R27-41 finalize pinned 定稿版本对非 UTF-8 有损留底**：finalize.ts:105 `fileBytes.toString('utf-8')` 后 writeVersion——GBK 源落 U+FFFD 失真副本进全系统唯一 pinned 永久版本；R26-52 已为同族修出 Buffer 直存（version.ts 已支持），此处漏改。
- **R27-42 书内 symlink 跳板绕过内部簿记防线**：save/updateChapterMeta/updateDocMeta 能力判定用**词法** relPath（service.ts:215），落写用 **realpath**（safe-path.ts:54 返回 abs=real）——书内 `设定/x.md` 实为指向 `项目/book.yaml` 的 symlink 时，设定写能力放行、原子写命中系统文件。resolveWithinRoot 只挡越根。
- **R27-60 三审启动清扫不查跨进程任务闸**：sweepStaleReviewDirs（review.ts:90-92 无条件执行、387-407 无差别 rmSync review-*）在 T2-4/R75-5 明确支持的双进程形态下会删掉他进程在途三审的 out_dir（分钟级任务白烧费用、信封降级 incomplete）。
- **R27-75 失败草稿「先删后查」**：useChatComposer.ts:37-42 `failedDrafts.delete(book)` 在判空前执行——stash 存在即无条件销毁，回填却以输入框空为前提；「切书后回来」（最常见找回场景）反而永久删稿，违背 R66-33 功能自身语义。
- **R27-90 握手超时 kill 后不等退出、不升级 SIGKILL**：server-manager.ts:616-623 裸 `proc.kill()` fire-and-forget——R26-87 已确认「SIGTERM 被吞」现实并在 stopChild/shutdown 补齐 killAwaitEscalating，唯握手超时路径漏应用；child 卡死占住端口 → 自动重启 EADDRINUSE 循环 / 首启 app.quit 后孤儿残留。
- **R27-120 self-heal 测试族无条件泄漏临时书仓库**：fixtures.ts:20 裸 mkdtempSync 不追踪；self-heal.test.ts / self-heal-f2.test.ts / w1-acceptance.test.ts 全文件 **0 个 rmSync**（self-heal-chain 只清 ud）——每次全量跑约 31 个含 .git 双书仓库沉积 $TMPDIR，成功路径也漏。
- **R27-130 v2/v3 迁移对 工作区/细纲.md 每次启动「搬进又搬回」**：v2 moveDrafts（migrate-layout-v2.ts:183）仍认领 细纲.md 搬去 写作/草稿/，v3 再搬回 工作区/，而运行时（outline.ts:84-85）永久写在 工作区/——每次启动对活跃文件两次 rename + 建删目录，同步盘用户持续变更风暴（迁移计数也虚增）。

### P3（50）

**A 域（7）**：R27-1 llm/call durationMs 混入记账 IO 耗时（recordUsageSafe 在 trace 前执行，含最长 5s 锁阻塞）；R27-2 anthropic usage「首见即定」与 openai 线 R26-3「末见」口径分叉且 R26-3 注释对 Anthropic 描述与实现相反；R27-3 chat_done 用量单 attempt 口径（self-heal 已改 attemptsUsage，chat 漏）；R27-4 generateTool 截断判据漏接「tool 在场但 input 空 {}」（anthropic content_block_stop 空 jsonBuf → {} 无 _raw）；R27-5 providers 明文 bak 覆写读回可落排队窗口读到旧密文，「下次 save 自然覆盖」无保证；R27-6 HTTP 408 不进可重试族（终态化 author 停机）；R27-7 退避 sleep 中断时同一 attempt 双 llm/call 行（审计计数失真，usage 均 null）。

**B 域（8）**：R27-22 isRealmBoundary 缺直角/弯引号（「筑基」形态证据提取失败）；R27-23 adjStackRegex 指数回退（maxAdjStack 无上界 + 长「的」串实测 2862ms）；R27-24 book.yaml 段头直挂块列表被静默吞进 value（leads.enabled 写法无声失效零警告）；R27-25 checkSectionCount 计代码围栏内 `##`；R27-26 parseFlat 同名键静默后胜（book.yaml 侧已 fail-loud，章 fm 无同款）；R27-27 readIronRules 缓存浅拷贝漏拷 unparsedBannedEntries（与注释承诺不符）；R27-28 imagery-seed 注释「≥3」与实际 `>3` 差一；R27-29 splitSentences 不切半角 `!?`（混排时句长虚高）。

**C 域（7）**：R27-43 executeSave 前段在契约 try 外（withManifestLock 超时/EISDIR/EIO 裸穿，R73-33 注释宣称不实）；R27-44 appendSettled 文件落盘后失败误报 WRITE_ERROR（R75-4 同型漏点：journal 悬置 pending + 编辑器误报）；R27-45 updateChapterMeta 三次独立读盘 + 619 行无守卫（R73-40 单读同源未覆盖）；R27-46 doTrash 基线读失败静默降级「从未定稿」（还原后防覆盖闸失守，至少须 warn）；R27-47 回收站往返丢 tags/order（TrashEntry 不携带，删→还原不可逆清零）；R27-48 listTrackedDocs readdirSync 无守卫（违背「旁路证据绝不阻断」自我定位）；R27-49 restore 前缀校验不排除 trash-manifest 自身（篡改条目可搬走清单本体）。

**D 域（4）**：R27-61 draft-save 无乐观锁无编排互斥（self-heal 在途时同章覆盖，后写赢；有留底故 P3）；R27-62 导出 FIFO 等待队列无界无超时无取消（队首挂死扩散全局导出不可用）；R27-63 learn-commit 无闸无幂等（双击双份条目）；R27-64 chat/confirm 缺 resolveBook（同族唯一漏挂，404 语义误导排障）。

**E 域（4）**：R27-76 ChatDock 输入框跨书残留（无 :key，A 书文本可直接误发进 B 书；:43 注释已与实现漂移）；R27-77 wb.warning 唯一消费面在 WorkbenchView（视图未挂载期间警告静默丢失，截断类提示失效）；R27-78 ReviewPanel 仅 watch docId（进书竞态下滞留「不可审阅」误报，CC-P1-4 同型漏配）；R27-79 删书不清 onboard-premise/tree-first-open 两 localStorage 键（同名重建书继承已删书梗概）。

**F 域（7）**：R27-91 渲染崩溃稳定窗口计时器不互撤（慢节奏崩溃循环永不触顶，server-manager 同型有身份校验）；R27-92 cc registerCtrl dispose 后复活 sessionCtrls 条目（真实请求控制权丢失 + Map 泄漏）；R27-93 embed 批内维度不校验（混维死行静默入库占索引）；R27-94 章指纹含 fmRaw（仅改 frontmatter 触发整章重嵌白烧费用）；R27-95 右键菜单净化总量无上限（200×5 指数积可阻塞主进程）；R27-96 relaunch 与单实例锁释放竞态（切书库后可能无实例存活）；R27-97 second-instance --book 解析回落首实例 env（普通二次拉起被意外导航）。

**G 域（3）**：R27-105 摘要锁持锁面覆盖全长 AI 调用且无续期，与 stale 门槛/任务默认超时同为 10 分钟——长调用窗口互斥被静默击穿（双生成双计费）；R27-106 foldSurface 存档插入位序按数组位序而非 seq（正确性依赖无断言的生产不变量）；R27-107 卷摘要「已过期但链不全」交集路径静默放弃 + 备料无陈旧闸（可证明过期的材料注入 prompt）。

**H 域（4）**：R27-121 userDataPath 硬编码 `/tmp/clwriting-test` 固定共享路径（脱离沙箱永不清算，同族两种口径并存）；R27-122 playwright.config 注释宣称的 E2E_SPEC_ORDER_SNAPSHOT 守卫全仓库不存在（幻影防线）；R27-123 R71-2 复检测试无「首检已过」时序证明（慢派发下静默退化首检臂，弱臂假覆盖）；R27-124 e2e global-setup EADDRINUSE 失败路径泄漏 workDir。

**I 域（6）**：R27-131 knowledge:update 空汇总仍落草稿文件但宣称「未产草稿」（占位文件静默堆积）；R27-132 migratePromptOverlays 未用 readOverlaySync（单 overlay 读异常中断整轮升级）；R27-133 v3 回收站分支不清主清单旧条目（悬挂 entry + 还原落点在退役目录）；R27-134 check-counts 不认 Playwright 裸 `test.skip()` 零参形态；R27-135 check-knowledge 反向扫描只认 .md（非 .md 资产两向不可见）；R27-136 v2 moveDrafts 同名跳过纯静默（同文件 moveTree 已按 R65-38① 记 errors）。

## 六、跨域观察

1. **双进程形态是本轮新视角**：R27-60/R27-105/R27-2 类问题在单进程假设下不可见——T2-4 开放双进程后，历史代码的「启动时无人持锁」「10 分钟够用」假设需要系统性复查。
2. **自家纪律的「同族漏点」模式**：R27-41（R26-52 漏 finalize）、R27-44（R75-4 漏 journal settled）、R27-45（R73-40 漏 updateChapterMeta）、R27-90（R26-87 漏握手路径）、R27-136（R65-38 漏 moveDrafts）——修复纪律建立后未做全域同型扫描，是最大的缺陷来源。
3. 测试工程整体质量高（判别联合断言风格、容差注释、golden 门），但 self-heal 一族是资源卫生洼地。

## 七、登记维持项（修复时逐项复核，属实则修）

- R27-5（明文 bak 排队窗口——评估同步直写改造面，过大则登记）
- R27-7（退避中断双 trace——「先落库后等待」纪律有意为之，改序会丢失败记录；登记或以不改语义的方式收口）
- R27-123（测试弱臂——补观测锚或登记）
- R27-94 若实施（指纹语义变更）将触发存量库一轮全量重嵌——自愈链可承接，修复时在案说明。

## 八、修复计划（批 A-I，作者指令「全部修复」已预授权）

| 批 | 范围 | 项 |
|---|---|---|
| A | document/fs（最高优先） | R27-40（P1）+ 41/42（P2）+ 43~49 |
| B | check/format | R27-20/21（P2）+ 22~29 |
| C | server api | R27-60（P2）+ 61~64 |
| D | web-next | R27-75（P2）+ 76~79 |
| E | desktop/rag/driver | R27-90（P2）+ 91~97 |
| F | ai | R27-1~7 |
| G | process/events | R27-105~107 |
| H | test 工程 | R27-120（P2）+ 121~124 |
| I | install/资源/脚本 | R27-130（P2）+ 131~136 |

终门：八件套全绿 + README 计数同步 + 本报告补验收记录；两轮报告一并同步 README 计数与总览文档地图。

## 九、修复收口记录（2026-08-30 验收）

**结果：60 项全办结——修 58，登记维持 2（R27-5 / R27-7，理由 §七 复核在案）。** 批 A-I 全部执行完毕；P1×1（R27-40）与 P2×9 全修，P3×50 = 修 48 + 维持 2。

**执行形态与代理事故记档**：
- 批 A/B/F/H（亲修主体）+ 批 E 终段：主评审亲笔。
- 批 D（web-next）：代理首发撞账户并发上限阵亡，重发成活，五条全修+变异验证（useChatComposer 失败草稿改「判空后删」、WorkspaceShell :key=bookName + wb.warning watch 上移、ReviewPanel watch [docId, node]、useShelf 删书清两 localStorage 键）。
- 批 G/I：代理成活（summary 锁续租 30s+测试注入、projection seq 锚、volumeSummaryProvablyStale 门；moveDrafts 窄化 `^草稿-\d+\.md$`+manifest 映射、knowledge-update 注入化、overlay per-file try、v3 trashPaths removeEntry、check-counts skipPlain、check-knowledge `_manifest.json` 豁免）。
- **批 C/E/F 三路代理静默失败**：`Model provider is not configured: builtin:bigmodel-start-plan`（作者报「任务卡死」后核查发现）——grep 确认三域零半成品改动（无 R27 标记残留，在树 diff 均为二十六轮未提交旧账）→ 主评审亲修补完，未重派。

**分批核销要点**（全部带 `// R27-xx（二十七轮）：根因→语义` 注释）：
- **批 A**（10/10 修）：R27-40 P1 写侧守卫——readManifest 读失败哨兵贯通 doTrash/finalize/upsert/restore/trash 全族写点，读失败不再整文件重写；R27-41 finalize pinned Buffer 直存（同族 R26-52 漏改点）；R27-42 symlink 能力判定改 realpath 口径。
- **批 B**（10/10 修）：R27-20 `###` 子标题不再截断采词；R27-21 lead-verb-invalid 黄项与并集校验口径归一；R27-22 边界字符类补全；R27-23 maxAdjStack 钳 0~20；R27-24 book.yaml 段头直挂块列表 warn；R27-25 代码围栏剥离后才数 `##` 标题；R27-26 frontmatter 重复键 warn；R27-27 **连带深修**——测试暴露 miss 路径返回缓存对象本体（首调方 mutate 污染缓存），cloneIronRules 双路径回拷贝；R27-28 比喻注释密度 >3；R27-29 句界含换行。+7 新测（r27-batch-b），R27-27 过变异验证（revert → 红）。
- **批 C**（5/5 修，代理阵亡亲修补完）：R27-60 sweep 查跨进程任务闸；R27-61 保存草稿撞 self-heal 在跑 409 BUSY；R27-62 导出等待者上限 8+超时 10min（ExportSlotWaitError → 503）；R27-63 收割样章内容指纹去重（sha256+同批去重，命中返回既有路径）；R27-64 chat/confirm 先 resolveBook 后读 body。
- **批 D**（5/5 修，代理）：见上。
- **批 E**（8/8 修，亲修）：R27-90 killAwaitEscalating 主体抽模块级 killProcAwaitEscalating，握手超时第三条 kill 路径同纪律（等退出+SIGKILL 升级后再收口启动失败，server-manager 41 测全绿）；R27-91 渲染崩溃稳定窗互撤（崩溃撤销在途复位计时器+重载撤旧排新，慢周期崩溃循环可累计触顶）；R27-92 registerCtrl 对 closed 会话拒收并 abort（不复活 sessionCtrls 条目、在途请求即停）；R27-93 embed 批内条数/维度校验（基准维逐行比对，混维批按失败续传零入库）；R27-94 章指纹去 fmRaw 只摘 body（**存量成本记档**：旧指纹全失配，升级后首轮 buildIndex 全量重嵌一次，自愈续传承接——§七 已预告）；R27-95 右键菜单跨层扁平项预算 200（每层独立上限的指数积封死；首版扣减时序错误被新增回归测试当场抓获——嵌套各自吃满预算得 1000 项，扣减挪到下钻前修复）；R27-96 relaunch 显式 releaseSingleInstanceLock（消除新实例扑空自 quit 的死局缝隙）；R27-97 second-instance 只认 argv（initialBookArgvOnly，无参双开不再被首实例 env 误导航）。回归测试 +4（r27-batch-e 2 例 + context-menu 1 + initial-book 1）。
- **批 F**（7 项：修 5 + 维持 2）：R27-1 durationMs 扣除记账 IO（busy-wait 80ms 实测 durationMs<50 锚定）；R27-2 anthropic usage 末见 wins（与 openai 线口径归一，adapter.test R73-13b 锚更新 9/9+幂等不变）；R27-3 chat_done 用 attemptsUsage 终兜底（**无专用测试记档**：retryable 失败不带 usage，无可达行为差异态，tsc+chat 套件覆盖）；R27-4 generateTool 判据补「input 空 {}」臂；R27-6 HTTP 408 入可重试族；R27-2 连带订正 openai-adapter 注释（旧文对 Anthropic 线描述与实现相反）；R27-5/7 登记维持（bak 原子写保证完整态读+同步写阻塞 UI 代价 / 「先落库后等待」纪律内蕴，§七 理由在案）。+7 新测（r27-batch-f）。
- **批 G**（3/3 修，代理）：见上。
- **批 H**（5/5 修）：R27-120 self-heal 测试族 trackTempDir 收编（三文件+fixtures 双轨工作目录，每测后清理）；R27-121 `/tmp/clwriting-test` 字面量清退（仅存注释 3 处）；R27-122 e2e 首检前移守卫（代理）；R27-123 r71 二次复查测试加早响应探针（慢分发退化到首检臂时测试显式红，弱臂锚）；R27-124 e2e 失败清理（代理）。
- **批 I**（7/7 修，代理）：见上。**存量观察记档**：曾跑过旧 v2+v3 迁移的书可能残留 `写作/草稿/细纲.md` 的 manifest 挂账条目（文件已搬回 工作区/ 而条目未清）——运行时以 工作区/ 为准无功能影响，属存量数据卫生项不在本轮代码修复面。

**测试增量**：555/4404 → **567 文件 / 4467 单测**（净 +12 文件 / +63 用例；r27-batch-b/c/e/f 四个新回归文件 + 各批既有文件扩测）。

**终门八件套**（2026-08-30 亲跑）：tsc / vue-tsc / eslint 0 错；vitest 全量 **567 文件 / 4467 单测全绿**（与 check:counts 实测一致；首轮全量中 R27-96 暴露一处测试桩缺口——main.test.ts app 桩缺 releaseSingleInstanceLock 致 1 条 Uncaught Exception 噪音〔未红任何用例〕，补桩后 desktop 11 文件 154 测全绿+tsc 复核 0 错）；；check:counts（README 计数同步 4467/567）、check:packaging、check:knowledge 过；e2e 43 过 / 2 skipped（release smoke 门控不变）。工作树未提交待作者指令。
