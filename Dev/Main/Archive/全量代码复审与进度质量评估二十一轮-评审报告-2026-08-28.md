# 全量代码复审与进度质量评估二十一轮-评审报告（链第七十三轮）

- **日期**：2026-08-28
- **基线**：HEAD `62a7075` 干净树（七十二轮修复批收口提交 `001e05a` 之后，另含两笔 CI 触发策略收窄提交 `47572ce`/`62a7075`，均纳入本轮评审面）
- **作者指令**：「忽略现有的评审文档，重新评审一遍项目所有代码，告诉我项目完成进度，完成质量，最后形成一个文档给我。记得编排下任务。」——**本轮不含修复**，修复计划见 §九 待作者指令。
- **确认闸记档**：报告落盘路径 `Dev/Main/01-评审/全量代码复审与进度质量评估二十一轮-评审报告-2026-08-28.md` 经作者确认获批（2026-08-28，作者选「规范路径落盘」）。
- **评审组织**：TodoWrite 全程编排 + 六路分域**禁读子代理**从零深审（A/B/C/D/E 首发成活；**F 路首发撞账户速率限制 1302 阵亡，错峰重发成活**，记档）。子代理全程禁读 `Dev/` 与 `.zcode/`（防历史结论锚定）；主评审未读任何历史评审报告正本，仅读总览/README 作为任务状态与进度对照基准。

---

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、结论摘要

| 维度 | 结论 |
|---|---|
| **完成进度** | **整体 ≈95%（核心写作主链 ≈97%）**。产品声明的全部功能面（建书/设定/写章/机检/三审/定稿/导出/伏笔/文风/对话助手/版本快照/事件审计）均有真实实现且门禁全绿；未收口项集中在 win 发布链（阶段 21 J0/J5/J6）、中转网关真机验证（阶段 13，外部依赖）与少量已登记敞口（§7.2） |
| **完成质量** | **8.8 / 10**（分域 A9.0 / B8.5 / C9.0 / D9.0 / E8.5 / F8.5，§八） |
| **发现** | **P1 ×0 / P2 ×12 / P3 ×71**；12 条 P2 全部经主评审亲验属实（9 条亲读源码/实测复现、3 条亲验 grep 证据），P3 逐条有 file:line 证据 |
| **门禁实测** | **七件套全绿**：502 文件/4066 单测全过 + tsc/vue-tsc 0 错 + ESLint 0 错 + check:counts/packaging/knowledge 三 check 过；**e2e 42 过 2 跳**（与根 README 声称逐项一致，零漂移） |
| **修复计划** | §九 批 A-E，**待作者指令** |

---

## 二、评审范围与覆盖面

| 域 | 范围 | 规模 | 覆盖方式 |
|---|---|---|---|
| A | AI 链路：src/ai（含 provider/prompts/tools/rules/contract/orchestrate/tasks）+ driver + rag + knowledge + learn | 88 文件 ≈1.39 万行 | **88/88 全文通读** |
| B | 机检与格式：src/check + format + metrics + review | 42 文件 ≈0.96 万行 | **42/42 全文通读**（关键正则 node 实测复现） |
| C | 核心数据服务：document + events + fs + cache + state + install + git + export + process + log | 70 文件 ≈1.57 万行 | **70/70 全文通读** |
| D | 服务端与桌面：studio/server + desktop + scripts + 工程配置（package.json/tsup/electron-builder/vitest/playwright/eslint/tsconfig/CI×2/.gitattributes 等 12 件） | ≈70 文件 ≈1.3 万行+配置 | **全量通读**（server 46/46、desktop 12/12、scripts 11/11、配置 12/12） |
| E | 前端：src/studio/web-next/src | 180 文件 ≈3.3 万行 | 逻辑层（api/stores/composables/shared/types/editor/views/pages）全量 + 高风险组件深读，共 **146 文件实读**，其余组件扫读找模式性缺陷 |
| F | 测试工程：test/ 全域 + vitest/playwright 配置 + coverage 工件 | 539 文件 ≈8.8 万行 | 全量盘点扫描（src↔test 逐文件对照 + 反模式全量 grep）+ **32 文件深读**（19 全文 + 13 结构性） |

合计：src ≈440 文件 ≈5.2 万行（不含前端）+ 前端 ≈3.3 万行 + test ≈8.8 万行 + scripts ≈0.15 万行 ≈ **990 文件 / 17.3 万行**。

---

## 三、门禁实测（主评审亲跑，2026-08-28）

| 门 | 结果 |
|---|---|
| `npm test` | **502 文件 / 4066 单测全过**（101s） |
| `tsc --noEmit` | 0 错 |
| `vue-tsc --noEmit` | 0 错 |
| `eslint .` | 0 错 |
| `check:counts` | 过——实测 502/4066 + 29 e2e spec/44 用例，与根 README 声称一致 |
| `check:packaging` | 过——resources/ 入打包清单，prompt 版本表对账一致 |
| `check:knowledge` | 过——知识层 13 条 manifest 条目与磁盘一致，反向扫描无未登记 |
| `npm run test:e2e` | **42 过 / 2 跳**（2 跳为发布 smoke，需 `CLWRITING_E2E_RELEASE`；27.5s） |

覆盖率注：`coverage/coverage-summary.json` 现为 **api 桶局部跑产物**（api 层 lines 89.32% / branches 95.83%，对 vitest.config 五桶门 87/93 有余），**非全书覆盖率**，不可作整体覆盖率解读（F 域 P3-2 登记该工件易误读问题）。

---

## 四、分域评审结果

### 4.1 A 域：AI 链路 —— 9.0/10（完成度 ≈95%）

三协议适配（Anthropic / OpenAI Chat / Responses）、runTask 编排五路径记账、预算三指标闸、重试/Retry-After/降级链、中断归因、chat 消毒/压缩/回滚、自愈闭环、RAG 增量指纹、知识登记、learn 收割全部有真实实现与失败路径处理。全域零 TODO/stub；秘钥脱敏三层正则 + 拒绝文案永不回显 key；ai-calls.json 串行写队列+跨进程锁+损坏保守阻断；providers.json 原子写+bak 恢复链+0600；RAG 块/游标/指纹同事务。**P2×2 / P3×11**。

### 4.2 B 域：机检与格式 —— 8.5/10（完成度 ≈90-92%）

机检红/黄链路、两端闭合、成长线语义、树红点增量缓存（纪元输入覆盖 checkWithDb 全部外部输入，写前复核、降级 fail-open 留痕）、三审契约（draft_hash 一致性、空证据硬闸、失败注入阻断防假通过）方向全部正确；字数口径全库单源（countWords 码点口径）核实无漂移。扣分集中在零 token 启发式的残留误报面与禁词解析静默漏报。**P2×3 / P3×15**。

### 4.3 C 域：核心数据服务 —— 9.0/10（完成度 ≈95%）

原子写（tmp+fsync+rename+目录 fsync）、跨进程锁（pid 存活探测+stale 接管+续期+释放前 payload 校验）、journal pending 全文快照、revision 字节指纹校验、事件库 WAL/迁移墓碑/逆序回滚全部真实落地，约七十轮修复迭代证据注释抽验与实现相符。扣分主因：AI 草稿双写面游离于保存协议之外、清单锁超时降级裸写两处「可用性优先」取舍。**P2×2 / P3×15**。

### 4.4 D 域：服务端与桌面 —— 9.0/10（完成度 ≈93%）

安全三层（Host/Origin/token）经对抗性核验无绕过：Host 校验 listening 前全 403、写请求 Origin 403+token 双闸、GET 豁免仅 `/api/boot` 与一次性 ticket 的 book stream、token 经 env 注入不经 argv 且日志面 urlPathOnly 防泄漏、静态资源 resolveWithinRoot 双侧 realpath。任务闸互斥矩阵、删书前六步排空、优雅停机 3s<3.5s 时序成立；CI/tag 门集合对齐、含打包态 Electron 真启动冒烟。唯一实质缺陷：删书落盘非原子。**P2×1 / P3×12**。

### 4.5 E 域：前端 —— 8.5/10（声明功能完成度 ≈100%）

竞态防护密度高：几乎每处 async 有代守卫+书名捕获+await 后复检+loading 复位；IME 组合守卫单源化 `shared/ime.ts` 覆盖全部 Enter 提交面；编辑器乐观锁冲突/外部替换/切文档清栈/卸载冲排闭环。全库无 v-html 注入面、无吞错永久 loading。扣分：双击重复提交这一已被项目自认并修过的缺陷类，仍漏三个表单入口。**P2×1（三处同族）/ P3×5**。

### 4.6 F 域：测试工程 —— 8.5/10

断言纪律同级罕见：真实夹具+真实 SQLite/文件系统为主、mock 一律落依赖边界、近 4000 用例零快照滥用、零无条件 skip（57 处条件 skipIf 均有平台守卫）、假定时器全部恢复；元门禁把「测试自身漂移」也纳入测试（coverage glob 双向锁、GET 路由源码穷举网、spec 顺序快照门、.only/对账门）。src 21 个顶层模块全部有对应测试目录，文件级零直测仅 2 个逻辑文件+1 个平凡函数。扣分：两个刚修过真实缺陷的 web-next helper 零直测、e2e 共享 workDir 契约迫使断言弱化、临时目录兜底 helper 采纳率 19/266。**P2×3 / P3×13**。

---

## 五、P2 发现清单（12 条，全部主评审亲验属实）

| # | 域 | 位置 | 问题 | 后果 | 亲验方式 |
|---|---|---|---|---|---|
| A-1 | A | `src/ai/provider/openai-adapter.ts:406`、`anthropic-adapter.ts:304` | 网关吞 usage（include_usage 不兼容/message_delta 无 usage）时按 **0/0 用量入账**（注释自认「0 成本是可得最优估计」的取舍） | 该类端点下预算闸 tokens/cost 两指标永不生效，成本报表系统性偏低（真实扣费已发生）；calls 计数仍闸得住 | 亲读双适配器源码 |
| A-2 | A | `src/ai/provider/store.ts:240-241` | providers.json 读路径内嵌迁移写回 + 降级持久化 load→save，**读-改-写无串行化** | 毫秒级窗口内与设置页保存并发时，旧快照后写覆盖先写，用户刚保存的供应商配置/Key 静默回滚；窗口窄但属数据丢失类。同库 ai/calls.ts 已有 serializedWrite 范式可复用 | 亲读源码 |
| B-1 | B | `src/check/count.ts:615`（SIMILE_RE）+ `runner.ts:282-297`（STRICT_SHORT_CHECK_IDS 含 simile-density） | 词内「像」未排除名词（图像/偶像/摄像/录像/影像/照相）——node 实测「他用图像处理软件处理图像数据。」命中 2 次、「摄像头对准了门口」命中 1 次 | 悬疑/科幻/娱乐圈题材密度虚高报黄；短篇 `short.strict` 或 strictShort 时升红 → 自愈打回重写循环烧调用 | **实测复现** |
| B-2 | B | `src/format/style-entry.ts:239-242` 对照 `iron-rules.ts:120-132` | 条目库禁词逐行取词仅按 `\n` 拆分，单行多词（顿号/逗号分隔，常见排版）整行当一个词 | `body.includes(整行)` 永不命中 → 禁词红闸静默全量漏报，且无「该条目未解析出词」提示；与铁律侧 parseBannedWordsLine 双口径 | 亲读双源码 |
| B-3 | B | `src/format/chapters.ts:64-66` | 章 fm 三个必填枚举（钩子类型/钩子强弱/情绪定位）缺失时静默补默认值，`validateEnums` 对默认值恒通过 | AI 产出/手改漏字段 → 机检 0 红照跑，与 `draft.ts:40`「至少包含」的错误文案相悖；节奏页统计拿到假默认值 | 亲读源码 |
| C-1 | C | `src/process/draft-pipeline.ts:75-117`（/draft-save 端点、chat 改写工具、self-heal 三路共用） | AI 草稿落盘 **不取保存锁、不写 journal、不做 revision 校验、不记字数日记、新文件不登记 manifest**，与编辑器 `executeSave` 的保存协议（per-doc 锁+指纹+journal）分裂 | 作者保存与 AI 落盘近同时（双进程场景），编辑器内容落在「留底读盘」与「atomicWriteFile」毫秒窗内被覆盖且快照/journal 双双无痕；AI 字数不入字数日记。非竞态场景有 snapshotBeforeOverwrite 兜底故 P2 | 亲读源码与调用点 |
| C-2 | C | `src/document/manifest.ts:181,201-223` | 清单锁超时（5s，大书+磁盘抖动现实可达）**降级无锁裸写**仅 log.warn | 双进程同时降级时后写整文件覆盖先写——finalizedRevision/清单条目丢行，定稿基线丢失。注释自认对齐 journal 先例的取舍（「清单自愈型」），但 finalizedRevision 非自愈面，与保存锁「超时拒绝不降级」纪律相悖 | 亲读源码 |
| D-1 | D | `src/studio/server/api/books.ts:268-275` | 删书 `rmSync(recursive, force)` 中途抛错（占用/权限/磁盘）时目录已半删，`removeBookEntry` 未执行 | 书架残留指向半删书的登记（不可逆数据损毁+登记悬空）；启动 repair 只兜底整目录缺失。前序六步排空完整，唯最后落盘非原子 | 亲读源码 |
| E-1 | E | `AiProviderEditor.vue:246`、`RagProviderEditor.vue:60`、`StyleEntryPanel.vue:118`（+`AiServicePanel.vue:121` save 无在途锁） | 提供方/RAG 提供方/文风条目三处保存按钮无 `:disabled`、无在途布尔锁 | 请求在途窗口双击：新增卡双 POST 落两条同名记录；编辑卡第二笔以陈旧 revision 409 弹误导性「并发冲突」。与项目已修过的 createBook 双击同类 | 亲读三组件源码 |
| F-1 | F | `src/studio/web-next/src/stores/sse-guards.ts`（65 行） | SSE 事件运行时 type guard（为防后端事件结构变化前端静默失败而收口）**零直测** | 守卫被放宽不会有任何红灯，恰好复活其要防的故障模式；仅经 store 测试间接触达 | grep test/ 零命中 |
| F-2 | F | `src/studio/web-next/src/shared/numeric-input.ts`（15 行） | 刚修过真实缺陷（空串被 clamp 成下限值「清空反写入 1」）的共享解析 helper **零直测**，五个设置组件统一消费 | helper 退化即五处回归且无门 | grep test/ 零命中 |
| F-3 | F | `test/e2e/usage-card.spec.ts:15-18` + `edit-save.spec.ts:21-31` | 共享 workDir 顺序契约下，edit-save 的 afterAll 只恢复章正文字节、不恢复派生状态（字数 journal/state db/树缓存），下游 spec 只能写「空态或表格两态都合法」类弱断言 | 用量记录、全书字数口径的整体回归在 e2e 层不可见，只剩单测守。属已披露设计代价，但实质收窄 e2e 断言面 | 亲读两 spec |

> P2 严重级口径：P1=数据丢失/损坏/安全漏洞（本轮六域均未发现）；P2=用户可实际踩到的功能缺陷/窄窗数据丢失。A-1/C-2 两条带注释认定的设计取舍，修复批内由作者裁定修法或登记维持（§九）。

---

## 六、P3 发现清单（71 条）

### A 域（11 条）
1. `anthropic-adapter.ts:67` unknown 家族模型走 Anthropic 协议时 max_tokens 兜底 8192，写长章（6 千字+）必截断且 MAX_TOKENS 终态不可重试；规避=模型行显式配 maxOutputTokens。
2. `knowledge/update.ts:159` 手编 manifest 缺 `entries` 字段时 TypeError 裸崩，应复用 validateKnowledgeManifest 口径报「manifest 非法」。
3. `rag/index.ts:375` commitIndexBatch 任一 embed 批失败整体失败，已成功批向量全弃、重试全量重 embed（重复计费）。
4. `provider/redact.ts` 裸 key 正则未覆盖智谱 `id.secret` 与 Gemini `AIza…` 形态（纵深防御缺口）。
5. `provider/vault-key.ts` vault 密钥源内 XOR 分片合成，属注释已认的混淆级防护，建议用户文档声明威胁模型边界。
6. `ai/calls.ts:308-330` 预算检查无锁读、检查与记账间 TOCTOU（注释已认，并发两路可同时过闸略超限）。
7. `driver/cc.ts:134-144`+`mock.ts:93-103` 队列首次溢出实际丢 2 个事件（设计内），notice 文案未反映数量。
8. `orchestrate/self-heal.ts` done 事件 usage 仅含末次成功 attempt，与 ai-calls.json「按次入账」口径不一致，前端成本显示偏低。
9. `orchestrate/chat/restore.ts:180` 空串用户消息入历史，消毒后数组可能为空 → 协议 400 报原始文案；入口应直接拒绝。
10. `rag/index.ts:487-490` 召回超 10 万块硬截断仅 log.warn，前端无感知（prompt 静默少素材）。
11. `knowledge/update.ts` fm 注入成功后 manifest 写失败的跨文件窗口（validate 可事后发现）；建议 manifest 先写或失败回滚注入。

### B 域（15 条）
1. `check/count.ts:157-163,218` 引导语含词表外动词（挥手/点头+冒号引语）时「住手」类 2 字对白仍报伪专名黄项。
2. `check/count.ts:489` adjStackRegex 匹配任意「X的」链，领属链与形容词堆叠同判误报。
3. `check/count.ts:141,319,352` 句长用 UTF-16 `.length`，与 countWords 码点口径不一致，含 emoji 句长虚高。
4. `format/yaml.ts:327-331` `growth.realm_span_max` 无正值校验，0/负数使一切正常晋阶报红。
5. `format/yaml.ts:145-151 vs 176-199` 顶层段重复 fail-loud、段内子键重复 find 静默取首，同文件两种容错策略。
6. `format/leads.ts:173-174` 账本 fm 类型/状态非法值静默默认，动词表按错类执行。
7. `check/lead-updates.ts:55-69` parseLeadUpdateLines 无续行合并（对照 parseHistory 有），证据折行截断 → 失配假红。
8. `check/leak-derive.ts:56` 行内数组正则要求行尾 `]`，带行尾注释整条派生静默失败（他键均有 stripInlineComment）。
9. `review/contract.ts:336-354` 聚合去重键同位置两条不同问题时第二条 issue/fix 文本被丢弃。
10. `review/run.ts:296-297,322-325` 合审档单文件存在即记全部视角已回收，无最小覆盖闸。
11. `check/tree-issues-cache.ts:38-45` 纪元指纹 mtimeMs 与章元数据 mtimeNs 粒度不一致，同毫秒改回不失效。
12. `format/frontmatter.ts:288-307` patchFlatFm 把段内注释行/空行后的任意非空行当嵌套子结构，纯平铺键跟注释时合法更新被拒（过宽 fail-loud）。
13. `check/leads.ts:316-317` 非成长线「末条 resolve 动词+状态已放弃」被硬拦，作者揭晓后弃线的合法序列误报。
14. `check/growth.ts:60-74` 多体系前缀重叠（炼气/炼气期）时序列选择可能取错，后续跃迁全按错基准判红。
15. `check/runner.ts:152`+`format/iron-rules.ts:52-70,235-243` 每次 runAllChecks 重扫条目库整读（O(章数×条目库) 重复 IO），建议指纹级缓存。

### C 域（15 条）
1. `document/service.ts:963,917` doTrash 确定性命名+清单条目删除失败残留 → 再次软删静默覆盖上次 .trash 文件，还原旧条目得错内容（有 .版本 留底故不到 P2）。
2. `document/version.ts:233-245` 去重循环对 fm 损坏的同源版本 continue 落到更旧版本比对，恰等旧版时跳写，快照链尾部失真。
3. `install/migrate-finalized-revision.ts:73` manifest 路径直拼未经安全校验，`../` 可读书根外文件并把其 sha256 写进清单（只读面小）。
4. `export/index.ts:161` 全部章正文一次驻留 worker 内存，极端大书逼近 1GB resourceLimits 被 OOM 击杀（不损数据）。
5. `events/store.ts` 全局单一 migrate.lock 串所有书，双进程异书启动互相串行（纯性能）。
6. `document/foreshadow.ts:120-129` existsSync→atomicWriteFile 跨进程 TOCTOU 双写窗（内容幂等，违库内 createFileExclusive 惯例）。
7. `document/service.ts` updateDocMeta 两次独立 readFileSync 微 TOCTOU（finalize.ts:69-70 同款已修成单读，此处漏齐）。
8. `state/batch-pause.ts` `.auto-batch.json` 读改写无锁，双进程暂停标记互覆。
9. `process/book-search.ts:20` scope「定稿」实搜 `写作/正文` 含未定稿草稿，与 assembleStatus 定稿口径不一致，草稿可注入 prompt。
10. `process/spill.ts:139-148` 极端小 maxInlineChars 时 preview 只剩通知行、模型上下文全失（有保底）。
11. `process/gui-active.ts:31-44` writeGuiActive 读改写非原子，双进程心跳 editing_workdir 位互踩。
12. `document/words-diary.ts:52,87` appendFileSync 无跨进程锁，Windows 无 O_APPEND 小写保证（坏行有跳行容错，损失单行）。
13. `process/lead-update-draft.ts:31-58`（同型 summary.ts:167）串行化队列/inFlight 均进程内 Map，双进程跨进程双写仍可交错（归档时间戳后缀兜底）。
14. `cache/rebuild.ts` `/^\d+$/` 白名单外手写摘要命名静默不进 index.db，近章结尾段静默缺段（与命名契约一致但无告警）。
15. `events/store.ts:411-433` busy_timeout+8 次退避耗尽仍可抛 SQLITE_BUSY，chat-bridge 首 flush 回滚兜底损失单批事件。

### D 域（12 条）
1. `api/stream-ticket.ts:22` tickets Map 模块级单例，同进程二次 startServer 旧票残留（生产单实例不触发）。
2. `scripts/corpus-commit.ts:109` 语料用例 writeFileSync 非原子，脚本被杀截断 JSON（R63-11 使损坏不扩散，但固化用例丢失）。
3. `studio/server/http.ts:96-143` readJson 对原语 JSON body 原样透传，无统一「body 须为 object」断言，错误信封口径不一。
4. `api/stream.ts:52,261` per-book SSE 上限 5，第 6 个标签页 429，前端对该 429 无「关闭多余标签」中文指引。
5. `desktop.yml:119` Electron 启动冒烟靠 grep 日志字符串判定，与 main.ts 措辞强耦合，措辞一改静默失效；建议结构化标记。
6. `ci.yml:8` CI 仅 workflow_dispatch 手动触发（`62a7075` 有意收窄、注释承诺恢复），恢复前全套门禁不自动执行，属已知敞口须按注释销账。
7. `eslint.config.js:29` lint 门射程仅 JS/MJS，TS/Vue 全部业务代码无 lint（注释自知另立账）。
8. `package.json:62-64` overrides.esbuild 强制 pin 无注释记录因由，升级时构建漂移风险集中。
9. `tsup.config.ts:8` rmSync('dist/desktop') 在配置加载期执行，watch 重载会删在用产物（仅 dev 循环）。
10. `.gitattributes:10-12` binary 白名单仅 png/ico/icns，未来新增 .woff2/.webp 若被 text=auto 误判，入库归一化破坏字节等价金测输入。
11. `desktop.yml:109` 冒烟取二进制 glob 依赖 electron-builder 输出目录命名约定（fail-closed 可接受）。
12. `playwright.config.ts:24` e2e baseURL 钉死 127.0.0.1:18999，端口被占即 globalSetup 失败，无预检/让位。

### E 域（5 条）
1. `views/WorkbenchView.vue:268` onSaveDraft 无本地在途锁（同文件其他生成动作均有），双击双发 saveDraft（幂等覆盖，仅双 toast/双 tree.load）。
2. `panels/chat/ChatMessages.vue:69` confirmingCallId 串行化所有工具确认，多张待确认卡并存时第二张点击被静默忽略。
3. `panels/TrashPanel.vue:63` restore 在 await 后用 `props.bookName` 触发 tree.load，在途切书后对新书冗余整树重扫（恢复本身无串书）。
4. `AiServicePanel.vue` save() 编辑卡双击第二笔必 409 弹误导性「并发冲突」（随 E-1 修法一并消除）。
5. `stores/learn.ts` commit() 用 `const gen = reqGen` 不推代，依赖 harvest 推代兜底——语义正确但脆弱，建议独立 commitGen。

### F 域（13 条）
1. 临时目录兜底 helper `mkdtempTracked` 采纳率 19/266，其余仍是「尾行 rmSync」模式——断言失败清理行不可达，失败跑批持续泄漏 $TMPDIR。
2. `coverage/coverage-summary.json` 为 api 桶局部跑产物（993 行），total 89.32% 极易被误读为全书覆盖率；配置注释引用的「全量实测」无全量工件留存。
3. `helpers/studio-token-setup.ts:31-48` 全局 fetch 包装对 GET /api/* 自动注入 token，未来「用 fetch 断言无凭据 403」的新测试会被静默救活，防线仅文字约定；建议包装层告警。
4. `playwright.config.ts:31-33` e2e 单 chromium 腿，dev:web 浏览器形态无 webkit/firefox 回归。
5. e2e 固定端口族 8 个硬编码（18999/19000/…），双 checkout 并行或 dev 抢占整批红（EADDRINUSE 已有指因提示）。
6. 顺序契约下 spec 进程级崩溃跳过恢复逻辑，下游在脏 workDir 上连坐红、无首因标记。
7. `ai/provider/adapter.test.ts:116-127` done 幂等仅断言次数，未断言 usage 取首条——实现改末条无红灯。
8. `ai/runner.test.ts:38` `http://localhost:1` 作必然拒绝端点，依赖错误形态为 ECONNREFUSED，沙箱/防火墙返回他族时断言漂。
9. `vitest.config.ts:62` testTimeout 30s 全局一刀切，GB 级 scale 与普通单测共用，慢速 CI 腿漂移即红（部分 spec 已自行放宽佐证漂移存在）。
10. `scripts/check-counts.mjs:34-49` 静态计数正则净化对模板串嵌套极端形态可能漂移（有直测兜底，风险低）。
11. `test/desktop/main.test.ts:22-27` utility child 为假件（灰盒边界已声明），真实 fork 行为仅靠 check:packaging+release-smoke 兜底。
12. `test/studio/webnext/` 与 `src/studio/web-next/` 命名不一致，目录对照/检索成本高。
13. 两个同名 `tierOf`（`src/format/style-compare.ts:66` 与 `web-next/src/shared/learn-tier.ts:9`），后者零直测（函数平凡）。

---

## 七、完成进度评估

### 7.1 功能面盘点（按 README 声明逐项核实）

| 功能声明 | 实现状态 |
|---|---|
| 建书（长短篇双轨、目录自动生成） | ✅ 完整（install/scaffold + onboard + e2e 全流程过） |
| 写设定（大纲三级/角色/世界观/物品，短篇反转线索+情绪曲线） | ✅ 完整 |
| 全自动写章（AI 起草→机检→打回循环→重试上限停问） | ✅ 完整（draft-pipeline/self-heal/retry；本轮 C-1 为其落盘协议缺口） |
| 机检（复读/句式/禁词/比喻密度等 11 项+短篇 4 项） | ✅ 完整接入 runner；B-1/B-2 为规则正确性缺口 |
| 三审（读者/编辑/设定校对；短篇钩子/反转/收尾） | ✅ 完整（契约含 draft_hash 防假通过） |
| 定稿（逐章确认+防吃书闸） | ✅ 完整 |
| 伏笔全程追踪/字数曲线/文风系统/选段改写分析/对话助手 | ✅ 完整 |
| 版本快照/恢复、回收站、事件审计、导出 | ✅ 完整 |
| 桌面壳（Electron+utilityProcess 崩溃隔离）、三协议 AI 接入 | ✅ 完整 |
| win 发布链（阶段 21 J0 打包/J5 体验/J6 发布） | ❌ 未做（见 7.2） |

### 7.2 开放任务核实（对照总览第三节，逐项代码实证）

- **阶段 13（Responses 启用批，开放=外部依赖）**：`scripts/verify-responses-relay.ts` 在位，余中转网关真机验证待作者凭据；「encrypted 中段链路无直测」挂账**维持**（本轮 grep `reasoningEncrypted` 于 test/ 零命中复核属实）。
- **阶段 21（win 适配）**：J1 cross-env ✅（package.json:23,39,50）、J2 sanitizeFileNamePart 单源 ✅（format/filename.ts:60）、J3 测试平台门 ✅（57 处条件 skipIf 均带守卫）、J4 CI win 腿 ✅（ci.yml:36 windows-latest×Node24）；**J0 未动**（electron-builder.yml win 段整段注释）、**J5/J6 未动**。H-1 无应用图标（electron-builder.yml 注释确认：零图标资产，作者资产决策）、H-2 仅 arm64（desktop.yml:29 注释确认，发行决策留作者）——两项均为已记档的决策待办而非缺陷。
- **CI 自动触发暂关**：`47572ce`/`62a7075` 两笔有意收窄（push/PR 自动腿摘除，验证走本地+手动 dispatch），注释承诺频推期后恢复——恢复前全套 CI 门不自动执行，属已声明敞口。
- **token 校准**：`process/prepare.ts:115-118` TOKEN_COEFFICIENTS 空表（注释自认「尚未实测」），预算精度靠缺省系数——功能可用、校准未跑。

### 7.3 进度结论

整体 **≈95%**：声明功能全部实现且门禁全绿，剩余 5% 为 win 发布链（J0/J5/J6）、中转真机验证（外部依赖）、图标/架构发行决策与 token 校准。核心写作主链（建书→设定→写章→机检→审稿→定稿→导出）**≈97%**——主链无未实现项，缺口服从属于正确性打磨（§五 P2）而非功能缺位。

---

## 八、完成质量评估

**总分 8.8/10**（六域均分 8.75）。

**给分依据（加分面）**：
1. 零 P1：数据安全主承诺（作者数据不被升级覆盖、定稿原子写+指纹、快照留底、api_key 不进 git/日志、服务端三层防攻击）经对抗性核验全部成立。
2. 门禁真实且闭环：502/4066 全绿非数字游戏——元门禁（counts 对账、coverage 分桶阈值、spec 顺序快照、GET 路由穷举网）把「测试自身的漂移」也锁住；e2e 42 过含建书→写章→机检→三审→定稿全流程冒烟。
3. 修复迭代证据链在树：关键边界普遍带轮次编号注释与对应回归用例，抽验相符。
4. 测试纪律：零快照滥用、零无条件 skip、mock 落依赖边界、真实 SQLite/文件系统夹具为主。

**扣分面（均对应 §五 P2 与 §六 P3）**：
1. 双进程并发面仍有「可用性优先」取舍残余（C-1 草稿游离保存协议、C-2 清单锁降级裸写、A-2 providers RMW）——单进程桌面常态无感，双进程场景有窄窗数据风险。
2. 机检零 token 启发式的正确性长尾（B-1 像系名词、B-2 禁词静默漏报、B-3 缺省遮蔽）——影响的是检查的可信度而非主流程。
3. 发布面欠账（无图标、arm64-only、未签名、win 打包未做）——均为已登记决策待办。
4. 测试覆盖末梢（两个刚修过缺陷的 helper 零直测、e2e 断言面被顺序契约收窄）。

---

## 九、修复计划建议（待作者指令，本轮未动任何源码）

- **批 A 数据安全（建议先行）**：C-1 saveDraft 接入保存协议（per-doc 锁+journal+字数记账，或直接走 save 协议 origin=ai）；C-2 清单锁超时改 fail-closed（对齐保存锁纪律）或有界退避后仍失败才拒绝写；D-1 删书先 rename 墓地再 rmSync；A-2 providers.json 写操作统一 serializedWrite 队列+持久化前重读合并。各配双进程竞态回归。
- **批 B 机检正确性**：B-1 SIMILE_RE 排除像系名词前字集或要求比喻尾缀；B-2 readBannedEntryWords 复用 parseBannedWordsLine 同套清洗劈分+无法解析出词条目产黄项；B-3 readChapter 用 hasOwn 区分缺省必填枚举产红项。
- **批 C AI 链路**：A-1 usage 缺失时以可得信号估计入账（输出≈累计 delta 折算、输入≈promptMeta.chars 折算）带 estimated 标记（或经作者裁定维持 0/0 取舍并登记）。
- **批 D 前端**：E-1 三处（连带 WorkbenchView onSaveDraft）补在途锁+`:disabled`。
- **批 E 测试工程**：F-1/F-2 两 helper 补直测；F-3 e2e 断言面（改派生状态恢复或登记接受）；P3 择优（mkdtempTracked 采纳率推广至存量 266 文件可机械批处理、coverage 工件误读注记等）。
- **维持/登记候选**：A-1（注释已认取舍）与 C-2（journal 先例口径）若作者裁定维持，须在源码注释与本报告 §五 双处留痕。

---

## 十、索引同步记录

本报告落盘后同步（历史正文不改写）：`Dev/Main/README.md`（目录地图 01-评审 暂存 6→7 篇 + 变更记录增条）、`Dev/Main/00-总览与实施路线-2026-08-15.md`（1.1 增二十一轮行）、`Dev/Main/01-评审/README.md`（当前暂存 6→7 篇）。全批零源码改动、零测试改动，唯一写入面为本报告 + 三处索引。

---

## 十一、收口记录（2026-08-29 回填）

作者指令「全部修复，编排下任务，并发进行！」。修复批以六路文件互斥并行代理执行（批 E 前端 / 批 F 测试工程成活；批 A/B/C/D 中途撞 API 限额阵亡，**改动大半已在树**，残留由主评审 + 两路收尾代理补齐核销）。收口编号口径：`R73-N` 为修复批 dispatch 编号，与 §五/§六 条目按内容对应，本记录以清单条目为准。

### 11.1 P2 ×12 全部修复（回归在树）

| 条目 | 处置 | 证据 |
|---|---|---|
| A-1 usage 0/0 入账 | 已修：估计原语 + 双适配器接线（`estimated` 标记），旧「0 成本取舍」升级 | `src/ai/provider/usage-estimate.ts`（R73-1）；`anthropic-adapter.ts:235/262/307`；openai-adapter 同族；`runner.ts:465-481` attemptsUsage 逐 attempt 累计 + `self-heal.ts:818` 消费（R73-10） |
| A-2 providers RMW | 已修：全部写路径 serializedWrite + 跨进程锁（同 calls.ts 范式） | `src/ai/provider/store.ts:285-338`（R73-2） |
| B-1 SIMILE_RE 名词误报 | 已修：前排他集纳入像系名词首字（实测复现对照） | `src/check/count.ts:676-693`（R73-14） |
| B-2 禁词静默漏报 | 已修：复用 parseBannedWordsLine 同套清洗 + 解析不出词条目 unparsed 回报产黄 | `src/format/style-entry.ts`（R73-15）；消费面 `runner.ts:157` |
| B-3 章 fm 必填枚举静默默认 | 已修：缺字段 fail-loud 产红（`_fmMissing` 同步） | `src/format/chapters.ts:61`（R73-16） |
| C-1 saveDraft 游离保存协议 | 已修：per-doc 锁 + journal pending/settled + manifest 登记 + 字数记账；4 回归（新建/覆盖/双进程竞态/stale 锁接管） | `src/process/draft-pipeline.ts`（R73-32）；`test/process/r73-save-draft-protocol.test.ts` |
| C-2 清单锁降级裸写 | 已修：fail-closed 不降级（有界重试后抛错拒绝写，宁拒绝不覆盖） | `src/document/manifest.ts:182-217`（R73-33） |
| D-1 删书非原子 | 已修：先 rename 入 `.删书墓地`（同盘原子）再清理；清理失败留档不阻断；2 回归（正常 + 权限中断形态） | `src/studio/server/api/books.ts`（R73-34）；`test/studio/books-delete-graveyard.test.ts` |
| E-1 三处保存无在途锁 | 已修：AiServicePanel/双 ProviderEditor/StyleEntryPanel/WbDraftCard(+WorkbenchView onSaveDraft)/ChatMessages(确认在途按 callId)/TrashPanel(切书捕获)/learn commitGen 七处在途锁 + `:disabled` | R73-62/63/64/65/66（批 E）；回归 `test/studio/webnext/` 下 r73-double-submit-guards / r73-chat-confirm-parallel / r73-trash-restore-book-guard / r73-learn-commit-gen |
| F-1 sse-guards 零直测 | 已修：16 例直测 | `test/studio/webnext/sse-guards.test.ts` |
| F-2 numeric-input 零直测 | 已修：5 例直测 | `test/studio/webnext/numeric-input.test.ts` |
| F-3 e2e 断言面收窄 | **维持登记**（已披露设计代价）：首因标记已修缓解连坐面（R73-76，`test/e2e/first-cause-reporter.ts`）；派生状态恢复改动面大收益窄，登记接受 | §五 F-3 + 本行 |

### 11.2 P3 ×71 处置汇总

- **已修（含核验实现已在、补配回归）共 62 条**：A 域 9（max_tokens 兜底 16384 / manifest 缺 entries 守卫 / commitIndexBatch 按章续传 / redact 智谱+Gemini 形态 / 队列溢出 notice 实数 / attemptsUsage 口径 / 空串消息入口拒绝 / 召回截断结构化出口 / manifest 写失败回滚注入）；B 域 15 全修（R73-14~31：含 growth 正值校验 R73-20、段内子键重复 fail-loud R73-21、账本类型/状态 fail-loud R73-22、lead resolve+放弃合法 R73-29、铁律缓存接线 R73-31 等）；C 域 11（导出流式 R73-37、伏笔迁移 TOCTOU R73-39、updateDocMeta 单读 R73-40、batch-pause 锁、搜索定稿口径、spill 保底、gui-active 原子写 R73-44、lead-update 跨进程锁、rebuild 白名单告警、events per-book 迁移锁等）；D 域 8（stream-ticket 实例化 R73-49、corpus-commit 原子写 R73-50、readJson object 断言 R73-51、SSE 429 前端指引 R73-67、desktop.yml 结构化标记 R73-53、esbuild 钉版因由 R73-56、tsup watch 守卫 R73-57、playwright 端口基址 R73-75）；E 域 5 全修（R73-62~66）；F 域 9（mkdtempTracked 采纳率清扫 94 文件、coverage 误读注记 R73-72、token-setup 告警 R73-73、端口族 R73-75、首因标记 R73-76、超时口径注记 R73-77、计数正则模板串支持 R73-78、adapter usage 首条断言、tierOf 直测 R73-81）。
- **维持登记 9 条**（源码裁注或本行留痕）：
  1. vault-key XOR 混淆级防护（威胁模型注释在位，用户文档声明另立）；
  2. calls.ts 预算 TOCTOU（R73-8 裁注：锁粒度不允许跨生成持有，退款事务错误面不成比例）；
  3. words-diary appendFileSync（R73-45 裁注：O_APPEND 内核原子 + <200B + 坏行容错，加锁每写一词争用不成比例）；
  4. events store SQLITE_BUSY 耗尽即抛（R73-48 裁注：fail-closed 正确出口，chat-bridge 首 flush 回滚兜底在位）；
  5. ci.yml 手动触发敞口（作者 `47572ce`/`62a7075` 有意收窄，注释承诺频推期后恢复）；
  6. eslint TS/Vue 射程（另立批；现行门 = tsc strict + vue-tsc + 521/4181 全绿）；
  7. e2e webkit/firefox 腿（R73-74 维持：Electron=Chromium 同核）；
  8. desktop.yml 产物 glob 命名约定依赖（fail-closed 可接受）；
  9. webnext 目录命名不一致 + fake utility child（灰盒边界已声明，check:packaging + release-smoke 兜底）。

### 11.3 修复批连带发现与当场收口（2 条）

1. **R73-22 连带回归**：账本类型/状态 fail-loud 使伏笔旧档迁移全量拒迁（旧档 `类型: 伏笔` 非法于六类）——`readLead` 增显式 `legacy` 旁路（`src/format/leads.ts`），迁移链按旧 scheme 容错解析（`src/document/foreshadow.ts`）；补 3 回归（非法类型/非法状态/缺字段回落 + legacy 放行，`test/format/leads.test.ts`）。存量测试「成长线 resolve+已放弃」按 R73-29 新约同步更新；「数字字段坏值不设键」测试剔除 realm_span_max（R73-20 起改 fail-loud）并补 R73-20 专测（坏值/0/负数 → 错误信封，`test/format/yaml.test.ts`）。
2. **vitest worker close 回调环境事实**：http server `close()` 回调在 worker 无其它 ref 句柄时不被驱动（forks/threads 同现，探针实测保活 interval 后 5ms 完成）——`runner.test.ts` beforeAll 以保活 interval 至 close 完成（R73-13c 收口注记），另修正 `node:net`→`node:http` 误 import。

### 11.4 收口门禁（2026-08-29 全量亲跑）

| 门 | 结果 |
|---|---|
| vitest 全量 | **521 文件 / 4181 例全绿**（基线 502/4066 → +19 文件 / +115 例） |
| tsc --noEmit / vue-tsc | 双 0 错 |
| ESLint | 0 错 |
| check:counts | 过（根 README 已同步 521/4181） |
| check:packaging / check:knowledge | 双过 |
| e2e | **42 过 / 2 跳**（release smoke 需 `CLWRITING_E2E_RELEASE`） |

**未实证面**：R73-53 结构化标记与 CI 矩阵属 `.github` 面，需下次 Actions 手动 dispatch 实证。**工作树未提交（待作者指令）**；按归档纪律，本报告收口后上一轮评审报告应移入 `Archive/`（移动属确认闸，待作者确认后执行）。

**本节为收口回填，§一〜§十为评审时点原文（历史正文不改写）。**
