# 全量代码复审与进度质量评估第五十九轮-评审报告-2026-09-07

> 📦 **归档记（2026-09-08）**：已收口，随「README 整理与清理」批移入 `../Archive/`（扁平冷库；确认闸 2026-09-08 修订——规则内免确认留痕）；历史正文不改写。

> 状态：**已收口**（2026-09-07 同日直接收口——P0×0/P1×0/P2×0/P3×0 零可分级缺陷，无待修项 = 收口条件天然满足，无修复批；同日附 §九清偿批记——R55–R58 批 3 遗留清偿随作者「全部修复」指令办结，非本报告缺陷面；归档移动待作者确认闸/归档批）。
> 路径经确认闸 AskUserQuestion 获批（第五十九轮正典名，2026-09-07）。轮号说明：README 2026-09-06 已引「五十九轮 win 实跑口径」作为 R55–R58 合流后 win 腿计账（929 文件 / 6089 过 + 77 跳），本报告沿用「第五十九轮」续 01-评审 链（上一份为五十八轮 2026-09-06），轮号同源不冲突。
> 收口记（2026-09-07）：文档链同步已执行——Dev/Main/README 目录地图 15→16 篇 + 变更记录新条目 / 总览 1.1 新增行 / 台账 §一 新增行 / 01-评审/README 维护日期条目 + 暂存表 15→16 篇；根 README 测试计数免修账（929/6162 本轮零代码零测试改动，check:counts 复验绿）；工作树未提交待作者指令。
> 清偿批修账（2026-09-07，见 §九）：根 README 计数 929/6162→**950/6234**（四处，check:counts 绿）；五、六节表格内「929 测试文件 / 6162 单测」「929/6162 全绿」为报告期实测数字，存档不改（历史正文不改写纪律），现行计数以根 README 为准。

## 一、轮次与审法

- **约束**：按作者指令「忽略现有评审文档，从头重新评审项目所有代码」，本轮盲审——未读取 01-评审/ 既有报告、未依赖历史结论，全部结论由本次独立审读得出。
- **基线**：dev 分支 HEAD 4373390a（R55–R58 修复批已并入），工作树干净。
- **方法**：
  1. 安全/持久化核心面全文精读：`main.ts`（1810 行）、`server-manager.ts`（939）、`graceful-shutdown.ts`（106）、`document/service.ts`（1951）、`state/state.ts`（1004）、`server/http.ts`（232）、`server/router.ts`（130）、`server/index.ts`（493），约 6700 行；
  2. 前端核心链精读：`api/client.ts`（237）、`composables/useSse.ts`（254）、`stores/doc.ts` flush 面（655）、`useHeartbeat.ts`；
  3. AI 层代表深读：`runner.ts`（704）预算/超时/退化/记账段；
  4. 全域风险模式快扫（只读 grep）：空 catch、setInterval 未清、TODO/FIXME、spawn/exec 红线、api_key 泄漏，共命中 4/9/3/0/0 处，全部逐项落地复查消解。
- **L2 门禁实测**（本机 mac 口径）：vitest 929 文件 / 6162 通过 + 4 跳过，119.8s，exit 0；`tsc` 0 错；`vue-tsc` 0 错；eslint 0 error（31 存量 warning）；`check:counts` / `check:packaging` / `check:knowledge` 三 check 通过且与 README 声称一致。e2e（29 specs/45 用例）本轮未跑——需 `build:web` 后另跑，不构成缺陷。

## 二、产品红线六条逐项对账（本轮独立复核）

| # | 红线 | 结论 | 证据定位 |
|---|------|------|----------|
| ① | 作者编辑内容永不静默丢失 | ✅ 闭合 | 关窗/退出三步 flush 链：`main.ts` 关窗拦截 → `executeJavaScript` 调 `__clwFlushPrefs` + `__clwFlushBeforeClose`（main.ts:830-833）→ `doc.ts:641` flushBeforeClose 上抛 `{failed, conflict}` → 主进程原生确认（R44-19）；flushDirty 循环重扫 + 在途落定上限 3 轮（doc.ts:588-617）；崩溃自愈（renderer 崩溃 ≤3 重启 / 加载失败退避 5 次 / server 重启耗弹窗） |
| ② | api_key 绝不进 git/明文日志 | ✅ 闭合 | 错误信封单一出口统一脱敏（http.ts:60-74 replyError → redactSecret）；dispatch 兜底 500 不透传原始 message（router.ts:120-125）；日志路径去 query 防 token 落盘（http.ts:93-97 urlPathOnly）；AI/服务侧 redact 使用面 18 文件；全库 console/log 带 key 扫描 0 命中 |
| ③ | 定稿原子写 + fingerprint | ✅ 闭合 | service.ts 保存协议 revision 校验 → journal → 快照 → 原子写+fsync → settle；跨进程锁序（save→wiring→manifest 锁）；非 UTF-8 拒绝；`createFileExclusive`/`linkOrRenameExclusive` TOCTOU 闭环；revision cache + PM-4 字缓存（4096 cap） |
| ④ | 升级不覆盖作者数据 | ✅ 闭合 | 启动迁移套件全部「只补不删」（repairBooks / layout v2·v3 / snapshots / foreshadows / finalized revisions / book defaults / prompt overlays）；无覆盖写路径 |
| ⑤ | 本地服务安全 | ✅ 闭合 | Host 白名单 + Origin 白名单（写/预检 403、ACAO 仅白名单）+ token 闸含 GET/HEAD（豁免仅 /api/boot + stream）；SSE 一次性 ticket + `?token=` 旧通道按 S7 收窄；路径归一化防 `/../` 绕过（R31-4/R33-11）；大写 /API/ 404；body 1MB + 闲置 30s + 413 排空宽限 1.5s + 慢速发送 destroy；`safeTokenCompare` timing-safe；`parseRequestUrl` 畸形 400 |
| ⑥ | AI 链路不 spawn CLI 子进程 | ✅ 闭合 | `src/ai` + server 面 grep spawn/exec/child_process 零实际调用（仅注释提及 /spawn 路由名与历史 P1-2 说明）；预算显式 resolve：`resolveProvider` → `tier.maxTokens` 透传（runner.ts:397-419），trace 落 resolve 后的 effort/timeoutMs/firstByteTimeoutMs（I7）；degraded 贯通最后一跳 B-2 |

## 三、逐域审读结论（精读面）

- **desktop/main.ts**：生命周期 flush 链完整；崩溃自愈仅限 renderer 侧且重启有界（≤3/退避 5）；IPC 校验白名单化（context-menu specs、颜色格式、resolveWithinRoot 路径守卫）；CSP 注入；pendingRelaunch 只在不可回头点设置。**闭合**。
- **desktop/server-manager.ts**：握手 30s；重启退避 [0,5s,15s] 上限 3；关停 3.5s 竞速（done/exit/timeout）；升级 SIGKILL 等待 2s；shuttingDown 生命周期门；session-end restartPinned 恢复；token 经 env 注入且清理大小写敏感变量 + 日志/DEV 变量屏蔽；stdio 转发 1MB 单行上限 + stopChild 前 flush。**闭合**。
- **graceful-shutdown.ts**：shutdownStudio 三 abort（self-heal/chat/spawn）+ task-gate 等待 + server.close + closeIdleConnections + 1.5s settle 预算。**闭合**。
- **document/service.ts**：保存协议各步均有事务化与失败语义；回收站「登记先于移文件、登记失败即中止软删」（GG-P2-6）；doMoveOrRename journal move-pending 恢复；updateChapterMeta/updateDocMeta 链式 meta 操作 + Z-13 capability 检查；META_SAVE_LOCK_TIMEOUT 5s。**闭合**。
- **state/state.ts**：状态机四健康检查（crashedWrite 读 journal / cloudCopy 60s TTL 节流 / finalizedLost / wiringMissing / manifestEmpty）；healMovePending；sweep 6h TTL；worker rebuild 通道。**闭合**。
- **server/http.ts + router.ts + index.ts**：错误信封唯一出口 + 全出口脱敏；`dispatch` 异常留诊断日志且 500 不透传；clientAbort 侧错误降 info（不把正常断连当故障）；readJson 闲置计时器武装早于占闸覆盖 body 悬持窗；413 排空不占 FD；路由模板正则转义含括号补集（R51-G-3）。**闭合**。
- **前端链（web-next）**：client.ts token 注入 + 401/403 防抖 re-boot + 同一请求至多重放一次 + 超时计时停表/重启（R42-15）+ 外部 signal 监听摘除；useSse 换票两段式 + fail-closed 接管退避 + 429 探测指引去重 + 连接代防悬挂 + 换票 5s 超时（R34D-23）；doc.ts flushDirty/flushBeforeClose/conflictedDirtyDocs/autosaveTick 卸载兜底 + console.warn 留痕；useHeartbeat 在途去重 + 10s 超时 + 捕获书名 leave + 在线信号复位。**闭合**。
- **AI 层（代表深读 runner.ts + 面扫描）**：重试/超时/用量记账集中于 runTask；attempt 时长口径（R65-12 + R27-1 终点快照）；mock 快路统一 MOCK_USAGE（B-11）；resolveProvider 失败路径 model 落非空桶（R75-A-P3a）；链路事件录制先于 mock 快路（P2）。**闭合**。

## 四、缺陷清单（本轮盲审新增）

- **P0 × 0**
- **P1 × 0**
- **P2 × 0**
- **P3 × 0**

零可分级缺陷。风险模式快扫命中的全部候选点逐项落地复查均消解（空 catch 4 处全部为注释声明的降级/防御语义；setInterval 9 处全部带 unref 或生命周期清理，含 stream.ts 心跳 close 即清、cross-process-lock 续期锁失败自停、FocusStatsBar ticker onBeforeUnmount 清理；TODO 3 处为文档库示例与注释文本，非挂账实现；spawn/key 泄漏 0 命中）。

**结论**：R55–R58 六批修复后代码已达质保收敛态——核心安全/持久化/前端保存链在前五十八轮反复评审形成的高频面，本轮盲审未发现新缺陷，佐证修复链的有效性与该面的成熟度。

## 五、完成进度评估（对照总览路线 0–24 阶段）

| 项 | 状态 |
|----|------|
| 阶段 0–23（核心引擎/文档模型/结构操作/状态机/本地服务/AI 三适配/前端/测试体系/win 对齐等） | 已闭合/已完成（依据总览第三节阶段行） |
| 阶段 24「章节目录操作」 | 设计方案（02-执行/章节结构操作-设计方案-2026-08-30）+ 执行方案（-执行方案-2026-09-04）已就位，**实现待作者命令**；结构操作类标记物（structure.merge / history-less rebuild 等）已散见于既有代码供落地 |
| 工程量 | src TS ~78.7k 行 + Vue ~25.8k 行；test 147k 行；929 测试文件 / 6162 单测；29 e2e specs / 45 用例 |
| **综合进度** | **≈ 97%**（23/24 阶段完结 + 阶段 24 设计完成待实现 + win 持续对齐面） |

## 六、完成质量评估（域加权）

| 域 | 权重 | 评分（10 分制） | 依据 |
|----|------|----------------|------|
| 保存/持久化 | 0.20 | 9.5 | 协议完整、失败语义全程显式、无静默丢失路径 |
| 本地服务安全 | 0.20 | 9.5 | 三闸 + GET 闸 + 票据化 + 脱敏全覆盖 |
| 桌面壳生命周期 | 0.15 | 9.0 | flush 链/崩溃自愈/关停竞速 |
| AI 编排 | 0.15 | 9.0 | 预算显式 resolve、记账口径、无 CLI 子进程 |
| 前端交互链 | 0.15 | 9.0 | token/超时/退避/卸载兜底四层防护 |
| 测试体系 | 0.15 | 9.5 | 929/6162 全绿 + e2e 45 例 + 三 check 门禁 |
| **综合** | 1.00 | **9.3** | — |

扣分说明（各域 0.5）：AI 域——报告期未跑通三方 provider 实连（无凭据，依赖 CI）；前端域——e2e 未在本机复核（需 build:web）；桌面域——win 腿 77 跳过为平台门（既定口径，不扣分项，仅记数）。

## 七、收口建议与后续

1. **无需修复批**：P0/P1/P2 为零，按 AGENTS.md「报告完成≠收口——P1/P2 修复+回归通过才收口」的逆否，本报告满足直接收口条件，报告落盘 + 索引同步即收口。
2. **文档链同步**（待确认闸批准后执行）：报告入 `Dev/Main/01-评审/`；README 计数/章节同步（测试数与文档数口径逐项核对，报告本身不改变 929/6162）；总览「一、文档地图」新增一行并标注状态。
3. **后续焦点建议**：阶段 24 章节目录操作实现开工（等作者命令）；e2e 全量本机复核一次（含 CLWRITING_E2E_RELEASE 两则发布 smoke）；win 腿配合 CI 三腿矩阵跑九件终门。
4. **工作树纪律**：本报告全程只读，未改任何源码；落盘为唯一写操作。

## 八、审读覆盖清单（精读/实跑证据）

- 全文精读（本报告期）：desktop/main.ts、server-manager.ts、graceful-shutdown.ts、document/service.ts、state/state.ts、server/http.ts、server/router.ts、server/index.ts、web-next/api/client.ts、web-next/composables/useSse.ts、web-next/stores/doc.ts（flush 面）、web-next/composables/useHeartbeat.ts、ai/runner.ts（预算/记账面）。
- 风险快扫（grep）：`catch{}` 空捕获 4 处、`setInterval` 9 处、`TODO|FIXME|HACK` 3 处、`spawn|exec|child_process` 0 实调、`console/log` 带 key 0 命中、`redactSecret` 使用面 18 文件。
- 实跑（L2 终门本机 mac 口径）：vitest 929/6162+4 跳过全绿（119.8s）；tsc、vue-tsc、eslint 0 error；check:counts/packaging/knowledge 通过。
## 九、清偿批记（2026-09-07 同日——R55–R58 批 3 遗留清偿，作者指令「全部修复，编排任务做。」）

本报告盲审零可分级缺陷，「全部修复」的唯一可修指向 = R55~R58 四报告「批 3 维持登记」遗留（含批 3 登记但具修面的高价值 P3）。按既有五路文件互斥并发代理惯例编排，22 项处置全办结：**21 项修复 + 1 项证据降级登记**；作者拍板/设计取舍项只列不动（见 §九.4）。

### 九.1 修复清单（21 项，注释引用格式「R59 清偿批（Rxx-X-N）」）

- **桌面/运行时（甲）**：R55-A-3 切库取消/失败回滚基线（switchRollbackStore 快照 + 三入口接线 + 两 before-quit 取消点回写）/ R55-A-4 KEY_MASK_HEADER_RE URL 值字符类收紧（`[^&\s#]+`→`[^\s&#\\"]+`，防引号逃逸脱敏边界）/ R55-B-2 事件注册 O(N) 全扫如实注释化（索引否决记档：迁移风险＞低频诊断收益）/ R55-B-4 链缓冲截断 warn 带丢弃计数。
- **AI 链路（乙）**：R55-C-7 kimi trimStop 逐条 ≤32 字节 UTF-8 安全截断（Buffer 字节回扫 + 序列完整度判定；MODEL_QUIRKS_VERSION → '2026-09-07.1'）/ R55-C-3 openai 截断估计补工具累积面 / R55-C-6 promptMeta 增 `tools?: string[]`（去重排序；hash/chars 刻意不变；经 runSpec spec.ts:166 对全部 tool-mode spec 统一接线，含 self-heal/review lens——主审完整性复核过）/ R55-B-3 缓存扫描非 `._` 非 .md 正规文件 warn。
- **服务端/校验/文档（丙）**：R55-E-2 静态 MIME 补 .txt/.webp/.gif/.woff / R55-E-3 CLW_DEV_API_PORT 拒 0（n<1 fail-loud；桌面端 R39-9 口径不动——彼处 0 合法）/ R57-G-1 元数据写链删重复 invalidateTreeIndex（链尾 doMoveOrRename :1532 单源，主审实证）/ R55-D-3 树红点缓存 epoch_fp 行级纪元戳（幂等 ALTER 补列 + 读侧锚校验 NULL 按 miss，单轮混纪元口径闭合）。
- **前端基础层（丁）**：R57-E-2 reloadFromRemote 决断时刻快照 + 双窗口四条件复检（窗口内新键入不再静默丢失）/ R57-E-3 clearBookMirrors 改 payload book 字段精确属主判定（书名含 `:` 前缀越界误伤闭合；损坏键保守不删）/ R55-F-5 apiJson 体内 abort 直通（不再伪报 MALFORMED_RESPONSE）/ R55-F-4 activeDocId 校验带 ownerBook 属主门 / R55-F-9 书级覆盖两键正数有限守卫 / R55-F-6 waitInflightSave 删除确认先落定在途保存。
- **前端组件层（戊）**：R57-F-2 三审/机检列表稳定键单源（shared/issue-keys.ts：内容组合键 + `#n` 消歧 + `\u0000` 分隔对齐 R49-27；复核发现 checkId 系检查器级 id，多条命中原实现必撞重复键——实伤较报告所述更重，两面板一并修）/ R55-F-8 dev 双基址失配诊断（连续 3 次 401/403 console.warn 一次，同纪元复位惯例）。

### 九.2 主审逐 diff 复核与两处修正

五路 diff 全量逐行复核通过（范围零越界、风格沿库）。复核期两事记档：① C-6 完整性——grep 证 self-heal 经 runSpec 走 spec.ts 中央 promptTools 登记，specs.ts 系声明处无缺口；② **R57-D-3 初版锚定过紧**——ANTI_RECON_HEADING_RE 锚死全形「反和解段（AI 味防御）」令 S5 以来 style-inject.test.ts 合并契约的裸形「## 反和解段」fixture 转红（L2 全量 vitest 抓获），主审亲修为后缀可选锚定 `/^##\s*反和解段(?:（AI 味防御）)?\s*$/`（「反和解心得/拾遗」仍拒，收窄目标不回退）+ 补裸形回归用例（backlog 测试 59/59 绿）。另补 tsc 抓获的 kimi 测试两处空值守卫（trimStop 可返 null）。

### 九.3 测试与终门

- 新增 21 测试文件/+72 用例（20 backlog-*.test.ts + 外来 r47-debounced-source 5 例，见 §九.5）；存量扩展 4 文件（trace/r33d-batch-b/tree-issues-cache/r51-j3-dev-api-port）。
- **L2 终门九件套亲跑全绿**：vitest 950 文件/6238 = **6234 过 + 4 跳 0 败**（117.2s；首轮 1 败即 §九.2② 锚定涟漪，修正后复跑绿）+ tsc/vue-tsc 0 错 + eslint 0 error 0 warning（存量 31 warning 本批清零）+ 三 check 过〔根 README 修账 929/6162→950/6234 四处 check:counts 绿〕+ build:web 过 + e2e 43 过 2 跳（34.3s）。

### 九.4 只列不动（作者拍板/设计取舍面，非可修项）

R55-A-5（font-list kill 生产接线=PM-12 同题）/ R55-C-5（anthropic reasoning 预留）/ R55-E-4（overview 同步窗，R37-20 已登记+stat-cache 缓解）/ R55-G-1（IME 组合期设计取舍）/ R56 三项（chat 列表虚拟化 / PM-12 / D5 e2e 留 mac）/ R58 C-1~C-4（台账 §三 拍板项）/ PM-10 尾读通道 / A7 接线 / SSE token 下线 / TOKEN_COEFFICIENTS 校准——维持登记或待拍板，出处各原报告与台账。

### 九.5 并行遗留记档（如实）

- **stash@{0} 未动**：「WIP: R47/R48/PM10/PM11 在途批中断现场（基于 dfb943d）2026-09-05」——本批开工前既存，非本批产物，原样保留。
- **外来两文件在盘并保留**：`src/studio/web-next/src/composables/useDebouncedSource.ts` + `test/studio/webnext/r47-debounced-source.test.ts`——乙路代理 stash 竞态恢复时自 e502df24 快照（HEAD 祖先）原样落盘（字节一致，mtime 12:37），实测兼容（r47 测试 5/5 过 + vue-tsc 净），按并行会话不代改纪律保留并计入本批门禁口径。
- **R55-G-3 降级登记**：外部改盘探针需新增服务端端点 + store 契约面（现树 revision 系进程级计数器，外部改盘不推进，无轻量探针可用）——ChapterTreePanel 原处注释登记维持现状，待立项。

工作树未提交待作者指令。
