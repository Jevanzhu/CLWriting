# 全量代码复审与进度质量评估六十一轮 — 评审报告

> 📦 **归档记（2026-09-08）**：已收口，随「README 整理与清理」批移入 `../Archive/`（扁平冷库；确认闸 2026-09-08 修订——规则内免确认留痕）；历史正文不改写。

- 日期：2026-09-07。状态：**已收口（2026-09-07 修复批同日办结——作者指令「开始修复，编排下任务。」：批 1 P2×1 主审亲修 + 批 2 高价值 P3×8 四路文件互斥并发代理全修全配回归 + 主审逐 diff 亲验；+9 测试文件/+25 用例 → 964/6280 = 6280 过 + 4 跳 0 败；L2 终门九件套亲跑全绿；根 README 修账 955/6255→964/6280 四处 check:counts 绿；批 3 维持登记；收口记 §八；归档移动待确认闸）**（报告完成 ≠ 收口——原评审面存档：落盘时未收口，修复计划见 §六）。
- 基线：dev HEAD `e074e7b5`（五十九轮附 §九清偿批 + 六十轮修复批合并提交）**干净树**，本轮全程只读零源码改动。
- 作者指令：「忽略现有的评审文档，重新评审一遍项目所有代码，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。记得编排下任务。」——与六十轮同款指令，本轮即其下一轮（第六十一轮）。
- 路径确认闸：会话自主执行模式（AskUserQuestion 不可达），按六十轮/十六轮先例以正典名落盘**待追认**。
- 禁读约束：主审与八域子代理全程未读 Dev/ 评审链、01-评审/、Archive/、CLAUDE.md、AGENTS.md 及任何评审/计划文档；进度盘点所需的计划文档（00-总览阶段行、02-执行/阶段 24 执行方案、00-台账）仅由主审在评审完成前读目录级状态行用于「计划 vs 实现」对账，未引用任何既有评审结论。**所有代码级结论均从源码独立重导。**

---

## 一、评审方式与覆盖度

**八域并发只读子代理**（Explore 型，遵 ≤5 并发约束分两波：波 1 A/B/C/D/E，波 2 F/G/H）全部交卷零阵亡：

| 域 | 范围（文件/行数） | 全文精读 | 快扫/未读 |
|---|---|---|---|
| A AI 链路 | src/ai 75 文件 / 13,299 行 | 58 文件（runner/gen/calls/三适配器/orchestrate 全链/prompts/tasks/contract/tools/rules/统计） | 快扫 10；未开 7（纯声明/数据/模板文件，经引用面与 catalog-sync 测试侧面确认） |
| B 桌面+进程 | desktop+install+process+driver 37 文件 / 11,524 行 | **全部 37 文件** | — |
| C 数据内核 | document+events+fs+git+state+cache 45 文件 / 14,357 行 | 20 个核心文件（service/journal/manifest/trash/version/tree/events store/fs 基座/cache rebuild+schema/state 等） | 其余 25 快扫 |
| D 校验+格式+长尾 | check+format+export+review+metrics+rag+knowledge+learn+log 56 文件 / 16,512 行 | **全部 56 文件** | — |
| E studio 服务端 | studio/server 49 文件 / 11,773 行 | 32 文件 ≈9,500 行（index/http/router/static + api 主体） | 4 个只读聚合端点结构抽查 ≈830 行 |
| F 前端基础层 | web-next 基础层 ≈60 文件 / ≈13,100 行 | ≈45 文件（全部 stores/核心 composables/client/关键 api/shared 主体） | 17 个薄端点 api + styles 模式化快扫；9 个 views 壳视图仅接线点抽查 |
| G 前端组件层 | web-next components+pages ≈130 文件 / ≈23,000 行 | ≈40 文件（CmHost/章节树/Check/Review/MetaForm/Settings/Book/Shelf 等主战场） | 节选快扫 ≈30；仅清单核对 ≈25（已过全域风险 grep，写操作守卫未逐一核验，§七如实声明） |
| H 测试+工具链 | vitest/tsup/eslint/playwright/tsconfig/electron-builder/CI×2/scripts×11/helpers/e2e 支撑件 + test/ 955 文件组织 | 配置与脚本全读；测试精读 15 个（8 全文） | 其余测试文件按结构/模式级扫描 + 全库 grep 统计 |

**主审亲审**：① P2 候选逐行亲验（B-1 成立，§五）；② 六条代表性发现抽样回验源码（A-1/C-1/C-2/D-1/D-2/E-1 逐条与源码吻合，代理报告可信度抽验 6/6）；③ 阶段 24 标记物九路独立扫描（§三）；④ 门禁九件套亲跑（§二）；⑤ 台账/总览对账（§三.3）。

覆盖面合计：src 464 文件 / 105,024 行（剔除 web-next node_modules）全部纳入分域，其中全文精读约 290 文件 / 约 7.9 万行（≈75%）；test 955 文件 / 146,699 行按组织+抽样评估。

---

## 二、门禁九件套实测（基线 e074e7b5，2026-09-07 本机 mac 亲跑）

| 门 | 结果 |
|---|---|
| vitest 全量 | **955 文件 / 6259 收集 = 6255 过 + 4 跳 0 败**（112.11s） |
| tsc --noEmit | 0 错 |
| vue-tsc --noEmit（web-next） | 0 错 |
| eslint . | **0 error / 0 warning** |
| check:counts | 过——实测 955 文件/6255 单测 + 29 e2e spec/45 用例，与根 README 声称一致 |
| check:packaging | 过——resources/ 入打包清单，prompt 版本表对账一致 |
| check:knowledge | 过——知识层 13 条 manifest 与磁盘一致，反向扫描无未登记资产 |
| build:web | 过（随 test:e2e 链） |
| e2e（Playwright） | **43 过 + 2 跳 0 败**（30.7s；2 跳 = 发布 smoke 需 CLWRITING_E2E_RELEASE） |

九件全绿，与六十轮收口记（955/6255 + 43/2）完全同数自洽。e2e 输出一行测试环境注记（task-gate 锁根被临时目录重复配置覆盖告警，shortflow spec 的双 workDir 夹具行为，非产品缺陷）。

---

## 三、完成进度评估：**≈97%（阶段 0-23 全部收口 + 阶段 24 方案就绪零实施）**

### 1. 计划口径对账（出处：00-总览第三节阶段行，本轮只读）

- 阶段 0-22（w/x/y/aa 清偿、0 前置决策、1 agent 化、2-6 批次 A-E、7 F1 全量、8-12 历轮清偿、13 Responses 启用、14 服务提供方、15-20 迭代/事故/发版、21 win 适配含 J6b、22 utilityProcess、23 迭代建议清偿关闭）：**全部已完成/已收口**。
- **阶段 24 章节结构操作（合并/拆分/卷重组）**：设计方案 v2（D1-D7 拍板）+ 执行方案（S2-S5 文件级蓝图）已落盘，**实施待作者指令**——唯一开放阶段。

### 2. 阶段 24 标记物独立扫描（主审亲验，九路全零命中）

`parseOrderOf`/`parseMergedInto`/`sortTreeByOrder`/`chapterTextByNumber`/`mergedIntoMap` 全仓 0 命中；`structure.merge`/`structure.split`/`structure.volume-move` 事件 0 命中；`src/document/structure.ts`、`src/format/chapter-lookup.ts` 两新文件不存在；`structure-plan`/`structure-apply`/`merge-undo` 端点 0 命中；`KNOWN_FM_KEYS`（chapters.ts:21）不含 `序`/`并入`。**「方案落盘、代码零实施」独立证实**，与总览状态行一致。

### 3. 收尾欠账盘点（计划外但影响「完成度」口径的事项）

- **挂账待拍板 ×2**（台账 §三）：PM-10 事件库尾读通道（sinceSeq 游标下推）；PM-12 font-cache 超时必杀生产接线（mac 打包态路径面，本域复核其死代码面见 B-8 同体）。
- **代码内显式备案的未接线面**：failure 决策表 `shrink-prompt`/`switch-provider` 动作零消费 + `MODEL_CATALOG` seeder 未落地（A-6/A-7，A7 预建资产）；SSE `?token=` 兼容旧通道（stream.ts:257 登记，待 useSse 下线）；`writePieceList` 章纲写路径生产零接线且未知段无保形通道（D-4，已武装陷阱备案）；`TOKEN_COEFFICIENTS` 空表待校准（prepare.ts:119-124 登记）。
- **六十轮批 3 维持登记项**（B-2 任务闸进程内互斥 / A-2 观察等 6+1 项）：本轮独立复核多数仍在（E-5 即其独立重现），属登记在案的可接受残留。
- stash@{0}（R47/R48 中断现场 147 文件，旧基线 dfb943d）仍保留未动——既知留置项。

**进度结论 ≈97%**：24 个计划阶段中 0-23 全收口；阶段 24 为纯增量（设计+执行蓝图就绪），加上上述收尾欠账，与近五轮（R55-R60）独立评审口径一致。产品主体（写作/保存/AI 链/机检/导出/桌面双平台/测试门禁）**无半成品面**——八域完成度盘点均为「完整实现，无桩、无 TODO 实体、无死入口」，扣项全部是显式登记而非隐性缺口。

---

## 四、完成质量评估：**8.9/10（A−，八域行数加权）**

### 1. 域评分表

| 域 | 评分 | 一句话判词 |
|---|---|---|
| A AI 链路（13,299 行） | 9.0 | 三协议适配/agent 循环/自愈闭环/计费/凭据加密全链完整，防御密度异常高；扣 mock 快路生命周期不对称与 A7 挂账面 |
| B 桌面+进程（11,524 行） | **8.5** | 崩溃窗口逐堵、竞态系统收口、覆盖 90.8%；扣唯一 P2（pickLibrary 入口漏接预探）与启动容错档缺口 |
| C 数据内核（14,357 行） | 9.0 | 保存链七层防护全链 fail-closed 方向一致，锁序统一；扣少量留痕/性能卫生项 |
| D 校验+格式+长尾（16,512 行） | 9.0 | 保形三通道/指纹失效链/跨进程竞态收口自洽且被测试钉死；两个候选 P2 经证据证伪 |
| E studio 服务端（11,773 行） | 9.0 | 鉴权分层+穿越全线 realpath 级+并发写治理达同类本地服务罕见水准；109 端点零裸路由 |
| F 前端基础层（≈13,100 行） | 9.0 | 「编辑永不静默丢失」五面（保存/切书/冲突/镜像/关窗）机制可验证、测试钉死；代数守卫成库级纪律 |
| G 前端组件层（≈23,000 行） | 9.0 | IME/竞态/让渡链/渲染上限纪律执行度极高，XSS 面为零（组件层 0 处 v-html）；一处防重放漏网 |
| H 测试+工具链 | 9.0 | 门禁有自检、假绿防线多层互锁、断言质量高于业界平均；扣用例级空洞无门与 eslint 类型感知缺位 |

行数加权（A-G 七域，103,565 行）：8.5×11,524 + 9.0×92,041 = **8.94 ≈ 8.9/10（A−）**；工程基建（H）9.0 单列。与近轮独立口径（R59 9.3 / R60 9.1）的差异主要来自本轮 B 域新抓到入口级 P2 与更严的卫生扣分，属评审噪声内波动。

### 2. 正面核销（本轮独立验证的既有防线，非引用旧结论）

- **数据红线**：保存链（dirty 队列→跨进程锁→journal pending→原子写→清单 fail-closed→版本快照→回收站软删）七层全链 fail-closed；前端侧快照/在途链/LRU 永不驱逐 dirty/镜像时效门闭环；未发现任何静默丢失路径（C 域+F 域独立双查一致）。
- **安全面**：服务只绑回环 + Host 精确白名单（DNS rebinding）+ 写端点 Origin+token 双闸 + GET token 闸精确豁免表；静态/文件/文档/快照全线 normalize+前缀判界+双侧 realpath；API Key 全链不入日志、vault 信封加密、redactSecret 单出口；前端 0 处 v-html/innerHTML；git spawn 数组参数 + ref 白名单。**未发现可利用的鉴权绕过/穿越/注入面。**
- **资源纪律**：监听器/定时器/句柄/db 连接/跨进程锁续期全域成对清理（A/B/C/E/F/G 六域独立核验一致）；缓存/队列/历史全有界（LRU/FIFO/上限+丢弃提示逐处落实）。
- **流式与取消**：runTask 总超时 + 逐 chunk 60s 看门狗 + abort 全链贯通 + 三协议截断契约齐备，无半截正文产出路径。

### 3. 质量画像

10.5 万行源码 + 14.7 万行测试（≈3:1.4），修复-回归一一映射（测试名带轮次编号）、每处取舍在代码内如实记档、门禁脚本自身有 13 个直测文件——**可审计的演化轨迹**是本仓最鲜明的质量特征。扣分集中在：一个入口级 P2、25 条 P3 卫生/一致性项（多数为「同族口径漏收口」形态）、以及依赖人工守护的不变量（Atomics.wait 同步壳、清单锁同步 fn、豁免表三副本互锁）。

---

## 五、发现清单（P1×0 / P2×1 / P3×25 / 观察×47）

### 1. P2×1（主审亲验成立）

**R61-B-1（P2）pickLibrary「打开书库」入口链缺可达性预探，失联网络卷可冻结主进程**
- 位置：`src/desktop/main.ts:566-634`（pickLibrary 主体）、`main.ts:1273-1282`（`desktop:open-library` IPC 入口）、`main.ts:670-681`（菜单 openLibraryAction）；对照 `main.ts:512-528`（probeDirReachable，唯一消费点在 switch-library 链 `main.ts:1290`）。
- 证据（主审亲验）：pickLibrary 全同步执行——`isLibraryDir`（main.ts:465→`findWorkDir` 同步 existsSync/statSync 祖先爬升，install/books.ts）+ `probeCaseSensitive`（fs/case-probe.ts:29-31 同步 `writeFileSync`/`existsSync`/`rmSync` 写探针）。切库链已为同款故障专门补了异步 `probeDirReachable`（stat + 2s 超时 + 哨兵三态），但「打开书库」两个入口均未复用。
- 影响：用户选择已失联/高延迟 SMB/NAS 挂载点时，同步 stat 爬升与写探针阻塞 Electron 主进程（可达数十秒），三窗全部无响应——切库链已修复的故障模式在另一入口残存。可恢复、无数据丢失，定 P2。
- 建议：pickLibrary 的 `isLibraryDir` 之前接入同款 `probeDirReachable`（两入口共用），超时弹「目录无响应」对话框返回；配注入式用例（probe 超时→拒绝）。

### 2. P3×25（卫生/一致性，按域列表；均有 file:line 实证，详证见各域交卷正文由主审留存）

| # | 域 | 摘要 | 位置 |
|---|---|---|---|
| A-1 | AI | mock 双快路绕过 `opts.register`/外部 ctrl，生命周期与真实路径不对称（mock-only 无实害） | ai/runner.ts:453-470 |
| A-2 | AI | CLW_VERIFY_VISIBLE 诊断通道走 console.warn 且 catch 全吞，绕过统一日志设施 | ai/orchestrate/chat/turns.ts:430-436 |
| B-2 | 桌面 | readStore 的 readFileSync 无容错档，EACCES/瞬时锁可致启动即退（与 parseStore 容错不对称） | desktop/main.ts:377-386 |
| B-4 | 桌面 | init.ts 尾部孤儿文档注释（所述函数已删） | install/init.ts:272-273 |
| B-6 | 进程 | 码点计数三处实现漂移（codePointLength 单源 vs 两处 Array.from 分配式） | process/settings-injection.ts:31 等 |
| B-8 | 桌面 | font-cache 自管 spawn 路径生产不可达（≈90 行死代码面，与 PM-12 待拍板同体） | desktop/font-cache.ts:150-208,279-299 |
| C-1 | 数据 | journal 文件级读失败静默返回空表零留痕（与同链路循环级 warn 口径不一致） | document/journal.ts:192-199 |
| C-2 | 数据 | appendPending 每笔保存无条件预构造降级行（全文 Buffer 拷贝，常态白付 MB 级拷贝） | document/journal.ts:122-126 |
| C-3 | 数据 | 孤儿 journal 归档 renameSync 无退避重试（win 瞬态占用下幽灵红不消） | state/state.ts:324-331 |
| D-1 | 校验 | tree_issues 缓存列名 `mtime_ms` 实存微秒值，持久 schema 命名与量纲不符（注释已声明不动因） | check/tree-issues-cache.ts:233 等 |
| D-2 | 格式 | style-migrate 序号播种正则大小写敏感，与同文件 isMdFileName 口径分裂（O_EXCL 兜底无覆写风险） | format/style-migrate.ts:100 |
| D-3 | 学习 | learn 收割长度门槛用 UTF-16 码元，与全库码位口径不一致（仅门控无劈切风险） | learn/index.ts:205-206,232 |
| E-1 | 服务 | 鉴权闸拒绝路径缺请求体排空钩子，与自订 R64-28 口径不一致（仅连接 churn） | studio/server/index.ts:312-399 多处 |
| E-2 | 服务 | providers/rag-providers/tiers 的 load→mutate→await save 残留跨请求丢更新窗（需跨进程锁争用，概率极低） | server/api/providers.ts:98-121 等 + ai/provider/store.ts:335 |
| E-3 | 服务 | rename 全量路径 clearChatHistory 裸 await 无防御（失败后目录已搬登记未动，可重启自愈；与删书路径不对称） | server/api/books.ts:691 |
| E-8 | 服务 | export 排队 waiter 不感知客户端断开，8 个弃请求可让导出 503 达 10min | server/api/io.ts:82-98 |
| E-12 | 服务 | learn-commit samples/quotes 数组无条数上限（1MB body 兜底，最坏一次写数千小文件于请求路径） | server/api/knowledge.ts:111-115 |
| F-1 | 前端 | 切书窗口内旧书 prefs 加载**失败**会清掉新书已回填的书级覆盖（catch 漏 gen 守卫；仅展示/节奏面） | web-next/stores/workspace.ts:101-119 |
| F-2 | 前端 | loadBookPrefs 未守 `r.prefs` 缺失，200 空信封即 TypeError + 浮调未处理 rejection（全局侧同款已修） | web-next/stores/workspace.ts:102,122 + api/prefs.ts:20 |
| F-3 | 前端 | prefs init 失败后 409 恢复把「全量默认值」当本窗修改回放，窄场景覆盖他窗非默认配置 | web-next/stores/prefs.ts:433-457 |
| F-4 | 前端 | boot 串行重试阻塞首屏挂载，纯离线白屏窗最长约 17s（已知取舍的量化提示） | web-next/api/client.ts:25-60 + main.ts:21 |
| G-1 | 组件 | MetaFormPanel.onSave 缺函数级在途锁（双触发双 PUT；同库其余面板均有） | web-next/components/panels/MetaFormPanel.vue:228-251 |
| G-2 | 组件 | CmHost 角色/物品补全名单只在切书时拉取，会话内新建/修改不进补全 | web-next/editor/CmHost.vue:371-388 |
| H-1 | 测试 | 用例级空洞测试无门（空洞门止于文件级；toBeDefined 179 + toBeTruthy 95 哨兵型弱断言） | scripts/check-counts.mjs:184-188 |
| H-2 | 工具 | eslint TS 面无类型感知规则 + prefer-const 显式关闭挂账（floating promise 零机器约束） | eslint.config.js:92-114 |

### 3. 观察×47（不确定/需产品拍板/已在代码内记档的取舍——不构成缺陷，逐条登记备查）

- A 域 ×9：timeoutMs 非正数钳制可达性未证（A-3）/ 降级 memo 键清理口径（A-4）/ store 单槽缓存多 path 抖动 + pricing 镜像文件名（A-5）/ failure 决策表动作零消费（A-6）/ MODEL_CATALOG 零消费预建（A-7）/ 预算闸 check-then-act 已两轮裁决（A-8）/ 读路径内联迁移写（A-9）/ it.return 不等待清理的明示取舍（A-10）/ book-rules.ts 头注过时（A-11）。
- B 域 ×3：渲染层 flush reject 归入「无钩子」路径绕过丢弃确认（B-3，渲染层是否会 throw 未证）/ prepare·summary 链同步 IO 在 server 子进程（B-5，架构取舍）/ mock 与 cc 事件总线双实现分叉（B-7，重构决策）。
- C 域 ×6：move 与 save 并发残余窗的「清单指回旧路径+双文件」后果形态（C-4，已在案取舍）/ words-diary 无 fsync（C-5，统计口径裁定）/ 开口标记非原子写自然收敛（C-6）/ Atomics.wait 同步壳仅 CLI 消费（C-7，须持续守护）/ withManifestLockAsync 重入不变量靠人工（C-8）/ scanCloudCopies 不跳内部目录（C-9，60s TTL 已节流）。
- D 域 ×5：writePieceList 已武装陷阱（D-4，接线前须先落保形补丁路径）/ knowledge 草稿模板字面 TODO（D-5，产品输出非代码债）/ learn-commit 外层空 catch 近不可达（D-6）/ docJoinKey 前置 replace 冗余（D-7）/ adjStack 正则残余回溯面已双防御（D-8，推演安全备案）。
- E 域 ×7：章节号入参只有下界（E-4）/ 生成类任务闸只查进程内（E-5，双进程互斥矩阵已知边界，即六十轮 B-2 同体重现）/ STORE_UNAVAILABLE 透传 e.message（E-6，IR-8 有意拍板）/ GET /books/:name 未对齐 resolveWithinRoot（E-7，依赖注册侧防线，零增量风险）/ SPA index 单槽缓存（E-9）/ 静态响应无 CSP（E-10，纵深项）/ stream-ticket 触顶逐出（E-11，注释登记取舍）。
- F 域 ×5：dirty 镜像键冒号歧义（F-5，已记档有 baseRev 兜底）/ 书级 prefs PUT 无乐观锁（F-6，备案后写者胜）/ discard 与在途保存迟到期回写（F-7，瞬态自愈）/ shelf 快照两字段无类型校验（F-8）/ apiFetch 重放 body 字符串假设（F-9，可加断言）。
- G 域 ×6：SearchPanel 行号键（G-3，需服务端口径）/ EditorDocHead 非空断言（G-4）/ ChapterTreeItem 双 ref（G-5，状态机互斥无实害）/ Shelf.vue Esc 无 overlay 让渡（G-6，两壳口径不一致）/ ContextMenu 无方向键导航（G-7，浏览器形态才暴露）/ index-key 残留 10 处均为只读展示（G-8）。
- H 域 ×6：vitest exclude 覆盖式丢默认排除面（H-3，补一行消隐患）/ e2e 顺序耦合固有级联红面（H-4，缓解充分）/ GET 豁免表三副本互锁（H-5）/ 无 dependabot（H-6）/ mac 仅 arm64 单腿发行决策（H-7）/ 脚本卫生微瑕（H-8）。

### 4. 证伪记录（代理候选经证据核验不成立，如实留痕防复评重复推演）

- D 域候选「metrics/style.ts 定稿过滤缺反斜杠归一」——证伪：docJoinKey→relPathKey 内部已做分隔符归一（safe-path.ts:169-182）。
- D 域候选「short-index scoreReversalQuality 结构损坏」——证伪：重读源码结构完好，系读取渲染伪影。

---

## 六、修复计划（评审止于评审——待作者开工指令后执行）

- **批 1（P2×1，主审亲修）**：R61-B-1 pickLibrary 两入口接入 probeDirReachable 预探 + 超时原生反馈 + 注入式用例（probe 超时→拒绝；对齐 switch-library 链 R54-A-2 口径）。
- **批 2（高价值 P3，建议 8 项，可四路文件互斥并发）**：
  - 甲 数据：C-1 journal 文件级读失败补 warn（对齐 R54-B-1）/ C-2 degradedFallback 惰性化（常态保存去 MB 级白付拷贝）；
  - 乙 桌面：B-2 readStore 读失败降级「无存储」+ warn；
  - 丙 服务端：E-1 排空钩子上提 createServer 回调统一挂（消 6 处不一致）/ E-3 rename 后 clearChatHistory 包 try/catch 降级对齐删书路径；
  - 丁 前端：F-1 catch 补 gen 守卫（一行）/ F-2 `r.prefs ?? {}` 对齐全局侧口径 / G-1 onSave 补函数级在途锁 / F-3 init 失败时 dirty 判定视为无本窗脏字段。
- **批 3（维持登记不修）**：其余 P3×17（A-1/A-2/B-4/B-6/B-8/C-3/D-1/D-2/D-3/E-2/E-8/E-12/F-4/G-2/H-1/H-2 及 B-8 关联 PM-12 待拍板）+ 观察×47（多为已在代码内如实记档的取舍）。
- 验收：批 1+批 2 逐项先红后绿 + 相关面回归；L2 终门九件套（vitest 全量 + 双 tsc + eslint + 三 check + build:web + e2e）；根 README 计数随新增测试修账。

---

## 七、覆盖度与如实声明

- G 域 ≈25 个组件（Settings 各 tab 全文、learn/overview/audit 子件、RelationDetail 等）仅过全域风险 grep 未逐行核验写操作守卫；F 域 9 个 views 壳视图仅接线点抽查；A 域 7 个纯声明文件未开；E 域 4 个只读聚合端点（overview/rhythm/foreshadows/progress ≈830 行）仅结构抽查、聚合算法内部正确性未逐行验证。
- H 域测试评估以「组织+模式+15 文件精读+全库 grep 统计」为界，955 文件未逐文件读。
- 平台面：本轮全部门禁在 mac 本机亲跑；win 真机回归按既定分工（D5/阶段 21）不在本轮范围。
- 子代理执行约束：≤5 并发全程遵守，零阵亡零重发；两波八域全部交卷。
- 本轮零代码零测试改动；根 README 测试计数免修账（check:counts 实测绿）。

（评审止于评审；评审轮零代码零测试改动、根 README 免修账〔check:counts 实测绿〕均为评审轮声明。修复批执行、README 修账与收口记见 §八。）

---

## 八、收口记（2026-09-07 修复批同日办结）

作者指令：「开始修复，编排下任务。」按 §六 计划执行，P1/P2 清零 + 回归全绿，**收口条件满足**。

### 1. 批 1（P2×1，主审亲修，先红后绿）

- **R61-B-1 pickLibrary 可达性预探**：`src/desktop/main.ts` pickLibrary 循环内、`isLibraryDir` 之前接入 `probeDirReachable(dir)`——命中 `unreachable` 即 `dialog.showErrorBox('目录无响应', …暂不可达…)` 并 `continue` 留在 E-9c 封顶循环重选。与切库链 R54-A-2 同款防线补齐「打开书库」/open-library IPC 两入口。
- 红测：`test/desktop/main.test.ts` 新增注入式用例——`fsPromisesMock.statGate` 返回永不落定的 Promise + `CLW_SWITCH_LIBRARY_PROBE_TIMEOUT_MS='150'`，断言 `{ok:false,canceled:true}` + errorBox 含「暂不可达」+ workdir.json 未被污染。修复前红（errorBox 断言失败），修复后绿 + desktop 全文件 75 过 1 跳 + tsc 0 错。

### 2. 批 2（高价值 P3×8，四路文件互斥并发代理，全修全配回归）

- **甲（数据）**：C-1 `journal.ts` findUnsettled 文件级读失败补 `log.warn('journal', …降级跳过…)` 后照旧 `return []`（对齐 R54-B-1 口径，语义不变留痕补齐）；C-2 degradedFallback 惰性化——预构造改 `() => string` 闭包，仅超阈值判定与锁超时降级兜底两消费点触发，常态保存去全文 UTF-8 拷贝；`appendLineAsync` 第三参扩展 `string | (() => string)` 向后兼容。**主审亲验惰性化等价前提：entry 为 const 局部对象，闭包定义后仅读 `entry.opId`，零变异，逐字节等价成立**。+2 测试文件 5 用例（红：C-1 warn 断言败 / C-2 构造计数败→绿）。
- **乙（桌面）**：B-2 readStore 读失败单独 try/catch——`log.warn('desktop', 'workdir.json 读取失败（按无存储降级）…')` + `storeCache = emptyStore()`（与 parseStore 失败同款：缓存降级结果不重读），消「parseStore 容错内容损坏 / loadWinState catch-all / readFileSync 抛错无人接」的不对称面。EISDIR 技法构造读失败，+1 测试文件 2 用例红→绿。
- **丙（服务端）**：E-1 排空钩子收敛入口单挂点——`createServer` 请求回调顶部 `res.on('finish', () => { if (!req.readableEnded) req.resume() })` 先于一切 replyError/return，六处闸拒绝路径（bad request 400/Host 403/OPTIONS 204/写 Origin 403/写 token 403/GET token 403）全覆盖；删 /api 分支（R64-28 原挂点）与 /API/ 404 分支重复钩子，全文件仅剩一处。**偏离记档（合理）**：static.ts 405 分支局部钩子保留不删——`createStaticHandler` 会被独立挂上裸 `http.createServer` 复用（static.test.ts 等 5 文件直挂形态，R65-47 钉住契约），双重挂载下 resume 幂等无副作用，注释已注明。红测双层断言（机制层 prototype 探针统计应用层钩子数——node ≥25 核心自动排空致行为层症状在本机不可复现，如实记档；行为层 keep-alive 同 socket 往返护栏守无核心自动排空运行时），红 6 失败/1 对照通过→绿 8/8。E-3 rename 后 clearChatHistory 包 try/catch + `log.warn('api', '改名清史失败（…改名已生效不回滚…）')` 后继续，对齐删书路径 L-S4；eventsMigrated 独立回传通道不受影响。红（500→期 200）→绿。+2 测试文件 8 用例；test/studio 全族 382 文件 2235 过 + 1 跳 0 败零回归。
- **丁（前端）**：F-1 workspace loadBookPrefs catch 首行补 `if (gen !== bookGen) return`（迟到失败先过切书代际守卫，不再清掉 B 书刚回填的覆盖值，对齐成功路径同款守卫）；F-2 `api/prefs.ts` `return r.prefs ?? {}`（对齐全局侧 R51-H-2 同款口径，200 空信封不再把 undefined 交给 Object.keys）；F-3 dirtyKeysOf `!lastPersisted` 时 `return []`；G-1 MetaFormPanel onSave 首行 `if (saving.value) return` 函数级在途锁。**前提修正记档**：报告 §五 F-3 原表述「init 失败（catch）lastPersisted 维持 null」不准确——亲验 `stores/prefs.ts` init 两条完成路径（迁移分支 ：190 置 prefs / else 分支 ：196 置 buildCache()，GET 失败 catch 不 return 会落 else）均置基线且此后不回 null，`!lastPersisted` 仅 init 未完成/未调用的防御窗可达；修复形态不变（`return []` 在该窗为安全保守语义），无合法路径依赖原「全量本地值当本窗修改」行为。+4 测试文件 9 用例红→绿；webnext 全族零回归 + typecheck:web-next 0 错。

### 3. 主审逐 diff 复核与汇总验收

- 四路 diff 逐行亲验零越界（工作树改动面恰为 9 源文件 + 1 测试改 + 9 新测试文件）；两处关键前提亲验（C-2 entry 不变异 / F-3 lastPersisted 路径）成立；丙路 static.ts 偏离决策核验合理记档如上。
- **+9 测试文件/+25 用例 → 964 文件 / 6280 收集 = 6280 过 + 4 跳 0 败**（113.70s）。
- **L2 终门九件套亲跑全绿**：vitest 964/6280 + 4 跳 0 败〔113.70s〕+ tsc/vue-tsc 0 错 + eslint 0/0 + 三 check 过〔根 README 修账 955/6255→**964/6280** 四处，check:counts 绿〕+ build:web 过 + e2e 43 过 2 跳〔28.5s〕。

### 4. 遗留与状态

- **批 3 维持登记不修**：其余 P3×17 + 观察×47（§六 原清单），待后续轮次按价值重评。
- 工作树（本批 9 源文件 + 10 测试文件 + 根 README 修账）已随 2026-09-07 作者指令「提交改动」提交 dev `22ecdb5d`（20 文件 +1441/−31），待提交项了结。
- 归档移动待确认闸（R60 报告同批）。
