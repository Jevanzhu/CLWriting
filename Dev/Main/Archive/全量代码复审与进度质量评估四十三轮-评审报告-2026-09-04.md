# 全量代码复审与进度质量评估四十三轮（评审报告）

- 日期：2026-09-04。
- 状态：**已收口**（修复批 R43-1~29 全处置 = 修 26 + 维持 1〔XB-3〕+ 挂账 1〔R43-25〕+ 证伪 1〔XD-6〕；终门九件套全绿：vitest 797 文件/5550 过 + 3 跳 0 败 + tsc/vue-tsc 0 错 + eslint 0 error〔31 warn 存量〕+ 三 check 过〔根 README 修账 788/5514→797/5550〕+ e2e 43 过 2 跳 + coverage 全桶过；收口记 §九）。
- 基线：HEAD `98d9d80`（dev 分支，四十二轮修复批收口提交）干净树。
- 作者指令（原文，同四十一轮引文）：「……修复完成之后，再重新评审一次，注意是重新！不要依靠之前的，从0开始再重新完成一次评审任务，并且形成文档，然后修复问题。」——本轮即该指令的最终一轮从零重评（四十二轮修复批收口后启动，全链禁读前轮正本、全新分轴）。
- 报告路径系原指令直接交付物按命名规范落盘（确认闸头注记，沿三十二/三十三/三十八/四十一/四十二轮口径；四十三轮正典名 + R43-N 编号；修复批随后执行）。
- 禁读口径：全链子代理与主评审均未读既有评审报告，从零独立评审；本轮分轴为**横切关注面**（写入原子性 / 异步并发 / 输入解析 / 前端状态渲染 / 错误处理降级 / 平台配置与测试工程），与四十一轮九域、四十二轮六域完全不同轴。
- 编号说明：本报告为 dev 分支链上的第四十三轮全量评审；四十二轮修复批（R42-1~42）经本轮横切深审**无回归**。

> 📦 **归档记（2026-09-06）**：随「归档 6 篇」批（作者确认闸拍板——链记已收口且修复批经核验在 dev 在位：四十三/四十四/四十五/四十六/四十九轮 + 全库代码重评审；四十七/PM/四十八三篇修复批未落 dev、待拍板了结前留暂存）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、评审口径与执行形态

### 1.1 任务编排

TodoWrite 全程编排：基线门禁沿用四十二轮收口终门（九件套全绿，零改动树）→ 六横切面禁读子代理**两波各三路**（小波次防 1302，沿用四十二轮成功形态）→ P2 候选主审逐条亲验（5 条全验：4 成立 + 1 降级）→ 待验证项主审补验（XD-6 证伪、XF-1 实证）→ 报告落盘 + 索引同步 → 修复批 → 终门。

### 1.2 六横切面划分与成活情况

| 横切面 | 评审方法 | 成活 |
|---|---|---|
| A 数据写入与原子性 | 全库写原语消费点分类核验（~120 直调/封装写点 + 3 SQLite 库）+ RMW/fsync/tmp 残留专项 | ✅ |
| B 异步与并发 | 定时器全量盘点（53 处）+ Promise 面 + Abort 传播 + 锁配对 + 事件循环同步 IO 可达面 + TOCTOU 残余 | ✅ |
| C 输入解析与校验 | 外部输入入口全盘点（HTTP/SSE/IPC/AI 输出/手编文件/子进程/env）+ 71 处 JSON.parse 逐点核验 | ✅ |
| D 前端状态与渲染 | 16 store 全读 + 编辑器集成层 + 乐观链/合并竞态/computed-watch/路由生命周期 | ✅ |
| E 错误处理与降级 | 空 catch 全库盘点（21 处逐点定性）+ 信封一致性 + 降级链 + 日志 + 重试/超时语义 | ✅ |
| F 平台适配+配置+打包+测试工程 | 平台分支矩阵（12 处三态）+ 打包面 asar 实物核验 + env 容错 + 测试平台分工 + scripts 门禁 | ✅ |

两波 3+3 全部首发成活**零阵亡**（连续第二轮全成活——小波次派工定型）。评审面 = src 全量 ≈9.7 万行 + 打包产物实物 + scripts 门禁逻辑。

### 1.3 亲验纪律

- **P2 候选×5 全部主审逐条亲验**：XA-1（files.ts:244-253 白名单 + PUT 临界段无锁 vs lead-finalize.ts 持锁 RMW 交叉核对——**成立**）；XB-1（SWEEP_SKIP_DIRS 仅 .git/node_modules + 注释自述「.版本 照扫」属有意但成本面失察——**成立**）；XC-1（chapter.ts fmVal 无 stringifyValue + 对照 frontmatter.ts:276 单源——**成立**）；XD-1（doc.ts:201-208 F8 契约性 false + HistoryPanel:102-108 误判链——**成立**）；XF-1（主审实证：check:counts 用 `vitest list --json` 长度直比；本机实测 list=5514=README 声称 ✓，mac 侧自洽；ubuntu 侧是否漂移取决于 skipIf(linux)×3 与 mac 侧跳过×3 的**巧合配平**——机理脆弱但当前不断裂，**降级 P3 挂账**，需 ubuntu 腿实测）。
- 待验证项补验：XD-6 证伪（providers.ts 与 rag-providers.ts 同源 `loadProviders(ctx.userDataPath)`，revision 同一计数器，前端共享单 ref 正确）；XE-2 泄漏面确认（适配器第一层 redact 在位，残余面为 runner 层非 GenError 抛出——纵深补齐合理，维持 P3）。
- 评审事故记档：XF-1 子代理在沙箱内重定向 `vitest list --json` 输出时误覆写 `test/document/r41-join-keys.test.ts` 与 `r1w3-trash-rollback.test.ts`（内容为 JSON dump、零断言），主审经 R76-40 零断言门当场发现并 `git checkout` 恢复，全树扫描确认无其它误写；check:counts 复跑过账（5514）。

## 二、门禁基线（沿用四十二轮收口终门，零改动树）

| 门禁 | 结果 |
|---|---|
| vitest 全量 | **788 文件 / 5514 过 + 3 条件跳，0 败** |
| tsc / vue-tsc | 0 错 / 0 错 |
| eslint | 0 error / 31 warn（存量口径） |
| check:counts / check:packaging / check:knowledge | 过 / 过 / 过 |
| e2e | **43 过 2 跳**（27.3s） |
| coverage | 全桶过（ai 95.69 / events 95.88） |

基线树含四十二轮修复批（R42-1~42）全部改动，经本轮六横切面从零深审**无回归**。

## 三、发现清单（R43-1 ~ R43-29 + 维持 1 + 证伪 1）

分级：P0×0 / P1×0 / **P2×4** / **P3×25**，共 29 项编号发现（另 XB-3 维持登记、XD-6 证伪、XB-6/7 既有登记复核无行动）。

### 3.1 P2（×4）

**R43-1〔both〕PUT /file 直写布线/关系线文件绕过布线锁——与定稿履历回写构成跨进程丢更新** — 横切 A，主审亲验成立

- 证据：`src/studio/server/api/files.ts:244-253`（writablePath 白名单放行 布线/、大纲/关系线/ 等 EDIT_DIRS；PUT 临界段直 atomicWriteFile **无锁**）vs `src/document/lead-finalize.ts:249-298`（同一文件持 `<file>.lock` 读→改→写 RMW）与 service.ts 三写路径（executeSave:385-405 / updateChapterMeta / updateDocMeta 均先取布线锁）。
- 触发：进程 B（CLI 批量定稿）持锁读到线索 C0 → 进程 A（HTTP 客户端/脚本）PUT 新内容 C1 落盘 → B 的 writeLead 以 C0+履历行覆盖 → **C1 无痕丢失**（快照留底的是覆写前 C0，C1 无任何副本）。布线账本系防吃书关键数据面。第一方前端当前仅 putContent 文风文件（树内文档走 saveContent 持锁），暴露面 = HTTP API 直调方。
- 定级：HTTP API 白名单即受支持写面（X-P2-14 只把 写作/正文 摘走），按「写协议矩阵漏一个写路径」族定 P2。
- 修复：PUT 临界段对 布线/、大纲/关系线/ 前缀路径取同名布线锁（acquireCrossProcessLockAsync，与 service 同序；超时 fail-closed 报锁忙可重试）。

**R43-2〔both〕崩溃 tmp 清扫在请求路径上全树同步扫描——同步盘上冻结事件循环数百 ms 至秒级** — 横切 B，主审亲验成立

- 证据：`src/fs/atomic.ts:332` `SWEEP_SKIP_DIRS = {'.git','node_modules'}`（有意不跳 .版本——tmp 确会残留该目录，但成本面失察）→ `src/state/state.ts:333` healthCheck 内 `sweepAbandonedTmpFiles(bookRoot)` → `/api/state`、`/api/overview` 请求路径（5s TTL 后）。对照同链 scanCloudCopies（exec.ts:317）已跳 .版本/.trash（X-P2-20「每书成百上千文件，进门全扫纯属浪费」）。
- 触发：SMB/坚果云卷 statSync 每文件 5-50ms → 进书/总览时事件循环冻结数百 ms-秒级，同进程全部书的 SSE/保存/生成停摆（R35-5/R35-6/R37-3 反复建立的纪律在本点漏网）。
- 修复：每书 TTL 节流（内存表，如 6h 至多一次）——崩溃 tmp 仍会被清扫，请求路径成本有界；SWEEP_SKIP_DIRS 同时补 `.trash`（tmp 不会残留于 .trash：trash 写走 renameWithRetry 无 tmp）。

**R43-3〔both〕AI 产出 fm 自由文本字段未走 stringifyValue 转义——`#`/`[`/`|`/纯数字值落盘读回静默损坏** — 横切 C，主审亲验成立

- 证据：`src/ai/contract/chapter.ts:101-117` `fmVal` 只 trim+去换行，对照系统正规写侧 `src/format/frontmatter.ts:276` `stringifyValue`（对含 `#`/`,`/`[`/纯数字/首尾空白加引号，与解析端 unquote 对称）。
- 触发：self-heal/对话写章 `submit_chapter` 产出标题「番外 #3」→ 读回被 stripInlineComment 截成「番外」；标题 `[终,局]` → 解析成数组；标题 `|`/`>` → 命中块标量分支吞掉后续 fm 行；纯数字标题 → number。draft-pipeline 原样落盘不重序列化 → 静默元数据损失（展示/导出标题行/近况复述全消费）。
- 修复：assembleChapter 的 fm 行改走 `stringifyValue(fmVal(...))` 单源。

**R43-4〔both〕恢复历史版本被 F8 排队保存的「契约性 false」误判为保存失败** — 横切 D，主审亲验成立

- 证据：`src/studio/web-next/src/components/panels/HistoryPanel.vue:102-108` `if (!saved) { toast('保存失败'); return }` vs `src/studio/web-next/src/stores/doc.ts:201-208` F8 契约：manual 遇在途保存 → await 落定后 dirty 已清 → **返回 false（内容实际已在盘）**。同型调用点 rewrite.ts:44-48（R34D-22）已补 `!saved && dirty` 复检，Y-9 新写的 HistoryPanel 未沿用。
- 触发：文档 dirty 且一次保存在途时点「恢复」→ 恢复被错误中止 + 「保存失败」恐吓性错误（实际保存成功）；再点一次即好。
- 修复：对齐 R34D-22——`if (!saved && doc.get(docId)?.dirty)` 才拦截。

### 3.2 P3（×25，分横切面明细）

**横切 A（3 项）**
- **R43-5**〔n/a〕`src/ai/calls.ts:297-303` `AI_CALLS_LOCK_TIMEOUT_MS` 违反 R30-18「const + 注入钩子」收口口径（全库唯一残留 export let）。修复 = const 化 + `__setForTest`。
- **R43-6**〔both〕`src/document/manifest.ts:197-206` 写前 .bak 影子 `fsync:false`——掉电可留撕裂 .bak，恰在其兜底场景（主清单损坏后的恢复源）失守。修复 = `{fsync:true}`（低频写，多一次 fsync 换影子确定可用）。
- **R43-7**〔both〕`src/document/lead-finalize.ts:259-296` 跨进程结构性移动 vs 定稿回写：writeLead 可在旧路径复活幽灵线索文件（同编号双文件）。修复 = writeLead 前锁内 existsSync 复核（源被搬走即放弃该条留 not-found），残窗注释登记。

**横切 B（3 项）**
- **R43-8**〔both〕`useSse.ts:198/172` doConnect 浮空 promise 无防御 catch（今日不可达 reject，未来加 await 即成断连链静默断）。修复 = `.catch(() => {})` 对齐同文件 probeSseBusy 纪律。
- **R43-9**〔both〕`useTheme.ts:60/68` ViewTransition ready/finished 无 catch——抢占时 overlaySweep 滞留 true 压制后续窗控色同步。修复 = 补 catch（finished.finally 前 .catch 空吞）。
- **R43-10**〔both〕`src/document/manifest.ts:314-345` withManifestLockAsync 重入计数的安全性依赖「fn 体零 await」未声明不变量（今日全库 fn 均同步，未来加一个 await 即静默失效且无测试红）。修复 = 注释显式声明 + dev 断言返回值非 thenable。
- **（维持）XB-3** `prefs.ts:451` syncOverlayDelayed 单发 setTimeout 不跟踪——单发 + 非 win 短路，无泄漏；维持现状注释登记。

**横切 C（3 项）**
- **R43-11**〔both〕`StartupNoticeBanner.vue:12-19` localStorage 解析缺 Array.isArray 守卫（兄弟实现 check.ts:41 有）——非数组 JSON 渲染期 TypeError。修复 = 对齐 isArry+typeof 过滤。
- **R43-12**〔both〕`src/state/state.ts:560-561` `.confirm.json` chapter 裸 as 强转——字符串/小数形态绕过章号校验链（`no === chapterNum` 严格等恒 false）。修复 = typeof+isSafeInteger+>0 三连守卫。
- **R43-13**〔both〕`analysis.ts:216/237` parseInt 与 `manifest.ts:158` Number 提取章号绕过 parseChapterFileName 的 isSafeInteger 守卫（16+ 位数字名 → 1e20 失真值入趋势/定稿集合）。修复 = 三处收敛单源或补守卫。

**横切 D（4 项）**
- **R43-14**〔both〕`workspace.ts:103-111` loadBookPrefs 失败路径清了书级覆盖但不 apply——`--page-width` 滞留前书。修复 = 清后补 `ps.apply()`。
- **R43-15**〔both〕`CmHost.vue:194-204` 组合期外部替换被双组合间隙 <1 帧守卫丢弃后无再触发路径（注释宣称的「watch 再触发」不成立）——clean 文档的他窗编辑被本地静默覆盖（窗口极窄）。修复 = 被丢弃时保留 `pendingExternal` 挂起标记由下一次 compositionend/watch 消费。
- **R43-16**〔both〕`doc.ts:240-243` save 成功不推进 treeRev——下一次树刷新把自客户端保存当外部变更整批重拉（最多 20 冗余 GET）。修复 = doSave 成功回填当前 tree.revision。
- **R43-17**〔both〕`EditorView.vue:104-112` 切文档瞬间字数短暂显示上一文档值（150ms 防抖窗）。修复 = 切 docId 同步重算初值。
- **（证伪）XD-6** providers/rag-providers revision 共享单 ref——两端点同源 loadProviders 同一计数器，前端共享正确，非缺陷。

**横切 E（7 项）**
- **R43-18**〔both〕rag 域六处 ROLLBACK 未防自身抛错（`rag/index.ts:259/392/613/674` + `rag/store.ts:290` + `cache/sync.ts:56`）——SQLite 自动回亡后 ROLLBACK 抛「no transaction is active」掩蔽原始写错误、降级文案被裸错替换（R61-10 已修 events/check/cache 三域，rag 漏网）。修复 = 六处统一 `try { ROLLBACK } catch { /* 已自动回亡 */ }`。
- **R43-19**〔both〕SSE 错误事件脱敏纵深不一致：self-heal/chat 路径（self-heal.ts:858/973、turns.ts:493、finish.ts:56、chat.ts:143/176）未过 redactSecret——与 stream.ts:216（R26-8）同族文本一脱一不脱。修复 = 统一补第二层 redactSecret。
- **R43-20**〔both〕`self-heal.ts:340-349` 连写暂停元状态落盘失败零留痕（用户重开书见连写无声停半途）。修复 = catch 补 log.warn（R66-4 留痕纪律）。
- **R43-21**〔服务端〕`stream.ts:380-409` SSE 消费循环异常只报客户端、服务端日志无痕（排障依赖用户转述）。修复 = 两处 catch 补 log.error（对齐 router.ts:104 纪律）。
- **R43-22**〔win 高发〕`learn/index.ts:257` 候选目录清理吞错注释失实（「不存在无所谓」不可达；真会抛的 EBUSY/EPERM 半删态留陈旧候选）。修复 = 注释改真实口径 + 失败 log.warn 提示残留。
- **R43-23**〔服务端〕`documents.ts:82-111` 伏笔事件差分链两处吞错零留痕（快照失败 → 伏笔审计事件整组静默跳过）。修复 = 两处补 log.warn（对齐 R67-7）。
- **R43-24**〔服务端〕rewrite/outline 端点把 out.code 坍缩成 500 GEN_FAIL（NO_PROVIDER/ABORTED 等语义丢失）。修复 = 透传 out.code 并按族映射状态（NO_* → 400）。

**横切 F（5 项）**
- **R43-25**〔n/a〕**XF-1（主审降级）**：check:counts 单测数对账的跨平台机理脆弱——list 计数排除平台 skipIf 用例，macos/ubuntu 两腿各自排除 3 条**恰好配平**才同为 5514；任一新增平台条件用例即破一腿。主审实证：本机 list=5514=声称 ✓（ubuntu 实测不可得）。**挂账**：CI ubuntu 腿实测 + 建议平台条件跳过收敛为运行期 skip 形态使计数平台不变（~70 点位，独立小批）。
  **作者裁决（2026-09-04）**：暂无 Linux/Ubuntu 适配计划——skipIf 收敛小批不排期（动机系多平台腿计数不变性，随单平台开发形态冻结）；ubuntu 腿不专门触发实测（check:counts 在 ci.yml ubuntu 矩阵腿自动跑，下次 PR→main 自然背书）；打包面 desktop.yml 本无 linux 腿（头注「linux 仍暂缓」），与裁决一致零改动；代码内 linux 第三态显式分支维持（默认路径防御非适配工作，拆除系零收益 churn）。挂账转**裁决冻结**，未来启动 Linux 适配再启。
- **R43-26**〔both〕dev-only 环境变量无 isPackaged/fork 剥除防线：`main.ts:577/1072` CLW_DEV_UI 泄漏进打包态 → 主窗白窗假死 + **CSP 注入被跳过**；`CLWRITING_RESOURCES_DIR` 残留读错资源根。修复 = main.ts 三处加 `!app.isPackaged` 守卫 + server-manager env 剥除名单补 CLW_DEV_UI/CLW_DEV_CORS。
- **R43-27**〔n/a〕`test/governance/coverage-threshold-globs.test.ts:33-34` EXCLUDE 抄本漏 `vite.config.ts`（与 vitest.config 漂移）。修复 = 抄本补齐。
- **R43-28**〔win〕`test/format/r40-batch-b1.test.ts:81` win-only 用例注释仍说「realpathSync 归一」而实现已改 trueCasePath（readdir 逐段）。修复 = 注释对齐。
- **R43-29**〔n/a〕`test/e2e/spec-order.guard.test.ts:34` localeCompare 未钉显式 locale——顺序契约的跨平台 collation 假设未锁死。修复 = `localeCompare(b, 'en')` + 注记。

## 四、平台适配专项结论（mac/win）

**win 侧**：平台分支矩阵 12 处三态（win32/darwin/linux）逐点核对无单向漏配；trueCasePath 归一/env 大小写剥除/WCO/PowerShell windowsHide/win-only 真夹具 CI 实跑均成体系；**打包面经 asar 清单实物核验自洽**（dependencies 收进 asar + font-list 智能解包，代理曾怀疑 node_modules 冗余经查撤回）。残余：R43-26（CLW_DEV_UI 族泄漏——dev 机污染打包态时 win/mac 同伤，且叠加 CSP 跳过）与 R43-25 的 win 豁免逻辑若属误诊则名不副实（挂账实测）。

**mac 侧**：darwin 专属值（hiddenInset/zoom role/app 菜单）边界清晰、linux 第三态显式处理；大小写敏感卷三层收口（探测+警告+披露）方向正确；NFD 由 docJoinKey/净化入口双侧归一。残余同 R43-26。

**跨平台**：横切 A 的写原子性、横切 B 的锁配对、横切 C 的输入容错经全库盘点无平台分叉缺口；R43-1/R43-2/R43-3 为平台无关的数据面/成本面缺陷。

## 五、进度结论

**总体 ≈98%（mac 生产形态 ≈98.5% / win RC ≈97.5%）**，与三十八~四十二轮口径一致（本轮 0×P0/0×P1；P2×4 中 2 条为横切新角度首次曝光的既有存量〔R43-1 写协议矩阵 API 面漏网、R43-3 AI 输出序列化单源未闭环〕，1 条成本面〔R43-2〕，1 条前端调用契约〔R43-4〕——四十二轮修复批 42 项经横切深审零回归）。

- 横切面完成度：A 95% / B 96% / C 95% / D 92% / E 96% / F 93%。三轮连续 P0/P1 为零，P2 收敛在 3-6 条窄面带。
- 开放任务（对照总览第三节，维持不变）：阶段 13 relay 真机验证（待凭据）、阶段 21 J6b mac 跨机实机确认、CI 形态/签名公证（作者拍板）、新增挂账：R43-25（CI 腿计数机理实测——**2026-09-04 作者裁决暂无 Linux/Ubuntu 适配计划，转裁决冻结**，见 §三）、R42-19（同步 XHR 实机复验）。

## 六、质量结论

**A−（8.9/10）**。依据：

- 门禁九件套全绿基线（788 文件/5514 单测 0 败 + e2e 43 过 2 跳 + 双 tsc 0 错 + eslint 0 error + 三 check 过 + coverage 全桶）；
- 横切面画像正面结论罕见地一致：写原子性「我见过的写纪律最完备的代码库之一」（XA）、竞态防御「几乎每一处曾有的竞态都有编号修复注释与回归锚点」（XB）、输入层「42 轮迭代后罕见地干净」（XC）、错误处理「成熟度最高的横切面之一」（XE）；
- 扣分项 = R43-1（写协议矩阵的 API 面漏网——跨进程丢更新可达）+ R43-3（AI 输出面序列化单源未闭环——静默元数据损坏）+ R43-2（最高频入口上的同步成本面）+ R43-26（CSP 跳过的 dev env 泄漏面）。
- 三轮从零重评（四十一/四十二/四十三）合计发现 16+42+29 = 87 项，P0/P1 恒零、P2 收敛于窄面，收敛趋势成立。

## 七、修复计划（批 A-B，文件互斥）

| 批 | 项 | 文件面 | 修法 |
|---|---|---|---|
| A | R43-1 | studio/server/api/files.ts | PUT 临界段对 布线/、大纲/关系线/ 前缀取布线锁（与 service 同序，超时 fail-closed 锁忙可重试）；补跨进程丢更新回归 |
| A | R43-2 | state/state.ts + fs/atomic.ts | sweep 每书 TTL 节流（6h 内存表）+ SWEEP_SKIP_DIRS 补 .trash；补节流回归 |
| A | R43-3 | ai/contract/chapter.ts | fm 行 stringifyValue 单源；补 `#`/`[`/`\|`/纯数字标题 round-trip 回归 |
| A | R43-4 | web-next HistoryPanel.vue | `!saved && dirty` 复检（对齐 R34D-22）；补契约性 false 回归 |
| B | R43-5 | ai/calls.ts | const 化 + __setForTest |
| B | R43-6 | document/manifest.ts | .bak fsync:true（与 R43-10/R43-13 同文件合批） |
| B | R43-7 | document/lead-finalize.ts | writeLead 前 existsSync 复核 + 残窗注释 |
| B | R43-8/9 | web-next useSse.ts + useTheme.ts | doConnect catch / ViewTransition catch |
| B | R43-10 | document/manifest.ts | 重入不变量注释 + dev 断言非 thenable |
| B | R43-11 | web-next StartupNoticeBanner.vue | Array.isArray 守卫 |
| B | R43-12 | state/state.ts | .confirm.json chapter 三连守卫（与 R43-2 同文件合批） |
| B | R43-13 | studio/server/api/analysis.ts + document/manifest.ts | 章号提取 isSafeInteger 收敛（manifest 与 R43-6/10 合批） |
| B | R43-14~17 | web-next workspace/doc/EditorView/CmHost | apply 补调 / treeRev 回填 / 字数初值 / pendingExternal 保留 |
| B | R43-18 | rag/index.ts + rag/store.ts + cache/sync.ts | 六处 ROLLBACK try-catch（R61-10 模板） |
| B | R43-19~21 | self-heal.ts + turns.ts + finish.ts + chat.ts + stream.ts | redactSecret 第二层 / 暂停留痕 / SSE 服务端 log |
| B | R43-22 | learn/index.ts | 注释真实口径 + warn |
| B | R43-23 | studio/server/api/documents.ts | 两处 warn |
| B | R43-24 | studio/server/api/rewrite.ts + outline.ts | out.code 透传 + NO_* → 400 |
| B | R43-26 | desktop/main.ts + desktop/server-manager.ts | CLW_DEV_UI isPackaged 守卫 + env 剥除名单 |
| B | R43-27~29 | test 三文件 | EXCLUDE 补齐 / 注释对齐 / localeCompare 'en' |
| 维持 | XB-3 | — | 单发定时器不跟踪，注释登记 |
| 挂账→冻结 | R43-25 | — | 作者裁决（2026-09-04）暂无 Linux/Ubuntu 适配计划：实测由 ci.yml ubuntu 矩阵腿自动背书、skipIf 收敛不排期 |
| 证伪 | XD-6 | — | 同源计数器实证，记档 |

执行口径：批 A（P2×4）亲修先行；批 B P3×21 按文件互斥四组并行；每项补回归；终门九件套。

## 八、收官记录

按作者同文指令，本轮修复批收口后本会话评审链收官（四十一修复 → 四十二从零重评+修复 → 四十三从零重评+修复，三轮闭环）。


## 九、收口记录（2026-09-04，修复批办结）

修复批 R43-1~29 全处置（修 26 + 维持 1 + 挂账 1 + 证伪 1），+9 测试文件/+36 用例；根 README 修账 788 文件/5514 单测 → 797 文件/5550 单测。执行形态：批 A（P2×4）主审亲修 + 批 B（P3）四路文件互斥并行子代理（后端核心 / rag·SSE 错误面 / 前端 / 平台与测试工程）全部首发成活。

- **R43-1**：PUT /file 临界段对 布线/、大纲/关系线/ 前缀取同名布线锁（wiringLockKeyForPut 与 service/lead-finalize 逐位同口径，5s 超时 fail-closed 409 可重试）；回归 test/studio/r43-put-wiring-lock（锁占用拒写不覆盖 + 释放后直写）。
- **R43-2**：sweep 每书 6h TTL 节流（`__resetSweepThrottleForTest` 钩子）；**方案偏离记档**：.trash 不入跳过表——实查 trash-manifest 本身是 atomicWriteFile 目标（工作区/.trash/ 内），其崩溃 tmp 须在清扫面，成本由节流有界。回归 test/state/r43-sweep-throttle。
- **R43-3**：assembleChapter fm 行改 `stringifyValue` 单源（escape-unquote 对称闭环）；回归 test/ai/r43-fm-roundtrip 六臂（#/[/|/纯数字/常规不虚胖）。
- **R43-4**：HistoryPanel 恢复前置保存改 `!saved && dirty` 复检（对齐 R34D-22）。
- **R43-5**：AI_CALLS_LOCK_TIMEOUT_MS const 化 + 钩子。
- **R43-6**：writeManifest .bak 影子回默认 fsync:true（静态锚回归）。
- **R43-7**：writeLead 前锁内 existsSync 复核（幽灵复活残窗收敛至复核-写入微窗）；node:fs 注入手法回归。
- **R43-8/9**：safeDoConnect 防御 catch / ViewTransition ready·finished 补 catch（overlaySweep 复位回归）。
- **R43-10**：重入不变量注释收口——**未装 dev 断言**（fallback 记档：r35-manifest-lock-async 既有裁定用例显式传 async fn 锚定「锁覆盖 fn 整个执行期」，断言将推翻既有裁定）。
- **R43-11~13**：StartupNoticeBanner 数组守卫 / .confirm.json chapter 三连守卫 / manifest·analysis 章号 isSafeInteger 三处。
- **R43-14~17**：loadBookPrefs 失败补 apply / CmHost 组合期丢弃分支保留 pendingExternal（自愈链，双应用由等值检查兜底）/ doSave 成功回填 treeRev / EditorView 切文档字数即切。前端组 8 用例含变异验证（还原 5 文件 7/8 转红）。
- **R43-18**：rag 域六处 ROLLBACK try-catch（含 cache/sync SAVEPOINT 对的语义差异注释）；回归两文件。
- **R43-19**：self-heal/turns/finish/chat 五处 SSE 错误面补 redactSecret 第二层（sk- key 掩码回归）。
- **R43-20~23**：暂停落盘留痕 / SSE 服务端 log.error / learn 清理真实口径+warn / 伏笔差分链两处 warn。
- **R43-24**：rewrite/outline 透传 out.code——NO_* → 400、ABORTED → 499（无既有先例，选「请求被取消」语义，文案不变）；outline-onboard-api 既有 500 断言随契约演进更新。
- **R43-26**：CLW_DEV_UI 三处读取 + `!app.isPackaged`（isPackaged 系属性非方法，按 electron 语义实现）；server-manager 剥除名单补 CLW_DEV_UI/CLW_DEV_CORS/CLWRITING_RESOURCES_DIR；打包态 CSP 恒注入回归。
- **R43-27~29**：coverage EXCLUDE 抄本补齐 / r40-batch-b1 注释对齐 trueCasePath / spec-order localeCompare 钉 'en'（排查确认 check-counts 无同款排序不需对齐）。
- **维持**：XB-3（syncOverlayDelayed 单发不跟踪，注释登记）。**挂账**：R43-25（2026-09-04 作者裁决暂无 Linux/Ubuntu 适配计划，转**裁决冻结**——ubuntu 计数实测由 ci.yml 矩阵腿下次 PR→main 自动背书、skipIf 收敛 ~70 点位不排期）。**证伪**：XD-6。

终门（全量亲跑）：vitest **797 文件/5550 过 + 3 跳 0 败** + tsc 0 错 + vue-tsc 0 错 + eslint 0 error（31 warn 存量）+ check:counts 过（修账后零漂移）+ check:packaging 过 + check:knowledge 过 + e2e **43 过 2 跳**（27.1s）+ coverage 复跑过（全桶零违例）。
