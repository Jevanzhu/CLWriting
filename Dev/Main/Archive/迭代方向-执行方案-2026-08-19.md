# 迭代方向·执行方案

- **日期**：2026-08-19
- **依据**：`03-设计/迭代方向-设计方案-2026-08-19.md`（技术设计 + 决策点 P1-P10）← `03-设计/迭代方向-方向方案-2026-08-19.md`（方向上游）
- **状态**：**已完成（2026-08-20，Dev-2 分支批 0-7 全落地 + 批 8 P3 闸实测关闭 + 收口门全过）**——批序与依赖见第二节；执行记录见第七节

---

## 第一节 开工闸（三条，全部满足才开工）

1. **决策拍板**：设计方案 P1-P10 至少拍板 P1/P2（批 1 前置）、P5/P6（批 6 前置）、P7（批 2 前置）、P9/P10（批 5 前置）；其余可随批拍。
2. **在途收口（阶段 19 重建收口）**：原「dev 工作区 22 改动 + 3 新增」已毁于 2026-08-19 工作区覆写事故（jj 轮评审子代理误 checkout；见 `工作区事故与重建-执行方案-2026-08-19.md`）——开工闸改为**阶段 19 重建收口**（S1-S7 完成 + S6 验证矩阵全过 + 提交落盘）；本方案全部批次在干净基线上跑（提交树 worktree 口径）。
3. **无共管波次**：开工批与并行会话无共管文件（第六节让路表逐批核对；让路原则沿巨石拆分批先例——共管文件对方在途则让位或合流，不并行改）。

## 第二节 批次总表

| 批 | 内容（设计出处） | 依赖 | 预估 | 发布硬伤 |
|---|---|---|---|---|
| 0 | A4 结构化日志 + 迁移失败横幅（§二A4） | 无 | 1 人日 | **是**（打包后失明） |
| 1 | A1 树红点增量缓存（§二A1） | P1/P2 拍板 | 2 人日 | **是**（打开书冻结） |
| 2 | C1 章摘要生成器（§四C1） | P7 拍板 | 2 人日 | 否 |
| 3 | C2+C3+C4 上下文域收口（§四C2-C4） | 批 2 | 2 人日 | 否 |
| 4 | D1 用量卡片（§五D1） | **巨石拆分批 7 收口** | 1 人日 | 否 |
| 5 | D2+D3 价格表与预算双口径（§五D2/D3） | 全局默认值批对齐 + P9/P10 拍板 | 3 人日 | 否 |
| 6 | B1-B4 语料回归域（§三B1-B4） | P5/P6 拍板 | 3-4 人日 | 否 |
| 7 | A3 RAG 惰性指纹 + 范数（§二A3） | P4 拍板 | 1-2 人日 | 否 |
| 8 | A2 worker 化（§二A2） | **P3 闸**：批 1 落地后实测打开书 p95 冻结仍超阈值 | 3-4 人日 | 否 |
| 收口 | 全量回归 + 打包态冒烟 + 文档链状态翻转 | 全批 | 0.5 人日 | — |

批 0/1 优先（硬伤）；批 2-7 之间无相互依赖，可按拍板节奏任意插入；批 8 是条件批，可能永不开。

## 第三节 各批细节（文件级落点）

### 批 0：A4 日志通道 + 迁移横幅

- **落点**：新 `src/log/index.ts`（JSONL 追加 / 串行队列 / 7 天清理 / dev 镜像 console）；grep 替换 src 下 50 处 `console.error/warn`（37+13 清单在设计依据探查记录，逐处保 tag）；`src/events/store.ts:399` 迁移失败 + `src/studio/server/index.ts` 启动迁移失败收集为 startupNotices；新 `GET /api/startup-notices`；前端 App 级横幅组件（一次性，关闭后不再弹）。
- **测试**：log 模块单测（队列串行 / 轮转清理 / err 序列化）；startup-notices 端点集成测（迁移失败注入 → 横幅数据）；替换点抽样走查（tag 不丢）。
- **回退**：logger 失败自动降级 console（fail-open——诊断通道不允许成为新故障源）。

### 批 1：A1 树红点增量缓存

- **落点**：`src/cache/schema.ts` 加 `tree_issues_cache` 表（DDL 见设计 §二A1）；`src/check/run.ts` collectTreeIssues 循环内先 stat 后算 + 回写；`src/document/tree.ts` invalidateTreeIndex 挂整表清空。
- **测试**：扩展 `test/check/tree-issues-scan-count.test.ts`（二次请求整读次数 = 变更章数）；**golden 对照**：随机组合改写/驳回/移动后与无缓存全量重算逐字节一致。
- **回退**：表缺席或读失败 → 跳过缓存直接现行路径（语义无损降级）。

### 批 2：C1 章摘要生成器

- **落点**：`src/ai/tasks` 注册 `summary-chapter`；finalize 管线挂 best-effort 钩子（失败 log.warn 不阻断）；`src/process/prepare.ts` 近章摘要缺失时按需补生成（计入 calls_per_chapter）；产物 `定稿/摘要/章摘要/` + front matter sourceHash；备料注入走 visibleInjections 登记 + verifyVisibleRecorded 治理测试扩条目。
- **测试**：生成器 mock driver 单测（预算截断 / 失败降级 / 自愈补漏）；注入登记治理测试；sourceHash 过期判定。
- **回退**：钩子整体可配开关（book.yaml `summary.auto: false` 关闭即回到手写约定现状）。

### 批 3：C2+C3+C4 收口

- **落点**：`summary-volume` 任务 + prepare rank-3 按需生成（章摘要不全不强行生成）；`api/outline.ts` prompt 增「当前卷进展」段（缺失省略 + promptMeta 留痕）；新 `scripts/calibrate-tokens.ts` + `prepare.ts` estimateTokens 按模型查表（0.6 兜底，常量注明测定日期）。
- **测试**：卷摘要输入=章摘要链的单测；细纲 prompt 含/缺卷摘要两态；校准脚本对事件库样本的拟合输出快照测试。
- **回退**：三者互为独立开关（卷摘要/细纲段/系数表），逐项可关。

### 批 4：D1 用量卡片（开工闸：巨石拆分批 7 收口）

- **落点**：WorkbenchView 新用量卡片组件（独立子组件，遵循分域拆分范式）；消费既有 trace-stats byTask + byDay；**后端零改动**。
- **测试**：组件测（数据形状/空态）；e2e 补一条（工作台用量卡可见）。
- **回退**：纯展示组件，摘除零影响。

### 批 5：D2+D3 价格与预算双口径

- **落点**：providers.json 加 `pricing`（provider 级 + models 级覆盖，加性）+ 设置页 AI 域价格编辑（对齐全局默认值批三层链口径）；新 `GET cost-stats` 端点（llm/call × 价格聚合，未配价返回 enabled:false）；`stream.ts:106` usage 事件有价算 cost 无价省略字段；`calls.ts` checkAiCallBudget 扩 tokens/cost 双检查 + book.yaml/budget 键 + 全局默认链登记；`review.ts:139` 三审降档取最紧口径。
- **测试**：价格解析（缺省继承/覆盖/非法值拒绝）；cost-stats 聚合正确性（cacheRead 分档计价）；预算双口径拦截含「无价格时 cost 口径静默不生效」；三审降档最紧口径单测。
- **回退**：pricing 未配置时 D2/D3 全链路等价于现状（加性扩展红线）。

### 批 6：B1-B4 语料回归域

- **落点**：`types.ts` 加 `check/false-positive` 事件类型 + ws 会话承载；新端点 check-false-positive（excerpt 服务端切 ±50 字、上限 200 字、幂等）；CheckPanel 行内误报动作（一次确认）；新 `scripts/harvest-corpus.ts`（.版本 快照 × 机检 × 幸存者判定 → 工作区/语料候选/）+ `npm run corpus:commit` 固化 `test/corpus/checks/<checkId>.json`；新 `test/check/corpus.test.ts` 回归门；`runner.ts:152` 供给链插 deriveLeakKeywords（账本 front matter `leak_keywords` 键）。
- **测试**：事件落库形状；excerpt 切片边界；自举脚本对构造版本档案的判定正确性（幸存/消失两路）；回归门空目录 skip / 入库生效两态；派生供给三级回落（显式 > book.yaml > 派生 > 空）。
- **回退**：误报按钮与回归门解耦——按钮不上线不影响既有用例入库；派生键未声明时语义与现状完全一致。

### 批 7：A3 RAG 惰性指纹 + 范数

- **落点**：`src/rag/schema.ts` chunks 加 norm 列（版本递增 + 打开库惰性回填）；`buildIndex` 写 norm；`store.ts` cosine 退化点积；`index.ts` recall 改 top-K'=20 候选 → 子集指纹校验 → 剔除递补。
- **测试**：scale 测试加断言（单次召回正文文件读 ≤ 20）；top-5 与现行实现对照一致；norm 回填幂等。
- **回退**：K' 走 book.yaml 可覆盖（`rag.candidate_depth`，缺省 20）。

### 批 8：A2 worker 化（P3 条件批）

- **落点**：新 `src/studio/server/labor/`（单 worker 串行队列 + 超时降级同步路径）；tsup 加 worker 入口；`api/state.ts`、`api/check.ts`、overview 缓存层改走 job 提交。
- **测试**：job 往返/超时/降级三态单测；大书 mock 下 SSE 心跳不断（集成）。
- **回退**：worker 启动失败自动回落同步路径（fail-closed 到现状）。

## 第四节 门禁（每批独立全绿）

- tsc + vue-tsc 零错；全量 vitest（**提交树 worktree 口径**，巨石拆分批 0 先例）；`check:counts` README 对账；动前端批（0/4/6）加 e2e；每批一提交，提交信息带批号与设计出处节号。

## 第五节 并行让路约定

| 共管面 | 对方 | 约定 |
|---|---|---|
| events/store.ts、check 域（runner/count）、前端 14 .vue | 工作区事故重建（阶段 19，执行中，最高优先） | 批 0↔S4、批 1↔S5、批 4↔S3、批 6 B4↔S5——开工前核对重建步骤是否在途，在途则让位待其提交；S5 落地后批 6 B4 基于其落地物开工 |
| WorkbenchView / EditorView / OnboardView | 巨石拆分批 7（顺延中） | 批 4 整批待其收口后开工；若本方案先动工其他批触及三视图，仅限非拆分区域 |
| settings 域 / book.yaml 三层偏好链 | 全局默认值批（在途） | 批 5 开工前对齐其落地物清单；预算键默认值登记走其 applyGlobalDefaults 口径 |
| events/store.ts、types.ts | 并行会话活跃期 | 批 0/6 触及事件库——开工前 grep 对方在途 import；同文件对方在途则让位 |
| check/run.ts / count.ts | 巨石拆分遗留（无） | 无冲突，正常动工 |

## 第六节 收口门

1. 设计方案第七节验收总清单逐条勾验；
2. `build:desktop` 打包态冒烟：日志文件产生、迁移横幅路径可见、无新 console 直印；
3. 全批提交树全量门禁绿 + e2e 全过；
4. 文档链收口：本方案状态翻「已完成」、设计方案移 `02-执行/`（方案归位口径：已开工设计转执行域）、总览地图/实施顺序阶段 18 状态同步、README 计数对账。

## 第七节 执行记录（2026-08-20 完工回填）

开工闸核验（2026-08-20）：①P1-P10 全部按设计方案建议拍板（均①，P4=K'20 写死 + book.yaml `rag.candidate_depth` 可覆盖）；②阶段 19 重建收口——代码+CI 面已收（S0-S6 全过、S7 三子项均已处置：章号口作者拍板维持、AuditView 已修、docs/ 三件经作者指令归档 7c88e2e）；③共管波次核对无冲突（巨石批 7 顺延无人认领、全局默认值批已落地、并行会话活跃文件 test/desktop/main.test.ts 与本方案零交集——提交全部显式路径添加防误收）。

- **批 0**：✅ 63d9906——src/log/ JSONL 通道 + 47 处 console 替换 + startupNotices 端点 + App 横幅；11 新测试
- **批 1**：✅ 0b55d48——tree_issues_cache 双指纹（正文 stat + 裁决信封）+ 全局纪元 + invalidateTreeIndex(structural)；golden 对照 + scan-count 扩展
- **批 2**：✅ db615bf——summary-chapter 任务 + 定稿 best-effort 钩子（API 层，依赖方向守门）+ 备料自愈 + promptMeta.files 可见⟺已记录；11 新测试
- **批 3**：✅ 2fc2548——summary-volume（链不全不强行生成）+ 细纲「当前卷进展」段 + token-calibration 拟合/脚本 + estimateTokens 按模型查表；8 新测试
- **批 7（提前执行）**：✅ 195286c——chunks.norm 列（DDL/ALTER/惰性回填幂等）+ recall 倒序校验（过滤闸语义，top-5 等价锚定）+ scale 断言「单次召回正文整读 ≤20」
- **批 5**：✅ b21ab9e——pricing（provider/models 两级）+ cost-stats 端点 + SSE cost 可选化 + pricing 编辑小节 + budget 双口径三层链 + checkAiCallBudget 三口径 + review 取最紧；13 新测试
- **批 6**：✅ c5877c5——误报按钮/端点/事件 + harvest-corpus 幸存者判定 + corpus:commit + 语料回归门（随库种子 2 条）+ leak_keywords 账本派生三级供给；8 新测试
- **批 4**：✅ ad7e83f——WbUsageCard（byTask 表格 + byDay sparkline + D2 金额两态）；组件测 4 + e2e 1（最小侵入落点，巨石批 7 拆分时可整体平移）
- **批 8**：❌ 不开工（P3 闸实测关闭）——700 章基准（Apple Silicon，2026-08-20）：冷启（无缓存全量）540ms、热路径 20-27ms、改 1 章后重查 80ms——「打开书冻结」已被批 1 消除到远低于感知阈值，worker 化（根治方案）按 P3-① 判定无必要；留大书用户反馈驱动再议
- **收口**：✅ 全量 285 文件 / 2613 单测绿 + e2e 38 过 2 跳 0 败（含批 4 新 spec）+ check:counts 对账（README 2613/285/27/40）+ 编译产物冒烟（dist/server-main 启动产生 userData/logs JSONL + startup-notices 可达 + 无 token 写 403）+ 本节回填 + 设计方案归位 02-执行/ + 总览阶段 18/19 状态同步
