# 六十轮修复批复审——专项评审

> 📦 **归档记（2026-09-08）**：已收口，随「README 整理与清理」批移入 `../Archive/`（扁平冷库；确认闸 2026-09-08 修订——规则内免确认留痕）；历史正文不改写。

- 日期：2026-09-07
- 评审形态：主审（ZCode）亲审——专项复审 R60 修复批（dev `e074e7b5` 提交内 R60 部分；R59 清偿批非本次对象，仅核查两批叠加面）
- 基线：dev HEAD `e074e7b5`（R59 清偿批 + R60 修复批合并提交）干净树——**复审全程只读零改动**
- 作者指令：「评审下这次修复的质量和内容。」
- 路径确认闸：AskUserQuestion 报备**未获回复**——按 MP/PM 与五十二/五十四/五十五轮修复批复审先例（专项命名不接全量轮号）以正典名落盘，**待作者追认**
- 禁读声明：不依赖收口记自证——独立重读修复后源码 + 亲跑测试 + 误伤面/叠加面独立核查

## 一、范围与方法

评审对象 = R60 修复批 6 项（批 1 P2×1 主审亲修 + 批 2 P3×5 四路并发代理）：
R60-D-1（prefs.ts 在途竞态）/ R60-A-1（turns.ts input 守卫）/ R60-B-1（main.ts loadURL 留痕）/ R60-B-3（saveWinState warn）/ R60-D-4（clw-last-book 单源）/ R60-D-3（check.ts 精确删键）。

方法（主审亲审，不派子代理——修复量小、近轮同题先例均亲审）：
1. **逐项独立核验**：重读修复后源码（提交 diff + 当前 HEAD 最终态），从四维判据评估——①缺陷形态是否真消除；②修复是否正确（含边界）；③实现是否优雅（含与周边风格一致性）；④回归测试是否钉住缺陷本质。
2. **误伤面与叠加面核查**：各修复点的调用方/同族函数/既有契约逐一重扫；`e074e7b5` 内 R59 与 R60 两批同文件共存面（prefs.ts / main.ts / App.vue / check.ts）逐 hunk 核对无相互覆盖。
3. **亲跑测试**：R60 五个新测试 + 全部相关族 + 双 typecheck（L1 面；L2 九件套引用提交前实测全绿态——复审零代码改动）。

## 二、逐项核验

### R60-D-1（P2，prefs.ts 在途竞态）——通过，最优项

**真修**：`flushPendingPersist` 由「在途即空返回」改为「await 在途真链落定 → 清防抖定时器 → 按届时最新快照直发补一笔」，返回真链 Promise 交主进程 `flushRendererWithBudget` 预算内等待——缺陷形态（在途 PUT 快照后的 <500ms 改动随窗口死亡）彻底消除。

**正确性**（独立重推时序）：
- `runPutChain` 单源：body 起跑即同步占位（`p = body().finally(...)` 表达式求值先于任何宏任务可观察点），R33D-24「占位先于 GET await」单飞不变式保持；`finally` 带 `putInFlight === p` 守卫只清自己链——多路交叠（关窗冲刷与在途/二次冲刷）不误清对方占位，语义比原 `finally { putInFlight = null }` 无条件清位更严谨。
- 在途链失败面：`doPersistPut` 内 409 走 `recoverFromConflict`（其 GET/重试失败均 catch 消化）、非 409 走一次性 toast 后 return——PUT 链实际不 reject；flush 的 `.catch(() => {})` 仅是防御性兜底，不改变「失败已消化、照常补发」语义。
- 防抖定时器竞速：flush 的 await 落定续体与 `clearTimeout(persistTimer)` 在同一微任务队列连续执行，在途期间重排的定时器（T2）无法插入——T2 被清、不叠发（测试 4 钉死）。
- 主进程侧零改动正确：`main.ts` executeJavaScript 本就 `await window.__clwFlushPrefs()`，App.vue:31 类型注记 `() => void | Promise<void>` 双向兼容。

**优雅度**：`runPutChain` 把定时器链与冲刷链两处「GET 对齐 + doPersistPut + finally 清位」抽为单源，消除重复且根治 `chain` 自引用闭包（TS2454）——本轮最优雅一项。注释含轮次出处、不变式、失败面推导，贴项目风格。

**测试**：4 用例断言全钉本质（在途补发带最新快照与对齐 revision / 冲刷 Promise 落定前不得 resolve〔主进程等待语义锚〕/ 无在途直发原路径不回归 / 防抖定时器随冲刷清掉不叠发）；红 4/4 → 绿，prefs 全家族 8 文件 43 用例零回归。

### R60-A-1（P3，turns.ts input 守卫）——通过

**真修**：`executeChatTool` 入口显式拒收 null/undefined/字符串/数字（含 args 解析为 `"null"` 的产物），回可诊断文案（含工具名 + 实际类型）；数组按契约侧 `typeof 'object'` 口径放行走字段校验——与 `contract/chapter.ts:94` 对称守卫逐字对齐。

**正确性**：守卫置于 try 外早返回，不落兜底 catch（消除原 `Cannot read properties of null` 不可诊断文案）；结果走既有 `{ ok:false, summary }` 通道，`toolCallEvent` 已先登记 input 原文、`toolResultEvent` 回填 summary——留痕链路复用不新造，铁律「模型可见⟺已记录」保持。

**优雅度**：5 行守卫 + 注释含触发形态、对齐依据、通道说明；`call.input === null ? 'null' : typeof call.input` 类型文案准确区分。略可挑：`!call.input || typeof ...` 双条件可并为 `(typeof call.input !== 'object' || call.input === null)`，但现写法与契约侧同构（`!input || typeof input !== 'object'`），一致性优先正确。

**测试**：8 用例双路径（注册表/switch）× 多形态 + 数组放行 + 合法基线；断言 `GUARD_PREFIX`/工具名/`null`/executor 未调用——钉「可诊断 + 不落兜底 + 不误调 executor」三重本质；红 5/8 → 绿，turns 族 7 文件 34 用例零回归。

### R60-B-1（P3，main.ts loadURL 留痕）——通过

**真修**：主窗 loadURL 补本地 `try/catch` 留痕（含首载 URL），**原样上抛**——独立复核 `main.test.ts:1074-1088` 时序 2 与 `bootstrap-runner.ts` 第九轮 L-3 注记，确认「bootstrap reject → 启动失败 + quit」为固化设计契约，catch 只留痕不吞，与书架/书库窗 R74-16 习语对称补齐。

**优雅度**：镜像既有 `.catch` 习语、留痕含 URL 现场（诊断窄竞窗的关键信息）、`log.error('desktop', ...)` 通道正确。测试含「留痕先于启动失败」「契约不被吞」「无 unhandledRejection」三重断言——正是修复语义边界。

### R60-B-3（P3，saveWinState warn）——通过

**真修**：main.ts 全文件唯一零留痕忽略处收口——`catch (e)` 补 `log.warn('desktop', ...)` 含失败原因，吞错不阻断关窗语义保持。

**优雅度**：warn 级对齐 R54-A-1 超时 warn 先例（窗口状态非关键数据，不扰民）；`e instanceof Error ? e.message : String(e)` 防御未知抛出物。2 用例断言 warn 留痕 + close 链不被阻断。

### R60-D-4（P3，clw-last-book 单源）——通过

**真修**：`shared/storage-keys.ts` 增 `LAST_BOOK_KEY`，App.vue（读）/ Shelf.vue（写）/ ShelfModal.vue（读删 + 写）四处字面量收敛 import——R28-3 点名的键名断裂族收口。独立 grep：src/ 内 `'clw-last-book'` 字面量零残留。

**优雅度**：常量注释含整键语义与断裂族缘由；测试走 r51-h4 单源契约习语（读源文件文本断言零残留 + 消费方 import 单源），比行为测试更直接钉「单源」本质。

### R60-D-3（P3，check.ts 精确删键）——通过

**真修**：`clearFalsePositiveMarksForDoc` 由整键前缀匹配改「书前缀 `\u0000` 边界命中 + docId 段精确等值」——`a.md` 清除时 `a.md2` 兄弟键存活。

**正确性**（键族一致性独立重扫）：`fpKey(name, docId) = fpBookPrefix(name) + docId = 'clw-fp:' + name + '\u0000' + docId`，`fpBookPrefix` 含尾 `\u0000`（R49-27 长书名不误吞口径）——`slice(bookPrefix.length) === docId` 精确得 docId 段，与 `loadFlagged/saveFlagged` 键形严丝合缝。同文件 `clearFalsePositiveMarks`（删整书）保留前缀匹配是正确语义（整书全清），非同族缺陷。倒序扫描与 best-effort catch 保持。

**优雅度**：复用既有 `fpBookPrefix` 单源（不重拼前缀，R50-D2-1/R51-H-4 双源教训已内化）；调用方 `useChapterTreeActions.ts:466` 语义（删章清该章键）正确。4 用例含前缀兄弟存活/反向/书名边界/静默四形态，红 1 → 绿 4。

## 三、误伤面与叠加面核查

- **两批叠加（e074e7b5）**：prefs.ts（R59 无改动，R60-D-1 单批）/ main.ts（R59 R55-A-3 切库回滚 hunk + R60 B-1/B-3 hunk 分处独立，逐 hunk 核对零相交）/ App.vue（R60 D-1 类型行 + D-4 键行，与 R58-B-2 已提交接线共存自洽）/ check.ts（R59 面板键单源在 shared/issue-keys，R60-D-3 改清理函数，异函数零交叠）——**无相互覆盖、无语义冲突**。
- **prefs flush 消费面**：`main.ts` executeJavaScript await 钩子（`flushRendererBeforeClose`）/ App.vue 注册与 onBeforeUnmount 删除——均与 Promise 化签名兼容。
- **同族函数**：`clearFalsePositiveMarks`（整书）前缀匹配正确；`fpKey`/`fpBookPrefix` 其余消费（loadFlagged/saveFlagged/useShelf 迁移）不受 D-3 改动影响。
- **固化契约**：main.test 时序 2（启动失败 + quit）、R33D-24 单飞、R58-B-2 关窗冲刷原路径——亲跑全保持。
- **误伤实跑**：相关族 27 文件 205 用例全绿（§四）+ 双 tsc 0 错——零旁路。

## 四、亲跑测试与门禁

- **相关族亲跑全绿**：前端 + AI 25 文件 128 用例（R60 新测试 5 文件 21 用例 + prefs 族 8 文件 + check 族 + storage-keys 族 + turns 族）；desktop 2 文件 77 过 + 1 跳（main.test 74 过 1 跳 + r60b 2 用例）。
- **双 typecheck**：tsc 0 错 / vue-tsc 0 错。
- **L2 九件套**（引用提交前实测全绿态——复审零代码改动，不重跑）：vitest 955/6255 + 4 跳 0 败〔117.53s〕+ tsc/vue-tsc 0 + eslint 0/0 + 三 check 过 + build:web 过 + e2e 43/2〔28.7s〕。

## 五、质量与优雅度评分

| 项 | 真修 | 正确性 | 优雅度 | 测试钉本质 |
|---|---|---|---|---|
| R60-D-1 | ✅ | 优（单飞不变式 + 失败面推导） | **9.5**（runPutChain 单源抽象，本轮最优） | 4/4 钉死 |
| R60-A-1 | ✅ | 优（契约侧对齐 + 留痕复用） | 9.0 | 8 钉死 |
| R60-B-1 | ✅ | 优（固化契约不吞 + URL 现场） | 9.0 | 2 钉死 |
| R60-B-3 | ✅ | 优 | 9.0 | 2 |
| R60-D-4 | ✅ | 优（字面量零残留） | 9.0 | 3 单源契约 |
| R60-D-3 | ✅ | 优（键族一致性 + 单源复用） | 9.0 | 4 钉死 |

**质量总评：9.2/10（A）**——6/6 真修、正确性全优、零误伤、零旁路；`runPutChain` 抽象为亮点。**实现优雅度总评：9.0/10**（与五十五轮修复批复审同口径；唯一可挑：A-1 守卫双条件写法与契约侧同构取舍正确，非缺陷）。

## 六、结论

1. **本次修复内容与质量**：R60 批 1（P2×1）+ 批 2（P3×5）六项全部真修，缺陷形态逐一消除，正确性经独立时序/键族/契约重扫成立；回归测试全部钉住缺陷本质（红绿验证有据）。
2. **无待修项，无修复批**：P0×0 / P1×0 / P2×0 / P3×0——专项复审当日直接收口。
3. 两批合并提交（`e074e7b5`）叠加面干净，无相互覆盖；相关族 205 用例 + 双 typecheck 全绿为收口背书。
4. 后续：本专项复审与 R60 主报告均在 `01-评审/` 暂存，归档移动待作者确认闸/归档批。

——复审止于复审；无待修项，当日收口。归档移动待作者确认闸。
