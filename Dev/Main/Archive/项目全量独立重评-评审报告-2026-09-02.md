# 项目全量独立重评与进度质量评估报告

- **日期**：2026-09-02
- **性质**：独立重评。作者指令「忽略现有的评审文档，重新评审一遍项目所有代码，告诉我项目完成进度，完成质量，最后形成一个文档给我。记得编排下任务。」——全程未读 `01-评审/` 与 `Archive/` 下任何既有评审报告（20 篇暂存全链禁读），结论全部来自本轮代码实读、验证套件亲测与总览/README 计划基线对照。独立命名不接轮号（与三十七轮报告 §六 预告的「三十八轮从零重评」同旨；轮号是否回接由作者拍板）。
- **报告路径**：经确认闸 AskUserQuestion 报作者**获批**（`Dev/Main/01-评审/项目全量独立重评-评审报告-2026-09-02.md`）。
- **评审基线**：工作区实态 = HEAD `db16c39`（dev，merge(review) dev←win R1W/R2W 并入）**+ 在途未完成合并**（win `ac9149a` 书架加载性能专项：`MERGE_HEAD` 在、`progress.ts` 冲突已解未暂存、`chapters.ts`/`shelf.ts`/两测试文件已暂存）。评审按工作区现状进行（详见 §四）。
- **评审执行记档（如实）**：并行子代理方案因账户限流（1302×3）与批量中止（5 只）收缩为主线程串行深评 + 1 只支撑模块子代理成活（29 文件全量通读）。最终覆盖 = 关键路径深读（§六明细）+ 全局扫描（TODO/占位/导出面）+ 客观门全量亲跑 + 支撑模块子域全量通读。

---

> 📦 **归档记（2026-09-04）**：随「已完成的归档」批（作者指令）移入 `../Archive/`（扁平冷库）；历史正文不改写，各轮结论以本报告正本与总览 1.3 行内摘要为准。

## 一、客观验证（全部亲测）

| 门 | 结果 | 备注 |
|---|---|---|
| vitest 全量 | **741 文件全绿，5247 过 + 2 跳（5249）** | 首跑 1 败（startup-notices 日志落痕断言）→ 隔离复跑 3/3 绿 → 全量复跑全绿，定性**并行负载 flaky 非回归**（IR-9） |
| e2e（29 spec/45 用例） | **43 过 + 2 跳（30.5s）** | build:web + playwright 全链 |
| tsc --noEmit | **0 错** | |
| vue-tsc --noEmit | **0 错** | |
| eslint | **0 error / 31 warn** | 全部为测试文件 `no-explicit-any` 存量 |
| check:counts | **失配** | 实测 741/5247 vs README 5237——差额 +10 恰为在途 win 合并带入的新测试（作者未提交、README 自然未修账），**预期漂移非基线缺陷**（§四） |
| check:packaging | **过** | resources 入包清单 + prompt 版本表对账一致 |
| check:knowledge | **过** | 知识层 13 条 manifest 与磁盘一致，反向扫描无未登记资产 |
| coverage 门（配置核验） | 分桶棘轮制在位 | 主 src 89/83/95/89（statements/branches/functions/lines）+ ai/events/server/web-next 各域独立门，基线−2pp 防回退不追高 |

---

## 二、完成进度评估

### 2.1 计划面（总览第三节 0-23 阶段逐项对照）

总览记载的阶段 0-23 中，除以下开放项外**全部标记已完成/已收口**，且本轮代码实证与记载相符：

- **阶段 13（Responses 启用批）**：验收 11 项余 1——**中转网关真机验证**（`scripts/verify-responses-relay.ts` 脚本在位，待作者网关凭据，外部依赖不阻塞）；测试缺口「encrypted 中段链路无直测」仍挂账。
- **阶段 21（win 适配）**：J0-J6 代码面全收口、win 真机全量回归九门全绿；仅 **J6b 跨平台书库迁移**一项挂账（需 mac 侧）。
- 阶段 23 已关闭（批 5/6 拍板不办）；阶段 22（utilityProcess 拆分）已收口后按实测回退内嵌形态（账目与崩溃隔离演练留档）。

### 2.2 功能面（README 承诺流程 ↔ 代码落点实证）

README 宣称的写作全流程逐项有代码落点且 e2e 覆盖主链：

| 功能 | 代码落点（实证） | e2e |
|---|---|---|
| 建书/脚手架/迁移 | install/{scaffold,books,migrate-*}、api/onboard.ts | shelf/short-flow |
| 设定（大纲/角色/世界观/物品/伏笔/文风） | api/{outline,foreshadows,style}.ts、document/foreshadow.ts | foreshadow/settings-book-scope |
| 正文编辑 + 自动保存 + 版本冲突 | api/documents.ts 乐观锁协议、document/service.ts WAL 式保存链、web-next stores/doc.ts | edit-save/conflict |
| 全自动写章（起草→机检→打回重写，上限停下问人） | process/draft-pipeline、check/*、ai/runner 重试/预算闸 | auto-write/check |
| 三审（长篇读者/编辑/设定校对；短篇钩子/情绪/收尾） | review/{contract,run}、api/review.ts | ai-review |
| 定稿 + 防吃书闸 | document/{finalize,lead-finalize}、check/manifest-check、LEAD_GATE 409 契约 | finalize/batch-finalize |
| 伏笔埋设→回收全程记录 | foreshadow 事件族（chain-bridge 差分落账） | foreshadow |
| 字数曲线/节奏预警 | progress/rhythm/words-diary | overview-short |
| 文风系统（样章/手法/禁词库） | style/*、check/leak-derive、imagery-seed | —（单测面） |
| 选段 AI 改写/分析 | api/{rewrite,analysis}.ts | rewrite/analysis |
| 对话助手（agent 工具循环 + 风险确认闸） | ai/orchestrate/chat、ai/tools、task-gate | ai-degrade |
| RAG 全书检索 | rag/*（SQLite embed/resolve/store）+ api/rag{,-providers}.ts | export-ai-settings |
| 导出（合并稿/分章/短篇投稿视图） | export/（worker 线程 + 流式原子写） | export-ai-settings |
| 版本快照/恢复 | document/{version,snapshot}、api/snapshots.ts | version-restore |
| 审计/对话史/分支/用量卡 | api/{audit,chat-history,chat-branches,cost-stats,trace-stats}.ts、events/* | audit/usage-card |
| 回收站（软删/还原/永久删） | document/trash.ts | tree-ops |

全局 TODO 扫描：**src 全树（约 10 万行）仅 1 处 TODO 字样**（`src/metrics/short-index.ts:89`，且为注释描述设计期占位的历史说明，非未完成工作）；「占位」命中全为 UI 布局用语。无空壳页面、无「暂未实现」文案、无未接线按钮面。

### 2.3 发布面

版本 `1.0.0-rc.1`（tag 已打、发布冒烟绿）；electron-builder mac + win NSIS 双平台配置完整（图标单源、产物命名、Windows 使用须知披露）；CI 双平台腿（win 仅门禁+构建+冒烟，e2e 留 mac = D5 拍板口径）。

### 2.4 进度结论

**≈98%**（mac 生产形态 ≈98.5% / win RC 形态 ≈97%）。扣分项全部是外部依赖挂账（中转网关真机凭据、J6b 跨平台迁移）与在途未提交合并，**非代码缺口**。

---

## 三、完成质量评估

### 3.1 分区深评结论（主线程深读 + 子代理全量通读）

- **服务端安全面（深读 index/http/static/router/safe-path 全文）**：三层闸真闭环——Host 头精确匹配回环+端口（DNS rebinding）、写方法 Origin 白名单 + session token（timingSafeEqual）、GET/HEAD token 闸（豁免面显式路径表）；路径判定与 token 闸同用规范化 pathname（dot-segment 绕过已闭合）；静态托管 normalize+双侧 realpath 判界 + win 保留设备名 404 + nosniff；路由 :param 正则转义 + null-prototype 防原型链注入；错误信封单出口统一过 redactSecret；413 排空带宽限 destroy；keep-alive 治理。安全测试为裸 socket 级行为锁定（api-token/boot-token/r31d-token-gate 600 行，覆盖豁免面/HEAD/通道收窄/超限）。**评价：显著高于同类本地服务基线。**
- **文档保存链（深读 documents.ts + service.ts executeSave 全段）**：WAL 式崩溃恢复协议（journal pending 先行→原子写→settled/aborted）；乐观锁 expectedRevision（sha256 基线）；per-docId 串行队列 + 跨进程保存锁（锁序 save→布线→清单，ABBA 已消）；出队复验 + 回收站双条件复活守卫；非 UTF-8 覆写防线（字节档唯一合法通道 + 原字节留底）；新建 createFileExclusive 独占防丢更新；落盘后步骤（清单/字数日记/journal settled）best-effort 分级、失败语义如实（不误报 WRITE_ERROR）。**评价：数据完整性工程达到生产级，防丢更新/防复活/防假留底三面都有显式设计。**
- **fs 原语（深读 atomic.ts 主体）**：fsync 文件+目录、EPERM/EBUSY 指数退避、link 独占建 + 非 NTFS 降级、流式原子写带 publish 裁定、tmp 命名带 pid+UUID 供清扫、rmQuietly 宁残留勿反转语义。
- **AI 链路（深读 retry-policy 全文 + runner/calls 关键段）**：退避公式（指数+对称抖动+Retry-After 尊重但封顶）、失败决策表单口径、attemptsUsage 覆盖失败调用（预算/成本不漏记）、章级预算闸 + 任务级账本 + 含估计标记（粘性）、跨进程串行写账本、损坏保守阻断。
- **前端（深读 stores/doc.ts 全文结构 + 保存/冲突/驱逐链）**：LRU 永不驱逐 dirty/conflict/saving；flushDirty 等待在途保存带防活锁轮次上限；卸载窗口同步 XHR 兜底 + re-bootstrap；冲突未决期间 autosave 跳过（必再冲突的正确处理）。
- **桌面（main.ts 生命周期段）**：before-quit 优雅链 + session-end 兜底 + SIGINT/SIGBREAK 幂等 quit + uncaughtException 留痕；启动链迁移逐书容错（单本失败降级该书）。
- **支撑模块（子代理 29 文件全量通读，发现经主评审亲核）**：rag 三级损坏自愈链完整；events WAL+busy_timeout+BEGIN IMMEDIATE+INSERT RETURNING；git 数组参数免注入 + 15s 超时 fail-closed + 64MB 缓冲上限；redact 多层（URL query/Bearer/裸前缀/智谱/Gemini 无前缀形态）。

### 3.2 分维度评价

| 维度 | 评价 |
|---|---|
| 安全 | 纵深多层且测试真锁行为；本地信任域内的防护密度（rebinding/token/路径/symlink/设备名/nosniff/脱敏）远超产品定位所需下限 |
| 数据完整性 | 保存链是全项目最强面：崩溃恢复协议 + 乐观锁 + 跨进程锁 + 编码防线齐备 |
| 并发 | 双进程同书场景系统化覆盖（四套跨进程锁 + 任务闸 + drain），锁序有全局注释口径 |
| 跨平台 | BOM/CRLF/大小写/保留名/设备名/长路径/EBUSY 退避等 win 面处置密集；README 披露如实 |
| AI 链路 | 重试/预算/事件可回溯（「模型可见⟺已记录」抽验在位）纪律执行良好 |
| 前端 | 状态机清晰，脏数据保护完备；35k LOC 组件层本轮为结构盘点 + 状态层深读 |
| 测试 | 741 文件/5249 用例 + 分桶 coverage 棘轮 + 29 spec e2e；抽验为真行为锁定（裸 socket/边界值/规模界值） |
| 可维护性 | 注释密度近 1:1 且带修复出处（决策溯源极强）；代价是信噪比低、个别注释先行失实（IR-1/IR-8） |

### 3.3 发现清单（P0×0 / P1×0 / P2×2 / P3×7，编号 IR-N）

**P1：无。** 未发现正常使用可致数据丢失、崩溃或可直接利用的安全漏洞。（对照：三十七轮报告所列 P1/P2 修复面——切书在途保存、delta 可选链、热路径扫描异步化、git windowsHide、读侧异步孪生——本轮实读全部在位，无回归。）

- **IR-1 | P2 | src/log/index.ts:172-190**：日志兜底层 `KEY_MASK_RE` 仅覆盖 4 种形态（sk-/Bearer/智谱 32hex.32hex/AIza），而 `redactSecret`（log/redact.ts）另覆盖 xai-/sk_/gsk_/hf_/glpat-/ghp- 前缀、x-api-key 头、URL query 凭据；注释宣称「词表与 redact.ts 全词表对齐」**失实**。子代理 9 组样本实测 9/9 穿透 maskKeys（redactSecret 全掩）。实际暴露面：`log.error(tag, redact(msg), err)` 模式第三参传原始 Error（如 state.ts:117），err.message 未过 redactSecret，落 JSONL 后仅剩兜底层。主防线（错误信封出口统一脱敏）在位，故 P2。
- **IR-2 | P2 | src/events/store.ts:527-561**：事件库文件级损坏（SQLITE_NOTADB）后 WAL 重试 8 次全败上抛，**无任何恢复路径**（该书对话史/审计/链路事件永久不可用，每次开库必抛），仅能手工删文件。对照 rag 子系统有完整自愈链（损坏判定→删库重建）。事件库属产品数据不可自动删，但连「检出损坏→提示/引导重置」的显式出口都没有。需产品拍板处置口径。
- **IR-3 | P3 | src/git/exec.ts:149-172**：gitAsync 超时/中止仅一次 SIGTERM 无升级（SIGKILL）；git 因网盘挂起进 D 态时句柄挂住事件循环、进程无法自然退出（win TerminateProcess 无此问题）。
- **IR-4 | P3 | src/cache/rebuild.ts:267-273**：index.db 开库不设 WAL（rag/events 库均设），全量重建单事务期间阻塞全部读者（各消费方 busy_timeout + 优雅降级，实害限于缓存缺口）。
- **IR-5 | P3 | src/export/index.ts:80-104**：purifyBody 围栏状态机对「未闭合 ```」无收尾——正文奇数个围栏行之后所有 `#%` 作者批注按代码字面量保留，泄漏进导出稿，违背模块契约（AI 产出常见畸形形态）。
- **IR-6 | P3 | src/events/store.ts 全文**：事件账本只增不减（除 clearBook/clearBooks），compaction 仅逻辑遮蔽无物理压实，无保留期/VACUUM/归档机制——append-only 审计账本属设计使然，长跑增长无界如实登记。
- **IR-7 | P3 | 多文件**：测试专用/兼容导出堆积（rag/index recall 包装、rag/store unlinkWithRetry、events store appendEvent/latestSession、lineage recordedSnapshots、goal-state getGoal、cache loadLeadFromCache、events 同步壳 openSessionStore）——注释均自认仅测试消费，属测试与生产 API 边界渗透的结构性债务。
- **IR-8 | P3 | src/studio/server/api/chat-history.ts:96-100**：注释称 openSessionStoreAsync「库损坏时返回 null」，实际损坏是抛错（仅 userDataPath 空时返 null）；`if (!store)` 分支近乎死码，实际靠 defineRoute 兜 500（错误文案降级为泛化 500）。
- **IR-9 | P3 | test/studio/startup-notices.test.ts**：日志落痕断言在全量并行负载下偶发假红（本轮首跑 1 败、隔离 3/3 绿、全量复跑绿）——历史上已三次加宽轮询窗，根治方向为确定性排空钩子（存量已有 flushLogsForTest 先例）。

**记档（非缺陷）**：①check:counts README 差 +10 = 在途合并预期漂移（§四）；②eslint 31 warn = 测试文件存量；③src/metrics/short-index.ts:89 「设计期占位」注释指已删除类型的说明，非待办。

### 3.4 质量评级

**A−（8.8/10）**。扣分点：IR-1 兜底脱敏弱于声明（安全纵深的一致性缺口）、IR-2 损坏恢复盲区、注释-实现偶发失实（IR-1/IR-8）、测试/生产 API 边界渗透（IR-7）。加分点在 3.1/3.2 已述。与客观门全绿相互印证。

---

## 四、在途工作区状态（评审期间发现，需作者收口）

工作区有**一次进行中的未完成合并**：win 分支 `ac9149a`（书架加载性能专项：单轮扫描摘要 + 摘要 TTL 5s→30s + 书架快照 localStorage 化）合入 dev。状态：`MERGE_HEAD` 存在、`progress.ts` 冲突已解（无冲突标记、注释已按双线合并口径改写、实现自洽）但**未 `git add`**，另 4 文件已暂存，带来 +10 测试（check:counts 失配根源）。本轮所有验证（vitest 741/5249 全绿 + e2e + tsc/vue-tsc/eslint）即在此工作区实态上通过——**合并内容本身质量无碍，待作者完成提交与 README 修账（5237→5247）**。

## 五、遗留风险与建议

1. **收口在途合并**（§四）——提交时同步根 README 四处单测数修账。
2. **IR-1 建议小修**：maskKeys 词表直接对齐 redactSecret 的前缀族（或单源化两词表）+ 修正失实注释；顺手把 `log.error(tag, msg, err)` 第三参的 err.message 纳入 redactSecret 面。
3. **IR-2 需产品拍板**：事件库损坏走「显式恢复端点/引导重置」还是维持现状（不可自动删，但至少要有检出提示）。
4. **IR-5 小修**：purifyBody 文件末尾 inFence 仍真时按未闭合处理（收尾按非围栏语义过滤 `#%`）。
5. 外部依赖两挂账维持：中转网关真机验证（待凭据）、J6b 跨平台书库迁移（待 mac 侧）。
6. IR-3/4/6/7/9 为低危/结构债，可并入下轮修复批或随相关文件触碰时顺修。

## 六、评审覆盖明细（如实）

- **主线程深读全文**：studio/server/{index,http,static,router}.ts（931 行）、fs/safe-path.ts、api/documents.ts（509 行）、document/service.ts 保存链（~380 行段）、fs/atomic.ts 主体、ai/retry-policy.ts、git/exec.ts 头段、log/redact.ts、web-next stores/doc.ts 结构与保存/驱逐链。
- **主线程定向核验**：ai/calls.ts 记账结构、runner attemptsUsage 段、events/store.ts WAL/事务 grep 面、rag/store.ts 损坏处理 grep 面、desktop/main.ts 生命周期 grep 面、format/chapters.ts 在途新面（readChapterDirSummary）、vitest.config.ts coverage 门、package.json/构建配置。
- **子代理全量通读**：src/{rag,cache,events,export,git,knowledge,learn,log} 29 文件 + 知识层/ 目录（其 7 项发现中 IR-1/IR-2/IR-8 经主评审亲读源码复核成立，IR-3~7 采信其行号证据）。
- **全局扫描**：TODO/FIXME/占位全树、导出面抽查、测试文件清单与安全测试抽样。
- **未覆盖面（如实）**：web-next components/ 89 文件（19.7k 行）未逐文件深读（以结构盘点 + stores/composables 深读 + e2e 行为覆盖替代）；src/ai 其余 ~70 文件、src/process/src/check 各半数文件未逐行深读（以关键链深读 + 全量测试门 + coverage 棘轮兜底）。后续轮次可补组件层逐文件深审。

---

## 七、修复批收口记（同日收口）

作者指令「全部修复，编排下任务」。九项处置：**修 7（IR-1/2/3/4/5/8/9）+ 维持 2（IR-6/IR-7 维持登记）**；方案偏离记档 5 + 测试构造迭代记档 1。全部在在途合并工作区实态上修复与验证（不触碰 MERGE_HEAD）。

### 7.1 逐项收口

- **IR-1｜修**：maskKeys 拆两族真对齐 redactSecret——头/查询族（case-insensitive /gi：Bearer〔值字符类补 +/=，原中途截断漏掩〕、x-api-key、URL query 凭据 api[_-]?key/key/token/access[_-]?key/authorization=）+ 裸 key 族（xai-/sk_/gsk_/hf_/glpat-/ghp_ 前缀 + 智谱 32hex.32hex + AIza）。掩码形貌升格：Bearer/x-api-key 保留前缀 + 全掩 + 末 4 位（原 m.slice(0,5) 产出「Beare***」破损外观）；URL query 保参数名 + ****（短值全掩）。失实注释改写如实（「兜底层不替代纪律」口径）。报告 §五.2 建议的 err.message 面**经查已闭合**（maskedErr 镜像面过 maskKeys + 落盘行级再过 maskKeys，词表对齐后自动覆盖，零改动收编——偏离记档 ③）。测试 +6 形态用例（裸前缀×6 / x-api-key / URL query 长短值 / Bearer base64pad / 大小写变体），11/11 绿。
- **IR-2｜修（偏离记档 ①）**：`isDbCorruptionError` 分类原语（NOTADB/映像畸形短语集匹配，宁漏判不误判 BUSY/IOERR）+ firstOpenStore 外层 catch 换**可行动人话错误**（含库路径 + 「先备份再移走、应用重建空库」恢复指引，原始错误挂 cause 保诊断链）；WAL 重试环遇损坏立即上抛（免 8×busy 空转 ~42s）。**不建恢复端点、不自动删库**（事件是对话史/审计产品数据）——报告「需产品拍板」项取最小可行动出口先行，端点化留待作者。测试 test/events/store-corruption.test.ts（损坏路径 + 干净路径回归）。
- **IR-3｜修**：`killWithEscalation` 可测原语——TERM 即发 → `GIT_KILL_ESCALATION_MS`(2s) 宽限 → SIGKILL 强制收口；升级定时器 unref 不持事件循环，对已退出进程 kill 无害化（ESRCH 吞）。接线 gitAsync 超时 + abort 两路，settle 有界语义不变（不等待 close）。测试 test/git/kill-escalation.test.ts 假 child 注入 3 用例（升级时序 / cancel 撤销 / ESRCH 不炸）。
- **IR-4｜修（偏离记档 ④）**：rebuild 建库补 `PRAGMA journal_mode = WAL`（busy_timeout 先设，N3 口径；**BUSY 回退臂**——并发窗切换撞锁不阻断重建，回退 delete 语义照旧 + log.warn 留痕，避免低危项引入新失败面）+ **孤儿侧车清除**：库文件缺失而 -wal/-shm 在位时建库前 rmSync（「可删 .cache/index.db 重试」指引留下的孤儿 WAL 不再回放进新库；库在位时不清——恢复 WAL 是正确行为）。测试 test/cache/rebuild-wal.test.ts 2 用例（落 wal 断言 / 孤儿侧车清除）。
- **IR-5｜修（偏离记档 ⑤）**：purifyBody 两遍收口——首遍照常；末态仍在围栏内（奇数 ``` 行/忘收口）→ 对原文按无围栏感知重剥。权衡登记：坏围栏章内**紧贴形态**代码字面 `#%` 接受误剥（宁误剥不泄漏批注——批注可能含剧透/内部备注，二者不对等）；`#` 前带空白的松散字面维持 E-9f 既有口径保留。测试 test/export/export-unclosed-fence.test.ts 2 用例（未闭合剥净 + N-6 闭合契约回归）。
- **IR-8｜修**：chat-history / chat-branches / audit×2 四端点同构收编——openSessionStoreAsync 裸抛路径 try/catch → 结构化 500 `STORE_UNAVAILABLE` + e.message 人话透传（含 IR-2 损坏指引；经 replyError 统一脱敏出口）；R62-43「损坏返回 null」失实注释四处一并勘误（null 仅为 userDataPath 空分支，上方已分流）。check.ts/documents.ts 观察层原有 catch 语义不变。测试 test/studio/chat-history-corrupted-store.test.ts HTTP 层 2 用例（history + branches 双闸）。
- **IR-9｜修（偏离记档 ②，定性修正）**：报告原判「测试并行假红、根治方向确定性排空钩子」——flushLogsForTest 已在位仍全量偶红，深挖后**真根因在 log 泵产品码**：在途泵排空 × initLogging 换目录交错时，排空目标已切新目录而新目录未建（init 的 mkdir 链排在本泵完成之后），appendFile ENOENT fail-open 丢行 + 空目录假象（D2 期「泵首 mkdir 兜底两类交错」结论不完备——泵首只做一次且目标可能是旧/错目录）。修 = 泵内**逐行盯目录**（目标目录与上次落盘目录不符即幂等 mkdir，含泵首，全部交错形态覆盖）。回归 test/log/log-dir-switch.test.ts 2 用例：**对旧实现确定性红**（暂回退实证 bulk-411 中段丢行）**/对本实现 ×8 绿**。
- **IR-6｜维持登记**：事件账本 append-only + 手动清理 = 2026-08-16 拍板的保留定版；保留期/压实/归档属产品决策，非本轮可代办。维持 §三.3 原样，挂产品待拍板。
- **IR-7｜维持登记**：测试专用导出为注释已自认的结构债（本批复新增的 killWithEscalation 等测试注入口随同口径挂账）；批量收口（包装层/工厂分离）不成比例，随相关文件触碰时顺修。

### 7.2 方案偏离记档

① IR-2 取「检出→可行动指引」最小出口，恢复端点留待产品拍板；② IR-9 定性由「测试假红」修正为「log 泵产品码丢行」，修复落 src/log/index.ts；③ IR-1 的 err.message 脱敏面经查已闭合（maskedErr 序列），零改动收编报告 §五.2 建议；④ IR-4 增补 WAL 切换 BUSY 回退臂；⑤ IR-5 明确「紧贴形态代码字面接受误剥」权衡（报告未展开）。

### 7.3 测试构造迭代记档（IR-9 回归测）

三迭代：单行 + tick(0) 构造在并行下非确定（泵首受 init mkdir 链门控，tick 可先于泵首）→ 2000 行构造撞 `MAX_PENDING_WRITES`(1024) **drop-oldest 背压**（bulk 头部行被合法丢弃，与被测病灶无关）→ 终稿 **800 行（<1024，末尾断言 dropped===0 自证未触背压）+ 20ms tick**（排空窗数十 ms ≫ 泵首延迟）。另 logsDir 不预建（真实形态惰性 mkdir），预建会让旧实现 ENOENT 病灶永不触发。

### 7.4 终门（九件套，全绿）

vitest **747 文件 / 5265 单测**全绿 + 2 平台跳（117.1s）+ tsc/vue-tsc 0 错 + eslint 0 error〔31 warn 存量〕+ **check:counts 过**（根 README 修账 741→747 文件 / 5247→5265 单测，在途合并 +10 漂移与本批 +19 新增一并归账，实测口径权威）+ check:packaging 过 + check:knowledge 过 + e2e 43 过 2 跳（28.4s）+ **coverage 多桶门过（exit 0）**。批内分域回归：events/端点族 165 过、git/cache 族 72 过、export 族 46 过、log/notices 族 31 过。已知无关项如实记档：r29-fe-e2-e7-panels 守卫用例在 coverage 慢载下并行偶发假红（win 阶段 21 既有记档形态，单跑恒绿，本轮 coverage 终跑 747/5265 全绿）。

**工作树未提交待作者指令**（含在途合并 MERGE_HEAD 实态一并保持，收口不动 git 态）。

---

*本报告为独立评审正本；索引同步：Dev/Main/README（变更记录 + 目录地图 01-评审 行 20→21 篇）+ 总览 1.1（新增行 + 收口状态回改）+ 01-评审/README（维护日期 + 暂存清单 + 状态回改）。*
