# 五十二轮修复批复审-评审-2026-09-06

> 📦 **归档记（2026-09-08）**：已收口，随「README 整理与清理」批移入 `../Archive/`（扁平冷库；确认闸 2026-09-08 修订——规则内免确认留痕）；历史正文不改写。

> 角色：对 R52 评审报告（§七 修复计划）同日办结的修复批做**专项复审**——审改动面本身，不进全库重评（全量重评由五十二轮报告完成，基线同树）。
> 路径经作者确认闸获批（2026-09-06，专项复审命名，不接全量轮号——MP/PM 专项先例）。
> 评审面：9 源码文件 + 5 测试文件（28 用例）+ 6 处文档索引同步，全程只读零改动。

## 一、复审范围与基线

- 指令链：作者「开始修复，全部修复，编排任务做」→ R52 报告 §七 修复计划批 1（P2×3）+ 批 2（P3×2）同日办结（收口记报告 §九）→ 作者「评审下这次修复，编排任务做」→ 本复审。
- 基线：dev HEAD `1ab9c962` + 五十一轮修复批在途工作树 + R52 修复批（工作树未提交待作者指令）。本复审在 R52 修复批之后的同一工作树上进行，只读。
- 复审对象：
  - 源码：`src/check/run.ts`（E-1）/ `src/desktop/main.ts`（A-1）/ `src/format/types.ts`·`yaml.ts`·`global-defaults.ts` + `src/check/runner.ts`（E-2）/ `src/studio/web-next/src/api/prefs.ts`·`stores/prefs.ts`·`components/ui/SettingsAnalysis.vue`（E-2 前端）/ `views/WorkbenchView.vue`（I-1/I-2）
  - 测试：`test/check/r52-epoch-fp-before-rebuild.test.ts`（2）/ `test/desktop/main.test.ts` +R52-A-1 describe（2）/ `test/format/r52-checks-thresholds.test.ts`（6）/ `test/format/r52-global-checks-defaults.test.ts`（6）/ `test/check/r52-check-threshold-config.test.ts`（7）/ `test/studio/webnext/r52-i1-i2-workbench-book-guard.test.ts`（5），共 28 用例
  - 文档：收口记 §九 + 台账 §一 + 总览 1.1 + `01-评审/README`（维护日期/暂存表）+ Dev/Main/README 变更记录 + 根 README 修账

## 二、逐项核验（主审独立复核，未读修复者结论——看代码与测试本身）

### 2.1 R52-E-1 纪元指纹基线先于 rebuild（P2，数据正确性级）——通过

- **修复目标**：消 rebuild 执行至 fp 计算之间的头窗缓存投毒（陈旧红值以新纪元落缓存、指纹自洽终核不失效、红点口径被固化到下纪元）。
- **实现核验**（`src/check/run.ts`）：`epochFp0` 前移至 `hasWiring` 块内、rebuild **之前**（:482-488，try 包裹——fp 前算失败不阻断 rebuild，保持 null 由 :530 `syncTreeIssuesEpoch` 自算兜底）；rebuild 在 :493；批尾终核 :728-735 `epochFpEnd !== epochFp0 → 整批丢弃`。头窗收编进终核防护的注释（:475-481）完整叙述了 R47-30 前移的不足与本次覆盖语义。
- **独立旁证**：无布线短篇不进 `hasWiring` 块（树缓存本就只对有布线书启用，语义无损）；rebuild 失败分支 fp 前算与之解耦（null → sync 自算 → cacheEnabled 判定不变）。
- **测试核验**：用例 1 用 mock 调用序断言「fp 首遍 < rebuild」+ 遍数不变式 2 次（首算 + 终核，sync 复用预计算不自算——R47-30 口径）；用例 2 在 fp 首遍后触碰文风铁律（纪元 A→B），断言第三轮与删库全量逐字节一致——直接钉「头窗不再固化陈旧红值」的语义面。两用例与 r47-tree-issues-epoch.test.ts（5 用例，复审亲跑全绿）构成前移无回归的双重锚。
- **结论**：修复正确、覆盖完整、测试钉值精确。评 9.5/10。

### 2.2 R52-A-1 pickLibrary「在此新建」嵌套书库防线（P2）——通过

- **修复目标**：目标位于既有书库内部（findWorkDir 命中祖先而非自身）时不再放行——内层 `.clwriting/` 建成后抢占 workDir 判定（server 端口/锁根/task-gate 单进程单锁契约被篡改面）。
- **实现核验**（`src/desktop/main.ts:501-527`）：`findWorkDir(dir)` 命中祖先 → `dialog.showErrorBox`（标题「所选位置在另一书库内部」+ 正文含「嵌套书库会使工作目录判定歧义」）+ `continue` 留在选择循环（受 :443 十次封顶约束）；与 `canSwitchLibraryDir`（switch-library 侧同款防线，:422-432）口径对齐；置于 R44-14 git 检查之前。
- **放行臂核验**：书库外普通目录 `findWorkDir` 返回 null → 嵌套检查不中 → 照常走 git 检查 + 大小写警告 + 落库，防线不误伤。
- **测试核验**（main.test.ts R52-A-1 describe）：拒臂（`{ok:false,canceled:true}` 封顶退出 + errorBox 标题/正文关键词 + 零 relaunch）+ 放臂（`workdir.json current` 持久化为证；relaunch 系 R51-A-1 意图式 before-quit 武装，测试面不可达——如实记）。复审亲跑 2 过。
- **结论**：修复正确、双面钉死。评 9.5/10。

### 2.3 R52-E-2 机检阈值五键配置面七层接线（P2）——通过（观察 1 条）

- **修复目标**：`repeat_threshold`/`repeat_chars_threshold`/`max_sentence_len`/`imagery_threshold`/`word_count_tolerance` 五键此前类型/解析/补丁白名单三层全断（作者手写静默丢弃、PUT /config 改键静默不落盘）；STRICT_SHORT_CHECK_IDS 漏 3 项。
- **实现核验（七层逐层）**：
  1. `types.ts` checks 5 键声明带语义注释（含「铁律配了 maxSentenceLen 时本键不生效」「语义为 > 阈才报」「仅长篇 checkWordCount 消费」等边界）✓
  2. `yaml.ts` parse：`parsePositiveNumber` 收值 + 非正数 `log.warn('book.yaml', …)` 按未设（R76-15 口径，调参面写坏不阻断写作）✓；stringify 键存在即输出（现仓库零改动红线）✓；CONFIG_PATCH_LEAVES 5 叶（PUT /config 补丁闸放行）✓
  3. `runner.ts` 四调用点直传 `config.checks?.X`——undefined 直落引擎默认 0.15/200/60/3/30，行为零漂移 ✓；`STRICT_SHORT_CHECK_IDS` 补 `repeat`/`sentence-length`/`imagery-overuse`，注释明确 `word-count` 刻意不列（长篇项，严格模式只作用短篇书，piece-word-\* 已覆盖短篇字数语义）✓
  4. `global-defaults.ts`：GlobalBookDefaults 5 键 + 逐键校验（`unitNum` 占比限 (0,1]——>1 = 比率口径永不命中的死值视作非法；计数类 posInt；容差 posNum）+ applyGlobalDefaults 逐键托底（无硬编码回落〔引擎默认即最终回落〕、书级优先逐键合并非整段翻案、`checksFill` 非空才实例化 checks 段——无键不造段）✓；头注计数滞后如实化 ✓
  5. 前端 `api/prefs.ts` GlobalPrefs 5 键声明（index signature 之前，类型安全）✓
  6. `stores/prefs.ts`：5 refs `undefined` 初值（刻意不进 DEFAULTS——未设 = 引擎默认）；applyPrefs 逐键守卫（占比 (0,1]、其余 >0 + Math.round）；buildCache 全量带（undefined 被 JSON.stringify 丢弃 = 不覆盖盘上已有值，与服务端合并写语义一致）；5 clamp setters（占比 (0,1] 两位小数、连续字数 2-1000、句长 10-500、意象 1-100、容差 1-500）+ 双导出 ✓
  7. `SettingsAnalysis.vue`：「AI 机检」卡 5 数字输入，`onGlobalCheckNum` 共用入口（parseNumericInput 空串/非数字不写 store，R72-11 口径），placeholder 标引擎默认、description 含边界提示（句长项「铁律单独配置时以铁律为准」、容差项「短篇书走短篇字数上下限」）✓
- **服务端零改动核验**（独立读 `src/studio/server/api/prefs.ts`）：library.prefs.put 合并写透传 `{...disk, ...prefs, revision}`（:194）——新键无需白名单；readGlobalBookDefaults 逐键校验即读侧闸。声明成立 ✓
- **消费点旁路核验**（全库 grep 四个机检函数调用点）：唯一旁路 `learn/index.ts:97 checkRepeat(body)`——只传 body 走引擎默认。判定：学习域自复盘是通用分析（非本章机检），五键语义（书级/全局调参）不要求作用其上，**维持现状不修，登记观察**（见 §四）。
- **测试核验**（3 文件 19 用例）：parse 合法收值含小数 / 坏值 warn 好键不连坐 / round-trip 保真 / 未设不落行 / 补丁白名单改键落盘 + 无变化零改写 / global 逐键校验含占比界外剔除 / 托底进段 / 书级优先逐键合并非整段翻案 / 无键不造段 / 四键生效链（默认报黄 → 配置静默）+ 复读双口径（比率/绝对兜底）/ 严格短篇 repeat 升红 + promoteStrictShort 三升一不升钉（word-count 边界）。钉值计算独立复核（REP 8-gram 重复率 ≈0.967、3700 字重复量、68 字句 50% 超长 >20% 阈值、空气 ×4）全部成立。
- **结论**：修复完整、七层一致、19 用例钉死。评 9.0/10（扣观察 1 条与说明性注记 1 条，见 §四）。

### 2.4 R52-I-1 成功 toast 的 await 后复检（P3）——通过

- 实现核验：`onSpawn`（WorkbenchView.vue:200-203）与 `onAutoWrite`（:245-249）在 `await spawnRole/autoWrite` 之后、`ui.toast` 之前补 `if (props.bookName !== book) return`（R70-10/R51-I-1 家族收口到成功路径；注释明确连写消息含 A 书章号、落 B 书更误导）。
- 测试核验：i1-i2 测试用例 1-3（spawn 在途切书 toast 不落 + autoWrite 连写消息不落 + 未切书对照照常）。
- 结论：正确。评 9.5/10。

### 2.5 R52-I-2 切书清 err/state 残留（P3）——通过

- 实现核验：`watch(bookName)`（:100-114）在既有 prompt/draftSaved 清除之后补 `err.value = null; state.value = null`（注释完整说明：err 是本窗动作失败落点、无清除路径——残留即「新书界面挂旧书错误」；state 置空后 refreshState 立即拉新书、在途慢响应由 stateGen 代守卫兜底）。
- 测试核验：用例 4-5（A 书动作失败 err 横幅切书后不残留；getState 慢响应窗口切书瞬间 state 已置空 + B 书响应回填时序）。
- 结论：正确。评 9.5/10。

## 三、测试与门禁验证

- **复审亲跑（L1 相关面）**：R52 五测试面 28 用例全绿 + R47 纪元锚 5 用例全绿（E-1 前移未动摇后向语义）+ A-1 describe 2 用例全绿。全部通过，零失败零跳。
- **L2 终门九件套**（修复收口时亲跑全绿，本复审引用）：vitest 902 文件/6037 过 + 4 跳 0 败（111.4s）+ tsc 0 错 + vue-tsc 0 错 + eslint 0/0 + check:counts 过（根 README 修账 897/6009→902/6037 四处锚点，本复审复跑三个轻量 check 仍绿）+ check:packaging 过 + check:knowledge 过 + e2e 43 过 2 跳（28.6s EXIT=0）；coverage 按 L2 纪律 CI 兜底。
- **文档同步核对**：收口记 §九 逐条与代码实际吻合（修复位置、测试分布、门禁数字、索引同步清单）；台账 §一/总览 1.1 R52 行已回改已收口；`01-评审/README` 两处（维护日期 + 暂存表 6 篇）补记时对 2026-09-06 落盘漏同步如实记档；Dev/Main/README 变更记录 R52 落盘+收口合并条目在顶。

## 四、发现与观察

- **P0×0 / P1×0 / P2×0 / P3×0**——修复批无新增必修项，五项修复目标全部按 §七 计划达成，无实现偏离（偏离实例：E-2 服务端零改动的声明核验成立）。
- **观察 1 条（维持不修，登记）**：`src/learn/index.ts:97 checkRepeat(body)` 是唯一未接五键的机检函数消费点（学习域自复盘走引擎默认阈值）。判定：学习复盘是通用文本分析而非本章机检，五键语义（作者调参）不要求作用其上；若后续学习域需要阈值可配，另行接线（本观察不构成缺陷）。
- **说明性注记 1 条（非缺陷）**：全局占比键校验下限 `>0`（unitNum）与前端 setter clamp 下限 `0.01`、连续字数校验下限 posInt `≥1` 与前端 clamp 下限 `2` 存在微小差异——均落在「合法值收敛」方向，无越界值可经任一入口生效，行为自洽；属边界口径说明，不修。
- **覆盖度差异声明**：本复审为专项复审（9 源码 + 5 测试 + 6 索引），未重审修复面之外的代码——全库面由 R52 全量重评与本轮门禁覆盖。

## 五、质量评分

| 项 | 分 | 依据 |
|---|---|---|
| R52-E-1 | 9.5 | 前移语义精确、注释含设计动机与失败兜底、双用例钉 call-order + 语义面 |
| R52-A-1 | 9.5 | 拒臂放臂双钉、与 switch-library 防线口径对齐、封顶防死循环 |
| R52-E-2 | 9.0 | 七层接线一致、19 用例覆盖 parse/round-trip/补丁/托底/生效链/严格模式全链；（观察 1 + 注记 1） |
| R52-I-1/I-2 | 9.5 | 家族收口到位、注释完整、5 用例含对照与时序 |
| 文档同步 | 9.5 | 收口记逐条与代码吻合、漏同步如实记档、根 README 计数与实测一致 |

**总体：9.3/10（A）**——修复质量高于五十二轮评审基线（8.9）与五十轮修复批（8.9 域均值），五项修复零偏离、测试完备、文档如实。

## 六、收口记（2026-09-06 本复审同日——已收口，无待修项）

- 本复审未发现 P1/P2/P3 必修项（观察维持 1 + 注记 1），按「报告完成≠收口——P1/P2 修复+回归通过才收口」判据：**无待修项 = 收口条件天然满足**，本复审报告记已收口，无修复批。
- 验证证据：R52 相关 28 用例 + R47 锚 5 用例亲跑全绿；L2 九件套（收口已跑）数字引用如上。
- 索引同步：本条落 `01-评审/` + 台账 §一（本复审行，已收口）+ 总览 1.1（文档地图加行）+ `01-评审/README`（维护日期新条目 + 当前暂存表 6→7 篇）+ Dev/Main/README 变更记录（本复审条目）。
- **工作树未提交待作者指令**（五十一轮批 + R52 修复批 + 本复审全在工作树在途）；R50/R47/PM/R48 四报告归档移动待作者确认闸（本复审报告连同后续归档批一并处理）。
- 〔2026-09-06 补记：作者指令「提交改动」——R51 批 + R52 批 + 本复审合并提交 dev `b6c9dabc`（105 文件 +5589/−193，工作树清零）；上行「工作树未提交」状态就此了结。〕

*收口记完。历史正文不改写。*
