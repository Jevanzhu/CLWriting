# win 腿间歇红台账（现行）

- 定位：win CI 腿计时敏感/竞态家族 known-red 的唯一在册正本。RC 全项目重审（GLM-5.3，2026-09-20）P2-3 立档——此前「R0911/R0912 台账在册」仅存 commit 消息自述，库内零实体，处置状态只能考古。
- 纪律：新 known-red 必登本表（文件/根因/处置态）；commit 消息不再作为台账载体。销账 = 根因修复批入库，或 win 腿（单测腿现由 ci.yml 全矩阵承载，tag 推送同样触发）两连绿观察期过。间歇真失败不得靠重跑静默吸收。

## 一、在册条目

| 测试 | 根因 | 处置态 |
|---|---|---|
| `test/document/tree-perf.test.ts` | 绝对墙钟帽（1000ms/250 章）在 CI 慢机系统性超时（530d3625 win 腿实录红；本机 245ms、CI 慢机可越帽） | RC 重审 P2-4 已修：断言改相对口径（buildTree 5 次中位 < 同书裸读全部章文件基线中位 × 20，机器快慢同向缩放）——观察中，rc.1 起 win 腿两连绿后销账 |
| `test/fs/cross-process-lock-renew.test.ts` | 跨进程锁续期等待计时敏感（win 计时粒度/CI 慢机放大） | 在册观察（530d3625 win 腿实录红；重跑定夺） |
| `test/learn/commit.test.ts`（原 `r0911-commit-yield.test.ts`，R0916-7-P3-5 行为命名合并） | 事件循环让出语义的时序断言计时敏感 | 在册观察（530d3625 win 腿实录红；重跑定夺） |

## 二、吸收与甄别机制

- `win-vitest-retry` composite action（ci.yml R0911-G-P1-1d 单接线——desktop.yml 的 R0912-G2-P3-5 份随「CI 触发与门分工」调整摘净：win 腿单测门现由 ci.yml 全矩阵承载，tag 推送同样触发）：首跑绿零成本；首跑红 → 同命令重跑一次——重跑绿 = vitest×tinypool forks 池 teardown `ERR_IPC_CHANNEL_CLOSED` 收尾竞态洗白（win 用例本身全绿的既证形态）；重跑仍红 = exit 1 真失败不吞只延后一轮。上游根治（vitest/tinypool 升级）后接线可撤。
- RC 重审 P2-4 起：首跑输出落盘留痕，重跑洗白面**可见化**——首跑红文件清单以 `::warning … 重跑洗白面` 上浮（ vitest `FAIL` 行摘取，兜底 `Test Files` 汇总行）。每条洗白 warning 须对本表甄别：在册家族 → 记观察；表外新面孔 → 登记新条目再放行，不得视而不见。

## 三、维护约定

- 本表只登 win 腿间歇红；mac/linux 腿间歇红按同款登记（条目注明腿别）。
- 台账与 win-vitest-retry 的分工：台账管「已知什么红、为什么、何时销」；action 管「首跑红的机械区分与留痕」。两者都改时同步本文件。
