# 未收口与挂账台账（单点入口）

- 日期：2026-09-04。状态：**持续维护**。
- 溯源：作者指令「文档里单独未收口的专门做一个文档放」（2026-09-04，同批指令含「已完成的归档」——27 篇已收口评审报告 + 书库平台规范化实施方案同日移入 `Archive/`）。本文件系该指令直接交付物，按命名规范落 `Dev/Docs` 根级（与总览同层的入口档），确认闸记档。
- 体例：只记**未收口 / 待拍板 / 挂账**三态事项与状态、出处；细节不复制（各事项正文以所指出处为准）。事项办结后：本文件状态回改 + 总览对应行同步，**不得只改一处**。办结/销项/超体例细节行随批移出冻结 `Archive/台账历史明细-归档-2026-09-08.md`（2026-09-08 精简批起，本文件只留开放项与存续约束）。
- 冲突优先级：总览 > 本文件。

## 一、未收口评审（01-评审/；2026-09-15 归档批清空后重开——五轮处置批落库待归档，余行均系已收口归档指针与沿革）

- 《全库源码重评五轮-进度与质量-评审-GLM-5.3-2026-09-16》（**重评-0916-五轮**，2026-09-16 纯评审落盘；作者指令「忽略现有的评审文档，重新评审一遍项目源代码，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。」）——**已处置·待归档（处置 = 2026-09-16 五轮重评处置批，作者指令「全部修复」，四路文件互斥后台代理 + 主审亲修核心数据面；处置记 = 行尾 + 报告 §十一；正本 `01-评审/` 未跟踪、归档移位由评审会话自理）**。基线 `51ebee09` 工作树净，零代码改动（L0 纯评审面）。覆盖 = 两波七路文件互斥只读子代理（数据账本/AI/服务端桌面壳/前端 + 机检审稿检索/测试工程/构建CI/横切声明核对；波 2 三路撞账户 5h 限额阵亡、重置后重派全回收记档）+ 主审亲验（七条承重发现逐条对码全属实 + 台账 §三 全量交叉去重三分类 + 关键统计亲测复算）。判定 = **P1×0 / P2×5（全为新发现）/ P3×18（新 13 + 在册族扩展 5）/ nano×20（归并）**：P2-1 = readManifest 容错版读失败静默空清单 → run-tree-issues 树红点聚合整树失明零降级标志（manifest.ts:100-112 + run-tree-issues.ts:174/347/440，违「降级必留痕」自设纪律；run.ts:231 定稿集空基准传导待验证）/ P2-2 = format→document 域级回边（draft.ts:13 import readManifest，违 stable-id.ts:7-8 叶子层纪律；模块级实测无环、域级环事实）/ P2-3 = desktop.yml tag v* 与 package.json version 无一致性校验（产物名错位无门拦）/ P2-4 = 测试批次号化石层（542–603/1171 文件轮次号命名，语义可导航性受损）/ P2-5 = webnext vi.mock 密度 187/266（70%）且 mock 兄弟 store（ui 43 处）。P3 新 13 = 批量定稿失败原因丢弃（useChapterTreeActions doBatchFinalize）/ Pinia store 模块环三组 / chat 等非写稿任务无按书预算配额（runner.ts:587 章号条件）/ prompt 注入残余加固 / check 域 prepared WeakMap 无配对 close（rag 侧已实证同款）/ RAG 分块 UTF-16 口径 / coerceIssue 空串零校验 / runAllChecks bannedWords 零消费 / 发布产物无 checksum / tsup 相对路径清理 / 治理静态扫正则逃逸面 / 小域池化桶稀释（metrics/driver/review）/ 知识层方法论注入未接线（设计留白待拍板）；在册族扩展 5 = doCreate/doCopy 不进 save 锁域 / withManifestLock 防自锁无静态断言 / journal 锁超时降级 fail-open（在册重证）/ CmHost 每击键全文拷贝（在册重证）/ 事件删除第二入口 README 漏报。在册重证≈13 项维持原处置态（报告 §五 列表）+ 记正 1 处（「parse 化停滞 15/113」实为 2026-09-15 机械批已分诊：迁移 12/跳过 8/维持 24）。质量门 = L2 九门主审亲跑一次全绿（vitest 1171 文件 = 7311 过 + 71 跳 0 败 328.81s + tsc/vue-tsc/eslint 0 + 三 check 过〔counts 1171/7311 + 32/53 账实一致〕 + e2e 51 过 2 跳 59.1s + soak 五段 5 OK）。进度结论 = 功能 24/24 阶段全收口、前端 15/15 流程接线、README 七条横切声明逐条对码属实、RC 打磨期（1.0.0-rc.1）综合 ≈97%（子代理独立口径 92% 并列披露，口径差 = 知识层留白与打包滞后是否计缺口）/ 质量结论 = 高（A−，七域 8.5–9/10）。处置建议 = 报告 §九（优先级：P2-1 → P2-3 → P2-2 → P2-4/5 随批渐进）。**处置记（2026-09-16 五轮重评处置批）**：P2×5 全落〔P2-1 readManifestDegraded + 树聚合 manifestDegraded 旗标 + 端点 warnings + run 族裸 close 改道 closeTreeIssuesDb / P2-2 守卫族逐字节上移 document/draft-path.ts + format 方向锁治理门 + 17 处 import 改道 / P2-3 desktop.yml tag-version 门 + SHA256 清单 / P2-4 命名纪律入 CLAUDE.md + §三 G 渐进登记 / P2-5 真 store helper + 7 文件迁移 + store 环治理门（第四组环补登记）+ §三 C 余量登记〕；P3 新 13 = 落 12 + coerceIssue 部分维持（fix/location 空串展示面）+ 知识层注入维持待拍板（§三 F）；在册族 5 = 落 4（doCopy 源锁 / doCreate 无锁论证 / manifest async 重入 fail-loud〔ALS〕/ journal 重试一档 / README 补记）+ CmHost 维持；nano×20 = 修·注记 10 + 维持 10；L2 九门亲跑全绿 = vitest 1181 文件 = 7344 过 + 72 跳 0 败（一次全绿 328.24s，净 +10 文件/+34 用例、差值锚 81 维持）+ tsc/vue-tsc/eslint 0 + 三 check（counts 1181/7344/32/53）+ e2e 51 过 2 跳 + soak 5 OK；批内加固 = re2 探测点 sleep→waitFor 事件协调（在册负载敏感族 5/5 绿）+ r64 迁移件 spy 类型修；明细 = 报告 §十一。另：并行会话随批评审《⑤⑤注释冻结剪枝批-评审-GLM-5.3-2026-09-16》判定合格已入库（P1×0/P2×0/P3×2 处置后全清），无开放项不入本账。
另①：《五轮重评处置批-评审-GLM-5.3-2026-09-16》（2026-09-16 随批评审）——对提交 `4ba1de72` 独立验收：机检账目（搬迁段 176 行逐字节一致 / 71 锚零新增 / 收集 33 恰合）+ 逐 hunk 对读 + coverage 全量 exit 0 实测，判定 **P1×0 / P2×0 / P3×1（提交叙述「src 改 29 新 1 / test 改 27 新 12」拆分 vs numstat 实际 28+1 / 26+12，+2 漂移——总量 74 与净账全对，commit 不可改记正了案）/ nano×3（TTL 载荷缓存 ≤5s 残留·同既有旗词语义不动 / chat_max_calls 正小数怪形·后续随 format 域触达顺手夹紧 / 编排互斥叙述两处交叉记正）**——合格可收口，无返工项，五轮链闭合。**【处置·2026-09-16 作者指令「修掉」】** nano-2 已修：failClosedNumParse 收正整数（正小数同走 warn「值非正整数」+ 落 0 fail-closed）+ chat-budget-gate 既有用例内扩 0.5/3 两形态断言（用例数零漂移免 README 修账）；L2 九门亲跑全绿（vitest 一次全绿 1181 文件 = 7344 过 + 72 跳 353.31s + tsc/vue-tsc/eslint 0 + 三 check 过 counts 零漂移 + e2e 51 过 2 跳 + soak 5 OK）；nano-1/nano-3 维持观察不动。
- 《全库源码重评四轮-进度与质量-评审-GLM-5.3-2026-09-15》（**重评-0915-四轮**，2026-09-15 纯评审落盘；作者指令「忽略现有的评审文档，重新评审一遍项目源代码，最后告诉我项目完成进度，完成质量，结果形成一个文档给我。」）——**已全量处置待归档·四轮处置批（2026-09-15，作者指令「按建议顺序开工」「编排下任务，并发做吧」延续 + 2026-09-16 维持项反转批「全部修复。」；「报告完成≠收口」，正本仍在 01-评审/ 未跟踪、归档移位由评审会话自理）**。基线 `2dd2b849` 工作树净，零代码改动（L0 纯评审面）。覆盖 = 三波九路文件互斥只读子代理（R1-R4 服务端/桌面·CI/AI·RAG/核心数据 + R5-R8〔含 R6 存储基础设施 34 文件、R7 前端逻辑 88 文件、R8 前端视图 118 文件全量；首派两路撞账户限额阵亡重派记档〕+ R9 测试工程）+ 主审亲读（处置批 27 src diff 复核 / main.test.ts 负载 OOM 败因链 4/8/36GB 堆实验 / 全部发现逐条亲验 + 台账 §三 去重：rag buildIndex 闸系 D-P3-5 在册重证归并，余全为新项）。判定 = **P1×0 / P2×1（测试工程）/ P3×21 / nano×21（锚定 14 + 波1 失传 7 仅存计数记档）**：P2 = `test/desktop/main.test.ts`（39 次重导入 × 每 6 个 process 监听器钉死、旧模块图不可 GC 的文件级累积底噪）win×Node 26.8.1 负载下全量门 4/4 worker OOM（堆 4/8/36GB 均耗尽）+ `ci.yml` exclude win+node26 零 CI 信号——空载同树 370.02s 全绿实录（并行会话），负载敏感失稳；主审初判「#35 单用例引爆」经 R9 静态复核（重启链封顶 + 时序矛盾）撤回记正；修法三件套 + CI 盲区披露 = 报告 §三。P3×21 / nano×21 明细与处置建议 = 报告 §四/§五/§十。质量门 = tsc/vue-tsc/eslint 0 + 三 check 过 + e2e 49 过 2 跳（1.0m；31 spec/51 用例盘点与 README 逐字吻合）+ soak 两段 OK + vitest 除 main.test.ts 7190 过 70 跳 2 败（re2-manifest-lock-reentry / r35-search-cache 负载时序抖动，孤立复跑 10/10 销案）。进度结论 = 功能 24/24 阶段全收口、RC 打磨期（1.0.0-rc.1）综合 ≈98% / 质量结论 = 高（A− 维持）。正本 `01-评审/`。**处置记（2026-09-15 四轮处置批）**：**P2×1 处置**〔main.test.ts 文件级 process.on/once 包装透传记账 + afterEach 逐用例拆除 + afterAll 兜底还原（注册行为逐字节不变、监听器寿命收敛单用例、旧模块图随用例脱钉；与两处 vi.spyOn(process,'on') 竞态用例共存实证）+ nano-12 forkBehavior 裸还原 try/finally + #35 断言后 drain 一拍 + nano-13 backlog 直写赋值改 vi.spyOn（P3-20/21 同链销账）+ CI 盲区 README 披露行（win×node26 无 CI 腿如实披露；补腿不立项）；#35 削薄不立项（server-manager.test 单元层已覆盖同场景）〕。**P3×21 = 修 13 + 随 P2 链销 2 + 已过时 1 + 维持登记 5**：修 = P3-1 review.ts 三审流双写点 bookMovedFailure 接线（verdict 在 readJson 窗后防误导 404、run 在写临界段防孤儿目录；r0915-srv-review-bookmoved 2 用例）/ P3-2 单件 finalize 补 'batch-finalize' 同名任务闸拒口径（batch-finalize.test +1）/ P3-3 rename 锁内重名重查（命中跳过登记更新走 repair 兜底；专项测试不立项——注入点不可达，产品钩子得不偿失）/ P3-5 resolveBook 成功臂携带 workDir（rag.ts 三处 ctx.workDir! 裸断言随类型可证消除；加性字段 72 站点零破坏）/ P3-7 saveDraft 三读盘单读收口（preserveStructureFmIn/snapshotBeforeOverwrite 各增 existingRaw 穿参契约，缺省自读、五外部调用方原样；+2 用例含 GBK 防线同形态上抛）/ P3-9 short-index 码点口径（u 标志 + codePointLength + 对拍用例）/ P3-10 revision/ref 章号转可选（§三 158 行销账）/ P3-12 migrate-layout-v3 readdirSync 收进 errors 契约（+1）/ P3-13 relink 改 isDirConfirmedMissing ENOENT-only（+1）/ P3-16 fetchChatHistory 返回处单源归一（+2）/ P3-17 snapshots entries 兜底（+1）/ P3-18 关系图自动路径 !== true（+2）/ P3-19 金句区 capView(100) 渲染帽 + 按需展开（+3，SampleCandidateList 样板）；随 P2 链销 = P3-20/21；已过时 = P3-4（cc89d2f8 已改）；维持登记 = P3-6（注释已论证改动反丢缓存）/ P3-8（posix 字面边角改动反风险）/ P3-11（潜伏面两消费方纯同步）/ P3-14（启动 pre-listen 窗 + 原子写）/ P3-15（毫秒窗一次性）。**nano×14 = 修 6 + 维持 8**：修 = nano-1 死 ?? / nano-4 missing.join 单源 / nano-7 缩进归位 / nano-12/13/14（随 P2/P3-7）；维持 = nano-2/3/5/6/8/10/11 + nano-9（经 document/stable-id 单源取 ulid 系有意耦合，绕开直连反失单源——主审裁定）；失传 7 项按报告口径不追补。编排 = 侦察子代理先行（处置表）+ 主审亲做 G1(P2)/G5(闸族)/G6(三读盘)/P3-10/G7 + 三路文件互斥机械子代理（G2 前端 8 用例 / G3 install 2 用例 / G4 杂项 1 用例）。测试账 = +3 文件/+16 用例（1146→1149 / 声明面 7367→7383；win 预期 7303 过 + 70 跳，差值锚 80 维持）。**二次处置记（2026-09-16 维持项反转批；作者指令「全部修复。」——批一维持裁定 13 项全数反转，主审亲修零代理；明细与手法 = 报告 §十二 批二）**：P3-6 ensureNormColumn 游标迭代内 UPDATE 改键集分页物化（批 512 + 每批事务，同连接查询中 DML 面消除；norm-null-index +1200 行跨批回填用例）/ P3-8 slashRelative 收编 normalizeWinSeparators 成 safe-path 同族第 9 站（posix 字面反斜杠身份保持；draft.test +posix 门用例 skipIf(win32)）/ P3-11 iterateEvents 改每调用新语句（listEvents 保 prepared 缓存；iterate-events +双层重入用例两腿各 6/6）/ P3-14 每书 book.yaml RMW 上跨进程锁（5s 档 testableConst 工厂注入——A4 解构导出不进 ForTest 守卫计数、天花板 71 不变；超时 fail-closed 跳过幂等重试；migrate-defaults +预持锁停走窗用例）/ P3-15 git status 与脏集计算移入 withManifestLock 回调内（幂等复查后、条目循环前；R64-23 注记记正；新文件 r0916-migrate-finalized-status-in-lock 偏序断言 lock-in→status→lock-out）；nano×8 = nano-2 contract 去重死码删 / nano-3 style-entry 冗余三元改 filter 直返 / nano-5 textOut 截断孤儿低位代理剔除（0xDC00 判别）/ nano-6 git-exec ETIMEDOUT 与外部 SIGTERM 分判 / nano-8 walk-md dirReal 递归传递免重复 realpath / nano-9 ulid 直连 fs/id（垫片收敛 document 域；批一「有意耦合」维持裁定随作者指令一并反转）/ nano-10 TabBar·ContextMenu 下拉坐标与翻位随窗口 resize 重算 / nano-11 --composer-min-h 变量化跨文件单源。波 1 失传 7 项明细不可恢复，不在可处置清单，维持原登记不追补。批内记正两处如实记档 = nano-5 初版判据取高位 0xD800（node 复现实证劈点落对内时孤儿系低位段）/ ContextMenu watch 体初版 async 包装多一跳微任务致 re2 焦点断言假红（热路径回内联同步，仅 resize 臂走异步）。测试账 = +1 文件/+6 用例声明（5 中性 + 1 posix 门；1149→1171 系批间他批并进，声明面 7386→7392 / win 7306→7311 过 + 70→71 跳，差值锚 80→81）。批二 L2 终门全绿 = vitest 1171 文件 = 7311 过 + 71 跳 0 败（一次全绿 343.91s）+ tsc/vue-tsc/eslint 0 + 三 check 过（check:counts 修账后复跑一致）+ e2e 51 过 2 跳（1.0m）+ soak 五段 5 OK。
- **已收口归档**：《全库源码重评三轮-进度与质量-评审-GLM-5.3-2026-09-14》（**重评-0914-三轮**）随 2026-09-15 处置批收口、2026-09-15 归档批归档（作者指令「全部修复」→「归档」——P2×3 全修 + P3 修 13/维持 1 + nano 修 14/维持 3；L2 九门 win 一次全绿 **1144 文件 = 7285 过 + 70 跳 0 败**；处置记 = 报告 §十一，正本 `Archive/全库源码重评三轮-进度与质量-评审-GLM-5.3-2026-09-14.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十八。
- **已收口归档**：《重评二轮修复批-优雅简洁复核-评审-GLM-5.3-2026-09-14》（**复审-0914-修复批**）随 2026-09-14 处置批收口、2026-09-15 归档批归档（作者指令「全部修复。」→「归档」——P3×14 + nano×2 全量处置〔含前科 2 项〕，主审亲修零代理，净增 +1 文件/+4 用例；L2 九门 win 一次全绿 1130 文件 = 7194 过 + 84 跳 0 败；处置记 = 报告 §五，正本 `Archive/重评二轮修复批-优雅简洁复核-评审-GLM-5.3-2026-09-14.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十八。
- **已收口归档**：《全库源码重评二轮-进度与质量-评审-GLM-5.3-2026-09-13》随 2026-09-13 修复批收口、2026-09-15 归档批归档（作者指令「完成之后，全部修复。」→「归档」——P2×2 + P3×4 六件全修，主审亲修零代理，净增 +3 文件/+14 用例；处置记 = 报告 §八，正本 `Archive/全库源码重评二轮-进度与质量-评审-GLM-5.3-2026-09-13.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十八。
- **已收口归档**：《全库源码重评-win适配与进度质量-评审-GLM-5.3-2026-09-13》随 2026-09-13 修复批收口、2026-09-15 归档批归档（作者指令「全部修复，开始吧。」→「归档」——P2×3 全修 + P3 13 修/2 维持，主审亲修零代理，净增 +4 文件/+17 用例；L2 九门 win 口径一次全绿 1126 文件 = 7176 过 + 84 跳 0 败；处置记 = 报告 §八，正本 `Archive/全库源码重评-win适配与进度质量-评审-GLM-5.3-2026-09-13.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十八。
- **已收口归档**：《专项精简优化-评审-GLM-5.3-2026-09-12》（win 线第九篇）随 2026-09-15 归档批归档（收口条件已满足〔执行 + L2 全绿〕；残余登记〔catalog 拍板 / TTL 族 / resolveBook / defineRoute 等〕= 报告 §五 + 台账 §三 H 域，正本 `Archive/专项精简优化-评审-GLM-5.3-2026-09-12.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十八。
- **已收口归档**：《mac平台适配-评审-GLM-5.3-2026-09-13》（**复审-0913-mac适配**）随 2026-09-14 mac 适配修复批收口归档（作者指令「全部修复，并发做。」——P2×1 + P3×14 全处置：修 P2×1〔README macOS 须知补 arm64 专行披露〕+ P3×11〔AppleDouble 过滤 / 反斜杠归一族 normalizeWinSeparators 单源收编 8 处 / samePath NFC darwin 臂 / git ENOENT 分平台文案 / ⌘F 全局查找链 / 触控板缩放 wheelScale / mac artifactName / 字体测试形态 / mac 打包态关窗链冒烟接线 / 折叠注释族记正 / APFS 论据记正〕+ P3-6 行为维持 + 文案披露单源（BOOK_NAME_INVALID_REASON）+ P3-4/P3-13 维持登记 §三 E 域；编排 = 波 1 四路文件互斥修复代理 + 主审亲修 8 处/5 文件同族收尾；L2 终门亲跑全绿 **1123 文件 = 7261 过 + 7 跳 0 败** + tsc/vue-tsc/eslint 0 + 三 check + e2e 49 过 2 跳；收口记 = 报告 §八，正本 `Archive/mac平台适配-评审-GLM-5.3-2026-09-13.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十五。
- 《win线并树合并批-评审-GLM-5.3-2026-09-13》（**复审-0913-合并批**，2026-09-13 纯评审落盘）随 2026-09-13 修复收口批收口归档（作者指令「全部修复！」——判定「优雅达标·简洁有失守」全处置：P2×2 修 = Archive/README 残留「当前含 26 篇」win 树快照行删 / 总览 §1.1 设计方案行改已实施完成口径；P3 记正×2 = 合并批批记行 export ≈214 处/119 文件 + 自动并合 17 文件（合并提交信息历史正文不改写）；P3-3 括号 ［］→〔〕 统一 ×3 行；P3-4 台账历史明细 §十一/§十二 双空行收敛（win 侧既有）；P3-5 登记本账 §三 B 域〔rag rebuild 闸备查——较报告 §七 建议的 G 域改登记 B 域系域归属纠偏，收口记如实记档〕；P3-6 登记本账 §三 E 域〔trash.delete 竞窗备查，与 locateMergeByDisk 反查歧义联动拍板〕；P3-7 Dev README 04-调研 行补目录存续口径注记；P3-8 批记行时序倒挂维持〔git 合并第一父链骨架自然形态，报告 §五 注记〕。L0 零测试（纯文档面），check:counts 复跑过；收口记 = 报告 §九，正本 `Archive/win线并树合并批-评审-GLM-5.3-2026-09-13.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十四。
- 《源代码抽样重评-进度与质量-评审-GLM-5.3-2026-09-13》（**复审-0913-源码**，2026-09-13 纯评审落盘）随 2026-09-13 源码重评修复批收口归档（作者指令「开始修复吧，全部修复。」——P1 = 在库复审-0913-结构同项并批同修 + P2 新 ×3 全修〔structure.ts 清单路径裸 join / leads 开启章守卫 / style readBaseline 静默降级〕+ P2 在库重证 ×2 并批〔prefs 族 / summary 双源〕+ P3 随批收 20 件 / 维持登记 7 + 拍板项 1〔export displayNum 单调性〕+ 待核 2 查证销案；编排 = 波 1 四路文件互斥修复代理〔A3 完整回收，A1/A2/A4 限额阵亡前改动已落盘、主审逐 diff 复核零回退 + 亲补 helper 换装缺口〕+ 主审亲修文档两件；L2 终门亲跑全绿 **1115 文件 = 7213 过 + 5 跳 0 败**〔净增 1 文件/17 用例〕+ coverage 91.71/87.27/96.23/91.71 + tsc/vue-tsc/eslint 0 + 三 check + e2e 49 过 2 跳〔41.0s〕+ soak 两段 OK；处置明细 = `Archive/README.md` 修复批收口批批记行 + 台账 §三 处置回填；收口记 = 报告 §八，正本 `Archive/源代码抽样重评-进度与质量-评审-GLM-5.3-2026-09-13.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十三。
- 《章节结构操作三批-评审-GLM-5.3-2026-09-13》（**复审-0913-结构**，2026-09-13 纯评审落盘）随 2026-09-13 源码重评修复批并批收口归档（作者指令「开始修复吧，全部修复。」——P1×1〔doMergeUndo 缺前置 flushUnsaved，本轮独立重证〕+ P2×5 六件全修：P1 前置落盘〔useChapterTreeActions.doMergeUndo，调用序 + 失败路 2 回归〕/ summary findChapterFile 收编 chapterPathByNumber 单源 / vitest 三件套 helper 收编 `test/helpers/structure.ts`（bind 工厂参数化）/ e2e 三方 helper 收编 `test/e2e/tree-actions.ts`（clickSubmenuItem 收 `string | RegExp` 超集统一，三 spec 10 用例零漂移）/ README:117 win 预期增量链补批 A +6/+48（1095+4+6+8+1=1114 算术闭合）/ 执行方案头部改「已实施完成」；P3×19 维持登记（明细 = 报告 §四，两拍板项维持待拍板）；收口记 = 报告头部收口补记，正本 `Archive/章节结构操作三批-评审-GLM-5.3-2026-09-13.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十三。（win→mac 合并批 2026-09-13 注：win 树本节曾挂本报告在库行系 win 合并 mac 时点早于 mac 收口，合并取收口态、在库行不回填。）
- **已收口归档**：《R0912-ds41收口批复核-评审-GLM-5.3-2026-09-13》随 2026-09-13 P1 修复批收口归档（作者指令「开始修复！」——P1×1 按方案 a 修复：三页根标记类 `is-drag` 改名 `is-desktop`〔Library/Shelf/Welcome 各模板根绑定 + scoped 后代拖拽选择器两处，注释随批记因〕+ utilities.css 头注记 `.is-drag` 专属语义〔真实拖拽容器单类，页面根桌面态标记一律 is-desktop〕+ 防复活锚 `test/studio/webnext/r0913-is-drag-scope.test.ts` 3 用例〔静态源码守卫：三页根零 is-drag 残留 / 四壳层仍绑 is-drag / utilities.css 有 .is-drag 无 .is-desktop〕+ 根 README 修账五处〔1101/7084 → 1102/7087，win 1102 = 7008 过 + 84 跳，与 7087 − 79 吻合〕；P3 择收登记 = §三 B/C/D/E 四行；P3-1 记档失实经 `Archive/README.md` 收口批批记行记正〔前批历史正文不改写〕；P3-6 维持。门亲跑全绿：守卫锚 3/3 + webnext 1663 过 + 全量 vitest 复跑 **1102 文件 = 7008 过 + 84 跳 0 败** + vue-tsc/tsc 0 错 + eslint 0/0 + counts 过；收口记 = 报告 §七，正本 `Archive/R0912-ds41收口批复核-评审-GLM-5.3-2026-09-13.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十二。
- 《全量代码重评与全覆盖精读-进度与质量-评审-GLM-5.3-2026-09-12》（**重评-0912-4**，合并线第十三篇）随 2026-09-12 修复批收口归档（作者指令「全部修复」→「编排下任务，并发做吧。」——P1×1 + P2×5 六件全修 + 在库 deepseek-P2-2 并批 + SearchPanel 切书 q 随批；P3≈45 按域登记（归并既有族为主，跨域索引行 = §三 头注）；L2 终门复跑全绿 **1099 文件 = 7084 过 + 5 跳 0 败** + coverage 四值 91.88/87.39/96.28/91.88 + e2e 43 过 2 跳 + soak 两段 + electron-builder --dir 出包 fontlist 实锤；修复中实抓两件如实记档——drainDraftSaveChainsUnder 首版判式漏「链键恰等于书根」形态恒 no-op（回归当场红，R71-10 双口径修正）+ doOpen 改 getContentPayload 打破 32 文件 mock 面（四路代理「委托式默认」处方全绿回收）；收口记 = 报告 §九，正本 `Archive/全量代码重评与全覆盖精读-进度与质量-评审-GLM-5.3-2026-09-12.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十。（win 合并批 2026-09-13 注：mac 树本节原挂《全量代码重评-进度质量与精简优化-评审-deepseek-v4.1-flash-2026-09-12》在库未收口行，该报告在 win 树已随 R0912-ds41 修复批收口归档（原行冻结台账历史明细 §十一），合并取收口态、在库行不回填——win→mac 合并批 2026-09-13 落实该口径。）
- **已收口归档**：《全库源码优化专项-评审-GLM-5.3-2026-09-14》（**复审-0914-优化**）随 2026-09-14 优化修复批收口归档（作者指令「全部修复！」——P1×7 + P2 六域 A1–F7 全修 + P3 择收〔维持项随报告收口补记备查〕；编排 = 主审亲建基础件（errMsg / codePointLength / testableConst 三单源）+ 三波 9 路文件互斥修复代理（在途 ≤4 全回收）+ 主审逐 diff 抽查 + 主审亲修两件漏派 P2 自纠〔D3 stream.ts chat 四端点拆 `api/chat.ts` / E7 `useLibraryIpc` 收编双页〕；落地实证逐项 grep 亲验 = errMsg 三目 210→17 残量（存量分批如实记）/ main.ts 2259→710 / yaml 三面表驱动 + 713 行快照先钉后改 / TTL 缓存 16 壳 / desktop 四模块拆分 等，全清单 = 报告收口补记；L2 终门复跑全绿 **1126 文件 = 7282 过 + 7 跳 0 败**（净增 3 文件/+21 用例）+ tsc/vue-tsc/eslint 0 + 三 check 过（根 README 修账后）+ e2e 49 过 2 跳；收口记 = 报告头部收口补记，正本 `Archive/全库源码优化专项-评审-GLM-5.3-2026-09-14.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十六。
- **已收口归档**：《全库代码重评-进度与质量-评审-GLM-5.3-2026-09-14》随 2026-09-14 全库重评修复批收口归档（作者指令「全部修复！」〔续「继续」×2〕——P1×0 / P2×6 全修〔review-verdict 裁决端点 409 运行闸 / doCopy 点段双拒 / healResult 消费面上移 WorkspaceShell〔缓存刷新抽 shared/doc-freshness 单源〕/ 铁律收起脏守卫 / 账本归档读旧合并重写〔L-P6 变体回落保留〕/ rag openRagDb PRAGMA 对齐 events N3〕+ P3 择收 25 件全收 / 登记 8 处置（7 维持登记 + #33 头注显式化落地）/ 销案 2 维持；编排 = 波 1 四路文件互斥修复代理（A 服务端+机检+RAG+进程搜索 / B document+format / C desktop+脚本+配置 / D ai 适配器；在途 ≤4——B 路撞 5 小时用量限额阵亡、余量主审接续逐 diff 复核零回退）+ 主审亲修 web P3 十二件（含 api/stream→workbench 改名 22 测试改指）+ P2-3/P2-4/P2-5 三件；L2 终门亲跑全绿 **1134 文件 = 7304 过 + 7 跳 0 败**（净增 8 文件/+22 用例）+ tsc/vue-tsc/eslint 0 + 三 check + e2e 49 过 2 跳〔38.1s〕；处置回填 = 台账 §三 + 报告 §十收口记，正本 `Archive/全库代码重评-进度与质量-评审-GLM-5.3-2026-09-14.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §十七。
- **在库评审行（清空态沿革·win 线）**：第十二篇《全量代码重评-进度与质量-评审-GLM-5.3-Flash-2026-09-12》（**重评-0912-3**）随 2026-09-12 修复批收口归档（作者指令「全部修复，编排下任务，并发做。」——P2×4 全修 + P3 择收 42 件随批 / 维持 14 件〔主审亲修 4 + 两波八路文件互斥代理，在途 ≤4，主审逐 diff 复核全量〕，L2 终门复跑全绿 **1095 文件 = 7063 过 + 5 跳 0 败**〔净增 7 文件/53 用例〕+ counts 修账 7010→7063 五处后绿 + e2e 43 过 + soak 两段 OK + 出包 fontlist 实锤；收口记 = 报告 §八，正本 `Archive/全量代码重评-进度与质量-评审-GLM-5.3-Flash-2026-09-12.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §九。第十篇《全量代码重评与内存性能专项-评审-GLM-5.3-2026-09-12》随 2026-09-12 修复批收口归档（作者指令「全部修复，编排下任务做。」——P2×4 全修 + P3 随批收 38 / 维持登记 9 / 单立 1〔两波五路文件互斥并发代理 + 主审逐 diff 复核修正 1 处：C2-P3-1 区间扫描序改回原数组序并修正失实锚注〕，L2 终门九件套亲跑全绿 win 口径 1055 文件 = 6767 过 + 80 跳 0 败；收口记 = 报告 §十一，正本 `Archive/全量代码重评与内存性能专项-评审-GLM-5.3-2026-09-12.md`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §一（尾续）。
- 《全量代码重评-进度与质量-评审-GLM-5.3-2026-09-12》（**重评-0912-2**，合并线第十一篇）随 2026-09-12 修复批收口归档（作者指令「全部修复，编排任务做。」——P2×5 全修〔rewrite×chat 互斥 / responses 回填门 textYielded 分家 / 布线锁键三侧补 NFC 归一〔含 files.ts PUT 侧同源缺口〕/ 收割链缓存异步读 + 按 doc 让出 / manifest 字段类型守卫〕+ P3 择收七件随批〔其余维持登记〕+ L2 终门九件套复跑全绿 **1088 文件 = 7010 过 + 5 跳 0 败**〔130.87s；净增 4 文件/26 用例全 r0912-2- 锚〕+ e2e 43 过 2 跳 + soak 两段 OK + 主 README 修账 1084/6984 → 1088/7010；处置明细 = `Archive/README.md` 修复批收口批批记行 + 报告 §十收口记，正本 `Archive/`）——原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §八。
- （清空态沿革·mac 线）重评-0911b（独立重评第八篇）与重评-0911c（GLM-5.3-Flash，mac 线同日第二轮独立重评）两报告随 2026-09-12 R0912 修复批（mac 线）收口归档 `Archive/`（扁平，各补收口记），§一 两行原样冻结 `Archive/台账历史明细-归档-2026-09-08.md` §七；两报告全部开放项处置明细 = `Archive/README.md` R0912 修复批批记行 + 两报告收口记。**双线篇号歧义注**：mac 线与 win 线对第九/十篇各自编号（mac 第九篇 = 重评-0911c；win 第九篇 = 专项精简优化、第十篇 = 重评-0912），2026-09-12 dev←win 合并批起引用一律以篇名 + 日期为准。
- 暂存区此前清空态沿革：六篇已收口评审（2026-09-08~09-10）随 2026-09-11 归档批移入 `Archive/`（六行原样冻结 `Archive/台账历史明细-归档-2026-09-08.md` §一 尾续）；第七篇（全量代码重评与进度质量-评审-GLM-5.3-2026-09-11）随 2026-09-11 修复批收口归档、第八篇《全量代码重评-进度质量与精简优化-评审-GLM-5.3-2026-09-11》随同日 win 线重评修复批收口归档（P2×4 = 修 3 + 单立 1、P3 随批收 13 / 维持登记 13 / 归并既有行 3，L2 终门九件套亲跑全绿 win 口径 1051 文件 = 6743 过 + 80 跳 0 败；收口记 = 报告 §八；mac 线 R0912 修复批对同批发现另行处置并补头部收口补记——两线收口记并存报告头部；原行均冻结件尾续，报告正本在 `Archive/` 扁平、头注已补归档记）。01-评审/ 未入库游离件两线注记口径不一（mac 线批核验本工作树无 deepseek-flash-2026-09-10 对应文件、疑 win 线在途；win 线批记另有未入库游离件 1 篇处置待作者定）——以合并后盘面实况为准，见主 README。
- 历轮已收口评审：终态见 `Archive/总览历史明细-归档-2026-09-08.md` §二/§五；本节原已收口行与 2026-09-08 整理批注行原样冻结 `Archive/台账历史明细-归档-2026-09-08.md` §一。

## 二、未收口方案（02-执行/ 与 03-设计/）

- `02-执行/章节结构操作-设计方案-2026-08-30.md` + `02-执行/章节结构操作-执行方案-2026-09-04.md`——**已拍板归位 + 已实施完成**（2026-09-13 阶段 24 全三批 A/B/C 收口〔win 合并批复核批 2026-09-13 状态改写：原「实施待作者指令」已随实施完成失效〕并随 win 合并批入库；执行方案头部「待开工」措辞滞后已随 2026-09-13 源码重评修复批收口批修正为「已实施完成」〔复审-0913-结构 P2〕；残留开放项 = §三 三项待拍板〔inline 章号盲区 / 显示序跨卷分组 / undo 反查歧义——三项均已随 2026-09-15 拍板快断批拍板落账：前端拼回档 / 维持档 / 择最新档，行见 §三 C·E 域〕；状态正本 = 总览第三节阶段 24 行；留洞制/fm `序`/`并入` 终态要义与 S2-S5 批次以两正本为准；原行全量 = 冻结件 §三）。

## 三、代码挂账（评审链登记——按域去重看板）

> 2026-09-11 整理批（作者指令「根据当前代码，来整理和更新下文档，特别是目前未做未修的一些项目。」）：本节由按评审轮次七行（R1010c/R1010b/R1010/R0910/R0909/PM-10/PM-12）重构为按域去重看板——交叉重复项并一（switch-provider / PM-12 / chapterNoFromName / CI 空窗 / asar / e2e 顺序契约等各 2~3 处归单）；重构前原行全量冻结 `Archive/台账历史明细-归档-2026-09-08.md` §六。
> 核对：本批对 11 项代表挂账按 HEAD `be7b1170`（dev←win 合并树，工作树净）逐一对码（〔核对 2026-09-11〕标），全部仍在位、无一项因合并失时效；其余各项系各出处批 file:line 亲验登记且其后零代码改动，结论沿承。处置态：【待拍板】需作者决策 /【单立】需独立批或重构 /【维持】权衡备案不修 /【有意】有意为之。
> 出处短名（各主题头部列全，条目不重复；带 ※ 六篇正本 2026-09-11 归档批/修复批与 2026-09-12 修复批移入 `Archive/`，节号不变；另两篇随 mac 线 R0912 批归档）：※独立复审 = `Archive/全量代码独立复审-进度与质量-评审-GLM-5.3-2026-09-10.md`；※内存专项重审 = `Archive/全量代码重审与内存专项-评审-GLM-5.3-2026-09-10.md`；※重评-0910 = `Archive/全量代码重审与进度质量评估-评审-GLM-5.3-2026-09-10.md`；※重评-0909 = `Archive/全量代码重审与进度质量评估-评审-GLM-5.3-2026-09-09.md`；PM 专项 = `Archive/性能与内存专项审查-评审-2026-09-05.md`；※重评-0911b = `Archive/全量代码重评-进度质量与精简优化-评审-GLM-5.3-2026-09-11.md`（已收口归档——win 线 2026-09-11 重评修复批〔§八收口记〕+ mac 线 2026-09-12 R0912 修复批〔头注收口补记〕双线并存；独立重评第八篇）；重评-0911c = `Archive/全量代码重评-进度质量与精简优化-评审-GLM-5.3-Flash-2026-09-11.md`（mac 线 2026-09-12 R0912 修复批收口归档，§九收口记）；※重评-0912 = `Archive/全量代码重评与内存性能专项-评审-GLM-5.3-2026-09-12.md`（已收口归档〔2026-09-12 修复批〕，win 线编号第十篇，2026-09-12）；重评-0912-2 = `Archive/全量代码重评-进度与质量-评审-GLM-5.3-2026-09-12.md`（已收口归档〔2026-09-12 修复批〕，合并线第十一篇，2026-09-12；原行指 `01-评审/` 在库——本批随盘面改指，收口见 §一 行）；重评-0912-3 = `Archive/全量代码重评-进度与质量-评审-GLM-5.3-Flash-2026-09-12.md`（已收口归档〔2026-09-12 修复批〕，合并线第十二篇〔GLM-5.3-Flash 主审〕，2026-09-12；收口记 = 报告 §八）；重评-0912-4 = `Archive/全量代码重评与全覆盖精读-进度与质量-评审-GLM-5.3-2026-09-12.md`（已收口归档〔2026-09-12 修复批〕，合并线第十三篇，2026-09-12——产品码全覆盖精读批：7 域子代理全量 + 波 3 四路限额阵亡转主审定向 4 域，P1×1 + P2×5 全对码实锤，编排/覆盖账 = 报告 §三；收口记 = 报告 §九）；复审-0913-源码 / 复审-0913-结构 = `Archive/`（2026-09-13 源码重评修复批收口归档）；复审-0913-合并批 = `Archive/win线并树合并批-评审-GLM-5.3-2026-09-13.md`（2026-09-13 修复收口批收口归档）；全库重评-0914 = `Archive/全库代码重评-进度与质量-评审-GLM-5.3-2026-09-14.md`（2026-09-14 修复批收口归档，§十收口记）

> 重评-0912-4 P3 批注（2026-09-12 修复批登记）：P3≈45 以归并既有族为主（B 域 config/prefs 越界族已升 P2-1 单立并处置 / D 域 TOKEN_COEFFICIENTS / E 域 chat 截断与事件读链 O(N) / G 域 sleep 脆弱面·巨件·desktop.yml 三连 / D 域 setting-rule BMP-only / F 域 learn repeat_threshold / B 域 completion-names 已并批处置——均既有行重证不另开行）；新登记代表项 SearchPanel 切书不清 q 已随修复批同修销账；其余新 P3 为各域小件（注释口径/样板冗余/低频边角）——【登记·待择收】明细汇总口径 = 归档正本报告 §五（评审会话未逐条落档，择收时以归档正本为准）。

> 全库重评-0914 P3 批注（2026-09-14 落盘批登记；2026-09-14 修复批收口回填）：P3×33 = **择收 25 / 登记 8**（报告 §四 表逐行；§八 prose 曾写「择收 22 /「登记」11」系笔误，随报告 §十收口记记正）——择收 25 件全修随批（明细 = 报告 §十收口记，代码侧锚「全库重评-0914 P3-N」注释逐处落地；web 含 api/stream→workbench 改名 22 测试改指）；登记 8 处置 = 7 件维持登记备查〔#3 ai contract 枚举域外值 / #8 calibrate-tokens argValue（既有 G 域行重证）/ #14 lead-finalize lock-timeout 死码（拍板意图）/ #15 writePieceList 零接线（既有 E 域行重证）/ #29 isWithinRoot 不对称（回填 E 域行）/ #31 rosterNamesCache（回填 F 域行）/ #32 chat-bridge sourceIdxs（回填 E 域行）〕+ #33 RAG 重建外部闸依赖头注显式化落地（回填 F 域行）。P2×6 处置回填各域行；**登记位置纠偏**：落盘批 P2 行原落于 D/F/G 邻域（与册注「P2×6 已按域入 B/C/E/F 行」不符），随处置回填归位——P2-3/P2-4 → C 域、P2-2/P2-5 → E 域、P2-6 → F 域。

### A. 桌面壳（Electron main / 打包）——出处：PM 专项 §8.1 / 内存专项重审 §十 / 独立复审 §十 / 重评-0910 §9.4·§十 / 重评-0911b §3.3·§四 / 重评-0912 §三 / 重评-0912-2 §3.4 / 重评-0912-3 §3.2

- **asarUnpack 通配缺 `dist/` 前缀（重评-0912 A-P2-1）**：`electron-builder.yml:20` `desktop/fontlist` 对 asar 内路径 `dist/desktop/fontlist` 恒不命中（files 规则决定 asar 内带 dist/ 前缀；app-builder-lib FileMatcher 语义经上游源码+Minimatch 实测核证）→ 上批 R0911-A-P2-1 的 mac 打包态字体二进制外置**实际无效**，运行时改写期望 `app.asar.unpacked/dist/desktop/fontlist` 必 ENOENT 回落慢路径；且 `check-packaging.mjs:153` covers 判定锚定同款错串（配置改对门反而红=假绿）。修复 = yml 改 `dist/desktop/fontlist` + 门与锚测试随改 + `build:desktop:dir` 实测落位。【已处置·2026-09-12 修复批 R0912-A-P2-1：yml 补前缀 + 门随改（旧错误形态判红 + 反向锚测试）+ Minimatch 9.0.9 实证两形态；darwin DMG 打包态实测仍挂本域既有单立】
- 重评-0912 A 域 P3 三条随修复批顺手：server-manager 崩溃封顶决断链无 .catch（对话框 reject 则决断落空）/ bootstrap storeCache 整覆窄竞窗 / initialBookArgvOnly 等价函数与 spawn 骨架重复收敛——明细 = 重评-0912 §四 A 节。【已随批·2026-09-12 修复批 R0912（A-P3-1～6 六项；A-P3-4 兜底 = 记错误日志保持不重启）】
- migrate-layout-v3 `草稿-N.md` 目录名形态 readFileSync EISDIR 被 catch 吞为 undefined 后走 rename 目录（重评-0911b A-P3-4，极边角不致错）。【维持】
- font-cache load 路径超时只弃等待不杀子进程（font-list 不暴露句柄，R40-28 头注既有备案沿用；重评-0911b A-P3-5 重证）。【维持】
- **PM-12 残留 = DMG 打包态实测复验（原「生产 kill 接线 + 字体枚举孤儿」主项已随 2026-09-11 修复批 A-P2-1 链收口）**：tsup 构建期拷贝 fontlist 二进制入 `dist/desktop` + electron-builder `asarUnpack` + `main.ts` `darwinFontListCommand` 注入（asar→asar.unpacked 同位改写）+ check-packaging 静态/dist 双门全落地（细节 = `Archive/全量代码重评与进度质量-评审-GLM-5.3-2026-09-11.md` §十 A-P2-1 段）；原「打包态路径不可解」的待拍板理由失效（作者指令「全部修复」口径下并链办结）。残留：真装 DMG 验字体下拉主路径 + 超时 kill 行为的一次实机复验。【单立·打包态实测】【处置注·2026-09-15：作者指令——DMG 实机验留 mac 端同事实机执行，win 线不阻塞、单立转交 mac 侧】
- **asarUnpack 通配不命中真实产物路径（重评-0911c P2·主审出包实锤，与上条 DMG 实测并批）**：`electron-builder.yml:21` `desktop/fontlist` 不匹配 asar 内实际路径 `dist/desktop/fontlist`（无 `**/` 前缀 minimatch 不命中；smartUnpack 只兜 node_modules 来源）——`electron-builder --dir` 实测 unpacked 仅 node_modules/font-list 自带二进制、`/dist/desktop/fontlist` 仍在 asar → 打包态 darwinFontListCommand 指向不存在路径 → spawn ENOENT 恒回落 font-list 库（字体功能不断），R0911-A-P2-1「超时必杀」加固打包态恒不生效、挂起每次白付 10s 熔断；门测试只锁模式字符串不验匹配语义（`check-packaging.test.ts:87-101`）。修法：pattern 改 `**/desktop/fontlist` + 门测试同步 + DMG 实包验证。**处置（R0912）**：模式改 `**/desktop/fontlist`（electron-builder.yml）+ 门同步校验「可命中 dist/desktop/fontlist」、裸模式判红 + 门测试密闭化；`electron-builder --dir` 出包复验 `app.asar.unpacked/dist/desktop/fontlist` 在位（137KB 带执行位），打包态 spawn 链恢复。【已处置·R0912】
- close/quit 链确认框同步泵消息循环（showMessageBoxSync 冻结三窗；A-P3-3，R1010-P3 G7-② 同族备案重证）。【维持】
- uncaughtException 200ms 兜底窗（R44-17 备案——窗内二次异常仍可丢退出）。【维持】
- isTrustedSender 拒绝路径 undefined 类型契约：正常形态不可达，desktop.d.ts 全 widen 牵面前端判空面〔核对 2026-09-11：`src/studio/web-next/src/desktop.d.ts:33` 成功路径 void 如实标注在位〕。【单立·待拍板】
- asar 冗余 node_modules（打包体积）。【单立】
- server-main 信号兜底修复无测试装置（import 期副作用，入口解耦后补）。【单立】
- Node libuv 线程池限制（平台性备案权衡）。【维持】
- A 域 P3 登记（重评-0912-2 §3.4）：bootstrap recent 失效过滤只改内存不落盘 / worker 化 rebuild 章读缓存每作业冷启动（性能项）/ 冷启动竞窗 menu-action 推送无渲染层就绪握手（单次点击丢失可重点）/ linux font-list load 超时不 kill（R48-17 在案重证）/ recent-filter await 窗内 recent 整覆（R0912-A-P3-5 姊妹位）/ 退出窗内可再开子窗（无 appTearingDown 守卫）/ loadLeadFromCache 生产零调用（测试资产自注）。【登记·待择收】
- A 域登记（重评-0912-3 §3.2，GLM-5.3-Flash 全量重评）：readBooksStrict stat 失败归空表绕过 DA-3 拒写（books.ts:117-122 非 ENOENT 未分诊；repairBooks 兜底故 P3）/ launch fork 后停机检查 kill 无等待升级链（server-manager.ts:361-364，killProcAwaitEscalating 未收编此路径）/ uncaughtException 200ms backstop 截断 stopChild 2s race——kill 从未发出成孤儿（main.ts:2067-2075，与 R44-17 备案窗不同后果面）/ second-instance 缺 darwin `app.focus({steal:true})`（main.ts:265，点窗可能不置前）/ removeBookEntry 双版写段无 try/catch（books.ts:368/396，与 appendBookLocked 收编不对称）/ 重复退出信号无强退通道（优雅停机取舍，备案）/ isTrustedSender undefined（**归并既有行**）/ asar 冗余 node_modules（**归并既有行**）。【已处置·R0912-3：前五件随批收口——readBooksStrict stat 失败按 errno 分诊（仅 ENOENT 归空表，其余归 null 走 DA-3 拒写面，调用链逐一核实有既有降级接手）〔**win 平台补·2026-09-12 合并复核批**：win 对「路径中间组件是普通文件」的 stat/readFileSync 均返 ENOENT（POSIX 归 ENOTDIR），纯 errno 分诊在 win 上会把 `.clwriting` 被损坏成文件的病态态误判为首启缺文件而回空表、绕过 DA-3；ENOENT 分支再复核父目录真身（存在且非目录 → null 拒写，父链本身缺失 = 真首启才归空表）——books-guard #33 用例原在 win 判红（`expected [] to be null`，未改测试，修复后即绿）〕/ launch fork 停机检查收编 killProcAwaitEscalating（等杀链走完再抛 SHUTDOWN，对齐握手超时先例）/ uncaughtException backstop 到点先经 manager 新增 killNow() 同步发 kill 再退出（无停机悬挂、正常退出零改动）/ second-instance darwin 补 app.focus({steal:true})/ removeBookEntry 双版写段 try/catch 对齐 appendBookLocked（同步/异步逐位一致）；重复退出信号维持备案；末两件归并既有行维持】

### B. 服务端（studio/server）——出处：独立复审 §十 / 内存专项重审 §十 / 重评-0909 §六·§九 / 重评-0910 §9.4 / 重评-0911b §3.3·§四 / 重评-0911c §4.2 / 重评-0912 §三 / 重评-0912-2 §3.2·§3.4 / 重评-0912-3 §3.2 / 重评-0912-4 §四 / R0912-ds41 收口批复核 §三·§四 / 复审-0913-合并批 §五 / 复审-0913-mac适配 §二·§四 / 全库重评-0914 §三

- **review-verdict 端点无运行闸——三审完成写静默清除在途已确认裁决（全库重评-0914 P2-1·主审亲验）**：`review.ts:253-261` 完成写 payload 全新构造不含 verdict 且不合并，`:285-345` 裁决端点无 `reviewRunning` 闸（对照 `:119-123` 三审端点自身 409 闸）；R-16 双读只护「三审在两读间完成」反方向；前端裁决钮跑审期间可点（ReviewPanel.vue:181 仅 `verdictSaving` 禁用，竞窗 UI 可达）。修法 = 裁决入口挂 409 REVIEW_RUNNING（最小）/ 完成写锁内 RMW 合并（彻底，verdict 保留语义留拍板）。**处置（全库重评-0914 修复批 2026-09-14）**：取最小闸档——裁决端点在解析出 name+docId 后、写盘前查 `reviewRunning`（reviewRunKey），命中 409 REVIEW_RUNNING（同码同文案对齐同文件三审端点自身闸）。**不排队**——排队只会把旧 verdict 在完成写之后覆写回去、时机不可预期；R-16 写前重读保留不动；完成写 RMW 合并一档未取（verdict 保留语义维持留拍板）。`__setReviewRunning` 增可选第三参 docId 供闸态预置；回归 = r0914b-review-verdict-gate 2 用例（真实 docId 键闸态下裁决 409 且不落 verdict 信封 / 闸释放后 200 正常落盘不误伤空闲路径）。【已处置·全库重评-0914 修复批】
- **TTL 缓存壳族第 4 份照抄 + completion-names 缺 in-flight 单飞（R0912-ds41 收口批复核 P3-2）**：completion-names 壳与 settings（R46-16）/overview（R47-7）/foreshadows·rhythm（R44-8）逐行同构（TTL 5000/MAX 32/三件套注入钩子/FIFO），四壳齐备后抽 `createTtlCacheShell` 泛型壳（参数化计算体/指纹/forget）收益已到阈值；completion-names 计算体已异步化而无 in-flight 单飞（同场景先例 = foreshadows PM-1 `getForeshadowsCachedAsync`，并发 MISS 双扫正确性无害仅效率小损）；forget 未挂 `books.ts forgetBookKeyedCaches`（正确性无洞：resolveBook 前置 404/换键 + 指纹含 mtime）。【已销账·2026-09-15 机械批核账：泛型壳与 in-flight 单飞两项均已随复审-0914-优化修复批 D1 `createTtlProbeCache`（src/studio/server/ttl-cache.ts）实际收敛——17 份同构壳（含 completion-names 异步 + in-flight、foreshadows 同步/异步孪生共壳）归一单源工厂，R47-18 过期逐出/两级探针/成功才落缓存等形态差异以选项参数化，收敛映射与沿革 = ttl-cache.ts 头注表；本行系台账滞留（登记时点早于 D1 落地），随批销账。forget 侧 win 合并批 2026-09-13 已注收编 forgetSettingsCache 同清两壳】
- **PUT /file 覆盖前快照 fail-open 吞非 UTF-8 拒绝 → 存量非 UTF-8 文件覆盖时原稿不可逆丢失（重评-0912-4 P1-1·主审对码实锤，本轮唯一 P1）**：`files.ts:171-176` 快照 try/catch 按 R26-9「IO 抖动 fail-open 继续保存」吞掉一切异常——但同一 catch 把 `snapshotBeforeOverwrite`（draft-pipeline.ts:66-74）对盘上旧文的 R66-1 非 UTF-8 确定性拒绝一并吞掉：存量 GBK/Big5 文件（中文网文作者导入旧稿现实形态）经 GET（:57-63，utf-8 解码返 U+FFFD 无警告）改存时，旧文唯一副本被覆盖、无快照、返 200，产品快照兜底承诺静默失效。修法 = catch 按错误类型分诊（R66-1 fail-closed 拒存 400 + 复用既有转码指引文案；仅瞬态 EACCES/EBUSY 维持 fail-open）+ GET 侧补编码探测告警。**处置（重评-0912-4 修复批 2026-09-12）**：draft-pipeline.ts R66-1 抛出位类型化为 `NonUtf8TargetError`；files.ts PUT 快照 catch 分诊 fail-closed——NonUtf8TargetError → 400 NOT_UTF8_TARGET（转码指引文案）、其余快照失败 → 409 WRITE_ERROR 拒写（主审对码裁决收紧：报告 §八 原建议瞬态 IO 维持 fail-open，实施取全 fail-closed 与 Y-3 saveDraft 同函数语义对齐，收口记如实记档）；GET 侧回 encodingSuspect/encodingHint + 前端 doOpen 改走 getContentPayload 完整载荷 toast 告警（32 个 mock api/documents 的测试文件补委托式默认）；回归 = r0912-4-file-put-utf8 5 用例（GBK 存量 GET 告警 / PUT 400 拒写·盘上字节逐位不变 / UTF-8 正常面）+ r0912-4-doc-open-encoding-toast 3 用例。【已处置·重评-0912-4】
- **draft-save 端点无任务闸/无 bookMovedFailure 重验 + mkdirSync 幽灵目录复活删书成果（重评-0912-4 P2-1·主审对码实锤）**：`draft.ts:50-68` 仅查 isSelfHealRunning；`draft-pipeline.ts:200-202` 落盘段 mkdirSync(recursive)+atomicWriteFile。触发链 A = 删书排水清单（books.ts:380-435 的 drain 族）不含 draft-save 链，graveyard rename 后迟到保存在旧书路径重建幽灵目录树、200 假成功；触发链 B = 改名后 stale 客户端续存无重验（B 域「config.ts/prefs.ts 同型越界命中」家族新成员，后果更重）。修法 = 入口补 bookMovedFailure 重验 + 删书排水清单收编 draft-save 链（drainDocumentSaves 同族）。**处置（重评-0912-4 修复批 2026-09-12）**：draft.ts 收编 per-book 串行链 draftSaveChains（filePutChains R69-25/R70-6 同款范式：链内临界段 bookMovedFailure 单源重验 + NonUtf8TargetError 分诊，迟到保存在书已删/改名后 409 拒写）+ books.ts 删书/改名排水段接 `drainDraftSaveChainsUnder`（判式收「链键恰等于书根」形态 + R71-10 realpath 双口径——首版仅词法前缀恒 no-op，回归测试当场抓出后修正，临时探针实锤幽灵目录 = 阻塞中的链任务在墓地 rename 后跑完 saveDraft 的 mkdirSync 重建）；回归 = r0912-4-draft-save-drain 3 用例（GBK 拒写 / save 锁占下删书等待链收尾·零幽灵目录 / 删后续存 404 + 书目录不复活）。【已处置·重评-0912-4】


- **中断语义族（重评-0911c P2·本轮最大新发现，B/D 两域独立确认 + 前端侧同源，四条一个批统一收口·已处置 R0912）**：① `/interrupt` 对 task-gate 族完全无效且假成功——interrupt 通道只覆盖 abortSelfHeal+abortChat+driver.interrupt（`stream.ts:666-682`），outline（`outline.ts:45`）/lead-updates（`lead-updates.ts:46`）/三审（`review.ts:346`）/analysis（`analysis.ts:348`）/onboard（`onboard.ts:42`）/relations-mine（`settings.ts:200`）/rewrite（`rewrite.ts:55`）/定稿摘要钩子（`summary.ts:293,627`）等 9+ 调用点 runTask 均未传 ctrl/register（`runner.ts:515` 可选），在途时 anyRunning（`stream.ts:674-679`）为 false → 返 200 什么都不停，分钟级任务白烧 API 费、唯一出口重启；② self-heal pass 后台账本草稿后台任务失联（`self-heal.ts:727-728` 传 state.ctrl.signal 但收尾后 running Map 已删 + `stream.ts:824` unregister → 跑满 10min 超时）；③ /spawn watchdog abortLikeUser 只 abort 不推 interrupted 事件（`stream.ts:181-183` vs self-heal :771-775 两路分叉）；④ watchdog 强释放注销 ctrl 后 /interrupt 永久失效（`stream.ts:781-791`「注销即失联」）。**处置（R0912）**：七端点统一 register-ctrl（owner `<端点>:<书名>`，analysis 按 action 分槽防同 owner 换新误伤）+ /interrupt 返回如实附 interrupted + watchdog 改走 driver.interrupt 全动作集 + 强释放 ctrl 留册至 settle（源锚 R0912- 注释为准）+ 定稿摘要钩子 documents.ts 惰性 ensureSession 接线 + self-heal 后台草稿改独立登记 ctrl（`bg-lead-draft:<书名>`）。【已处置·R0912】
- **卷复盘两端共同功能缺口（重评-0911c P2）**：state 5 routeState 返回 action='volume-review'（`state.ts:875-882`，M4 未实装自认），服务端无任何卷复盘端点；前端 WbStateCard.vue:23-25 把该动作映射 emit spawn → 点「卷复盘」实际走 writer 链生成下一章正文（B 域定性：两端共同缺口非误接线）。**处置（R0912 FE）**：按钮文案对齐实际行为（「继续写作（下一章）」+ title「卷复盘功能规划中，当前直接开写下一章」+ 卡内说明行），emit spawn 语义不变——误导面已消除。【已处置·R0912 文案对齐；卷复盘产品语义仍【待拍板】，端点单立随拍板】
- **spawn/auto-write/chat 互斥闸只查进程内闸（重评-0911c P2 疑似·限双进程形态）**：`stream.ts:593/720/882` heldTaskGatesFor 未并 crossProcessHeldTaskGatesFor——dev-api/CLI 与 GUI 双进程开同书时细纲覆盖写与写稿互踩（R67-13 要防形态）；`books.ts:243` busyGate 已并两侧，修法现成 = 换 `allHeldTaskGatesFor`（audit.ts:193）。**处置（R0912）**：stream.ts spawn/auto-write/chat 各闸点换 allHeldTaskGatesFor + 跨进程锁文件 4 用例（手写活 pid/陈锁）。【已处置·R0912】

- **rag/rebuild 前端断头（重评-0911b P2①）**：`api/rag.ts` rebuild 端点在位（R26-16），前端唯一接线 `triggerRagBuild`→`/rag/build`（web-next `api/books.ts:139`）；`rag/index.ts:387/593/717` 三处失配错误文案引导 GUI 不可达的 POST /rag/rebuild——embedding 模型/维度变更后 RAG 无法自愈（死路）；SettingsBookAnalysis.vue 失败提示文案亦与实际调用的 build 语义不符。修法：前端接 rebuild（或 build 失配失败后引导重建）+ RagStatus 类型补 indexModelMismatch/indexState。【待修·重评-0911b P2；重评-0911c 未独立复现（G 域判 RAG 域完整、未核失配文案接线面），维持重评-0911b 登记效力】→ **处置（R0912 FE）**：api/books.ts 补 triggerRagRebuild（POST /rag/rebuild——服务端 R26-16 端点与 indexState/indexModelMismatch 字段本就在位，零服务端改动）+ SettingsBookAnalysis 失配提示与「重建索引」钮（同套在途锁/轮询）+ RagStatus 类型补两字段。【已处置·R0912】
- **五处 TTL 缓存「出生即折旧」（重评-0912 B-P2-1）**：state/overview/check/health/knowledge 五端点缓存 set 时 `ts` 复用计算前捕获的 `now`（await 秒级全书计算之后写入）——大书计算 ≥TTL（5s 档）时写完即过期、缓存完全失效，多窗轮询退化为重复全书扫描；同族已有九处 `ts: Date.now()` 既修纪律，此五处为漏网。修复 = 五行 `ts: Date.now()`。【已处置·2026-09-12 修复批 R0912-B-P2-1：五处落位 + r0912-ttl-write-clock 五用例（临时还原旧写法即红=证伪验证）】
- 重评-0912 B 域 P3 两条随批：review-verdict 常态白付整章同步读盘（惰性化）/ bookMovedFailure 四处同构归一 book-context（~35-50 行）。【已随批·2026-09-12 修复批 R0912（B-P3-1/2；documents 响应契约经 bookMovedFailureOk 包装逐字节保持）】

- **rag/rebuild 前端断头（重评-0911b P2①）**：`api/rag.ts` rebuild 端点在位（R26-16），前端唯一接线 `triggerRagBuild`→`/rag/build`；`rag/index.ts` 三处失配错误文案引导 GUI 不可达的 POST /rag/rebuild——embedding 模型/维度变更后 RAG 无法自愈（死路）；SettingsBookAnalysis.vue 失败提示文案亦与实际调用的 build 语义不符。【已处置·双线各一批并合：win 线 R0911b-P2①（2026-09-11 重评修复批）+ mac 线 R0912-FE（2026-09-12）——client 补 triggerRagRebuild（POST /rag/rebuild，服务端端点与 indexState/indexModelMismatch 字段本就在位，零服务端改动）+ SettingsBookAnalysis 失配提示与「重建索引」入口（ragNeedsRebuild 三形态：失配标记/失败文案含重建/本组件重建失败；ragBuilding 锁 + 轮询复用，重建按钮动态文案）+ R28-22 误挂记忆修正（build 增量不清库不置重建提示）；如实记档：RAG 库文件级损坏时 status 500 前端拿不到失配标记不出重建按钮——普通建立索引经 D-P3-1 损坏自愈可达，无死路】

- **锁超时降级取舍族**：journal/analysis/words-diary/lead-update-draft 四处（R0910 P2-6，涉崩溃安全语义重设计）+ batch-pause 锁超时裸写（R1010c 自认）——代码已记档。【维持】
- config.ts PUT /config 与 prefs.ts PUT/DELETE 同型越界命中（照 BOOK_MOVED 同款收口）。**处置（复审-0913-源码修复批 2026-09-13）**：prefsPath ok 分支增返 bookRoot，books.prefs.put 在 readJson 窗口后、mkdirSync/写盘前插 bookMovedFailure 单源重验（config R0911-B-P3-4 同款，命中 409 BOOK_MOVED）；回归 = r0913-srv-prefs-bookmoved 3 用例（窗口删书→409 无幽灵目录 / 窗口改名→409 新旧路径无残留 / 正常书 PUT 不变）。库内现存 prefs 端点仅 GET/PUT（无 DELETE，台账行原记系历史口径）。【已处置·复审-0913-源码】
- finalize 重验需动第三文件。【单立】
- rename 微任务残窗：单元首行重验后微任务窗残留（files.ts R70-6 架构同源，彻底闭合 = 重验下沉 DocumentService.executeSave）。【单立】
- stream 互斥矩阵（备案权衡）。【维持】
- SSE `?token=` 兼容通道下线（移除条件 = useSse 回退路径下线）〔核对 2026-09-11：`src/studio/server/api/stream.ts:379` 注释登记在案〕。【维持·条件下线】
- audit 全量折叠无事件循环让出（R0909 P3-5，数十 ms 级，收益/风险不称）。【维持】
- defineRoute parse 声明迁移 45 处（`schema.ts:12` 自记 RC 后分批）。【2026-09-15 机械批处置：44 处存量内联 readJson 站点逐点评估——**迁移 12 / 跳过 8 / 维持 24**。迁移 12 = providers 族 9〔providers.post/current/tiers/tiers.chat/put/pricing/models + rag-providers.post/put——B 路子代理，测试面核查无旧优先级钉序，三处 body 校验先于 409/404 的翻转已在站点注释显式记录〕+ chat.confirm / library.prefs.put / onboard-save〔D 路子代理，同款核查〕；跳过 8 = 容错读语义不可表达 3〔providers.delete/test + rag-providers.delete——readJson 带 catch 兜底空 body 继续（P4 无 body 放行 / R26-63 连接层异常兜底），parse 化必改语义〕+ 钉序 5〔chat.regenerate（orchestrator-mutex-gates:165 钉「闸先于 body 校验」）/ stream.spawn（holdSpawnGate 覆盖 body 在途窗口 RB-SV-P2-1/CC-P2-9）/ stream.auto-write（r0912-cross-process-write-gates:119 钉首检即拦）/ prefs.put（r0913-srv-prefs-bookmoved 钉悬持 body 判序）/ onboard-ai（runGatedGeneration 闸内回调读体）〕；维持 24 = 其余书域写端点（documents/style/rewrite/draft 等）按 CC-P2-9「先占书级闸再读体」顺序纪律（R51-G-2 body 悬持计时与闸占持耦合）+ resolveBook 404 前置——迁移需逐端点「错误优先级矩阵」先行，无矩阵前维持。新路由 parse-first 纪律不变（schema.ts M-5）；chat.send 系 3/107 基线先例零改动】
- 记账同步 IO。【维持】
- 导出排队持闸。【维持】
- migrateBookDefaults 启动全量读盘（SMB 场景）〔核对 2026-09-11：`src/install/migrate-defaults.ts` 挂 server/index.ts 在用〕。【维持】
- 回收站 restore 未接伏笔差分链（伏笔快照缺位→差分静默跳过，防御性安全、计数不更新）。【维持】
- rename 跨域判定用链前 docPath（伏笔快照读全局不受影响，主审复核无正确性损失）。【维持】
- 导出 io.ts skippedDrafts 未透传前端（服务端已剥草稿，前端仅缺计数可见性）。【单立·小】
- 误报语料查询侧未接线（B-P3-2：只写不读、存储积压——`check.ts` 注释自认有意留白，与语料 harvest/commit 通道分阶段建设）。【有意·维持】
- git/ai-track `recordAiVersion` 同步孪生生产零调用（异步孪生已重写非委托；重评-0911b B-P3-3）+ `isNfcName` 仅测试消费（B-P3-4）——2026-09-12 精简批处置：`isNfcName` 已删（toNfcName 生产在用保留）；`recordAiVersion` 同步版**销账不删**（生产 0 但 9 测试文件 45+ 处 fixture 种子消费，同步 API 是测试种子的自然形态；证据 = 精简报告 §四）。【isNfcName 已办结 / recordAiVersion 销账】
- **rag rebuild 端点缺编排互斥查询（复审-0913-合并批 P3-5·登记备查）**：`studio/server/api/rag.ts:77` rebuild（resetIndexFirst=true 时 resetRagIndex 清库）只持自身 'rag-build' 闸、不查 orchestrationBusyFor——与 prune 端点同形（清理 vs 在途编排写索引行；R0912-ds41 P3-9 已补 prune 向）。后果面较 prune 轻：索引陈旧可下次增量重建自愈、better-sqlite3 同步写无损坏，非收尾数据丢失面，属可辩护不设闸。处置建议 = 下一批触达 rag.ts 时对齐 prune 形态补前置查询（~3 行，查法照抄 snapshots.ts prune 端点）。【已处置·2026-09-15 机械批：rag.ts rebuild 端点照 snapshots.ts prune 精确形态补 orchestrationBusyFor 前置查询（先查编排闸再占自身 'rag-build' 闸，409 code/error 与同族端点逐字节一致）；回归 = r0915-rag-rebuild-gate 2 用例（spawn 在途 409 BUSY 文案逐字节 + 不残留死闸 / 无在途放行进 startRagBuild 前置校验 400 原文案）·复审-0913-合并批】
- **rewrite 端点缺 chat 在途互斥面（重评-0912-2 P2-1·主审对码实锤）**：`rewrite.ts:87-98` 只查 isSelfHealRunning（R66-2 面）+ isSpawnRunning（R70-3 面）+ 'rewrite' 任务闸，缺同族五端点（analyze/outline/onboard-ai/relations.mine/lead-updates）均有的 orchestrationBusyFor（含 isChatRunning + hasBackgroundTasks）——反方向已封（chat.send/auto-write/chat.clear 查 allHeldTaskGatesFor 时 'rewrite' 闸在持即 409），唯正向漏；纯文本对话在途时编辑器整章改写可并发起跑 = 分钟级双份 LLM 费用 + 过期基线提案；测试矩阵（orchestrator-mutex-gates）无 rewrite×chat 角。修法 = 入口补 orchestrationBusyFor 三行（lead-updates 同款）+ 矩阵补角。【已处置·R0912-2 修复批：rewrite.ts 补 orchestrationBusyFor（409 透传家族文案，lead-updates 同款）+ orchestrator-mutex-gates 矩阵补 rewrite×chat 角（r0912-2 锚）；既有 self-heal/spawn 两面文案语义不变】
- B 域 P3 登记（重评-0912-2 §3.4）：onboard-save 占闸晚于 readJson（全库「先闸」纪律唯一漂移）/ API 面 404 vs 静态面 405 语义不一致 / settings GET MISS 全同步扫描（唯一无 async 孪生书键端点）/ tree-issues `warnings[]` 前端零消费 / check-false-positive 语料回路只写不读（与既有 B-P3-2 行同族，本批评「半闭环」）/ defineRoute parse 采用率 3/110 / draft-save 互斥面只查 self-heal（自记维持）/ review-verdict 背靠背双 readAnalysis。【登记·待择收】
- B 域登记（重评-0912-3 §3.2）：books.ts:482-526 删书 handler 临界段整段少缩进一层（清理密集区维护误判面，零行为影响）/ analysis.ts:406 GET 存量分析判 stale 同步整章读（settings GET 全同步扫描同族边角残留，KB 级亚毫秒）。本域 110 端点逐一核对零 P1/P2——服务端连续两轮全量重评零二级发现。【已处置·R0912-3：缩进整段 +2 空格（git diff -w 空验证零字符变化）/ GET 存量分析 stale 判定改 readMdTextCachedAsync（R37-5 既有原语，降级语义逐项等价 + 缓存命中行为不变用例）】
- **补全名单端点无缓存 + 整文件读（重评-deepseek-v4.1-flash P2-2·主审对码实锤）**：`settings.ts:146-157` 的 `books.completion-names` handler **同步**且无任何缓存键，每次两遍 `readFmNames`；`readFmNames`（`:42-55`）调 `readFile(join(dir,f))` 不带 content 参数 → `format/frontmatter.ts:382-391` 走 `readFileSync` **整读含角色卡正文**而只用 `fmRaw`。同文件 `getSettingsCached`（`:113-129`）已有「目录指纹 + TTL 5s + FIFO 32」缓存壳（同族唯一漏网）。前端触发面：`CmHost.vue:398-409` 切书即拉 + `:411-429` 5 分钟 TTL 过期后 @ 击键后台补拉。影响 = 数百角色卡书库每次同步阻塞事件循环 ≈80-200ms（SSE 心跳/写稿事件推送停摆），用户可感知非数据错误。修法 = 复用同文件缓存壳（≈25 行）+ `readFmNames` 改 async，或只读到 fm 段结束（≈15 行）。【已处置·双树各一批：win 树 R0912-ds41（2026-09-13）——`readFmNames` 异步化 + `format/frontmatter.ts` 新增 `readFileFmOnly`（头部 8KB 限量提 fm，围栏不完整回退全读，解析与错误文案单源不变）+ completion-names「目录指纹（角色/物品两目录 mtime）+ TTL 5s + FIFO 32」缓存壳（TTL/MISS 注入钩子对齐 R46-16 先例，handler 改 async），r0912-ds41-completion-names-cache 4 用例全绿；mac 树 重评-0912-4 修复批并批（2026-09-12）——completionNamesCache 落地（R46-16 同款壳 + forgetSettingsCache 钩子同清两壳），r0912-4-completion-names-cache 3 用例；**win 合并批（2026-09-13）收口合成**：取 win 异步壳（IO 面更优）+ 收编 mac forget 同清 + TTL 生效值链（本壳注入口 → settings 注入口 → 常量），mac 测试直调改 await，两套回归（4+3 用例）并存全绿】
- B 域 P3 登记（重评-deepseek-v4.1-flash）：5 个零引用 `ForTest` 钩子 + 2 组只写不读扫描计数器（`overview.ts:71-78`/`:119`、`settings.ts:76-79`/`:84-92`/`:120`——该族本有测试消费范式，属收尾遗漏；settings/overview 的 TTL 语义缺注入式回归门）/ `files.ts:157` 全库最后一处内联 `BOOK_MOVED`（文案与单源漂移）/ 89 处「仅本文件内使用」的冗余导出（服务端 24 处）去 `export`。【已处置·R0912-ds41：钩子族补门收编非裸删——r0912-ds41-ttl-gates 6 用例（两壳 TTL 命中/过期/指纹失效三态）后 5 钩子全数有消费方，零删除；`files.ts` 重验判定+信封文案一并收敛 book-context 单源（主审核定单源「reason 各端点一致」为设计不变量，旧文案全域唯一零测试钉值）；冗余导出按当前实态全 src 树扫描摘除 218 处/122 文件（评审 89 为 studio 两域口径，本次新增核心域 141 处确认）——win 合并批注：mac 树同项曾维持登记，合并取 win 处置态】

- **书名 win 非法字符全集跨平台硬拒（复审-0913-mac适配 P3-6）**：`install/books.ts` isInvalidBookName 对 `[\\/:*?"<>|]` + win 保留名族全平台统一拒绝——mac 上仅 `/` 非法，含 `:`/`?` 的合法 mac 名被误拒。系**有意设计**（win 书库可被 mac 打开的数据面对称约束，源码注释明示），分层放宽会破坏跨平台互拷兼容。【维持·文案已披露——2026-09-13 mac 适配修复批：拒绝文案收编 `BOOK_NAME_INVALID_REASON` 单源（doInit + server 建书/改名三消费点），补跨平台原因披露「书名需兼容 Windows/macOS 双平台书库互拷，故统一限制」】

### C. 前端（web-next）——出处：独立复审 §十 / 内存专项重审 §十 / 重评-0910 §9.4·§十 / 重评-0911b §3.3·§四 / 重评-0911c §4.2 / 重评-0912 §四 C 节 / 重评-0912-2 §3.4 / 重评-0912-3 §3.1·§3.2 / 重评-0912-4 §四 / R0912-ds41 收口批复核 §三·§四 / 阶段 24 批 B e2e 实抓 / 全库重评-0914 §三

- **webnext ui-store mock 余量 ~36 文件渐进迁移（重评-0916-五轮 P2-5 余量）**：处置批已落真 store 测试 helper（test/studio/webnext/helpers/real-stores.ts：setupRealStores / recordToasts / autoConfirm，头注纪律「新测试不 mock 兄弟 store：真 store + mock api 层」）+ 7 存量文件迁移（用例语义零变化）+ webnext-store-import-cycles 治理门（静态初等环枚举 + 白名单防僵尸，四组环全登记——评审报三组，全量枚举补得第四组 doc→words→tree→doc，同属函数内延迟取实例安全模式）；余量 ~36 文件（深依赖 mock 语义/观察口、多 store 联动整族 mock 需成组改写、环境限制 prefs 等）随触达渐进迁移，不单独立批。【登记·渐进收敛·2026-09-16 五轮处置批】

- **inline 新建章清掉 seed 章号前缀 → 文件名无章号 → fm 章号盲区连号重号（阶段 24 批 B e2e 实抓·前端既有行为非本批引入）**：树面板 inline 新建输入 seed 预填「NNNN-未命名」，作者若清掉前缀只填标题 → 落盘文件名无章号前缀（裸标题 .md）→ `nextChapterNo` 前端取号扫描看不见该文件 → 连建多章 fm 章号重号（服务端 warn「章号 N 重复入库」仍落盘），且重号章被结构合并干跑按「跨卷重号章」400 拒收互不可合并；读侧 `parseChapterFileName` 对无前缀文件同样失明（树可显但游离于章号体系）。属行为取舍非缺陷：改法候选 = 新建端点服务端归一强制补前缀 / inline 提交时前端拼回 seed 前缀 / 接受现状（章可见、仅结构操作面受限）。【已拍板处置·2026-09-15 拍板快断批：作者指令「按建议顺序开工」取主审建议「前端拼回」档——useChapterTreeActions 提交侧对无章号形态拼回 seedPrefix（作者自填「NNNN-…」/「第N章…」形态不覆盖不双拼），回归 = r0915-inline-seed-prefix 3 用例（拼回 / 自填不覆盖 / 章纲同款）】
- **树显示序跨卷语义 = 卷优先分组 DFS，非 fm 序跨卷全局排（阶段 24 批 C e2e 实抓·设计口径细化取舍）**：sortTreeByOrder 只重排**同目录内**章文件（fm 序 ?? 章号），卷目录位置由 compareNode 目录优先钉死 → 卷内章恒渲染在散章之前、跨卷显示序前章 = 同卷 DFS 前驱而非全局 fm 序前驱（e2e 实抓：乙章号 6 移入卷后排在散章甲章号 5 上方，与「显示序 = fm 序全局排」预设相反；「并入上一章」跨卷命中需两卷构型）。现行为自洽（同卷内 fm 序成立、D1 显示序语义在同层内闭合），取舍点 = 是否改为卷间也按 fm 序穿插全局排（牵 sortTreeByOrder compareNode + 树交互语义，非缺陷修复而是语义拍板）。【已拍板维持·2026-09-15 拍板快断批：作者指令「按建议顺序开工」取主审建议「维持」档——卷优先分组 DFS 语义自洽（同卷内 fm 序成立、D1 语义同层闭合），卷间穿插全局排不改；sortTreeByOrder JSDoc 补口径注记钉边界】

- **SearchPanel open() 失败 err 顶替整个结果列表（重评-0912-4 P2-4·主审对码实锤）**：`SearchPanel.vue:92-96` open catch 置 `err.value` + `:116` 模板 `v-else-if="err"` 整列表替换——搜索结果已展示时点开单条失败（文件被外部移动/锁定），其余 N-1 条结果全部不可见（错误作用域混淆：单条打开失败吞掉整个搜索会话成果）；重搜索可恢复故低危。修法 = err 作用域收窄到 open 动作（行内提示/toast），列表维持渲染。随批 P3：切书 watch（:61-72）清 results/err/loading 不清查询词 q——切书后残留旧书查询词，回车即对新书重搜旧词（与 M-7 切书清面板意图不合）。**处置（重评-0912-4 修复批 2026-09-12）**：openErr 独立 ref + 模板行内 err 提示行（`v-else` 结果列表维持渲染，单条打开失败不再顶替整个搜索会话）；随批 P3 同修——切书 watch 补 q 清空 + openErr 复位；回归 = search-panel.test.ts +4 用例（open 失败列表保留 / err 恢复可再点 / 切书清 q / 切书清 err）。【已处置·重评-0912-4（P2-4 + 随批 P3）】

- **utilities.css 全局 `.is-drag` 同名异语义泄漏（R0912-ds41 收口批复核 P1·主审亲核）**：本批 CSS 收敛（P3-10）把 `.is-drag` 收成全局单类时只定位了 4 个壳层拖拽容器（SidebarLeft/SidebarRight/TabBar/ViewHeader，收敛等价无损），漏了三页根的同名**状态标记类**用法（`Library.vue:80`/`Shelf.vue:85`/`Welcome.vue:63` 既有 `:class="{…, 'is-drag': hasDesktop}"`，HEAD 全局零 `.is-drag` 规则、drag 原只落页内标题栏后代选择器）→ 新全局规则首次命中页根自身，app-region 继承语义使桌面态三页整页成拖拽面、书卡/按钮点击被窗口拖拽吞掉；e2e 跑纯 Chromium（该属性惰性）+ 单测不渲染真桌面窗，全部门不捕获。修法 = 方案 a 三页根改 `is-desktop`（根治同名异语义，三页 scoped 后代选择器随改）/ 方案 b utilities.css 退收 `.is-drag`（四壳层复原 scoped 12 行）。【已处置·R0913 修复批：方案 a——三页根标记类 `is-drag` 改名 `is-desktop`（每页模板根绑定 + scoped 后代拖拽选择器两处，注释随批记因）；utilities.css 头注记 `.is-drag` 专属语义；防复活锚 `test/studio/webnext/r0913-is-drag-scope.test.ts` 3 用例（静态源码守卫，app-region 无运行时门可捕获）】
- **main.ts 装载注释失实 + `.val` 死规则（R0912-ds41 收口批复核 P3-3）**：`main.ts:13` 注释「.val/.save-btn/.seg 药丸等被设置域外组件消费」——`.seg` 经 P3-11 前缀收窄后已全在设置域内，注释未随批更新易误导后续收敛；`settings-shared.css:417` `.val` 全库零模板消费点（前批遗留死规则，grep 实证）。【登记·待择收】
- **前端组件模板/CSS 重复面（重评-0912 C2 域，精简最大单块 ~490-570 行）**：TierSection 三卡同构 / style 四件 CSS 逐字重复（.panel/.btn-*/.kind-badge/.token-chip）/ ChatPanel×ChatDock composer 模板+150 行 CSS 双份（逻辑层已共享）/ ShelfHeroCard×ShelfModalHero hero-list / Review·CheckPanel 红/黄分组——抽 TierCard/style-shared.css/ChatComposer 优先（重复面积最大且各只剩单份事实源）。【已处置·2026-09-12 修复批波 2 R0912（C2-P3-2～6 五件抽取产品净 −284 行：TierCard/style-shared.css〔全局非 scoped，.panel 全库八处声明核对零视觉差〕/ChatComposer〔差异 5 处归一 glass prop〕/ShelfHeroList〔variant 双档〕/分组 v-for）】
- C1 域杂项随批：check/review store lastDocId 死字段 ×2、getConfig 双函数委托、api 层 53 处 Content-Type+stringify 样板 apiJson `json` 选项化（~50 行）。【随批】

- **openTab 切档假警报（重评-0911b P2②；重评-0911c 独立复现）**：`stores/workspace.ts:297` 切文档遇旧文档保存在途，`doc.save(id,'autosave')` 对 saving 态直接返 false（`stores/doc.ts:387`）→ 弹「切换文档时自动保存失败」误导 toast——在途保存正常进行、内容不丢；且 conflict 未决路径同样触发；系 2026-09-09 可见化清偿批引入的回归。doDelete 已用 `waitInflightSave`（doc.ts:701）解决同型问题，openTab 未同步。【已处置·双线各一批并合：win 线 R0911b-P2②（2026-09-11 重评修复批）+ mac 线 R0912-FE（2026-09-12）——openTab 改 doDelete（R55-F-6）同族：waitInflightSave 落定 → 复查 dirty/saving → 补存一次 → 仅 R49-25 判式（返 false 且仍 dirty 且无新在途）notify；conflict 未决不补存不提示（编辑器自有冲突 UI）；bookName 切书早退守卫 + fire-and-forget 与 bookAtEntry 守卫保持；两线各 4 回归用例并合去重】

- ContextMenu G6⑦ 浏览器回退版飞出层键盘不可达（Enter 可开层但 Tab 关菜单、ArrowRight 未接线；桌面端原生 Menu 全键盘可达不受影响，回退版仅 dev/web 触达；接线需跨层焦点机，触发条件 = 浏览器版转正，代码注释档已声明）。【维持·条件触发】
- flushDirty for(;;) 理论无界。【维持】
- 全库无请求取消接线。【单立】【已过时·中件组批（2026-09-15）对码记正：取消链已全环在役——runner.ts 编排级 AbortController/register/forwardAbort + ABORTED 错误映射与可中断退避 sleep；适配器 SDK `create(params,{signal})` 断在途 HTTP；gen.ts per-attempt abort（RB-AI-P2-3 onStall 先断请求）；`POST /api/books/:name/interrupt` → driver.interrupt → abort 全部在册 ctrl + interrupted 事件；rewrite/review/analysis 三端点 R0912-P2-①（2026-09-12）全部接入 registerCtrl（owner 带书名防跨书误伤）——本行系 0911 时点登记，行随记正销，残余面（非流式端点用户级取消 UI）无登记诉求】
- discard 窄窗自愈。【维持】
- 全屏书架无上限。【有意】
- onSaveMeta/doMove 同族 fp 键孤儿（改名链只修 onRenameCommit，同款清理待评估）。【单立·待评估】
- refresh 脏分支收编基线 + CmHost O(n) 物化（前端核心 P3-②③）。【维持】
- prefs lastPersisted（重评-0910 G5③ 证伪维持——「移到 PUT 后」经推演破坏 R61-F-3 零脏字段基线，R32-26 服务器对账路径在位）。【维持】
- 书级 prefs beforeunload 刷新路径不冲刷（C1-P3-3 修复批残留：关窗主路径已接 `__clwFlushBeforeClose` 首位冲刷；刷新走 beforeunload 仅文档面——500ms 防抖窗内末次布局态仍可丢，刷新频率低取舍维持，FE1 代理报备）。【维持】
- useChatTier 模块级单例绑定首次创建的 pinia 实例（重评-0911b C1-P3-3；生产单 pinia 无碍）。【维持】
- ChatMessages variantGroups computed O(messages×branches) 嵌套 find（重评-0911b C2-P3-3；百级会话无感，超长会话理论放大未证实卡顿）。【维持】
- settings-shared.css（772 行）经 main.ts 全局注入（重评-0911b C2-P3-4；他域消费缘由注释已声明，可接受权衡）。【维持】
- C 域 P3 登记（重评-0912-2 §3.4）：`api/stream.ts:22-26` crashedPendingOpIds 注释过期（声称服务端未透出，实际 state.ts 已透出且 WbStateCard/WorkbenchView 已消费——错误现状记录）/ chat 历史 200 条截断 `truncated/total` 声明零消费（api/chat.ts:52-65，静默截断无提示）/ BOOK_MOVED(409) 无前端专属处理（doc.ts:447-456，多窗改名错误文案失真）/ ChapterMetaDialog 缺 `role="dialog"`+aria-modal（全库 8 模态唯一漏）+ 中文 prop 名「标题」全库 96 件孤例 / `.panel` 样式块总览族四处逐字重复（OverviewView/RhythmDistPanel/WordCurveChart/ShortProfileGaps——R0912 收敛批未覆盖）【已登记——其中四件随 R0912-2 修复批收口：crashedPendingOpIds 过期注释修账 / chat truncated 提示接线（store 透出 + ChatMessages 条件行，+6 用例）/ ChapterMetaDialog 补 role="dialog"+aria-modal+aria-label 与 prop「标题」→title（+2 用例）/ `.panel` 四处删除走 style-shared 全局；BOOK_MOVED 等余项维持】
- **LearnView 零候选空态死分支（重评-0912-3 P2-3·主审对码实锤）**：`views/LearnView.vue:96` 外层 `v-if="learn.hasResult"` 与 `:122-126` 内层 EmptyState `v-if="!learn.samples.length && !learn.quotes.length"` 逻辑互斥（根因 `stores/learn.ts:24` hasResult = samples>0||quotes>0）——该分支任何状态不可渲染；收割返回零候选时界面回落 :88 收割前引导文案，作者无法区分「未收割/收割了但全不合格」，上一轮候选若存在被清空视觉消失。`learn-store.test.ts:55` 只锁 store 层契约，视图死分支恰好无测试可达。修法 = 视图层「本次收割已跑」判据（harvest 成功置位）+ 空态三态化。【已处置·R0912-3 修复批：store 增 lastHarvestRan 判据（harvest 成功置位〔零候选也算跑过〕/clear 复位/失败不置位）+ LearnView 空态三态化（逻辑互斥死分支删除）；learn-store 用例含零候选置位/失败不置位/clear 复位】
- C 域登记（重评-0912-3 §3.2）：api/prefs.ts GlobalPrefs 缺 `uiFontSizeStep` 键声明（store 真实读写，靠索引签名过编译，契约穷举性缺口）/ TabBar·StatusBar 声明后从未使用的 `bookName` 必填 prop / 对话历史「200」三处字面量无单源（api/chat.ts:65·stores/chat.ts:50·ChatMessages.vue:205，注释自证曾失同步一次——与已收口 truncated 接线同域不同点）/ ChatDock 收起 FAB 即卸载 Composer 丢未发草稿（同书内口径不一致）/ learn harvest·commit 缺函数级在途锁（R35-34 家族漏网，同帧双击双发）/ WbStateCard 建议按钮与 Enter 绕 AI 可用性闸（主按钮有闸旁路无）/ AuditView 失败态 tab 区空态文案与错误横幅同屏矛盾 / WbUsageCard 取数失败渲染成「未配置价格表」误导归因 / RelationGraph·RelationDetail v-for key `-` 裸拼接理论撞 key / Welcome·Library loadError 双职顶掉已加载最近列表 / RhythmDistPanel 短篇「n/0」规划占位 / SampleCandidateList expandedGroups 跨收割不重置 / WorkbenchView 生成 toast 章号 toast 时刻重读 / ContextQuickPanel 可空 docId 作 key / CheckPanel `.check-item` 两段分离 / `.src-dot` 两处逐字重复（style-shared 收敛漏网）/ TierCard 表达式语句代 if。【已处置·R0912-3：17 件全收——prefs 补 uiFontSizeStep 键声明 / TabBar·StatusBar 删未用必填 prop（WorkspaceShell 两处残留绑定主审收尾）/ 「200」抽 shared/chat-history.ts 单源 CHAT_HISTORY_LIMIT（刻意不入 api/chat.ts——该模块被 21 测试文件整 mock）/ ChatDock 改 v-show 保 composer 实例（草稿不再丢；R48-97 豁免理由注：dock 唯一自持 composer 无双实例双监听）/ learn harvest·commit 函数级在途锁 / WbStateCard 建议按钮+WorkbenchView Enter 补 AI 可用性闸 / AuditView 空态守卫（范围收窄防 R57-F-1「续页失败数据仍在」抵触）/ WbUsageCard 取数失败三态 / RelationGraph·Detail key 工厂 \u0000 分隔 / Welcome·Library 交互失败 toast 分职（loadError 归还 load 单职）/ RhythmDistPanel 短篇守卫 / SampleCandidateList expandedGroups loading 落跳重置 / WorkbenchView toast 章号请求时刻捕获 / ContextQuickPanel key 兜底 path / CheckPanel .check-item 规则合并 / .src-dot 收敛 style-shared（先例 .panel）/ TierCard 表达式语句改 if】
- **全自动写章收工跳转在「工作台未挂载」窗口整链失效（全库重评-0914 P2-3·主审亲验，R27-77 同型漏配）**：`WorkbenchView.vue:149-166` healResult watch 无 immediate + `Book.vue:366-381` 视图切换无 KeepAlive 即整实例卸载；`wb.warning` 同型已上移 WorkspaceShell 常驻层（R27-77），healResult 漏配——tool_use 模式下收工跳转是作者看到成品的唯一通道，此窗跳过（草稿已落盘不升 P1）。修法 = 比照 R27-77 上移消费面。**处置（全库重评-0914 修复批 2026-09-14）**：healResult watch 整段上移 WorkspaceShell 常驻层（guard/书名捕获/tree.load/复检/refreshCachedDoc/openTab/toast 全链，比照 R27-77 wb.warning 先例）；缓存刷新语义抽 `shared/doc-freshness.ts` 单源（WorkbenchView 局部副本删除改指，防双份漂移）；回归 = 既有 webnext store 级 healResult 面（b2a-workbench-healresult-residue / workbench-selfheal）+ L2 全量兜底。【已处置·全库重评-0914 修复批】
- **文风铁律编辑收起再展开静默覆盖未保存修改（全库重评-0914 P2-4·主审亲验）**：`StyleBaselineCard.vue:90-96` 收起分支不查 `rulesDirty`、:100-105 重展开无条件重取覆盖本地；库内同型场景均已配脏守卫（OnboardView R8a-P2-2「手改未保存不静默丢稿」口径），此处漏配。修法 = 收起时脏先 danger 确认 / 重展开本地脏且 baseRev 未变则保留。**处置（全库重评-0914 修复批 2026-09-14）**：取脏先确认档——收起分支 `if (rulesDirty.value)` 走 `ui.ask` danger 确认（「收起并丢弃」，取消则不收起），对齐 OnboardView R8a-P2-2 口径；重展开保留档未取（收起后已无脏态残留，语义闭合）。回归 = L1/L2 全量兜底。【已处置·全库重评-0914 修复批】

### D. AI 链路与编排——出处：独立复审 §十 / 重评-0910 §9.4·§十 / 重评-0909 §5 / 重评-0911b §3.3·§四 / 重评-0911c §4.2 / 重评-0912 §四 D 节 / 重评-0912-2 §3.2·§3.4 / 重评-0912-3 §3.2 / R0912-ds41 收口批复核 §四

- **LlmRetryData 零代码引用（R0912-ds41 收口批复核 P3-4）**：`events/types.ts:158` 全库仅 `turns.ts:60/:646` 两处注释提及（落库写字面量不 import）——按 R0912-ds41 批死码标准属下轮候选，现为载荷形态文档锚（本批保留不算错）。【登记·待择收】

- 重评-0912 D 域 P3 登记随批：码点计量 `Array.from().length` 全量物化（chat 轮循环热路径，改零分配计数循环 ±0 行）/ 三适配器 tool_choice 意图翻译三份同构（归一 ~25-35 行）/ leads.ts 手抄章号校验接入单源 chapterInput / gen.ts 每 chunk 定时器可选 refresh 化。内存轴零新发现。【已随批·2026-09-12 修复批 R0912（D-P3-1～5；D-P3-4 单 deferred+refresh 悬挂期解武装；tool_choice 单源 = 新模块 ai/provider/tool-choice.ts，wire 逐字节不变）；D-P3-6 登记性随拍板维持】

- **重写循环 draftPath 与最新稿脱节（重评-0911c P2·窄触发）**：`loop.draftPath` 仅首稿设定、重写落盘不回写（`self-heal.ts:675-687`/:796 恒打首稿）；tool_use 未命中降级自由文本（:974-978）+ AI 自带异章号 fm 时 `resolveDraftPath` 按章号失配（`format/draft.ts:79-106`）→ 每次重写在 写作/正文 新建 000N-* 孤儿文件、机检恒打首稿旧文件 → 红项恒同烧满 maxAttempts 后 escalate，残留树红点失明的坏 fm 章。修法：rewriteOnce 落盘后以返回 relPath 刷新 loop.draftPath（或 saveDraft 内容章号≠入参章号时拒绝/告警）。**处置（R0912 D/AI）**：rewriteOnce 落盘后以 save 返回 relPath 回写 loop.draftPath + readDraft 纯内存比对异章号 warn 防线（不阻断）。【已处置·R0912】

- **ai→studio 反向依赖（重评-0911b P2③；重评-0911c 独立复核属实）**：`orchestrate/chat/turns.ts:46` `import { acquireTaskGate } from '../../../studio/server/api/task-gate.js'`——ai 底层引 studio 消费方，与域自治方向相悖；注释自认分层债并写明迁移触发条件（task-gate 现依赖 node:fs/cross-process-lock/ai-log，搬层非平凡）。形态债非正确性缺陷。**处置（R0912 主审集成）**：ai 层新建 orchestrate/task-gate-port.ts 端口（未注册 no-op 放行），turns.ts 换端口取闸，stream.ts registerStreamRoutes 注册真实闸；5 用例锚定（注册前放行/透传/重注册覆盖 + registerStreamRoutes 源锚 + ai 层零 studio import 分层源锚）。【已处置·R0912 依赖倒置】

- **switch-provider 失败决策无消费者**（换网关策略消费需作者指令；shrink-prompt 已由清偿批最小接线 chat 侧——ctxOverflow 侧信道 + ⌊historyBudget/2⌋ 预算重试 + a7-shrink-retry 3 用例，非 chat 不收缩维持 A7 范围）〔核对 2026-09-11：`src/ai/provider/failure.ts:8,93` 注释自认无消费者〕。【待拍板】
- chat 历史 LRU（上限 8）逐出在途书（>8 本并发对话才触发，注释留账不修）。【维持】
- stopReason 三线。【维持】
- TOKEN_COEFFICIENTS（估算口径）。【维持】
- MAX_AGENT_TURNS=5 保守。【待拍板·产品】
- rulesPromptParts 伪路径标签（hash 兜底）。【维持】
- 加密推理多条仅末条（A-5 既有留痕）。【维持】
- X-4 残余。【维持】
- AI 域 P3-1·4·5（重评-0909 报告细目）。【维持】
- AI⑤⑥ 备案权衡（重评-0910 沿用行）。【维持】
- `recordAiCall` 生产零调用仅测试记账入口（重评-0911b D-P3-1，calls.ts:532 一带 ~45 行）——2026-09-12 精简批复核：9 测试文件 40+ 处消费（跨进程锁语义起真子进程 + 预算闸测试全挂此入口），删需改写 40+ 处不成比例——**维持不删**，随测试面迁移批（如立项）一并退役；证据 = 精简报告 §四。【维持·复核回填】
- writeChains 登记排队分支实际不可达（重评-0911b D-P3-2；R33-17 声明保留作异步化接管面）。【**前提修账·重评-0912-3 主审对码**：serializedWrite（calls.ts:283-298）在 R30-3 锁等待异步化后，锁被占时 writeWithCrossProcessLock 返回在途 Promise 且紧随 `writeChains.set(bookRoot, r)` 置位排队——R33-17 段注释「writeChains 从不置位、排队分支不可达」失实，同文件 :288-290 R30-3 注与本行原表述相矛盾；排队分支在役，修法 = R33-17 注释段改写（本行原「不可达」定性随之作废）】
- revision 事件无章上下文记 `chapter ?? 0` 口径含混（重评-0911b D-P3-5，微小）。【维持】【**销账·重评-0915-四轮 P3-10（2026-09-15 四轮处置批）**：revisionRefEvent 构造器章号转可选 + turns.ts 调用点缺省不传（未选章不再伪装成无效章号 0）；唯一 src 消费方 lineage registeredRecords 只读 revision 不读章号，契约零影响——chain-bridge 构造器用例 + chat-prompt-trace 未选章断言钉边界】
- 「模型可见⟺已记录」校验仅 CLW_VERIFY_VISIBLE=1 opt-in 抽查非生产强制（重评-0911b D-P3-6；设计决策）。【维持·设计】
- **responses 伪流回填门与计费口径共用 outText（重评-0912-2 P2-2·主审对码实锤）**：`responses-adapter.ts:319`（R74-1 reasoning delta 入 outText 计费）使伪流回填门 `outText.length===0`（:398）误判「已有产出」——网关形态「reasoning delta 有流出、text delta 全缺、completed.output 带全文」时正文永不回填 yield 且 hasOutput（:419）判成功收尾，主产出文本静默丢失不报错不重试（触发面窄但违背 R35-18 修复本意）。修法 = 回填门/判空改独立 textYielded 布尔（outText 保持计费口径）+ 混合形态适配器测试钉。【已处置·R0912-2 修复批：textYielded 分家（回填门 !textYielded + hasOutput 正文面判据同步改，outText 计费不动；reasoning-only 流回归空产出报错）+ 混合形态恰回填一次/reasoning-only 报错 2 用例，R35-18/R26-4 既有用例零回归】
- D 域 P3 登记（重评-0912-2 §3.4）：CHAT_TOOL_NAMES 双份（有意登记）/ pricing `'providers.json'` 字面量镜像 store FILE 常量 / trace-stats durations O(calls) 物化 / catalog.gen 零运行时消费（A7 资产在案；已随 2026-09-15 拍板快断批随 catalog 三件套删除销案——Z-P2-4 销账）/ chat histories LRU 在途驱逐（Low-5 在案）。【登记】
- D 域登记（重评-0912-3 §3.2）：calls.ts:255-258 R33-17 注释漂移（见上行修账——排队分支 R30-3 起在役，注释声称「不可达保留」与同文件 :288-290·:350-351 矛盾）/ responses-adapter.ts:454-507 post-terminal-error 守卫只覆盖 completed，incomplete(max_tokens) emitDone 后流尾 failed/error 仍可翻转回合（重审-批2-1 同型另一半，建议守卫放宽 `terminal!=='none'` + 回归用例）/ openai-adapter 缺 index 兜底聚合 startsCall 判据在「每分片重复携带 id」网关形态下拆散单一 tool 调用（低置信，未实测此形态网关）/ openai-adapter:129 仅 reasoning 块 assistant 消息回写 `content:null` 严格网关可能 400（低置信）/ runner extractUsage 只查键存在不验值类型（三适配器有兜底，近零触发面）。零 P1/P2；provider 三协议 wire/计费口径交叉核验一致。【已处置·R0912-3：R33-17 注释段改写（排队分支在役口径，:350-351 同漂顺手对齐，零行为）/ post-terminal-error 守卫放宽 terminal!=='none'（含 incomplete，不变量已证：能续走流的终态必已 emitDone；回归 3 用例含 incomplete 正常收尾护栏）/ extractUsage 值类型守卫（连带可选字段同款丢弃防「05」式烂账，良型输入逐字段等价）；openai 两低置信项（index 兜底拆散/content:null）维持登记。**新增登记（R0912-3 修复批代理上抛）**：ai/rules/setting-rule.ts 名册抄本 parseRosterNamesLocal/PURE_HANZI_RE 仍 BMP-only——Ext-B 名 setting 域判重不可见，与 check 域 #32 修复后家族漂移，需后续批在 ai 域收敛。【登记·待择收】【销账·重评-0914-三轮 P2-3/P3-3（2026-09-14 处置批）：setting-rule 守卫族/名册解析五处抄本全删，改直接 import check/count.ts 导出单源（ATTRIBUTION_RE / SPEECH_ATTRIBUTION_RE / DIALOGUE_GUIDE_RE / ROSTER_NAME_RE / parseRosterNames）+ 候选窗改 codePointLength——Ext-B 名两检同判（re2-p3-setting-rule-check-parity 对拍 +2 用例双向断言）】

### E. 核心域 / 持久化 / 文档数据——出处：内存专项重审 §十 / 独立复审 §十 / 重评-0909 §六·§九 / PM 专项 §8.1 / 重评-0911c §4.2 / 重评-0912 §四 E 节 / 重评-0912-2 §3.2·§3.4 / 重评-0912-3 §3.1·§3.2 / 重评-0912-4 §四 / R0912-ds41 收口批复核 §四 / 阶段 24 批 C e2e 实抓 / 复审-0913-源码 §五 / 复审-0913-合并批 §五 / 复审-0913-mac适配 §二·§四 / 全库重评-0914 §三

- **structure.ts 消费清单路径裸 join 越书根（复审-0913-源码 P2-1·主审对码实锤）**：`document/structure.ts:174-185`（另 :439 同源消费）readChapterState 经 `svc.resolvePathAsync(docId)` 取清单 path 后 `join(bookRoot, path)` + readFileSync 直读，未走 safeManifestPath/resolveWithinRoot——全仓 manifest 可篡改数据面唯一裸奔消费点（service.resolveSafePath / state.ts safeManifestPath / trash.ts Y-18·R51-D-1 / tree.ts / finalize.ts 均设防，grep 证实本文件零 import 防线单源）；`:176` startsWith(BODY_PREFIX) 只锁前缀，`正文/../..` 形态可越书根读文件进 merge plan 拼接正文/sourcePreview/字数（读取/信息面；写侧 svc.save 有防线，不升 P1）。触发 = 清单条目被外部篡改后执行合并/拆分/撤销。**处置（复审-0913-源码修复批 2026-09-13）**：readChapterState 改 safeManifestPath（null → BAD_INPUT 拒收），同文件 applyChapterMerge/finishMerge（回收站 originalPath 同为清单派生面，非法 → NOT_MERGE_STATE 交作者）/locateMergeByBody 三处同源收口；回归 = structure-merge +2 用例（源/目标清单 path 篡改越界 → plan/apply 400 且盘面逐字节不动）。【已处置·复审-0913-源码】
- **leads 开启章缺整数/正数守卫（复审-0913-源码 P2-2·主审对码实锤）**：`format/leads.ts:345-355` 仅 `Number.isFinite`——chapters.ts:131 同语义字段（章号）有 `!Number.isSafeInteger(章号) || 章号 < 1` fail-loud（R31-15），R75-2 自称对齐口径实修一半；`开启章: -3` / `12.5` 无拦截无留痕直落数据模型 → readStaleLeads（read.ts:40）age 恒虚高、「悬太久」黄项对整条线持续误报 + 章号区间比较错位。修法 = 同式补守卫（缺字段/空串语义不变，~2 行）。**处置（复审-0913-源码修复批 2026-09-13）**：`Number.isSafeInteger && >= 1` 才采信否则回落 0，leads.test +2 用例。【已处置·复审-0913-源码】
- **style readBaseline 坏 JSON 静默降级零留痕（复审-0913-源码 P2-3·主审对码实锤）**：`metrics/style.ts:313-322` catch → null 与「文件不存在」同判、无 log——基线.json 在盘损坏/不可读时 styleConsistencyRule toPrompt/check（style-rule.ts:89/95）判无基线，文风约束注入与 7 维偏离去检双静默失效（5 个调用方无补留痕）；同库同场景先例均有 warn（short-index.ts:278 / runner.ts R37-9），违「降级必留痕」。修法 = catch 补 log.warn 区分「不存在→null（正常）」与「损坏→warn + null」（~3 行）。**处置（复审-0913-源码修复批 2026-09-13）**：catch (e) 补 log.warn（带 bookRoot 与病因，「文风对照将不生效」归因线索），不存在路径仍静默 null；metrics/style.test 补 warn tag/root/语义三断言。【已处置·复审-0913-源码】
- **SHORT_DEFAULTS 数组实例跨域共享（R0912-ds41 收口批复核 P3-5）**：`shared/short-defaults.ts` 单源后两消费方浅拷贝展开（`{ ...DEFAULT_SHORT_CHECKS }` / `{ ...DEFAULT_SHORT_CONFIG, ...shortConfig }`），`target_emotions` 等数组仍指向正本同一实例——单源前 install 侧 mutate 不波及 metrics，现会跨域污染正本；仓库无 mutate 默认表惯例（理论级），防漂移锚 `toEqual` 值比较不防 mutate。【登记·维持观察】

- **复审-0913-源码 P3 处置总账（2026-09-13 修复批）**：随批收 20 件——state.ts encodeOrLiteralNames 死码删（journalFile 参数必选化）〔①〕/ structure.ts expectedRevision 改 t.rev 消冗余重读（读后并发改 → revision 拒收，更 fail-closed）〔②〕/ service.ts SAVE 锁超时常量化 + __setSaveLockTimeoutForTest 钩（R30-18 惯例）〔③〕/ status.ts 已发布判定收编 chapters.isPublishedValue 单源〔⑥〕/ branch-tree sortEvents 收编 projection〔⑦〕/ style-migrate 播种收编 parseSampleFileName（.MD 口径同修）〔⑧〕/ style-inject 截断判据码位化〔⑨〕/ iron-rules 缓存挂 forgetBookKeyedCaches〔⑩〕/ chain-bridge 伏笔差分键 标题→file（ForeshadowEntry 盘上唯一身份，数据模型查明后取 file 非 编号——同标题双伏笔吞变更修复 + lineage 回归）〔⑪〕/ leads parseLeadFileName 陷阱分支统一〔⑫〕/ snapshots resolveDoc 收编 resolveBook（响应字节不变）〔⑬〕/ summary batch 版补整段自留痕〔⑯〕/ materials RAG 异常附病因〔⑱〕/ review contract 删冗余 as〔⑲〕/ 前端 formatWanZi 单源收编五处（各面输出逐字节保持）〔㉕〕/ 「写作/正文/」三处收编 isBodyKind〔㉖〕/ OverviewView 继续写作补 ownerBook+tree.error 守卫（R35-10 同款）〔㉒〕/ provider·Overview 次级加载降级 console.warn 留痕〔㉓㉔〕/ desktop IPC \0 防御对称；**维持登记** = ④容错 trash 清单无缓存 / ⑤doCreate 消毒口径 / ⑭错误码词表演进 / ⑮analysis includes 微项 / ⑰export displayNum 单调性【已拍板维持·2026-09-15 拍板快断批：作者指令「按建议顺序开工」取主审建议「维持」档——已发布章号不可变优先于显示序单调，〔6,7,8,9,5,10〕形态接受不改，export/index.ts D7 注记扩注钉口径】/ ⑳rules 登记粒度 / ㉑prepare 深度口径 / 拆分光标换算双源（注释互指维持）；**两待核查证销案** = events migrateBookSession 窗（删书/改名同受 busyGate 互斥，产品链无并发删库路径）/ status.ts 缺文件落 final（tree 走 readdirSync 盘面枚举，缺文件不可达树节点）——明细 = 报告 §五 5.4。
- **撤销并入回收站反查歧义：locateMergeByDisk 按章号反查「首个」同章号条目可误认领（阶段 24 批 C e2e 实抓·structure-volume-move spec 头注④）**：`structure.ts` undo 定位链兜底 locateMergeByDisk 取 max(并入) 章号后按 `chapterNoFromName(originalPath)` 匹配回收站**第一条**——回收站存在同章号历史软删条目（删章→同号重建→再合并→再撤销链）时误还原旧条目（e2e 实抓：structure-ops 残留 0005/0006 ×2 被误认领，源章未还原 + 连坐 KPI）；事件定位（userDataPath 在档）优先时不受影响，仅事件库缺形态暴露。修法候选 = 反查加 originalPath 与源章清单原路径一致性校验 / 回收站条目按 createdAt 择最新 / merge 事件缺时拒收指引人工。**【已拍板处置·2026-09-15 拍板快断批：作者指令「按建议顺序开工」取主审建议「择最新」档——locateMergeByDisk 同章号多条按 trashedAt 择最新（合并时点条目），回归 = r0915-merge-undo-newest 2 用例（旧软删条目 trashedAt 回拨不误认领 + 无歧义基线）】**
- **trash.delete 单条彻底删除与 finishMerge 软删入回收站理论竞窗（复审-0913-合并批 P3-6·登记备查）**：撤销窗内回收站条目被作者手动彻底删除后 undo 还原链的条目缺失路径——单条作者动作、非「批量删/清理」族、先于合并 `0b312a97` 存在（非合并引入）。后果 = undo 该路报错交作者处置、无数据损毁（源章正文仍在）。处置建议 = 触达 structure.ts/documents.ts 时补条目缺失分支显式报文。〔与上行 locateMergeByDisk 反查歧义同域联动拍板——上行已随 2026-09-15 拍板快断批「择最新」档处置；本行竞窗维持登记备查〕【登记·备查·复审-0913-合并批】

- **syncRenamePieceList 命中读用容忍版 readManifest → 瞬态锁占时 docId 身份断裂（重评-0912-4 P2-2·主审对码实锤）**：`service.ts:1242` `[...readManifest(...).entries].find(...)`——容忍语义（manifest.ts:85-134：EACCES/EBUSY/EIO → 空清单）下章纲清单改名撞瞬态锁占 → oldListRel 未命中 → 裸 linkOrRenameExclusive 回退 → 清单孤儿条目 + docJoinKey 失配 → docId 退化（N-7 同款危害终点）。系 R0912 已修 lookupPathByDocIdAdoptAsync 命中读 strict（上行「保存守卫清单读容错口径分裂」）的**同族漏网成员**。修法 = 命中读改 readManifestStrict（同族修法）。**处置（重评-0912-4 修复批 2026-09-12）**：命中读改 `readManifestStrict` + 撞锁（EBUSY 等）warn 留痕后 return——章纲滞留旧名（登记与盘上文件保持一致，不走裸 linkOrRenameExclusive 兜底），正文改名不受阻断（调用点在正文 rename 落定之后）；回归 = r37-piece-list-rename +1 用例（EBUSY 注入：新位零副本 / 旧名留存 / warn「章纲清单读失败」/ 正文 rename ok）。【已处置·重评-0912-4】
- **reconcileSavePending 无 save 锁在持检测 → 在途保存被误报 crashedWrite（重评-0912-4 P2-3·主审对码实锤）**：`state.ts:726-756` `rev === p.baseRevision` 即维持 crashedWrite 报告、全函数无 `<journal>.save.lock` 在持检测——慢盘（SMB/杀软扫描）大章保存在途窗（journal pending 已写、revision 未推进）内并发 `/state` 轮询报「可能丢字」幽灵警报，作者被误导中断/重复保存/误点 acknowledge；R0912 刚建的 reconcile/acknowledge 闭环被在途窗削弱。修法 = 补 save 锁在持检测（锁在持 = 在途非崩溃，跳过报红）。**处置（重评-0912-4 修复批 2026-09-12）**：`queryLockHeld` 探测 `<journal>.save.lock`——rev === baseRevision 且锁在持 → 判 'inflight' 不报红；reconcileSavePending 返回值三态化（settled / inflight / crashed，保守分支一律 'crashed'）；回归 = r0912-save-pending-reconcile +1 用例（真跨进程锁占下不报 crashedWrite，释放后照常消解）。【已处置·重评-0912-4】

- **events checkpoint 裸 close 纪律加固（重评-0912 E-P3-3）**：`events/store.ts:1267` migrateBookSession 的 cp 连接裸 `cp.close()` 违反本文件自设「close 一律走 closeEventsDb」纪律（事实安全——未入 prepared 缓存构造上无 ephemeron 面，但属纪律地雷：后续惯用法改 prepared() 即复活 R0911-G-P3-4 泄漏）；同族 `rag/store.ts:89-94` legacy checkpoint 裸 close + `:236-239` prepared() 头注仍宣称「close 后随 GC 消失」**与 ephemeron 实测结论矛盾**（G1-P3-1 失实注释，会误导拆除断链）。两处改 closeXxxDb + 注释修账，~5 行。【已随批·2026-09-12 修复批 R0912（E-P3-3 + G1-P3-1：closeEventsDb/closeRagDb + prepared() 失实注释修账）】
- E 域其余 P3（writeVersion→pruneVersions 双 listVersions / executeSave 21 处 Promise.resolve 冗余 / trash 清单无指纹缓存 / export 微瑕）明细 = 重评-0912 §四 E 节。【已随批·2026-09-12 修复批 R0912（E-P3-1/2/4/6；E-P3-2 传 knownList 补新条目+比较器重排——修复中抓出裸 append 误删最新版真 bug，等价性/幂等测试钉定；E-P3-1 指纹用 size:mtimeNs 覆盖强于报告建议；E-P3-5 登记性维持）】

- **save 类崩溃 pending 无消解闭环（重评-0911c P2·主审对码坐实）**：healthCheck 对 save 类 pending 一律报 crashedWrite（`state.ts:350-370`），journal compact 恒保留未结算（`journal.ts:342-383`），无 baseRevision×盘上指纹复核、无 acknowledge 通道（全仓 grep 证实）→ settled 写失败（`service.ts:629-633` best-effort）后幽灵红每次进门重复报「可能丢字」、应用内无法清账。修法：确定性复核（指纹不一致 ⇒ 已落盘自动补 settled）+ acknowledge 端点。**处置（R0912 CORE）**：两段全落地——reconcileSavePending 自动消解（null/ENOENT/EBUSY 边界保守报红）+ `POST /api/books/:name/journal/:opId/acknowledge`（幂等）+ `/state` payload 透出 crashedPendingOpIds（主审集成）+ FE「忽略此提醒」按钮贯通。【已处置·R0912】
- **结构性操作不持 save 锁的双向复活窗（重评-0911c P2）**：doTrash 落位（`service.ts:1811`）/doMoveOrRename 落位（:1473）全程无 `<journal>.save.lock`——他进程 save 过守卫后、rename 前 atomicWriteFile 在旧路径复活已删文件（后续还原报 OCCUPIED、清单无条目）；R76-22（:419-422）登记的只是「复核→写盘」单侧毫秒窗。修法：结构性操作 rename/rm 源前同取 save 锁（锁序先例现成）。**处置（R0912 CORE）**：doTrash/doMoveOrRename 落位段取锁（STRUCT_SAVE_LOCK_TIMEOUT_MS=5s + holdSaveLock 缺省 fail-loud），锁序 save→journal→清单与 executeSave 先例一致，4 用例互斥验证。【已处置·R0912】
- **保存守卫清单读容错口径分裂（重评-0911c P2·主审对码坐实）**：`lookupPathByDocIdAdoptAsync:1567` 用容错版 `readManifest`（瞬态 EBUSY/EACCES → 空表 → 「文档已移动」守卫静默失效 → 同内容双文件身份分裂，不丢数据）；RMW 链 :1552/:1583 已 strict（R27-40）、同函数回收站守卫 :337 亦 strict——口径不对称。修法：命中读改 strict。**处置（R0912 CORE）**：lookupPathByDocIdAdoptAsync 命中读改 readManifestStrict + 六调用面（save/trash/move/copy/meta×2）收口 WRITE_ERROR，3 用例（EBUSY 未落盘+恢复后成功等）。【已处置·R0912】
- **SessionRecorder.flush 失败路径 pending 无上限（重评-0911c P2）**：`chat-bridge.ts:183-206` 持续落库失败（SQLITE_BUSY 耗尽/磁盘满）+ 长对话下 pending 无界增长；同文件 ChainRecorder 有 256 上限（chain-bridge.ts:148,197-208），recorder 无对应闸。修法：对齐加闸+留痕。**处置（R0912 CHECK）**：SESSION_PENDING_MAX=256 对齐 ChainRecorder + 丢最旧 warn 留 turn 区间 + 批内序号平移加固（sourceIdxs 摘除/前移，防恢复轮撞 store 越界校验）；3 用例含恢复轮整批落库。【已处置·R0912】
- meta PATCH 双路径写回无 journal pending（`service.ts:1071-1078`/:1328-1334）——「每写必有 pending」协议在元数据链豁免未登记。**处置（R0912 CORE·选「真补」）**：updateChapterMetaLocked/updateDocMetaLocked 补 pending/settled（写前指纹 → atomicWriteFile → 失败 aborted/成功 settled best-effort），与消解闭环自洽；3 用例（配对清零/双 pending/ENOSPC 留痕）。【已处置·R0912】
- version.ts:377 版本列表 ULID `localeCompare` 排序疑似漂移面。【已处置·R0912：改字节序比较 + 排序性质测试】
- **事件读链 O(N)（= PM-10 残余真·尾读）**：store 层 sinceSeq 游标下推 + skeleton 计数通道（listEvents 现为游标流式全量投影，total/truncated 契约需全量；契约注释 + 6 用例守门在位，改造时为验收面）。【待拍板·立项】
- execRing 会话级单环并发 owner 回放丢段（chat×self-heal 并发 + 断线重连窗，per-owner 环单立）。【单立】
- md 缓存 stat→read 指纹错配（ns 级自愈型，读后复验 IO 翻倍不称）。【维持】
- 跨进程锁续期 timer 丢失 release 空转（防御性缺口，当前零违约调用方）。【维持】
- WAL Atomics。【维持】
- **chapterNoFromName 裸数字 / `1.md` 扩集**（既定边界，扩集牵动全部消费点；注释已修失实 + callshape 对表测试钉分裂现状）〔核对 2026-09-11：`src/format/filename.ts` 正则 `^(\d+)(?:[-—]|\s|$)`——带扩展名全名形态不中，边界与登记一致〕。【已拍板维持·2026-09-15 拍板快断批：作者指令「按建议顺序开工」取主审建议「维持」档——扩集牵动全部消费点，既定边界保持不改】
- leads.ts:537 `\d{3}` 劈分语义备案。【维持】
- piece-list-core 头注「MetaFormPanel 直接 import」与现状不符（零 import，历史注释记档）。【维持】
- 文档格式 P3 ①③④⑤⑥（重评-0909 报告细目）。【维持】
- P3-20 读侧关联词「值 === 标题且含逗号 → 整词单项」收口（可收回旧迁移存量且不伤表单路径；写侧数组化已落）。【已拍板处置·2026-09-15 拍板快断批：作者指令「按建议顺序开工」取主审建议「读收口」档——foreshadow.ts 读侧字符串关联词整串等于含逗号标题 → 整词保留不劈分，foreshadow.test.ts +1 用例】
- mac 上事件库 bookHash 不折叠大小写（E-P3-1：books.jsonl 路径漂移分裂事件库「丢史」假象——store.ts R40-23 注释登记在案）。【维持】（复审-0913-mac适配 P3-4 重证维持：blanket 折叠会重键存量库，同族修法 = 先探测卷敏感性再折叠；文档身份键侧已由 platformCaseFold 覆盖、仅事件库键口径分叉，触发面窄）
- **platformCaseFold 平台级判定、无卷级大小写敏感性感知（复审-0913-mac适配 P3-13）**：`fs/safe-path.ts` 按 `process.platform` 折叠（win32/darwin），感知不了「mac 手动格式化成大小写敏感 APFS / 部分外置盘」——此类卷上 `1-A.md` 与 `1-a.md` 两真实文件折叠同键（manifest/树 join 混叠）。缓解在位：README「macOS 版使用须知」已披露敏感卷风险 + 选库 probeCaseSensitive 探测警告（fail-open）；代码面彻底解 = 折叠键外保留精确键双查或按卷探测，属重设计非小改。【维持·并入敏感卷既有议题（随 bookHash E-P3-1 同族择机统一处置）】
- writePieceList 生产零接线、章纲未知段无保形写通道（E-P3-4，R48-51 登记）。【维持】
- isPieceBody 每次 meta 编辑同步整读+解析 book.yaml（重评-0911b E-P3-3；方法内已缓存局部值，低频可接受）。【维持】
- **布线锁键缺 NFC 归一，同一布线文件可派生两个锁文件（重评-0912-2 P2-3·主审对码实锤）**：`service.ts:832-846` wiringFileLockKey 与 `lead-finalize.ts:121-123` wiringFileLockKeyOf 只过 platformCaseFold（`safe-path.ts:159-162` 仅大小写折叠）不过 toNfcName，而文档身份键 docJoinKey（`safe-path.ts:180-182` R41-2）叠加 NFC——「判同」与「互斥」口径分裂：mac NFD 盘上形态（外源工具常产）× NFC 清单形态下，保存链与终稿链（锁内重读-合并-写回）对同一布线文件各派生锁文件，丢失更新窗静默重开（后写覆盖先写、无痕、终稿侧无快照兜底）。修法 = 两侧折叠前叠 toNfcName（与 docJoinKey 同口径；NFC 多数派键字节不变、与 R45-2 字节稳定不变量相容）+ 同查 files.ts PUT 侧同源派生 + NFC/NFD 互斥用例。【已处置·R0912-2 修复批：三侧折叠前补 toNfcName——service/lead-finalize 外，同源排查抓出 files.ts `wiringLockKeyForPut`（PUT 直写链）第三处同型缺口同批修齐；r0912-2-wiring-lock-nfc 6 用例（三侧 NFC/NFD 同键字节钉定 + 真实跨进程锁行为级互斥 + R45-2 防回归 + 静态序扫描）】
- **文风收割链事件循环阻塞（重评-0912-2 P2-4·主审对码实锤）**：`process/style-harvest.ts:185` 逐 tracked doc 裸 readFileSync 整读章正文（不走 readMdTextCachedAsync 无指纹缓存）+ `:192-199` collectDocSignalsAsync 内 compareVersions O(P²) 段对矩阵同步计算（format/style-compare 纯 CPU 无让出）——R44-13 已清该链最后一处同步 spawnSync，此为同族残留；200 万字书数十 tracked doc 冷缓存时 harvest 端点可冻结事件循环数百 ms 至秒级（SSE 心跳/保存停摆）。修法 = 正文读改 readMdTextCachedAsync + compareVersions 按 doc 让出（scanChaptersAsync 同款范式）。【已处置·R0912-2 修复批：readMdTextCachedAsync（缓存面 = 原始文本逐位等价，读失败 null 映射既有 continue 口径）+ 循环按 doc yieldToEventLoop（async.ts 单源）；同步孪生不动（测试资产）；缓存命中/改道输出逐位等价 2 用例 + 同步异步等价对照 9 文件 65 用例全绿】
- E 域 P3 登记（重评-0912-2 §3.4）：章纲清单 relink 未用 docJoinKey（service.ts:1228）/ clearBatchPause 裸 rmSync ×2 / restoreTrash originalPath 无内部区校验 / updateDocMetaLocked 过度失效（全量 invalidateTreeIndex）/ 死码小项（generateFolderId·searchForeshadowTrails ~60-100 行）/ journal pending 256KB 截断（PM-3 在案）/ analysis 锁超时降级裸写（B-15 在案）；进程域十条：recordAiVersion 失败完全静默（「丢事件必留痕」局部不自洽）/ 同步 git() 超时只 TERM 无 KILL 升级 / validateEventStream 校验口径与真实产流劈裂（只能跑人工夹具）/ TOKEN_COEFFICIENTS 空表（R26-106 在案）/ sync-async 孪生双轨 ~500 行（searchBook 同步三件套零生产调用）/ saveDraft 旧文双读 / mock·cc 总线 ~180 行重复（R62-40 在案）/ test-only 导出四件 / prepare·summary 找章深度口径不一 / gitAsync kill 窗僵尸持锁（有界 fail-closed）。【登记·待择收】
- **move 的 toDir 接受 `..`/`.` 段且原文入清单 → docId 身份分裂（重评-0912-3 P2-1·主审对码实锤）**：`service.ts:209-215` normalizeMoveToDir 只拒前导 `/` 与空串，`:1510-1514` safeSegs「已存在则原样保留」对 `..` 恒命中（existsSync(join(root,'a','..')) 即 root）——`toDir='a/../写作/正文'` 时 `..` 原文直拼 manifest，物理落位经 resolveSafePath 词法消解落在正确目录：登记与盘上路径分裂 → docJoinKey 失配 docId 退化 legacyId → executeSave registered≠relPath 守卫命中**保存恒 REVISION_CONFLICT 且无法自愈**；finalizedPathSet 失配（定稿章被导出/文风/学习链当草稿）。R66-5（尾斜杠同后果）/R51-D-3（doCopy 显式拒 `..`）同族——copy/create/rename 各自落地唯 move 的 toDir 漏网；端点层原样透传；r66-move-todir-normalize 只锚斜杠族。修法 = normalizeMoveToDir 增 `segs.includes('..')||segs.includes('.')` → BAD_INPUT + 1 用例。【已处置·R0912-3 修复批：normalizeMoveToDir 归一后增 segs.includes('..')||includes('.') → BAD_INPUT（口径对齐 doCopy R51-D-3；safeSegs 语义不动，`工作区/.trash` 类点开头段不误伤）；6 形态拒绝 + 被拒后身份完好用例】
- **doTrash 定稿基线快照与清单删除跨锁窗不覆盖 finalize 链（重评-0912-3 P2-2·主审对码实锤，触发窗窄）**：`service.ts:1944-1963` 无锁快照 priorFinalized → `:1980` TrashEntry 按快照落账 → `:2053-2069` 清单锁内**新鲜读**整条 delete；对照 `finalize.ts:224-237` finalize 持清单锁写基线但**不持** doTrash 的 per-doc save 锁（R0912-2 收口的不对称面）。时序：doTrash 读快照 A（无基线）→ 并发 finalize 写入基线（pinned 版本已落盘）→ 删除 RMW 把带基线条目整条删掉——还原后该章无定稿基线，`ensureChapterNotFinalized` 防覆盖闸失守（W-P2-1 自述要防的威胁）；tags/order 同窗同失。pinned 版本在 `.版本/` 可考（非物理丢失）故不升 P1。修法 = 「TrashEntry 落账 + 清单删除」收进同一清单锁临界段——删除 RMW 锁内以当次 strict 读 entry 回填/覆盖 TrashEntry 基线字段后再 delete（两锁串联不嵌套，与既有锁序兼容）。【已处置·R0912-3 修复批：TrashEntry 落账+清单删除收进删除 RMW withManifestLockAsync 临界段——锁内 strict 新鲜读按 trashBaselineOf 单源投影（字段集+键序钉定，JSON 比对判据）与无锁快照比对、不一致先按当次值回填 TrashEntry（append 同 id 替换）再 delete；真实跨锁竞态用例（持 trash 登记锁闸 doTrash→finalize 写基线→放行）红绿验证 + 无并发逐字节不变对照；锁序单向（save→主清单→trash 登记）无环 grep 核实，trash.ts Z-5「全程不嵌套」过时注释随批修账】
- E 域登记（重评-0912-3 §3.2）：save absPath 入队前一次性 resolve 队列延迟窗不重解析（symlink 竞争下 rename 替换 symlink 本身致路径对账漂移，现实概率极低）/ updateDocMetaLocked fm-only 整书失效（**归并既有行**——上行重评-0912-2 E 域 P3 已登记同点）/ rebuild SOURCE_PROBE_TTL 3s 节流按 R47-11 登记口径落地（对账确认非缺陷）。本域零 P1，原子写/锁谱系/缓存失效挂点核验闭合。【处置·R0912-3：save absPath 重解析维持（symlink 竞争现实概率极低）；updateDocMetaLocked 归并既有行维持；SOURCE_PROBE_TTL 对账非缺陷维持。**新增已处置补记（R0912-3 修复批）**：spill 写入失败全链静默降级缺留痕（报告 #28，`process/spill.ts:77-79/150`——落盘批台账漏登记本条，随处置补记）——writeSpillFile catch 补按书根 60s 窗节流 warn（源头单点留痕：书根/失败原因/降级后果），降级内联行为逐字节不变 + 3 用例（warn 恰一次/连发节流/降级逐位相等）】
- **doCopy 目录段 `.` 不拒——清单登记与物理落位分裂致 docId 身份分裂（全库重评-0914 P2-2·主审亲验，R51-D-3 同族漏网）**：`service.ts:1796` 只拒 `..`，:1797-1799 目录段「已存在则原样保留」分支对 `.` 恒命中 → `a/./b.md` 原文入清单而盘上折叠为 `a/b.md`（树 annotate 按 docJoinKey 失配退 legacyId）；对照 normalizeMoveToDir :213-219 `..`/`.` 双拒。触发面 = API/AI/CLI 传含点段 relPath。修法 = `relSegs.includes('.')` 一并拒绝。**处置（全库重评-0914 修复批 2026-09-14）**：doCopy 归一后 `if (relSegs.includes('..') || relSegs.includes('.'))` → PATH_ESCAPE 拒（口径对齐 normalizeMoveToDir）；回归 = r0914b-copy-dot-seg 2 用例（`写作/正文/./0002-副本.md` 拒且不落盘 / 合法 relPath 复制不受影响）。【已处置·全库重评-0914 修复批】
- **账本推进归档时间戳变体对全部消费方不可见——写入-读取契约破裂（全库重评-0914 P2-5·主审亲验）**：`lead-update-draft.ts:193-197` 同章已存在时落 `第N章-${Date.now()}.md`（L-P6 保两代），而 `check/run.ts:310` 正则 `^第(\d+)章\.md$` 与 `check/lead-updates.ts:178` 精确路径 archivePath 均只认标准名 → 第二代归档对机检两端闭合预扫与定稿防吃书闸/履历回写完全不可见（已声明推进被静默丢弃）。修法 = 写侧去时间戳化（同 tag 读旧+追加合并重写标准名，锁内原子性已有，推荐）/ 读侧放宽多文件按章合并。**处置（全库重评-0914 修复批 2026-09-14）**：取写侧去时间戳化（报告推荐档）——标准名在盘时读旧 + 按「leadId+动词」键合并重写（新证据覆同键旧值、旧序保留、新键追加；导出纯函数 `mergeLeadUpdateEntries`）；旧档读失败 / 新档零有效条目回落时间戳变体（L-P6 两代保全不破坏，回落路径逐字节不变）；回归 = r0914b-lead-archive-merge 4 用例（同键覆新+新键追加+旧序保留 / 零有效条目回落原档不动 / 裸改名形态不变 / 读侧闭环 readChapterUpdatesForChapterChecked 双条目可见）。【已处置·全库重评-0914 修复批】
- **isWithinRoot 对不存在路径不消解中间 symlink，与 resolveWithinRoot 不对称（全库重评-0914 P3-29·既有记档重证）**：`fs/safe-path.ts:106-111` 仅词法前缀比对，不 realpath 消解中间段 symlink（头注既有登记，本轮独立重证成立）；触发 = 「不存在路径 + 中间段为 symlink」双条件，当前调用点均为存在路径，防御纵深项非现网缺陷。修法方向 = 不存在路径时回退最近存在祖先 realpath 再比对，或显式拒绝。【登记·全库重评-0914】
- **chat-bridge 丢最旧平移不覆盖调用方持有 sourceIdxs（全库重评-0914 P3-32·主审亲核消费面）**：`events/chat-bridge.ts:211-226` SESSION_PENDING_MAX=256 丢最旧时批内序号平移（摘除/前移）不回写调用方已持有引用（turns.ts:535-550 lineageIdx 回合内消费）——越界由 store 校验兜住整批回滚（有界 fail-closed 不出错链）、不越界则静默错链；触发 = 持续落库失败 >256 条叠加跨丢弃窗引用，概率极低。修法方向 = 持有侧按批内序号惰性解析或平移时回写持有面。【登记·全库重评-0914】

### F. RAG / 知识 / 机检——出处：独立复审 §十 / 重评-0910 §十 / 重评-0911b §3.3·§四 / 重评-0911c §4.2 / 重评-0912 §三·§四 / 重评-0912-2 §3.2·§3.4 / 重评-0912-3 §3.2 / 重评-0912-4 §四 / 全库重评-0914 §三

- **知识层方法论注入未接线（重评-0916-五轮 P3-13）**：知识层 13 条 manifest 资产在册且 check:knowledge 门绿，方法论条目未注入 AI 链路——设计留白；接线口径（注入时机 / 选取策略 / 预算占用）待作者拍板后单立。【登记·待拍板·2026-09-16 五轮处置批】

- **opening-env 入严格升红集使 R29-4 截断残余成为红项误报 → 白烧重写费（重评-0912-4 P2-5·主审对码实锤）**：`count.ts:1117` 窗口先截断后剥引号（`stripQuotedSpans([...body.slice(0, openingChars*2)].slice(0, openingChars).join(''))`）——窗尾截断的半个引号 span 不被识别、引号内容仍参与匹配，R29-4 注释自认「误报向残余，黄项 advisory」；但 `runner.ts:399` 将 `'opening-env'` 列入 STRICT_SHORT_CHECK_IDS、`:422-425` 严格模式黄→红——短篇 strict 下开篇窗尾截断对白（角色嘴里的环境词）被升红拦定稿闸，AI 重写循环对非缺陷重写烧调用费。R29-4 的 advisory 前提被升红矩阵破坏；R0912-3 刚收口 fm 章号/引号集族的本族升红矩阵面漏网。修法 = 先 stripQuotedSpans 全文再开窗（swap 两步）或 opening-env 移出 STRICT_SHORT_CHECK_IDS。**处置（重评-0912-4 修复批 2026-09-12）**：checkOpeningNoEnv 改先 stripQuotedSpans 全文再码点开窗（R33-32 窗机制不动，窗口语义落在去对白后的叙述面，opening-env 维持在升红集——真环境词防线不弱化）；回归 = r29 +1 用例（窗尾截断 span 零误报 / 剥后窗外对照零误报 / 窗内正面锚仍命中）。【已处置·重评-0912-4】

- **adjStack 嵌套量词残余回溯向量（重评-0912 F-P2-1，主审本机实测复现）**：`check/count.ts:750` 守卫 `/的{8,}/` 只切 ≥8 连「的」游程，4-7 连游程（每游程 2-8 种单元分解）× 高位 maxAdjStack（clamp 允许 20，脚手架默认 3）×「恰差几个单元」失败形态 → 指数回溯：41 字符病理片段 469ms（收紧后 0.1ms）、12KB 混 20 病理簇 18,350ms 同步阻塞（扫描管线内不可让出）——系 R27-23/R46-46 两次修复只封主通道的残余向量。修复 = 守卫收紧 `/的{3,}/`（一行，正常文本命中经实测零变化）+ 钉值测试。【已处置·2026-09-12 修复批 R0912-F-P2-1：界收至歧义起点 {3,}（c(2)=1 以下）+ 钉值测试 3 例（病理片段空命中+耗时上界 / 正常 25 单元堆叠命中数组精确锚 / 3 连「的」不产垃圾）】
- **knowledge manifest JSON `null` 崩点（重评-0912 G1-P2-1）**：`knowledge/manifest.ts:67` `JSON.parse("null")` 返 null 不抛 → `:76` 守卫只查 undefined 漏 null → `:81`/`update.ts:184` TypeError 穿透 KnowledgeManifestReport 信封（违背 R73-4 自设形状守卫口径，触发面 = 手编/半写 manifest 为 null）。修复 = readKnowledgeManifest 单点 null/非对象守卫 3-4 行。【已处置·2026-09-12 修复批 R0912-G1-P2-1：parse 后形状守卫 + 3 用例（null/标量/commit 链）】
- RAG 热路径 P3 随批：3 条固定 SQL 绕过 prepared() 缓存每次重编译 / safeRollback 样板 5 处归一 / deleteRagDbFiles TOCTOU ENOENT 误报失败 / learn 金句判定可读性改写。明细 = 重评-0912 §四 G1 节。【已随批·2026-09-12 修复批 R0912（G1-P3-2/3/5/7；G1-P3-7 带 TOCTOU 语义+EIO 对照测试；G1-P3-4 登记性维持）】

- **机检剥引号口径分裂（重评-0911b P2④；重评-0911c 独立复现并补证）**：`check/count.ts` checkBodyParts（:879）/checkSimile（:932）不剥对白引号 span，同文件禁词（:119）/意象（:483）/开头环境（:1061）均 `stripQuotedSpans`，且 R51-E-N5 注释（:478-483）自证「同族均剥、唯本检查吃原文」家族约定存在——对白密集章 body-parts/比喻密度系统性虚高，两项又属严格短篇升红族（runner.ts:377-405）可致对白密集章被误打回重写白烧模型调用；无注释声明有意为之。【已处置·双线各一批并合：win 线 R0911b-P2④+F-P3-1（2026-09-11 重评修复批）+ mac 线 R0912-CHECK（2026-09-12）——两函数调用点补剥（SIMILE_RE 本体不动——scripts/harvest-corpus.ts 语料收割复用正则直扫原文，剥引号由消费方各自决定）+ style-dialogue-tag 堆叠项对齐 + R73-14 注释漂移修账（前排他集补「群像」）；语料锚 fire/silent 逐锚核实零翻转 + r0911b-quoted-span 7 用例 + R0912 5 用例（改前口径自证 + 叙述真实命中仍报防矫枉过正）】
- **knowledge manifest 字段类型守卫缺口（重评-0912-2 P2-5·子代理 tsx repro + 主审对码实锤）**：`manifest.ts:103` 条目守卫只拦 null/非对象，`:123` isAbsolute / `:135` startsWith 对非字符串 target/sha256 裸 TypeError——check:knowledge 门裸栈崩而非列 issue；commitKnowledgeFile 尾部对账在**登记已落盘后**崩（CLI 报栈但 manifest 实已写入，作者状态认知错位）。R40-16/R73-4/R0912-G1-P2-1 同族序列漏网。修法 = validateEntry 入口对 `typeof` 非 string 字段报 issue + continue + 同族用例。【已处置·R0912-2 修复批：validateEntry 字段类型守卫（报 issue 不崩，对齐 ：103 降级口径）+ 随批 P3 corpus null/非对象项剔除 + warn 留痕不崩整轮；4 用例（两类字段崩点/commit 链落盘不抛/corpus 坏项）】
- F 域 P3 登记（重评-0912-2 §3.4）：knowledge corpus 数组 null 项崩整轮汇总（update.ts:91,98，随 P2-5 同批）/ learn 样章块长 UTF-16 双口径 / 样章切分不匹配 CRLF（外部编辑章候选静默全灭）/ 单坏 fm 封死全书收割（与 RAG 坏章跳过口径不一致）/ defaultCommitYield 与 async.ts 同体重复（「五处收敛」漏网）/ 背带掩码缺口（api-key:/Basic 头形，纵深非现实面）/ readAllChunks 零调用且毒行逻辑双份 / rag 注释失实与边界文案 / check 域七条（checkNewNames ~170 行移植副本对拍排除面 / ReviewTierDecision ledger_check 恒写死字段 / leads.ts:326 UTF-16 截断作 grep 锚——增补平面字伪红面 / style-entry 手写 .md 检查 / tree-issues-cache 失败指纹与空目录不可区分 / isSectionHead 兜底逐次 new RegExp / writePieceList 零接线 R48-51 在案）【已登记——其中五件随 R0912-2 修复批收口：corpus null 项（随 P2-5 同批）/ 样章切分 CRLF / 样章块长码点计数（与金句 R0912-7 口径收敛 codePointLength 单源）/ defaultCommitYield 收敛 yieldToEventLoop 单源 / leads.ts:326 码点截断（+1 用例钉增补平面字边界）；余项维持】
- RAG watchdog 双跑窗。【维持】
- RAG test-only wrapper。【维持】
- F 域登记（重评-0912-3 §3.2）：机检四条——章号-文件名前缀解析双口径（count.ts:49 自带正则 vs leads.ts:102 chapterNoFromName 宽容集，fm-chapter-mismatch 对 `6—标题.md` 宽容分隔形态失明静默跳过）/ book 层 repeat_threshold 只验 >0 不夹紧 (0,1]（runner.ts:199，global 层 unitNum 有夹紧——层间不对称，手写 1.5 静默杀死复读检查，违「配置不生效必留痕」自设纪律）/ 宽容引号集缺 ASCII 单引号（quotes.ts:19-20，单引号包裹证据 → lead-evidence-miss 伪红，fail-noisy 向）/ checkNewNames 名册正则 BMP + UTF-16 长度窗 CJK Ext-B 双向盲区（count.ts:308·446——与已收口 leads.ts:326 增补平面族同域不同点）；scripts 四条——check-knowledge 反向扫描精确比对未走 caseFold/NFC（:97-107，大小写/归形漂移 CI 假红；caseFoldKey 单源唯一漏改点）/ knowledge-commit 预存坏行时「登记已成功却报 manifest 未写入」自相矛盾文案（:41-44，作者重试撞「已在 manifest」）/ harvest-corpus 早退 process.exit 绕 finally db.close（:135-138，R71-34 不变量该路径不成立，进程即退无实害）/ corpus-commit 空白摘录行静默丢弃不进 droppedExcerpts 哨兵计数（:73）。【已处置·R0912-3：机检四条全收——章号解析收编 chapterNoFromName 单源（`6—标题.md` 宽容形态不再失明，双向用例；R33-30 路径容忍保留）/ repeat_threshold 消费点夹紧 (0,1] 上界到 1 + warn 留痕（对齐 global 层 unitNum 先例；夹紧到 1 = 关比率口径、绝对字数兜底；≤0 维持解析层拒绝）/ 宽容引号集补 ASCII 单引号（误剥面评估通过：span 面 QUOTED_SPAN_RE 无 ASCII 引号零波及，证据面多候选兜底；双向用例）/ checkNewNames 码点窗（本文件 R73-19 codePointLength 单源复用，不引 process/summary 版防分层上探）+ 名册 astral 区段 \u{20000}-\u{2FA1F}·\u{30000}-\u{323AF} + u 标志（HANZI 常量本体不动）；scripts 四条全收——check-knowledge 比对收敛 caseFoldKey(toNfcName(…))（docJoinKey 管线同序）/ knowledge-commit 文案两态区分（登记已落盘+预存坏行 vs 未写入，沙箱三态自测固化）/ harvest-corpus labeled break 保 finally db.close（退出码 1 与候选不落盘语义不变）/ corpus-commit 空白行进 droppedExcerpts + warn（退出码哨兵真实生效）。**新增登记（R0912-3 修复批代理上抛）**：learn/index.ts:107 直吃 book 层 repeat_threshold 原值 >1 不夹紧（advisory 收割非机检闸门，与 runner.ts 夹紧修复家族漂移）。【登记·待择收】
- **rag openRagDb PRAGMA 顺序与 events 侧 N3 修复不同步——并发首开可立即 SQLITE_BUSY（全库重评-0914 P2-6·主审亲验）**：`rag/store.ts:219-220` `journal_mode=WAL` 先于 `busy_timeout=5000` 且 WAL 切换无重试；`events/store.ts:666-690`（N3）已修同款（busy_timeout 前置 + 8 次退避，注释记该场景真实发生过）。触发 = 双进程并发首开同一 delete 模式 RAG 库。修法 = 对齐 events 侧（前置 + 退避重试 / 引同款单源）。**处置（全库重评-0914 修复批 2026-09-14）**：openRagDb 对齐 events 侧 N3——`PRAGMA busy_timeout = 5000` 前置，WAL 切换并入 8 次线性退避重试（Atomics.wait 微睡；已切 wal 短路；isRagDbCorruptionError 立即上抛、绝不误判损坏送删库链，R73-48 fail-closed 出口）。回归 = 既有 rag 套件（同族 N3 形态）+ L1/L2 全量兜底（并发首开时序无可控注入面，如实记档）。【已处置·全库重评-0914 修复批】
- **rosterNamesCache 无 forget 挂点（全库重评-0914 P3-31·登记）**：`check/count.ts:340-341` 名册正则缓存（32 条上限有界、键含书根无串味）未挂 `forgetBookKeyedCaches`——纯内存卫生项，与 R0912 系「缓存挂 forget」纪律的漏网点同族。【登记·全库重评-0914】
- **RAG 重建互斥完全依赖外部任务闸，本域内无跨进程互斥（全库重评-0914 P3-33·头注显式化已落地）**：`rag/index.ts` buildIndex/resetRagIndex 无自有跨进程锁（单实例由服务端任务闸兜住）——报告建议头注登记依赖，**本批已落地**（rag/index.ts 头注增 P3-33 依赖登记锚，防后续调用面误以为域内自洽）。【登记·维持·全库重评-0914】

### G. 测试工程 / CI / 工具链——出处：独立复审 §十 / 重评-0909 §九 / 重评-0910 §9.4 / 重评-0911c §4.2 / 重评-0912 §四 G2 节 / 重评-0912-2 §3.3·§3.4 / 重评-0912-3 §3.1·§3.2 / 重评-0912-4 §五 / 复审-0914-优化 §三-A4

- **测试批次号化石层渐进改名（重评-0916-五轮 P2-4 余量）**：~540+/1181 文件轮次号命名（r09xx / 重评-N 前缀，语义可导航性受损）；纪律已入 CLAUDE.md 测试分层节（新测试行为命名、批次号留文件头注释与源码锚注可考）；存量随域测试触达渐进改名，不做专项批（避免无语义收益的搅动面）。【登记·渐进收敛·2026-09-16 五轮处置批】

- G 域重证（重评-0912-4 §五·主审指标面实测）：sleep 脆弱面 `await sleep(` 实测 **227 处** vs waitFor 159（上行既有行数字更新，家族维持）/ >1000 行测试巨件 **4 个**（main.test.ts 2731 · adapter.test.ts 1475 · server-manager.test.ts 1431 · chat.test.ts 1011——上行既有行数字更新）/ 断言文化三维实测：**0 快照断言 / 0 真实 `.only`（8 处命中全为 check-counts 守卫自测字符串夹具）/ expect 21,837 ÷ 7,063 用例 ≈ 3.09 断言每用例** / e2e 29 spec 顺序契约双守卫在位——均既有登记族重证，无新增单立项。〔处置注·重评-0914-三轮（2026-09-14）：sleep 脆弱面「悬持窗族」收口一批——r0911-srv-write-bookmoved ×7 / r0913-srv-prefs-bookmoved / r1010b-srv-documents-bookmoved 经 fake-reqres `waitForBodyArmed` 就绪探针轮询化、r0910-w SSE 连接登记改 waitFor 探针（P3-12）；r55:129 经判定非悬持窗族（断言后收口等待，实质断言在上方）维持；mtime 垫片族/负向观察窗维持原判。巨件 4 个维持（重评-0914-三轮 P3-13：重设计体量，mock 工厂拆 fixtures 非一线修）。〕〔处置注·结构大件波1批（2026-09-16）：巨件 4 → 剩 2——main.test.ts 2800 行拆 1 残核 + 5 域件 + 2 共享件〔fixtures 511 行 + ④批 P2 监听器治理 harness 抽共享〕、adapter.test.ts 1475 行拆 1 残核 + 5 域件 + 1 fixtures，用例逐位零变化（93=93 / 70=70）、纯结构搬移；server-manager 1430 / chat 1011 随波2〕〕〔处置注·结构大件波2批（2026-09-16）：巨件 4 → **0**——server-manager.test.ts 1430 行拆 1 残核 + 5 域件 + server-manager-fixtures（62=62 用例逐位、DI 假 fork 无 vi.mock 提升面）+ chat.test.ts 1011 行拆 1 残核 + 5 域件 + chat-fixtures（28=28 用例逐位、与既有 12 个 chat-*.test.ts 零重名）；>1000 行测试巨件清零，行销案〕

- **测试装置裸 startServer + fetch 端口抖动致全量假红（重评-deepseek-v4.1-flash P2-1·主审本机独立复现）**：`test/studio/error-envelope.test.ts:106-109` 用裸 `startServer({ port: 0, workDir })` 后紧接 `fetch(api/boot)`，而同文件 `:66` 已用 `startServerSafe`；全库同类 13 文件 16 处（纯裸 6：boot-token / r27-batch-c / r36-24-book-prefs-revision / server-reentrant / source-hash-race / startup-notices；混用 7：api-error-branches / api-integration / chat-branches / cov-server-config-draft-branches / cov-server-io-gate / error-envelope / stream-ticket）。根因 = `port 0` 由 OS 从本机动态段分配，命中 fetch（undici/Chromium 同表）受限端口黑名单即整 describe 抛 `bad port`；`test/helpers/safe-port.ts:21-27` 已内建黑名单 + `startServerSafe` 重绑（122 文件采用），此 13 文件属迁移遗漏。本机 `netsh int ipv4 show dynamicport tcp` 实测动态段 = 1024 + 13,977（Windows 默认 49152-65535），相交概率 17/13,977 ≈ 0.12%/处 → 单次全量至少一红期望 ≈1.9%；**CI 腿（默认动态段）不复现**，故表现为「本地偶发假红」。本轮主审全量实跑唯一失败即此文件（1079 过 / 8 跳 / 1 败），子代理独立复跑 8/8 通过。【已处置·R0912-ds41：14 文件 18 处绑定点全迁 `await startServerSafe(...)`（helper 已代等 listening，两行式合并一行；y2 的 rebinding listen 重绑形态一并收编防绕过黑名单复检；server-timeout 两 it 同步改 async；api-integration:38 构造即抛不绑端口为例外留裸+注）；合并 await 13 处 + helper 内吸收 3 处；15 文件 vitest 136 用例全绿】
- G 域 P3 登记（重评-deepseek-v4.1-flash）：测试侧本地 `sleep` 定义 28 份 / `await sleep(` 84 处 / 12 份本地 `waitFor` 未收敛 `helpers/wait-for.ts`（既有登记的 sleep 族重证，本批补量化本地 helper 份数）/ 5 个 >800 行测试文件（main.test.ts 2661 等）/ 前端 CSS 重复规则体 76 组 Σ(n−1)=388 份 + `.spin` 14 处三档时长不统一 + `settings-shared.css`（797 行全局装载）短名越界已致 2 组件改名规避（`.audit-seg`/`.sb-seg`）/ 89 处冗余导出（前端 63 处）去 `export`。【已处置·R0912-ds41：sleep 28 份定义清零收编 wait-for.ts 单源（84 调用点零漂移；waitFor 既有单源确认）；CSS 收 18 组同名同体规则 + `.spin` 众数档归一（同名异体与竞态面如实不收，净 −55 行）；settings-shared.css 39 条规则加 `.settings-content` 前缀收窄（装载序竞态面保守未动记因）；冗余导出全 src 树摘除 218 处/122 文件；>800 行测试文件维持登记待同事测试批】

- **check-packaging「真实仓库脚本直跑」用例非密闭（重评-0911c P2·主审 L2 实测复现）**：`test/desktop/check-packaging.test.ts:46-49` 直跑真实脚本，工作树 dist 半新态（main.js 在而 fontlist 缺）时 exit 1 → 全量单测单点红（2026-09-11 本机实测复现，`npm run build` 后复绿）；CI 免疫（无 dist 场景门跳过 / mac 腿 build:all 先行），纯本地污染。修法：用例容忍「问题清单恰为 dist-fontlist 一条」或脚本 dist 实存门改可注入。**处置（R0912 主审）**：`CLW_CHECK_PACKAGING_SKIP_DIST_GATE=1` 环境注入口 + 用例置变量只锁配置/资源对账面（dist 门语义由 problemsForDistFontList 直测锚定）；CI 不设变量门照常。【已处置·R0912】
- check:packaging 门文案混叠（陈旧 dist 被报成「tsup onSuccess 拷贝失效回潮」，`check-packaging.mjs:169`）——fail-closed 方向正确但环境陈旧与配置回潮不可区分，误导排障；CI 接线两处均安全无需改门序。修法：文案区分两态。【已处置·R0912 主审：陈旧 dist 与拷贝失效两态并列文案，fail-closed 方向不变】

- **desktop.yml win 腿缺重跑兜底（重评-0912 G2-P3-5）**：`desktop.yml:63-64` win 腿裸 `npm test` 无 tinypool 收尾竞态重跑兜底，与 `ci.yml:109-123` 专用步**不对称**——同族竞态可让 tag 出包门假红且无重试。修复 = 复制同款 pwsh 重跑块（或上游修复后两处同撤，随 §三 G 上游评估行联动）。【已随批·2026-09-12 修复批 R0912-G2-P3-5：win 腿拆专用步照搬 ci.yml R0911-G-P1-1d 结构（非 win 维持原样），js-yaml 解析实证】
- **测试造书脚手架参数化（重评-0912 G2-P2-2，测试侧精简最大单块）**：makeBook 族本地复制实测 78 文件 + makeDriver 20 文件 + tempUserData 6 文件——test/helpers 参数化 builder，估 800-1500 + ~300 行。【部分销账·测试精简批 2026-09-12：scaffoldBook 落地收编 12 文件；studio/ai 域 makeBook 随 bootStudio 启动收编内联消解；makeDriver→`test/ai/fake-driver.ts` 单源 29 文件引用；其余域 ~89 份本地 makeBook **维持本地**（2–4 行/件低收益 + 清理语义耦合），净省远低于原毛估——余量降格【维持】不再单立；明细 = `02-执行/测试精简优化-执行-2026-09-12.md` §四/§六】
- 重评-0912 G2 域其余登记：check-counts 全量 vitest list 七腿重复执行（mac/linux 二选一省分钟级）/ 真睡眠负窗断言 22 处逐步 waitFor 化 / main.test.ts god-file（既有行）。死测试零残留（7 个已删产品符号 test/ 零 import）+ env/chdir 还原卫生 + 门禁接线真实性经核验全部通过。【登记】

- **CI 红四族（2026-09-11 新登记行 → 同日修复批全数处置，待六腿 CI 复验）**：原 run 34503257752 四族（R71-8 大小写 FS 假设 / kk-P2-8 ubuntu 差异 / r43 慢机 TTL 时序 / win tinypool teardown 竞态）已全处置——① R71-8 用例改运行时 FS 大小写探测分支断言（敏感卷真改名/不敏感原位，两分支各锚产品实际行为）；② kk-P2-8 定性收束 = `M.msgBox` 文件级累积面被敏感卷上前序切库警告污染（got 3 = 2 残留 + 1 本用例，精确对账）→ 改快照增量口径；③ TTL 族 7 处真睡眠改 `toFake:['Date']` 注入时钟（相关面 -93% 耗时，r43 慢机压力源同批移除）；④ win 腿测试步改「首跑非零同命令重跑一次」兜底（真失败不吞）。残留：六腿 CI 复验（修复合入 main 前下一轮 workflow_dispatch 观察）+ win 通过数 CI 腿确认（2026-09-11 重评修复批主审本地实测 1051 文件 / 6743 过 + 80 跳 0 败，README 已按实测修账）。【已处置·待 CI 复验】
- vitest/tinypool forks 收尾竞态上游根治评估（win 重跑兜底的撤除条件）：查 vitest >3.2.7 / tinypool >1.1.1 changelog 是否含 teardown 竞态修复（关键词 ERR_IPC_CHANNEL_CLOSED / unhandledRejection during teardown）；有则升级 + L2 回归后撤兜底。备选 = tinypool patch（`_removeWorker(...)?.catch?.(() => {})` 两处，patch-package 面，仅收尾期吞 reject 不掩盖测试成败）——兜底被判定不可接受时提案。【单立·评估】
- CI dev push 腿空窗（恢复与否）〔核对 2026-09-11：ci.yml `on:` 仅 main push / main PR / workflow_dispatch〕。【待拍板】
- Electron GUI 交互 e2e 缺位（release-smoke 已在主 CI e2e job 日常跑；残余 = Playwright `_electron` 交互 e2e 投入）〔核对 2026-09-11：test/e2e 零 `_electron` 引用〕。【待拍板】
- win32 74 处 skipIf 用例三腿皆不实跑（win 腿补口方案）〔核对 2026-09-11：`skipIf(process.platform === 'win32')` 实测恰 74 处〕。【待拍板】
- corpus 语料 7/14 checkIds（余 7 受门 fixture 硬约束：铁律阈值段/短篇 kind/布线书）〔核对 2026-09-11：test/corpus/checks/ 7 json 在位〕。【待拍板】
- e2e 长尾 4 旅程（导入旧书 / 备份恢复 / 导出成品 UI / 升级向导）。【单立】【已处置·中件组批（2026-09-15）四项逐一对码：①导出成品 UI——已被 export-ai-settings.spec.ts 覆盖（P2-TST-7：弹窗→格式/平台→导出→toast→关），销；②升级向导——现役产品形态 = 启动通告横幅 StartupNoticeBanner（迁移/自愈通告 A4 批 0 落横幅），新增 startup-notices.spec.ts 2 用例（幽灵登记→boot repairBooks 自愈通告横幅可见 repair-books+缺失计数；「知道了」dismiss 一次性语义 localStorage 指纹刷新不弹；独立 server 端口偏移 5、spec-order 快照随批插行 31→32 spec/51→53 用例）；③导入旧书——无产品面（无端点/IPC/组件：books.post 系新建非导入，desktop 无 showOpenDialog 导入链），0911 时点愿景项记正不立项；④备份恢复——同无产品面（仅 state.ts 诊断 fix 提示文案提及「从备份恢复」），记正不立项】
- e2e 共享 workDir 顺序契约（R9-P2-3 架构级；缓解已立：全新 workDir / 顺序快照门 / workers:1）〔核对 2026-09-11：playwright.config.ts workers:1 在位〕。【维持】
- mock-cc 分叉。【维持】
- 临时目录清理双轨（单立机械批；2026-09-12 精简批实测更新：裸 mkdtempSync 506 处/341 文件 vs 托管 mkdtempTracked+trackTempDir 590 处/246 文件——旧记 328 处为当时口径，双轨并存且裸形态仍在增长）。【**已处置·临时目录收敛批（2026-09-15，作者指令「可以」拍板开工顺序后首批）**：AST 普查（TypeScript compiler API，临时分析件不入库）实测本批开工时裸形态 460 处/319 文件〔较 09-12 缓降：四轮处置波 1 顺手收编等〕，按 enclosing 生命周期分类 = 可换装 293（it 113 + beforeEach 114 + 文件内纯测试寿命助手 66〔调用图判定：全部调用点均落 it/beforeEach〕）/ 保留 167（beforeAll 121 + module 顶层 17 + e2e 8〔Playwright 无 vitest 钩子〕+ 混合寿命助手 ~21 + 无名/跨文件不可判 8——登记会被 afterEach 首用例后误删共享目录，按 temp-dir.ts 头注设计约束豁免）。codemod（AST 引导文本手术，非整文件重打印零格式噪声）换装 293 处/194 文件 = 站点标识符替换 + temp-dir import 按既有惯例插入（node:/vitest 内建 import 之后）+ 裸形态清零文件从 node:fs 具名 import 摘除 mkdtempSync〔坑一处如实记档：vi.mock('node:fs') 文件常有双 import〔mock 前真件 + mock 后取 mock 件〕，摘除须锚定含 mkdtempSync 元素的那个、取最后一个即漏〕。src 产品侧 0 站点不受涉；测试计数零变化；vi.mock 透传语义核对（importOriginal 展开式局部覆写，mkdtempSync/rmSync 透传真件）不涉行为。temp-dir.ts 头注双体系数字随批更新；新增测试默认走 mkdtempTracked 纪律不变】
- 批次号命名·注释编号考古税（文化项）。【维持】
- 双包宇宙版本偏斜（workspace 化大重构；sharedRuntimeVersionDrift 对账门已立作缓解）。【单立】
- check-counts skip 门禁正则枚举（AST 化）。【单立】
- 测试工程 P3-②③⑤⑥（重评-0909 报告细目）。【维持】
- test/desktop/main.test.ts 2583 行单文件可拆（重评-0911b G-P3-1）——随 H 档三结构性批。【维持→H 档三】
- **测试 sleep 竞态窗系统性脆弱面（重评-0912-2；子代理报 P2 主审复核改判 P3）**：全 test/ 真实 `await sleep(` 84 处（锁/TTL/缓存/停机语义域），窗口失守以假红（慢环境闪红）为主；子代理假绿示例 r1010b:236-240 经主审证伪（:241 即有 `expect(seen)` 断言），如实记档报告 §3.3。择收 = 轮询超时路径补终态断言 + 悬挂窗换可控 Promise 门（r0912-interrupt-semantics release 手法）。【已择收·中件组批（2026-09-15）】74 处存量全量分诊（grep -B1 -A1 逐站点）：①轮询化 4 站点（终态必然到达的正向等待改 vi.waitFor 轮询，慢机不假红）= kill-escalation SIGKILL 升级到达 1 + r27-summary-lock-renew 摘要锁 mtime 续期 ×2 + cross-process-lock-renew 锁 mtime 续期 1；②维持 ~70 = mtime 跨毫秒垫片族 ~24（写前 sleep(5-20) 使指纹必然失配，三轮处置注原判维持）/ 负向观察窗 ~15（断言「未发生」不可约，需观测窗；server-manager 封顶门 5 + r0910-w 压制窗 + r51-g2 迟到 destroy 窗 + chat-steer 续链窗 + kill-escalation cancel 撤销窗等）/ 锁停走编排 ~10（re2-manifest 重入窗等并发序编排，sleep 即语义）/ TTL 实钟语义（r36-12 越窗 80ms——TTL 面已注入钟化占多，余为钟不可达处）/ 微任务排水 sleep(0/1)（与 setImmediate 等价）；悬挂窗族已于三轮 P3-12 收口（waitForBodyArmed 轮询化）。〔族记正·结构大件波1批（2026-09-16）〕mtime 垫片族细分两型：写前 sleep 使指纹必然失配型维持原判；**同毫秒双写/写读撞车型**（无垫片、依赖「写入时刻不同」自然 bump mtime，快盘热缓存下写入延迟降进时钟粒度内即确定性假红）七站确定性加固 = pm11 memo / pricing R42-2 / kk-P2-15 探测梯 ×3 / providers-api 切换 / providers-revision-models ×2 / r35 目录探针——修法统一为显式 utimesSync 前推 60s（测试侧与写入时刻解耦，生产陈旧窗既有口径不动）；族余量维持登记
- **soak 面窄于「200 万字不崩」卖点（重评-0912-2；改判 P3）**：soak.ts 仅 piece-list 往返 + RAG 召回两段热路径；文档清单 jsonl 重写放大 / 事件库长会话增长 / service 索引累积无内存断言。与 desktop.yml 打包态冒烟接线（重评-0912-2 §3.4 TOOL 域：electron-smoke CLW_SMOKE_APP_BIN 通道建成未接线，tag 门窗口循环链缺位）同批单立。【已处置·中件组批（2026-09-15）补段三：soak.ts 2→5 段——段 3 文档清单 jsonl 重写放大（200 条 × 3000 次读改写全链，0.07MB）/ 段 4 事件库长会话增长（2000 批 × 10 事件持续追加单会话，lastSeq 终态钉账，0.00MB）/ 段 5 service save 链（DocumentService.createDocument+save 真链 600 次：journal pending+留底 dedupe+清单 RMW+字数 delta+revision 链，内容逐变不短路，0.30MB）——同段纪律（固定输入无定时器、功能性底座防空转、失败路径清场 FAIL）；ci.yml OK 行数断言 2→5 同步（行 262 fail-closed 口径）；desktop.yml 冒烟接线部分已随三轮 P3-5 win 腿收口，mac 腿 CI 在役，余量无】
- G 域 P3 登记（重评-0912-2 §3.4）：前后端 API 契约双侧各自钉无共享单源（兜底 e2e）/ main.test.ts 2661 行（既有行数字更新）/ ~118 个 server-boot 测试手写启动样板（withStudioServer 组合 helper ~2.5–3.5k 行——与 makeBook 参数化单立行同族并批；**已大部处置·测试精简批 2026-09-12**：bootStudio〔`test/helpers/studio-server.ts`〕收编 82 文件 + 47 件 triage 维持〔多服多书/迁移序耦合/特型选项/被测面本身，四类逐一记因 = 执行文档 §4.2〕，测试侧净 −1386 行、用例零漂移、coverage 逐位持平——毛估修账：原估未扣 helper +184 行成本与不可转换面）/ 零直测 src 模块 7 个（均经父模块间接覆盖）/ README「26 LTS」与 ci.yml「26=Current」口径漂移 / calibrate-tokens 无 npm 别名 / desktop.yml mac 腿 playwright 无缓存 / electron-smoke 冗余 statSync。【登记】
- **ci.yml soak 门段 2 失败假绿洞（重评-0912-3 P2-4·主审对码实锤；与上行既有「soak 面窄」〔单立·可开工〕行不同面）**：`ci.yml:249-252` 两层独立掩蔽——① 步未设 `shell: bash`，GitHub Actions 默认 `bash -e` 无 pipefail，`npm run soak | tee` 中 soak 退出 1 被 tee 吞，soak 自身退出码语义失效；② `grep -q '\[soak\] OK'` 语义是「至少一个 OK」——段 1（soak.ts:127）通过即打印 OK 行，其后 RAG 段失败（:160/169/189 exit 1）仍步绿，步注释自称「SKIP/FAIL 均判红」实际仅段 1 成立；RAG 段恰是防线性泄漏断言面，段数增长（既有 soak 补段单立项）洞面随之扩大。修法 = `shell: bash` + `set -o pipefail`（或重定向直捕退出码）+ OK 行数 = 段数断言（`grep -c`）。【已处置·R0912-3 修复批（主审亲修）：shell: bash（GH Actions 显式 bash = -eo pipefail，teed soak 非零退出不再被吞）+ OK 行数恰=段数断言 grep -c（现 2 段；补段须同步改断言，fail-closed）；js-yaml 解析实证 + 语义抽取核对】
- G 域登记（重评-0912-3 §3.2）：desktop.yml mac 腿三连重构建（test:e2e + test:e2e:release + build:desktop 各自全量构建产物互弃，纯时长 1–2 次全量构建；ci.yml e2e job 同理重复一次）/ web-next tsconfig include `env.d.ts` 死配置（文件不存在，types: vite/client 实供）/ check-counts skipEach 正则对含 `)` 的 each 参数数组盲区（R31-35 同族更窄，漏检向）/ verify-responses-relay vs calibrate-tokens argValue 参数解析口径不一（后者不拒 flag 名误吞）/ tool-choice.ts 决策单源（4×4 组合空间）无直接单测钉（仅适配器发射断言间接守护）/ 临时目录清理双轨 ~97 文件裸 mkdtempSync 失败漏收 / 超长测试文件 3 个（main.test.ts 2661 **归并既有行**数字更新；adapter.test 1475·server-manager.test 1322 新增）/ fakeReqRes 3 份近同构未收编（waitFor 本地 3 份登记维持）/ service.test.ts:431 上限式时间窗断言慢机假红向 / fullscreen.ts 38 行零测试引用（仅 e2e 间接）/ desktop.yml playwright 无缓存（**归并既有行**——上行已登记）/ win 腿重跑洗白窗·soak 面窄（**既有登记重证**）。测试面镜像度/断言文化/平台门口径三维审计结论 = 报告 §3.3 C4 行（0 快照断言、2.9 断言/用例、meta-守卫四处齐备）。【处置·R0912-3：修 6 件——desktop.yml mac 腿 playwright 缓存（主审亲修：macOS 缓存位 ~/Library/Caches/ms-playwright、键控同 ci.yml）/ web-next tsconfig env.d.ts 死配置删（实文件在 src/ 已被 include 覆盖）/ check-counts skipEach 正则改一层平衡括号近似（全仓 1125 测试文件新旧 A/B 对扫 0 差异 + 直跑绿）/ tool-choice 决策单源 32 格（4 mode×4 intent×name 有无）直接单测钉 / service.test.ts:431 上限式时间窗改可控闸门终态断言（GatedSaveQueue 注入，非永真）/ argValue 口径——verify-responses-relay 经代理读码**证伪**报告定性（已是严格侧），钉注释+回归测试防回退，真缺陷侧 calibrate-tokens.ts:23-26 宽松 argValue 主审判式修正对齐；维持——desktop.yml 三连重构建（构建缓存单立）/ 临时目录清理双轨 ~97 文件 / 超长测试文件 3 个 / fakeReqRes 3 份收编〔**已销账·测试精简批 2026-09-12**：`test/helpers/fake-reqres.ts` 单源 + 3 文件换装 19/19 绿〕 / fullscreen.ts 零测试（成本>收益）/ win 腿重跑洗白窗·soak 面窄（既有登记）】

- **测试常量三件套样板全仓统一（复审-0914-优化 A4，2026-09-14 优化修复批登记）**：`export const X` + `let x = X` + `__setXForTest()` 三件套全库 46 处定义 + 22 处调用（grep 实测）；修复批已落 `shared/testable.ts` `testableConst<T>(def)` 工厂单源并起步收编 8 文件（P1-1/B4 等新符号即用工厂形态），**存量全仓统一换装待作者拍板**（R30-18 约定级；起步口径已在报告收口补记如实记档）。【登记·待拍板】〔处置注·重评-0914-三轮 P3-14（2026-09-14）：`__xxxForTest` 导出面已落数量守卫 test/check/r0914c-fortest-hooks-guard.test.ts——src 全树 *.ts/*.vue 递归实测在锚 71、超限即红须有意抬锚留因（单向守卫：只拦增长不拦收编），防测试后门无意识增殖；与拍板行联动，全量 testableConst 换装仍待作者拍板。〕

### H. 全仓精简总账——出处：重评-0911b §四 / 重评-0911c §五 / 专项精简优化-评审-GLM-5.3-2026-09-12 §五 / 重评-0912 §五 / 重评-0912-2 §六（重评-0911b/0911c/专项精简优化 正本均已归档 `Archive/`）

- **重评-0911b 三档总账（2.6k–4.9k 行，≈107k 的 2.5–4.5%）**：档一低风险机械批 ~800–1000 行（F 域 test-only 死导出 ~270〔enableRag 族/recall 包装/unlinkWithRetry/readAllChunks/formatStyleReport 族，均 grep 核实零生产调用〕/ E snapshot.ts 别名层整删 ~111〔头注 O-12 退役登记在案〕/ E 测试专用导出退役 ~205 / A·B 小件 ~100）；档二需拍板 ~1.5k–2.6k（D catalog.ts+catalog.gen.ts ~454 全仓零消费·最大单块 / A 同步孪生 ~240·属有意等价对照口径 / B 内联校验迁 defineRoute ~200–400 / C2 Settings 开关样板抽 SettingToggle ~250–400 / C1 prefs setter 表驱动 ~150–200 / E yaml 补丁族+service 锁样板 ~160–250）；档三结构性（13 件 >800 行拆分、ai↔studio 解环、documents.ts runX 样板收敛、main.test.ts 拆）。不建议动清单（foreshadow 孪生/RagProviderEditor 合并/R 编号审计注释等）见报告 §四末。【档三已大部处置·结构大件波1批（2026-09-16）＝作者指令「继续，全部做完！」战斗序⑤：①documents.ts runX 收敛落库（五站闭包+两处守卫四连单源化 runBookScopedOp/structureBusyGuarded，信封逐字节不变，净 +63 行系锚注与 helper 成本、样板 −51，价值在单源防漂移；plan 干跑异序与 acquireTaskGate 调用点审计计数面原样未动）②main.test.ts 拆落库 ③ai↔studio 解环侦察定谳 = 静态环已为零（ai→studio 0 边，末条反向边已由 R0912 task-gate-port 端口化；studio→ai 64 边全部组合根），随批落守门固化件 test/governance/ai-studio-direction.test.ts（方向锁模块级全量 + 组合根形状锁 + 白名单防僵尸），后续类型胶水批（events⇢ai ContentBlock/ChatMsg 抽中性层）与 SCC-A 值环整编、组合根纯化登记挂账；13 件产品巨件拆分随 ⑤④ 后续波次】
- **重评-0911c 五档总账**：产品侧结构性冗余 **≈0.9k–1.2k 行（约 src 的 1%）**——一档零风险死码 330–380（useDebouncedSource 连测试 166 / events 旧载荷接口 ~100 / 死类型散点 19 处）+ 二档低风险合并 550–860（**前端三组双实现为最大点对**：ChatDock↔ChatPanel composer 滑窗 108 窗 / ShelfHeroCard↔ShelfModalHero / Shelf↔ShelfModal；driver cc↔mock 总线 / Book.vue 切书取消分支三份 / 仿树行样式三份）；三档 oversized 29 个纯移动拆分（净减 0）；四档注释冻结剪枝理论 src 2.4k–4k + test 2k–3k（需作者拍板、按冻结纪律剪出归档非直删；承载现行语义段不可剪）；五档测试侧 2.8k–5k（req 帮手 46 份归一 / 书架脚手架 344 文件抽工厂 / 大段复制点对）；观察项 = 六族启动迁移+token-calibration 退役 1.4k–1.6k（需拍板）。依赖面零闲置。与重评-0911b 档二差异点：catalog.gen 454 行块重评-0911c 未复核（存疑待域评交叉）。【待拍板】【四档已拍板·2026-09-16「不想挂账」全域执行】【处置注·⑤⑤注释冻结剪枝批（2026-09-16）：四档注释冻结剪枝收口——src 侧五路（desktop 试点三路 + export·cache/install 扩展两路，R0916-5k）14 文件 + F 路 test·state 1 文件，六路净删 120 行 = 注释 118 + 随块空行收拢 2，剪出归档非直删（Archive 注释冻结件六份，逐块原行号 + 判剪理由 + 复原锚 480252fe），git diff 机检纯注释删除零代码行；**实测定谳 = 考古密度远低于毛估**（本行四档估 src 2.4k–4k / 重评-0912-2 修账 1.3–1.5k〔desktop+export/cache/install〕，src 实收 117 注释行低一个数量级——锚注骨架占绝对主体）；**test 侧 2k–3k 判定性不立项**：全域只读普查四路（S1 studio 474 文件 8338 注释行 0 / S2 ai·process·rag·knowledge·learn 244 文件 0 / S3 desktop·driver·e2e·governance·scripts·soak 115 文件 0 / S4 其余十五子树 ~240 文件 1）实测合计 1 行差三个数量级，唯一墓碑行（test/state/resume.test.ts:5）经作者「全部修复」处置随批剪除——非挂账，翻案须新证据；AI 域 1.5–2k 按重评-0912-2 在册判定维持不动（评审结论非挂账）；主审全批评审复核 = 纯度/完整性/出处/账目四道机检全过 + 边界判剪六处单源实证在位（初判两处「无注单源」系假阳性记正）+ grep -x 单行 argv 怪象记档冻结件备查段〕
- **重评-0912（第十篇）剩余精简空间（在其上批 −882 行之后的余量，2026-09-12 实测登记）**：产品侧 **700–830 行**可直接回收（C2 组件模板/CSS 重复面 ~490-570 最大单块 / B bookMovedFailure 归一 / C1 api 样板与死字段 / A 三件 / D tool_choice 归一 / E·F·G1 杂项）+ 测试侧 **1100–1900 行**（makeBook 78 文件参数化 + makeDriver/tempUserData 单源 + 负窗轮询化）；另登记性 ~610 行「有决策记录的刻意保留」（catalog 三件套随 A7 拍板〔已拍板删·2026-09-15 拍板快断批，~454 行销账〕/ writeChains 接管面 / E 同步孪生五族随测试面迁移批）不构成欠账。明细 = 重评-0912 §五，各域登记行见 §三 A–G。【产品侧已随 2026-09-12 修复批大部回收（C2 波 2 −284 / apiJson 55 处样板 −82 / B·A·D·E·F·G1 杂项，收口记 = 重评-0912 §十一）；测试侧 1100–1900 已随同日测试精简批部分回收（净 −1386：bootStudio 82 文件 + makeDriver 29 单源 + fakeReqRes 3 + scaffoldBook 12，用例零漂移、coverage 逐位持平，明细 = `02-执行/测试精简优化-执行-2026-09-12.md` §四/§六）；剩余 = 负窗轮询化（随 §三 sleep 脆弱面行）+ 登记性 ~610】【测试侧大部已处置·余量维持登记】
- **重评-0912-2（第十一篇）精简总账（2026-09-12 实测登记）**：产品侧 **≈3.6–4.4k 行（≈3–4%）**——SRV handler 三元组样板 + TTL 缓存壳泛型 ≈0.6–0.8k / 进程域同步孪生 + mock·cc 总线 + test-only 导出 ≈0.5k / 机检守卫族抽共享 ≈0.17k / RAG 收敛 ≈0.07k / 文档域死码 ≈0.1k / AI 域 calls.ts 死入口等 ≈0.15–0.25k + **注释考古冻结 ≈1.3–1.5k（desktop ~1.0k + export/cache/install ~0.33k——应按冻结纪律移 Archive 冻结件非删；AI 域 1.5–2k 理论值不建议动）**；测试侧 **≈3.0–4.5k**（withStudioServer 组合 helper 收编 ~118 文件启动样板 ≈2.5–3.5k 最大头 + r-批次按域归并 ≈0.5–1.0k）。依赖面零闲置；明细 = 重评-0912-2 §六。【最大头已处置·测试精简批 2026-09-12：bootStudio 收编 82 文件、测试侧净 −1386（对 ≈2.5–3.5k 毛估修账——47 件 triage 维持不可转换 + helper 自身 +184 行成本，明细 = 执行文档 §四）；r-批次按域归并 ≈0.5–1.0k 维持【待拍板】（测试组织形态重构非样板面，本批红线外）】

- **2026-09-11/12 专项精简批已执行（原档一真死码 + 档二风险可控三件 + 新发现死件）**：产品侧净 **−881 行**——snapshot.ts 别名层整删〔O-12 收尾，−94〕/ enableRag·unlinkWithRetry·formatStyleReport·leadUpdates 死导出族〔配套测试直写复刻或同删〕/ 前端死封装·死状态·死 composable〔api/analysis 双函数、useDebouncedSource、provider ensureModels 链、issuesWarning、aiDriver、pendingCount 等〕/ **SettingItem+SettingToggle 55 块抽取**〔−340 净〕/ **prefs 33 setter 表驱动**〔21 表 + 12 异形手写，+20 用例钉界锚测试〕/ yieldToEventLoop 五拷贝单源化 src/async.ts / sigStatFor·chapterInput·knowledge guard 同体收敛。L2 九件套全绿（1050 文件 = 6739 过 + 80 跳）；明细 = 报告 §三，源锚 r0911-。【已办结·2026-09-12】
- **旧档一校正（证据 = 报告 §四，勿再翻案）**：readAllChunks〔14 测试文件，毒行/早停测试原语〕/ recall 包装〔30 处断言，R50-B-2 随批删口径〕/ appendEvent·writeAnalysis·latestSession〔44/40/6 处测试消费〕/ 同步孪生四件〔等价 oracle + 跨进程锁测试基材〕/ writePieceList〔结构操作备案〕——**维持不删**；recordAiVersion 同步版「→H 档一候选」**销账**（45+ 测试 fixture 种子面）；forget 循环不办（收益 < 锚注可读性）；strictShort 旧候选失效（生产活码）。
- **【待拍板】**：~~D catalog 三件套 ~454~~〔D 已拍板处置·2026-09-15 拍板快断批：作者指令「按建议顺序开工」取主审建议「删」档——catalog.ts / catalog.gen.ts / scripts/generate-model-catalog.ts / catalog-sync.test.ts（6 用例）四件 git rm + index.ts `MODEL_CATALOG`/`MODEL_CATALOG_VERSION` 导出与 package.json `generate:catalog` 脚本摘除，全库零残留引用（Z-P2-4 销账、git 历史可考）；model-quirks.ts 纯数据约束注记随批改写〕/ SRV deleteAiVersions 补端点或删（35+87）/ AEC resolveBookRoot CLI 遗产去留（68+150）。
- **【单立机械批·2026-09-15 机械批一组处置】**：书键 TTL 结果缓存族 12 处同构壳 ~110〔**已销账**——实际随复审-0914-优化修复批 D1 `createTtlProbeCache` 收敛 17 壳（R47-18 过期逐出统一于选项参数），本行系台账滞留，行见上方 R0912-ds41 P3-2 销账记〕/ resolveBook 双行样板 ×72 ~65〔**已处置**——`book-context.ts` 新单源 `resolveBookOrReply`（失败即回写 replyError 信封逐字节同源、返回 null）收编全部 72 处路由入口样板：主审亲扫 62〔documents 16 + G 组扫掠 29 + style 包装 1 + books 手工 2 + 其余〕+ D 路子代理 10〔chat 4 / stream 4 / onboard 2〕；三处 `r.entry` 消费点（books ×2 / overview）经 helper 返回成功臂保持；snapshots `resolveDoc` 与 prefs `prefsPath` 系自定义联合包装非该样板，维持〕/ defineRoute parse 迁移 44 处〔**逐点评估处置**——迁移 12 / 跳过 8 / 维持 24，明细见上行 defineRoute 86 行〕/ 临时目录双轨收敛〔**已处置·临时目录收敛批（2026-09-15）独立批落地**——与上注同行：293 处/194 文件换装 mkdtempTracked，保留 167 处系 beforeAll/module/e2e/混合寿命豁免形态，明细见行 253 处置记〕。
- **【低优先登记】**：yaml 补丁族 helper −30~35 / service 锁样板 −30 / ~~documents.ts runX −30~40~~〔已处置·结构大件波1批（2026-09-16）R0916-5a：五站闭包+守卫四连单源化，样板 −51 行、净 +63 系 helper 与锚注成本——原估行数口径修正，价值在单源防漂移非行数〕 / win-fonts·font-cache 收敛 −30~40 / lensToRole 5+10（先核语义）/ C2 CSS 重复族（composer −34·刻意契约、.panel×8 −63、.btn×13 −45）/ isPlaceholder·avg 微收敛——报告 §五。
- **档三结构项**（13 件 >800 行拆分、ai↔studio 解环、main.test.ts 拆）：维持重评-0911b 建议不变，随重构批。〔处置注·结构大件波1批（2026-09-16）：main.test.ts 拆已落库（1 残核 + 5 域件 + 2 共享件，93=93 用例逐位）、ai↔studio 解环已定谳静态零环并落守门固化（test/governance/ai-studio-direction.test.ts 方向锁+形状锁+防僵尸；R0912 端口先案已在役），余 = 13 件产品巨件拆分（⑤④ 逐波）+ 类型胶水/SCC-A 挂账项；拆分承重墙如实记档 = ④批 P2 监听器治理 harness 抽 test/desktop/main-process-harness.ts 逐件复刻、R0912-3 #35 pending settle 迟到弹于 spy 在役窗内放行握手确定性拆除（旧巨件时代靠同文件后续用例 spy 吸收，拆分后须自洁）〕〔处置注·⑤④产品拆分波1批（2026-09-16）：产品巨件重普查定谳 17 件 >800 行（service 2243/events store 1355/yaml 1234/state 1175/count 1144/server-manager.ts 1127/rag 1086/self-heal 1077/documents 1019/turns 958/structure 928/useChapterTreeActions 901/stream 879/prefs store 871/books 870/run 849/install books 818；desktop main.ts 2227 已拆 740 系先例）+ 五波批次表 + 不动清单六项（createStudioServerManager 状态机/firstOpenStore/prefs store 本体/executeSave/documents 基建段/count 正则共享族）；波1 四件低风险纯移动落地 = service 缝 A+B〔guards 77 + helpers 106，残核 2110〕/ yaml 缝 A+B〔spec 660 + patch 315，残核 319，yaml-schema-snapshot 快照门零漂移；renderScalar 实读改落 spec 防环 + RawSection 升类型导出〕/ count 缝 A+B〔dialogue 411 + style 384，残核 437，导出面 24=24 逐名；HANZI/SPEECH_VERBS 单源 dialogue 防 TDZ 环、DIALOGUE_TAG_* 族实读归 style〕/ install books 缝 A+B〔repair 271 + resolve 127，残核 477；books⇄新模块函数体级 ESM 环 TDZ 安全经定向实跑验证；BOOK_NAME_INVALID_REASON 留残核〕——re-export 桥接全库消费面零改动、两道治理门双零白名单维持；余 = service 缝 C〔meta 参数化 ~500 行〕拍板项待报作者〕〔处置注·⑤④产品拆分波2批（2026-09-16）：波2 四件低风险纯移动落地 = structure 三缝一体〔structure-core 257（公共底座：读态派生/plan 指纹/折叠拼接/事件副录/取号）+ structure-merge 496（S3 合并编排 + undo 三级定位）+ structure-split 178（S4 拆分编排），残核 111（门面 + 14 名 re-export 桥），core←merge/split 单向无环〕/ state 缝 A+B〔health 796（健康检查族 + 三组每书 TTL 节流缓存 + 判定辅助族——DEFAULT_VOLUME_SIZE 单源落此防 state↔recap 环回引）+ recap 122（近况复述族），残核 346，recap→health 单向〕/ run 缝 A〔run-tree-issues 443（树红点聚合族 collectTreeIssues 三态 + 双降级注入口），残核 469〕/ rag 缝 A+B〔chunk 103（分块纯函数族）+ build 735（建索引全族），残核 332（召回 + 域总述 + 逐名再导出）〕；真缺陷一枚经门暴露记档 = DEFAULT_VOLUME_SIZE 具名导出缺失（vitest transform 不校验具名导出、真 Node ESM 拒绝——server-main-error 子进程真链 2/2 确定性钉死，export 前缀修复 + tsc --noEmit 全树 TS2305 验证）；余 = 波4-5 批次表 + service 缝 C 拍板项待报作者〕〔处置注·⑤④产品拆分波3批（2026-09-16）：波3 四件落地 = self-heal 缝 A+B〔self-heal-registry 80（RunState + running/settling 两顶层 Map 单源、零 import 依赖叶子）+ self-heal-generate 184（runGenerate/emit——emit 系生成族唯一回引值依赖随族迁保单向边），残核 861，桥 6 名〕/ chat/turns 缝 A+B〔turns-tools 376（READ_*/AI_GEN_TOOLS/REWRITE_GATE_TOOLS 顶层 Set 单源 + waitConfirm/executeChatTool）+ turns-visibility 63（verifyVisibleSampled，实读 44 行族仅此一件），残核 574，桥 3 名〕/ events/store 三缝〔store-open-markers 125 + store-rows 67 + store-migrate 153，残核 1104，桥 5 名；三处实读收敛硬依据 = r0911-g close-cache 与 r41-tombstone 源文本钉测试钉住 closeEventsDb/migrateBookSession 本体留残核、OPEN_MARKER_RENEW_MS 家规禁 export let 留残核；firstOpenStore 516 行零触碰〕/ desktop/server-manager 两缝〔server-log 156 + server-proc 185，残核 841，桥 5 名；共用契约/两超时/ServerBootError 随 proc 缝单源迁出为唯一无环解；createStudioServerManager 状态机字节级零触碰〕；波3 门实录 = vitest 一次全绿 314.83s 免兜底；余 = 波5 批次表 + service 缝 C 拍板项待报作者〕〔处置注·⑤④产品拆分波4批（2026-09-16）：波4 四件落地 = api/documents 四件〔documents-core 360（基建段单源——任务口径「留残核」与「消费面零改动 + vite 无环」实测不可兼得，取内容逐字节零触碰迁移；A 路实跑并暴露 esbuild isolatedModules 真坑：overload 签名带 export、实现签名不带 → 导出整个被 strip）+ documents-save 257（W1 保存/文件树/定稿/批量定稿）+ documents-crud 295（字数日记/新建/PATCH/copy/软删/回收站）+ documents-structure 207（plan/apply/merge-undo），残核 59（聚合 + 10 名桥），路由序 16/16 逐字节一致、acquireTaskGate 4 处调用不变〕/ api/books 两缝〔books-lifecycle 421（删书路由 + shelfGuard TTL/墓地清理/forgetBookKeyedCaches 共用族）+ books-rename 301（改名路由 + initialBook 直进指针单源），残核 235，桥 3 名；新文件对残核仅 import type 运行时零回边；机械核对当场抓修 2 处漏 export 前缀〕/ api/stream 两缝〔stream-sse-writer 123（连接记账 + 背压双判死写出器——缝实读扩张：记账族与写出器族物理连续合缝）+ stream-watchdog 110（两段式挂死兜底），残核 705，桥 6 名；registerStreamRoutes 留残核装配点零改动〕/ useChapterTreeActions 两缝〔useChapterTreeCreate 243（含 seedPrefix 拍板链路逐字节保持）+ useChapterTreeStructure 253（合并/撤销/拆分），残核 558，外部消费面仅主 composable 1 名无需桥〕；波4 门实录 = vitest 一次全绿 314.68s（连续三波免兜底）；余 = 波5（prefs theme-apply + service 缝 C 拍板项待报作者）〕〔处置注·⑤④产品拆分波5批（2026-09-16）：波5 收官 = web-next stores/prefs.ts 871→791〔theme-apply 缝落 src/shared/theme-apply.ts 126 行——窗控 overlay 色族 + theme-instant 压制代数（R48-86）+ applyTheme 本体；store 残核（refs/DEFAULTS/OLD_LS 迁移链/PREF_ROWS 表驱动/门口径）零触碰；唯一结构性参数化 = createThemeApply(theme: Ref<ThemeId>) 工厂闭包——rAF 回调须按执行时刻活读主题与遮罩浓度（传值参数会冻结读点=行为改写）+ overlayAlpha/themeInstantGen 系 per-store 实例态（上提模块顶层会跨实例串态，j5 J5-5 用例可证）；j5-overlay-dim 源码锁冲突经残核缝指针注记显式携带 theme-instant 两字面量承接（锁保护功能仍生效，后续测试批宜改锁读文件指向 shared/theme-apply.ts——建议登记）；useTheme 边界实读零重叠〕；**⑤④ 五波收官：普查 17 件 >800 行巨件全数落拆分批次**（波1 service/yaml/count/install·books → 波2 structure/state/run/rag → 波3 self-heal/turns/events·store/server-manager → 波4 documents/useChapterTreeActions/stream/books → 波5 prefs），不动区在册维持（createStudioServerManager 状态机/firstOpenStore/executeSave 保存链/prefs store 本体/documents 基建段——基建段已于波4 按实读改道单源迁移记档）；门内顺带加固 = mtime 垫片族第 8 站（pm1-foreshadow-async 目录签名失效用例显式 utimesSync 前推 60s，同族 R0916-5d 七站先例，生产陈旧窗口径不动）〕〔处置注·⑤④收官补批·service 缝C（2026-09-16）：作者拍板「不想挂账」后唯一非纯移动缝落地（R0916-5j）——meta 族 454 行随注迁 service-meta.ts 561 行（updateChapterMetaLocked/rollbackMetaOnRenameFail/syncRenamePieceList/updateDocMetaLocked 四方法降自由函数、首参 svc: MetaHost 接口 = 实读 9 成员消费面）；非纯移动账本逐处记档 = this→svc 改写 24 处 + 兄弟调用降级 3 处 + 签名改写 4 行 + 成员最小放宽 9 处（bookRoot/journalDir/snapshotsDir/manifestPath 四字段 + withSaveLocks/resolveSafePath/snapshotPolicy/doMoveOrRename/lookupPathByDocIdAdoptAsync 五方法，逐处 @internal(R0916-5j) 标注、本体逐字节零触碰，queue/userDataPath/docWordsCache/globalPolicyCache 未放宽）+ 调用点接线 2 处 + import 收缩 14 名；per-实例红线 = metaOpChains Map + chainDocMetaOp 留类体单源未提模块级；新件对残核仅 import type 运行时零回边、桥 0 名（无既有导出迁出）；剥前缀多重集机检逻辑行零增删；executeSave 保存链/doMoveOrRename 族零 hunk 触及（doMoveOrRename 仅签名行放宽）；残核 2110→1664。**⑤④ 至此全部收口：普查 17 件全落拆分 + 缝C 拍板项清零**〕

- 已办结/已销项（R42-40 rmWithRetry 递归档 / R43-25 ubuntu 观察项 / R35 存量卫生 / R47/R48 stash 残件清理）：原行冻结件 §四。

## 四、归档精简（Archive 删除空间）——D 档（2026-09-04）+ E 档（2026-09-11）均已执行

- **E 档已执行（2026-09-11，作者拍板「归档文件夹里不用的文件，全部删掉，包括备份的ZIP」）**：Archive 76 md + zip×3 → 7 md + zip×0——删 69 篇归档正本（终态行内摘要仍在 `Archive/总览历史明细-归档-2026-09-08.md` §二/§五）+ B/C/D 三个精简备份 zip；删前核验 git `95663895` 树 79/79 字节级在位（含 zip cmp 抽查）——**git 历史（Dev/ 链 2026-09-11 首次入库提交）取代 zip 成为唯一复原通道**，原「zip 唯一副本不可删」约束失效；留存 7 篇全为活档指向（Archive/README 批记正本 / 冻结件×4 / 总览 §五 现行口径正本·书库平台规范化 / 本台账 §三 开放项出处·性能与内存专项），明细 = `Archive/README.md` 头部「当前含」行 + E 档批记行。
- **D 档已执行（2026-09-04，作者拍板「归档里如果不用的，可以都删掉」）**：26 篇正本删（原 zip 备份随 E 档删，复原锚同 git `95663895`）；明细原行 = 冻结件 §五。
- **维持约束（不可删）**：`Archive/总览历史明细-归档-2026-09-08.md` §二 = 历档已删正本唯一行内结论载体（E 档 69 篇终态亦在其 §二/§五 行内摘要）；`Archive/README.md` = 归档规则与批记链正本。

## 五、维护约定

- 事项办结/状态变化：本文件回改 + 总览对应行同步（含 README 计数，如涉及）。
- 新挂账产生：随评审轮次收口记登记至 §三；新待拍板方案落盘后登记至 §二。
- 本文件不复制细节，只做状态路由；考古一律回报告正本与总览行内摘要。
