# 未收口与挂账台账（单点入口）

- 日期：2026-09-04。状态：**持续维护**。
- 溯源：作者指令「文档里单独未收口的专门做一个文档放」（2026-09-04，同批指令含「已完成的归档」——27 篇已收口评审报告 + 书库平台规范化实施方案同日移入 `Archive/`）。本文件系该指令直接交付物，按命名规范落 `Dev/Docs` 根级（与总览同层的入口档），确认闸记档。
- 体例：只记**未收口 / 待拍板 / 挂账**三态事项与状态、出处；细节不复制（各事项正文以所指出处为准）。事项办结后：本文件状态回改 + 总览对应行同步，**不得只改一处**。办结/销项/超体例细节行随批移出冻结 `Archive/台账历史明细-归档-2026-09-08.md`（2026-09-08 精简批起，本文件只留开放项与存续约束）。
- 冲突优先级：总览 > 本文件。

## 一、未收口评审（01-评审/）

- （空）暂存区清空态：重评-0911b（第八篇）与重评-0911c（第九篇·GLM-5.3-Flash）两报告随 2026-09-12 R0912 修复批收口归档 `Archive/`（扁平，各补收口记），§一 两行原样冻结 `Archive/台账历史明细-归档-2026-09-08.md` §七；两报告全部开放项处置明细 = `Archive/README.md` R0912 修复批批记行 + 两报告收口记。此前沿革：第七篇随 2026-09-11 修复批收口归档；六篇已收口评审随 2026-09-11 归档批移入 `Archive/`（原行冻结 `Archive/台账历史明细-归档-2026-09-08.md` §一）。
- 暂存区此前清空态沿革：六篇已收口评审（2026-09-08~09-10）随 2026-09-11 归档批移入 `Archive/`（六行原样冻结 `Archive/台账历史明细-归档-2026-09-08.md` §一 尾续）；第七篇（全量代码重评与进度质量-评审-GLM-5.3-2026-09-11）随 2026-09-11 修复批收口归档（原行同冻结件 §一 尾续，报告正本在 `Archive/` 扁平、头注已补归档记）。
- 历轮已收口评审：终态见 `Archive/总览历史明细-归档-2026-09-08.md` §二/§五；本节原已收口行与 2026-09-08 整理批注行原样冻结 `Archive/台账历史明细-归档-2026-09-08.md` §一。

## 二、未收口方案（02-执行/ 与 03-设计/）

- `02-执行/章节结构操作-设计方案-2026-08-30.md` + `02-执行/章节结构操作-执行方案-2026-09-04.md`——**已拍板归位 + 执行方案落盘（2026-09-04），实施待作者指令**（状态正本 = 总览第三节阶段 24 行；留洞制/fm `序`/`并入` 终态要义与 S2-S5 批次以两正本为准；原行全量 = 冻结件 §三）。

## 三、代码挂账（评审链登记——按域去重看板）

> 2026-09-11 整理批（作者指令「根据当前代码，来整理和更新下文档，特别是目前未做未修的一些项目。」）：本节由按评审轮次七行（R1010c/R1010b/R1010/R0910/R0909/PM-10/PM-12）重构为按域去重看板——交叉重复项并一（switch-provider / PM-12 / chapterNoFromName / CI 空窗 / asar / e2e 顺序契约等各 2~3 处归单）；重构前原行全量冻结 `Archive/台账历史明细-归档-2026-09-08.md` §六。
> 核对：本批对 11 项代表挂账按 HEAD `be7b1170`（dev←win 合并树，工作树净）逐一对码（〔核对 2026-09-11〕标），全部仍在位、无一项因合并失时效；其余各项系各出处批 file:line 亲验登记且其后零代码改动，结论沿承。处置态：【待拍板】需作者决策 /【单立】需独立批或重构 /【维持】权衡备案不修 /【有意】有意为之。
> 出处短名（各主题头部列全，条目不重复；带 ※ 四篇正本 2026-09-11 归档批移入 `Archive/`，节号不变）：※独立复审 = `Archive/全量代码独立复审-进度与质量-评审-GLM-5.3-2026-09-10.md`；※内存专项重审 = `Archive/全量代码重审与内存专项-评审-GLM-5.3-2026-09-10.md`；※重评-0910 = `Archive/全量代码重审与进度质量评估-评审-GLM-5.3-2026-09-10.md`；※重评-0909 = `Archive/全量代码重审与进度质量评估-评审-GLM-5.3-2026-09-09.md`；PM 专项 = `Archive/性能与内存专项审查-评审-2026-09-05.md`；重评-0911b = `Archive/全量代码重评-进度质量与精简优化-评审-GLM-5.3-2026-09-11.md`（2026-09-12 R0912 修复批收口归档，头注收口补记）；重评-0911c = `Archive/全量代码重评-进度质量与精简优化-评审-GLM-5.3-Flash-2026-09-11.md`（2026-09-12 R0912 修复批收口归档，§九收口记）。

### A. 桌面壳（Electron main / 打包）——出处：PM 专项 §8.1 / 内存专项重审 §十 / 独立复审 §十 / 重评-0910 §9.4·§十

- **PM-12 残留 = DMG 打包态实测复验（原「生产 kill 接线 + 字体枚举孤儿」主项已随 2026-09-11 修复批 A-P2-1 链收口）**：tsup 构建期拷贝 fontlist 二进制入 `dist/desktop` + electron-builder `asarUnpack` + `main.ts` `darwinFontListCommand` 注入（asar→asar.unpacked 同位改写）+ check-packaging 静态/dist 双门全落地（细节 = `Archive/全量代码重评与进度质量-评审-GLM-5.3-2026-09-11.md` §十 A-P2-1 段）；原「打包态路径不可解」的待拍板理由失效（作者指令「全部修复」口径下并链办结）。残留：真装 DMG 验字体下拉主路径 + 超时 kill 行为的一次实机复验。【单立·打包态实测】
- **asarUnpack 通配不命中真实产物路径（重评-0911c P2·主审出包实锤，与上条 DMG 实测并批）**：`electron-builder.yml:21` `desktop/fontlist` 不匹配 asar 内实际路径 `dist/desktop/fontlist`（无 `**/` 前缀 minimatch 不命中；smartUnpack 只兜 node_modules 来源）——`electron-builder --dir` 实测 unpacked 仅 node_modules/font-list 自带二进制、`/dist/desktop/fontlist` 仍在 asar → 打包态 darwinFontListCommand 指向不存在路径 → spawn ENOENT 恒回落 font-list 库（字体功能不断），R0911-A-P2-1「超时必杀」加固打包态恒不生效、挂起每次白付 10s 熔断；门测试只锁模式字符串不验匹配语义（`check-packaging.test.ts:87-101`）。修法：pattern 改 `**/desktop/fontlist` + 门测试同步 + DMG 实包验证。**处置（R0912）**：模式改 `**/desktop/fontlist`（electron-builder.yml）+ 门同步校验「可命中 dist/desktop/fontlist」、裸模式判红 + 门测试密闭化；`electron-builder --dir` 出包复验 `app.asar.unpacked/dist/desktop/fontlist` 在位（137KB 带执行位），打包态 spawn 链恢复。【已处置·R0912】
- close/quit 链确认框同步泵消息循环（showMessageBoxSync 冻结三窗；A-P3-3，R1010-P3 G7-② 同族备案重证）。【维持】
- uncaughtException 200ms 兜底窗（R44-17 备案——窗内二次异常仍可丢退出）。【维持】
- isTrustedSender 拒绝路径 undefined 类型契约：正常形态不可达，desktop.d.ts 全 widen 牵面前端判空面〔核对 2026-09-11：`src/studio/web-next/src/desktop.d.ts:33` 成功路径 void 如实标注在位〕。【单立·待拍板】
- asar 冗余 node_modules（打包体积）。【单立】
- server-main 信号兜底修复无测试装置（import 期副作用，入口解耦后补）。【单立】
- Node libuv 线程池限制（平台性备案权衡）。【维持】

### B. 服务端（studio/server）——出处：独立复审 §十 / 内存专项重审 §十 / 重评-0909 §六·§九 / 重评-0910 §9.4 / 重评-0911b §3.3·§四 / 重评-0911c §4.2

- **中断语义族（重评-0911c P2·本轮最大新发现，B/D 两域独立确认 + 前端侧同源，四条一个批统一收口·已处置 R0912）**：① `/interrupt` 对 task-gate 族完全无效且假成功——interrupt 通道只覆盖 abortSelfHeal+abortChat+driver.interrupt（`stream.ts:666-682`），outline（`outline.ts:45`）/lead-updates（`lead-updates.ts:46`）/三审（`review.ts:346`）/analysis（`analysis.ts:348`）/onboard（`onboard.ts:42`）/relations-mine（`settings.ts:200`）/rewrite（`rewrite.ts:55`）/定稿摘要钩子（`summary.ts:293,627`）等 9+ 调用点 runTask 均未传 ctrl/register（`runner.ts:515` 可选），在途时 anyRunning（`stream.ts:674-679`）为 false → 返 200 什么都不停，分钟级任务白烧 API 费、唯一出口重启；② self-heal pass 后台账本草稿后台任务失联（`self-heal.ts:727-728` 传 state.ctrl.signal 但收尾后 running Map 已删 + `stream.ts:824` unregister → 跑满 10min 超时）；③ /spawn watchdog abortLikeUser 只 abort 不推 interrupted 事件（`stream.ts:181-183` vs self-heal :771-775 两路分叉）；④ watchdog 强释放注销 ctrl 后 /interrupt 永久失效（`stream.ts:781-791`「注销即失联」）。**处置（R0912）**：七端点统一 register-ctrl（owner `<端点>:<书名>`，analysis 按 action 分槽防同 owner 换新误伤）+ /interrupt 返回如实附 interrupted + watchdog 改走 driver.interrupt 全动作集 + 强释放 ctrl 留册至 settle（源锚 R0912- 注释为准）+ 定稿摘要钩子 documents.ts 惰性 ensureSession 接线 + self-heal 后台草稿改独立登记 ctrl（`bg-lead-draft:<书名>`）。【已处置·R0912】
- **卷复盘两端共同功能缺口（重评-0911c P2）**：state 5 routeState 返回 action='volume-review'（`state.ts:875-882`，M4 未实装自认），服务端无任何卷复盘端点；前端 WbStateCard.vue:23-25 把该动作映射 emit spawn → 点「卷复盘」实际走 writer 链生成下一章正文（B 域定性：两端共同缺口非误接线）。**处置（R0912 FE）**：按钮文案对齐实际行为（「继续写作（下一章）」+ title「卷复盘功能规划中，当前直接开写下一章」+ 卡内说明行），emit spawn 语义不变——误导面已消除。【已处置·R0912 文案对齐；卷复盘产品语义仍【待拍板】，端点单立随拍板】
- **spawn/auto-write/chat 互斥闸只查进程内闸（重评-0911c P2 疑似·限双进程形态）**：`stream.ts:593/720/882` heldTaskGatesFor 未并 crossProcessHeldTaskGatesFor——dev-api/CLI 与 GUI 双进程开同书时细纲覆盖写与写稿互踩（R67-13 要防形态）；`books.ts:243` busyGate 已并两侧，修法现成 = 换 `allHeldTaskGatesFor`（audit.ts:193）。**处置（R0912）**：stream.ts spawn/auto-write/chat 各闸点换 allHeldTaskGatesFor + 跨进程锁文件 4 用例（手写活 pid/陈锁）。【已处置·R0912】

- **rag/rebuild 前端断头（重评-0911b P2①）**：`api/rag.ts` rebuild 端点在位（R26-16），前端唯一接线 `triggerRagBuild`→`/rag/build`（web-next `api/books.ts:139`）；`rag/index.ts:387/593/717` 三处失配错误文案引导 GUI 不可达的 POST /rag/rebuild——embedding 模型/维度变更后 RAG 无法自愈（死路）；SettingsBookAnalysis.vue 失败提示文案亦与实际调用的 build 语义不符。修法：前端接 rebuild（或 build 失配失败后引导重建）+ RagStatus 类型补 indexModelMismatch/indexState。【待修·重评-0911b P2；重评-0911c 未独立复现（G 域判 RAG 域完整、未核失配文案接线面），维持重评-0911b 登记效力】→ **处置（R0912 FE）**：api/books.ts 补 triggerRagRebuild（POST /rag/rebuild——服务端 R26-16 端点与 indexState/indexModelMismatch 字段本就在位，零服务端改动）+ SettingsBookAnalysis 失配提示与「重建索引」钮（同套在途锁/轮询）+ RagStatus 类型补两字段。【已处置·R0912】

- **锁超时降级取舍族**：journal/analysis/words-diary/lead-update-draft 四处（R0910 P2-6，涉崩溃安全语义重设计）+ batch-pause 锁超时裸写（R1010c 自认）——代码已记档。【维持】
- config.ts PUT /config 与 prefs.ts PUT/DELETE 同型越界命中（照 BOOK_MOVED 同款收口）。【单立·可开工】
- finalize 重验需动第三文件。【单立】
- rename 微任务残窗：单元首行重验后微任务窗残留（files.ts R70-6 架构同源，彻底闭合 = 重验下沉 DocumentService.executeSave）。【单立】
- stream 互斥矩阵（备案权衡）。【维持】
- SSE `?token=` 兼容通道下线（移除条件 = useSse 回退路径下线）〔核对 2026-09-11：`src/studio/server/api/stream.ts:379` 注释登记在案〕。【维持·条件下线】
- audit 全量折叠无事件循环让出（R0909 P3-5，数十 ms 级，收益/风险不称）。【维持】
- defineRoute parse 声明迁移 45 处（`schema.ts:12` 自记 RC 后分批）。【单立】
- 记账同步 IO。【维持】
- 导出排队持闸。【维持】
- migrateBookDefaults 启动全量读盘（SMB 场景）〔核对 2026-09-11：`src/install/migrate-defaults.ts` 挂 server/index.ts 在用〕。【维持】
- 回收站 restore 未接伏笔差分链（伏笔快照缺位→差分静默跳过，防御性安全、计数不更新）。【维持】
- rename 跨域判定用链前 docPath（伏笔快照读全局不受影响，主审复核无正确性损失）。【维持】
- 导出 io.ts skippedDrafts 未透传前端（服务端已剥草稿，前端仅缺计数可见性）。【单立·小】
- 误报语料查询侧未接线（B-P3-2：只写不读、存储积压——`check.ts` 注释自认有意留白，与语料 harvest/commit 通道分阶段建设）。【有意·维持】

### C. 前端（web-next）——出处：独立复审 §十 / 内存专项重审 §十 / 重评-0910 §9.4·§十 / 重评-0911b §3.3·§四 / 重评-0911c §4.2

- **openTab 切档假警报（重评-0911b P2②；重评-0911c 独立复现）**：`stores/workspace.ts:297` 切文档遇旧文档保存在途，`doc.save(id,'autosave')` 对 saving 态直接返 false（`stores/doc.ts:387`）→ 弹「切换文档时自动保存失败」误导 toast——在途保存正常进行、内容不丢；且 conflict 未决路径（doc.ts:397 autosave 跳过为设计行为）同样触发，conflict 文档每次切走必弹；系 2026-09-09 可见化清偿批引入的回归。doDelete 已用 `waitInflightSave`（doc.ts:701）解决同型问题，openTab 未同步。修法现成。【已处置·R0912：waitInflightSave 后复查 dirty，conflict 未决不弹（编辑器自有冲突 UI）；入口书名+docId 双快照守卫】

- ContextMenu G6⑦ 浏览器回退版飞出层键盘不可达（Enter 可开层但 Tab 关菜单、ArrowRight 未接线；桌面端原生 Menu 全键盘可达不受影响，回退版仅 dev/web 触达；接线需跨层焦点机，触发条件 = 浏览器版转正，代码注释档已声明）。【维持·条件触发】
- flushDirty for(;;) 理论无界。【维持】
- 全库无请求取消接线。【单立】
- discard 窄窗自愈。【维持】
- 全屏书架无上限。【有意】
- onSaveMeta/doMove 同族 fp 键孤儿（改名链只修 onRenameCommit，同款清理待评估）。【单立·待评估】
- refresh 脏分支收编基线 + CmHost O(n) 物化（前端核心 P3-②③）。【维持】
- prefs lastPersisted（重评-0910 G5③ 证伪维持——「移到 PUT 后」经推演破坏 R61-F-3 零脏字段基线，R32-26 服务器对账路径在位）。【维持】
- 书级 prefs beforeunload 刷新路径不冲刷（C1-P3-3 修复批残留：关窗主路径已接 `__clwFlushBeforeClose` 首位冲刷；刷新走 beforeunload 仅文档面——500ms 防抖窗内末次布局态仍可丢，刷新频率低取舍维持，FE1 代理报备）。【维持】

### D. AI 链路与编排——出处：独立复审 §十 / 重评-0910 §9.4·§十 / 重评-0909 §5 / 重评-0911b §3.3·§四 / 重评-0911c §4.2

- **重写循环 draftPath 与最新稿脱节（重评-0911c P2·窄触发）**：`loop.draftPath` 仅首稿设定、重写落盘不回写（`self-heal.ts:675-687`/:796 恒打首稿）；tool_use 未命中降级自由文本（:974-978）+ AI 自带异章号 fm 时 `resolveDraftPath` 按章号失配（`format/draft.ts:79-106`）→ 每次重写在 写作/正文 新建 000N-* 孤儿文件、机检恒打首稿旧文件 → 红项恒同烧满 maxAttempts 后 escalate，残留树红点失明的坏 fm 章。修法：rewriteOnce 落盘后以返回 relPath 刷新 loop.draftPath（或 saveDraft 内容章号≠入参章号时拒绝/告警）。**处置（R0912 D/AI）**：rewriteOnce 落盘后以 save 返回 relPath 回写 loop.draftPath + readDraft 纯内存比对异章号 warn 防线（不阻断）。【已处置·R0912】

- **ai→studio 反向依赖（重评-0911b P2③；重评-0911c 独立复核属实）**：`orchestrate/chat/turns.ts:46` `import { acquireTaskGate } from '../../../studio/server/api/task-gate.js'`——ai 底层引 studio 消费方，与域自治方向相悖；注释自认分层债并写明迁移触发条件（task-gate 现依赖 node:fs/cross-process-lock/ai-log，搬层非平凡）。形态债非正确性缺陷。**处置（R0912 主审集成）**：ai 层新建 orchestrate/task-gate-port.ts 端口（未注册 no-op 放行），turns.ts 换端口取闸，stream.ts registerStreamRoutes 注册真实闸；5 用例锚定（注册前放行/透传/重注册覆盖 + registerStreamRoutes 源锚 + ai 层零 studio import 分层源锚）。【已处置·R0912 依赖倒置】

- **switch-provider 失败决策无消费者**（换网关策略消费需作者指令；shrink-prompt 已由清偿批最小接线 chat 侧——ctxOverflow 侧信道 + ⌊historyBudget/2⌋ 预算重试 + a7-shrink-retry 3 用例，非 chat 不收缩维持 A7 范围）〔核对 2026-09-11：`src/ai/provider/failure.ts:8,93` 注释自认无消费者〕。【待拍板】
- chat 历史 LRU（上限 8）逐出在途书（>8 本并发对话才触发，注释留账不修）。【维持】
- stopReason 三线。【维持】
- TOKEN_COEFFICIENTS（估算口径）。【维持】
- MAX_AGENT_TURNS=5 保守。【待拍板·产品】
- rulesPromptParts 伪路径标签（hash 兜底）。【维持】
- 加密推理多条仅末条（A-5 既有留痕）。【维持】
- X-4 残余。【维持】
- AI 域 P3-1·4·5（重评-0909 报告细目）。【维持】
- AI⑤⑥ 备案权衡（重评-0910 沿用行）。【维持】

### E. 核心域 / 持久化 / 文档数据——出处：内存专项重审 §十 / 独立复审 §十 / 重评-0909 §六·§九 / PM 专项 §8.1 / 重评-0911c §4.2

- **save 类崩溃 pending 无消解闭环（重评-0911c P2·主审对码坐实）**：healthCheck 对 save 类 pending 一律报 crashedWrite（`state.ts:350-370`），journal compact 恒保留未结算（`journal.ts:342-383`），无 baseRevision×盘上指纹复核、无 acknowledge 通道（全仓 grep 证实）→ settled 写失败（`service.ts:629-633` best-effort）后幽灵红每次进门重复报「可能丢字」、应用内无法清账。修法：确定性复核（指纹不一致 ⇒ 已落盘自动补 settled）+ acknowledge 端点。**处置（R0912 CORE）**：两段全落地——reconcileSavePending 自动消解（null/ENOENT/EBUSY 边界保守报红）+ `POST /api/books/:name/journal/:opId/acknowledge`（幂等）+ `/state` payload 透出 crashedPendingOpIds（主审集成）+ FE「忽略此提醒」按钮贯通。【已处置·R0912】
- **结构性操作不持 save 锁的双向复活窗（重评-0911c P2）**：doTrash 落位（`service.ts:1811`）/doMoveOrRename 落位（:1473）全程无 `<journal>.save.lock`——他进程 save 过守卫后、rename 前 atomicWriteFile 在旧路径复活已删文件（后续还原报 OCCUPIED、清单无条目）；R76-22（:419-422）登记的只是「复核→写盘」单侧毫秒窗。修法：结构性操作 rename/rm 源前同取 save 锁（锁序先例现成）。**处置（R0912 CORE）**：doTrash/doMoveOrRename 落位段取锁（STRUCT_SAVE_LOCK_TIMEOUT_MS=5s + holdSaveLock 缺省 fail-loud），锁序 save→journal→清单与 executeSave 先例一致，4 用例互斥验证。【已处置·R0912】
- **保存守卫清单读容错口径分裂（重评-0911c P2·主审对码坐实）**：`lookupPathByDocIdAdoptAsync:1567` 用容错版 `readManifest`（瞬态 EBUSY/EACCES → 空表 → 「文档已移动」守卫静默失效 → 同内容双文件身份分裂，不丢数据）；RMW 链 :1552/:1583 已 strict（R27-40）、同函数回收站守卫 :337 亦 strict——口径不对称。修法：命中读改 strict。**处置（R0912 CORE）**：lookupPathByDocIdAdoptAsync 命中读改 readManifestStrict + 六调用面（save/trash/move/copy/meta×2）收口 WRITE_ERROR，3 用例（EBUSY 未落盘+恢复后成功等）。【已处置·R0912】
- **SessionRecorder.flush 失败路径 pending 无上限（重评-0911c P2）**：`chat-bridge.ts:183-206` 持续落库失败（SQLITE_BUSY 耗尽/磁盘满）+ 长对话下 pending 无界增长；同文件 ChainRecorder 有 256 上限（chain-bridge.ts:148,197-208），recorder 无对应闸。修法：对齐加闸+留痕。**处置（R0912 CHECK）**：SESSION_PENDING_MAX=256 对齐 ChainRecorder + 丢最旧 warn 留 turn 区间 + 批内序号平移加固（sourceIdxs 摘除/前移，防恢复轮撞 store 越界校验）；3 用例含恢复轮整批落库。【已处置·R0912】
- meta PATCH 双路径写回无 journal pending（`service.ts:1071-1078`/:1328-1334）——「每写必有 pending」协议在元数据链豁免未登记。**处置（R0912 CORE·选「真补」）**：updateChapterMetaLocked/updateDocMetaLocked 补 pending/settled（写前指纹 → atomicWriteFile → 失败 aborted/成功 settled best-effort），与消解闭环自洽；3 用例（配对清零/双 pending/ENOSPC 留痕）。【已处置·R0912】
- version.ts:377 版本列表 ULID `localeCompare` 排序疑似漂移面。【已处置·R0912：改字节序比较 + 排序性质测试】
- **事件读链 O(N)（= PM-10 残余真·尾读）**：store 层 sinceSeq 游标下推 + skeleton 计数通道（listEvents 现为游标流式全量投影，total/truncated 契约需全量；契约注释 + 6 用例守门在位，改造时为验收面）。【待拍板·立项】
- execRing 会话级单环并发 owner 回放丢段（chat×self-heal 并发 + 断线重连窗，per-owner 环单立）。【单立】
- md 缓存 stat→read 指纹错配（ns 级自愈型，读后复验 IO 翻倍不称）。【维持】
- 跨进程锁续期 timer 丢失 release 空转（防御性缺口，当前零违约调用方）。【维持】
- WAL Atomics。【维持】
- **chapterNoFromName 裸数字 / `1.md` 扩集**（既定边界，扩集牵动全部消费点；注释已修失实 + callshape 对表测试钉分裂现状）〔核对 2026-09-11：`src/format/filename.ts` 正则 `^(\d+)(?:[-—]|\s|$)`——带扩展名全名形态不中，边界与登记一致〕。【待拍板】
- leads.ts:537 `\d{3}` 劈分语义备案。【维持】
- piece-list-core 头注「MetaFormPanel 直接 import」与现状不符（零 import，历史注释记档）。【维持】
- 文档格式 P3 ①③④⑤⑥（重评-0909 报告细目）。【维持】
- P3-20 读侧关联词「值 === 标题且含逗号 → 整词单项」收口（可收回旧迁移存量且不伤表单路径；写侧数组化已落）。【待拍板】
- mac 上事件库 bookHash 不折叠大小写（E-P3-1：books.jsonl 路径漂移分裂事件库「丢史」假象——store.ts R40-23 注释登记在案）。【维持】
- writePieceList 生产零接线、章纲未知段无保形写通道（E-P3-4，R48-51 登记）。【维持】

### F. RAG / 知识——出处：独立复审 §十 / 重评-0910 §十 / 重评-0911b §3.3·§四 / 重评-0911c §4.2

- **机检剥引号口径分裂（重评-0911b P2④；重评-0911c 独立复现并补证）**：`check/count.ts` checkBodyParts（:879）/checkSimile（:932）不剥对白引号 span，同文件禁词（:119）/意象（:483）/开头环境（:1061）均 `stripQuotedSpans`，且 R51-E-N5 注释（:478-483）自证「同族均剥、唯本检查吃原文」家族约定存在——对白密集章 body-parts/比喻密度系统性虚高，两项又属严格短篇升红族（runner.ts:377-405）可致对白密集章被误打回重写白烧模型调用；无注释声明有意为之（R51-E-N5 同族遗漏）。修法：两处补 `stripQuotedSpans` 或登记有意口径。**处置（R0912 CHECK）**：两处补剥（prose 口径对齐同文件家族）+ R73-14 注释漂移修账（前排他集补「群像」）；5 用例含改前口径自证 + 叙述真实命中仍报（防矫枉过正）。【已处置·R0912】
- RAG watchdog 双跑窗。【维持】
- RAG test-only wrapper。【维持】

### G. 测试工程 / CI / 工具链——出处：独立复审 §十 / 重评-0909 §九 / 重评-0910 §9.4 / 重评-0911c §4.2

- **check-packaging「真实仓库脚本直跑」用例非密闭（重评-0911c P2·主审 L2 实测复现）**：`test/desktop/check-packaging.test.ts:46-49` 直跑真实脚本，工作树 dist 半新态（main.js 在而 fontlist 缺）时 exit 1 → 全量单测单点红（2026-09-11 本机实测复现，`npm run build` 后复绿）；CI 免疫（无 dist 场景门跳过 / mac 腿 build:all 先行），纯本地污染。修法：用例容忍「问题清单恰为 dist-fontlist 一条」或脚本 dist 实存门改可注入。**处置（R0912 主审）**：`CLW_CHECK_PACKAGING_SKIP_DIST_GATE=1` 环境注入口 + 用例置变量只锁配置/资源对账面（dist 门语义由 problemsForDistFontList 直测锚定）；CI 不设变量门照常。【已处置·R0912】
- check:packaging 门文案混叠（陈旧 dist 被报成「tsup onSuccess 拷贝失效回潮」，`check-packaging.mjs:169`）——fail-closed 方向正确但环境陈旧与配置回潮不可区分，误导排障；CI 接线两处均安全无需改门序。修法：文案区分两态。【已处置·R0912 主审：陈旧 dist 与拷贝失效两态并列文案，fail-closed 方向不变】

- **CI 红四族（2026-09-11 新登记行 → 同日修复批全数处置，待六腿 CI 复验）**：原 run 34503257752 四族（R71-8 大小写 FS 假设 / kk-P2-8 ubuntu 差异 / r43 慢机 TTL 时序 / win tinypool teardown 竞态）已全处置——① R71-8 用例改运行时 FS 大小写探测分支断言（敏感卷真改名/不敏感原位，两分支各锚产品实际行为）；② kk-P2-8 定性收束 = `M.msgBox` 文件级累积面被敏感卷上前序切库警告污染（got 3 = 2 残留 + 1 本用例，精确对账）→ 改快照增量口径；③ TTL 族 7 处真睡眠改 `toFake:['Date']` 注入时钟（相关面 -93% 耗时，r43 慢机压力源同批移除）；④ win 腿测试步改「首跑非零同命令重跑一次」兜底（真失败不吞）。残留：六腿 CI 复验（修复合入 main 前下一轮 workflow_dispatch 观察）+ win 通过数确认（预期 1049 文件 / 6720 过 + 80 跳）。【已处置·待 CI 复验】
- vitest/tinypool forks 收尾竞态上游根治评估（win 重跑兜底的撤除条件）：查 vitest >3.2.7 / tinypool >1.1.1 changelog 是否含 teardown 竞态修复（关键词 ERR_IPC_CHANNEL_CLOSED / unhandledRejection during teardown）；有则升级 + L2 回归后撤兜底。备选 = tinypool patch（`_removeWorker(...)?.catch?.(() => {})` 两处，patch-package 面，仅收尾期吞 reject 不掩盖测试成败）——兜底被判定不可接受时提案。【单立·评估】
- CI dev push 腿空窗（恢复与否）〔核对 2026-09-11：ci.yml `on:` 仅 main push / main PR / workflow_dispatch〕。【待拍板】
- Electron GUI 交互 e2e 缺位（release-smoke 已在主 CI e2e job 日常跑；残余 = Playwright `_electron` 交互 e2e 投入）〔核对 2026-09-11：test/e2e 零 `_electron` 引用〕。【待拍板】
- win32 74 处 skipIf 用例三腿皆不实跑（win 腿补口方案）〔核对 2026-09-11：`skipIf(process.platform === 'win32')` 实测恰 74 处〕。【待拍板】
- corpus 语料 7/14 checkIds（余 7 受门 fixture 硬约束：铁律阈值段/短篇 kind/布线书）〔核对 2026-09-11：test/corpus/checks/ 7 json 在位〕。【待拍板】
- e2e 长尾 4 旅程（导入旧书 / 备份恢复 / 导出成品 UI / 升级向导）。【单立】
- e2e 共享 workDir 顺序契约（R9-P2-3 架构级；缓解已立：全新 workDir / 顺序快照门 / workers:1）〔核对 2026-09-11：playwright.config.ts workers:1 在位〕。【维持】
- mock-cc 分叉。【维持】
- 临时目录清理双轨 328 处（单立机械批）。【单立】
- 批次号命名·注释编号考古税（文化项）。【维持】
- 双包宇宙版本偏斜（workspace 化大重构；sharedRuntimeVersionDrift 对账门已立作缓解）。【单立】
- check-counts skip 门禁正则枚举（AST 化）。【单立】
- 测试工程 P3-②③⑤⑥（重评-0909 报告细目）。【维持】

### H. 全仓精简总账（重评-0911b §四 / 重评-0911c §五）——出处：重评-0911b §四（正本在库 `01-评审/`）+ 重评-0911c §五（正本在库）

- **重评-0911c 五档总账**：产品侧结构性冗余 **≈0.9k–1.2k 行（约 src 的 1%）**——一档零风险死码 330–380（useDebouncedSource 连测试 166 / events 旧载荷接口 ~100 / 死类型散点 19 处）+ 二档低风险合并 550–860（**前端三组双实现为最大点对**：ChatDock↔ChatPanel composer 滑窗 108 窗 / ShelfHeroCard↔ShelfModalHero / Shelf↔ShelfModal；driver cc↔mock 总线 / Book.vue 切书取消分支三份 / 仿树行样式三份）；三档 oversized 29 个纯移动拆分（净减 0）；四档注释冻结剪枝理论 src 2.4k–4k + test 2k–3k（需作者拍板、按冻结纪律剪出归档非直删；承载现行语义段不可剪）；五档测试侧 2.8k–5k（req 帮手 46 份归一 / 书架脚手架 344 文件抽工厂 / 大段复制点对）；观察项 = 六族启动迁移+token-calibration 退役 1.4k–1.6k（需拍板）。依赖面零闲置。与重评-0911b 档二差异点：catalog.gen 454 行块重评-0911c 未复核（存疑待域评交叉）。【待拍板】
- **重评-0911b 三档总账（2.6k–4.9k 行，≈107k 的 2.5–4.5%）**：档一低风险机械批 ~800–1000 行（F 域 test-only 死导出 ~270〔enableRag 族/recall 包装/unlinkWithRetry/readAllChunks/formatStyleReport 族，均 grep 核实零生产调用〕/ E snapshot.ts 别名层整删 ~111〔头注 O-12 退役登记在案〕/ E 测试专用导出退役 ~205 / A·B 小件 ~100）；档二需拍板 ~1.5k–2.6k（D catalog.ts+catalog.gen.ts ~454 全仓零消费·最大单块 / A 同步孪生 ~240·属有意等价对照口径 / B 内联校验迁 defineRoute ~200–400 / C2 Settings 开关样板抽 SettingToggle ~250–400 / C1 prefs setter 表驱动 ~150–200 / E yaml 补丁族+service 锁样板 ~160–250）；档三结构性（13 件 >800 行拆分、ai↔studio 解环、documents.ts runX 样板收敛、main.test.ts 拆）。不建议动清单（foreshadow 孪生/RagProviderEditor 合并/R 编号审计注释等）见报告 §四末。【待拍板】

- 已办结/已销项（R42-40 rmWithRetry 递归档 / R43-25 ubuntu 观察项 / R35 存量卫生 / R47/R48 stash 残件清理）：原行冻结件 §四。

## 四、归档精简（Archive 删除空间）——D 档（2026-09-04）+ E 档（2026-09-11）均已执行

- **E 档已执行（2026-09-11，作者拍板「归档文件夹里不用的文件，全部删掉，包括备份的ZIP」）**：Archive 76 md + zip×3 → 7 md + zip×0——删 69 篇归档正本（终态行内摘要仍在 `Archive/总览历史明细-归档-2026-09-08.md` §二/§五）+ B/C/D 三个精简备份 zip；删前核验 git `95663895` 树 79/79 字节级在位（含 zip cmp 抽查）——**git 历史（Dev/ 链 2026-09-11 首次入库提交）取代 zip 成为唯一复原通道**，原「zip 唯一副本不可删」约束失效；留存 7 篇全为活档指向（Archive/README 批记正本 / 冻结件×4 / 总览 §五 现行口径正本·书库平台规范化 / 本台账 §三 开放项出处·性能与内存专项），明细 = `Archive/README.md` 头部「当前含」行 + E 档批记行。
- **D 档已执行（2026-09-04，作者拍板「归档里如果不用的，可以都删掉」）**：26 篇正本删（原 zip 备份随 E 档删，复原锚同 git `95663895`）；明细原行 = 冻结件 §五。
- **维持约束（不可删）**：`Archive/总览历史明细-归档-2026-09-08.md` §二 = 历档已删正本唯一行内结论载体（E 档 69 篇终态亦在其 §二/§五 行内摘要）；`Archive/README.md` = 归档规则与批记链正本。

## 五、维护约定

- 事项办结/状态变化：本文件回改 + 总览对应行同步（含 README 计数，如涉及）。
- 新挂账产生：随评审轮次收口记登记至 §三；新待拍板方案落盘后登记至 §二。
- 本文件不复制细节，只做状态路由；考古一律回报告正本与总览行内摘要。
