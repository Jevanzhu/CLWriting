# 测试精简优化-执行-2026-09-12

- 状态：**已完成**（2026-09-12 开工并收口；作者指令「现阶段的测试太多了，七千多个测试，我需要精简和优化！」→ 中断后续作「继续，记得编排下任务做。」——后者为 §六 预登记销账项的执行许可）
- 执行模型：GLM-5.3（主审 + 域执行代理同模型）
- 基线：mac HEAD `29291f67` 工作树净；vitest **1095 文件 = 7063 过 + 5 跳 0 败**（声明 7068）；coverage 全量基线 st 91.82 / br 87.39 / fn 96.19 / ln 91.82（分桶阈值全过，留作收口对比锚）。
- 性质：执行批（非评审批）——台账 H/G 域预登记测试侧精简项的落地（销账对象见 §六）。

## 一、勘察结论（四路只读勘察，2026-09-12）

1. **用例级冗余极小**：全库近重复标题仅 16 组（全部为「多形态钉定」非复制粘贴）；无死码测试（src 零消费方模块经查全在役）；巨阵面健康（最大 41 格且格间语义实质）；弱断言无主导文件。**干净可删用例估计 ≈0–20 例且多被锚保护拦下**——用例数不是肥肉所在。
2. **保护面占 75.1%**：锚文件（文件名 r\d+ 前缀 ∪ 文内 R\d+ 锚码）822/1095；desktop/git/learn/governance/corpus/e2e/check/rag 近 100% 锚定；自由池 273 文件（studio 132、ai 34、document 28、process 15、fs 14、format 11、install 10…）。
3. **结构肥肉（本批主战场，台账预登记项）**：
   - server 启动样板：`startServerSafe` 手写样板 **121 文件**（studio 本级 119 + check 1 + document 1）——tmp workDir + books.jsonl + 起服 + /api/boot token + 本地 req() + afterAll 清理，每文件 ~15–30 行（台账 L201/L210，估 2.5–3.5k 行）；
   - 造书脚手架：本地 `makeBook` 89 份 + `makeDriver` 20 份 + `tempUserData` 6 份（台账 L181/L209，估 0.8–1.9k 行）；
   - `fakeReqRes` 三份逐字相同拷贝（r1010b / r0911 / r0912-acknowledge，重评-0912-3 #54）。
4. **平台门账目闭合**：win 门 78 例 + darwin-only 1 例 − win-only 4 例 = 差值 75（与 README「实测差 75 恒定」一致）；mac 5 跳 = 4 win-only + 1 gc 门。**本批不动任何平台门用例，差值账目保持。**
5. 套件时间分布均匀（最慢 rag/scale 19.6s 属保护面；studio 本级 188 文件合计 89s CPU 时间）——运行时无单点热点，体量才是杠杆。

## 二、精简方案与红线

### 2.1 新增/扩充共享 helper（主审亲写）

| helper | 位置 | 收编对象 |
|---|---|---|
| `bootStudio()` 组合 harness（workDir + books.jsonl + 起服 + token + req() + close() 幂等收口） | `test/helpers/studio-server.ts`（新） | 121 文件启动样板 |
| `scaffoldBook()` 参数化工厂（mkdtempTracked + 可选 .clwriting/book.yaml/dirs/files） | `test/helpers/book.ts`（扩充） | 89 份本地 makeBook 公共骨架 |
| `fakeReqRes()` 单一真相源 | `test/helpers/fake-reqres.ts`（新） | 三份逐字拷贝 |
| `makeDriver` mock 单源 | `test/ai/fake-driver.ts`（波 1 ai 代理建） | test/ai 域 ~20 份近同构 |

样板转换参照件：`test/studio/r43-put-wiring-lock.test.ts`（主审亲转，90 → 50 行，用例逐字不变，vitest 2 过）。

### 2.2 红线（全体执行代理必守）

1. **用例红线**：结构收编波不改任何用例数——it/test 声明、断言、标题逐字保持；仅替换启动/脚手架样板。
2. **锚红线**：锚文件（文件名锚或文内锚码）只允许样板替换；锚码注释必须存续。
3. **平台门红线**：`skipIf(process.platform…)` / `{skip:…}` 用例一律原样。
4. **语义等价**：盘面（books.jsonl 字节、book.yaml、目录/文件、env 注入时序、workDir 前缀）逐项等价；req() 仅在「常规 JSON 端点」面替换，畸形 body/特殊 header/免 token 面保留本地函数。
5. **存疑不转**：每文件转换后 `npx vitest run <file>` 必须绿；转换存疑（时序耦合/多服重启/非标启动项）→ 跳过并记录原因。
6. 不动：src/、scripts/、e2e spec（顺序快照契约）、soak、test/governance/、test/check/scale.test.ts、test/rag/scale.test.ts、coverage 独占文件（`cov-*`、`*-short` 族）。

### 2.3 自由池用例精简（第二阶段，保守）

自由池（无锚 273 文件）内做有限用例级精简：同文件内逐断言碎用例聚合、跨文件真重复（同 src 分支同断言）删冗；**每删一例记录档案**，收口前 coverage 对比分桶验证不回退。预期收益有限（勘察结论 1），以实际核实为准，不设硬指标。

## 三、执行编排（波内 ≤4，文件互斥）

| 波 | 代理 | 辖区 | 任务 |
|---|---|---|---|
| 1 | S-A / S-B | test/studio 本级（a–m / n–z + y2） | bootStudio 收编（S-B 兼 fakeReqRes 两份替换） |
| 1 | AI | test/ai | makeBook→scaffoldBook、makeDriver 单源、tempUserData |
| 1 | DOC | test/document | makeBook→scaffoldBook、启动样板（1 文件） |
| 2 | PROC | test/process + cache + learn | makeBook/makeDriver/tempUserData 收编 |
| 2 | CKRG | test/check + rag + export + install | 同上（check/rag 基本全锚——只做样板面） |
| 2 | FMT | test/format + fs + state + events/log/metrics/knowledge/git/review/scripts/corpus/driver/根 | 脚手架收编 + fakeReqRes（state 1 份） |
| 2 | WBN | test/studio/webnext | mock 样板/本地 helper 近同构收编（先勘察后动，保守） |
| 3 | （视 1/2 结果） | studio 本级 + ai + document 自由池 | 用例级精简（§2.3） |

## 四、执行结果

### 4.1 编排实录

- 波 1 四路代理（S-A/S-B/AI/DOC）推进中遭遇用量限额整体阵亡（在途 ~79 文件改动）；恢复盘点：79 文件合跑仅 snapshots-api 1 处 import 断裂（修复后全绿），波 1 成果全部保全。作者指令「继续，记得编排下任务做。」后转**主审亲做 + 三路补齐代理**：G-1（studio 头部 40 件）/ MID-1（r31–r67 段 13 件）/ MID-2（r71–review 段 12 件），全程在途 ≤4，主审逐 diff 复核（MID 两面全量 diff 过目 + G-1 面高风险 4 件抽核：rag.secret 落 key 时序改道经 src/rag/resolve.ts 惰性读码证实等价、userData 迁出 workDir、env undefined=删除语义）。
- 收编实数：**bootStudio 82 个测试文件**（studio 本级 81 + document 1）+ **scaffoldBook 12** + **fakeReqRes 3 文件换装**（helpers/fake-reqres.ts 单源）+ **makeFakeDriver 29**（test/ai/fake-driver.ts 单源）。

### 4.2 studio 本级保留面（47 文件本地启动，triage 逐一记因）

1. **多服/多书形态**（被测面本身）：api-error-branches（双服 + books.jsonl 追加）、stream-api（BOOK+IDLE_BOOK 双书）、stream-ticket（中途关服二次裸 startServer 验票库隔离）、server-reentrant（双服并存）、server-migrate-resilience（好书+坏书双登记）、rhythm-api（长篇+短篇双轨）、startup-notices（每用例独立起服×3）、io-export-worker、r0912-interrupt-semantics（三书）、r0912-ttl-write-clock（五书）、dual-track、books-delete-events、books-rename、r47/r48（双书）等。
2. **迁移序耦合**（`/api/boot` 即跑 migrateFinalizedRevisions——定稿正文文件 + manifest 动态种子须先于起服落盘，bootStudio 的 files 选项与起服之间无法插 git/manifest 步骤）：rewrite-doc-api、review-verdict-race（主审亲试还原）、review-doc-api、review-verdict-api（MID-2 亲试还原）、batch-finalize、documents-api-w2a（G-1 亲试还原）、finalize-api。**三路独立复现同一定性**（主审 2 + MID-2 2 + G-1 3 共 7 件实测红后 `git checkout` 还原，工作树无半转换残留）。
3. **特型选项/收尾**：static-branch-error 与 r29-server-api-case-prefix 与 r35-api-prefix-404（staticDir 超集 + closeAllConnections 特型收尾）、rag-providers-api（workDir:null 无书库服务）、error-envelope（workDir:null + 自定 token）、r0910-w-shutdown-teardown（每用例独立起服、close 语义即被测面）、r67-server-discipline（userDataPath 嵌套 workDir 内须起服前预建）、r71-books-rename-case / books-delete-clear-warn / books-delete-graveyard 外的 path≠name 登记（backlog-r61e3、prefs-heartbeat-words、cov-server-* 三件 path=`books/名`）、r35-search-cache / r36-24（裸 startServer + doInit 非样板形态）、chat-branches（三书 + 双服）。
4. **构造器/启动语义被测面**：server-timeout（keepAliveTimeout 构造器断言）、api-integration（已收编 bootStudio，保留 1 处合法裸 startServer——host:'0.0.0.0' 须抛「非回环」负路径断言）。

### 4.3 行数与用例账

- **行数**：test 树 126 文件 +1874/−3444（净 −1570）+ 新增 3 helper（studio-server 119 + fake-reqres 39 + fake-driver 26 = +184）= **测试侧净 −1386 行**（用例零漂移下纯样板/重复消除）。
- **用例**：**零漂移**——1095 文件 / 7063 活动用例逐文件计数与基线全同（`vitest list` 对照 5 处文件级差 = 既有平台门跳过用例不收集〔4 win-only + 1 gc〕，账目既有非本批变化）。
- 对原估修账（如实）：L201/L210 的「~2.5–3.5k」系启动样板**毛**估——未扣 helper 自身 +184 行成本与 47 件不可转换面，实收净 −1386（含 makeBook/makeDriver/fakeReqRes 面）落在原估区间下段之下，属预估口径偏宽非执行缩水。
- **§2.3 自由池用例精简：未执行（定性收口）**——勘察结论 1 实锤用例级可删 ≈0–20 且多被锚保护拦下；本批以「用例零漂移」为红线，用例删除属计数变化须 README 修账 + 作者拍板，预期收益 <20 例不成批；如需推进另立（登记 §六）。
- 顺带修正：早期阵亡代理残留 2 处未用 import（r0912-rewrite-draftpath DriverEvent / r40-style-harvest-gate join，typecheck 红——主审清理）；2 处原文件 userDataPath 泄漏（r35-rag-db-corruption / r43-rewrite-code-mapping afterAll 漏清——转换时按 helper 惯例补自建自清）；r31d books.jsonl 无 kind 字段收编归一补 `kind:'long'`（只断言状态码，全绿）。

## 五、L2 终门实测（主审亲跑，2026-09-12）

- vitest 全量（带 coverage）：**1095 文件 = 7063 过 + 5 跳 0 败**（EXIT 0）。
- tsc 0 错 + vue-tsc 0 错 + eslint 0/0（`--max-warnings 0` 全树）。
- 三 check：counts 过（README 声称 1095/7063 与实测一致——零漂移免修账）/ packaging 过 / knowledge 13 条过。
- build:web 过；e2e **43 过 + 2 跳**〔26.2s〕；soak 两段 OK（有界往返 −0.02MB / RAG 召回 +0.05MB，上界 24MB）。
- coverage 对比开工基线：st 91.82 / br 87.39 / fn 96.19 / ln 91.82——**四值逐位相同**（收编对覆盖零扰动实锤）。

## 六、台账销账与移交

| 台账行 | 处置 |
|---|---|
| L181 makeBook 参数化【单立·机械批】 | **部分销账**：scaffoldBook 落地收编 12 文件；studio/ai 域 makeBook 随 bootStudio 内联消解；makeDriver→fake-driver.ts 单源 29 文件；其余域 ~89 份本地拷贝**维持本地**（2–4 行/件低收益 + 清理语义耦合），余量降格【维持】不再单立 |
| L201 G 域 withStudioServer ~118 文件【登记】 | **大部销账**：bootStudio 收编 82 文件 + 47 件 triage 维持（§4.2 四类逐一记因），净 −1386 对毛估修账 |
| L203 fakeReqRes 3 份【原维持】 | **销账**：helpers/fake-reqres.ts 单源 + 3 文件换装（19/19 绿） |
| L209 重评-0912 测试侧 1100–1900【余量单立·随作者指令】 | **部分销账**：本批净 −1386 落区间下段；负窗轮询化未做维持登记 |
| L210 重评-0912-2 测试侧 3.0–4.5k【待拍板】 | **最大头销账**：withStudioServer 收编落地；「r-批次按域归并 ≈0.5–1.0k」维持待拍板（测试组织形态重构，非样板面，红线外） |

- **移交/新登记**：①自由池用例级精简（预期 <20 例，须 README 修账 + 拍板）不随批，如需另立；②三路代理 triage 维持清单（47 件）已固化于 §4.2，后续如重构 bootStudio 选项面（如 books 数组 / staticDir / 迁移序钩子）可按图索骥二次收编。
- 工作树留作者（未 commit）；根 README 计数免修（零漂移自动对账）。
