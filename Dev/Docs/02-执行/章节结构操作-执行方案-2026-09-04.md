# 章节结构操作（合并/拆分/卷重组）-执行方案-2026-09-04

- 日期：2026-09-04 落盘；**2026-09-13 v2 复审修订**。状态：**已实施完成（2026-09-13 全三批 A/B/C 收口，执行记录 = §六；状态正本 = 总览第三节阶段 24 行）**（设计 D1-D7 已于 2026-09-04 拍板；v2 = 对 HEAD `304174b1` 全量对码后的修订版，对码与修订范围见修订记）。
- 路径确认闸：目标路径经 AskUserQuestion 报作者**获批**（2026-09-04：执行方案命名 + 设计稿留 02-执行/ 两项同批确认）；v2 为原地修订（不触发闸）。
- 体例：代码级实施蓝图（现状层核查 + 改动面总表 + 文件级落点 + 测试计划 + 门禁验收 + 执行编排 + 执行记录回填）。**设计正本 = 同目录《章节结构操作-设计方案-2026-08-30.md》**（v3 同批对码修订；留洞制/文件本位原则/D1-D7 全在那边）——本文档只做落点展开，不复制设计论证；口径冲突以设计方案为准（v2 对设计的三处前提勘误已随批并入设计 v3，两文档无冲突挂起项）。
- 溯源：v1——作者指令「可以开始把设计稿转化为执行了」+ 纠偏「我只是让你做执行文档，没让你直接开始做」。v2——作者指令「我们继续讨论阶段 24 章节结构操作……」「你再次评审下这个功能的所有文档，我需要更加完善的计划。」：四路只读核查代理（读取层定位族 / 保存·软删·锁·恢复 / 闸矩阵·事件·AI 保形·RAG / 测试基建·门禁·前端，单波 ≤4）对 HEAD `304174b1` 逐锚对码。本批零代码改动（纯文档）。

## 修订记（2026-09-13 v2；对码基线 HEAD 304174b1）

### A. 锚点漂移勘误表（v1 → HEAD `304174b1`，全部经代理亲读核实）

| v1 锚 | 现锚 | 备注 |
|---|---|---|
| `draft.ts:139` 定稿守卫拦 saveDraft | `src/format/draft.ts:139`（`ensureChapterNotFinalized`，由 `resolveDraftPath` :84 调用） | 文件下沉 format 层；**前提仍成立**（saveDraft 全链生效），行号巧合不变 |
| `documents.ts:326-386` PATCH handlers | `documents.ts:465-542`（op 分派 :489-530） | handler 扩容并入伏笔链 |
| export `:239/:312/:429/:574` | `:274-276`（units 构造）/`:357`（排序）/`:477`（分章前缀 padStart）/`:629`（scanShortCollection） | **「已发布」不入导出**（全文件 0 处；定稿过滤走 finalizedPathSet :287-305）——D7 判据实施路径随之变化（见 B.4） |
| `state.ts:847` .cache 删损兜底 | `src/state/state.ts:1082`（且挂在「无布线书直接扫正文」分支；.cache 读损另有 `fallbackRecapSnapshot` :1104-1113） | 语义家族未变（文件名最大章号做下限），分支归属漂移 |
| `leads.ts:134` future 判定 | `leads.ts:140-148`；future 基准参数实为 `maxWrittenChapter`（`check/run.ts:129` 消费 + `maxWrittenChapterOf` :143-170 预扫：readChapterDir + manifest finalized 过滤取最大定稿章号） | 并入感知修正的落点从「leads 判定行」上移到 `maxWrittenChapterOf`（见 S2 表） |
| frontmatter `:71/:63/:118` | parseValue 整数正则 :73 / splitInlineArray :29（已升级双引号状态机）/ stringifyValue :119 | 语义未变；frontmatter-core.ts 拆分（:23 re-export） |
| RAG 精准读取 `rag/resolve.ts` | `src/process/materials.ts:64-82`（`readChapterBodiesByNumbers`） | rag/resolve.ts 现为 RAG 配置解析（:41-66），与读取无关 |
| 按名定位族含 growth | **growth 已退出**（`checkGrowth` :34-193 纯 db 读，零正文文件）；新增 `summary.ts` 两点（findChapterFile :158-165 + 定稿章识别 :585） | 消费面五处成员更新（见 S2 表） |

### B. 设计前提勘误（随批并入设计稿 v3；D1-D7 拍板零变更）

1. **「伏笔快照先行（Z-P2-6）」从合并管线移除**：现状 Z-P2-6 是 `设定/伏笔/` 路径作用域机制（`documents.ts:486-487` 快照 + :534-536 仅伏笔前缀入链），合并/拆分操作的是 `写作/正文/` 章文件——**根本不触发该机制**，设计 §5.3 把它列为管线第一步系对机制作用域的误读。
2. **崩溃恢复挂点改既有闭环**：startup-notices（`api/startup-notices.ts:33-52`）是 server 生命周期内一次性通告通道（头注明示不落库不重试），不适合书内恢复检查。S5 的 repair 判定改挂 **detectState issues → 态 1 报文 → crashedPendingOpIds/acknowledge 既有闭环**（`state/state.ts:740-775` 三态 + `api/state.ts:113-119/:152-198`），不另起机制、不新建恢复 UI。
3. **`structure.volume-move` 事件取消**：卷重组已降级为批 2 验收项（物理 move 走既有 op=move 端点，零新端点）——该事件无产生点。事件类型收敛为 `structure.merge` / `structure.split` 两枚。
4. **D7 已发布判据实施路径修正**：导出侧现状不读 fm `已发布`（fm 键只进树/状态链：`tree.ts` probe :290-299、`document/status.ts:54-68`）。D7 分流的 published 判定 = units 组装时从 readChapterDir meta 的 `_raw.已发布` 解析（readChapter 将其容错落 `_raw`——**合并/拆分重写目标章 fm 时必须保形 `_raw` 既有键行**，含 `已发布`）。

### C. 新增集成点（2026-09-04 落盘后代码新增，v1 无从覆盖；全部为 v2 新增设计内容）

1. **合并/拆分写入通道 = `svc.save` origin `'external-merge'`（现成）**：executeSave（`service.ts:306-701`）全程无 finalize 守卫、可写定稿章；origin 白名单已含 `external-merge`（`documents.ts:659`）且该 origin **强制留底快照**（maybeSnapshot :719-737）——「专用通道」不需要新建原语，直接复用；**禁止绕开它裸 atomicWriteFile**（会失去保存锁/锁内复核 R76-22/journal 闭环三件套）。
2. **per-book 串行链 + 排水第 5 drain（重评-0912-4 幽灵目录同型教训）**：structure 端点收编 per-book 串行链（照 `api/draft.ts:51-84` draftSaveChains 范式：链 key=书根、单元首行 `bookMovedFailure` 单源重验 `book-context.ts:56-66`）；`books.ts` 删书/改名排水段（:408-424/:656-669，现为四个 drain）追加 `drainStructureChainsUnder` 第 5 调用——否则在途合并跨墓地 rename 会按旧书根重建幽灵目录树。
3. **互斥面清单（apply/undo 双向）**：入口实序照 `rewrite.ts:82-107` 样板——`resolveBook` → `isSelfHealRunning` → `isSpawnRunning` → `orchestrationBusyFor`（`task-gate.ts:228-238`，现含 self-heal/chat/spawn/background 四面）→ `isReviewRunningForBook`（改写正文端点加，`stream.ts:667` spawn 先例）→ `acquireTaskGate(name,'structure')`。KNOWN_ACTIONS 17→18（`task-gate.ts:161-179`，R77-2 对账门 `test/governance/known-actions-audit.test.ts` 自动锁字面量）+ busyGate 注册清单（task-gate.ts:157，使删书/改名复查 :436/:677 看得见）。**反向零接线**（chat.send/auto-write/spawn/chat.clear/删书 busyGate 查 `allHeldTaskGatesFor` 全集，新 action 自动生效）。plan 干跑只读：只走 resolveBook + orchestrationBusyFor，不占闸。
4. **非 UTF-8 分诊（重评-0912-4 P1-1 家族）**：干跑预检双方/三方正文 `encodingSuspect`（GET /file 既有探测口径）；apply 侧 executeSave :494-500 非 UTF-8 拒天然 fail-closed 兜底。
5. **RAG 清理函数定死（v1「实施首步定位」已偿）**：`openRagDb` → 源章号 `deleteChunksByChapter(db,n)`（store.ts:606）+ `deleteRagMeta(db, chapterHashKey(n))` 成对、事务包裹（`rag/index.ts:407-409` 同款先例）；目标章只需 `deleteRagMeta(chapterHashKey(target))` 即变 missingFingerprint。**best-effort 不阻断主流程**（失败 warn）：自愈兜底 = 下轮 buildIndex（已删章残留清理 :393-426 + stale 指纹重嵌 :439-457，源章文件已删即被自动清理）。
6. **干跑引文预演复用导出纯函数**：`leadEvidenceMatchesBody(body, evidence)`（`check/lead-updates.ts:226`，导出纯函数）+ `evidenceNeedles`（`check/leads.ts:345`）——对合并后正文串逐条调用，无需改整书检查入口（finalize.ts:350 / run.ts:301 已是复用先例）。
7. **undo 摘 `并入` 不需要扩 `patchFlatFm` 删键**（v1 未察的缺口，v2 以顺序优化消解）：patchFlatFm（`frontmatter.ts:272-345`）只能替换/追加、无删键——undo 顺序改为 **①目标章版本回滚**（snapshots restore `snapshots.ts:477-523`，走 svc.save origin `'restore'` 强制留底——回滚前当前内容自动留底不丢；**fm 随内容整体回滚，`并入` 键自然消失**）→ **②restoreTrash 还源章**（`trash.ts:276-391`）。该顺序下中途态**永不违反崩溃不变量**（先回滚时源章仍在回收站无违反；还原后 `并入` 已摘无违反），且「合并后作者新修改」不丢（留底成版本）。
8. **undo 定位主路径 = 事件载荷**：`structure.merge` 事件携带 `{ targetDocId, sourceDocId, sourceChapterNo, trashEntryId, rollbackSnapshotId, planHash }`——undo 直查最近 merge 事件取三 id；降级路径 = 回收站清单按 originalPath 反查 + 目标章版本时间线推演 + fm `并入` 数组含源章号校验。
9. **保形透传精确落点**：`assembleChapter`（`ai/contract/chapter.ts:90-127`）签名无路径入参——最短实现 = 调用方 `self-heal.ts:998`（持 bookRoot）组装前读盘上既有章 fm 取 `序`/`并入` 注入（`并入` 写侧数组形态）；saveDraft 回补校验落 `draft-pipeline.ts:198-215` 锁内写盘前段。
10. **skipFinalizedChapters（CC-P1-6 篇号永不复用）**：state 下章号 = max+1 后再 `skipFinalizedChapters`（`state.ts:275/:310/:944-948`）——拆分取号照抄同语义（**不能复用已定稿章号**，留洞制下合并产生空洞时尤须如此）；回退 helper 不动 state（零改动确认维持）。

## 一、现状层核查（v1 三项结论维持 + v2 增核四项；file:line 实证）

1. **parseFlat 小数口径（`序: 12.5`）**：`parseValue` 只认纯整数为 number（`frontmatter.ts:73` `/^-?\d+$/`），小数落字符串。读侧强转收编（number/数字串双认 + `Number.isFinite && > 0` 校验，非法按缺省）；写侧 `stringifyValue`（:119）number 原样 `String(val)` 落 `12.5`——写读闭环成立，**不动 parseValue**。（v1 结论不变。）
2. **`并入` 多值分隔符 = 逗号**：规范写形态 = 数组（`stringifyValue` 渲染 `并入: [12, 13]`）；读侧三形态兼容归一 `number[]`（number / 字符串 `,`/`、` 双认切分 / 数组逐项强转）。（v1 结论不变。）
3. **投稿导出章号来源**：长篇分章 = fm 章号全链（units `:274-276` → 排序 `:357` → 分章前缀 `:477` padStart(4)）；投稿视图短篇专用（`:629`）；全本 merged 无章号呈现。**D7 落点两处**：分章文件名前缀（displayNum）+ 全链排序键。（v1 结论不变，锚点更新；:477 内联 padStart 未走 `chapterFilePrefix`（`words.ts:79-81`）单源——S2 顺手收编。）
4. **【v2】定稿章写入通道**：executeSave 可写定稿章（全文无 finalize 守卫）；saveDraft 对定稿目标必被 `format/draft.ts:139` 拦——「executeSave 语义专用通道」= `svc.save` origin `'external-merge'`，**无需新建原语**（见 C.1）。
5. **【v2】undo 三步齐备度**：还原回收站（restoreTrash :276-391，TrashEntry 含 finalized 基线/tags/order）**齐备**；摘 `并入`（按 C.7 顺序优化，版本回滚天然完成，patchFlatFm 维持现状）**齐备**；目标章版本回滚（`snapshots.ts:477-523`，origin 'restore' 字节保真 + 强制留底）**齐备**。
6. **【v2】事件通道**：EventType 现存 24 个（`events/types.ts:15-45`），无任何 structure/move/trash 类——事件副录为纯新增；写入通道 = chain-bridge 构造器（照 :103 foreshadowChangeEvent 形）+ `openSessionStoreAsync → appendEvents(workspaceSession(bookHash))`（:185 先例）；只做审计副录、**不进 SURFACE_EVENT_TYPES**（:82，投影零改动）。
7. **【v2】导出「已发布」现状**：0 处消费（见 B.4）；树侧 probe 已带出 published（`tree.ts:290-299`）——D7 分流判据 = readChapterDir `_raw.已发布` 解析（与 probe 正则同式，实施时抽共享小函数）。

## 二、改动面总表（S2→S5 × 文件 × 动作）

### S2 读取层接线（改读取不改数据；旧书零迁移——无 `序`/`并入` 行为 = 现状）

| 文件 | 动作 |
|---|---|
| `src/format/types.ts` | ChapterMeta（:136-159）增可选字段 `序?: number`、`并入?: number[]` |
| `src/format/chapters.ts` | KNOWN_FM_KEYS（:21）扩 `序`/`并入`；readChapter（:28）解析两键（`parseOrderOf`/`parseMergedInto` 归一小函数供 tree probe 复用）；**`_raw` 保形注记**：解析两键后 `_raw` 不再含它们，`已发布` 仍留 `_raw`——fm 重组侧必须整体保形（见 S3） |
| `src/format/chapter-lookup.ts`（新） | 章号回退 helper 单源：`chapterTextByNumber(bookRoot, n)` + `mergedIntoMap(bookRoot)`（`并入` Map 含折叠链；readChapterDir meta-only 一遍扫描，按需构建 + 目录 (mtimeNs,size) 指纹缓存——实施首步核实 readChapterDir 既有缓存形态，无则加） |
| `src/document/tree.ts` | probeFile（:268-307，已同出 rev/wordCount/published）增 `order` 解析（regex 同 parsePublishedValue :313-323 式，复用已读字节零额外读）；buildTree annotate pass（:180-213）后加 `sortTreeByOrder`（仅 `写作/正文` 子树 chapter 角色按 `序 ?? 文件名章号` 重排，tie-break 章号 → path，稳定排序） |
| `src/check/run.ts` | `maxWrittenChapterOf`（:143-170）**并入感知**：基准 = max(现值, `并入` Map 在档最大**源**章号)——堵「合并最高定稿章后源章文件/manifest 均已摘 → 预扫扫不出源章号 → 既有履历行被判 lead-chapter-future 假红」（leads.ts:140-148 消费参数不变；.cache 删损重建场景 state.ts:1082 兜底同族覆盖） |
| `src/export/index.ts` | units 组装（:274-276）增 `sortKey`（`序 ?? 章号`）与 `published` 判定（`_raw.已发布` 解析，B.4）；排序（:357）换 sortKey；分章前缀（:477）换 displayNum（D7 分流：已发布章固定本地章号，其后未发布段从「已发布最大章号+1」连续编——v1 拍板口径不变）+ **顺手收编 `chapterFilePrefix` 单源**（替内联 padStart）；全本/投稿视图零改动 |
| 消费面五处接线（成员更新） | leads `chapterPathOf`（:96-108）/ 伏笔 `collectChapterTexts`（:460/:537）/ **materials `readChapterBodiesByNumbers`（:64-82，替代原 rag/resolve.ts 位）** / **summary findChapterFile（:158-165）+ 定稿章识别（:585）（新增成员）** / prepare `findChapterByNumber`（:36-73）——正文按名定位 miss 时走 chapterTextByNumber 回退；growth 已退出（纯 db）不接；**prepare 顺手把窄正则 `parseChapterFileName`（words.ts:61-70）统一到 `chapterNoFromName`（filename.ts:137-140）宽容集**（`5—标题.md` 形态对 AI 前章读取不再失明，加回归用例） |

### S3 合并流程

| 文件 | 动作 |
|---|---|
| `src/document/structure.ts`（新） | ① 干跑预览：双方/三方正文 `encodingSuspect` 预检（C.4）+ 正文拼接摘要 + 受影响消费面清单 + **履历引文命中率预演**（`leadEvidenceMatchesBody` 对拼接正文逐条调，C.6）+ RAG 将清除 chunk 数预估 + plan 指纹（参数 + 双方 fm/正文哈希，apply 复核防 TOCTOU）；② 合并执行编排（见 §三.8 锁序）：**链首 bookMovedFailure 重验（C.2）→ task-gate 'structure' → ①目标章正文并入**（svc.save origin `'external-merge'`：新 fm = patchFlatFm(原 fm, {并入: [...]})——**其余键行逐字节保形含 `_raw` 已发布** + 拼接正文（canonicalize 口径见 §三.11）；`并入` 与正文同笔原子写 + 留底版本即 rollbackSnapshotId）**→ ②源章软删**（`svc.trashDocument` 既有管线：自取源 docId save 锁 + trashBaselineOf RMW 基线落账，零新代码）→ ③事件副录 `structure.merge`（载荷见 C.8）→ ④RAG 清理 best-effort（C.5）→ ⑤缓存失效（tree-issues/书摘要，structural）③ 撤销合并一键（C.7/C.8：版本回滚 → restoreTrash → 事件 `structure.merge-undo`） |
| `src/studio/server/api/documents.ts` | 三端点：`POST /documents/:docId/structure-plan`（干跑，不占闸）/ `structure-apply`（携 plan 指纹执行，op=merge/split）/ `merge-undo`；互斥清单照 rewrite.ts:82-107 实序（C.3）；**per-book structure 串行链**（照 draft.ts:51-84 范式 + 测试钩子） |
| `src/studio/server/api/draft.ts` 或 documents.ts 内 | `drainStructureChainsUnder` 导出（判式照 draft.ts:72-84：恰等于书根 + realpath 双口径） |
| `src/studio/server/api/books.ts` | 删书（:408-424）/改名（:656-669）排水段追加第 5 个 drain 调用（C.2） |
| `src/studio/server/api/task-gate.ts` | KNOWN_ACTIONS 扩 `'structure'`（17→18）+ busyGate 注册清单（:157） |
| `src/events/types.ts` + `src/ai/orchestrate/chain-bridge.ts` | EventType 增 `structure.merge` / `structure.split` / `structure.merge-undo`（审计副录；volume-move 取消见 B.3）+ 构造器（照 :103 形） |
| `src/rag/*`（store.ts / index.ts） | 合并清理：openRagDb → 源章 `deleteChunksByChapter` + `deleteRagMeta(chapterHashKey)` 成对事务；目标章 `deleteRagMeta` 失效（C.5，函数已定死非「实施首步定位」） |
| `src/ai/orchestrate/self-heal.ts` + `src/ai/contract/chapter.ts` | **结构键保形**：调用方 self-heal.ts:998 组装前读盘上既有 `序`/`并入` 强制透传（C.9；assembleChapter 签名不动或扩 preserveFm 入参，实施取短） |
| `src/process/draft-pipeline.ts` | saveDraft 回补校验（`序`/`并入` 不丢；落 :198-215 锁内写盘前段，C.9） |
| `src/document/service.ts` | updateChapterMeta/updateDocMeta 保形**核查项**：patchFlatFm 只改目标键、其余行逐字节保留——预期天然保形，测试锁定即可（零改动） |
| 前端（web-next） | `api/documents.ts` 增三调用（apiJson json 选项，apply/undo 显式 timeoutMs——batchFinalize 120s 先例 :213-222）；`useTreeMenu.ts`（:52-130）增动作 `merge-into-prev`（并入上一章——**按 `序` 显示序找前一章，非章号−1**）/ `merge-undo`；干跑确认弹窗仿 `ui.ask`（ConfirmPrompt + stores/ui.ts:125-143，引文命中率预演列表为增强区）；动作前 `waitInflightSave` + 复查 dirty（照 doDelete 范式 `useChapterTreeActions.ts:438-479`） |

### S4 拆分流程

| 文件 | 动作 |
|---|---|
| `src/document/structure.ts` | op=split：新章号 = 全书 max+1 **再 skipFinalizedChapters**（与 state :275/:310 同语义，C.10）；新章 fm `序` = 拆分点两侧有效序中值；**新章标题必填**（拆分弹窗入参，文件名走 sanitizeFileNamePart/chapterFilePrefix 单源；checkNewNames 不咬文件名/标题——只扫正文引号 span，count.ts:372-480 已核）；原章截断走 svc.save（自然留版本）；事件 `structure.split`；**已发布段提示不硬拦**（干跑对 fm `已发布` 章提示「平台连载无插入机制」，作者确认继续——作者即唯一用户原则）；缓存失效 |
| `src/studio/web-next/src/editor/CmHost.vue` + `views/EditorView.vue` + `stores/workspace.ts` | **getCursorOffset 暴露**（`view.state.selection.main.head`——现状 defineExpose :538 只有 getSelection（:466-471 空选区空串），无光标 offset）；CmHostExposed（:151-163）与 workspace 注册（:392-394 同 setEditorGetSelection 式）同步扩 |
| 前端（web-next） | 拆分弹窗（光标处拆分 + 新章标题必填 + 干跑确认）；树右键动作 `split-here`（对当前打开章生效时走编辑器光标，未打开章走章尾拆分——实施时定，倾向只对打开章开放） |

### S5 崩溃恢复（挂接既有闭环，不另起炉灶——B.2）

| 文件 | 动作 |
|---|---|
| `src/document/structure.ts` | repair 判定式：崩溃不变量（**`并入` 所指章不得存活于正文**）比对盘面（正文/回收站/fm 并入）——violation 即未完成态：**幂等续跑**（①后崩溃：重跑 apply 即收敛）或**整体回退**（走 merge-undo）。挂点 = detectState 书内检查 → 态 1 报文 issue（照 crashedWrite 样式带 opId + acknowledge 语义），**不用 startup-notices**（server 生命周期一次性通道）、不新建恢复 UI |
| 既有闭环继承（零新代码） | 目标章写入崩溃窗 = executeSave pending/settled + reconcileSavePending 三态自愈（含 queryLockHeld 在持探测）；源章软删半途态 = 回收站既有无害态（登记成功/rename 失败孤儿条目）+ Z-6 复活守卫 + restore 续跑（R65-36 字节一致放行）；undo 顺序（C.7）保证中途态永不违反不变量 |
| 测试 | 崩溃注入两形态（见 §四）：①「①②间 kill」确定性窗口用 fakeReqRes 悬停复现；②盘面终态（`并入` 指向存活源章 / 回收站孤儿条目）用 scaffoldBook 直接构造后直调 repair 断言收敛 |

## 三、文件级落点明细（关键函数级说明；1-7 为 v1 条目锚点更新，8-11 为 v2 新增）

1. **readChapter（chapters.ts:28）**：`map.get('序')` → `parseOrderOf`（number 直取；字符串 trim 后 `Number()` 强转；`Number.isFinite && > 0` 否则 undefined——缺省语义不报错）；`map.get('并入')` → `parseMergedInto`（number → [n]；字符串按 `,`/`、` 切分逐项强转正整数、非法项丢弃全空则 undefined；数组逐项同校验）。两键入 KNOWN_FM_KEYS 后不再落 `_raw`。
2. **chapter-lookup.ts（新）**：`mergedIntoMap(bookRoot)` = readChapterDir(正文目录) meta-only 一遍 → Map<源章号, 目标章 path>（每章 `并入` 数组逐项登记；折叠链写侧单跳化——11 并 12,13 时源 13 直接重指向 11，读侧无递归）；`chapterTextByNumber(bookRoot, n)` = 既有按名定位（chapterNamePrefixes 三口径，chapters.ts:150-152）命中优先 → miss 查 Map → 目标章 body → 再 miss 返 null（调用方既有容错）。**跨卷重号 warn 先例**（foreshadow.ts:556-558）对齐：Map 构建遇重章号 warn 不炸。
3. **tree.ts sortTreeByOrder**：annotate 后遍历，仅 `写作/正文` 前缀目录 children 重排（其余目录维持 localeCompare 现状 :130）；chapter 节点 key = probe.order ?? chapterNoOf(name)（probe miss 回落文件名章号），非数字前缀文件不参与重排保持原相对位；probe.order 来自 probeFile 新增 `序` 行 regex 解析（复用已读字节）。
4. **run.ts maxWrittenChapterOf 并入感知**：预扫值与 `mergedIntoMap` 在档最大源章号取 max——单点，其余检查器零改动（leads.ts:140-148 判定式消费参数不变）。
5. **export/index.ts**：units 组装带出 `ch.序`（readChapterDir 路径已含）+ published（`_raw.已发布` 解析）；`sortKey = 序 ?? 章号`；displayNum 分流（已发布 = 本地章号；未发布段从已发布最大章号+1 连续编——v1 拍板口径）；分章前缀 padStart(displayNum) 走 `chapterFilePrefix` 单源；全本与 warnings 文案章号引用维持本地章号。
6. **structure-plan 干跑**：对 merge 返回——目标/源章 meta、encodingSuspect 预检、正文拼接摘要（源章字数/段落 previews）、履历引文命中预演（evidenceNeedles + leadEvidenceMatchesBody 对拼接正文）、RAG 清除 chunk 数预估、受影响消费面清单、plan 指纹。对 split 返回——新章号/序中值预览、原章截断摘要、已发布提示。
7. **六态回落（合并/拆分定稿章）**：svc.save 写完 → 哈希与 manifest finalizedRevision 失配 → deriveStatus 回落草稿——**预期行为**：须作者重审重新定稿（重走三检/防吃书闸）；窗口内「只导定稿」不含该章、D7 已发布判定回落，均属预期（验收覆盖）。**v2 补**：拆分原章截断同后果（原章回落草稿 + 新章草稿），验收同覆盖。
8. **合并编排锁序（v2 新增，重要）**：**不自造并行持双 docId 锁的编排**——两步顺序执行各取各放：目标章写入（svc.save 自取目标 docId save 锁）→ 源章软删（trashDocument 自取源 docId save 锁 + 内嵌清单/trash RMW），天然无环；全仓单向序 save → 布线 → journal → 主清单 → trash 不变。per-book structure 链保证同书合并串行、与 draft-save/file-put 链的排水互斥由 books.ts drain 族收口。
9. **undo 编排（v2 定序）**：task-gate 'structure' → 定位（C.8 事件主路径/降级推演）→ ①目标章 snapshots restore（rollbackSnapshotId；当前内容先留底）→ ②restoreTrash（trashEntryId；OCCUPIED 干跑预检）→ ③事件 merge-undo。三步各取各锁顺序执行。
10. **RAG 清理 best-effort**：openRagDb/事务失败 warn 不阻断合并主流程（RAG 是投影非权威源）——自愈兜底 = 下轮 buildIndex 已删章清理 + stale 指纹。
11. **拼接规范形**：合并正文 = canonicalize(目标正文) + 章节分隔（空行，实施时与 canonicalizeText :141 口径对齐）+ canonicalize(源正文)——与 save 链规范形一致，避免拼接缝产生机检伪红。

## 四、测试计划（v2 重写：bootStudio/scaffoldBook 组合 + L2 终门）

| 测试文件 | 组合 | 覆盖 |
|---|---|---|
| `test/format/chapter-structure-fm.test.ts` | scaffoldBook 纯逻辑 | 序/并入 解析矩阵：number/数字串/小数串/顿号/数组/引号串/非法值缺省/旧书无键零迁移；`_raw.已发布` 保形 |
| `test/format/chapter-lookup.test.ts` | scaffoldBook | 回退：正文命中优先（不咨询 Map）/折叠链单跳/Map miss 返 null/回收站还原后正文命中优先于陈旧并入/跨卷重号 warn |
| `test/document/tree-order.test.ts` | scaffoldBook | 树重排：序 缺省=章号（现状不变）/序 越卷重排/tie-break 章号→path/非正文目录不受影响 |
| `test/check/leads-merge-aware.test.ts` | scaffoldBook + makeGitBookWithChapters | **缓存重建假红回归**：合并最高定稿章后（源章摘除+并入登记）→ maxWrittenChapterOf 并入感知 → lead-chapter-future 零假红；.cache 删损重建形态（fallbackRecapSnapshot 路径）同覆盖 |
| `test/export/export-order.test.ts` | scaffoldBook | sortKey 排序 + D7 分流（已发布固定/未发布段连续编）+ 全本保留本地章号 + chapterFilePrefix 单源收编 |
| `test/document/structure-merge.test.ts` | **bootStudio**（端点级） | 端到端合并：干跑指纹 TOCTOU 拒绝/svc.save 通道（定稿目标章可写）/并入 同笔/fm `_raw` 保形/软删登记（TrashEntry 基线）/事件副录（载荷含 trashEntryId+rollbackSnapshotId）/RAG 清理（deleteChunksByChapter 断言）/撤销一键（版本回滚摘键 + 还原 + 留底不丢）/**六态回落流**（回落→重定稿→导出恢复）/GBK 目标章 400 拒（C.4） |
| `test/document/structure-split.test.ts` | **bootStudio** | max+1 取号 + skipFinalizedChapters（不复用定稿号）/序中值/原章截断留版本/新章标题必填/已发布段提示不硬拦/六态回落（原章） |
| `test/document/structure-crash.test.ts` | **两形态**：fakeReqRes 悬停（withRouteTable + getRouteSchema('books.documents.structure-apply')，r1010b 样板 r1010b-srv-documents-bookmoved.test.ts:110-134）+ scaffoldBook 终态构造 | ①后 kill（并入指向存活源章→幂等续跑）/②半途（回收站孤儿条目无害态）/undo 中途态零违反/repair 收敛终态一致/detectState issue + acknowledge 语义 |
| `test/studio/orchestrator-mutex-gates.test.ts`（并入既有） | bootStudio | **structure 闸互斥角**：structure-apply 在持 → /chat、/auto-write、/spawn 409；chat 在途 → structure-apply 409；self-heal 在途 → 409；review 在途 → 409（isReviewRunningForBook 面） |
| `test/governance/known-actions-audit.test.ts` | 零改动自动锁 | KNOWN_ACTIONS 扩 'structure' 后对账门自动覆盖（R77-2 字面量纪律） |
| `test/studio/structure-chain-drain.test.ts` | bootStudio | per-book structure 链 + drainStructureChainsUnder：删书排水等待链收尾零幽灵目录（r0912-4-draft-save-drain.test.ts 同款范式） |
| `test/ai/`（并入既有 contract 文件） | makeFakeDriver | **结构键保形回归**：assembleChapter 读盘透传 序/并入；saveDraft 回补；updateChapterMeta 不摘键 |
| `test/studio/webnext/`（前端新增） | mount 仿 chapter-meta-dialog.test.ts:18-26 | 拆分弹窗（标题必填校验）/干跑确认弹窗（预演列表渲染）/useTreeMenu 新动作构建/api/documents.ts 三调用（apiJson 载荷）/CmHost getCursorOffset 暴露 |
| `test/e2e/structure-ops.spec.ts`（新） | 共享 workDir（不需独立端口） | 右键合并/拆分/撤销 + 干跑确认 `.cp-modal` 动线（手法抄 tree-ops.spec.ts:27-49）；**必接 attachPageErrorBaseline + 至少一处 expect + 插序确认后两步闸重拍 spec-order.snapshot.txt** |

**门禁（L2 终门九件套，收口一次摊销）**：① `npm run test:coverage`（vitest 全量 + 记 coverage 四值）② `npm run typecheck` ③ `npm run typecheck:web-next` ④ `npm run lint`（--max-warnings 0）⑤ `npm run check:counts` ⑥ `npm run check:knowledge` ⑦ `npm run check:packaging` ⑧ `npm run test:e2e`（+ `test:e2e:release` 视改动面）⑨ `npm run soak`（断言恰 2 行 `[soak] OK`）。日常 L1：`npm run test:related -- <改动文件>` + typecheck（动前端加 web-next）。

**README 修账点（check-counts 锚定 9 处 :527-549）**：新增单测文件 → badge（:8）/npm test（:101）/技术栈（:121）/状态段文件数与单测数（:117）；新增 e2e spec → spec 数/用例数（:121/:117 两处 + 开发节 :537-546）+「29 个 spec」表述改写 + 快照重拍。**win 差值锚 79 不动**（结构操作纯 fs 逻辑，不加平台门——若确需 `skipIf(win32)` 必须同步改「实测差 N 恒定」口径，否则 win 腿 check:counts 红）。

**coverage 注意**：`src/document/structure.ts`（新）落主桶（阈值 89/83/95/89，vitest.config.ts:106-157）——新文件低覆盖直接稀释均值，必须同批带足直测；documents.ts 端点改动落 studio/server 桶（branches 门 76 最紧）+ 主桶；前端 api/documents.ts 落 api 桶（ln87/br93）。

**命名锚**：功能批，沿用描述性名（structure-*）批内统一（对齐 v1 清单；r 前缀锚是评审修复批惯例，不强制）。

## 五、执行编排（v2 新增）

- **批次切分**（建议三批，可按作者指令并批）：
  - **批 A = S2 读取层**（独立先行）：fm 两键解析 + 回退 helper + 树/导出排序 + leads 并入感知 + prepare 宽容集统一——改读取不改数据、旧书零迁移、无端点无 UI，风险最低，先行合入即部分兑现价值（`序` 键可用于手工标注重排）。
  - **批 B = S3+S4 主体**（S4 复用 S3 基建）：structure.ts 编排 + 三端点 + 互斥矩阵 + 事件 + RAG 清理 + 保形三路径 + 前端（树菜单/弹窗/CmHost/拆分）。
  - **批 C = S5 收尾**：repair 挂 detectState + 崩溃注入回归 + 卷重组验收项（跨卷 move 全链核对 + e2e 1 spec，批 1 降级项在此收口）。
- **编排纪律**：子代理单波 ≤4（作者纪律条）；批内按文件互斥分路（structure.ts/api 层/前端/测试四域天然互斥）；主审逐 diff 复核。
- **风险登记**：
  1. patchFlatFm 保形（S3）——fm 重组动 format 层公共原语调用面，回归面 = 全部 fm 写路径（updateChapterMeta/updateDocMeta 既有测试锚逐字节比对）；
  2. sortTreeByOrder 时机（annotate 后）——树排序变更影响前端树渲染序与 e2e 快照，需全量 e2e 验证；
  3. mergedIntoMap 扫描成本——大书（200 万字/千章）meta-only 扫描按需 + 指纹缓存，勿在热路径每请求重建；
  4. coverage 主桶稀释——structure.ts 同批直测足量（§四）；
  5. prepare 宽容集统一（S2）——行为扩展（`5—标题.md` 等形态从失明变可见），加回归用例双向锚定。

## 六、执行记录（开工后回填）

### 批 A（S2 读取层）——2026-09-13 实施，L2 终门全绿

- **范围**：S2 全项（§二 S2 表）——ChapterMeta 增 `序`/`并入` 可选字段（types.ts）；KNOWN_FM_KEYS 12 键 + readChapter 解析（chapters.ts）；`parseOrderOf`/`parseMergedInto`/`isPublishedValue` 归一小函数（probe 与导出复用）；chapter-lookup.ts 新建（`mergedIntoMap` 折叠链单跳 / `chapterTextByNumber` 回退 / `chapterPathByNumber` / `registerMergedInto` 登记 warn 单源）；tree.ts probe 增 order + `sortTreeByOrder`（annotate 后、仅 `写作/正文` 子树、tie-break 章号→path、非章文件沉底保原相对序）；run.ts `maxWrittenChapterOf` 并入感知（max(定稿基准, 并入 在档最大源章号)）；export units 增 sortKey/published（`_raw.已发布` 解析）+ 排序换 sortKey + D7 displayNum 分流（已发布固定本地章号、未发布段从已发布最大+1 连续编）+ 分章前缀收编 `chapterFilePrefix` 单源；消费面五处接线（leads `chapterTextOf` / 伏笔 collectChapterTexts 同步+异步孪生 / materials `readChapterBodiesByNumbers` / summary `findChapterFile` / prepare `findChapterByNumber`）+ prepare 宽容集统一 `chapterNoFromName`。
- **实施实抓（如实记档）**：伏笔足迹回退首版直调 `mergedIntoMap`（readChapterDir meta 全扫），`r66-foreshadow-chapter-cache` 回归当场红（首扫每章多付一次整读，破坏 R66-6「二扫零重读」指纹缓存契约）——修正为 walk 内「同一次读取顺带解析」（`collectMergedFromRaw`：readMdTextCached 同份原文 parseFlat+parseMergedInto+`registerMergedInto`，零额外 IO），mergedIntoMap 保留给按名 miss 后惰性回退面（leads/materials/summary/prepare；materials 循环内建 Map 提升到循环外一次）；§五 风险登记 3「勿在热路径每请求重建」由此落成两条纪律（惰性 miss 触发 / 同读顺带解析）写进 chapter-lookup 头注。
- **测试**：新增 6 文件 48 用例（`test/format/chapter-structure-fm` ×18 / `chapter-lookup` ×10 / `test/document/tree-order` ×7 / `test/check/leads-merge-aware` ×2〔含「无并入登记仍产 future 红」感知面不越界对照〕/ `test/export/export-order` ×6 / `test/process/prepare-locate` ×5〔宽容集双向锚定 = 风险 5 处置〕）；未加平台门，win 差值锚 79 不动。
- **L2 终门九件套（主审亲跑全绿）**：vitest **1105 文件 = 7132 过 + 5 跳 0 败**（基线 1099/7084 → 净增 6 文件/48 用例，全 structure 描述性命名）+ coverage 四值 **st 91.87 / br 87.39 / fn 96.30 / ln 91.87**（主桶门 89/83/95/89 全过；st/ln 较基线 91.88 微降 0.01 = chapter-lookup.ts 新文件稀释，fn 微升）+ tsc/vue-tsc 0 错 + eslint 0/0（--max-warnings 0）+ check:counts 过（README 修账五处：徽章/npm test/状态段 1099→1105 与 7084→7132/技术栈 + win 预期行 1105 文件/7053 过+84 跳）+ check:knowledge 过（13 条）+ check:packaging 过 + e2e 43 过 2 跳〔28.9s；无 序 键旧书排序零漂移，spec 顺序快照零改动免重拍〕+ soak 两段 OK。
- **改动面**：src 10 文件修改（+272/−25）+ 新增 src/format/chapter-lookup.ts + test 新增 6 件 + 根 README 修账 + 本文档 §六回填 + 总览阶段 24 行推进 + Archive/README 批记行。工作树留作者（未提交；README.md/package.json 的 test:related 脚本为作者既有改动，原样保留）。

### 批 B（S3+S4 主体）——2026-09-13 实施，L2 终门全绿

- **范围**：S3+S4 全项（§二 S3/S4 表）——**服务端**：`src/document/structure.ts` 新建（干跑预览/合并执行/撤销合并/拆分干跑/拆分执行五函数 + `foldMergedInto` 链式折叠单跳化 + planHash TOCTOU 复核 + 幂等续跑〔并入 在档 + 回收站在档 → 跳写入直续收尾；回收站无条目 = 人工处置 → NOT_MERGE_STATE 拒〕+ `skipFinalized` 取号 + `splitOrderMid` 序中值 + 事件副录三族）；documents.ts 三端点（structure-plan 干跑不占闸 / structure-apply 五重入口实序 + 任务闸 'structure' + per-book 串行链〔draftSaveChains 同款范式 + 链内 bookMovedFailure 单源重验〕/ merge-undo 恒发 {}）；books.ts 删书排水第 5 drain `drainStructureChainsUnder`（幽灵目录同型防线）；task-gate.ts 'structure' 入 KNOWN_ACTIONS（17→18，R77-2 对账门自动锁）；events 三载荷（structure.merge/split/merge-undo，merge 携 trashEntryId+rollbackSnapshotId）；RAG 清理（源章 deleteChunksByChapter + chapter_hash 指纹成对、目标章仅指纹失效、ragDbExists 守卫）；保形三路径（self-heal assembleChapter 透传 / saveDraft 锁内 `preserveStructureFmIn` 回补〔chapter-lookup.ts 扩〕/ updateChapterMeta 零改动）。**前端**：api 三调用（plan/apply 120s 慢档/undo）；useTreeMenu 三动作（并入上一章按显示序 prevBodyChapterInDisplayOrder〔跨卷非章号−1〕/撤销并入恒显〔服务端 NOT_MERGE_STATE 是唯一真相〕/在光标处拆分仅打开章）；doMergeIntoPrev 干跑→ui.ask（.cp-modal 多行 message：missCount 预演/RAG 预估/拼接预览）→apply→弃缓存刷树重对齐；doSplitHere 光标偏移换算全文坐标（splitFrontmatter 同源 bodyStart）；SplitChapterDialog 新建（干跑视图 + 标题必填 + IME/Enter 让渡，照 ChapterMetaDialog 形态）；CmHost getCursorOffset + EditorView 双注册双注销；ConfirmPrompt `.cp-body` pre-line（既有单行零扰动）。
- **编排**：主审亲写核心两件端点测试（structure-merge ×13 / structure-split ×10：干跑基本面/GBK encodingSuspect 字节逐位不变/执行拼接保形/链式折叠/TOCTOU/定稿回落/幂等续跑〔真合并后放回源文件 + 清单补登构造〕/undo 双路径/RAG 双章三清理/光标三形态/skipFinalized/序中值/已发布警示）；三路文件互斥测试代理（C 链排水+互斥补角 / D 保形三路径 / E 前端 mount）+ e2e 路代理（structure-ops.spec ×3 + 快照两步闸重拍），全程在途 ≤4，主审逐 diff 复核（C 的「删书入口 busyGate 409 拒收」按实际实现锚定裁决成立——structure-apply 全程持任务闸与 draft-save 无闸不同，测试头注如实记档）。
- **实施实抓（如实记档）**：① **G5 依赖方向守护拦 structure.ts 直 import rag**（document 底座 → rag 生成层违 EDITOR_BASE/AI_LAYER 分界，KNOWN 白名单 E2 治理后清零态不回填）→ **端口注入反转**：`StructureRagPort` 接口（estimateRagChunkCount/cleanupRagAfterMerge 两方法）+ 四导出函数签名显式收 `rag` 参数（undo 的 rag 置于可选 hints 之前过 TS1016），api 层注入 rag/index 实现——零白名单回填、零文件位移、落点不动，等价性由端点级 RAG 用例钉定。② **R77-2 KNOWN_ACTIONS 对账门 19≠20 假红**：注释里 `acquireTaskGate('structure')` 字样被 OCCUR_RE 同计而 CALL_RE 只认真调用点——注释措辞修正 + 留痕注记（审计门对注释与代码同计的既定行为，非门缺陷）。③ **L2 首跑抓 ChapterTreePanel 挂载族 13 用例 TypeError + Book.vue E-7 超时 30s 同根因**：四面板测试文件 mock useChapterTreeActions 缺批 B 新增 `splitEditing`/`onSplitCommit` 且缺 SplitChapterDialog 桩，面板模板新引用渲染即 `undefined.value`、mount 抛错挂死 waitFor——四文件补齐（splitEditing ref + onSplitCommit + dialog 桩）后 17/17 绿含超时件自愈。④ **spec-order.guard 两步闸写入当跑必 1 红的守卫自身时序缺陷**（批 B 重拍实抓）：describe 级收集期捕获旧快照 vs reporter onEnd 运行期现读盘，主用例本轮写入新快照即失配——reporter 门两用例改测试体内现读（skipIf 收集期只查存在性），「收集期旧序 + 运行期写盘」失败形态复刻验证 8/8 绿 + 终态逐字节一致。⑤ e2e 实抓前端既有行为观察项（**inline 新建章清掉 seed「NNNN-」前缀 → 文件名无章号前缀对 nextChapterNo 不可见 → 连建两章 fm 章号重号且互不可合并**）——spec 内保留前缀规避 + 注释留档，登记台账 §三 待拍板。
- **测试**：新增 10 件（vitest 侧 9 件 59 用例：structure-merge ×13 / structure-split ×10 / structure-chain-drain ×2 / chapter-lookup-preserve ×8 / structure-fm-preserve-draft ×2 / split-chapter-dialog ×8 / tree-menu-structure ×7 / chapter-tree-actions-structure ×3 + 既有追加 mutex-gates ×5 / documents-meta-int ×1；e2e 侧 structure-ops.spec ×3）+ 既有修补 6 处（spec-order.guard reporter 门现读修 / 四面板测试 mock 补齐 / spec-order.snapshot.txt 两步闸重拍 29→30）；未加平台门，win 差值锚 79 不动。
- **L2 终门九件套（主审亲跑全绿）**：vitest **1113 文件 = 7191 过 + 5 跳 0 败**（基线 1105/7132 → 净增 8 文件/59 用例）+ coverage 四值 **st 91.62 / br 87.22 / fn 96.15 / ln 91.62**（主桶门 89/83/95/89 全过；较批 A 微降 = structure.ts 新文件稀释）+ tsc/vue-tsc 0 错 + eslint 0/0（--max-warnings 0）+ check:counts 过（README 修账六处：徽章/npm test/合入门槛行 1105→1113 与 7132→7191/win 预期 1113 文件/7112 过+84 跳〔差值锚 79 不动〕/test:e2e 行与技术栈行 29→30 specs、45→48 用例、常规 43→46）+ check:knowledge 过（13 条）+ check:packaging 过 + e2e 46 过 2 跳〔34.2s；30 specs 含新增 structure-ops，快照两步闸重拍后全链绿〕+ soak 两段 OK（−0.02MB / +0.05MB，上界 24MB）。
- **改动面**：src 新增 2（document/structure.ts + web-next SplitChapterDialog.vue）+ src 修改 20（服务端 11：api/documents·books·task-gate / events types·chain-bridge / rag index·store / process/draft-pipeline / ai/orchestrate/self-heal / format/chapter-lookup·words；前端 9：api/documents / shared/chapter-tree / composables useTreeMenu·useChapterTreeActions / stores/workspace / editor/CmHost / views/EditorView / panels/ChapterTreePanel / ui/ConfirmPrompt）+ test 新增 10 / 修补 7 + e2e 快照重拍 + 根 README 修账六处 + 本文档 §六回填 + 总览阶段 24 行推进 + Archive/README 批记行 + 台账 §三 登记一项。工作树留作者（未提交；README.md/package.json 的 test:related 脚本为作者既有改动，原样保留）。

### 批 C（S5 收尾 + 卷重组验收）——2026-09-13 实施，L2 终门全绿（阶段 24 全三批完成）

- **范围**：S5 全项（§二 S5 表）+ 卷重组验收项。**崩溃形态收敛面补齐**（structure.ts）：applyChapterMerge 崩溃形态判定**前移重排**——回收站条目在档形态（②后崩溃：源章已软删、清单条目已摘、readChapterState(源) 必失败）不再依赖读源章，章号从 TrashEntry.originalPath 反查 + fm 并入 对应校验后续跑；源章存活正文形态（①后崩溃）重跑 = 幂等续跑补完收尾（S5 起不再 409 拒收——该形态正是 detectState 报文指引的收敛路径）；finishMerge 的 trash 段幂等自查（条目在档**且源文件确已不在原路径**才跳过——文件被人工放回正文的混合态仍补软删，防 并入 所指章永久存活）；undoChapterMerge 定位链第四路 `locateMergeByBody`（正文盘面定位：事件缺 + 回收站缺的①后形态，按清单路径章号反查 docId、trashEntryId 留空）+ 还原段对空 trashEntryId 跳过 restoreTrash。**detectState 挂点**（state/state.ts）：HealthIssue kind 联合 + 'structurePending'，healthCheck 尾段调 `detectStructureViolations`（structure.ts 导出的只读判定式：walkMdEach 扫正文 fm `并入` × 正文章号全集，violation 即未完成态；按目标/源章号排序保报文确定性）报 issue（humanMsg 指引两收敛路径 + fix 文案）；**不进 crashedPendingOpIds 提取**（结构半成态无 journal pending 可 abort——「忽略」按钮对无持久登记的报红是假消解，指引作者收敛、盘面收敛报文自消才是真闭环）；检查段 try/catch 降级 warn 留痕（R54-B-1 纪律）。**api 口径**：structStatus WRITE_ERROR 500→409（apply 收尾段锁等待超时信封自带「重试将自动续跑收尾」语义，与 files.ts PUT 409 WRITE_ERROR 可重试口径对齐）。
- **测试**：新增 test/document/structure-crash.test.ts ×4（崩溃注入两形态：①「①②间中断」用源章 save 锁占 + 结构锁超时收短制造确定性窗口——apply#1 trash 段超时失败留①后盘面 → detectState 态 1 报 structurePending → apply#2 重跑幂等收敛 + 盘面收敛报文自消；①变体 undo 整体回退收敛〔正文盘面定位 + 还原段跳过 + fm 逐字节回滚〕；②②后崩溃终态〔真合并半途 + 源文件放回 + 清单补登〕→ detectState 报 → apply 重跑走回收站形态收敛〔alreadyTrashed 修后补软删〕；不误报三态对照〔撤销还原/完成态源在 .trash/零结构〕）；structure-merge.test.ts 既有「人工处置形态」用例按 S5 口径改写（fm 已含源 + 源章存活 = ①后形态重跑续跑收敛 200；源章被人工清理 = 404 NOT_FOUND）；structure-volume-move.spec ×3（卷重组验收 e2e：跨卷移动显示序实测钉 / 跨卷并入两卷构型 / 净零收尾）+ spec-order.snapshot.txt 两步闸重拍 30→31。
- **实施实抓（如实记档）**：① 批 B 幂等续跑分支的真实覆盖缺口在批 C 收口——批 B 构造「放回源文件 + 清单补登」实为混合态，真实②后崩溃（文件在 .trash、清单已摘）下 readChapterState(源) 必失败走不到续跑分支，本批以形态分流前移收口（structure-crash 形态②用例钉定）；② alreadyTrashed 首版只查回收站条目，混合态（文件放回正文）误跳软删被 structure-crash 形态②用例当场抓红（existsSync 断言）——补「源文件不在原路径」判据；③ detectState 直调需自解 readBookConfig 判别联合（{ok,config} 形态）；④ 共享书测试的回收站/事件断言须按 docId 过滤（全书级断言被前序用例残留打红）；⑤ detectStructureViolations 首跑「误报 2 条」实为前序用例失败中止残留的①后态——检测器行为正确性的反向实锤。
- **卷重组 e2e 四处实测偏离（代理如实记档，主审复核通过，未改产品码）**：① 显示序实测 = 卷优先分组 DFS（sortTreeByOrder 只重排同目录内章文件、compareNode 目录优先钉卷位置）——「fm 序跨卷全局排」的任务预设与实测相反，spec 按实测钉、语义取舍留作者拍板（登记台账）；② 跨卷并入需两卷构型（散章前章恒散章）；③ 建卷即自动建「NNNN-未命名」种子章（onCreateCommit volume 分支既有行为），spec 清种子章规避；④ **撤销并入回收站反查歧义（产品缺陷候选，登记台账）**：e2e 无 userDataPath 时事件定位恒 null，undo 兜底 locateMergeByDisk 按 max(并入) 章号反查回收站**首个**同章号条目——共享 workDir 同章号历史软删条目会被误认领还原（spec 以开场清空回收站去歧义）。
- **L2 终门九件套（主审亲跑全绿）**：vitest **1114 文件 = 7196 过 + 5 跳 0 败**（基线 1113/7191 → 净增 1 文件/5 用例：structure-crash ×4 + structure-merge 拆分 +1）+ coverage 四值 **st 91.66 / br 87.25 / fn 96.19 / ln 91.66**（主桶门 89/83/95/89 全过，较批 B 微升）+ tsc/vue-tsc 0 错 + eslint 0/0（--max-warnings 0）+ check:counts 过（README 修账：徽章/npm test/合入门槛行 1113→1114 与 7191→7196/win 预期 1114 文件/7117 过+84 跳〔差值锚 79 不动〕/e2e 行与技术栈行 30→31 specs、48→51 用例、常规 46→49）+ check:knowledge 过（13 条）+ check:packaging 过 + e2e 亲跑 49 过 2 跳 0 败〔38.8s；31 specs 含新增 structure-volume-move，快照两步闸重拍后全链绿〕+ soak 两段 OK。
- **改动面**：src 修改 3（document/structure.ts 崩溃形态三分支 + finishMerge 幂等 + locateMergeByBody + detectStructureViolations 导出；state/state.ts HealthIssue 联合 + healthCheck 挂点 + import；studio/server/api/documents.ts structStatus WRITE_ERROR→409）+ test 新增 1（structure-crash ×4）/ 改 2（structure-merge 口径改写 ×2 用例 + 头注；e2e 快照重拍）+ 新增 e2e spec 1（structure-volume-move ×3）+ 根 README 修账 + 本文档 §六回填 + 总览阶段 24 行收口 + Archive/README 批记行 + 台账 §三 登记两项。工作树留作者（未提交；README.md/package.json 的 test:related 脚本为作者既有改动，原样保留）。
