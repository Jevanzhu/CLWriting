# 注释冻结 — 桌面域 server 族（⑤⑤注释冻结剪枝试点波 B 路，R0916-5k，2026-09-16）

- **作者指令链与试点波定位**：作者 ⑤⑤「注释冻结剪枝试点」指令（2026-09-16）→ 波 B 路代理（GLM-5.3-Flash，子代理）执行。范围 = 桌面域 server 族五文件（src/desktop/server-manager.ts / server-log.ts / server-proc.ts / server-utility.ts / server-boot.ts）；纪律 = 剪出归档非直删、代码行零改动（diff 只含注释行删除 + 被清空块残留空行收拢）、逐块判剪宁少剪不误剪、R 编号锚注与现行语义承载段（fork/kill 时序、SIGKILL 升级链、锁序、平台差异分支理由、端口钉住语义、JSDoc 契约）一律不剪、server-manager/server-log/server-proc 三件头注拆分沿革注（R0916-5g）与 server-proc 头注「共用契约随 proc 缝单源迁出为唯一无环解」设计论证不剪。
- **性质**：考古性注释逐字收容件（剪前原文逐字粘贴，非转写）。每块附剪判理由与现行语义留痕（单源指向）。
- **路径解读注记**：本件内 src/ 相对路径按仓库根解析；「原 L 起-止」= 基线提交 480252fe 工作树的剪前行号。
> 纪律：剪出归档非直删；git 历史 + 本件双锚可考（复原锚 = 提交 480252fe 的对应源文件）。

---

## src/desktop/server-manager.ts 原 L378-379（launch 内稳定窗口定时器：R58-B-1 旧缺陷复盘）

```
    // R58-B-1（五十八轮）：句柄留存 + exit 路径清除——此前不成对，child 提前退出后
    // 定时器仍滞留 stabilityResetMs（5 分钟），闭包持死 proc 引用并空跑一次回调
```

- 剪判：旧 bug 复盘——「此前不成对…滞留…空跑」记述的是已修复缺陷的旧形态；现行行为（exit 监听内 `clearTimeout(stabilityTimer)` 配对清除）由代码自明，前行 L377 已载现行语义（unref 不拖退出、到点仍是他为 active 才清零）。
- 现行语义留痕：src/desktop/server-manager.ts launch() 内 L377 锚注 + exit 监听代码本体。

## src/desktop/server-manager.ts 原 L687-691（shutdown 空 active 分支：killStartingProc 调用点 rationale 重复段）

```
          // R44-12（四十四轮）：预算耗尽且握手未收口——在途 fork 不再等 30s 握手超时，
          // 就地 kill + 等退出收口（killProcAwaitEscalating 纪律原样复用：killWaitMs
          // 等待 + SIGKILL 升级，本段不新增等待语义）。settled=true 的空 active 属
          // 握手已失败/child 已退形态，其自身路径已收口，此处无需动作。
          // R0912-A-P3-2：同构块收拢为 killStartingProc（行为零变化）。
```

- 剪判：同一论点他处已有单源记载的重复段——与 stopChild 侧同名块（原 L649-653）逐字同构（仅 R 锚引文与缩进异），且 killStartingProc JSDoc（原 L272-278）已单源载明「预算耗尽就地 kill 收口、`!settled && startingProc` 守卫留在调用点、同构块收拢行为零变化」。保留 stopChild 侧首现与 JSDoc，剪 shutdown 侧副本。

## src/desktop/server-manager.ts 原 L721-725（shutdown 停机结果留痕：nano R2-1 日志文案拆分旧缺陷复盘）

```
        // nano R2-1（重评-0914-三轮）：settle.by 三态（done/exit/timeout）原并轨成两句
        // 固定文案，归因失真——'exit'（未回执但已自行退出）被误标「超时…已强杀」（实际
        // 无超时，下方 kill 只是幂等兜底）；'exit'/'timeout' 但子进程已被替换/先行收口
        //（active?.proc !== current.proc）被误标「shutdown 指令链路」（回执从未到达）。
        // 分支结构不变，仅按实际 settle 原因拆文案恢复区分度。
```

- 剪判：旧 bug 复盘——「原并轨成两句固定文案，归因失真」记述已修复的日志误标旧形态；现行四分支文案自身已按 settle 原因区分（消息文本内嵌归因说明），前行 L720 已留运维口径（批 U3 崩溃重启归因依赖 graceful/强杀区分）。

## src/desktop/server-manager.ts 原 L764-770（restartPinned 入口：R55-A-1 有界等待 rationale 第三处重复）

```
      // R55-A-1（五十五轮）：shuttingDown 态不再立即拒自愈——观察窗（main 侧缺省 5s）
      // 与停机链最坏预算失配（settle 2s + 总超时 3.5s + kill 2s×2，慢而正常收尾 ~5.5s /
      // child 挂死 ~9.5-11.5s），OS 关机被取消 + child 收尾偏慢的复合场景下窗口到点时
      // 停机仍在途，原「立即返 null」令用户面对 API 不可用手动恢复。改有界等待停机
      // 收口（事件驱动：shutdown finally 复位 shuttingDown 时唤醒，不轮询），上限见
      // RESTART_SHUTDOWN_WAIT_MS（可注入）；复位后重试一次原路径（下方流程原样）；
      // 超时仍走原 null 返回（调用方既有「恢复失败」留痕不变）。
```

- 剪判：同一论点他处已有单源记载的重复段（同段第三处复述）——接口 JSDoc restartPinned（原 L174-185，契约全量：有界等待停机收口、重试一次原路径、超时/退出链 → null、上限可注入）+ RESTART_SHUTDOWN_WAIT_MS 常量 JSDoc（原 L75-86，观察窗与停机链预算失配数字单源）+ shutdownSettledWaiters 声明注（原 L254-257，事件驱动唤醒机制、不轮询）均已载。

## src/desktop/server-log.ts 原 L110-111（splitLines 流 error 分支：R55-A-2 留痕 rationale 重复段）

```
    // R55-A-2（五十五轮）：空吞改留痕——child 日志链路断裂原先零痕迹不可观测；
    // 经 onError（forwardChildStdio 接 logger.warn）补一条，仍不上抛、行为不变
```

- 剪判：同一论点重复段——同文件 forwardChildStdio 内 R55-A-2 注（原 L37-39）已单源载明（原先空回调零痕迹不可观测、附 err message、不上抛不重试转发尽力而为）；本处前行 L109 已留现行口径（流异常不反噬 main、丢行不丢进程）。R55-A-2 锚注单源保留于原 L37-39。

## src/desktop/server-boot.ts 原 L72-73（parseServerArgs --port 校验：R26-94 静默缺省旧缺陷复盘）

```
  // 落缺省——原口径 Number(portRaw) 收 NaN/''.（Number('') === 0）分别落 portDefault
  // 或随机端口：外部脚本拼错参数时服务起在意外端口上，无人知晓也无从排查。
```

- 剪判：旧 bug 复盘——记述已修复的「静默落缺省端口」旧形态；现行行为由校验代码（trim/isFinite/isInteger/0–65535 + fatal 人话文案）自明，且 R28-19 块（原 L74-76，保留）已载「非法一律走既有 fatal 人话通道」。R26-94 锚注与现行语义（显式报错退出，不再静默）保留于前行 L71；R26-94/R28-19 编号另见 resolveEnvPort JSDoc（原 L97-98）交叉引用，不受影响。

---

## 判留抽样（本波未剪的代表性保留段，供复核）

- src/desktop/server-manager.ts 原 L605-619（停机门复位单点合并时序安全性论证 ①②③）：现行单点条件复位的交织正确性证明，属 fork/kill 时序约束——保留。
- src/desktop/server-manager.ts 原 L482-487（R1010b-DSK-P3-2 在途不覆写）：starting 通道竞逐独有记载，他处无单源——保留。
- src/desktop/server-manager.ts 原 L75-86 / L174-185：R55-A-1 预算失配数字与 restartPinned 契约的单源所在（L764-770 剪判的依据正本）——保留。
- src/desktop/server-proc.ts 原 L83-88（R28-21 升级前重读 pid）：SIGKILL 升级链约束 + 残余窗口如实记档——保留。
- src/desktop/server-manager.ts / server-log.ts / server-proc.ts 头注拆分沿革注（R0916-5g）与 server-proc.ts 头注共用契约单源论证：按指令保护——保留。
- src/desktop/server-utility.ts 全件：未检出可剪块（头注握手协议 = 现行契约，R65-41/R48-18/R0910-W/R71-13 各注均现行语义承载），最小剪枝如实记录。
