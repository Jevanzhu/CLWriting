# 注释冻结 — 安装域（⑤⑤注释冻结剪枝扩展波 E 路，R0916-5k，2026-09-16）

> 纪律：剪出归档非直删；git 历史 + 本件双锚可考（复原锚 = 提交 480252fe 的对应源文件，按各节标注的原行号逐行回插即得原文）。

- **波次定位**：试点波（desktop 域）之后的扩展波收尾 install 域。判剪口径与试点波同源：剪 = 与现行行为无关的修复前 bug 长篇复盘、对已删除代码的墓碑/考古复述（同一论点他处已有单源记载）；不剪 = R 编号现行语义锚注、JSDoc 契约、migrate 幂等/回滚与锁语义、repair 候选扫描不变量、BOOK_NAME_INVALID_REASON 跨平台文案单源注、AppleDouble 过滤与 dot 前缀跳过（mac 适配批资产）、books.ts/books-repair.ts/books-resolve.ts 头注 ⑤④波1 拆分沿革注（R0916-5e）。
- **钉点门（剪前核对）**：全 test/ 无任何钉测试以 readFileSync/readFile 读 src/install/ 源文本（38 个引用 src/install 的测试文件逐一核查，唯一直读文件的 test/studio/r36-24-book-prefs-revision.test.ts 读的是 bookPrefs 运行时数据文件而非源码）；governance 门（known-actions-audit / dependency-direction / ai-studio-direction）按正则扫 src/ 全树，剪枝只删注释行、不引入任何 `acquireTaskGate(` 或 import 形字样。钉点存活面无风险。
- **实收**：10 文件中仅 2 文件各剪 1 块，共 9 注释行 + 1 空行收拢；其余 8 文件（books.ts / books-resolve.ts / books-repair.ts / scaffold.ts / data.ts / migrate-layout-v2.ts / migrate-layout-v3.ts / migrate-finalized-revision.ts）如实零剪——其注释为锚注骨架与现行语义承载段（与试点波 desktop 域实测结论同型：考古密度远低于毛估）。

---

## src/install/init.ts 原 L101-104（doInitAsync 登记成功后写 active 指针调用点的 R44-18 注——修复前行为复盘段；现行语义单源承载于下方 writeActiveGuarded 函数 JSDoc 与孪生同步版 doInit 调用点的紧凑锚注「见 writeActiveGuarded 头注」（原 L76-77），非 JSDoc 的行内块）

```text
  // R44-18（四十四轮）：writeActive 调用点收编——appendBookAsync 成功（登记已落盘）
  // 后写 active 指针此前无 try/catch：mkdirSync/atomicWriteFile 抛 EACCES 等直接
  // reject，建书端点 500，且作者重试同名会撞「已有一本叫…」误导性拒绝。收编按登记
  // 面真实状态给 reason（见 writeActiveGuarded 头注），契约「永不 reject」闭合。
```

## src/install/migrate-defaults.ts 原 L114-118（「文本操作」分节注下方的 P1-6 孤立注——为已删除的本地第四份段定位复制（matchesKeyLineCRLF + topSectionSpan + 段内最小缩进循环）所留墓碑注；单源委托已在 deleteSectionKey/deleteTopSection 两个调用点行内锚注「P1-6：段定位委托 yaml.ts 单源」，现行段定位口径正本在 format/yaml.ts locateTopSection，非 JSDoc 的孤立游离块）

```text
/** P1-6（复审-0914-优化修复批）：段定位单源化——本地第四份复制（matchesKeyLineCRLF +
 *  topSectionSpan + 段内最小缩进循环）删，改委托 yaml.ts locateTopSection（含 BOM 剥除
 *  形态，与本函数头注「同 yaml.ts matchesKeyLine 口径」的意图对齐；历史本地复制漏 BOM
 *  属口径漂移，单源后按注释意图收齐）。边界语义（含 CRLF 形态、空行/注释归属）逐位不变，
 *  由 test/install/migrate-defaults.test.ts + r37-migrate-defaults-crlf.test.ts 锁定。 */
```

（后随空行收拢一行：原 L113/L119 两连续空行剪块后留一。）
