# 注释冻结 — 导出·缓存域（⑤⑤注释冻结剪枝扩展波 D 路，R0916-5k，2026-09-16）

> 纪律：剪出归档非直删；git 历史 + 本件双锚可考（复原锚 = 提交 480252fe 的对应源文件）。
> 性质：注释行剪枝（考古性注记迁出），代码行零改动；本件行号为剪枝前原文件行号（即 480252fe 工作树态）。
> 范围：src/export/*.ts + src/cache/*.ts 共 8 文件。实收 = 4 块 7 行（export/index.ts 3 块 4 行 + cache/rebuild.ts 1 块 3 行），余 6 文件（export-worker / run-async / rebuild-worker / run-rebuild-async / schema / sync〔全保指令〕）零剪——锚注骨架占绝对主体，如实小收成。

## src/export/index.ts 原 L361-362（exportBook 内 X-P2-4 注组尾：exportable 纯别名删除记档——别名已不存在，纯历史复述）
```
  // R0912-E-P3-6（2026-09-12 独立重评修复批）：`const exportable: ExportUnit[] =
  // filtered` 纯别名删除——后续全用 filtered 本名（类型标注本就来自 filtered 声明处）。
```

## src/export/index.ts 原 L503（writeSplit 闭包定义前：nano R4-2 重排缩进记档——纯排版零语义，所指旧排版已无痕）
```
  // 重评-0914-三轮 nano R4-2：本闭包整体重排缩进（此前 body 少一层、层级错乱）——纯排版，零语义变化。
```

## src/export/index.ts 原 L641（投稿视图 entries 处：exportableNums 纯别名删除记档——别名已不存在，同 L361-362 同批同型）
```
    // P3（复审-0914-优化修复批）：exportableNums 纯别名删——消费点直用 writtenNums
```

## src/cache/rebuild.ts 原 L370-372（rebuild() 内增量探测调用点 R48-14 注——与 tryIncrementalRebuild 形参 JSDoc（原 L248-249「未命中走全量时 rebuild 复用本次 stats（SMB/网盘卷免二次全树 stat）；探测在扫描前即退出时 holder 保持空」）单源重复，且为旧路径历史框架复述；单源记载仍在形参 JSDoc 与原 L283 行内注）
```
  // R48-14（四十八轮）：增量探测未命中时复用其已扫源树 stats——原路径 tryIncrementalRebuild
  // 内 walkSourceStats 全树 stat 一遍、miss 后此处再扫一遍，SMB/网盘卷成本翻倍；
  // 探测在扫描前即退出（库打不开/旧基准缺失）时 holder 为空，照旧实扫
```

---

## 附：保留判定抽样（不剪理由备查）

- **src/export/index.ts L104-127 purifyBody 块注（24 行）**：E-9f/N-6/R49-2/IR-5/重评-P3-19 各段均承载现行剥除算法语义（两种标记形态、围栏状态机、未闭合两遍收口、首现位截断）+ 权衡登记（宁误剥字面不泄漏批注）——现行契约非沿革，JSDoc 契约保护。
- **src/export/index.ts L202-213 sanitizeFileName 块注**：导出格式字节红线（255B 封顶 = 203B 名字预算 + 52B tmp 余量、FF-F3 双封顶）——指令明列保护类。
- **src/export/index.ts L403-409 S2 D7 分流 + 拍板快断批（2026-09-15）**：displayNum 单调性口径拍板批资产——指令明确不剪。
- **src/export/index.ts L291-295 relPosix JSDoc**：R40-21 源码钉测试（test/document/r40-static-anchors.test.ts）关联单源声明。
- **src/cache/rebuild.ts L36-54 R37-16 章读指纹缓存块注（19 行）**：stat 先于 read 的不可换序、mtimeMs/size/ino 三元组、FIFO 容量口径——现行不变量。
- **src/cache/rebuild.ts L206-220 R47-11 节流块注**：增量探测 3s TTL 的 opt-in 口径①②③，含 test/cache/rebuild.test.ts 锚定声明——缓存失效语义保护类。
- **src/cache/rebuild.ts L93-118 __testHooks 系注**：测试钩子注——指令全保。
- **src/cache/sync.ts 全件**：指令全保。
- **worker 四件（export-worker/run-async/rebuild-worker/run-rebuild-async）头注**：worker 卸载动因（SSE 心跳停摆）、超时档、入口解析契约、R57-A-1 单飞互斥——现行架构语义；S4 留档系台账备查注。

## 附：指路漂移备查（观察记录，源文件未动——mid-line 不可线级手术）

- src/cache/rebuild.ts 原 L47-48「口径对齐 search.ts R35-7 dirSignature」——search.ts 现居 src/studio/server/api/search.ts（符号在，路径前缀漂移）。
- src/cache/rebuild.ts 原 L211「照抄 state.ts sweepLastAt（R43-2）纪律」——sweepLastAt 现居 src/state/health.ts（符号在，路径前缀漂移）。

## 剪枝门与验证（R0916-5k 实测）

- 钉点门：test/document/r40-static-anchors.test.ts（export/index.ts 负断言 + relPosix(e.file)）、test/document/r38-batch-f.test.ts（docJoinKey）、test/check/r0914c-fortest-hooks-guard.test.ts（__…ForTest 行首扫描）、test/governance/known-actions-audit.test.ts（acquireTaskGate( 全 src 计数，本路 0）——剪后全部存活。
- 四项验证：a 纯注释删除断言（git diff 删除行全注释形态/新增仅空行/范围仅本路 2 文件）＋ b 定向 vitest ＋ c eslint --max-warnings 0 ＋ d test/governance/ ——数字见批次汇报。
