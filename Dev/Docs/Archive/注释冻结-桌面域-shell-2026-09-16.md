# 注释冻结 — 桌面域 shell 族（⑤⑤注释冻结剪枝试点波 C 路，R0916-5k，2026-09-16）

- **作者指令链**：作者指令「⑤⑤注释冻结剪枝试点」——对桌面域 shell 族七文件（lifecycle.ts / windows.ts / workdir-controller.ts / workdir-store.ts / ipc.ts / win-fonts.ts / font-cache.ts）执行注释冻结剪枝：考古性注释逐字迁入本冻结件（含出处），再从源文件删除；代码行零改动。
- **试点波定位**：波 C 路 = 桌面域 shell 族（并行另有波 A/B 路处理 main 域等其他文件，见 `注释冻结-桌面域-main-2026-09-16.md`）。
- **纪律**：剪出归档非直删；git 历史 + 本件双锚可考（复原锚 = 提交 `480252fe` 的对应源文件，按各节标注的原行号逐行回插即得原文）。
- **判剪口径**：剪 = F1 拆分收编面沿革清单（原 main.ts 行号映射流水账，现行结构由代码与函数级 JSDoc 单源承载）、纯「已于 X 批收敛单源」重构沿革复述（无现行行为内容）、指向已迁移代码的过时指路。不剪 = R 编号现行语义锚注、窗口生命周期/session-end/quit 链设计约束、workdir 迁移不变量与切库回滚基线语义、IPC 通道契约、平台差异分支理由、GBK/字体枚举现行链路（复审-0914-修复批资产）、JSDoc 契约、台账/挂账备查注。
- **钉点门（剪前核对）**：消费源文本的钉测试共 3 处，均钉代码行不钉注释——`test/desktop/r38-exit-guards.test.ts` 钉 ipc.ts `const hexColor = (/^#(?:…)$/)` 代码行；`test/desktop/r30-async-handler-guard.test.ts` 钉 ipc.ts openShelfWindow()/openLibraryWindow() 调用行×2（L225/L245，剪枝集不含含该字面量的注释行）；`test/desktop/win-fonts.test.ts` 钉 PS_FONT_SCRIPT 代码常量。剪枝后全部存活。
- **机检**：git diff 全部删除行均为注释形态（`//`、`/*`、`*`、`*/` 前缀），新增行仅空行收拢；全 diff 零 `acquireTaskGate(` 字样。

---

## src/desktop/lifecycle.ts 原 L4-16（文件头收编面沿革清单——F1 拆分自 main.ts 的行号映射与链条清单；现行链条语义由各函数级 R 编号锚注单源承载）

```text
 * 收编面（原 main.ts 对应节）：
 * - close 链（原 :1316-1408，挂 bootstrap 内主窗 'close'）——关窗拦截 + 渲染层兜底
 *   flush（预算竞速）+ 冲突/保存失败原生确认 + destroy 收口 + quit 汇入（R49-5）；
 * - session-end 链（原 :1413-1472）——win OS 关机/注销：置旗直关 + 并行尽力 flush +
 *   停机指令 + 观察窗自愈（R1W-9/R53-A-1/R50-A-1）；
 * - quit 链（原 :2124-2248 before-quit）——退出先行 flush + 确认可取消（R44-19）+
 *   不可回头点武装切库意图（R51-A-1）+ beginShutdown/shutdown + destroy 全窗收口；
 * - 主窗其余生命周期监听（focus 关书库窗 / closed → app.quit / 全屏反向同步）随主窗
 *   装配迁入（原 :1473-1498）；
 * - F2 同批样板收敛：runFlushConfirm——close/quit 两链的「flushRendererWithBudget →
 *   超时/无钩子留痕 → conflict/failed 原生确认 → 取消复位」流程双写单源（差异以参数
 *   注入：文案/预算/skipConfirms/onCancel；确认框语义、文案、取消旗复位时机逐位不变）。
 *
```

（末行 ` *` 为块内空注释行，随块删除、由下一行直接承接，空行收拢至多一行的纪律不变。）

## src/desktop/windows.ts 原 L4-11（文件头收编面沿革清单——原 main.ts 行号映射；wins holder 的 ESM live-binding 理由行 L12-14 与模块级零副作用纪律行保留）

```text
 * 收编面（原 main.ts 对应节，行号以拆分前 2259 行版为基准）：
 * - createSecureWindow 安全五件套工厂 + 纵深防御 + IPC 白名单登记（trackWindow）+
 *   渲染崩溃/加载失败自愈（attachRendererCrashSelfHeal）+ dev 代理记账（devProxyApplied）
 *  （原 :65-910）；
 * - openShelfWindow / openLibraryWindow 单例子窗（原 :912-1010；F2 同批把两函数骨架
 *   双写收敛为 openSingletonWindow——appUrl 守卫/单例聚焦/workArea 尺寸/closed 置空/
 *   devProxyApplied 复验全套单源，文案与时序逐位不变）；
 * - 主窗 bounds 持久化（window-state.json 读/存，原 :367-402）；
```

## src/desktop/workdir-controller.ts 原 L4-13（文件头收编面沿革清单——原 main.ts :404-767 行号映射与 F2 收敛清单；架构纪律段 L14-17 保留）

```text
 * 收编面（原 main.ts :404-767 对应节）：
 * - workdir.json 持久化（storePath/readStore/writeStore/saveCurrent 族 + 切库回滚基线）；
 * - bootstrap 实际 workDir 记账（currentWorkDir 统一取值面，M-3）；
 * - 切库守卫与可达性预探（isLibraryDir/canSwitchLibraryDir/probeDirReachable）；
 * - 原生目录选择器与切库入口（pickLibrary/warnIfCaseSensitive/openLibraryAction）；
 * - relaunch 意图与不可回头点武装（pendingRelaunch/armPendingRelaunchIfAny，R51-A-1）；
 * - F2 同批样板收敛：resolveReachableWorkDir（三 IPC 入口内联的「workDir 判空 +
 *   probeDirReachable unreachable 错误框」×3 单源）与 findBookEntry（readBooks().find ×2
 *   单源）、msgBox/openDirDialog（dialog 双参/单参三元 ×3 单源）。
 *
```

## src/desktop/workdir-controller.ts 原 L260-264（msgBox 函数头注——纯「×3 收敛单源、行为逐位不变」重构沿革复述，parent 缺省走无 parent 重载的形态由函数体三元即见）

```text
/**
 * P3（复审-0914-优化修复批）：dialog 双参/单参三元 ×3（warnIfCaseSensitive 的
 * showMessageBox、pickLibrary 的 showOpenDialog + showMessageBox）收敛单源——
 * parent 缺省时走无 parent 重载，行为逐位不变。
 */
```

## src/desktop/workdir-controller.ts 原 L397-400（findBookEntry 函数头注——纯「×2 收敛单源、语义逐位不变」重构沿革复述，无现行契约内容）

```text
/**
 * F2（复审-0914-优化修复批）：show-in-folder / open-book-dir 内联的
 * `readBooks(workDir).find((b) => b.name === name)` ×2 收敛单源（语义逐位不变）。
 */
```

## src/desktop/workdir-controller.ts 原 L471（跨模块导出面节内沿革行——「原 main.ts 内联符号纯移动」复述，导出面现行事实由 export 语句单源承载）

```text
// 全部为原 main.ts 内联符号的纯移动（handler 语义与文案逐位不变）。
```

## src/desktop/ipc.ts 原 L4-13（文件头收编面沿革清单——原 main.ts :1544-1850 行号映射、通道清单（通道契约由各 handleTrusted 注册行单源承载）、F2 handleTrusted 收敛叙事（现行拒绝契约由 L74-79 函数头注单源承载）、「注释随实现迁走」路线注）

```text
 * 收编面（原 main.ts :1544-1850「── IPC ──」节，channel 名/handler 语义/文案逐位不变）：
 * - desktop:open-library / switch-library / get-recent / get-current / show-in-folder /
 *   open-book-dir / get-system-fonts / open-shelf / open-book / open-library-window /
 *   open-library-dir / set-fullscreen / set-titlebar-overlay（ipcMain.handle ×13）
 *   + desktop:context-menu（ipcMain.on ×1）；
 * - F2 同批样板收敛：handleTrusted/onTrusted 守卫包装（原 14 处手工
 *   `if (!isTrustedSender(e)) return` 单点化——白名单判定本体 isTrustedSender 仍在
 *   windows.ts，此处只做接线形态收敛，拒绝语义=静默 undefined 逐位不变）。
 *
 * 字体枚举/右键菜单/全屏/窗控 overlay 的实现细节注释随实现迁走（R 编号沿革原样保留）。
```

## src/desktop/workdir-store.ts 原 L4-6（文件头过时指路——「Electron 绑定层在 main.ts 内联调用本模块纯函数」；F1 拆分后 Electron 绑定层为 workdir-controller.ts，main.ts 现仅 import 纯函数 filterValidRecentBudgeted，指向已失真）

```text
 * Electron 绑定层（app.getPath + 文件读写）在 main.ts 内联调用本模块纯函数，
 * 避免把 'electron' 运行时依赖引入测试。
 *
```

## src/desktop/win-fonts.ts 原 L26-27（文件头 R48-72 收编沿革行——「与 font-cache.ts 逐字双实现收编、删本地副本改消费导出、口径零变化」纯重构沿革复述；现行消费关系由 import 语句单源承载）

```text
 * R48-72（四十八轮）：bareFontName/排序比较器与 font-cache.ts 逐字双实现收编——
 * 删本地副本改消费 font-cache 导出（单源），口径零变化。
```

---

## font-cache.ts 零剪说明

font-cache.ts（402 行）逐块判定后零剪：文件头 TTL/serve-stale 契约、spawnCollectKillFonts 骨架单源注（含 decodeStdout 码页感知解码单源注）、PM-12 熔断设计与进程级理由、R48-17 备案沿革 + R0911 已接线收口段（含「打包态实测复验登记台账」备查指针）、darwinFontListCommand 打包路径链，均为现行语义承载段（且该文件刚经历复审-0914-修复批，现行解码语义注按指令不剪）。

## 剪枝量总账

| 文件 | 剪前行数 | 剪出块数 | 剪出注释行数 |
| --- | --- | --- | --- |
| src/desktop/lifecycle.ts | 536 | 1 | 13 |
| src/desktop/windows.ts | 522 | 1 | 8 |
| src/desktop/workdir-controller.ts | 481 | 4 | 20 |
| src/desktop/ipc.ts | 372 | 1 | 10 |
| src/desktop/workdir-store.ts | 187 | 1 | 3 |
| src/desktop/win-fonts.ts | 231 | 1 | 2 |
| src/desktop/font-cache.ts | 402 | 0 | 0 |
| 合计 | — | 9 | 56 |

## 机检通道备查（主审评审记档，2026-09-16）

- 「src/desktop/workdir-controller.ts 原 L471」块内行经 `grep -x -F` 精确比对不命中（LC_ALL=C 亦然），`diff` 仲裁字节一致——msys grep 对该行字节序列的 argv 往返怪象，非内容差异。未来机检复验遇此类单行 MISS 先以 diff 仲裁，勿误判冻结缺块。
