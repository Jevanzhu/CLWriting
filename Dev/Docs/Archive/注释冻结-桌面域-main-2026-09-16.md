# 注释冻结 — 桌面域 main.ts（⑤⑤注释冻结剪枝试点波 A 路，R0916-5k，2026-09-16）

> 纪律：剪出归档非直删；git 历史 + 本件双锚可考（复原锚 = 提交 480252fe827e97dd862dea90fc00da66874607a2 的 src/desktop/main.ts）。
> 本件按剪出顺序记录，每块注明原文件行号区间与随迁语境。

- **作者指令链**：「继续，全部做完！」→「不想挂账」→「继续」。
- **试试点波定位**：⑤⑤注释冻结剪枝试点波 A 路（B/C 路并行改 src/desktop 其他文件，本批只动 src/desktop/main.ts + 本冻结件）。基线 480252fe 工作树净。剪出 7 块 22 行注释，另随 L169 剪出收拢残留空行 1 行（原 L170），源文件 740 → 717 行；代码行零改动（diff 仅注释行删除 + 空行收拢，至多每处留一空行）。实读判定：本文件考古密度低于预估，可剪量 <50 行，按试点波口径如实最小剪枝、不硬凑数。
- **钉点门（剪前普查、剪后全部存活）**：读 src/desktop/main.ts 源文本的测试共 3 处——test/desktop/r38-exit-guards.test.ts（钉 `process.on('SIGINT'/'SIGBREAK'/'SIGTERM', ...)` 三行与 `process.on('unhandledRejection'` 块内无 process.exit，均为代码行，未动）；test/desktop/r0913-r2-main-guard-static.test.ts（GUARD_RE 钉双标志守卫行 `if (gotSingleInstanceLock && appInstanceGuard.acquired) {` + 守卫索引后 800 字窗内 whenReady，均未动）；test/desktop/r30-async-handler-guard.test.ts（钉 openShelfWindow()/openLibraryWindow() 调用面合计恰 2 行且各带 .catch，调用行未动）。test/desktop/main.test.ts 与 test/desktop/main-reachability.test.ts 不 readFileSync 本文件（仅运行时动态 import 与 JSON 数据面断言，注释删除零影响）。
- **known-actions-audit 门**：扫描面 = src/**/*.ts（test/governance/known-actions-audit.test.ts listTs），本件 .md 不在扫描面；main.ts 全文无 `acquireTaskGate(` 字样，剪枝只删不增，门天然满足。

## src/desktop/main.ts 原 L118-119（second-instance --book 直达：旧等价函数收编沿革）

```
    // R0912-A-P3-1（2026-09-12 独立重评修复批）：initialBookArgvOnly 与本函数
    // allowEnvFallback:false 分支逐位等价，删等价函数改传参收编（语义零变化）
```

> 判剪：「删等价函数改传参收编」系「已于X批改」历史复述，被收编的 initialBookArgvOnly 已不存在；现行 `allowEnvFallback: false` 取舍由相邻保留的 R27-97 锚注（只认本次 argv，回落 env 读到首实例值）单源承载。

## src/desktop/main.ts 原 L169（second-instance 处理器与 serverManager 之间的孤儿 tombstone）

```
/** 前端静态目录已随 server 拆分下沉 child（server-boot deriveStaticDir，批 U1）。 */
```

> 判剪：无代码挂靠的「已随拆分下沉」墓碑；deriveStaticDir 正本单源在 src/desktop/server-boot.ts（server-main/server-utility 消费）。随迁语境：本块剪出后原 L168/L170 双空行收拢为一。

## src/desktop/main.ts 原 L419-424（attachMainWindowLifecycle 之后的拆分墓碑三连）

```
  // R1010-P3（G7-③）：preload-error 监听移入 createSecureWindow 工厂（三窗同享，
  // 带窗口名）——原主窗专属块随此删除。
  // R67-16（十五轮）：渲染崩溃自愈已随 createSecureWindow 工厂挂载（原主窗专属块
  // 删除——attachRendererCrashSelfHeal 原样承接 dd-P3/X-26 退避 + S6 稳定复位），
  // 书架/书库子窗口同享。R1010-P3（G7-③）：preload-error 监听亦随工厂挂载（下方
  // 原主窗专属块删除），三窗同享。
```

> 判剪：「原主窗专属块删除」同型墓碑三连，preload-error 一事两处复述；单源在 src/desktop/windows.ts 工厂本体（R67-16 attachRendererCrashSelfHeal 随工厂挂载 + R1010-P3 G7-③ preload-error 同款入工厂，windows.ts L86-90/L317-321）。现行装配语境由紧随保留的「纵深防御监听与 dev 代理已由 createSecureWindow 统一挂载；此处 await 一次保证主窗首载前代理确定生效」注承载。

## src/desktop/main.ts 原 L643-644（createBootstrapRunner 引入处的批次沿革流水账）

```
  // O-4（第十三轮）：三段守卫语义抽 createBootstrapRunner 可测（Y-P2-7 重入挡 +
  // 第九轮 L-3 重试关旧 server + 低-8 退出竞态直通），销第十轮 M-6 留账
```

> 判剪：三段守卫语义单源在 bootstrap-runner.ts 头注（「收编三段历史修复语义」Y-P2-7/L-3/低-8 清单）；「销第十轮 M-6 留账」为台账流水。现行接线由保留的 S-4 换轨注（「重试前关旧 server」经 legacyStopHandle）承载。

## src/desktop/main.ts 原 L648-649（runner deps 字面量上方的死接线墓碑）

```
      // R48-75（四十八轮）：getMainWindow 死接线删除——R-14 后 runner 判据为「存在旧
      // server 即关」，窗口引用不再被读取（接口谎称有用的残留随批清理）
```

> 判剪：已删除依赖的墓碑 + 清理叙事；现行判据（存在旧 server 即关）由保留的 P3 hasPendingRestart 注与 S-4 注承载，deps 接口面单源在 bootstrap-runner.ts 头注。

## src/desktop/main.ts 原 L655-657（runner deps 内 setStudioServer 成员删除墓碑）

```
      // P3（复审-0914-优化修复批）：setStudioServer 成员删除——child 生命周期归
      // serverManager 自持后生产恒传 no-op（原 main.ts `setStudioServer: () => undefined`），
      // runner 内 `deps.setStudioServer(null)` 同步移除，接口面与行为同时收窄
```

> 判剪：引用已不存在代码的墓碑（逐字引用旧 main.ts 行）；「child 生命周期归 serverManager 自持」单源在 server-manager.ts 头注与 bootstrap-runner.ts deps 契约。

## src/desktop/main.ts 原 L695-700（uncaughtException 内 stopChild 旧实现长篇复盘）

```
    // R0910-W（2026-09-10 修复批）：原实现 stopChild fire-and-forget 后固定 200ms 裸退
    // ——stopChild 内含停机 settle 竞速（预算 2s），200ms 到点常早于 kill 下发/收口，
    // best-effort 停机被自身截断；清理链来源的异常（窗口 closed 监听等）同经此路，
    // 裸退还会打断在途优雅停机收尾。改为「停机落定即退、到点兜底强退」：stopChild
    // 落定后延迟一拍（让日志泵落盘）再 process.exit，未落定则由既有 200ms 兜底硬退。
    // 留痕 / 不吞 / 半坏状态不续跑的退出语义不变。
```

> 判剪：旧 bug 长篇复盘（修复本身已在码：backstop + stopChild().then 结构）；现行设计由保留的 R0912-3 块（killNow 同步发 kill + 200ms 上限硬约束）、then 内行内注「已落定：撤 200ms 兜底，延迟一拍让日志泵落盘后硬退」与上方「不吞、不续跑半坏状态」不变量注共同承载，本块无独有现行语义。
