# 全局行尾策略（win 适配批 3 / 阶段 21，2026-08-27）：文本入库统一 LF，
# 防跨机检出漂移破坏字节等价金测与脚本;二进制显式排除防规范化损坏。
# eol=lf（win 合并后检查轮升格）：text=auto 只管入库归一，检出仍随 Git-for-Windows
# 默认 core.autocrlf=true 落 CRLF——字节等价金测在 win 本地即红。检出也钉死 LF，
# 跨平台工作区字节一致；批处理脚本（cmd.exe 对 LF 的标签/续行有边角缺陷）例外保 CRLF。
* text=auto eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf
# 批2-8（2026-09-07 全量代码重审 批2）：原单列行「知识层/** text eol=lf」已删——与
# 全局 * text=auto eol=lf 对纯文本目录等价（text=auto 探测为文本 + eol=lf，入库归一/
# 检出同口径），纯冗余；知识层现全为 md/json 文本，未来若入二进制资产由下方 binary
# 后缀行兜底，且 text=auto 对二进制不强制归一（比显式 text 更安全）。
*.png binary
*.ico binary
*.icns binary
# R73-58（二十一轮）：常见二进制资产后缀补全（字体/位图/音视频/归档/wasm）——
# 防 text=auto 误规范化损坏；新增此类资产入仓时对齐此处
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
*.webp binary
*.gif binary
*.jpg binary
*.jpeg binary
*.mp3 binary
*.mp4 binary
*.zip binary
*.wasm binary
